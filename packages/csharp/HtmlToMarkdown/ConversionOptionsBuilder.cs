// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:40f55f415d95c132f0cc252ba5e50b165fc4b1987f41686a07d79ef81829141c
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
#nullable enable

using System;
using Microsoft.Win32.SafeHandles;
using System.Runtime.InteropServices;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Collections.Generic;

namespace HtmlToMarkdown;

internal sealed class ConversionOptionsBuilderSafeHandle : SafeHandle
{
    internal ConversionOptionsBuilderSafeHandle(IntPtr handle) : base(IntPtr.Zero, true)
    {
        SetHandle(handle);
    }

    public override bool IsInvalid => handle == IntPtr.Zero;

    protected override bool ReleaseHandle()
    {
        NativeMethods.ConversionOptionsBuilderFree(handle);
        return true;
    }
}

/// <summary>
/// Builder for [`ConversionOptions`].
///
/// All fields start with default values. Call `.build()` to produce the final options.
/// </summary>
public sealed class ConversionOptionsBuilder : IDisposable
{
    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault
    };

    private readonly ConversionOptionsBuilderSafeHandle _safeHandle;

    internal ConversionOptionsBuilder(IntPtr handle)
    {
        _safeHandle = new ConversionOptionsBuilderSafeHandle(handle);
    }

    internal IntPtr Handle => _safeHandle.DangerousGetHandle();

    public void Dispose() => _safeHandle.Dispose();

    /// <summary>
    /// Set the list of HTML tag names whose content is stripped from output.
    /// </summary>
    public ConversionOptionsBuilder StripTags(List<string> tags)
    {
        var tagsJson = JsonSerializer.Serialize(tags, JsonOptions);
        var tagsHandle = Marshal.StringToHGlobalAnsi(tagsJson);
        var nativeResult = NativeMethods.ConversionOptionsBuilderStripTags(
            Handle,
            tagsHandle
        );
        if (nativeResult == IntPtr.Zero)
        {
            throw new HtmlToMarkdownRsException(0, "ConversionOptionsBuilderStripTags failed");
        }
        var returnValue = new ConversionOptionsBuilder(nativeResult);
        Marshal.FreeHGlobal(tagsHandle);
        return returnValue;
    }

    /// <summary>
    /// Set the list of HTML tag names that are preserved verbatim in output.
    /// </summary>
    public ConversionOptionsBuilder PreserveTags(List<string> tags)
    {
        var tagsJson = JsonSerializer.Serialize(tags, JsonOptions);
        var tagsHandle = Marshal.StringToHGlobalAnsi(tagsJson);
        var nativeResult = NativeMethods.ConversionOptionsBuilderPreserveTags(
            Handle,
            tagsHandle
        );
        if (nativeResult == IntPtr.Zero)
        {
            throw new HtmlToMarkdownRsException(0, "ConversionOptionsBuilderPreserveTags failed");
        }
        var returnValue = new ConversionOptionsBuilder(nativeResult);
        Marshal.FreeHGlobal(tagsHandle);
        return returnValue;
    }

    /// <summary>
    /// Set the list of HTML tag names whose `<img>` children are kept inline.
    /// </summary>
    public ConversionOptionsBuilder KeepInlineImagesIn(List<string> tags)
    {
        var tagsJson = JsonSerializer.Serialize(tags, JsonOptions);
        var tagsHandle = Marshal.StringToHGlobalAnsi(tagsJson);
        var nativeResult = NativeMethods.ConversionOptionsBuilderKeepInlineImagesIn(
            Handle,
            tagsHandle
        );
        if (nativeResult == IntPtr.Zero)
        {
            throw new HtmlToMarkdownRsException(0, "ConversionOptionsBuilderKeepInlineImagesIn failed");
        }
        var returnValue = new ConversionOptionsBuilder(nativeResult);
        Marshal.FreeHGlobal(tagsHandle);
        return returnValue;
    }

    /// <summary>
    /// Set the list of CSS selectors for elements to exclude entirely from output.
    /// </summary>
    public ConversionOptionsBuilder ExcludeSelectors(List<string> selectors)
    {
        var selectorsJson = JsonSerializer.Serialize(selectors, JsonOptions);
        var selectorsHandle = Marshal.StringToHGlobalAnsi(selectorsJson);
        var nativeResult = NativeMethods.ConversionOptionsBuilderExcludeSelectors(
            Handle,
            selectorsHandle
        );
        if (nativeResult == IntPtr.Zero)
        {
            throw new HtmlToMarkdownRsException(0, "ConversionOptionsBuilderExcludeSelectors failed");
        }
        var returnValue = new ConversionOptionsBuilder(nativeResult);
        Marshal.FreeHGlobal(selectorsHandle);
        return returnValue;
    }

    /// <summary>
    /// Set the visitor used during conversion.
    /// </summary>
    public ConversionOptionsBuilder Visitor(VisitorHandle? visitor)
    {
        var nativeResult = NativeMethods.ConversionOptionsBuilderVisitor(
            Handle,
            visitor!.Handle
        );
        if (nativeResult == IntPtr.Zero)
        {
            throw new HtmlToMarkdownRsException(0, "ConversionOptionsBuilderVisitor failed");
        }
        var returnValue = new ConversionOptionsBuilder(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Set the pre-processing options applied to the HTML before conversion.
    /// </summary>
    public ConversionOptionsBuilder Preprocessing(PreprocessingOptions preprocessing)
    {
        var preprocessingJson = JsonSerializer.Serialize(preprocessing, JsonOptions);
        var preprocessingHandle = NativeMethods.PreprocessingOptionsFromJson(preprocessingJson);
        var nativeResult = NativeMethods.ConversionOptionsBuilderPreprocessing(
            Handle,
            preprocessingHandle
        );
        if (nativeResult == IntPtr.Zero)
        {
            throw new HtmlToMarkdownRsException(0, "ConversionOptionsBuilderPreprocessing failed");
        }
        var returnValue = new ConversionOptionsBuilder(nativeResult);
        NativeMethods.PreprocessingOptionsFree(preprocessingHandle);
        return returnValue;
    }

    /// <summary>
    /// Build the final [`ConversionOptions`].
    /// </summary>
    public ConversionOptions Build()
    {
        var nativeResult = NativeMethods.ConversionOptionsBuilderBuild(
            Handle
        );
        if (nativeResult == IntPtr.Zero)
        {
            throw new HtmlToMarkdownRsException(0, "ConversionOptionsBuilderBuild failed");
        }
        var jsonPtr = NativeMethods.ConversionOptionsToJson(nativeResult);
        var json = Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.ConversionOptionsFree(nativeResult);
        var returnValue = JsonSerializer.Deserialize<ConversionOptions>(json ?? "null", JsonOptions)!;
        return returnValue;
    }
}
