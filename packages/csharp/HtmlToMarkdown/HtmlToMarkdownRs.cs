// This file is auto-generated by alef. DO NOT EDIT.
// alef:hash:38590a6c7413300d231f3fa0843bc743b96f2c659eec30b3ff49228569a0a1bf
#nullable enable

using System;
using System.Collections.Generic;
using System.Runtime.InteropServices;
using System.Text.Json;
using System.Text.Json.Serialization;
namespace HtmlToMarkdown;

public static class HtmlToMarkdownRs
{
    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault
    };

    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull
    };

    /// <summary>
    /// Convert HTML to Markdown, returning a `ConversionResult` with content, metadata, images,
    /// and warnings.
    /// </summary>
    /// <param name="html">— the HTML string to convert.</param>
    ///
    /// <param name="options">— conversion options. The parameter bound is `impl Into&lt;Option&lt;ConversionOptions&gt;&gt;`, so any of the following call shapes are accepted:</param>
    ///
    /// <param name="convert(html,">ConversionOptions.default())` — bare options.</param>
    ///
    /// <param name="convert(html,">opts)` — bare options.</param>
    ///
    /// <param name="convert(html,">Some(opts))` — explicit `Option`.</param>
    ///
    /// <param name="convert(html,">None)` — fall back to `ConversionOptions.default`.</param>
    ///
    /// <exception cref="HtmlToMarkdownRsException">Returns an error if HTML parsing fails or if the input contains invalid UTF-8.</exception>
    /// <param name="html"></param>
    /// <param name="options">Optional.</param>
    public static ConversionResult Convert(string html, ConversionOptions? options)
    {
        var visitor = options?.Visitor;
        var optionsJson = options != null ? JsonSerializer.Serialize(options, JsonSerializationOptions) : "null";
        var optionsHandle = NativeMethods.ConversionOptionsFromJson(optionsJson);
        try
        {
            if (visitor != null)
            {
                using var bridge = new HtmlVisitorBridge(visitor);
                lock (HtmlVisitorBridge._registryLock)
                {
                    HtmlVisitorBridge._bridgeRegistry[bridge._bridgeId] = bridge;
                }
                var bridgeHandle = NativeMethods.HtmlVisitorBridgeNew(bridge._vtable, bridge._bridgeId);
                if (bridgeHandle == IntPtr.Zero) throw GetLastError();
                try
                {
                    NativeMethods.ConversionOptionsSetVisitor(optionsHandle, bridgeHandle);
                    var nativeResult = NativeMethods.Convert(html, optionsHandle);
                    if (nativeResult == IntPtr.Zero) throw GetLastError();
                    var jsonPtr = NativeMethods.ConversionResultToJson(nativeResult);
                    var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
                    NativeMethods.FreeString(jsonPtr);
                    NativeMethods.ConversionResultFree(nativeResult);
                    return JsonSerializer.Deserialize<ConversionResult>(json ?? "null", JsonOptions)!;
                }
                finally
                {
                    NativeMethods.HtmlVisitorBridgeFree(bridgeHandle);
                    lock (HtmlVisitorBridge._registryLock)
                    {
                        HtmlVisitorBridge._bridgeRegistry.Remove(bridge._bridgeId);
                    }
                }
            }
            else
            {
                var nativeResult = NativeMethods.Convert(html, optionsHandle);
                if (nativeResult == IntPtr.Zero) throw GetLastError();
                var jsonPtr = NativeMethods.ConversionResultToJson(nativeResult);
                var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
                NativeMethods.FreeString(jsonPtr);
                NativeMethods.ConversionResultFree(nativeResult);
                return JsonSerializer.Deserialize<ConversionResult>(json ?? "null", JsonOptions)!;
            }
        }
        finally
        {
            NativeMethods.ConversionOptionsFree(optionsHandle);
        }
    }

    /// <summary>
    /// Return a reference to the attribute map.
    ///
    /// If the context was built with `NodeContext.with_lazy_attributes`, the
    /// map is materialized on the first call and cached for subsequent calls.
    /// If this method is never called, no allocation occurs for attributes.
    /// </summary>
    public static Dictionary<string, string> NodeContextAttributes(IntPtr handle)
    {
        var nativeResult = NativeMethods.NodeContextAttributes(
            handle
        );
        if (nativeResult == IntPtr.Zero)
        {
            throw GetLastError();
        }
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        var returnValue = JsonSerializer.Deserialize<Dictionary<string, string>>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>
    /// Construct a `NodeContext` with an owned attribute map.
    ///
    /// Prefer `NodeContext.with_lazy_attributes` (pub(crate)) inside the
    /// converter to avoid the eager `collect_tag_attributes` allocation.
    /// </summary>
    /// <param name="nodeType"></param>
    /// <param name="tagName"></param>
    /// <param name="attributes"></param>
    /// <param name="depth"></param>
    /// <param name="indexInParent"></param>
    /// <param name="parentTag">Optional.</param>
    /// <param name="isInline"></param>
    public static NodeContext NodeContextWithOwnedAttributes(NodeType nodeType, string tagName, Dictionary<string, string> attributes, ulong depth, ulong indexInParent, string? parentTag, bool isInline)
    {
        ArgumentNullException.ThrowIfNull(tagName);
        var nodeTypeHandle = (int)nodeType;
        var attributesJson = JsonSerializer.Serialize(attributes, JsonSerializationOptions);
        var attributesHandle = global::System.Runtime.InteropServices.Marshal.StringToCoTaskMemUTF8(attributesJson);
        var nativeResult = NativeMethods.NodeContextWithOwnedAttributes(
            nodeTypeHandle,
            tagName,
            attributesHandle,
            depth,
            indexInParent,
            parentTag!,
            isInline
        );
        if (nativeResult == IntPtr.Zero)
        {
            throw GetLastError();
        }
        var jsonPtr = NativeMethods.NodeContextToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.NodeContextFree(nativeResult);
        var returnValue = JsonSerializer.Deserialize<NodeContext>(json ?? "null", JsonOptions)!;
        if (attributesHandle != global::System.IntPtr.Zero) global::System.Runtime.InteropServices.Marshal.FreeCoTaskMem(attributesHandle);
        return returnValue;
    }

    /// <summary>
    /// Promote any borrowed fields into owned storage so the context can outlive `'a`.
    /// </summary>
    public static NodeContext NodeContextIntoOwned(IntPtr handle)
    {
        var nativeResult = NativeMethods.NodeContextIntoOwned(
            handle
        );
        if (nativeResult == IntPtr.Zero)
        {
            throw GetLastError();
        }
        var jsonPtr = NativeMethods.NodeContextToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.NodeContextFree(nativeResult);
        var returnValue = JsonSerializer.Deserialize<NodeContext>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>Complete native registration of a HtmlVisitor implementation</summary>
    public static void RegisterHtmlVisitor(IntPtr handle)
    {
        if (handle == IntPtr.Zero) throw new ArgumentException("handle is null");
        var bridge = GCHandle.FromIntPtr(handle).Target as HtmlVisitorBridge ?? throw new InvalidOperationException("Invalid bridge handle");
        var impl = bridge.GetType().GetField("_impl", System.Reflection.BindingFlags.NonPublic | System.Reflection.BindingFlags.Instance)?.GetValue(bridge) as IHtmlVisitor;
        if (impl == null) throw new InvalidOperationException("Cannot extract impl from bridge");
        var name = System.Guid.NewGuid().ToString();
        var ec = NativeMethods.RegisterHtmlVisitor(name, bridge._vtable, handle, out var outError);
        if (ec != 0) {
            var msg = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(outError) ?? "Register failed";
            throw new HtmlToMarkdownRsException(ec, msg);
        }
    }
    private static Exception GetLastError()
    {
        var code = NativeMethods.LastErrorCode();
        var ctxPtr = NativeMethods.LastErrorContext();
        var message = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(ctxPtr) ?? "Unknown error";        if (code == 1) return new InvalidInputException(message);        if (message.StartsWith("Invalid configuration:")) return new ConfigErrorException(message);        if (message.StartsWith("HTML parsing error:")) return new ParseErrorException(message);        if (message.StartsWith("Sanitization error:")) return new SanitizationErrorException(message);        if (message.StartsWith("Conversion error:")) return new OtherException(message);        if (message.StartsWith("Internal panic:")) return new PanicException(message);        if (message.StartsWith("I/O error:")) return new IoErrorException(message);        if (code == 2) return new ConversionErrorException(message);        return new HtmlToMarkdownRsException(code, message);
    }
}
