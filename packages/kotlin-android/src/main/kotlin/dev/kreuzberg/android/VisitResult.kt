// Generated by alef. Do not edit by hand.
// alef:hash:6b0d5fe8bdcedf50ff7b9fd54eb88cec5b22be0ffcfd2da4673bebebf302c8af
@file:Suppress(
    "ktlint:standard:trailing-comma-on-call-site",
    "ktlint:standard:trailing-comma-on-declaration-site",
    "ktlint:standard:spacing-between-declarations-with-comments",
    "ktlint:standard:spacing-between-declarations-with-annotations",
    "ktlint:standard:when-entry-bracing",
    "ktlint:standard:blank-line-between-when-conditions",
    "ktlint:standard:blank-line-before-declaration",
    "ktlint:standard:chain-method-continuation",
    "ktlint:standard:annotation",
    "ktlint:standard:max-line-length",
    "ktlint:standard:no-semi",
    "ktlint:standard:statement-wrapping",
    "MaxLineLength",
    "TooManyFunctions",
    "FunctionParameterNaming",
    "LongParameterList",
    "CyclomaticComplexMethod",
    "LongMethod",
    "MagicNumber",
    "NestedBlockDepth",
)

package dev.kreuzberg.android

/**
 * Result of a visitor callback.
 *
 * Allows visitors to control the conversion flow by either proceeding with default behavior,
 * providing custom output, skipping elements, preserving HTML, or signaling errors.
 */
@com.fasterxml.jackson.databind.annotation.JsonDeserialize(using = VisitResultDeserializer::class)
@com.fasterxml.jackson.databind.annotation.JsonSerialize(using = VisitResultSerializer::class)
sealed class VisitResult {
    /** Continue with default conversion behavior */
    object Continue : VisitResult()

    /**
     * Replace default output with custom markdown
     *
     * The visitor takes full responsibility for the markdown output of this node and its children.
     */
    data class Custom(val value: String) : VisitResult()

    /**
     * Skip this element entirely (don't output anything)
     *
     * The element and all its children are ignored in the output.
     */
    object Skip : VisitResult()

    /**
     * Preserve original HTML (don't convert to markdown)
     *
     * The element's raw HTML is included verbatim in the output.
     */
    object PreserveHtml : VisitResult()

    /**
     * Stop conversion with an error
     *
     * The conversion process halts and returns this error message.
     */
    data class Error(val value: String) : VisitResult()
}

private class VisitResultDeserializer :
    com.fasterxml.jackson.databind.deser.std.StdDeserializer<VisitResult>(VisitResult::class.java) {
    @Suppress("LongMethod", "CyclomaticComplexMethod", "ReturnCount")
    override fun deserialize(
        parser: com.fasterxml.jackson.core.JsonParser,
        ctx: com.fasterxml.jackson.databind.DeserializationContext,
    ): VisitResult {
        val node = parser.codec.readTree<com.fasterxml.jackson.databind.JsonNode>(parser)
        if (node.isTextual) {
            return when (node.asText()) {
                "Continue" -> VisitResult.Continue
                "Skip" -> VisitResult.Skip
                "PreserveHtml" -> VisitResult.PreserveHtml
                else ->
                    throw com.fasterxml.jackson.databind.exc.InvalidFormatException(
                        parser,
                        "Unknown VisitResult unit variant",
                        node.asText(),
                        VisitResult::class.java,
                    )
            }
        }
        if (node.isObject) {
            val obj = node as com.fasterxml.jackson.databind.node.ObjectNode
            val it = obj.fields()
            if (it.hasNext()) {
                val entry = it.next()
                if (!it.hasNext()) {
                    val payload = entry.value
                    return when (entry.key) {
                        "Custom" ->
                            VisitResult.Custom(
                                ctx.readTreeAsValue<String>(payload, String::class.java)
                            )
                        "Error" ->
                            VisitResult.Error(
                                ctx.readTreeAsValue<String>(payload, String::class.java)
                            )
                        else ->
                            throw com.fasterxml.jackson.databind.exc.InvalidFormatException(
                                parser,
                                "Unknown VisitResult data variant",
                                entry.key,
                                VisitResult::class.java,
                            )
                    }
                }
            }
        }
        throw com.fasterxml.jackson.databind.exc.InvalidFormatException(
            parser,
            "Cannot deserialize VisitResult: expected string or single-field object",
            null,
            VisitResult::class.java,
        )
    }
}

private class VisitResultSerializer :
    com.fasterxml.jackson.databind.ser.std.StdSerializer<VisitResult>(VisitResult::class.java) {
    @Suppress("LongMethod")
    override fun serialize(
        value: VisitResult,
        gen: com.fasterxml.jackson.core.JsonGenerator,
        provider: com.fasterxml.jackson.databind.SerializerProvider,
    ) {
        @Suppress("UNCHECKED_CAST")
        val mapper =
            (gen.codec as? com.fasterxml.jackson.databind.ObjectMapper)
                ?: com.fasterxml.jackson.databind.ObjectMapper().findAndRegisterModules()
        when (value) {
            is VisitResult.Continue -> gen.writeString("Continue")
            is VisitResult.Custom -> {
                gen.writeStartObject()
                gen.writeFieldName("Custom")
                mapper.writeValue(gen, value.value)
                gen.writeEndObject()
            }
            is VisitResult.Skip -> gen.writeString("Skip")
            is VisitResult.PreserveHtml -> gen.writeString("PreserveHtml")
            is VisitResult.Error -> {
                gen.writeStartObject()
                gen.writeFieldName("Error")
                mapper.writeValue(gen, value.value)
                gen.writeEndObject()
            }
        }
    }
}
