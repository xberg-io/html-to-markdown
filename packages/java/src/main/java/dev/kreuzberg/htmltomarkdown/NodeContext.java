// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c7fefa1f90f498b589cdb595f5dd16298c54c2943c7c9ea9f7bcb194e6db7d37
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package dev.kreuzberg.htmltomarkdown;

import com.fasterxml.jackson.annotation.JsonProperty;
import org.jspecify.annotations.Nullable;

/**
 * Context information passed to all visitor methods.
 *
 * Provides comprehensive metadata about the current node being visited,
 * including its type, tag name, position in the DOM tree, and parent context.
 *
 * ## Attributes
 *
 * Access attributes via NodeContext.attributes, which returns
 * {@code &amp;BTreeMap&lt;String, String&gt;}. When the context was built with
 * NodeContext.with_lazy_attributes (the hot path inside the converter),
 * the map is only materialized on the first call — if the visitor never reads
 * attributes, the allocation is skipped.
 *
 * ## Lifetimes
 *
 * String fields use Cow&lt;'_, str&gt; so the converter can pass slices directly
 * out of the parsed DOM without allocating. Visitor implementations that need
 * to outlive the callback should call NodeContext.into_owned.
 */
public record NodeContext(
    /**
     * Coarse-grained node type classification
     */
    @JsonProperty("node_type") NodeType nodeType,
    /**
     * Raw HTML tag name (e.g., "div", "h1", "custom-element")
     */
    @JsonProperty("tag_name") String tagName,
    /**
     * Depth in the DOM tree (0 = root)
     */
    long depth,
    /**
     * Index among siblings (0-based)
     */
    @JsonProperty("index_in_parent") long indexInParent,
    /**
     * Parent element's tag name (null if root)
     */
    @Nullable @JsonProperty("parent_tag") String parentTag,
    /**
     * Whether this element is treated as inline vs block
     */
    @JsonProperty("is_inline") boolean isInline
) {
    /**
     * Construct a {@code NodeContext} with an owned attribute map.
     *
     * Prefer NodeContext.with_lazy_attributes (pub(crate)) inside the
     * converter to avoid the eager {@code collect_tag_attributes} allocation.
     */
    public static NodeContext withOwnedAttributes(
        NodeType nodeType,
        String tagName,
        Map<String, String> attributes,
        long depth,
        long indexInParent,
        String parentTag,
        boolean isInline
    ) {
        throw new UnsupportedOperationException("withOwnedAttributes is not yet bridged via JNI; use the Builder instead.");
    }
    /**
     * Promote any borrowed fields into owned storage so the context can outlive {@code 'a}.
     */
    public NodeContext intoOwned() {
        throw new UnsupportedOperationException("intoOwned is not yet bridged via JNI; reconstruct via Builder.");
    }
}
