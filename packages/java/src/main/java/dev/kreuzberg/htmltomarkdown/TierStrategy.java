// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:094484387808183514a801bab05d27e52c1d7d3d595e8913feed7cad1498eee3
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.htmltomarkdown;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

/**
 * Controls which conversion tier is used.
 */
public enum TierStrategy {
    /** Automatically pick the best tier for the input (default). */
    Auto("auto"),
    /**
     * Always use the Tier-2 ({@code tl.parse} + walk) path, skipping Tier-1.
     */
    Tier2("tier2"),
    /** Force the Tier-1 byte scanner; if it bails, fall back to Tier-2. */
    Tier1("tier1");

    /** The string value. */
    private final String value;

    TierStrategy(final String value) {
        this.value = value;
    }

    /** Returns the string value. */
    @JsonValue
    public String getValue() {
        return value;
    }

    /** Creates an instance from a string value. */
    @JsonCreator
    public static TierStrategy fromValue(final String value) {
        for (TierStrategy e : values()) {
            if (e.value.equalsIgnoreCase(value)) {
                return e;
            }
        }
        throw new IllegalArgumentException("Unknown value: " + value);
    }

    /** Returns the wire-format string value (matches JSON serialization). */
    @Override
    public String toString() {
        return value;
    }
}
