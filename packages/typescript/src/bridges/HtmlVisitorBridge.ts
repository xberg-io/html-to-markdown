/**
 * Generated by alef. Do not edit by hand.
 * alef:hash:7d62cf0c304e24a167a710e943106d8203118ef6cb83aa812f3133c24bfa7c7f
 *
 * TypeScript trait bridge for outbound plugin implementations.
 */

export interface HtmlVisitorBridge {
	name(): string;
	version(): string;
	initialize(): Promise<void>;
	shutdown(): Promise<void>;
	visitText(ctx: string, text: string): string;
	visitElementStart(ctx: string): string;
	visitElementEnd(ctx: string, output: string): string;
	visitLink(ctx: string, href: string, text: string, title: string): string;
	visitImage(ctx: string, src: string, alt: string, title: string): string;
	visitHeading(ctx: string, level: string, text: string, id: string): string;
	visitCodeBlock(ctx: string, lang: string, code: string): string;
	visitCodeInline(ctx: string, code: string): string;
	visitListItem(ctx: string, ordered: string, marker: string, text: string): string;
	visitListStart(ctx: string, ordered: string): string;
	visitListEnd(ctx: string, ordered: string, output: string): string;
	visitTableStart(ctx: string): string;
	visitTableRow(ctx: string, cells: string, isHeader: string): string;
	visitTableEnd(ctx: string, output: string): string;
	visitBlockquote(ctx: string, content: string, depth: string): string;
	visitStrong(ctx: string, text: string): string;
	visitEmphasis(ctx: string, text: string): string;
	visitStrikethrough(ctx: string, text: string): string;
	visitUnderline(ctx: string, text: string): string;
	visitSubscript(ctx: string, text: string): string;
	visitSuperscript(ctx: string, text: string): string;
	visitMark(ctx: string, text: string): string;
	visitLineBreak(ctx: string): string;
	visitHorizontalRule(ctx: string): string;
	visitCustomElement(ctx: string, tagName: string, html: string): string;
	visitDefinitionListStart(ctx: string): string;
	visitDefinitionTerm(ctx: string, text: string): string;
	visitDefinitionDescription(ctx: string, text: string): string;
	visitDefinitionListEnd(ctx: string, output: string): string;
	visitForm(ctx: string, action: string, method: string): string;
	visitInput(ctx: string, inputType: string, name: string, value: string): string;
	visitButton(ctx: string, text: string): string;
	visitAudio(ctx: string, src: string): string;
	visitVideo(ctx: string, src: string): string;
	visitIframe(ctx: string, src: string): string;
	visitDetails(ctx: string, open: string): string;
	visitSummary(ctx: string, text: string): string;
	visitFigureStart(ctx: string): string;
	visitFigcaption(ctx: string, text: string): string;
	visitFigureEnd(ctx: string, output: string): string;
}

export class HtmlVisitorAdapter {
	constructor(private bridge: HtmlVisitorBridge) {}

	async visitText(ctx: string, text: string): Promise<string> {
		try {
			const result = await this.bridge.visitText(ctx, text);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitElementStart(ctx: string): Promise<string> {
		try {
			const result = await this.bridge.visitElementStart(ctx);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitElementEnd(ctx: string, output: string): Promise<string> {
		try {
			const result = await this.bridge.visitElementEnd(ctx, output);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitLink(ctx: string, href: string, text: string, title: string): Promise<string> {
		try {
			const result = await this.bridge.visitLink(ctx, href, text, title);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitImage(ctx: string, src: string, alt: string, title: string): Promise<string> {
		try {
			const result = await this.bridge.visitImage(ctx, src, alt, title);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitHeading(ctx: string, level: string, text: string, id: string): Promise<string> {
		try {
			const result = await this.bridge.visitHeading(ctx, level, text, id);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitCodeBlock(ctx: string, lang: string, code: string): Promise<string> {
		try {
			const result = await this.bridge.visitCodeBlock(ctx, lang, code);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitCodeInline(ctx: string, code: string): Promise<string> {
		try {
			const result = await this.bridge.visitCodeInline(ctx, code);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitListItem(ctx: string, ordered: string, marker: string, text: string): Promise<string> {
		try {
			const result = await this.bridge.visitListItem(ctx, ordered, marker, text);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitListStart(ctx: string, ordered: string): Promise<string> {
		try {
			const result = await this.bridge.visitListStart(ctx, ordered);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitListEnd(ctx: string, ordered: string, output: string): Promise<string> {
		try {
			const result = await this.bridge.visitListEnd(ctx, ordered, output);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitTableStart(ctx: string): Promise<string> {
		try {
			const result = await this.bridge.visitTableStart(ctx);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitTableRow(ctx: string, cells: string, isHeader: string): Promise<string> {
		try {
			const result = await this.bridge.visitTableRow(ctx, cells, isHeader);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitTableEnd(ctx: string, output: string): Promise<string> {
		try {
			const result = await this.bridge.visitTableEnd(ctx, output);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitBlockquote(ctx: string, content: string, depth: string): Promise<string> {
		try {
			const result = await this.bridge.visitBlockquote(ctx, content, depth);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitStrong(ctx: string, text: string): Promise<string> {
		try {
			const result = await this.bridge.visitStrong(ctx, text);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitEmphasis(ctx: string, text: string): Promise<string> {
		try {
			const result = await this.bridge.visitEmphasis(ctx, text);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitStrikethrough(ctx: string, text: string): Promise<string> {
		try {
			const result = await this.bridge.visitStrikethrough(ctx, text);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitUnderline(ctx: string, text: string): Promise<string> {
		try {
			const result = await this.bridge.visitUnderline(ctx, text);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitSubscript(ctx: string, text: string): Promise<string> {
		try {
			const result = await this.bridge.visitSubscript(ctx, text);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitSuperscript(ctx: string, text: string): Promise<string> {
		try {
			const result = await this.bridge.visitSuperscript(ctx, text);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitMark(ctx: string, text: string): Promise<string> {
		try {
			const result = await this.bridge.visitMark(ctx, text);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitLineBreak(ctx: string): Promise<string> {
		try {
			const result = await this.bridge.visitLineBreak(ctx);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitHorizontalRule(ctx: string): Promise<string> {
		try {
			const result = await this.bridge.visitHorizontalRule(ctx);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitCustomElement(ctx: string, tagName: string, html: string): Promise<string> {
		try {
			const result = await this.bridge.visitCustomElement(ctx, tagName, html);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitDefinitionListStart(ctx: string): Promise<string> {
		try {
			const result = await this.bridge.visitDefinitionListStart(ctx);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitDefinitionTerm(ctx: string, text: string): Promise<string> {
		try {
			const result = await this.bridge.visitDefinitionTerm(ctx, text);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitDefinitionDescription(ctx: string, text: string): Promise<string> {
		try {
			const result = await this.bridge.visitDefinitionDescription(ctx, text);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitDefinitionListEnd(ctx: string, output: string): Promise<string> {
		try {
			const result = await this.bridge.visitDefinitionListEnd(ctx, output);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitForm(ctx: string, action: string, method: string): Promise<string> {
		try {
			const result = await this.bridge.visitForm(ctx, action, method);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitInput(ctx: string, inputType: string, name: string, value: string): Promise<string> {
		try {
			const result = await this.bridge.visitInput(ctx, inputType, name, value);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitButton(ctx: string, text: string): Promise<string> {
		try {
			const result = await this.bridge.visitButton(ctx, text);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitAudio(ctx: string, src: string): Promise<string> {
		try {
			const result = await this.bridge.visitAudio(ctx, src);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitVideo(ctx: string, src: string): Promise<string> {
		try {
			const result = await this.bridge.visitVideo(ctx, src);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitIframe(ctx: string, src: string): Promise<string> {
		try {
			const result = await this.bridge.visitIframe(ctx, src);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitDetails(ctx: string, open: string): Promise<string> {
		try {
			const result = await this.bridge.visitDetails(ctx, open);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitSummary(ctx: string, text: string): Promise<string> {
		try {
			const result = await this.bridge.visitSummary(ctx, text);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitFigureStart(ctx: string): Promise<string> {
		try {
			const result = await this.bridge.visitFigureStart(ctx);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitFigcaption(ctx: string, text: string): Promise<string> {
		try {
			const result = await this.bridge.visitFigcaption(ctx, text);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}

	async visitFigureEnd(ctx: string, output: string): Promise<string> {
		try {
			const result = await this.bridge.visitFigureEnd(ctx, output);
			return JSON.stringify({ ok: result });
		} catch (e) {
			return JSON.stringify({ err: String(e) });
		}
	}
}
