// Generated by alef. Do not edit by hand.
// alef:hash:13b7bce1b8084474dd907fe08752e4afc7c05b5c42cd4030ec54e77e76527ab1

import 'html_to_markdown_rs_bridge_generated/lib.dart';

/// Abstract class for the `HtmlVisitor` Rust trait.
///
/// Implement this class and register your implementation via:
/// ```dart
/// class MyHtmlVisitor implements HtmlVisitor {
///   @override
///   Future<VisitResult> visitText(...) async { ... }
///   @override
///   Future<VisitResult> visitElementStart(...) async { ... }
///   @override
///   Future<VisitResult> visitElementEnd(...) async { ... }
///   @override
///   Future<VisitResult> visitLink(...) async { ... }
///   @override
///   Future<VisitResult> visitImage(...) async { ... }
///   @override
///   Future<VisitResult> visitHeading(...) async { ... }
///   @override
///   Future<VisitResult> visitCodeBlock(...) async { ... }
///   @override
///   Future<VisitResult> visitCodeInline(...) async { ... }
///   @override
///   Future<VisitResult> visitListItem(...) async { ... }
///   @override
///   Future<VisitResult> visitListStart(...) async { ... }
///   @override
///   Future<VisitResult> visitListEnd(...) async { ... }
///   @override
///   Future<VisitResult> visitTableStart(...) async { ... }
///   @override
///   Future<VisitResult> visitTableRow(...) async { ... }
///   @override
///   Future<VisitResult> visitTableEnd(...) async { ... }
///   @override
///   Future<VisitResult> visitBlockquote(...) async { ... }
///   @override
///   Future<VisitResult> visitStrong(...) async { ... }
///   @override
///   Future<VisitResult> visitEmphasis(...) async { ... }
///   @override
///   Future<VisitResult> visitStrikethrough(...) async { ... }
///   @override
///   Future<VisitResult> visitUnderline(...) async { ... }
///   @override
///   Future<VisitResult> visitSubscript(...) async { ... }
///   @override
///   Future<VisitResult> visitSuperscript(...) async { ... }
///   @override
///   Future<VisitResult> visitMark(...) async { ... }
///   @override
///   Future<VisitResult> visitLineBreak(...) async { ... }
///   @override
///   Future<VisitResult> visitHorizontalRule(...) async { ... }
///   @override
///   Future<VisitResult> visitCustomElement(...) async { ... }
///   @override
///   Future<VisitResult> visitDefinitionListStart(...) async { ... }
///   @override
///   Future<VisitResult> visitDefinitionTerm(...) async { ... }
///   @override
///   Future<VisitResult> visitDefinitionDescription(...) async { ... }
///   @override
///   Future<VisitResult> visitDefinitionListEnd(...) async { ... }
///   @override
///   Future<VisitResult> visitForm(...) async { ... }
///   @override
///   Future<VisitResult> visitInput(...) async { ... }
///   @override
///   Future<VisitResult> visitButton(...) async { ... }
///   @override
///   Future<VisitResult> visitAudio(...) async { ... }
///   @override
///   Future<VisitResult> visitVideo(...) async { ... }
///   @override
///   Future<VisitResult> visitIframe(...) async { ... }
///   @override
///   Future<VisitResult> visitDetails(...) async { ... }
///   @override
///   Future<VisitResult> visitSummary(...) async { ... }
///   @override
///   Future<VisitResult> visitFigureStart(...) async { ... }
///   @override
///   Future<VisitResult> visitFigcaption(...) async { ... }
///   @override
///   Future<VisitResult> visitFigureEnd(...) async { ... }
/// }
///
/// final impl = createHtmlVisitorDartImpl(
///   visitText: (...) => myInstance.visitText(...),
///   visitElementStart: (...) => myInstance.visitElementStart(...),
///   visitElementEnd: (...) => myInstance.visitElementEnd(...),
///   visitLink: (...) => myInstance.visitLink(...),
///   visitImage: (...) => myInstance.visitImage(...),
///   visitHeading: (...) => myInstance.visitHeading(...),
///   visitCodeBlock: (...) => myInstance.visitCodeBlock(...),
///   visitCodeInline: (...) => myInstance.visitCodeInline(...),
///   visitListItem: (...) => myInstance.visitListItem(...),
///   visitListStart: (...) => myInstance.visitListStart(...),
///   visitListEnd: (...) => myInstance.visitListEnd(...),
///   visitTableStart: (...) => myInstance.visitTableStart(...),
///   visitTableRow: (...) => myInstance.visitTableRow(...),
///   visitTableEnd: (...) => myInstance.visitTableEnd(...),
///   visitBlockquote: (...) => myInstance.visitBlockquote(...),
///   visitStrong: (...) => myInstance.visitStrong(...),
///   visitEmphasis: (...) => myInstance.visitEmphasis(...),
///   visitStrikethrough: (...) => myInstance.visitStrikethrough(...),
///   visitUnderline: (...) => myInstance.visitUnderline(...),
///   visitSubscript: (...) => myInstance.visitSubscript(...),
///   visitSuperscript: (...) => myInstance.visitSuperscript(...),
///   visitMark: (...) => myInstance.visitMark(...),
///   visitLineBreak: (...) => myInstance.visitLineBreak(...),
///   visitHorizontalRule: (...) => myInstance.visitHorizontalRule(...),
///   visitCustomElement: (...) => myInstance.visitCustomElement(...),
///   visitDefinitionListStart: (...) => myInstance.visitDefinitionListStart(...),
///   visitDefinitionTerm: (...) => myInstance.visitDefinitionTerm(...),
///   visitDefinitionDescription: (...) => myInstance.visitDefinitionDescription(...),
///   visitDefinitionListEnd: (...) => myInstance.visitDefinitionListEnd(...),
///   visitForm: (...) => myInstance.visitForm(...),
///   visitInput: (...) => myInstance.visitInput(...),
///   visitButton: (...) => myInstance.visitButton(...),
///   visitAudio: (...) => myInstance.visitAudio(...),
///   visitVideo: (...) => myInstance.visitVideo(...),
///   visitIframe: (...) => myInstance.visitIframe(...),
///   visitDetails: (...) => myInstance.visitDetails(...),
///   visitSummary: (...) => myInstance.visitSummary(...),
///   visitFigureStart: (...) => myInstance.visitFigureStart(...),
///   visitFigcaption: (...) => myInstance.visitFigcaption(...),
///   visitFigureEnd: (...) => myInstance.visitFigureEnd(...),
/// );
/// ```
///
abstract class HtmlVisitor {
  /// Visit text nodes (most frequent callback - ~100+ per document).
  ///
  /// # Arguments
  /// - `ctx`: Node context (will have `node_type: NodeType::Text`)
  /// - `text`: The raw text content (HTML entities already decoded)
  Future<VisitResult> visitText(NodeContext ctx, String text);

  /// Called before entering any element.
  ///
  /// This is the first callback invoked for every HTML element, allowing
  /// visitors to implement generic element handling before tag-specific logic.
  Future<VisitResult> visitElementStart(NodeContext ctx);

  /// Called after exiting any element.
  ///
  /// Receives the default markdown output that would be generated.
  /// Visitors can inspect or replace this output.
  Future<VisitResult> visitElementEnd(NodeContext ctx, String output);

  /// Visit anchor links `<a href="...">`.
  ///
  /// # Arguments
  /// - `ctx`: Node context with link element metadata
  /// - `href`: The link URL (from `href` attribute)
  /// - `text`: The link text content (already converted to markdown)
  /// - `title`: Optional title attribute
  Future<VisitResult> visitLink(
    NodeContext ctx,
    String href,
    String text,
    String? title,
  );

  /// Visit images `<img src="...">`.
  ///
  /// # Arguments
  /// - `ctx`: Node context with image element metadata
  /// - `src`: The image source URL
  /// - `alt`: The alt text
  /// - `title`: Optional title attribute
  Future<VisitResult> visitImage(
    NodeContext ctx,
    String src,
    String alt,
    String? title,
  );

  /// Visit heading elements `<h1>` through `<h6>`.
  ///
  /// # Arguments
  /// - `ctx`: Node context with heading metadata
  /// - `level`: Heading level (1-6)
  /// - `text`: The heading text content
  /// - `id`: Optional id attribute (for anchor links)
  Future<VisitResult> visitHeading(
    NodeContext ctx,
    int level,
    String text,
    String? id,
  );

  /// Visit code blocks `<pre><code>`.
  ///
  /// # Arguments
  /// - `ctx`: Node context
  /// - `lang`: Optional language specifier (from class attribute)
  /// - `code`: The code content
  Future<VisitResult> visitCodeBlock(
    NodeContext ctx,
    String? lang,
    String code,
  );

  /// Visit inline code `<code>`.
  ///
  /// # Arguments
  /// - `ctx`: Node context
  /// - `code`: The code content
  Future<VisitResult> visitCodeInline(NodeContext ctx, String code);

  /// Visit list items `<li>`.
  ///
  /// # Arguments
  /// - `ctx`: Node context
  /// - `ordered`: Whether this is an ordered list item
  /// - `marker`: The list marker (e.g., "-", "1.", "a)")
  /// - `text`: The list item content (already converted)
  Future<VisitResult> visitListItem(
    NodeContext ctx,
    bool ordered,
    String marker,
    String text,
  );

  /// Called before processing a list `<ul>` or `<ol>`.
  Future<VisitResult> visitListStart(NodeContext ctx, bool ordered);

  /// Called after processing a list `</ul>` or `</ol>`.
  Future<VisitResult> visitListEnd(
    NodeContext ctx,
    bool ordered,
    String output,
  );

  /// Called before processing a table `<table>`.
  Future<VisitResult> visitTableStart(NodeContext ctx);

  /// Visit table rows `<tr>`.
  ///
  /// # Arguments
  /// - `ctx`: Node context
  /// - `cells`: Cell contents (already converted to markdown)
  /// - `is_header`: Whether this row is in `<thead>`
  Future<VisitResult> visitTableRow(
    NodeContext ctx,
    List<String> cells,
    bool isHeader,
  );

  /// Called after processing a table `</table>`.
  Future<VisitResult> visitTableEnd(NodeContext ctx, String output);

  /// Visit blockquote elements `<blockquote>`.
  ///
  /// # Arguments
  /// - `ctx`: Node context
  /// - `content`: The blockquote content (already converted)
  /// - `depth`: Nesting depth (for nested blockquotes)
  Future<VisitResult> visitBlockquote(
    NodeContext ctx,
    String content,
    int depth,
  );

  /// Visit strong/bold elements `<strong>`, `<b>`.
  Future<VisitResult> visitStrong(NodeContext ctx, String text);

  /// Visit emphasis/italic elements `<em>`, `<i>`.
  Future<VisitResult> visitEmphasis(NodeContext ctx, String text);

  /// Visit strikethrough elements `<s>`, `<del>`, `<strike>`.
  Future<VisitResult> visitStrikethrough(NodeContext ctx, String text);

  /// Visit underline elements `<u>`, `<ins>`.
  Future<VisitResult> visitUnderline(NodeContext ctx, String text);

  /// Visit subscript elements `<sub>`.
  Future<VisitResult> visitSubscript(NodeContext ctx, String text);

  /// Visit superscript elements `<sup>`.
  Future<VisitResult> visitSuperscript(NodeContext ctx, String text);

  /// Visit mark/highlight elements `<mark>`.
  Future<VisitResult> visitMark(NodeContext ctx, String text);

  /// Visit line break elements `<br>`.
  Future<VisitResult> visitLineBreak(NodeContext ctx);

  /// Visit horizontal rule elements `<hr>`.
  Future<VisitResult> visitHorizontalRule(NodeContext ctx);

  /// Visit custom elements (web components) or unknown tags.
  ///
  /// # Arguments
  /// - `ctx`: Node context
  /// - `tag_name`: The custom element's tag name
  /// - `html`: The raw HTML of this element
  Future<VisitResult> visitCustomElement(
    NodeContext ctx,
    String tagName,
    String html,
  );

  /// Visit definition list `<dl>`.
  Future<VisitResult> visitDefinitionListStart(NodeContext ctx);

  /// Visit definition term `<dt>`.
  Future<VisitResult> visitDefinitionTerm(NodeContext ctx, String text);

  /// Visit definition description `<dd>`.
  Future<VisitResult> visitDefinitionDescription(NodeContext ctx, String text);

  /// Called after processing a definition list `</dl>`.
  Future<VisitResult> visitDefinitionListEnd(NodeContext ctx, String output);

  /// Visit form elements `<form>`.
  Future<VisitResult> visitForm(
    NodeContext ctx,
    String? action,
    String? method,
  );

  /// Visit input elements `<input>`.
  Future<VisitResult> visitInput(
    NodeContext ctx,
    String inputType,
    String? name,
    String? value,
  );

  /// Visit button elements `<button>`.
  Future<VisitResult> visitButton(NodeContext ctx, String text);

  /// Visit audio elements `<audio>`.
  Future<VisitResult> visitAudio(NodeContext ctx, String? src);

  /// Visit video elements `<video>`.
  Future<VisitResult> visitVideo(NodeContext ctx, String? src);

  /// Visit iframe elements `<iframe>`.
  Future<VisitResult> visitIframe(NodeContext ctx, String? src);

  /// Visit details elements `<details>`.
  Future<VisitResult> visitDetails(NodeContext ctx, bool open);

  /// Visit summary elements `<summary>`.
  Future<VisitResult> visitSummary(NodeContext ctx, String text);

  /// Visit figure elements `<figure>`.
  Future<VisitResult> visitFigureStart(NodeContext ctx);

  /// Visit figcaption elements `<figcaption>`.
  Future<VisitResult> visitFigcaption(NodeContext ctx, String text);

  /// Called after processing a figure `</figure>`.
  Future<VisitResult> visitFigureEnd(NodeContext ctx, String output);
}
