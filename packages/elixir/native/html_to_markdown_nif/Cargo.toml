# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:8088f533b09ce20aa1d0469f2404f0d8e865bf694d4a62e47dc933805d849f18
# To regenerate: alef generate
# To verify freshness: alef verify

[package]
name = "html_to_markdown_nif"
version = "3.15.0"
edition = "2024"
license = "MIT"
description = "High-performance HTML to Markdown converter"
readme = false
keywords = ["converter", "html", "markdown"]
categories = []

[package.metadata.cargo-machete]
ignored = ["tokio", "async-trait", "tracing", "alloc-no-stdlib", "alloc-stdlib", "brotli-decompressor"]

[workspace]

[lib]
name = "html_to_markdown_nif"

crate-type = ["cdylib"]

[features]
default = ["inline-images", "metadata", "testkit", "visitor"]
inline-images = ["html-to-markdown-rs/inline-images"]
metadata = ["html-to-markdown-rs/metadata"]
testkit = ["html-to-markdown-rs/testkit"]
visitor = ["html-to-markdown-rs/visitor"]

[dependencies]
alloc-no-stdlib = "=3.0.0"
alloc-stdlib = "=0.3.0"
async-trait = "0.1"
brotli-decompressor = "=6.0.0"
html-to-markdown-rs = { version = "3.15.0", path = "../../../../crates/html-to-markdown", features = ["serde", "metadata", "visitor", "inline-images", "testkit"] }
rustler = "0.38"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread", "sync"] }
tracing = "0.1"
[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(feature, values("inline-images", "metadata", "testkit", "visitor"))'] }

# This crate deliberately does not use `[lints]` / `workspace = true`: its C-ABI /
# PyO3 / napi / ext-php-rs / NIF boundary requires `unsafe` code, and the workspace's
# `[workspace.lints.rust]` sets `unsafe_code = "deny"` -- an all-or-nothing table that
# would turn every such boundary into a compile error. The `[lints.clippy]` block below
# instead carries the subset of the workspace's deny-by-default lint policy this crate
# can actually satisfy. ~keep
[lints.clippy]
dbg_macro = "deny"
print_stderr = "deny"
print_stdout = "deny"
