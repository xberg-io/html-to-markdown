% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/options.R
\name{conversion_options}
\alias{conversion_options}
\title{Create a ConversionOptions list for generated bindings}
\usage{
conversion_options(
  heading_style = NULL,
  list_indent_type = NULL,
  list_indent_width = NULL,
  bullets = NULL,
  strong_em_symbol = NULL,
  escape_asterisks = NULL,
  escape_underscores = NULL,
  escape_misc = NULL,
  escape_ascii = NULL,
  code_language = NULL,
  autolinks = NULL,
  default_title = NULL,
  br_in_tables = NULL,
  compact_tables = NULL,
  highlight_style = NULL,
  extract_metadata = NULL,
  whitespace_mode = NULL,
  strip_newlines = NULL,
  wrap = NULL,
  wrap_width = NULL,
  convert_as_inline = NULL,
  sub_symbol = NULL,
  sup_symbol = NULL,
  newline_style = NULL,
  code_block_style = NULL,
  keep_inline_images_in = NULL,
  preprocessing = NULL,
  encoding = NULL,
  debug = NULL,
  strip_tags = NULL,
  preserve_tags = NULL,
  skip_images = NULL,
  url_escape_style = NULL,
  link_style = NULL,
  output_format = NULL,
  include_document_structure = NULL,
  extract_images = NULL,
  max_image_size = NULL,
  capture_svg = NULL,
  infer_dimensions = NULL,
  max_depth = NULL,
  exclude_selectors = NULL,
  visitor = NULL
)
}
\arguments{
\item{heading_style}{Heading style to use in Markdown output (ATX \verb{#} or Setext underline)}

\item{list_indent_type}{How to indent nested list items (spaces or tab)}

\item{list_indent_width}{Number of spaces (or tabs) to use for each level of list indentation}

\item{bullets}{Bullet character(s) to use for unordered list items (e.g. \code{"-"}, \code{"*"})}

\item{strong_em_symbol}{Character used for bold/italic emphasis markers (\code{*} or \verb{_})}

\item{escape_asterisks}{Escape \code{*} characters in plain text to avoid unintended bold/italic}

\item{escape_underscores}{Escape \verb{_} characters in plain text to avoid unintended bold/italic}

\item{escape_misc}{Escape miscellaneous Markdown metacharacters (\verb{[]()#} etc.) in plain text}

\item{escape_ascii}{Escape ASCII characters that have special meaning in certain Markdown dialects}

\item{code_language}{Default language annotation for fenced code blocks that have no language hint}

\item{autolinks}{Automatically convert bare URLs into Markdown autolinks}

\item{default_title}{Emit a default title when no \verb{<title>} tag is present}

\item{br_in_tables}{Render \verb{<br>} elements inside table cells as literal line breaks}

\item{compact_tables}{Emit tables without column padding (compact GFM format)}

\item{highlight_style}{Style used for \verb{<mark>} / highlighted text (e.g. \verb{==text==})}

\item{extract_metadata}{Populate \code{result.metadata} with \verb{<head>} / \verb{<meta>} extraction}

\item{whitespace_mode}{Controls how whitespace sequences are normalised in the converted output}

\item{strip_newlines}{Strip all newlines from the output, producing a single-line result}

\item{wrap}{Wrap long lines at \href{Self::wrap_width}{\code{wrap_width}} characters}

\item{wrap_width}{Maximum output line width in characters when \href{Self::wrap}{\code{wrap}} is \code{true} (default \code{80})}

\item{convert_as_inline}{Treat the entire document as inline content (no block-level wrappers)}

\item{sub_symbol}{Markdown notation for subscript text (e.g. \code{"~"})}

\item{sup_symbol}{Markdown notation for superscript text (e.g. \code{"^"})}

\item{newline_style}{How to encode hard line breaks (\verb{<br>}) in Markdown}

\item{code_block_style}{Style used for fenced code blocks (backticks or tilde)}

\item{keep_inline_images_in}{HTML tag names whose \verb{<img>} children are kept inline instead of block}

\item{preprocessing}{Options for the HTML pre-processing pass applied before conversion begins}

\item{encoding}{Expected character encoding of the input HTML (default \code{"utf-8"})}

\item{debug}{Emit debug information during conversion}

\item{strip_tags}{HTML tag names whose content is stripped from the output entirely}

\item{preserve_tags}{HTML tag names that are preserved verbatim in the output}

\item{skip_images}{Skip conversion of \verb{<img>} elements (omit images from output)}

\item{url_escape_style}{URL encoding strategy for link and image destinations}

\item{link_style}{Link rendering style (inline or reference)}

\item{output_format}{Target output format (Markdown, plain text, etc.)}

\item{include_document_structure}{Include structured document tree in result}

\item{extract_images}{Extract inline images from data URIs and SVGs}

\item{max_image_size}{Maximum decoded image size in bytes (default 5MB)}

\item{capture_svg}{Capture SVG elements as images}

\item{infer_dimensions}{Infer image dimensions from data}

\item{max_depth}{Maximum DOM traversal depth. \code{None} means unlimited}

\item{exclude_selectors}{CSS selectors for elements to exclude entirely (element + all content)}

\item{visitor}{(feature-gated) Optional visitor for custom traversal logic}
}
\value{
A named list suitable for the \code{options} argument of \code{\link[=convert]{convert()}}.
}
\description{
All parameters default to \code{NULL}, which means the Rust default is used.
Pass named arguments to override individual settings.
}
