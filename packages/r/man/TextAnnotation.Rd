% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{TextAnnotation}
\alias{TextAnnotation}
\title{A styling or semantic annotation that applies to a byte range within a node's text}
\usage{
TextAnnotation
}
\description{
Unlike \code{\link{DocumentNode}}, which captures block-level structure (headings, paragraphs, etc.),
a \code{TextAnnotation} describes inline-level markup — bold, italic, links, code spans, and
similar — that spans a contiguous run of bytes inside \code{DocumentNode::content}'s text field.
}
\details{
Byte offsets (\code{start}..\code{end}) are into the UTF-8 encoded text of the parent node. The range
follows Rust slice conventions: \code{start} is inclusive and \code{end} is exclusive, so the annotated
text is \verb{text[start as usize..end as usize]}.

Multiple annotations on the same node can overlap (e.g. bold-italic text), and they are
stored in the order they are encountered during DOM traversal.

See \code{\link{AnnotationKind}} for the full list of supported annotation types.
}
\section{Fields}{

\describe{
\item{\code{start}}{Start byte offset (inclusive) into the parent node's text.}

\item{\code{end}}{End byte offset (exclusive) into the parent node's text.}

\item{\code{kind}}{The type of annotation.}
}}
