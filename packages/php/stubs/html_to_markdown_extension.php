<?php

// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c4123217bb42e6cbe7cc59851037f301994cdb660a4a2934d8b056b2b5fc5adf
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Type stubs for the native PHP extension — declares classes
// provided at runtime by the compiled Rust extension (.so/.dll).
// Include this in phpstan.neon scanFiles for static analysis.

declare(strict_types=1);

namespace HtmlToMarkdown {
class HtmlToMarkdownException extends \RuntimeException
{
    public function getErrorCode(): int { throw new \RuntimeException('Not implemented.'); }
}

/**
 * Document-level metadata extracted from `<head>` and top-level elements.
 *
 * Contains all metadata typically used by search engines, social media platforms,
 * and browsers for document indexing and presentation.
 *
 * # Examples
 */
final class DocumentMetadata
{
    public function __construct(
        /** @var array<string> Document keywords from `<meta name="keywords">` tag, split on commas */
        public readonly array $keywords,
        /**
         * Open Graph metadata (og:* properties) for social media
         * Keys like "title", "description", "image", "url", etc.
         *
         * @var array<string, string>
         */
        public readonly array $openGraph,
        /**
         * Twitter Card metadata (twitter:* properties)
         * Keys like "card", "site", "creator", "title", "description", "image", etc.
         *
         * @var array<string, string>
         */
        public readonly array $twitterCard,
        /**
         * Additional meta tags not covered by specific fields
         * Keys are meta name/property attributes, values are content
         *
         * @var array<string, string>
         */
        public readonly array $metaTags,
        /** @var ?string Document title from `<title>` tag */
        public readonly ?string $title = null,
        /** @var ?string Document description from `<meta name="description">` tag */
        public readonly ?string $description = null,
        /** @var ?string Document author from `<meta name="author">` tag */
        public readonly ?string $author = null,
        /** @var ?string Canonical URL from `<link rel="canonical">` tag */
        public readonly ?string $canonicalUrl = null,
        /** @var ?string Base URL from `<base href="">` tag for resolving relative URLs */
        public readonly ?string $baseHref = null,
        /** @var ?string Document language from `lang` attribute */
        public readonly ?string $language = null,
        /** @var ?TextDirection Document text direction from `dir` attribute */
        public readonly ?TextDirection $textDirection = null
    ) { }
}

/**
 * Header element metadata with hierarchy tracking.
 *
 * Captures heading elements (h1-h6) with their text content, identifiers,
 * and position in the document structure.
 *
 * # Examples
 */
final class HeaderMetadata
{
    public function __construct(
        /** @var int Header level: 1 (h1) through 6 (h6) */
        public readonly int $level,
        /** @var string Normalized text content of the header */
        public readonly string $text,
        /** @var int Document tree depth at the header element */
        public readonly int $depth,
        /** @var int Byte offset in original HTML document */
        public readonly int $htmlOffset,
        /** @var ?string HTML id attribute if present */
        public readonly ?string $id = null
    ) { }
    public function isValid(): bool
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

/**
 * Hyperlink metadata with categorization and attributes.
 *
 * Represents `<a>` elements with parsed href values, text content, and link type classification.
 *
 * # Examples
 */
final class LinkMetadata
{
    public function __construct(
        /** @var string The href URL value */
        public readonly string $href,
        /** @var string Link text content (normalized, concatenated if mixed with elements) */
        public readonly string $text,
        /** @var LinkType Link type classification */
        public readonly LinkType $linkType,
        /** @var array<string> Rel attribute values (e.g., "nofollow", "stylesheet", "canonical") */
        public readonly array $rel,
        /** @var array<string, string> Additional HTML attributes */
        public readonly array $attributes,
        /** @var ?string Optional title attribute (often shown as tooltip) */
        public readonly ?string $title = null
    ) { }
}

/**
 * Image metadata with source and dimensions.
 *
 * Captures `<img>` elements and inline `<svg>` elements with metadata
 * for image analysis and optimization.
 *
 * # Examples
 */
final class ImageMetadata
{
    public function __construct(
        /** @var string Image source (URL, data URI, or SVG content identifier) */
        public readonly string $src,
        /** @var ImageType Image type classification */
        public readonly ImageType $imageType,
        /** @var array<string, string> Additional HTML attributes */
        public readonly array $attributes,
        /** @var ?string Alternative text from alt attribute (for accessibility) */
        public readonly ?string $alt = null,
        /** @var ?string Title attribute (often shown as tooltip) */
        public readonly ?string $title = null,
        /** @var ?ImageDimensions Image dimensions in pixels, if available. */
        public readonly ?ImageDimensions $dimensions = null
    ) { }
}

/**
 * Structured data block (JSON-LD, Microdata, or `RDFa`).
 *
 * Represents machine-readable structured data found in the document.
 * JSON-LD blocks are collected as raw JSON strings for flexibility.
 *
 * # Examples
 */
final class StructuredData
{
    public function __construct(
        /** @var StructuredDataType Type of structured data (JSON-LD, Microdata, `RDFa`) */
        public readonly StructuredDataType $dataType,
        /** @var string Raw JSON string (for JSON-LD) or serialized representation */
        public readonly string $rawJson,
        /** @var ?string Schema type if detectable (e.g., "Article", "Event", "Product") */
        public readonly ?string $schemaType = null
    ) { }
}

/**
 * Comprehensive metadata extraction result from HTML document.
 *
 * Contains all extracted metadata types in a single structure,
 * suitable for serialization and transmission across language boundaries.
 *
 * # Examples
 */
final class HtmlMetadata
{
    public function __construct(
        /** @var DocumentMetadata Document-level metadata (title, description, canonical, etc.) */
        public readonly DocumentMetadata $document,
        /** @var array<HeaderMetadata> Extracted header elements with hierarchy */
        public readonly array $headers,
        /** @var array<LinkMetadata> Extracted hyperlinks with type classification */
        public readonly array $links,
        /** @var array<ImageMetadata> Extracted images with source and dimensions */
        public readonly array $images,
        /** @var array<StructuredData> Extracted structured data blocks */
        public readonly array $structuredData
    ) { }
}

/**
 * Main conversion options for HTML to Markdown conversion.
 *
 * Use `ConversionOptions.builder()` to construct, or `Default.default()` for defaults.
 *
 * # Example
 */
final class ConversionOptions
{
    public function __construct(
        /** @var HeadingStyle Heading style to use in Markdown output (ATX `#` or Setext underline). */
        public readonly HeadingStyle $headingStyle,
        /** @var ListIndentType How to indent nested list items (spaces or tab). */
        public readonly ListIndentType $listIndentType,
        /** @var int Number of spaces (or tabs) to use for each level of list indentation. */
        public readonly int $listIndentWidth,
        /** @var string Bullet character(s) to use for unordered list items (e.g. `"-"`, `"*"`). */
        public readonly string $bullets,
        /** @var string Character used for bold/italic emphasis markers (`*` or `_`). */
        public readonly string $strongEmSymbol,
        /** @var bool Escape `*` characters in plain text to avoid unintended bold/italic. */
        public readonly bool $escapeAsterisks,
        /** @var bool Escape `_` characters in plain text to avoid unintended bold/italic. */
        public readonly bool $escapeUnderscores,
        /** @var bool Escape miscellaneous Markdown metacharacters (`[]()#` etc.) in plain text. */
        public readonly bool $escapeMisc,
        /** @var bool Escape ASCII characters that have special meaning in certain Markdown dialects. */
        public readonly bool $escapeAscii,
        /** @var string Default language annotation for fenced code blocks that have no language hint. */
        public readonly string $codeLanguage,
        /** @var bool Automatically convert bare URLs into Markdown autolinks. */
        public readonly bool $autolinks,
        /** @var bool Emit a default title when no `<title>` tag is present. */
        public readonly bool $defaultTitle,
        /** @var bool Render `<br>` elements inside table cells as literal line breaks. */
        public readonly bool $brInTables,
        /**
         * Emit tables without column padding (compact GFM format).
         *
         * When `true`, column widths are not computed and cells are emitted with
         * no trailing spaces. Separator rows use exactly `---` per column.
         * Produces token-efficient output suitable for RAG / LLM contexts.
         *
         * Default `false` (aligned padding preserved).
         *
         * @var bool
         */
        public readonly bool $compactTables,
        /** @var HighlightStyle Style used for `<mark>` / highlighted text (e.g. `==text==`). */
        public readonly HighlightStyle $highlightStyle,
        /**
         * Populate `result.metadata` with `<head>` / `<meta>` extraction
         * (title, description, Open Graph, Twitter Card, JSON-LD, …).
         *
         * Default `true`. Disabling skips the metadata pass only — table
         * extraction into `result.tables` runs unconditionally.
         *
         * @var bool
         */
        public readonly bool $extractMetadata,
        /**
         * Controls how whitespace sequences are normalised in the converted output.
         *
         * - [`WhitespaceMode::Normalized`] (default) — collapses consecutive whitespace characters
         * (spaces, tabs, newlines) to a single space, matching browser rendering behaviour.
         * - [`WhitespaceMode::Strict`] — preserves all whitespace exactly as it appears in the
         * source HTML, including runs of spaces and embedded newlines.
         *
         * Choose `Strict` only when the source HTML uses deliberate whitespace (e.g. pre-formatted
         * content outside `<pre>` tags). For most documents `Normalized` produces cleaner output.
         *
         * @var WhitespaceMode
         */
        public readonly WhitespaceMode $whitespaceMode,
        /** @var bool Strip all newlines from the output, producing a single-line result. */
        public readonly bool $stripNewlines,
        /** @var bool Wrap long lines at [`wrap_width`](Self::wrap_width) characters. */
        public readonly bool $wrap,
        /**
         * Maximum output line width in characters when [`wrap`](Self::wrap) is `true` (default `80`).
         *
         * Lines are broken at word boundaries so that no line exceeds this length. A value of `0`
         * is treated as "no limit" — equivalent to leaving [`wrap`](Self::wrap) disabled. Has no
         * effect when `wrap` is `false`.
         *
         * @var int
         */
        public readonly int $wrapWidth,
        /** @var bool Treat the entire document as inline content (no block-level wrappers). */
        public readonly bool $convertAsInline,
        /** @var string Markdown notation for subscript text (e.g. `"~"`). */
        public readonly string $subSymbol,
        /** @var string Markdown notation for superscript text (e.g. `"^"`). */
        public readonly string $supSymbol,
        /** @var NewlineStyle How to encode hard line breaks (`<br>`) in Markdown. */
        public readonly NewlineStyle $newlineStyle,
        /** @var CodeBlockStyle Style used for fenced code blocks (backticks or tilde). */
        public readonly CodeBlockStyle $codeBlockStyle,
        /** @var array<string> HTML tag names whose `<img>` children are kept inline instead of block. */
        public readonly array $keepInlineImagesIn,
        /**
         * Options for the HTML pre-processing pass applied before conversion begins.
         *
         * Pre-processing runs before the HTML is handed to the converter and can perform operations
         * such as unwrapping redundant wrapper elements, removing tracking pixels, and normalising
         * vendor-specific markup. See [`PreprocessingOptions`] for the full set of knobs.
         *
         * Defaults to [`PreprocessingOptions::default()`], which enables the standard cleaning
         * passes. Set individual fields on [`PreprocessingOptions`] (or construct via
         * [`ConversionOptions::builder`]) to opt in or out of specific passes.
         *
         * @var PreprocessingOptions
         */
        public readonly PreprocessingOptions $preprocessing,
        /** @var string Expected character encoding of the input HTML (default `"utf-8"`). */
        public readonly string $encoding,
        /** @var bool Emit debug information during conversion. */
        public readonly bool $debug,
        /** @var array<string> HTML tag names whose content is stripped from the output entirely. */
        public readonly array $stripTags,
        /** @var array<string> HTML tag names that are preserved verbatim in the output. */
        public readonly array $preserveTags,
        /** @var bool Skip conversion of `<img>` elements (omit images from output). */
        public readonly bool $skipImages,
        /**
         * URL encoding strategy for link and image destinations.
         *
         * Controls how special characters in URL destinations are escaped:
         * - [`UrlEscapeStyle::Angle`] (default) — wraps the destination in angle brackets when it
         * contains spaces or newlines. Some parsers misinterpret `>` inside such a destination.
         * - [`UrlEscapeStyle::Percent`] — percent-encodes every character that is not an RFC 3986
         * unreserved character or `/`, producing a destination that all Markdown parsers handle
         * correctly even when the URL contains `<`, `>`, spaces, or parentheses.
         *
         * @var UrlEscapeStyle
         */
        public readonly UrlEscapeStyle $urlEscapeStyle,
        /** @var LinkStyle Link rendering style (inline or reference). */
        public readonly LinkStyle $linkStyle,
        /** @var OutputFormat Target output format (Markdown, plain text, etc.). */
        public readonly OutputFormat $outputFormat,
        /** @var bool Include structured document tree in result. */
        public readonly bool $includeDocumentStructure,
        /** @var bool Extract inline images from data URIs and SVGs. */
        public readonly bool $extractImages,
        /** @var int Maximum decoded image size in bytes (default 5MB). */
        public readonly int $maxImageSize,
        /** @var bool Capture SVG elements as images. */
        public readonly bool $captureSvg,
        /** @var bool Infer image dimensions from data. */
        public readonly bool $inferDimensions,
        /**
         * CSS selectors for elements to exclude entirely (element + all content).
         *
         * Unlike `strip_tags` (which removes the tag wrapper but keeps children),
         * excluded elements and all their descendants are dropped from the output.
         * Supports any CSS selector that `tl` supports: tag names, `.class`,
         * `#id`, `[attribute]`, etc.
         *
         * Invalid selectors are silently skipped at conversion time.
         *
         * Example: `vec![".cookie-banner".into(), "#ad-container".into(), "[role='complementary']".into()]`
         *
         * @var array<string>
         */
        public readonly array $excludeSelectors,
        /**
         * Which conversion tier to use.
         *
         * - [`TierStrategy::Auto`] (default) — automatically choose the best path.
         * - [`TierStrategy::Tier2`] — always use the Tier-2 DOM-walk path.
         * - `TierStrategy::Tier1` — always attempt Tier-1 (testkit only).
         *
         * @var TierStrategy
         */
        public readonly TierStrategy $tierStrategy,
        /**
         * Maximum DOM traversal depth. `None` means unlimited.
         * When set, subtrees beyond this depth are silently truncated.
         *
         * @var ?int
         */
        public readonly ?int $maxDepth = null,
        /**
         * Optional visitor for custom traversal logic.
         *
         * When set, the visitor's callbacks are invoked for matching HTML elements
         * during conversion, allowing custom output, skipping, or HTML preservation.
         * See `HtmlVisitor`.
         *
         * @var ?VisitorHandle
         */
        public readonly ?VisitorHandle $visitor = null
    ) { }
    public static function default(): ConversionOptions
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

/**
 * Partial update for `ConversionOptions`.
 *
 * Uses `Option<T>` fields for selective updates. Bindings use this to construct
 * options from language-native types. Prefer `ConversionOptionsBuilder` for Rust code.
 */
final class ConversionOptionsUpdate
{
    public function __construct(
        /** @var ?HeadingStyle Optional override for [`ConversionOptions::heading_style`]. */
        public readonly ?HeadingStyle $headingStyle = null,
        /** @var ?ListIndentType Optional override for [`ConversionOptions::list_indent_type`]. */
        public readonly ?ListIndentType $listIndentType = null,
        /** @var ?int Optional override for [`ConversionOptions::list_indent_width`]. */
        public readonly ?int $listIndentWidth = null,
        /** @var ?string Optional override for [`ConversionOptions::bullets`]. */
        public readonly ?string $bullets = null,
        /** @var ?string Optional override for [`ConversionOptions::strong_em_symbol`]. */
        public readonly ?string $strongEmSymbol = null,
        /** @var ?bool Optional override for [`ConversionOptions::escape_asterisks`]. */
        public readonly ?bool $escapeAsterisks = null,
        /** @var ?bool Optional override for [`ConversionOptions::escape_underscores`]. */
        public readonly ?bool $escapeUnderscores = null,
        /** @var ?bool Optional override for [`ConversionOptions::escape_misc`]. */
        public readonly ?bool $escapeMisc = null,
        /** @var ?bool Optional override for [`ConversionOptions::escape_ascii`]. */
        public readonly ?bool $escapeAscii = null,
        /** @var ?string Optional override for [`ConversionOptions::code_language`]. */
        public readonly ?string $codeLanguage = null,
        /** @var ?bool Optional override for [`ConversionOptions::autolinks`]. */
        public readonly ?bool $autolinks = null,
        /** @var ?bool Optional override for [`ConversionOptions::default_title`]. */
        public readonly ?bool $defaultTitle = null,
        /** @var ?bool Optional override for [`ConversionOptions::br_in_tables`]. */
        public readonly ?bool $brInTables = null,
        /** @var ?bool Optional override for [`ConversionOptions::compact_tables`]. */
        public readonly ?bool $compactTables = null,
        /** @var ?HighlightStyle Optional override for [`ConversionOptions::highlight_style`]. */
        public readonly ?HighlightStyle $highlightStyle = null,
        /** @var ?bool Optional override for [`ConversionOptions::extract_metadata`]. */
        public readonly ?bool $extractMetadata = null,
        /** @var ?WhitespaceMode Optional override for [`ConversionOptions::whitespace_mode`]. */
        public readonly ?WhitespaceMode $whitespaceMode = null,
        /** @var ?bool Optional override for [`ConversionOptions::strip_newlines`]. */
        public readonly ?bool $stripNewlines = null,
        /** @var ?bool Optional override for [`ConversionOptions::wrap`]. */
        public readonly ?bool $wrap = null,
        /** @var ?int Optional override for [`ConversionOptions::wrap_width`]. */
        public readonly ?int $wrapWidth = null,
        /** @var ?bool Optional override for [`ConversionOptions::convert_as_inline`]. */
        public readonly ?bool $convertAsInline = null,
        /** @var ?string Optional override for [`ConversionOptions::sub_symbol`]. */
        public readonly ?string $subSymbol = null,
        /** @var ?string Optional override for [`ConversionOptions::sup_symbol`]. */
        public readonly ?string $supSymbol = null,
        /** @var ?NewlineStyle Optional override for [`ConversionOptions::newline_style`]. */
        public readonly ?NewlineStyle $newlineStyle = null,
        /** @var ?CodeBlockStyle Optional override for [`ConversionOptions::code_block_style`]. */
        public readonly ?CodeBlockStyle $codeBlockStyle = null,
        /** @var ?array<string> Optional override for [`ConversionOptions::keep_inline_images_in`]. */
        public readonly ?array $keepInlineImagesIn = null,
        /** @var ?PreprocessingOptionsUpdate Optional override for [`ConversionOptions::preprocessing`]. */
        public readonly ?PreprocessingOptionsUpdate $preprocessing = null,
        /** @var ?string Optional override for [`ConversionOptions::encoding`]. */
        public readonly ?string $encoding = null,
        /** @var ?bool Optional override for [`ConversionOptions::debug`]. */
        public readonly ?bool $debug = null,
        /** @var ?array<string> Optional override for [`ConversionOptions::strip_tags`]. */
        public readonly ?array $stripTags = null,
        /** @var ?array<string> Optional override for [`ConversionOptions::preserve_tags`]. */
        public readonly ?array $preserveTags = null,
        /** @var ?bool Optional override for [`ConversionOptions::skip_images`]. */
        public readonly ?bool $skipImages = null,
        /** @var ?UrlEscapeStyle Optional override for [`ConversionOptions::url_escape_style`]. */
        public readonly ?UrlEscapeStyle $urlEscapeStyle = null,
        /** @var ?LinkStyle Optional override for [`ConversionOptions::link_style`]. */
        public readonly ?LinkStyle $linkStyle = null,
        /** @var ?OutputFormat Optional override for [`ConversionOptions::output_format`]. */
        public readonly ?OutputFormat $outputFormat = null,
        /** @var ?bool Optional override for [`ConversionOptions::include_document_structure`]. */
        public readonly ?bool $includeDocumentStructure = null,
        /** @var ?bool Optional override for [`ConversionOptions::extract_images`]. */
        public readonly ?bool $extractImages = null,
        /** @var ?int Optional override for [`ConversionOptions::max_image_size`]. */
        public readonly ?int $maxImageSize = null,
        /** @var ?bool Optional override for [`ConversionOptions::capture_svg`]. */
        public readonly ?bool $captureSvg = null,
        /** @var ?bool Optional override for [`ConversionOptions::infer_dimensions`]. */
        public readonly ?bool $inferDimensions = null,
        /** @var ?int Optional override for [`ConversionOptions::max_depth`]. */
        public readonly ?int $maxDepth = null,
        /** @var ?array<string> Optional override for [`ConversionOptions::exclude_selectors`]. */
        public readonly ?array $excludeSelectors = null,
        /** @var ?TierStrategy Optional override for [`ConversionOptions::tier_strategy`]. */
        public readonly ?TierStrategy $tierStrategy = null,
        /** @var ?VisitorHandle Optional override for [`ConversionOptions::visitor`]. */
        public readonly ?VisitorHandle $visitor = null
    ) { }
}

/**
 * HTML preprocessing options for document cleanup before conversion.
 */
final class PreprocessingOptions
{
    public function __construct(
        /** @var bool Enable HTML preprocessing globally */
        public readonly bool $enabled,
        /** @var PreprocessingPreset Preprocessing preset level (Minimal, Standard, Aggressive) */
        public readonly PreprocessingPreset $preset,
        /** @var bool Remove navigation elements (nav, breadcrumbs, menus, sidebars) */
        public readonly bool $removeNavigation,
        /** @var bool Remove form elements (forms, inputs, buttons, etc.) */
        public readonly bool $removeForms
    ) { }
    public static function default(): PreprocessingOptions
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

/**
 * Partial update for `PreprocessingOptions`.
 *
 * This struct uses `Option<T>` to represent optional fields that can be selectively updated.
 * Only specified fields (values) will override existing options; null values leave the
 * corresponding fields unchanged when applied via `PreprocessingOptions.apply_update`.
 */
final class PreprocessingOptionsUpdate
{
    public function __construct(
        /** @var ?bool Optional global preprocessing enablement override */
        public readonly ?bool $enabled = null,
        /** @var ?PreprocessingPreset Optional preprocessing preset level override (Minimal, Standard, Aggressive) */
        public readonly ?PreprocessingPreset $preset = null,
        /** @var ?bool Optional navigation element removal override (nav, breadcrumbs, menus, sidebars) */
        public readonly ?bool $removeNavigation = null,
        /** @var ?bool Optional form element removal override (forms, inputs, buttons, etc.) */
        public readonly ?bool $removeForms = null
    ) { }
}

/**
 * Image dimensions in pixels.
 *
 * Binding-safe replacement for `(u32, u32)` tuples, which degrade to
 * `Vec<Vec<String>>` when sanitized for cross-language binding generation.
 * Used by both `ImageMetadata` and
 * `InlineImage`.
 */
final class ImageDimensions
{
    public function __construct(
        /** @var int Width in pixels. */
        public readonly int $width,
        /** @var int Height in pixels. */
        public readonly int $height
    ) { }
}

/**
 * A structured document tree representing the semantic content of an HTML document.
 *
 * Uses a flat node array with index-based parent/child references for efficient traversal.
 */
final class DocumentStructure
{
    public function __construct(
        /** @var array<DocumentNode> All nodes in document reading order. */
        public readonly array $nodes,
        /** @var ?string The source format (always "html" for this crate). */
        public readonly ?string $sourceFormat = null
    ) { }
}

/**
 * A single node in the document tree.
 */
final class DocumentNode
{
    public function __construct(
        /** @var string Deterministic node identifier. */
        public readonly string $id,
        /** @var NodeContent The semantic content of this node. */
        public readonly NodeContent $content,
        /** @var array<int> Indices of child nodes in reading order. */
        public readonly array $children,
        /** @var array<TextAnnotation> Inline formatting annotations (bold, italic, links, etc.) with byte offsets into the text. */
        public readonly array $annotations,
        /** @var ?int Index of the parent node (None for root nodes). */
        public readonly ?int $parent = null,
        /**
         * Format-specific attributes preserved from the source HTML element.
         *
         * Keys are lowercased attribute names as they appear in the HTML (e.g. `"class"`, `"id"`,
         * `"data-foo"`). Values are the raw attribute strings, copied verbatim from the source —
         * no HTML entity decoding is applied here.
         *
         * The map is `None` when no attributes are present (omitted entirely in serialized output).
         * Not every HTML attribute is preserved: only attributes that carry semantic or structural
         * significance for the node type are collected. For example, heading nodes capture the `"id"`
         * attribute for anchor linking; other element-level attributes may be silently dropped.
         *
         * @var ?array<string, string>
         */
        public readonly ?array $attributes = null
    ) { }
}

/**
 * A styling or semantic annotation that applies to a byte range within a node's text.
 *
 * Unlike `DocumentNode`, which captures block-level structure (headings, paragraphs, etc.),
 * a `TextAnnotation` describes inline-level markup — bold, italic, links, code spans, and
 * similar — that spans a contiguous run of bytes inside `DocumentNode.content`'s text field.
 *
 * Byte offsets (`start`..`end`) are into the UTF-8 encoded text of the parent node. The range
 * follows Rust slice conventions: `start` is inclusive and `end` is exclusive, so the annotated
 * text is `text[start as usize..end as usize]`.
 *
 * Multiple annotations on the same node can overlap (e.g. bold-italic text), and they are
 * stored in the order they are encountered during DOM traversal.
 *
 * See `AnnotationKind` for the full list of supported annotation types.
 */
final class TextAnnotation
{
    public function __construct(
        /** @var int Start byte offset (inclusive) into the parent node's text. */
        public readonly int $start,
        /** @var int End byte offset (exclusive) into the parent node's text. */
        public readonly int $end,
        /** @var AnnotationKind The type of annotation. */
        public readonly AnnotationKind $kind
    ) { }
}

/**
 * A single key-value metadata entry from `<head>` meta tags.
 *
 * Binding-safe replacement for `(String, String)` tuples used in
 * `NodeContent.MetadataBlock`. Tuple pairs cannot be represented
 * across language boundaries without lossy degradation.
 */
final class MetadataEntry
{
    public function __construct(
        /** @var string Metadata key (e.g. `"title"`, `"description"`, `"og:title"`). */
        public readonly string $key,
        /** @var string Metadata value. */
        public readonly string $value
    ) { }
}

/**
 * The primary result of HTML conversion and extraction.
 *
 * Contains the converted text output, optional structured document tree,
 * metadata, extracted tables, images, and processing warnings.
 *
 * # Example
 *
 * ```text
 * use html_to_markdown_rs::{convert, ConversionOptions};
 *
 * let result = convert("<h1>Hello</h1><p>World</p>", None)?;
 * assert!(result.content.is_some());
 * assert!(result.warnings.is_empty());
 * ```
 */
final class ConversionResult
{
    public function __construct(
        /** @var HtmlMetadata Extracted HTML metadata (title, OG, links, images, structured data). */
        public readonly HtmlMetadata $metadata,
        /** @var array<TableData> Extracted tables with structured cell data and markdown representation. */
        public readonly array $tables,
        /** @var array<ProcessingWarning> Non-fatal processing warnings. */
        public readonly array $warnings,
        /**
         * Converted text output (markdown, djot, or plain text).
         *
         * `None` when `output_format` is set to `OutputFormat::None`,
         * indicating extraction-only mode.
         *
         * @var ?string
         */
        public readonly ?string $content = null,
        /**
         * Structured document tree with semantic elements.
         *
         * Populated when `ConversionOptions::include_document_structure` is `true`. `None`
         * otherwise (the default), which avoids the overhead of building the tree.
         *
         * When present, the tree mirrors the converted document: headings open
         * `Group` sections, paragraphs and list items carry
         * inline `TextAnnotation`s, and tables reference the same
         * `TableGrid` data exposed in [`Self::tables`].
         *
         * Note: this field is independent of the `metadata` feature flag. Document structure
         * collection is always available at runtime; it is gated only by the runtime option, not
         * by a compile-time feature.
         *
         * @var ?DocumentStructure
         */
        public readonly ?DocumentStructure $document = null
    ) { }
}

/**
 * A structured table grid with cell-level data including spans.
 */
final class TableGrid
{
    public function __construct(
        /** @var int Number of rows. */
        public readonly int $rows,
        /** @var int Number of columns. */
        public readonly int $cols,
        /**
         * All cells in the table as a flat, sparse list.
         *
         * The list is ordered by `(row, col)` but is **not** a dense `rows × cols` matrix: cells
         * that are covered by a spanning cell (via `row_span > 1` or `col_span > 1`) do not appear
         * in the list. Only the top-left "origin" cell of a span is present, with its `row_span`
         * and `col_span` fields set accordingly.
         *
         * To reconstruct the full visual grid, iterate over all cells and mark the rectangular
         * region `[row .. row+row_span, col .. col+col_span]` as occupied by that cell. Any
         * `(row, col)` position that is not the origin of any cell is covered by a span from an
         * earlier cell.
         *
         * The length of this vec is `≤ rows * cols`. An empty table (`rows == 0 || cols == 0`)
         * produces an empty vec.
         *
         * @var array<GridCell>
         */
        public readonly array $cells
    ) { }
}

/**
 * A single cell in a table grid.
 */
final class GridCell
{
    public function __construct(
        /** @var string The text content of the cell. */
        public readonly string $content,
        /** @var int 0-indexed row position. */
        public readonly int $row,
        /** @var int 0-indexed column position. */
        public readonly int $col,
        /** @var int Number of rows this cell spans (default 1). */
        public readonly int $rowSpan,
        /** @var int Number of columns this cell spans (default 1). */
        public readonly int $colSpan,
        /** @var bool Whether this is a header cell (`<th>`). */
        public readonly bool $isHeader
    ) { }
}

/**
 * A top-level extracted table with both structured data and markdown representation.
 */
final class TableData
{
    public function __construct(
        /** @var TableGrid The structured table grid. */
        public readonly TableGrid $grid,
        /** @var string The markdown rendering of this table. */
        public readonly string $markdown
    ) { }
}

/**
 * A non-fatal diagnostic produced during HTML conversion.
 *
 * Warnings indicate that conversion completed but some content may have been handled
 * differently than expected — for example, an image that could not be extracted, a truncated
 * input, or malformed HTML that was repaired with best-effort parsing.
 *
 * Conversion always succeeds (returns `ConversionResult`) even when warnings are
 * present. Callers should inspect `warnings` and decide how to
 * handle them based on their tolerance for partial results:
 *
 * - **Logging pipelines**: emit each warning at `WARN` level and continue.
 * - **Strict pipelines**: treat any warning as a hard error by checking
 *   `result.warnings.is_empty()` before using the output.
 *
 * See `WarningKind` for the full taxonomy of warning categories.
 */
final class ProcessingWarning
{
    public function __construct(
        /** @var string Human-readable warning message. */
        public readonly string $message,
        /** @var WarningKind The category of warning. */
        public readonly WarningKind $kind
    ) { }
}

/**
 * Context information passed to all visitor methods.
 *
 * Provides comprehensive metadata about the current node being visited,
 * including its type, tag name, position in the DOM tree, and parent context.
 *
 * ## Attributes
 *
 * Access attributes via `NodeContext.attributes`, which returns
 * `&BTreeMap<String, String>`. When the context was built with
 * `NodeContext.with_lazy_attributes` (the hot path inside the converter),
 * the map is only materialized on the first call — if the visitor never reads
 * attributes, the allocation is skipped.
 *
 * ## Lifetimes
 *
 * String fields use `Cow<'_, str>` so the converter can pass slices directly
 * out of the parsed DOM without allocating. Visitor implementations that need
 * to outlive the callback should call `NodeContext.into_owned`.
 */
final class NodeContext
{
    public function __construct(
        /** @var NodeType Coarse-grained node type classification */
        public readonly NodeType $nodeType,
        /** @var string Raw HTML tag name (e.g., "div", "h1", "custom-element") */
        public readonly string $tagName,
        /** @var int Depth in the DOM tree (0 = root) */
        public readonly int $depth,
        /** @var int Index among siblings (0-based) */
        public readonly int $indexInParent,
        /** @var bool Whether this element is treated as inline vs block */
        public readonly bool $isInline,
        /** @var ?string Parent element's tag name (None if root) */
        public readonly ?string $parentTag = null
    ) { }
    public function attributes(): array
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public static function withOwnedAttributes(NodeType $node_type, string $tag_name, array $attributes, int $depth, int $index_in_parent, ?string $parent_tag = null, ?bool $is_inline = null): NodeContext
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
    public function intoOwned(): NodeContext
    { throw new \RuntimeException('Not implemented — provided by the native extension.'); }
}

enum TextDirection: string
{
    case LeftToRight = 'ltr';
    case RightToLeft = 'rtl';
    case Auto = 'auto';
}

enum LinkType: string
{
    case Anchor = 'anchor';
    case Internal = 'internal';
    case External = 'external';
    case Email = 'email';
    case Phone = 'phone';
    case Other = 'other';
}

enum ImageType: string
{
    case DataUri = 'data_uri';
    case InlineSvg = 'inline_svg';
    case External = 'external';
    case Relative = 'relative';
}

enum StructuredDataType: string
{
    case JsonLd = 'json_ld';
    case Microdata = 'microdata';
    case RDFa = 'rdfa';
}

enum TierStrategy: string
{
    case Auto = 'Auto';
    case Tier2 = 'Tier2';
    case Tier1 = 'Tier1';
}

enum PreprocessingPreset: string
{
    case Minimal = 'Minimal';
    case Standard = 'Standard';
    case Aggressive = 'Aggressive';
}

enum HeadingStyle: string
{
    case Underlined = 'Underlined';
    case Atx = 'Atx';
    case AtxClosed = 'AtxClosed';
}

enum ListIndentType: string
{
    case Spaces = 'Spaces';
    case Tabs = 'Tabs';
}

enum WhitespaceMode: string
{
    case Normalized = 'Normalized';
    case Strict = 'Strict';
}

enum NewlineStyle: string
{
    case Spaces = 'Spaces';
    case Backslash = 'Backslash';
}

enum CodeBlockStyle: string
{
    case Indented = 'Indented';
    case Backticks = 'Backticks';
    case Tildes = 'Tildes';
}

enum HighlightStyle: string
{
    case DoubleEqual = 'DoubleEqual';
    case Html = 'Html';
    case Bold = 'Bold';
    case None = 'None';
}

enum LinkStyle: string
{
    case Inline = 'Inline';
    case Reference = 'Reference';
}

enum UrlEscapeStyle: string
{
    case Angle = 'Angle';
    case Percent = 'Percent';
}

enum OutputFormat: string
{
    case Markdown = 'Markdown';
    case Djot = 'Djot';
    case Plain = 'Plain';
}

/**
 * The semantic content type of a document node.
 *
 * Uses internally tagged representation (`"node_type": "heading"`) for JSON serialization.
 */
final class NodeContent
{
}

/**
 * The type of an inline text annotation.
 *
 * Uses internally tagged representation (`"annotation_type": "bold"`) for JSON serialization.
 */
final class AnnotationKind
{
}

enum WarningKind: string
{
    case ImageExtractionFailed = 'image_extraction_failed';
    case EncodingFallback = 'encoding_fallback';
    case TruncatedInput = 'truncated_input';
    case MalformedHtml = 'malformed_html';
    case SanitizationApplied = 'sanitization_applied';
    case DepthLimitExceeded = 'depth_limit_exceeded';
}

enum NodeType: string
{
    case Text = 'Text';
    case Element = 'Element';
    case Heading = 'Heading';
    case Paragraph = 'Paragraph';
    case Div = 'Div';
    case Blockquote = 'Blockquote';
    case Pre = 'Pre';
    case Hr = 'Hr';
    case List = 'List';
    case ListItem = 'ListItem';
    case DefinitionList = 'DefinitionList';
    case DefinitionTerm = 'DefinitionTerm';
    case DefinitionDescription = 'DefinitionDescription';
    case Table = 'Table';
    case TableRow = 'TableRow';
    case TableCell = 'TableCell';
    case TableHeader = 'TableHeader';
    case TableBody = 'TableBody';
    case TableHead = 'TableHead';
    case TableFoot = 'TableFoot';
    case Link = 'Link';
    case Image = 'Image';
    case Strong = 'Strong';
    case Em = 'Em';
    case Code = 'Code';
    case Strikethrough = 'Strikethrough';
    case Underline = 'Underline';
    case Subscript = 'Subscript';
    case Superscript = 'Superscript';
    case Mark = 'Mark';
    case Small = 'Small';
    case Br = 'Br';
    case Span = 'Span';
    case Article = 'Article';
    case Section = 'Section';
    case Nav = 'Nav';
    case Aside = 'Aside';
    case Header = 'Header';
    case Footer = 'Footer';
    case Main = 'Main';
    case Figure = 'Figure';
    case Figcaption = 'Figcaption';
    case Time = 'Time';
    case Details = 'Details';
    case Summary = 'Summary';
    case Form = 'Form';
    case Input = 'Input';
    case Select = 'Select';
    case Option = 'Option';
    case Button = 'Button';
    case Textarea = 'Textarea';
    case Label = 'Label';
    case Fieldset = 'Fieldset';
    case Legend = 'Legend';
    case Audio = 'Audio';
    case Video = 'Video';
    case Picture = 'Picture';
    case Source = 'Source';
    case Iframe = 'Iframe';
    case Svg = 'Svg';
    case Canvas = 'Canvas';
    case Ruby = 'Ruby';
    case Rt = 'Rt';
    case Rp = 'Rp';
    case Abbr = 'Abbr';
    case Kbd = 'Kbd';
    case Samp = 'Samp';
    case Var = 'Var';
    case Cite = 'Cite';
    case Q = 'Q';
    case Del = 'Del';
    case Ins = 'Ins';
    case Data = 'Data';
    case Meter = 'Meter';
    case Progress = 'Progress';
    case Output = 'Output';
    case Template = 'Template';
    case Slot = 'Slot';
    case Html = 'Html';
    case Head = 'Head';
    case Body = 'Body';
    case Title = 'Title';
    case Meta = 'Meta';
    case LinkTag = 'LinkTag';
    case Style = 'Style';
    case Script = 'Script';
    case Base = 'Base';
    case Custom = 'Custom';
}

enum VisitResult: string
{
    case Continue = 'Continue';
    case Custom = 'Custom';
    case Skip = 'Skip';
    case PreserveHtml = 'PreserveHtml';
    case Error = 'Error';
}

class HtmlToMarkdownApi
{
    public static function convert(string $html, ?\HtmlToMarkdown\ConversionOptions $options = null): \HtmlToMarkdown\ConversionResult { throw new \RuntimeException('Not implemented.'); }
}

} // end namespace
