# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:973b8763029a78842677f88730fb4b00959ca938b8e209971fa416885d1f5ef1
# To regenerate: alef generate
# To verify freshness: alef verify
# frozen_string_literal: true

require "html_to_markdown"
require "json"

RSpec.describe "conversion" do

  it "adjacent_emphasis_merged: Adjacent emphasis elements share one delimiter run" do
    result = HtmlToMarkdown.convert("<p><i>A</i><i>B</i><i>C</i></p>")
    expect(result.content.to_s).to(eq("*ABC*\n"))
  end

  it "blockquote_code_block_indentation_preserved: Code block inside a blockquote keeps its line indentation" do
    result = HtmlToMarkdown.convert("<blockquote><pre><code>line1\n    line2 indented</code></pre></blockquote>")
    expect(result.content.to_s).to(eq("> ```\n> line1\n>     line2 indented\n> ```\n"))
  end

  it "blockquote_multiple_paragraphs: Blockquote with multiple paragraphs has each paragraph prefixed" do
    result = HtmlToMarkdown.convert("<blockquote><p>First paragraph.</p><p>Second paragraph.</p></blockquote>")
    expect(result.content.to_s).to(include("> First paragraph."))
    expect(result.content.to_s).to(include("> Second paragraph."))
  end

  it "blockquote_nested: Nested blockquote produces double-prefixed lines" do
    result = HtmlToMarkdown.convert(
      "<blockquote><p>Outer quote.</p><blockquote><p>Inner quote.</p></blockquote></blockquote>"
    )
    expect(result.content.respond_to?(:empty?) ? !result.content.empty? : !result.content.nil?).to(be(true))
    expect(result.content.to_s).to(include("Outer quote."))
    expect(result.content.to_s).to(include("Inner quote."))
  end

  it "blockquote_nested_list_indentation_preserved: Nested list inside a blockquote keeps its continuation indentation" do
    result = HtmlToMarkdown.convert("<blockquote><ul><li>item a<ul><li>sub a1</li></ul></li></ul></blockquote>")
    expect(result.content.to_s).to(eq("> - item a\n>   * sub a1\n"))
  end

  it "blockquote_simple: Simple blockquote" do
    result = HtmlToMarkdown.convert("<blockquote><p>Quote text</p></blockquote>")
    expect(result.content.to_s).to(include("> Quote text"))
  end

  it(
    "blockquote_text_then_paragraph_gets_blank_line: Bare text followed by a paragraph inside a blockquote gets a blank-line separator instead of merging into one line"
  ) do
    result = HtmlToMarkdown.convert("<blockquote>Just text, then <p>a paragraph</p></blockquote>")
    expect(result.content.to_s).to(eq("> Just text, then\n>\n> a paragraph\n"))
  end

  it "blockquote_with_list: Blockquote containing a list preserves list items inside quote" do
    result = HtmlToMarkdown.convert(
      "<blockquote><p>Quote intro:</p><ul><li>Point one</li><li>Point two</li></ul></blockquote>"
    )
    expect(result.content.respond_to?(:empty?) ? !result.content.empty? : !result.content.nil?).to(be(true))
    expect(result.content.to_s).to(include("Quote intro:"))
    expect(result.content.to_s).to(include("Point one"))
    expect(result.content.to_s).to(include("Point two"))
  end

  it "bold_and_italic: Nested bold and italic" do
    result = HtmlToMarkdown.convert("<p><strong><em>both</em></strong></p>")
    expect(result.content.to_s).to(include("***both***"))
  end

  it "bold_strong: Strong tag converts to bold" do
    result = HtmlToMarkdown.convert("<p><strong>bold</strong></p>")
    expect(result.content.to_s).to(include("**bold**"))
  end

  it "code_block: Code block with language preserves content" do
    result = HtmlToMarkdown.convert("<pre><code class=\"language-python\">print('hello')</code></pre>")
    expect(result.content.respond_to?(:empty?) ? !result.content.empty? : !result.content.nil?).to(be(true))
    expect(result.content.to_s).to(include("print('hello')"))
  end

  it "code_block_no_language: Code block without a language class preserves content" do
    result = HtmlToMarkdown.convert("<pre><code>plain code here</code></pre>")
    expect(result.content.respond_to?(:empty?) ? !result.content.empty? : !result.content.nil?).to(be(true))
    expect(result.content.to_s).to(include("plain code here"))
  end

  it "code_inline_in_paragraph: Inline code element nested inside a paragraph" do
    result = HtmlToMarkdown.convert("<p>Call the <code>initialize()</code> method first.</p>")
    expect(result.content.to_s).to(include("`initialize()`"))
  end

  it "code_with_backticks_in_content: Inline code containing backtick characters is properly escaped" do
    result = HtmlToMarkdown.convert("<p>Use <code>`backtick` here</code> carefully.</p>")
    expect(result.content.respond_to?(:empty?) ? !result.content.empty? : !result.content.nil?).to(be(true))
    expect(result.content.to_s).to(include("backtick"))
  end

  it(
    "conversion_autolink_filename_not_autolinked: Bare filename href (no URI scheme) renders as inline link, not autolink (#397)"
  ) do
    result = HtmlToMarkdown.convert("<a href=\"foobar.png\">foobar.png</a>")
    expect(result.content.to_s).to(eq("[foobar.png](foobar.png)\n"))
  end

  it "conversion_autolink_https_url: Absolute https URL with matching text renders as autolink (#397)" do
    result = HtmlToMarkdown.convert("<a href=\"https://example.com\">https://example.com</a>")
    expect(result.content.to_s).to(eq("<https://example.com>\n"))
  end

  it "conversion_autolink_mailto: mailto: link with matching email text renders as autolink (#397)" do
    result = HtmlToMarkdown.convert("<a href=\"mailto:a@b.com\">a@b.com</a>")
    expect(result.content.to_s).to(eq("<a@b.com>\n"))
  end

  it "conversion_autolink_mixed_filename_and_url: Mixed filename + URL: only the URL becomes an autolink (#397)" do
    result = HtmlToMarkdown.convert(
      "<a href=\"foobar.png\">foobar.png</a> <a href=\"https://www.heise.de\">https://www.heise.de</a>"
    )
    expect(result.content.to_s).to(eq("[foobar.png](foobar.png) <https://www.heise.de>\n"))
  end

  it "conversion_autolink_relative_path_not_autolinked: Relative path href renders as inline link (#397)" do
    result = HtmlToMarkdown.convert("<a href=\"/docs/intro.html\">/docs/intro.html</a>")
    expect(result.content.to_s).to(eq("[/docs/intro.html](/docs/intro.html)\n"))
  end

  it "emphasis_mark_highlight: mark tag produces highlighted output" do
    result = HtmlToMarkdown.convert("<p><mark>highlighted</mark></p>")
    expect(result.content.respond_to?(:empty?) ? !result.content.empty? : !result.content.nil?).to(be(true))
    expect(result.content.to_s).to(include("highlighted"))
  end

  it "emphasis_strikethrough_del: del tag converts to GFM strikethrough" do
    result = HtmlToMarkdown.convert("<p><del>deleted text</del></p>")
    expect(result.content.to_s).to(include("~~deleted text~~"))
  end

  it "emphasis_strikethrough_s: s tag converts to GFM strikethrough" do
    result = HtmlToMarkdown.convert("<p><s>strikethrough</s></p>")
    expect(result.content.to_s).to(include("~~strikethrough~~"))
  end

  it "emphasis_subscript: sub tag content is preserved" do
    result = HtmlToMarkdown.convert("<p>H<sub>2</sub>O</p>")
    expect(result.content.to_s).to(include("H"))
    expect(result.content.to_s).to(include("2"))
    expect(result.content.to_s).to(include("O"))
  end

  it "emphasis_superscript: sup tag content is preserved" do
    result = HtmlToMarkdown.convert("<p>x<sup>2</sup></p>")
    expect(result.content.to_s).to(include("x"))
    expect(result.content.to_s).to(include("2"))
  end

  it "emphasis_underline_u: u tag content is preserved in output" do
    result = HtmlToMarkdown.convert("<p><u>underlined</u></p>")
    expect(result.content.to_s).to(include("underlined"))
  end

  it "form_input_elements: Form input elements produce readable output without form mechanics" do
    result = HtmlToMarkdown.convert(
      "<form><label for=\"name\">Name:</label><input type=\"text\" id=\"name\" placeholder=\"Enter name\"></form>",
      HtmlToMarkdown::ConversionOptions.new(preprocessing: {"remove_forms" => false})
    )
    expect(result.content.respond_to?(:empty?) ? !result.content.empty? : !result.content.nil?).to(be(true))
    expect(result.content.to_s).to(include("Name"))
  end

  it "form_select_options: Select element with options produces readable output" do
    result = HtmlToMarkdown.convert(
      "<form><label>Color:</label><select><option value=\"red\">Red</option><option value=\"blue\" selected>Blue</option><option value=\"green\">Green</option></select></form>",
      HtmlToMarkdown::ConversionOptions.new(preprocessing: {"remove_forms" => false})
    )
    expect(result.content.respond_to?(:empty?) ? !result.content.empty? : !result.content.nil?).to(be(true))
    expect(result.content.to_s).to(include("Color"))
  end

  it "form_textarea: Textarea element produces readable output" do
    result = HtmlToMarkdown.convert(
      "<form><label>Message:</label><textarea>Default text content</textarea></form>",
      HtmlToMarkdown::ConversionOptions.new(preprocessing: {"remove_forms" => false})
    )
    expect(result.content.respond_to?(:empty?) ? !result.content.empty? : !result.content.nil?).to(be(true))
    expect(result.content.to_s).to(include("Message"))
  end

  it "heading_h1: H1 heading" do
    result = HtmlToMarkdown.convert("<h1>Heading 1</h1>")
    expect(result.content.to_s).to(eq("\# Heading 1\n"))
  end

  it "heading_h2: H2 heading" do
    result = HtmlToMarkdown.convert("<h2>Heading 2</h2>")
    expect(result.content.to_s).to(eq("\#\# Heading 2\n"))
  end

  it "heading_h3: H3 heading" do
    result = HtmlToMarkdown.convert("<h3>Heading 3</h3>")
    expect(result.content.to_s).to(eq("\#\#\# Heading 3\n"))
  end

  it "heading_h4: H4 heading" do
    result = HtmlToMarkdown.convert("<h4>Heading 4</h4>")
    expect(result.content.to_s).to(eq("\#\#\#\# Heading 4\n"))
  end

  it "heading_h5: H5 heading" do
    result = HtmlToMarkdown.convert("<h5>Heading 5</h5>")
    expect(result.content.to_s).to(eq("\#\#\#\#\# Heading 5\n"))
  end

  it "heading_h6: H6 heading" do
    result = HtmlToMarkdown.convert("<h6>Heading 6</h6>")
    expect(result.content.to_s).to(eq("\#\#\#\#\#\# Heading 6\n"))
  end

  it "image_figure_figcaption: Figure with figcaption preserves both image and caption" do
    result = HtmlToMarkdown.convert(
      "<figure><img src=\"sunset.jpg\" alt=\"A sunset\"><figcaption>Beautiful sunset over the ocean</figcaption></figure>"
    )
    expect(result.content.to_s).to(include("![A sunset](sunset.jpg)"))
    expect(result.content.to_s).to(include("Beautiful sunset over the ocean"))
  end

  it "image_linked: Image inside an anchor produces a linked image" do
    result = HtmlToMarkdown.convert("<a href=\"https://example.com\"><img src=\"icon.png\" alt=\"Icon\"></a>")
    expect(result.content.to_s).to(include("![Icon](icon.png)"))
    expect(result.content.to_s).to(include("https://example.com"))
  end

  it "image_no_alt: Image without alt text produces image markdown" do
    result = HtmlToMarkdown.convert("<img src=\"banner.jpg\">")
    expect(result.content.respond_to?(:empty?) ? !result.content.empty? : !result.content.nil?).to(be(true))
    expect(result.content.to_s).to(include("banner.jpg"))
  end

  it "image_simple: Image with alt text" do
    result = HtmlToMarkdown.convert("<img src=\"photo.jpg\" alt=\"A photo\">")
    expect(result.content.to_s).to(include("![A photo](photo.jpg)"))
  end

  it "image_with_title: Image with title attribute includes title in output" do
    result = HtmlToMarkdown.convert("<img src=\"chart.png\" alt=\"Sales chart\" title=\"Q3 Sales\">")
    expect(result.content.to_s).to(include("![Sales chart](chart.png"))
    expect(result.content.to_s).to(include("Q3 Sales"))
  end

  it "inline_code: Inline code" do
    result = HtmlToMarkdown.convert("<p>Use <code>console.log()</code> to debug</p>")
    expect(result.content.to_s).to(include("`console.log()`"))
  end

  it "italic_em: Em tag converts to italic" do
    result = HtmlToMarkdown.convert("<p><em>italic</em></p>")
    expect(result.content.to_s).to(include("*italic*"))
  end

  it "line_break_br_tag: Single br tag produces a line break in output" do
    result = HtmlToMarkdown.convert("<p>First line.<br>Second line.</p>")
    expect(result.content.to_s).to(include("First line."))
    expect(result.content.to_s).to(include("Second line."))
  end

  it "line_break_hr_tag: hr tag produces a horizontal separator between content" do
    result = HtmlToMarkdown.convert("<p>Before rule.</p><hr><p>After rule.</p>")
    expect(result.content.respond_to?(:empty?) ? !result.content.empty? : !result.content.nil?).to(be(true))
    expect(result.content.to_s).to(include("Before rule."))
    expect(result.content.to_s).to(include("After rule."))
  end

  it "line_break_multiple_br: Multiple consecutive br tags in sequence" do
    result = HtmlToMarkdown.convert("<p>Start.<br><br>End.</p>")
    expect(result.content.to_s).to(include("Start."))
    expect(result.content.to_s).to(include("End."))
  end

  it "link_anchor_fragment: Fragment-only anchor link is preserved" do
    result = HtmlToMarkdown.convert("<a href=\"#section\">Jump to section</a>")
    expect(result.content.to_s).to(include("[Jump to section](#section)"))
  end

  it "link_empty_href: Link with empty href produces output with the link text" do
    result = HtmlToMarkdown.convert("<a href=\"\">No destination</a>")
    expect(result.content.to_s).to(include("No destination"))
  end

  it "link_image_inside: Image inside a link produces a linked image" do
    result = HtmlToMarkdown.convert("<a href=\"https://example.com\"><img src=\"logo.png\" alt=\"Logo\"></a>")
    expect(result.content.to_s).to(include("![Logo](logo.png)"))
    expect(result.content.to_s).to(include("https://example.com"))
  end

  it "link_mailto: Mailto link is preserved with mailto: scheme" do
    result = HtmlToMarkdown.convert("<a href=\"mailto:user@example.com\">Email us</a>")
    expect(result.content.to_s).to(include("mailto:user@example.com"))
  end

  it "link_simple: Simple link" do
    result = HtmlToMarkdown.convert("<a href=\"https://example.com\">Example</a>")
    expect(result.content.to_s).to(include("[Example](https://example.com)"))
  end

  it "link_with_bold_text: Link containing bold text preserves formatting" do
    result = HtmlToMarkdown.convert("<a href=\"https://example.com\"><strong>Bold link</strong></a>")
    expect(result.content.to_s).to(include("**Bold link**"))
    expect(result.content.to_s).to(include("https://example.com"))
  end

  it "link_with_title: Link with title attribute" do
    result = HtmlToMarkdown.convert("<a href=\"https://example.com\" title=\"Example Site\">Example</a>")
    expect(result.content.to_s).to(include("[Example](https://example.com"))
    expect(result.content.to_s).to(include("Example Site"))
  end

  it "list_definition_dl: Definition list with dt and dd elements" do
    result = HtmlToMarkdown.convert(
      "<dl><dt>Term One</dt><dd>Definition of term one.</dd><dt>Term Two</dt><dd>Definition of term two.</dd></dl>"
    )
    expect(result.content.to_s).to(include("Term One"))
    expect(result.content.to_s).to(include("Definition of term one."))
    expect(result.content.to_s).to(include("Term Two"))
    expect(result.content.to_s).to(include("Definition of term two."))
  end

  it "list_item_multiple_paragraphs: List item containing multiple paragraphs" do
    result = HtmlToMarkdown.convert(
      "<ul><li><p>First paragraph in item.</p><p>Second paragraph in item.</p></li><li>Simple item</li></ul>"
    )
    expect(result.content.to_s).to(include("First paragraph in item."))
    expect(result.content.to_s).to(include("Second paragraph in item."))
    expect(result.content.to_s).to(include("Simple item"))
  end

  it "list_mixed_nested: Mixed list: ordered list nested inside unordered list" do
    result = HtmlToMarkdown.convert("<ul><li>Item A<ol><li>Sub 1</li><li>Sub 2</li></ol></li><li>Item B</li></ul>")
    expect(result.content.to_s).to(include("Item A"))
    expect(result.content.to_s).to(include("Sub 1"))
    expect(result.content.to_s).to(include("Sub 2"))
    expect(result.content.to_s).to(include("Item B"))
  end

  it "list_nested_ordered: Nested ordered list with two levels of depth" do
    result = HtmlToMarkdown.convert("<ol><li>Step 1<ol><li>Step 1a</li><li>Step 1b</li></ol></li><li>Step 2</li></ol>")
    expect(result.content.to_s).to(include("Step 1"))
    expect(result.content.to_s).to(include("Step 1a"))
    expect(result.content.to_s).to(include("Step 1b"))
    expect(result.content.to_s).to(include("Step 2"))
  end

  it "list_nested_unordered: Nested unordered list with two levels of depth" do
    result = HtmlToMarkdown.convert(
      "<ul><li>Parent A<ul><li>Child A1</li><li>Child A2</li></ul></li><li>Parent B</li></ul>"
    )
    expect(result.content.to_s).to(include("Parent A"))
    expect(result.content.to_s).to(include("Child A1"))
    expect(result.content.to_s).to(include("Child A2"))
    expect(result.content.to_s).to(include("Parent B"))
  end

  it "list_task_checkboxes: Task list with checked and unchecked checkboxes" do
    result = HtmlToMarkdown.convert(
      "<ul><li><input type=\"checkbox\" checked> Done task</li><li><input type=\"checkbox\"> Pending task</li></ul>"
    )
    expect(result.content.respond_to?(:empty?) ? !result.content.empty? : !result.content.nil?).to(be(true))
    expect(result.content.to_s).to(include("Done task"))
    expect(result.content.to_s).to(include("Pending task"))
  end

  it "ordered_list: Ordered list" do
    result = HtmlToMarkdown.convert("<ol><li>First</li><li>Second</li><li>Third</li></ol>")
    expect(result.content.to_s).to(include("1. First"))
    expect(result.content.to_s).to(include("2. Second"))
    expect(result.content.to_s).to(include("3. Third"))
  end

  it "paragraph_multiple: Multiple paragraphs are separated by a blank line" do
    result = HtmlToMarkdown.convert("<p>First paragraph.</p><p>Second paragraph.</p>")
    expect(result.content.to_s).to(include("First paragraph."))
    expect(result.content.to_s).to(include("Second paragraph."))
  end

  it "paragraph_nested_divs: Text nested inside divs is extracted correctly" do
    result = HtmlToMarkdown.convert("<div><div><p>Nested text</p></div></div>")
    expect(result.content.to_s).to(include("Nested text"))
  end

  it "paragraph_simple: Simple paragraph converts to plain text" do
    result = HtmlToMarkdown.convert("<p>Hello World</p>")
    expect(result.content.to_s).to(eq("Hello World\n"))
  end

  it "paragraph_with_inline_formatting: Paragraph with bold, italic, and a link" do
    result = HtmlToMarkdown.convert(
      "<p>This has <strong>bold</strong>, <em>italic</em>, and a <a href=\"https://example.com\">link</a>.</p>"
    )
    expect(result.content.to_s).to(include("**bold**"))
    expect(result.content.to_s).to(include("*italic*"))
    expect(result.content.to_s).to(include("[link](https://example.com)"))
  end

  it "paragraph_with_line_breaks: Paragraph with br tags produces line breaks in output" do
    result = HtmlToMarkdown.convert("<p>Line one.<br>Line two.<br>Line three.</p>")
    expect(result.content.respond_to?(:empty?) ? !result.content.empty? : !result.content.nil?).to(be(true))
    expect(result.content.to_s).to(include("Line one."))
    expect(result.content.to_s).to(include("Line two."))
    expect(result.content.to_s).to(include("Line three."))
  end

  it "semantic_abbr: Abbreviation element text is preserved" do
    result = HtmlToMarkdown.convert("<p>The <abbr title=\"World Wide Web\">WWW</abbr> is global.</p>")
    expect(result.content.to_s).to(include("WWW"))
  end

  it "semantic_article: Article element wrapping content preserves inner content" do
    result = HtmlToMarkdown.convert("<article><h2>Article Title</h2><p>Article body.</p></article>")
    expect(result.content.to_s).to(include("Article Title"))
    expect(result.content.to_s).to(include("Article body."))
  end

  it "semantic_definition_list: Definition list with term and description" do
    result = HtmlToMarkdown.convert(
      "<dl><dt>HTML</dt><dd>HyperText Markup Language</dd><dt>CSS</dt><dd>Cascading Style Sheets</dd></dl>"
    )
    expect(result.content.to_s).to(include("HTML"))
    expect(result.content.to_s).to(include("HyperText Markup Language"))
    expect(result.content.to_s).to(include("CSS"))
    expect(result.content.to_s).to(include("Cascading Style Sheets"))
  end

  it "semantic_details_summary: Details and summary elements produce readable output" do
    result = HtmlToMarkdown.convert("<details><summary>Click to expand</summary><p>Hidden content here.</p></details>")
    expect(result.content.respond_to?(:empty?) ? !result.content.empty? : !result.content.nil?).to(be(true))
    expect(result.content.to_s).to(include("Click to expand"))
  end

  it "semantic_hr: Horizontal rule produces a separator in output" do
    result = HtmlToMarkdown.convert("<p>Above</p><hr><p>Below</p>")
    expect(result.content.respond_to?(:empty?) ? !result.content.empty? : !result.content.nil?).to(be(true))
    expect(result.content.to_s).to(include("Above"))
    expect(result.content.to_s).to(include("Below"))
  end

  it "semantic_mark_highlight: Mark tag produces highlighted output" do
    result = HtmlToMarkdown.convert("<p>This is <mark>highlighted text</mark> in a sentence.</p>")
    expect(result.content.respond_to?(:empty?) ? !result.content.empty? : !result.content.nil?).to(be(true))
    expect(result.content.to_s).to(include("highlighted text"))
  end

  it "semantic_section_with_heading: Section element with heading preserves structure" do
    result = HtmlToMarkdown.convert("<section><h3>Section Heading</h3><p>Section content.</p></section>")
    expect(result.content.to_s).to(include("Section Heading"))
    expect(result.content.to_s).to(include("Section content."))
  end

  it "semantic_sub_superscript: Subscript and superscript elements are preserved in output" do
    result = HtmlToMarkdown.convert("<p>H<sub>2</sub>O and E=mc<sup>2</sup></p>")
    expect(result.content.respond_to?(:empty?) ? !result.content.empty? : !result.content.nil?).to(be(true))
    expect(result.content.to_s).to(include("H"))
    expect(result.content.to_s).to(include("2"))
    expect(result.content.to_s).to(include("O"))
    expect(result.content.to_s).to(include("E=mc"))
  end

  it "simple_table: Simple table with header" do
    result = HtmlToMarkdown.convert(
      "<table><thead><tr><th>Name</th><th>Age</th></tr></thead><tbody><tr><td>Alice</td><td>30</td></tr></tbody></table>"
    )
    expect(result.content.to_s).to(include("Name"))
    expect(result.content.to_s).to(include("Age"))
    expect(result.content.to_s).to(include("Alice"))
    expect(result.content.to_s).to(include("30"))
    expect(result.content.to_s).to(include("|"))
    expect(result.content.to_s).to(include("---"))
  end

  it "table_empty: Empty table produces no output or minimal output" do
    result = HtmlToMarkdown.convert("<table></table>")
    expect(result.content.to_s).to(eq(""))
  end

  it(
    "table_layout_cell_paragraphs_stay_separated: Adjacent paragraphs in a layout-table cell are separated rather than concatenated into one run of text"
  ) do
    result = HtmlToMarkdown.convert(
      "<table><tr><td><p><b>Alice Example</b></p><p><i>Customer Service</i></p></td><td>Logo</td></tr><tr><td>A</td><td>B</td><td>C</td></tr></table>",
      HtmlToMarkdown::ConversionOptions.new(compact_tables: true)
    )
    expect(result.content.to_s).not_to(include("**Alice Example***Customer Service*"))
    expect(result.content.to_s).to(include("Alice Example"))
    expect(result.content.to_s).to(include("Customer Service"))
    expect(result.content.to_s).to(include("Logo"))
  end

  it(
    "table_nested_chain_not_misclassified_as_layout: A straight chain of one-nested-table-per-cell tables renders as pipe tables, not a broken layout list"
  ) do
    result = HtmlToMarkdown.convert(
      "<table><tr><td><table><tr><td><table><tr><td>leaf</td></tr></table></td></tr></table></td></tr></table>"
    )
    expect(result.content.respond_to?(:empty?) ? !result.content.empty? : !result.content.nil?).to(be(true))
    expect(result.content.to_s).to(include("leaf"))
    expect(result.content.to_s).to(include("| ---"))
  end

  it "table_nested_in_single_cell_renders_separately: A table nested in a single-cell row renders as its own table" do
    result = HtmlToMarkdown.convert(
      "<table><tr><th>Summary</th></tr><tr><td>Before<table><tr><th>Item</th><th>Amount</th></tr><tr><td>Alpha</td><td>10</td></tr></table></td></tr></table>"
    )
    expect(result.content.to_s).to(include("| Item  | Amount |"))
    expect(result.content.to_s).to(include("| Alpha | 10     |"))
    expect(result.content.to_s).not_to(include("\\|"))
  end

  it "table_no_thead: Table without thead uses first row as implied header" do
    result = HtmlToMarkdown.convert(
      "<table><tr><td>Product</td><td>Price</td></tr><tr><td>Apple</td><td>1.00</td></tr></table>"
    )
    expect(result.content.respond_to?(:empty?) ? !result.content.empty? : !result.content.nil?).to(be(true))
    expect(result.content.to_s).to(include("Product"))
    expect(result.content.to_s).to(include("Price"))
    expect(result.content.to_s).to(include("Apple"))
    expect(result.content.to_s).to(include("1.00"))
    expect(result.content.to_s).to(include("|"))
  end

  it "table_pipe_chars_in_content: Table cells containing pipe characters are escaped in output" do
    result = HtmlToMarkdown.convert(
      "<table><thead><tr><th>Expression</th><th>Result</th></tr></thead><tbody><tr><td>a | b</td><td>true</td></tr></tbody></table>"
    )
    expect(result.content.respond_to?(:empty?) ? !result.content.empty? : !result.content.nil?).to(be(true))
    expect(result.content.to_s).to(include("Expression"))
    expect(result.content.to_s).to(include("Result"))
    expect(result.content.to_s).to(include("true"))
  end

  it(
    "table_ragged_row_fewer_cells_than_header: A data row with fewer cells than the header is padded to the declared column count"
  ) do
    result = HtmlToMarkdown.convert(
      "<table><tr><th>A</th><th>B</th><th>C</th></tr><tr><td>1</td><td>2</td></tr></table>"
    )
    expect(result.content.to_s).to(eq("| A | B | C |\n| --- | --- | --- |\n| 1 | 2 |   |\n"))
  end

  it(
    "table_ragged_row_more_cells_than_header: A data row with more cells than the header widens the separator so no cell is silently dropped"
  ) do
    result = HtmlToMarkdown.convert(
      "<table><tr><th>A</th><th>B</th></tr><tr><td>1</td><td>2</td><td>3</td></tr></table>"
    )
    expect(result.content.to_s).to(eq("| A | B |   |\n| --- | --- | --- |\n| 1 | 2 | 3 |\n"))
  end

  it "table_with_alignment: Table with column alignment attributes" do
    result = HtmlToMarkdown.convert(
      "<table><thead><tr><th align=\"left\">Left</th><th align=\"center\">Center</th><th align=\"right\">Right</th></tr></thead><tbody><tr><td>L</td><td>C</td><td>R</td></tr></tbody></table>"
    )
    expect(result.content.respond_to?(:empty?) ? !result.content.empty? : !result.content.nil?).to(be(true))
    expect(result.content.to_s).to(include("Left"))
    expect(result.content.to_s).to(include("Center"))
    expect(result.content.to_s).to(include("Right"))
    expect(result.content.to_s).to(include("L"))
    expect(result.content.to_s).to(include("C"))
    expect(result.content.to_s).to(include("R"))
    expect(result.content.to_s).to(include("|"))
  end

  it "table_with_colspan: Table with colspan attribute in a header cell" do
    result = HtmlToMarkdown.convert(
      "<table><thead><tr><th colspan=\"2\">Full Name</th></tr></thead><tbody><tr><td>John</td><td>Doe</td></tr></tbody></table>"
    )
    expect(result.content.respond_to?(:empty?) ? !result.content.empty? : !result.content.nil?).to(be(true))
    expect(result.content.to_s).to(include("Full Name"))
    expect(result.content.to_s).to(include("John"))
    expect(result.content.to_s).to(include("Doe"))
  end

  it "tagged_space_not_duplicated: A whitespace-only inline element contributes exactly one space" do
    result = HtmlToMarkdown.convert("<p>A<i> </i>B</p>")
    expect(result.content.to_s).to(eq("A B\n"))
  end

  it "unordered_list: Unordered list" do
    result = HtmlToMarkdown.convert("<ul><li>Item 1</li><li>Item 2</li><li>Item 3</li></ul>")
    expect(result.content.to_s).to(include("- Item 1"))
    expect(result.content.to_s).to(include("- Item 2"))
    expect(result.content.to_s).to(include("- Item 3"))
  end
end
