// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8b3e3f77d8a143057014a8513b0c79f2580124b20b47a68e97fe5dc86baac328
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

using System;
using System.Collections.Generic;
using System.Linq;
using System.Net.Http;
using System.Text;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading.Tasks;
using Xunit;
using HtmlToMarkdown;
using static HtmlToMarkdown.HtmlToMarkdownRs;

namespace HtmlToMarkdown
{
    /// <summary>E2e tests for category: conversion.</summary>
    public class ConversionTests
    {
            private static readonly JsonSerializerOptions ConfigOptions = new() { Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) }, DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault };

        [Fact]
        public void Test_BlockquoteMultipleParagraphs()
        {
            // Blockquote with multiple paragraphs has each paragraph prefixed
            var result = HtmlToMarkdownRs.Convert("<blockquote><p>First paragraph.</p><p>Second paragraph.</p></blockquote>", null);
    Assert.Contains("> first paragraph.", result.Content.ToString().ToLower());
    Assert.Contains("> second paragraph.", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_BlockquoteNested()
        {
            // Nested blockquote produces double-prefixed lines
            var result = HtmlToMarkdownRs.Convert("<blockquote><p>Outer quote.</p><blockquote><p>Inner quote.</p></blockquote></blockquote>", null);
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.Contains("outer quote.", result.Content.ToString().ToLower());
    Assert.Contains("inner quote.", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_BlockquoteSimple()
        {
            // Simple blockquote
            var result = HtmlToMarkdownRs.Convert("<blockquote><p>Quote text</p></blockquote>", null);
    Assert.Contains("> quote text", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_BlockquoteWithList()
        {
            // Blockquote containing a list preserves list items inside quote
            var result = HtmlToMarkdownRs.Convert("<blockquote><p>Quote intro:</p><ul><li>Point one</li><li>Point two</li></ul></blockquote>", null);
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.Contains("quote intro:", result.Content.ToString().ToLower());
    Assert.Contains("point one", result.Content.ToString().ToLower());
    Assert.Contains("point two", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_BoldAndItalic()
        {
            // Nested bold and italic
            var result = HtmlToMarkdownRs.Convert("<p><strong><em>both</em></strong></p>", null);
    Assert.Contains("***both***", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_BoldStrong()
        {
            // Strong tag converts to bold
            var result = HtmlToMarkdownRs.Convert("<p><strong>bold</strong></p>", null);
    Assert.Contains("**bold**", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_CodeBlock()
        {
            // Code block with language preserves content
            var result = HtmlToMarkdownRs.Convert("<pre><code class=\"language-python\">print('hello')</code></pre>", null);
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.Contains("print('hello')", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_CodeBlockNoLanguage()
        {
            // Code block without a language class preserves content
            var result = HtmlToMarkdownRs.Convert("<pre><code>plain code here</code></pre>", null);
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.Contains("plain code here", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_CodeInlineInParagraph()
        {
            // Inline code element nested inside a paragraph
            var result = HtmlToMarkdownRs.Convert("<p>Call the <code>initialize()</code> method first.</p>", null);
    Assert.Contains("`initialize()`", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_CodeWithBackticksInContent()
        {
            // Inline code containing backtick characters is properly escaped
            var result = HtmlToMarkdownRs.Convert("<p>Use <code>`backtick` here</code> carefully.</p>", null);
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.Contains("backtick", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_ConversionAutolinkFilenameNotAutolinked()
        {
            // Bare filename href (no URI scheme) renders as inline link, not autolink (#397)
            var result = HtmlToMarkdownRs.Convert("<a href=\"foobar.png\">foobar.png</a>", null);
    Assert.Equal("[foobar.png](foobar.png)", result.Content!.Trim());

        }

        [Fact]
        public void Test_ConversionAutolinkHttpsUrl()
        {
            // Absolute https URL with matching text renders as autolink (#397)
            var result = HtmlToMarkdownRs.Convert("<a href=\"https://example.com\">https://example.com</a>", null);
    Assert.Equal("<https://example.com>", result.Content!.Trim());

        }

        [Fact]
        public void Test_ConversionAutolinkMailto()
        {
            // mailto: link with matching email text renders as autolink (#397)
            var result = HtmlToMarkdownRs.Convert("<a href=\"mailto:a@b.com\">a@b.com</a>", null);
    Assert.Equal("<a@b.com>", result.Content!.Trim());

        }

        [Fact]
        public void Test_ConversionAutolinkMixedFilenameAndUrl()
        {
            // Mixed filename + URL: only the URL becomes an autolink (#397)
            var result = HtmlToMarkdownRs.Convert("<a href=\"foobar.png\">foobar.png</a> <a href=\"https://www.heise.de\">https://www.heise.de</a>", null);
    Assert.Equal("[foobar.png](foobar.png) <https://www.heise.de>", result.Content!.Trim());

        }

        [Fact]
        public void Test_ConversionAutolinkRelativePathNotAutolinked()
        {
            // Relative path href renders as inline link (#397)
            var result = HtmlToMarkdownRs.Convert("<a href=\"/docs/intro.html\">/docs/intro.html</a>", null);
    Assert.Equal("[/docs/intro.html](/docs/intro.html)", result.Content!.Trim());

        }

        [Fact]
        public void Test_EmphasisMarkHighlight()
        {
            // mark tag produces highlighted output
            var result = HtmlToMarkdownRs.Convert("<p><mark>highlighted</mark></p>", null);
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.Contains("highlighted", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_EmphasisStrikethroughDel()
        {
            // del tag converts to GFM strikethrough
            var result = HtmlToMarkdownRs.Convert("<p><del>deleted text</del></p>", null);
    Assert.Contains("~~deleted text~~", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_EmphasisStrikethroughS()
        {
            // s tag converts to GFM strikethrough
            var result = HtmlToMarkdownRs.Convert("<p><s>strikethrough</s></p>", null);
    Assert.Contains("~~strikethrough~~", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_EmphasisSubscript()
        {
            // sub tag content is preserved
            var result = HtmlToMarkdownRs.Convert("<p>H<sub>2</sub>O</p>", null);
    Assert.Contains("h", result.Content.ToString().ToLower());
    Assert.Contains("2", result.Content.ToString().ToLower());
    Assert.Contains("o", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_EmphasisSuperscript()
        {
            // sup tag content is preserved
            var result = HtmlToMarkdownRs.Convert("<p>x<sup>2</sup></p>", null);
    Assert.Contains("x", result.Content.ToString().ToLower());
    Assert.Contains("2", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_EmphasisUnderlineU()
        {
            // u tag content is preserved in output
            var result = HtmlToMarkdownRs.Convert("<p><u>underlined</u></p>", null);
    Assert.Contains("underlined", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_FormInputElements()
        {
            // Form input elements produce readable output without form mechanics
            var result = HtmlToMarkdownRs.Convert("<form><label for=\"name\">Name:</label><input type=\"text\" id=\"name\" placeholder=\"Enter name\"></form>", new ConversionOptions { Preprocessing = JsonSerializer.Deserialize<PreprocessingOptions>("{\"remove_forms\":false}", ConfigOptions)! });
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.Contains("name", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_FormSelectOptions()
        {
            // Select element with options produces readable output
            var result = HtmlToMarkdownRs.Convert("<form><label>Color:</label><select><option value=\"red\">Red</option><option value=\"blue\" selected>Blue</option><option value=\"green\">Green</option></select></form>", new ConversionOptions { Preprocessing = JsonSerializer.Deserialize<PreprocessingOptions>("{\"remove_forms\":false}", ConfigOptions)! });
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.Contains("color", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_FormTextarea()
        {
            // Textarea element produces readable output
            var result = HtmlToMarkdownRs.Convert("<form><label>Message:</label><textarea>Default text content</textarea></form>", new ConversionOptions { Preprocessing = JsonSerializer.Deserialize<PreprocessingOptions>("{\"remove_forms\":false}", ConfigOptions)! });
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.Contains("message", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_HeadingH1()
        {
            // H1 heading
            var result = HtmlToMarkdownRs.Convert("<h1>Heading 1</h1>", null);
    Assert.Equal("# Heading 1", result.Content!.Trim());

        }

        [Fact]
        public void Test_HeadingH2()
        {
            // H2 heading
            var result = HtmlToMarkdownRs.Convert("<h2>Heading 2</h2>", null);
    Assert.Equal("## Heading 2", result.Content!.Trim());

        }

        [Fact]
        public void Test_HeadingH3()
        {
            // H3 heading
            var result = HtmlToMarkdownRs.Convert("<h3>Heading 3</h3>", null);
    Assert.Equal("### Heading 3", result.Content!.Trim());

        }

        [Fact]
        public void Test_HeadingH4()
        {
            // H4 heading
            var result = HtmlToMarkdownRs.Convert("<h4>Heading 4</h4>", null);
    Assert.Equal("#### Heading 4", result.Content!.Trim());

        }

        [Fact]
        public void Test_HeadingH5()
        {
            // H5 heading
            var result = HtmlToMarkdownRs.Convert("<h5>Heading 5</h5>", null);
    Assert.Equal("##### Heading 5", result.Content!.Trim());

        }

        [Fact]
        public void Test_HeadingH6()
        {
            // H6 heading
            var result = HtmlToMarkdownRs.Convert("<h6>Heading 6</h6>", null);
    Assert.Equal("###### Heading 6", result.Content!.Trim());

        }

        [Fact]
        public void Test_ImageFigureFigcaption()
        {
            // Figure with figcaption preserves both image and caption
            var result = HtmlToMarkdownRs.Convert("<figure><img src=\"sunset.jpg\" alt=\"A sunset\"><figcaption>Beautiful sunset over the ocean</figcaption></figure>", null);
    Assert.Contains("![a sunset](sunset.jpg)", result.Content.ToString().ToLower());
    Assert.Contains("beautiful sunset over the ocean", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_ImageLinked()
        {
            // Image inside an anchor produces a linked image
            var result = HtmlToMarkdownRs.Convert("<a href=\"https://example.com\"><img src=\"icon.png\" alt=\"Icon\"></a>", null);
    Assert.Contains("![icon](icon.png)", result.Content.ToString().ToLower());
    Assert.Contains("https://example.com", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_ImageNoAlt()
        {
            // Image without alt text produces image markdown
            var result = HtmlToMarkdownRs.Convert("<img src=\"banner.jpg\">", null);
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.Contains("banner.jpg", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_ImageSimple()
        {
            // Image with alt text
            var result = HtmlToMarkdownRs.Convert("<img src=\"photo.jpg\" alt=\"A photo\">", null);
    Assert.Contains("![a photo](photo.jpg)", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_ImageWithTitle()
        {
            // Image with title attribute includes title in output
            var result = HtmlToMarkdownRs.Convert("<img src=\"chart.png\" alt=\"Sales chart\" title=\"Q3 Sales\">", null);
    Assert.Contains("![sales chart](chart.png", result.Content.ToString().ToLower());
    Assert.Contains("q3 sales", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_InlineCode()
        {
            // Inline code
            var result = HtmlToMarkdownRs.Convert("<p>Use <code>console.log()</code> to debug</p>", null);
    Assert.Contains("`console.log()`", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_ItalicEm()
        {
            // Em tag converts to italic
            var result = HtmlToMarkdownRs.Convert("<p><em>italic</em></p>", null);
    Assert.Contains("*italic*", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_LineBreakBrTag()
        {
            // Single br tag produces a line break in output
            var result = HtmlToMarkdownRs.Convert("<p>First line.<br>Second line.</p>", null);
    Assert.Contains("first line.", result.Content.ToString().ToLower());
    Assert.Contains("second line.", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_LineBreakHrTag()
        {
            // hr tag produces a horizontal separator between content
            var result = HtmlToMarkdownRs.Convert("<p>Before rule.</p><hr><p>After rule.</p>", null);
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.Contains("before rule.", result.Content.ToString().ToLower());
    Assert.Contains("after rule.", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_LineBreakMultipleBr()
        {
            // Multiple consecutive br tags in sequence
            var result = HtmlToMarkdownRs.Convert("<p>Start.<br><br>End.</p>", null);
    Assert.Contains("start.", result.Content.ToString().ToLower());
    Assert.Contains("end.", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_LinkAnchorFragment()
        {
            // Fragment-only anchor link is preserved
            var result = HtmlToMarkdownRs.Convert("<a href=\"#section\">Jump to section</a>", null);
    Assert.Contains("[jump to section](#section)", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_LinkEmptyHref()
        {
            // Link with empty href produces output with the link text
            var result = HtmlToMarkdownRs.Convert("<a href=\"\">No destination</a>", null);
    Assert.Contains("no destination", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_LinkImageInside()
        {
            // Image inside a link produces a linked image
            var result = HtmlToMarkdownRs.Convert("<a href=\"https://example.com\"><img src=\"logo.png\" alt=\"Logo\"></a>", null);
    Assert.Contains("![logo](logo.png)", result.Content.ToString().ToLower());
    Assert.Contains("https://example.com", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_LinkMailto()
        {
            // Mailto link is preserved with mailto: scheme
            var result = HtmlToMarkdownRs.Convert("<a href=\"mailto:user@example.com\">Email us</a>", null);
    Assert.Contains("mailto:user@example.com", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_LinkSimple()
        {
            // Simple link
            var result = HtmlToMarkdownRs.Convert("<a href=\"https://example.com\">Example</a>", null);
    Assert.Contains("[example](https://example.com)", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_LinkWithBoldText()
        {
            // Link containing bold text preserves formatting
            var result = HtmlToMarkdownRs.Convert("<a href=\"https://example.com\"><strong>Bold link</strong></a>", null);
    Assert.Contains("**bold link**", result.Content.ToString().ToLower());
    Assert.Contains("https://example.com", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_LinkWithTitle()
        {
            // Link with title attribute
            var result = HtmlToMarkdownRs.Convert("<a href=\"https://example.com\" title=\"Example Site\">Example</a>", null);
    Assert.Contains("[example](https://example.com", result.Content.ToString().ToLower());
    Assert.Contains("example site", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_ListDefinitionDl()
        {
            // Definition list with dt and dd elements
            var result = HtmlToMarkdownRs.Convert("<dl><dt>Term One</dt><dd>Definition of term one.</dd><dt>Term Two</dt><dd>Definition of term two.</dd></dl>", null);
    Assert.Contains("term one", result.Content.ToString().ToLower());
    Assert.Contains("definition of term one.", result.Content.ToString().ToLower());
    Assert.Contains("term two", result.Content.ToString().ToLower());
    Assert.Contains("definition of term two.", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_ListItemMultipleParagraphs()
        {
            // List item containing multiple paragraphs
            var result = HtmlToMarkdownRs.Convert("<ul><li><p>First paragraph in item.</p><p>Second paragraph in item.</p></li><li>Simple item</li></ul>", null);
    Assert.Contains("first paragraph in item.", result.Content.ToString().ToLower());
    Assert.Contains("second paragraph in item.", result.Content.ToString().ToLower());
    Assert.Contains("simple item", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_ListMixedNested()
        {
            // Mixed list: ordered list nested inside unordered list
            var result = HtmlToMarkdownRs.Convert("<ul><li>Item A<ol><li>Sub 1</li><li>Sub 2</li></ol></li><li>Item B</li></ul>", null);
    Assert.Contains("item a", result.Content.ToString().ToLower());
    Assert.Contains("sub 1", result.Content.ToString().ToLower());
    Assert.Contains("sub 2", result.Content.ToString().ToLower());
    Assert.Contains("item b", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_ListNestedOrdered()
        {
            // Nested ordered list with two levels of depth
            var result = HtmlToMarkdownRs.Convert("<ol><li>Step 1<ol><li>Step 1a</li><li>Step 1b</li></ol></li><li>Step 2</li></ol>", null);
    Assert.Contains("step 1", result.Content.ToString().ToLower());
    Assert.Contains("step 1a", result.Content.ToString().ToLower());
    Assert.Contains("step 1b", result.Content.ToString().ToLower());
    Assert.Contains("step 2", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_ListNestedUnordered()
        {
            // Nested unordered list with two levels of depth
            var result = HtmlToMarkdownRs.Convert("<ul><li>Parent A<ul><li>Child A1</li><li>Child A2</li></ul></li><li>Parent B</li></ul>", null);
    Assert.Contains("parent a", result.Content.ToString().ToLower());
    Assert.Contains("child a1", result.Content.ToString().ToLower());
    Assert.Contains("child a2", result.Content.ToString().ToLower());
    Assert.Contains("parent b", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_ListTaskCheckboxes()
        {
            // Task list with checked and unchecked checkboxes
            var result = HtmlToMarkdownRs.Convert("<ul><li><input type=\"checkbox\" checked> Done task</li><li><input type=\"checkbox\"> Pending task</li></ul>", null);
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.Contains("done task", result.Content.ToString().ToLower());
    Assert.Contains("pending task", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_OrderedList()
        {
            // Ordered list
            var result = HtmlToMarkdownRs.Convert("<ol><li>First</li><li>Second</li><li>Third</li></ol>", null);
    Assert.Contains("1. first", result.Content.ToString().ToLower());
    Assert.Contains("2. second", result.Content.ToString().ToLower());
    Assert.Contains("3. third", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_ParagraphMultiple()
        {
            // Multiple paragraphs are separated by a blank line
            var result = HtmlToMarkdownRs.Convert("<p>First paragraph.</p><p>Second paragraph.</p>", null);
    Assert.Contains("first paragraph.", result.Content.ToString().ToLower());
    Assert.Contains("second paragraph.", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_ParagraphNestedDivs()
        {
            // Text nested inside divs is extracted correctly
            var result = HtmlToMarkdownRs.Convert("<div><div><p>Nested text</p></div></div>", null);
    Assert.Contains("nested text", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_ParagraphSimple()
        {
            // Simple paragraph converts to plain text
            var result = HtmlToMarkdownRs.Convert("<p>Hello World</p>", null);
    Assert.Equal("Hello World", result.Content!.Trim());

        }

        [Fact]
        public void Test_ParagraphWithInlineFormatting()
        {
            // Paragraph with bold, italic, and a link
            var result = HtmlToMarkdownRs.Convert("<p>This has <strong>bold</strong>, <em>italic</em>, and a <a href=\"https://example.com\">link</a>.</p>", null);
    Assert.Contains("**bold**", result.Content.ToString().ToLower());
    Assert.Contains("*italic*", result.Content.ToString().ToLower());
    Assert.Contains("[link](https://example.com)", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_ParagraphWithLineBreaks()
        {
            // Paragraph with br tags produces line breaks in output
            var result = HtmlToMarkdownRs.Convert("<p>Line one.<br>Line two.<br>Line three.</p>", null);
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.Contains("line one.", result.Content.ToString().ToLower());
    Assert.Contains("line two.", result.Content.ToString().ToLower());
    Assert.Contains("line three.", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_SemanticAbbr()
        {
            // Abbreviation element text is preserved
            var result = HtmlToMarkdownRs.Convert("<p>The <abbr title=\"World Wide Web\">WWW</abbr> is global.</p>", null);
    Assert.Contains("www", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_SemanticArticle()
        {
            // Article element wrapping content preserves inner content
            var result = HtmlToMarkdownRs.Convert("<article><h2>Article Title</h2><p>Article body.</p></article>", null);
    Assert.Contains("article title", result.Content.ToString().ToLower());
    Assert.Contains("article body.", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_SemanticDefinitionList()
        {
            // Definition list with term and description
            var result = HtmlToMarkdownRs.Convert("<dl><dt>HTML</dt><dd>HyperText Markup Language</dd><dt>CSS</dt><dd>Cascading Style Sheets</dd></dl>", null);
    Assert.Contains("html", result.Content.ToString().ToLower());
    Assert.Contains("hypertext markup language", result.Content.ToString().ToLower());
    Assert.Contains("css", result.Content.ToString().ToLower());
    Assert.Contains("cascading style sheets", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_SemanticDetailsSummary()
        {
            // Details and summary elements produce readable output
            var result = HtmlToMarkdownRs.Convert("<details><summary>Click to expand</summary><p>Hidden content here.</p></details>", null);
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.Contains("click to expand", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_SemanticHr()
        {
            // Horizontal rule produces a separator in output
            var result = HtmlToMarkdownRs.Convert("<p>Above</p><hr><p>Below</p>", null);
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.Contains("above", result.Content.ToString().ToLower());
    Assert.Contains("below", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_SemanticMarkHighlight()
        {
            // Mark tag produces highlighted output
            var result = HtmlToMarkdownRs.Convert("<p>This is <mark>highlighted text</mark> in a sentence.</p>", null);
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.Contains("highlighted text", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_SemanticSectionWithHeading()
        {
            // Section element with heading preserves structure
            var result = HtmlToMarkdownRs.Convert("<section><h3>Section Heading</h3><p>Section content.</p></section>", null);
    Assert.Contains("section heading", result.Content.ToString().ToLower());
    Assert.Contains("section content.", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_SemanticSubSuperscript()
        {
            // Subscript and superscript elements are preserved in output
            var result = HtmlToMarkdownRs.Convert("<p>H<sub>2</sub>O and E=mc<sup>2</sup></p>", null);
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.Contains("h", result.Content.ToString().ToLower());
    Assert.Contains("2", result.Content.ToString().ToLower());
    Assert.Contains("o", result.Content.ToString().ToLower());
    Assert.Contains("e=mc", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_SimpleTable()
        {
            // Simple table with header
            var result = HtmlToMarkdownRs.Convert("<table><thead><tr><th>Name</th><th>Age</th></tr></thead><tbody><tr><td>Alice</td><td>30</td></tr></tbody></table>", null);
    Assert.Contains("name", result.Content.ToString().ToLower());
    Assert.Contains("age", result.Content.ToString().ToLower());
    Assert.Contains("alice", result.Content.ToString().ToLower());
    Assert.Contains("30", result.Content.ToString().ToLower());
    Assert.Contains("|", result.Content.ToString().ToLower());
    Assert.Contains("---", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_TableEmpty()
        {
            // Empty table produces no output or minimal output
            var result = HtmlToMarkdownRs.Convert("<table></table>", null);
    Assert.Equal("", result.Content!.Trim());

        }

        [Fact]
        public void Test_TableNoThead()
        {
            // Table without thead uses first row as implied header
            var result = HtmlToMarkdownRs.Convert("<table><tr><td>Product</td><td>Price</td></tr><tr><td>Apple</td><td>1.00</td></tr></table>", null);
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.Contains("product", result.Content.ToString().ToLower());
    Assert.Contains("price", result.Content.ToString().ToLower());
    Assert.Contains("apple", result.Content.ToString().ToLower());
    Assert.Contains("1.00", result.Content.ToString().ToLower());
    Assert.Contains("|", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_TablePipeCharsInContent()
        {
            // Table cells containing pipe characters are escaped in output
            var result = HtmlToMarkdownRs.Convert("<table><thead><tr><th>Expression</th><th>Result</th></tr></thead><tbody><tr><td>a | b</td><td>true</td></tr></tbody></table>", null);
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.Contains("expression", result.Content.ToString().ToLower());
    Assert.Contains("result", result.Content.ToString().ToLower());
    Assert.Contains("true", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_TableWithAlignment()
        {
            // Table with column alignment attributes
            var result = HtmlToMarkdownRs.Convert("<table><thead><tr><th align=\"left\">Left</th><th align=\"center\">Center</th><th align=\"right\">Right</th></tr></thead><tbody><tr><td>L</td><td>C</td><td>R</td></tr></tbody></table>", null);
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.Contains("left", result.Content.ToString().ToLower());
    Assert.Contains("center", result.Content.ToString().ToLower());
    Assert.Contains("right", result.Content.ToString().ToLower());
    Assert.Contains("l", result.Content.ToString().ToLower());
    Assert.Contains("c", result.Content.ToString().ToLower());
    Assert.Contains("r", result.Content.ToString().ToLower());
    Assert.Contains("|", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_TableWithColspan()
        {
            // Table with colspan attribute in a header cell
            var result = HtmlToMarkdownRs.Convert("<table><thead><tr><th colspan=\"2\">Full Name</th></tr></thead><tbody><tr><td>John</td><td>Doe</td></tr></tbody></table>", null);
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.Contains("full name", result.Content.ToString().ToLower());
    Assert.Contains("john", result.Content.ToString().ToLower());
    Assert.Contains("doe", result.Content.ToString().ToLower());

        }

        [Fact]
        public void Test_UnorderedList()
        {
            // Unordered list
            var result = HtmlToMarkdownRs.Convert("<ul><li>Item 1</li><li>Item 2</li><li>Item 3</li></ul>", null);
    Assert.Contains("- item 1", result.Content.ToString().ToLower());
    Assert.Contains("- item 2", result.Content.ToString().ToLower());
    Assert.Contains("- item 3", result.Content.ToString().ToLower());

        }


    }
}
