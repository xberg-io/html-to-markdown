/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:dc3006841dc241bc0f92951e9527885f22ec3208a95d4d84f8d55a051c2148f6
 * To regenerate: alef e2e generate
 * To verify freshness: alef verify
 */
/* E2e tests for category: smoke */

#include "html_to_markdown.h"
#include "test_runner.h"
#include <assert.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void test_smoke_empty_string(void) {
  /* Empty string produces empty output */
  HTMAlefHandle result = htm_convert("", 0);
  assert(result != 0 && "expected call to succeed");
  char *content = htm_conversion_result_content(result);
  assert(content != NULL && strcmp(content, "") == 0 &&
         "equals assertion failed");
  htm_free_string(content);
  htm_conversion_result_free(result);
}

void test_smoke_simple_heading(void) {
  /* H1 heading converts to ATX markdown */
  HTMAlefHandle result = htm_convert("<h1>Title</h1>", 0);
  assert(result != 0 && "expected call to succeed");
  char *content = htm_conversion_result_content(result);
  assert(content != NULL && strstr(content, "# Title") != NULL &&
         "expected to contain substring");
  htm_free_string(content);
  htm_conversion_result_free(result);
}

void test_smoke_simple_paragraph(void) {
  /* Simple paragraph converts correctly */
  HTMAlefHandle result = htm_convert("<p>Hello World</p>", 0);
  assert(result != 0 && "expected call to succeed");
  char *content = htm_conversion_result_content(result);
  assert(content != NULL && strcmp(content, "Hello World\n") == 0 &&
         "equals assertion failed");
  assert(content != NULL && strlen(content) > 0 && strcmp(content, "[]") != 0 &&
         strcmp(content, "{}") != 0 && "expected non-empty value");
  htm_free_string(content);
  htm_conversion_result_free(result);
}
