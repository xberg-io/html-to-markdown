# Generated by alef. Do not edit by hand.

[package]
name = "html-to-markdown-rs-jni"
version.workspace = true
edition.workspace = true
license.workspace = true

# Deps below are emitted unconditionally by the alef JNI scaffold for
# manifest stability across regens. h2m's umbrella crate has no streaming
# adapters / async fns / JSON-marshalled return types today, so
# `futures-util`, `serde_json`, and `tokio` are unused. Silence
# cargo-machete rather than flapping on every prek run.
[package.metadata.cargo-machete]
ignored = ["futures-util", "serde_json", "tokio"]

[lib]
name = "html_to_markdown_rs_jni"
crate-type = ["cdylib"]

[dependencies]
futures-util = "0.3"
html-to-markdown-rs = { path = "../html-to-markdown", features = [
    "full",
    "metadata",
    "visitor",
    "serde",
    "inline-images",
] }
jni = "0.22"
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "sync"] }
