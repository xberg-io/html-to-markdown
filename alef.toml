[workspace]
alef_version = "0.85.15"
languages = [
  "python",
  "node",
  "ruby",
  "php",
  "ffi",
  "go",
  "java",
  "csharp",
  "elixir",
  "wasm",
  "r",
  "kotlin_android",
  "jni",
  "swift",
  "dart",
  "zig",
]

# ~keep Consumer manifests contain maintained toolchain/dependency settings and JNI tasks.
# Their release dependency pins are updated by scripts/sync_consumer_versions.py.
[workspace.ownership]
user_owned = [
  "test_apps/dart/pubspec.yaml",
  "test_apps/elixir/mix.exs",
  "test_apps/kotlin_android/build.gradle.kts",
  "test_apps/zig/build.zig",
]

[workspace.docs]
reference_output = "docs-site/src/content/docs/reference"

[workspace.docs.cli]
sources = ["crates/html-to-markdown-cli/src/args.rs"]

[workspace.docs.mcp]
sources = ["crates/html-to-markdown/src/mcp/server.rs"]


[workspace.docs.skills]
template_dir = "templates/docs/skills"
outputs = [".codex/skills", ".agents/skills", ".github/skills", ".claude/skills"]

[workspace.docs.snippets]
# `generated/` is the fixture-driven regression corpus (alef-owned, one topic per fixture
# category). The per-language roots below are the hand-written narrative snippets used directly
# by usage.mdx / errors.mdx / tables.mdx / visitor.mdx (getting-started, error-handling,
# table-extraction, metadata "basic_extraction", visitor "basic_visitor"). They demonstrate the
# public field-access idiom (`result.content` / `result.tables` / `result.metadata`) that the
# fixture-driven snippets don't show, so they aren't dropped in favor of a generated equivalent
# -- but until now they sat entirely outside `dirs`, so nothing ever validated they still compile.
# Listing each language root (not a glob) picks up only the hand-written topic dirs; it does not
# overlap `generated/<lang>/...`, which lives one level deeper under its own subtree. ~keep
dirs = [
  "docs-site/src/snippets/generated",
  "docs-site/src/snippets/c",
  "docs-site/src/snippets/csharp",
  "docs-site/src/snippets/dart",
  "docs-site/src/snippets/elixir",
  "docs-site/src/snippets/go",
  "docs-site/src/snippets/java",
  "docs-site/src/snippets/kotlin_android",
  "docs-site/src/snippets/php",
  "docs-site/src/snippets/python",
  "docs-site/src/snippets/r",
  "docs-site/src/snippets/ruby",
  "docs-site/src/snippets/rust",
  "docs-site/src/snippets/swift",
  "docs-site/src/snippets/typescript",
  "docs-site/src/snippets/wasm",
  "docs-site/src/snippets/zig",
]
# The gap pass (unreferenced snippets, missing language variants) is skipped entirely unless at
# least one of `docs_dirs`/`required_languages` is set, and a `--strict` run refuses to pass on a
# check that compared nothing. These two mirror the flags `task docs:snippets:gaps` already
# passes, so `alef snippets check --strict` runs the same comparison CI does instead of erroring.
# kotlin is absent from the required set: alef.toml gives kotlin_android
# exclude_functions = ["visitor"], because its trait-bridge emitter cannot bind via an options
# field. The binding therefore has no visitor API and alef stops emitting the 49 visitor and 5
# visitor edge-case snippets for it, which requiring kotlin here would report as 54 permanent
# gaps. ~keep
docs_dirs = ["docs-site/src/content/docs"]
required_languages = [
  "rust",
  "python",
  "typescript",
  "go",
  "ruby",
  "php",
  "java",
  "csharp",
  "elixir",
  "r",
  "c",
  "swift",
  "dart",
  "zig",
]
validation_level = "compile"
# `timeout_secs` bounds both snippet validation and every session's `before` hook (see
# `DocsSnippetsConfig::timeout_secs` in alef's `src/core/config/output.rs`). 120s is enough for
# `syntax`/`typecheck` snippets but not for a cold `before` build: kotlin_android's
# `assembleDebug`, wasm's `pnpm run build:all`, and swift's `swift build` can each take several
# minutes on a fresh checkout, so those sessions' snippets were reclassified `unresolved_dependency`
# (reported as `Unavailable`) and failed CI under `--strict` even though every snippet itself was
# fine. 900s matches the precedent set in tree-sitter-language-pack's alef.toml for the same
# multi-minute `before` hooks. ~keep
timeout_secs = 900
strict = true
deny_unclassified = true

# Every session below needs a `before` that produces the artifact its snippets compile against.
# `validation_level = "compile"` reads a built artifact, but `alef all` does not build one, so any
# session without a `before` is an ordering gap: alef refused 2603 snippets across c, dart, elixir,
# php, python, r, ruby, rust and zig with "no pipeline-guaranteed build for their language" and
# failed the strict run. The seven sessions that already had a `before` were never in that list.
# Each command below is the same one the matching `[crates.test.<lang>].before` uses, rebased on
# the session's own `cwd`. ~keep
[workspace.docs.snippets.sessions.python]
cwd = "packages/python"
manifest = "packages/python/pyproject.toml"
# `uv sync` here resolves against the uv workspace root, so the venv it populates is ../../.venv,
# not a local one; maturin needs VIRTUAL_ENV pointed at it or it cannot find an interpreter. ~keep
# maturin is only a `[build-system] requires` entry, so no sync ever puts it on PATH and the hook
# died with `sh: 1: maturin: not found`, failing all 289 python snippets as ERROR. `uvx` fetches it
# into an ephemeral env and it still installs into the VIRTUAL_ENV named above. ~keep
before = [
  "uv sync --no-install-project --no-install-workspace",
  "VIRTUAL_ENV=$PWD/../../.venv uvx maturin develop --release",
]

[workspace.docs.snippets.sessions.node]
cwd = "crates/html-to-markdown-node"
manifest = "crates/html-to-markdown-node/package.json"
# The hand-written typescript snippets import the published package name
# (`@xberg-io/html-to-markdown`), same as a real consumer would -- but this
# session's cwd IS that package, so nothing installs it into its own
# node_modules the way `npm install @xberg-io/html-to-markdown` would for a
# consumer. Self-link it so the compile check resolves the same import a
# reader copies. ~keep
before = ["mkdir -p node_modules/@xberg-io && ln -sfn ../.. node_modules/@xberg-io/html-to-markdown"]

[workspace.docs.snippets.sessions.wasm]
cwd = "crates/html-to-markdown-wasm"
manifest = "crates/html-to-markdown-wasm/package.json"
# Same hand-written typescript snippets are validated again from this
# session; they still import `@xberg-io/html-to-markdown` (the node
# package), not this crate's own wasm package name, so link to the sibling
# node package rather than self-linking. ~keep
before = [
  "pnpm run build:all",
  "mkdir -p node_modules/@xberg-io && ln -sfn ../../../html-to-markdown-node node_modules/@xberg-io/html-to-markdown",
]

[workspace.docs.snippets.sessions.ruby]
cwd = "packages/ruby"
# `rm -rf .bundle tmp` first, exactly as `[crates.test.ruby].before` does: ruby/setup-ruby writes
# `.bundle/config` with `path: vendor/bundle` + `deployment: true`, and build-extension.sh unsets
# every BUNDLE_* variable before `exec rake compile`, so bare rake looks in the default gem home
# and the Rakefile's `require "rb_sys/extensiontask"` raised LoadError -- failing all 289 ruby
# snippets as ERROR. Dropping the local config makes `bundle install` land where bare rake looks.
# ~keep
before = ["rm -rf .bundle tmp", "bundle install", "./build-extension.sh"]

[workspace.docs.snippets.sessions.php]
cwd = "."
manifest = "composer.json"
before = ["cargo build --release --locked -p html-to-markdown-php"]

[workspace.docs.snippets.sessions.go]
cwd = "packages/go"
manifest = "packages/go/go.mod"
before = ["cd ../.. && cargo build --release --locked -p html-to-markdown-ffi", "cd ../.. && scripts/ci/go/copy-ffi.sh"]

[workspace.docs.snippets.sessions.java]
cwd = "packages/java"
manifest = "packages/java/pom.xml"
before = ["cd ../.. && cargo build --release --locked -p html-to-markdown-ffi", "mvn package -DskipTests -q"]

[workspace.docs.snippets.sessions.csharp]
cwd = "packages/csharp"
manifest = "packages/csharp/HtmlToMarkdown/HtmlToMarkdown.csproj"
before = [
  "cd ../.. && cargo build --release --locked -p html-to-markdown-ffi",
  "cd ../.. && scripts/ci/csharp/copy-ffi.sh",
]

[workspace.docs.snippets.sessions.elixir]
cwd = "packages/elixir"
manifest = "packages/elixir/mix.exs"
before = ["mix deps.get", "HTML_TO_MARKDOWN_BUILD=1 mix compile"]

[workspace.docs.snippets.sessions.rust]
cwd = "crates/html-to-markdown"
manifest = "crates/html-to-markdown/Cargo.toml"
before = ["cargo build --release --locked"]

[workspace.docs.snippets.sessions.kotlin_android]
cwd = "packages/kotlin-android"
manifest = "packages/kotlin-android/build.gradle.kts"
before = [
  "cd ../.. && cargo build --release --locked -p html-to-markdown-rs-jni",
  "./gradlew assembleDebug -Palef.skipHostJni=true",
]

[workspace.docs.snippets.sessions.swift]
cwd = "packages/swift"
manifest = "packages/swift/Package.swift"
before = ["swift build"]

[workspace.docs.snippets.sessions.dart]
cwd = "packages/dart"
manifest = "packages/dart/pubspec.yaml"
before = ["cd ../.. && cargo build --release --locked -p html-to-markdown-rs-dart", "dart pub get"]

[workspace.docs.snippets.sessions.zig]
cwd = "packages/zig"
manifest = "packages/zig/build.zig"
before = ["cd ../.. && cargo build --release --locked -p html-to-markdown-ffi", "zig build"]
env = { ZIG_GLOBAL_CACHE_DIR = ".zig-global-cache", ZIG_LOCAL_CACHE_DIR = ".zig-cache" }

[workspace.docs.snippets.sessions.c]
cwd = "."
manifest = "crates/html-to-markdown-ffi/include/html_to_markdown.h"
before = ["cargo build --release --locked -p html-to-markdown-ffi"]

[workspace.docs.snippets.sessions.r]
cwd = "packages/r"
manifest = "packages/r/DESCRIPTION"
before = ["Rscript -e \"devtools::load_all('.')\""]
[workspace.sync]
extra_paths = [
  "packages/ruby/lib/html_to_markdown/version.rb",
  "packages/python/html_to_markdown/__init__.py",
  "crates/html-to-markdown-node/package.json",
  "crates/html-to-markdown-node/npm/*/package.json",
  "package.json",
]

[workspace.citation]
title = "html-to-markdown"
abstract = "Fast, lossless HTML to Markdown engine."
repository-code = "https://github.com/xberg-io/html-to-markdown"
url = "https://xberg.io"
license = "MIT"
date-released = "2026-06-12"

[[workspace.citation.authors]]
family-names = "Hirschfeld"
given-names = "Na'aman"
email = "naaman@xberg.io"
orcid = "https://orcid.org/0009-0000-2247-5072"

[[workspace.sync.text_replacements]]
path = "test_apps/node/package.json"
search = '"@xberg-io/html-to-markdown": "[^"]*"'
replace = '"@xberg-io/html-to-markdown": "^{version}"'

[[workspace.sync.text_replacements]]
path = "test_apps/bun/package.json"
search = '"@xberg-io/html-to-markdown": "[^"]*"'
replace = '"@xberg-io/html-to-markdown": "^{version}"'

[[workspace.sync.text_replacements]]
path = "test_apps/wasm/package.json"
search = '"@xberg-io/html-to-markdown-wasm": "[^"]*"'
replace = '"@xberg-io/html-to-markdown-wasm": "^{version}"'

[[workspace.sync.text_replacements]]
path = "test_apps/java/pom.xml"
search = "<artifactId>html-to-markdown</artifactId>\\s*<version>[^<]*</version>"
replace = "<artifactId>html-to-markdown</artifactId>\n      <version>{version}</version>"

[[workspace.sync.text_replacements]]
path = "test_apps/csharp/XbergIo.HtmlToMarkdown.E2eTests.csproj"
search = 'Include="XbergIo.HtmlToMarkdown" Version="[^"]*"'
replace = 'Include="XbergIo.HtmlToMarkdown" Version="{version}"'

[[workspace.sync.text_replacements]]
path = "test_apps/python/pyproject.toml"
search = '"html-to-markdown>=[^"]*"'
replace = '"html-to-markdown>={python_version}"'

[[workspace.sync.text_replacements]]
path = "test_apps/ruby/Gemfile"
search = "gem 'html-to-markdown', '[^']*'"
replace = "gem 'html-to-markdown', '~> {ruby_version}'"

[[workspace.sync.text_replacements]]
path = "test_apps/go/go.mod"
search = 'github\.com/xberg-io/html-to-markdown/packages/go/v3 v[0-9][^\s]*'
replace = "github.com/xberg-io/html-to-markdown/packages/go/v3 v{version}"

[[workspace.sync.text_replacements]]
path = "test_apps/r/DESCRIPTION"
search = 'htmltomarkdown \([^)]*\)'
replace = 'htmltomarkdown ({r_version})'

[[workspace.sync.text_replacements]]
path = "test_apps/rust/Cargo.toml"
search = 'package = "html-to-markdown-rs", version = "[^"]*"'
replace = 'package = "html-to-markdown-rs", version = "{version}"'

[[workspace.sync.text_replacements]]
# The swift e2e app consumes the release/swift/<version> branch the release workflow
# publishes, not a `from:` semver bound, so alef's built-in `from:` sync never reaches it.
# Without this entry the pin silently stayed four releases behind. ~keep
path = "test_apps/swift_e2e/Package.swift"
search = 'release/swift/[^"]*'
replace = 'release/swift/{version}'

[workspace.poly]
# Skip poly's whole-project lint phase in `poly lint`: the shared CI validate job installs only
# Rust/Python/Java, so the other whole-workspace linters (credo, dart-analyze, golangci-lint,
# rubocop, steep, pyrefly) cannot run there. They run via the git pre-commit hooks and each
# language's dedicated CI job. Equivalent to always passing `poly lint --no-workspace`.
# Lives here, not as a hand-edit to the generated poly.toml, because `alef all --clean` eats
# hand-edits -- which is exactly how c99104d1f's fix was lost. ~keep
lint-workspace = false
exclude = [
  "docs-site/src/snippets/**",
  "**/_generated/**",
  "**/coverage/**",
  "**/pkg/**",
  "**/.next/**",
  "**/*.min.js",
  "**/vendored/**",
  "**/stopwords/**",
  "**/testdata/**",
  "**/e2e/**",
  "**/fixtures/**",
  "**/*.diff",
  "**/*.patch",
  "**/frb_generated.rs",
  "charts/**",
  # html-to-markdown specific
  "**/tsconfig*.json",
  "**/*_bridge_generated/**",
  "crates/html-to-markdown-node/index.js",
  "test_apps/wasm/tests/**",
  # cbindgen-generated FFI headers: keep byte-identical to cbindgen output; poly's
  # C doc-comment reflow would diverge from the canonical generated form. ~keep
  "crates/html-to-markdown-ffi/include/html_to_markdown.h",
  "packages/go/include/html_to_markdown.h",
  # Alef-generated C#/Java bindings: poly's formatters disagree with the generator's output, so
  # `poly fmt` and the regeneration freshness check fight over every line. ~keep
  "packages/csharp/**/*.cs",
  "packages/java/**/*.java",
  "test_apps/csharp/**/*.cs",
  "test_apps/java/**/*.java",
  # htmbench-promoted calibration pair: serde_json's pretty printer always expands short
  # arrays that poly collapses onto one line, so formatting them is undone by the next
  # `bench:calibrate` promotion and the two fight forever. ~keep
  "tools/benchmark-harness/baselines/baseline.json",
  "tools/benchmark-harness/guardrails.json",
  # Vendored wasm-bindgen artifact (docs live demo); hand-written script.js /
  # style.css / index.html in the same dir stay linted. ~keep
  "docs-site/public/demo/html_to_markdown_wasm*",
  # ai-rulez-generated coding-agent plugin outputs: owned by `ai-rulez generate
  # --plugin`, never reformatted by poly (would break BLAKE3 provenance). Sources
  # under plugin/.ai-rulez/ + plugin/scripts/ stay poly-managed. ~keep
  "plugin/.claude-plugin/**",
  "plugin/.codex-plugin/**",
  "plugin/.cursor-plugin/**",
  "plugin/.factory-plugin/**",
  "plugin/.opencode/**",
  "plugin/.hermes/**",
  "plugin/skills/**",
  "plugin/.mcp.json",
  "plugin/gemini-extension.json",
  "plugin/kimi.plugin.json",
  "plugin/package.json",
  "plugin/.ai-rulez-generated.json",
  ".claude-plugin/marketplace.json",
  ".agents/plugins/marketplace.json",
  ".ai-rulez-generated.json",
]

# Opt-in comment-removal sweep. Preserve doc comments, TODO/FIXME, and
# SAFETY/license markers. Generated binding output is exempted via
# per-file-ignores below. ~keep
[workspace.poly.uncomment]
enabled = true
remove-todos = false
remove-fixme = false
remove-docs = false
use-default-ignores = true
preserve-patterns = ["SAFETY", "SPDX", "Copyright", "License", "licensed under"]

[workspace.poly.typos.extend-words]
# Deliberate misspelling inside a generated comment that quotes a type-checker error
# message verbatim ("has no attribute `addresss`"). Must stay as-is. ~keep
addresss = "addresss"
ake = "ake"
algoritmos = "algoritmos"
arange = "arange"
bre = "bre"
caf = "caf"
charater = "charater"
chlidren = "chlidren"
cliper = "cliper"
cursos = "cursos"
delocate = "delocate"
docling = "docling"
edn = "edn"
ein = "ein"
excepcional = "excepcional"
execpt = "execpt"
extention = "extention"
faiure = "faiure"
fith = "fith"
flate = "flate"
formated = "formated"
headerr = "headerr"
helo = "helo"
hocr = "hocr"
hom = "hom"
inh = "inh"
internacional = "internacional"
ist = "ist"
kreuzberg = "kreuzberg"
layrnorm = "layrnorm"
legt = "legt"
markitdown = "markitdown"
ment = "ment"
mis = "mis"
mmaped = "mmaped"
multline = "multline"
odf = "odf"
onnx = "onnx"
openwebui = "openwebui"
opf = "opf"
ove = "ove"
ower = "ower"
pard = "pard"
portait = "portait"
programa = "programa"
pymupdf = "pymupdf"
relatie = "relatie"
rumdl = "rumdl"
seperator = "seperator"
ser = "ser"
similarily = "similarily"
siz = "siz"
specifing = "specifing"
splitted = "splitted"
substract = "substract"
surrealdb = "surrealdb"
tbe = "tbe"
tesseract = "tesseract"
tha = "tha"
thr = "thr"
threed = "threed"
ths = "ths"
thw = "thw"
traineddata = "traineddata"
tre = "tre"
unparseable = "unparseable"
utiliza = "utiliza"
webui = "webui"
yor = "yor"

[workspace.poly.typos.extend-identifiers]
MarkItDown = "MarkItDown"
PDFium = "PDFium"
PyMuPDF = "PyMuPDF"
SurrealDB = "SurrealDB"
WebUI = "WebUI"
traineddata = "traineddata"

[workspace.poly.per-file-ignores]
# Alef-generated config files. Both grow with the size of the surface alef manages and
# with the quality-debt baseline below; a line count on generated config is not a
# maintainability signal. ~keep
"poly.toml" = ["file-too-long"]
".alef-toml-merge-provenance.toml" = ["file-too-long"]
# --- quality-debt baseline (generated) ---
# 161 size/complexity findings across 79 files that predate these
# metrics. Listed per file rather than switching the rules off, so the metrics still
# fire on every file NOT listed here -- new code stays covered, and each path below is
# one discrete unit of work.
# TODO(xberg-io/html-to-markdown#465): pay this down file by file and delete each entry as it goes green.
# Do not add new paths here without a very good reason. ~keep
".github/workflows/ci-e2e.yaml" = ["file-too-long"]
"CHANGELOG.md" = ["file-too-long"]
"alef.toml" = ["file-too-long"]
"crates/html-to-markdown-cli/src/args.rs" = ["type-too-long"]
"crates/html-to-markdown-cli/src/convert.rs" = ["function-too-long"]
"crates/html-to-markdown-php/src/HtmlVisitor.php" = ["type-too-long"]
"crates/html-to-markdown/examples/test_inline_formatting.rs" = ["function-too-long"]
"crates/html-to-markdown/examples/test_tables.rs" = ["function-too-long"]
"crates/html-to-markdown/src/convert_api.rs" = ["function-too-long"]
"crates/html-to-markdown/src/converter/block/container.rs" = ["too-many-parameters"]
"crates/html-to-markdown/src/converter/block/div.rs" = [
  "cyclomatic-complexity",
  "function-too-long",
  "too-many-parameters",
]
"crates/html-to-markdown/src/converter/block/heading.rs" = [
  "cyclomatic-complexity",
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
]
"crates/html-to-markdown/src/converter/block/horizontal_rule.rs" = ["function-too-long", "too-many-parameters"]
"crates/html-to-markdown/src/converter/block/line_break.rs" = ["function-too-long", "too-many-parameters"]
"crates/html-to-markdown/src/converter/block/paragraph.rs" = [
  "cyclomatic-complexity",
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
]
"crates/html-to-markdown/src/converter/block/table/builder.rs" = [
  "cyclomatic-complexity",
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
]
"crates/html-to-markdown/src/converter/block/table/caption.rs" = ["too-many-parameters"]
"crates/html-to-markdown/src/converter/block/table/cell.rs" = [
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
]
"crates/html-to-markdown/src/converter/block/table/cells.rs" = ["function-too-long", "too-many-parameters"]
"crates/html-to-markdown/src/converter/block/table/mod.rs" = ["nesting-too-deep", "too-many-parameters"]
"crates/html-to-markdown/src/converter/block/unknown.rs" = ["function-too-long", "too-many-parameters"]
"crates/html-to-markdown/src/converter/context.rs" = ["function-too-long", "too-many-parameters"]
"crates/html-to-markdown/src/converter/form/elements.rs" = ["too-many-parameters"]
"crates/html-to-markdown/src/converter/form/mod.rs" = ["too-many-parameters"]
"crates/html-to-markdown/src/converter/handlers/blockquote.rs" = [
  "cyclomatic-complexity",
  "function-too-long",
  "too-many-parameters",
]
"crates/html-to-markdown/src/converter/handlers/code_block.rs" = [
  "cyclomatic-complexity",
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
]
"crates/html-to-markdown/src/converter/handlers/graphic.rs" = [
  "cyclomatic-complexity",
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
]
"crates/html-to-markdown/src/converter/handlers/image.rs" = [
  "cyclomatic-complexity",
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
]
"crates/html-to-markdown/src/converter/handlers/link.rs" = [
  "cyclomatic-complexity",
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
]
"crates/html-to-markdown/src/converter/inline/code.rs" = [
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
]
"crates/html-to-markdown/src/converter/inline/emphasis.rs" = [
  "cyclomatic-complexity",
  "function-too-long",
  "too-many-parameters",
]
"crates/html-to-markdown/src/converter/inline/link.rs" = [
  "cyclomatic-complexity",
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
]
"crates/html-to-markdown/src/converter/inline/mod.rs" = ["too-many-parameters"]
"crates/html-to-markdown/src/converter/inline/ruby.rs" = [
  "cyclomatic-complexity",
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
]
"crates/html-to-markdown/src/converter/inline/semantic/marks.rs" = ["function-too-long", "too-many-parameters"]
"crates/html-to-markdown/src/converter/inline/semantic/mod.rs" = ["too-many-parameters"]
"crates/html-to-markdown/src/converter/inline/semantic/typography.rs" = ["function-too-long", "too-many-parameters"]
"crates/html-to-markdown/src/converter/list/definition.rs" = ["function-too-long", "too-many-parameters"]
"crates/html-to-markdown/src/converter/list/item.rs" = [
  "cyclomatic-complexity",
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
]
"crates/html-to-markdown/src/converter/list/mod.rs" = ["too-many-parameters"]
"crates/html-to-markdown/src/converter/list/ordered.rs" = ["function-too-long", "too-many-parameters"]
"crates/html-to-markdown/src/converter/list/unordered.rs" = ["function-too-long", "too-many-parameters"]
"crates/html-to-markdown/src/converter/list/utils.rs" = ["nesting-too-deep", "too-many-parameters"]
"crates/html-to-markdown/src/converter/main.rs" = [
  "cyclomatic-complexity",
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
]
"crates/html-to-markdown/src/converter/media/embedded.rs" = ["function-too-long", "too-many-parameters"]
"crates/html-to-markdown/src/converter/media/image.rs" = ["cyclomatic-complexity", "function-too-long"]
"crates/html-to-markdown/src/converter/media/mod.rs" = ["too-many-parameters"]
"crates/html-to-markdown/src/converter/media/svg.rs" = ["too-many-parameters"]
"crates/html-to-markdown/src/converter/metadata.rs" = ["nesting-too-deep", "too-many-parameters"]
"crates/html-to-markdown/src/converter/plain_text.rs" = [
  "cyclomatic-complexity",
  "function-too-long",
  "nesting-too-deep",
]
"crates/html-to-markdown/src/converter/prescan.rs" = ["cyclomatic-complexity", "function-too-long", "nesting-too-deep"]
"crates/html-to-markdown/src/converter/semantic/attributes.rs" = ["too-many-parameters"]
"crates/html-to-markdown/src/converter/semantic/definition_list.rs" = ["too-many-parameters"]
"crates/html-to-markdown/src/converter/semantic/figure.rs" = [
  "cyclomatic-complexity",
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
]
"crates/html-to-markdown/src/converter/semantic/mod.rs" = ["too-many-parameters"]
"crates/html-to-markdown/src/converter/semantic/sectioning.rs" = ["too-many-parameters"]
"crates/html-to-markdown/src/converter/semantic/summary.rs" = [
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
]
"crates/html-to-markdown/src/converter/text_node.rs" = [
  "cyclomatic-complexity",
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
]
"crates/html-to-markdown/src/converter/tier1/router.rs" = ["cyclomatic-complexity", "function-too-long"]
"crates/html-to-markdown/src/converter/tier1/scanner.rs" = [
  "cyclomatic-complexity",
  "file-too-long",
  "function-too-long",
  "nesting-too-deep",
]
"crates/html-to-markdown/src/converter/utility/content.rs" = ["function-too-long", "nesting-too-deep"]
"crates/html-to-markdown/src/converter/utility/preprocessing.rs" = [
  "file-too-long",
  "nesting-too-deep",
  "too-many-parameters",
]
"crates/html-to-markdown/src/metadata/extraction.rs" = ["cyclomatic-complexity", "function-too-long"]
"crates/html-to-markdown/src/types/structure_builder.rs" = [
  "cyclomatic-complexity",
  "function-too-long",
  "nesting-too-deep",
]
"crates/html-to-markdown/src/visitor_helpers.rs" = ["too-many-parameters"]
"crates/html-to-markdown/src/visitor_helpers/helpers/state.rs" = ["too-many-parameters"]
"crates/html-to-markdown/src/wrapper/sync.rs" = ["cyclomatic-complexity", "function-too-long"]
"crates/html-to-markdown/tests/visitor_integration_test.rs" = ["file-too-long"]
"docs-site/src/content/docs/changelog.md" = ["file-too-long"]
"scripts/check_ffi_symbols.py" = ["function-too-long", "nesting-too-deep"]
"tools/benchmark-harness/examples/mdream_compare.rs" = ["function-too-long"]
"tools/benchmark-harness/src/main.rs" = ["function-too-long"]
# --- end quality-debt baseline ---
# Quality metrics do not apply to test code. A test function's length is its assertion
# count and a table-driven test's arity mirrors its cases; splitting either to satisfy a
# production-code maintainability metric makes the test harder to read, not easier. Only
# `file-too-long` and `type-too-long` still apply here -- an oversized test FILE is a real
# organisational problem, unlike an oversized test function. ~keep
"**/tests/**/*" = ["cyclomatic-complexity", "function-too-long", "nesting-too-deep", "too-many-parameters"]
"**/test/**/*" = ["cyclomatic-complexity", "function-too-long", "nesting-too-deep", "too-many-parameters"]
"**/benches/**/*" = ["cyclomatic-complexity", "function-too-long", "nesting-too-deep", "too-many-parameters"]
"**/*_test.rs" = ["cyclomatic-complexity", "function-too-long", "nesting-too-deep", "too-many-parameters"]
"**/*_tests.rs" = ["cyclomatic-complexity", "function-too-long", "nesting-too-deep", "too-many-parameters"]
"**/test_*.py" = ["cyclomatic-complexity", "function-too-long", "nesting-too-deep", "too-many-parameters"]
"**/*_test.py" = ["cyclomatic-complexity", "function-too-long", "nesting-too-deep", "too-many-parameters"]
"**/conftest.py" = ["cyclomatic-complexity", "function-too-long", "nesting-too-deep", "too-many-parameters"]
"**/*.test.ts" = ["cyclomatic-complexity", "function-too-long", "nesting-too-deep", "too-many-parameters"]
"**/*.test.js" = ["cyclomatic-complexity", "function-too-long", "nesting-too-deep", "too-many-parameters"]
"**/*.test.mjs" = ["cyclomatic-complexity", "function-too-long", "nesting-too-deep", "too-many-parameters"]
"**/*.spec.ts" = ["cyclomatic-complexity", "function-too-long", "nesting-too-deep", "too-many-parameters"]
"**/*.spec.js" = ["cyclomatic-complexity", "function-too-long", "nesting-too-deep", "too-many-parameters"]
"**/*_test.go" = ["cyclomatic-complexity", "function-too-long", "nesting-too-deep", "too-many-parameters"]
"**/*Test.java" = ["cyclomatic-complexity", "function-too-long", "nesting-too-deep", "too-many-parameters"]
"**/*Tests.cs" = ["cyclomatic-complexity", "function-too-long", "nesting-too-deep", "too-many-parameters"]
"**/*_test.dart" = ["cyclomatic-complexity", "function-too-long", "nesting-too-deep", "too-many-parameters"]
"**/*_spec.rb" = ["cyclomatic-complexity", "function-too-long", "nesting-too-deep", "too-many-parameters"]
# Stylistic rules disabled repo-wide. Each is a formatting or idiom preference with no
# correctness or security consequence, and each fires in volumes no one can hand-maintain
# (`require-unicode-regexp` alone was 724 across the polyrepo, `no-inline-comments` 200).
# `eqeqeq` is here for a stronger reason: every occurrence audited was the deliberate
# `== null` nullish idiom, so the rule had a 100% false-positive rate here.
# Rules with a real correctness or security angle are deliberately NOT listed and stay on:
# unicorn/require-post-message-target-origin, unicorn/no-array-callback-reference,
# unicorn/no-immediate-mutation, unicorn/no-array-fill-with-reference-type. ~keep
"**" = [
  "CPY001",
  "UnorderedKey",
  "eqeqeq",
  "no-inline-comments",
  "no-negated-condition",
  "require-unicode-regexp",
  "unicorn/consistent-function-scoping",
  "unicorn/escape-case",
  "unicorn/explicit-length-check",
  "unicorn/new-for-builtins",
  "unicorn/no-array-sort",
  "unicorn/no-hex-escape",
  "unicorn/no-lonely-if",
  "unicorn/no-negated-condition",
  "unicorn/no-typeof-undefined",
  "unicorn/no-useless-undefined",
  "unicorn/prefer-add-event-listener",
  "unicorn/prefer-at",
  "unicorn/prefer-code-point",
  "unicorn/prefer-dom-node-append",
  "unicorn/prefer-dom-node-remove",
  "unicorn/prefer-import-meta-properties",
  "unicorn/prefer-native-coercion-functions",
  "unicorn/prefer-number-coercion",
  "unicorn/prefer-query-selector",
  "unicorn/prefer-single-call",
  "unicorn/prefer-string-replace-all",
  "unicorn/prefer-string-slice",
  "unicorn/prefer-top-level-await",
  "unicorn/prefer-type-error",
  "unicorn/require-module-specifiers",
]
# The `quality` rules below are suppressed on these generated paths for a reason distinct
# from the uncomment exemption. poly's quality tier measures function length, parameter count,
# file length, nesting depth and cyclomatic complexity -- metrics written for hand-maintained
# code. A generated binding surface cannot satisfy them and must not try: a binding function's
# parameter list mirrors the core API it wraps one-for-one, and a single-file binding crate is
# one file by construction. poly still REPORTS on generated files (only `--fix-generated` gates
# rewriting, and a `DO NOT EDIT` banner holds nothing back), so without these entries the
# metrics fire on every regeneration and no edit here could silence them -- regeneration would
# revert it. `astgrep` and language-specific rules stay ACTIVE on these paths on purpose:
# those catch real generator defects. ~keep
# Alef-generated binding output — never hand-clean comments; regeneration owns
# them. Exempts these paths from the uncomment sweep. ~keep
".cargo/config.toml" = ["comment", "doc-comment"]
"Package.swift" = ["comment", "doc-comment"]
"packages/**" = [
  "comment",
  "doc-comment",
  "cyclomatic-complexity",
  "file-too-long",
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
  "type-too-long",
]
"crates/html-to-markdown-node/**" = [
  "comment",
  "doc-comment",
  "cyclomatic-complexity",
  "file-too-long",
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
  "type-too-long",
]
# The php crate is MIXED: src/lib.rs, the visitor interface and the stubs are alef-generated,
# but src/HtmlVisitor.php is hand-written and must keep its quality metrics. ~keep
"crates/html-to-markdown-php/**" = ["comment", "doc-comment"]
"crates/html-to-markdown-php/src/lib.rs" = [
  "comment",
  "doc-comment",
  "cyclomatic-complexity",
  "file-too-long",
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
  "type-too-long",
]
"crates/html-to-markdown-php/src/HtmlVisitorInterface.php" = [
  "comment",
  "doc-comment",
  "cyclomatic-complexity",
  "file-too-long",
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
  "type-too-long",
]
"crates/html-to-markdown-php/stubs/**" = [
  "comment",
  "doc-comment",
  "cyclomatic-complexity",
  "file-too-long",
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
  "type-too-long",
]
"crates/html-to-markdown-rs-jni/**" = [
  "comment",
  "doc-comment",
  "cyclomatic-complexity",
  "file-too-long",
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
  "type-too-long",
]
# ffi/py/wasm are MIXED: only the alef-generated src/lib.rs (+ ffi
# build.rs/cbindgen.toml/header) is exempt; other modules are hand-written and
# get the uncomment sweep. ~keep
"crates/html-to-markdown-ffi/src/lib.rs" = [
  "comment",
  "doc-comment",
  "cyclomatic-complexity",
  "file-too-long",
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
  "type-too-long",
]
"crates/html-to-markdown-ffi/build.rs" = ["comment", "doc-comment"]
"crates/html-to-markdown-ffi/cbindgen.toml" = ["comment", "doc-comment"]
"crates/html-to-markdown-ffi/include/html_to_markdown.h" = ["comment", "doc-comment"]
"crates/html-to-markdown-py/src/lib.rs" = [
  "comment",
  "doc-comment",
  "cyclomatic-complexity",
  "file-too-long",
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
  "type-too-long",
]
"crates/html-to-markdown-wasm/src/lib.rs" = [
  "comment",
  "doc-comment",
  "cyclomatic-complexity",
  "file-too-long",
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
  "type-too-long",
]
# alef-generated e2e registry scaffold apps (alef:hash headers) — exempt from the
# uncomment sweep; regeneration owns their comments. Distinct glob from the
# builtin "**/test_apps/**" ruff-relaxation key. ~keep
"test_apps/**" = [
  "comment",
  "doc-comment",
  "cyclomatic-complexity",
  "file-too-long",
  "function-too-long",
  "nesting-too-deep",
  "too-many-parameters",
  "type-too-long",
]
# alef-generated Python entrypoint; RET504 fires on the passthrough return. ~keep
"packages/python/**/api.py" = ["RET504"]
# Test-code relaxations on top of the builtin "**/tests/**" defaults. ~keep
"**/tests/**/*.py" = ["PT019", "PLC0415", "SLF001"]
# html-to-markdown specific
"scripts/**/*.py" = ["INP001", "S607", "T201", "D103", "D212"]
# Redundant "# noqa: S603" (S603 is globally ignored) in a hand-written proxy module.
"cli-proxy/**/*.py" = ["PLR0917", "RUF100"]
"readme_templates/**" = ["MD057"]
# Alef-generated API reference pages: one page per language, each a full rendering of the
# public surface, so `file-too-long` is a property of the surface's size. ~keep
"docs-site/src/content/docs/reference/**" = ["file-too-long"]
".github/workflows/publish.yaml" = ["file-too-long", "typo"]
"packages/r/R/version.R" = ["empty_file"]
"crates/html-to-markdown/src/visitor/types.rs" = ["too-many-parameters", "typo"]
"packages/python/html_to_markdown/options.py" = ["F821"]
"packages/kotlin-android/src/main/kotlin/**/*.kt" = ["typo"]
"packages/swift/Sources/RustBridge*/**" = ["trailing-whitespace"]
"e2e/**/*.py" = ["Q000"]
"**/skills/**/SKILL.md" = ["MD032"]
# Node build-time scripts whose console output IS their product: the two `verify-*` guards
# report the check they ran, and fail the build via `console.error` + `process.exit(1)`;
# `patch-bundler-entry.js` reports the wasm entry it rewrote. These are standalone scripts run
# by npm/task with no subscriber or logger to route through. ~keep
"crates/html-to-markdown-node/scripts/**" = ["no-console"]
"crates/html-to-markdown-wasm/scripts/**" = ["no-console"]
# Standalone browser demo asset, loaded straight into a `<script type="module">` on the docs
# site with no bundler and no logger. Every `console.error` here already has a paired
# user-visible status message; the console line is the devtools-side detail. ~keep
"docs-site/public/demo/**" = ["no-console"]
# Vendored upstream Apache Maven wrapper (Apache-2.0, (c) the original authors). The empty
# `catch (IOException)` around a stream close is upstream's; editing it forks a file we take
# verbatim and would re-take on every wrapper upgrade. ~keep
"**/MavenWrapperDownloader.java" = ["empty-catch-block"]
# `@opencode-ai/plugin` contract entrypoints. The exported `Plugin` factory is typed to return
# a promise of the hooks object, and each tool `execute` relies on `async` to turn its
# argument-validation `throw` into a rejection the OpenCode runtime handles. Dropping `async`
# to satisfy require-await would make those throws synchronous and break the contract. ~keep
"plugin/.ai-rulez/opencode/index.js" = ["function-too-long", "require-await"]

[[crates]]
name = "html-to-markdown-rs"
core_import = "html_to_markdown_rs"
error_type = "ConversionError"
error_constructor = "html_to_markdown_rs::ConversionError::Other({msg})"
sources = [
  "crates/html-to-markdown/src/lib.rs",
  "crates/html-to-markdown/src/options/conversion.rs",
  "crates/html-to-markdown/src/options/preprocessing.rs",
  "crates/html-to-markdown/src/options/validation.rs",
  "crates/html-to-markdown/src/types/result.rs",
  "crates/html-to-markdown/src/types/document.rs",
  "crates/html-to-markdown/src/types/tables.rs",
  "crates/html-to-markdown/src/types/warnings.rs",
  "crates/html-to-markdown/src/error.rs",
  "crates/html-to-markdown/src/convert_api.rs",
  "crates/html-to-markdown/src/metadata/types.rs",
  "crates/html-to-markdown/src/visitor/traits.rs",
  "crates/html-to-markdown/src/visitor/types.rs",
  "crates/html-to-markdown/src/visitor/default_impl.rs",
]
version_from = "Cargo.toml"
workspace_root = "."
features = ["serde", "metadata", "visitor", "inline-images", "testkit"]

[crates.python]
module_name = "_html_to_markdown"
pip_name = "html-to-markdown"
reexported_types = ["ConversionResult"]
sdist_include = ["crates/html-to-markdown"]

[crates.python.stubs]
output = "packages/python/html_to_markdown/"

[crates.node]
package_name = "@xberg-io/html-to-markdown"
crate_dir = "crates/html-to-markdown-node"

[crates.ruby]
gem_name = "html-to-markdown"
exclude_types = ["ConversionOptionsBuilder"]

[crates.ruby.stubs]
output = "packages/ruby/sig/"

[crates.php]
extension_name = "html_to_markdown"
namespace = "HtmlToMarkdown"

[crates.elixir]
app_name = "html_to_markdown"
exclude_types = ["ConversionOptionsBuilder"]
cpu_bound_functions = ["convert"]

[crates.ffi]
prefix = "htm"
header_name = "html_to_markdown.h"
lib_name = "html_to_markdown_ffi"
visitor_callbacks = true

[crates.go]
module = "github.com/xberg-io/html-to-markdown/packages/go/v3"
package_name = "htmltomarkdown"

[crates.java]
package = "io.xberg.htmltomarkdown"
group_id = "io.xberg"
artifact_id = "html-to-markdown"

[crates.csharp]
namespace = "HtmlToMarkdown"
package_id = "XbergIo.HtmlToMarkdown"

[crates.r]
package_name = "htmltomarkdown"

[crates.kotlin_android]
package = "io.xberg.android"
namespace = "io.xberg.android"
artifact_id = "html-to-markdown-android"
group_id = "io.xberg"
abis = ["arm64-v8a", "x86_64", "armeabi-v7a", "x86"]
features = ["serde", "metadata", "inline-images", "testkit"]
exclude_functions = ["visitor"]

[crates.swift]
module_name = "HtmlToMarkdown"
package_name = "HtmlToMarkdown"
features = ["full", "testkit"]

[crates.dart]
pubspec_name = "h2m"
lib_name = "h2m"
style = "frb"
frb_version = "2.13.0"
features = ["full", "testkit"]

[crates.wasm]
crate_dir = "crates/html-to-markdown-wasm"
features = ["serde", "metadata", "visitor", "inline-images", "testkit"]

[crates.zig]
module_name = "html_to_markdown_rs"

[crates.output]
python = "crates/html-to-markdown-py/src/"
node = "crates/html-to-markdown-node/src/"
ruby = "packages/ruby/ext/html_to_markdown_rb/src/"
php = "crates/html-to-markdown-php/src/"
ffi = "crates/html-to-markdown-ffi/src/"
go = "packages/go/"
java = "packages/java/src/main/java/"
elixir = "packages/elixir/native/html_to_markdown_nif/src/"
wasm = "crates/html-to-markdown-wasm/src/"
r = "packages/r/src/rust/src/"
kotlin_android = "packages/kotlin-android/src/main/kotlin/io/xberg/android/"
swift = "packages/swift/Sources/HtmlToMarkdown/"
dart = "packages/dart/lib/src/"
zig = "packages/zig/src/"

[crates.exclude]
types = []
functions = []
# `NodeContext.serialize`/`.deserialize` used to be listed here and warned on every run as
# "exclude.methods entry did not match any extracted item". They are `serde::Serialize` /
# `serde::Deserialize` trait impls (crates/html-to-markdown/src/visitor/types.rs), and the
# extractor only collects inherent-impl methods, so no qualification of those names could ever
# match. `with_borrowed_attributes` is an inherent method and does match. ~keep
methods = ["NodeContext.with_borrowed_attributes"]

[crates.include]
types = [
  "ConversionOptions",
  "ConversionResult",
  "HtmlVisitor",
  "ImageDimensions",
  "MetadataEntry",
  "NodeContext",
  "VisitResult",
]
functions = ["convert"]

[crates.test.python]
precondition = "command -v uv >/dev/null 2>&1"
before = "cd e2e/python && uv sync --no-install-project --no-install-workspace && uv pip install --refresh ../../packages/python"
e2e = "cd e2e/python && uv run --no-sync pytest tests/ -q"

[crates.test.node]
precondition = "command -v pnpm >/dev/null 2>&1"
before = "cd crates/html-to-markdown-node && pnpm install && pnpm run build"
e2e = "cd e2e/node && pnpm install && pnpm exec vitest run"

[crates.test.go]
precondition = "command -v go >/dev/null 2>&1"
before = "cargo build --release --locked -p html-to-markdown-ffi && cd e2e/go && go mod tidy"
e2e = "cd e2e/go && go test ./... -count=1"

[crates.test.ruby]
precondition = "command -v bundle >/dev/null 2>&1"
before = "cd packages/ruby && gem pristine --all 2>/dev/null || true && rm -rf .bundle tmp && bundle install && ./build-extension.sh"
e2e = "cd e2e/ruby && bundle install && bundle exec rspec"

[crates.test.php]
precondition = "command -v php >/dev/null 2>&1"
before = "cargo build --release -p html-to-markdown-php && cd e2e/php && composer install"
e2e = "cd e2e/php && composer test"

[crates.test.java]
precondition = "command -v mvn >/dev/null 2>&1"
before = "cargo build --release --locked -p html-to-markdown-ffi && cd packages/java && mvn package -DskipTests -q"
e2e = "cd e2e/java && mvn test -q"

[crates.test.csharp]
precondition = "command -v dotnet >/dev/null 2>&1"
before = "cargo build --release --locked -p html-to-markdown-ffi && scripts/ci/csharp/copy-ffi.sh"
e2e = "dotnet test e2e/csharp"

[crates.test.elixir]
precondition = "command -v mix >/dev/null 2>&1"
before = "cd packages/elixir && mix deps.get && HTML_TO_MARKDOWN_BUILD=1 mix compile"
e2e = "cd e2e/elixir && mix deps.get && HTML_TO_MARKDOWN_BUILD=1 mix compile && HTML_TO_MARKDOWN_BUILD=1 mix test"

[crates.test.r]
precondition = "which Rscript 2>/dev/null"
e2e = "cd e2e/r && Rscript -e \"devtools::load_all('../../packages/r'); testthat::set_max_fails(Inf); testthat::test_dir('tests')\""

[crates.test.wasm]
precondition = "command -v pnpm >/dev/null 2>&1"
before = "cd crates/html-to-markdown-wasm && pnpm run build:all && cd ../.. && cd e2e/wasm && pnpm install"
e2e = "cd e2e/wasm && pnpm test"

[crates.test.rust]
precondition = "command -v cargo >/dev/null 2>&1"
e2e = "cd e2e/rust && cargo test"

[crates.test.c]
precondition = "command -v make >/dev/null 2>&1"
before = "cargo build --release --locked -p html-to-markdown-ffi"
e2e = "cd e2e/c && make clean && make && DYLD_LIBRARY_PATH=../../target/release LD_LIBRARY_PATH=../../target/release ./run_tests"

[crates.test.kotlin_android]
precondition = "command -v gradle >/dev/null 2>&1"
before = "cargo build --release --locked -p html-to-markdown-rs-jni"
e2e = "cd e2e/kotlin_android && gradle test --no-daemon"

[crates.test.swift]
precondition = "command -v swift >/dev/null 2>&1"
before = "cargo build --release --locked -p html-to-markdown-ffi -p html-to-markdown-rs-swift"
e2e = "cd e2e/swift_e2e && swift test"

[crates.test.dart]
precondition = "command -v dart >/dev/null 2>&1"
before = "{ command -v flutter_rust_bridge_codegen >/dev/null 2>&1 || cargo install flutter_rust_bridge_codegen --version 2.13.0; } && cd packages/dart && dart pub get && cd rust && flutter_rust_bridge_codegen generate --config-file flutter_rust_bridge.yaml && cd ../../.. && cargo build --release --locked -p html-to-markdown-rs-dart"
e2e = "cd e2e/dart && DYLD_FALLBACK_LIBRARY_PATH=$PWD/../../target/release LD_LIBRARY_PATH=$PWD/../../target/release FRB_DART_LOAD_EXTERNAL_LIBRARY_NATIVE_LIB_DIR=$PWD/../../target/release dart pub get && DYLD_FALLBACK_LIBRARY_PATH=$PWD/../../target/release LD_LIBRARY_PATH=$PWD/../../target/release FRB_DART_LOAD_EXTERNAL_LIBRARY_NATIVE_LIB_DIR=$PWD/../../target/release dart test"

[crates.test.zig]
precondition = "command -v zig >/dev/null 2>&1"
before = "cargo build --release --locked -p html-to-markdown-ffi"
e2e = "cd e2e/zig && zig build test"

[crates.publish]
core_crate = "crates/html-to-markdown"


[crates.publish.languages.elixir]
nif_versions = ["2.16", "2.17"]

[crates.publish.languages.r]
vendor_mode = "full"

[crates.publish.languages.ffi]
pkg_config = true
cmake_config = true

[crates.e2e]
fixtures = "fixtures"
output = "e2e"
languages = [
  "python",
  "node",
  "ruby",
  "php",
  "php_ext",
  "c",
  "go",
  "java",
  "csharp",
  "elixir",
  "wasm",
  "r",
  "rust",
  "kotlin_android",
  "swift",
  "dart",
  "zig",
]

result_fields = ["content", "document", "metadata", "tables", "warnings"]
fields_optional = [
  "content",
  "document",
  "metadata.document.title",
  "metadata.document.description",
  "metadata.document.author",
  "metadata.document.canonical_url",
  "metadata.document.open_graph[title]",
  "metadata.document.open_graph[description]",
  "metadata.document.open_graph[image]",
  "metadata.document.open_graph[type]",
  "metadata.document.open_graph[url]",
  "metadata.document.open_graph[site_name]",
  "metadata.document.twitter_card[card]",
  "metadata.document.twitter_card[title]",
  "metadata.document.twitter_card[description]",
]

[crates.e2e.snippets]
output = "docs-site/src/snippets/generated"
languages = [
  "python",
  "node",
  "ruby",
  "php",
  "c",
  "go",
  "java",
  "csharp",
  "elixir",
  "wasm",
  "r",
  "rust",
  "kotlin_android",
  "swift",
  "dart",
  "zig",
]
# The 96 files below are hand-authored on purpose and have no fixture behind them, so alef will
# never generate an equivalent: `alef e2e snippets-migrate docs-site/src/snippets` reports every
# one as `no_generated_equivalent`. Five narrative topics x 16 language roots (wasm has no
# `error-handling` variant, hence 95 rather than 96) demonstrate the public field-access idiom
# -- `result.content` / `result.metadata` / `metadata.document.title` -- that the fixture-driven
# corpus under `output` never emits; `feedback.md` is an MDX prose partial, not code at all.
# Declaring them here is what turns them from `UnaccountedSnippet` warnings into recorded
# curation. Patterns resolve against the PROJECT ROOT, not against `output` -- these files sit
# beside the generated tree, so an `output`-relative pattern could not name them. The topic
# names are disjoint from every directory under `generated/<lang>/`, so none of these patterns
# can shadow a generated file. ~keep
curated_snippets = [
  "docs-site/src/snippets/feedback.md",
  "docs-site/src/snippets/*/error-handling/*.md",
  "docs-site/src/snippets/*/getting-started/*.md",
  "docs-site/src/snippets/*/metadata/basic_extraction.md",
  "docs-site/src/snippets/*/table-extraction/*.md",
  "docs-site/src/snippets/*/visitor/basic_visitor.md",
]

[crates.e2e.call]
function = "convert"
module = "html_to_markdown_rs"
result_var = "result"
options_type = "ConversionOptions"
args = [
  { name = "html", field = "html", type = "string" },
  { name = "options", field = "options", type = "json_object", optional = true },
]

[crates.e2e.call.overrides.rust]
crate_name = "html_to_markdown_rs"
module = "html_to_markdown_rs"
function = "convert"
options_type = "ConversionOptions"
wrap_options_in_some = true
returns_result = true
visitor_trait = "HtmlVisitor"

[crates.e2e.call.overrides.python]
module = "html_to_markdown"
function = "convert"
options_type = "ConversionOptions"
enum_module = "html_to_markdown.options"

[crates.e2e.call.overrides.python.enum_fields]
headingStyle = "HeadingStyle"
codeBlockStyle = "CodeBlockStyle"
listIndentType = "ListIndentType"
newlineStyle = "NewlineStyle"
whitespaceMode = "WhitespaceMode"
highlightStyle = "HighlightStyle"
linkStyle = "LinkStyle"
outputFormat = "OutputFormat"
urlEscapeStyle = "UrlEscapeStyle"

[crates.e2e.call.overrides.node]
module = "@xberg-io/html-to-markdown"
function = "convert"
options_type = "ConversionOptions"

[crates.e2e.call.overrides.node.enum_fields]
headingStyle = "HeadingStyle"
codeBlockStyle = "CodeBlockStyle"
listIndentType = "ListIndentType"
newlineStyle = "NewlineStyle"
whitespaceMode = "WhitespaceMode"
highlightStyle = "HighlightStyle"
linkStyle = "LinkStyle"
outputFormat = "OutputFormat"
urlEscapeStyle = "UrlEscapeStyle"

[crates.e2e.call.overrides.go]
module = "github.com/xberg-io/html-to-markdown/packages/go/v3"
function = "Convert"
alias = "htmd"
options_type = "ConversionOptions"
returns_result = true
options_ptr = true

[crates.e2e.call.overrides.java]
class = "io.xberg.htmltomarkdown.HtmlToMarkdownRs"
function = "convert"
options_type = "ConversionOptions"
nested_types_optional = false

[crates.e2e.call.overrides.java.enum_fields]
headingStyle = "HeadingStyle"
codeBlockStyle = "CodeBlockStyle"
listIndentType = "ListIndentType"
newlineStyle = "NewlineStyle"
whitespaceMode = "WhitespaceMode"
highlightStyle = "HighlightStyle"
linkStyle = "LinkStyle"
outputFormat = "OutputFormat"
urlEscapeStyle = "UrlEscapeStyle"

[crates.e2e.call.overrides.java.nested_types]
preprocessing = "PreprocessingOptions"

[crates.e2e.call.overrides.csharp]
class = "HtmlToMarkdownRs"
function = "Convert"
options_type = "ConversionOptions"

[crates.e2e.call.overrides.csharp.enum_fields]
headingStyle = "HeadingStyle"
codeBlockStyle = "CodeBlockStyle"
listIndentType = "ListIndentType"
newlineStyle = "NewlineStyle"
whitespaceMode = "WhitespaceMode"
highlightStyle = "HighlightStyle"
linkStyle = "LinkStyle"
outputFormat = "OutputFormat"
urlEscapeStyle = "UrlEscapeStyle"

[crates.e2e.call.overrides.csharp.nested_types]
preprocessing = "PreprocessingOptions"

[crates.e2e.call.overrides.php]
module = "HtmlToMarkdown"
class = "HtmlToMarkdown"
function = "convert"
options_type = "ConversionOptions"
options_via = "from_json"

[crates.e2e.call.overrides.php.enum_fields]
headingStyle = "HeadingStyle"
codeBlockStyle = "CodeBlockStyle"
listIndentType = "ListIndentType"
newlineStyle = "NewlineStyle"
whitespaceMode = "WhitespaceMode"
highlightStyle = "HighlightStyle"
linkStyle = "LinkStyle"
outputFormat = "OutputFormat"
urlEscapeStyle = "UrlEscapeStyle"

[crates.e2e.call.overrides.ruby]
module = "html_to_markdown"
# Bare, NOT `HtmlToMarkdownRs::ConversionOptions`. The ruby emitter qualifies `options_type` with
# the binding's own module, so a value that is already namespaced comes out double-qualified as
# `HtmlToMarkdown::HtmlToMarkdownRs::ConversionOptions` -- and `HtmlToMarkdownRs` is defined by
# `ruby.define_module("HtmlToMarkdownRs")` at TOP level, never nested under `HtmlToMarkdown`, so
# that path raises `NameError: uninitialized constant HtmlToMarkdown::HtmlToMarkdownRs`. Measured
# against 0.67.6 output: 227 call sites over 118 files -- 112 generated doc snippets, 5 e2e/ruby
# specs and 1 test_apps/ruby spec. The bare name
# resolves through the `ConversionOptions = HtmlToMarkdownRs.const_get(:ConversionOptions)`
# re-export in packages/ruby/lib/html_to_markdown/native.rb and yields the same class. ~keep
options_type = "ConversionOptions"

[crates.e2e.call.overrides.ruby.enum_fields]
headingStyle = "HeadingStyle"
codeBlockStyle = "CodeBlockStyle"
listIndentType = "ListIndentType"
newlineStyle = "NewlineStyle"
whitespaceMode = "WhitespaceMode"
highlightStyle = "HighlightStyle"
linkStyle = "LinkStyle"
outputFormat = "OutputFormat"
urlEscapeStyle = "UrlEscapeStyle"

[crates.e2e.call.overrides.elixir]
module = "HtmlToMarkdown"
function = "convert"
options_type = "ConversionOptions"
returns_result = true

[crates.e2e.call.overrides.elixir.enum_fields]
headingStyle = "HeadingStyle"
codeBlockStyle = "CodeBlockStyle"
listIndentType = "ListIndentType"
newlineStyle = "NewlineStyle"
whitespaceMode = "WhitespaceMode"
highlightStyle = "HighlightStyle"
linkStyle = "LinkStyle"
outputFormat = "OutputFormat"
urlEscapeStyle = "UrlEscapeStyle"

[crates.e2e.call.overrides.r]
module = "html_to_markdown"
function = "convert"
options_type = "ConversionOptions"
result_is_r_list = true

[crates.e2e.call.overrides.wasm]
options_type = "WasmConversionOptions"
bigint_fields = ["maxImageSize"]

[crates.e2e.call.overrides.wasm.enum_fields]
headingStyle = "WasmHeadingStyle"
codeBlockStyle = "WasmCodeBlockStyle"
listIndentType = "WasmListIndentType"
newlineStyle = "WasmNewlineStyle"
whitespaceMode = "WasmWhitespaceMode"
highlightStyle = "WasmHighlightStyle"
linkStyle = "WasmLinkStyle"
outputFormat = "WasmOutputFormat"
urlEscapeStyle = "WasmUrlEscapeStyle"
preset = "WasmPreprocessingPreset"

[crates.e2e.call.overrides.wasm.nested_types]
preprocessing = "WasmPreprocessingOptions"

[crates.e2e.call.overrides.c]
header = "html_to_markdown.h"
function = "htm_convert"
prefix = "htm"
result_type = "ConversionResult"

[crates.e2e.call.overrides.kotlin_android]
class = "io.xberg.android.HtmlToMarkdown"
function = "convert"
options_type = "ConversionOptions"

[crates.e2e.call.overrides.kotlin_android.enum_fields]
headingStyle = "HeadingStyle"
codeBlockStyle = "CodeBlockStyle"
listIndentType = "ListIndentType"
newlineStyle = "NewlineStyle"
whitespaceMode = "WhitespaceMode"
highlightStyle = "HighlightStyle"
linkStyle = "LinkStyle"
outputFormat = "OutputFormat"
urlEscapeStyle = "UrlEscapeStyle"

[crates.e2e.call.overrides.swift]
module = "HtmlToMarkdown"
function = "convert"
options_type = "ConversionOptions"
options_via = "from_json"

[crates.e2e.call.overrides.dart]
module = "h2m"
function = "convert"
options_type = "ConversionOptions"

[crates.e2e.call.overrides.zig]
module = "html_to_markdown"
function = "convert"
result_is_json_struct = true

[crates.e2e.registry]
github_repo = "https://github.com/xberg-io/html-to-markdown"
output = "test_apps"
categories = ["smoke", "conversion"]

[crates.e2e.registry.packages.python]
name = "html-to-markdown"
version = ">=3.12.4"

[crates.e2e.registry.packages.node]
name = "@xberg-io/html-to-markdown"
version = "^3.12.4"

[crates.e2e.registry.packages.rust]
name = "html-to-markdown-rs"
version = "3.12.4"

[crates.e2e.registry.packages.go]
module = "github.com/xberg-io/html-to-markdown/packages/go/v3"
version = "v3.12.4"

[crates.e2e.registry.packages.java]
name = "io.xberg:html-to-markdown"
version = "3.12.4"

[crates.e2e.registry.packages.csharp]
name = "XbergIo.HtmlToMarkdown"
version = "3.12.4"

[crates.e2e.registry.packages.php]
name = "xberg-io/html-to-markdown"
version = ">=3.12.4"

[crates.e2e.registry.packages.ruby]
name = "html-to-markdown"
version = "~> 3.12.4"

[crates.e2e.registry.packages.elixir]
name = "html_to_markdown"
version = "~> 3.12.4"

[crates.e2e.registry.packages.wasm]
name = "@xberg-io/html-to-markdown-wasm"
version = "^3.12.4"

[crates.e2e.registry.packages.r]
name = "htmltomarkdown"
version = "3.12.4"

[crates.e2e.registry.packages.c]
name = "html-to-markdown-rs-ffi"
version = "3.12.4"

[crates.e2e.registry.packages.kotlin_android]
name = "io.xberg:html-to-markdown-android"
version = "3.12.4"

[crates.e2e.registry.packages.swift]
name = "HtmlToMarkdown"
version = "3.12.4"

[crates.e2e.registry.packages.dart]
name = "h2m"
version = "^3.12.4"

[crates.e2e.registry.packages.zig]
name = "html_to_markdown"
version = "3.12.4"
hash = "html_to_markdown_rs-3.12.4-QtXyW6p0AQARDHIsBKu1XVrpRf5J7ornocJZf2alzaq5"

[crates.e2e.registry.packages.php_ext]
name = "xberg-io/html-to-markdown"
version = "3.12.4"

[crates.e2e.registry.packages.homebrew]
tap = "xberg-io/tap"
cli_formula = "html-to-markdown"
ffi_formula = "libhtml-to-markdown"
version = "3.12.4"

[crates.e2e.packages.rust]
path = "../../crates/html-to-markdown"

[crates.e2e.packages.python]
name = "html-to-markdown"
path = "../../packages/python"

[crates.e2e.packages.node]
name = "@xberg-io/html-to-markdown"
path = "../../crates/html-to-markdown-node"

[crates.e2e.packages.go]
module = "github.com/xberg-io/html-to-markdown/packages/go/v3"
path = "../../packages/go"
version = "v0.0.0"

[crates.e2e.packages.php]
name = "xberg-io/html-to-markdown"
path = "../../crates/html-to-markdown-php"

[crates.e2e.packages.java]
name = "io.xberg:html-to-markdown"
path = "../../packages/java"

[crates.e2e.packages.csharp]
name = "HtmlToMarkdown"
path = "../../packages/csharp/HtmlToMarkdown/HtmlToMarkdown.csproj"

[crates.e2e.packages.ruby]
name = "html-to-markdown"
path = "../../packages/ruby"

[crates.e2e.packages.elixir]
name = "html_to_markdown"
path = "../../packages/elixir"

[crates.e2e.packages.wasm]
name = "@xberg-io/html-to-markdown-wasm"
path = "../../crates/html-to-markdown-wasm"

[crates.e2e.packages.r]
name = "htmltomarkdown"
path = "../../packages/r"

[crates.e2e.packages.c]
name = "html_to_markdown_ffi"
path = "../../crates/html-to-markdown-ffi"

[crates.e2e.packages.kotlin_android]
module = "io.xberg.android"
path = "../../packages/kotlin-android"

[crates.e2e.packages.swift]
name = "HtmlToMarkdown"
path = "../../packages/swift"

[crates.e2e.packages.dart]
name = "h2m"
path = "../../packages/dart"

[crates.e2e.packages.zig]
path = "../../packages/zig"

[crates.e2e.fields]
"metadata.title" = "metadata.document.title"
"metadata.description" = "metadata.document.description"
"metadata.author" = "metadata.document.author"
"metadata.canonical_url" = "metadata.document.canonical_url"
"metadata.headings" = "metadata.headers"
"metadata.open_graph.title" = "metadata.document.open_graph[title]"
"metadata.open_graph.description" = "metadata.document.open_graph[description]"
"metadata.open_graph.image" = "metadata.document.open_graph[image]"
"metadata.open_graph.type" = "metadata.document.open_graph[type]"
"metadata.open_graph.url" = "metadata.document.open_graph[url]"
"metadata.open_graph.site_name" = "metadata.document.open_graph[site_name]"
"metadata.twitter.card" = "metadata.document.twitter_card[card]"
"metadata.twitter.title" = "metadata.document.twitter_card[title]"
"metadata.twitter.description" = "metadata.document.twitter_card[description]"

[crates.e2e.fields_c_types]
"conversion_result.metadata" = "HtmlMetadata"
"conversion_result.document" = "DocumentStructure"
"html_metadata.document" = "DocumentMetadata"

[crates.scaffold]
description = "High-performance HTML to Markdown converter"
license = "MIT"
repository = "https://github.com/xberg-io/html-to-markdown"
authors = ["Na'aman Hirschfeld <naaman@xberg.io>"]
keywords = ["html", "markdown", "converter"]

[crates.readme]
template_dir = "readme_templates"
snippets_dir = "docs-site/src/snippets"
discord_url = "https://discord.gg/xt9WY3GnKR"

[crates.readme.targets.root]
name = "html-to-markdown"
template = "root.md"
output_path = "README.md"

[crates.readme.languages.rust]
name = "Rust"
template = "rust.md"
output_path = "crates/html-to-markdown/README.md"
package_manager = ["cargo"]
package_name = "html-to-markdown-rs"
install_command = "cargo add html-to-markdown-rs"
description = """
High-performance HTML to Markdown converter built with Rust.
This crate is the core engine compiled into every binding and CLI, ensuring identical Markdown
output across every language.
"""
migration_version = ""

[crates.readme.languages.rust.features]
visitor_pattern = true
metadata_extraction = true
cli_proxy = false

[crates.readme.languages.rust.performance]
platform = "Apple M4"
function = "convert()"
note = "Real Wikipedia documents"

[[crates.readme.languages.rust.performance.benchmarks]]
name = "Lists (Timeline)"
size = "129KB"
latency = "0.62ms"
throughput = "208 MB/s"

[[crates.readme.languages.rust.performance.benchmarks]]
name = "Tables (Countries)"
size = "360KB"
latency = "2.02ms"
throughput = "178 MB/s"

[[crates.readme.languages.rust.performance.benchmarks]]
name = "Mixed (Python wiki)"
size = "656KB"
latency = "4.56ms"
throughput = "144 MB/s"

[crates.readme.languages.rust.snippets]
basic_usage = "getting-started/basic_usage.md"
with_options = "getting-started/with_options.md"
metadata_extraction = "metadata/basic_extraction.md"
visitor_basic = "visitor/basic_visitor.md"
table_extraction = "table-extraction/basic_extraction.md"

[crates.readme.languages.python]
name = "Python"
template = "language_package.md"
package_manager = ["pip"]
package_name = "html-to-markdown"
install_command = "pip install html-to-markdown"
description = """
High-performance HTML to Markdown converter with a clean Python API (powered by a Rust core).
The same engine also drives the Node.js, Ruby, PHP, and WebAssembly bindings, so rendered Markdown
stays identical across runtimes. Wheels are published for Linux, macOS, and Windows.
"""
migration_version = ""

[crates.readme.languages.python.features]
visitor_pattern = true
metadata_extraction = true
cli_proxy = false

[crates.readme.languages.python.performance]
platform = "Apple M4"
function = "convert()"
note = "Real Wikipedia documents"

[[crates.readme.languages.python.performance.benchmarks]]
name = "Lists (Timeline)"
size = "129KB"
latency = "0.62ms"
throughput = "208 MB/s"

[[crates.readme.languages.python.performance.benchmarks]]
name = "Tables (Countries)"
size = "360KB"
latency = "2.02ms"
throughput = "178 MB/s"

[[crates.readme.languages.python.performance.benchmarks]]
name = "Mixed (Python wiki)"
size = "656KB"
latency = "4.56ms"
throughput = "144 MB/s"

[crates.readme.languages.python.snippets]
basic_usage = "getting-started/basic_usage.md"
with_options = "getting-started/with_options.md"
metadata_extraction = "metadata/basic_extraction.md"
visitor_basic = "visitor/basic_visitor.md"
table_extraction = "table-extraction/basic_extraction.md"

[crates.readme.languages.typescript]
name = "TypeScript (Node.js)"
template = "language_package.md"
output_path = "crates/html-to-markdown-node/README.md"
package_manager = ["npm", "pnpm", "yarn", "bun"]
package_name = "@xberg-io/html-to-markdown"
install_command = "pnpm add @xberg-io/html-to-markdown"
description = """
High-performance HTML to Markdown converter for Node.js and Bun with full TypeScript support.
Native NAPI-RS bindings to the Rust core with a type-safe API.
"""
migration_version = "3.1.0"

[crates.readme.languages.typescript.features]
visitor_pattern = true
metadata_extraction = true
cli_proxy = false

[crates.readme.languages.typescript.performance]
platform = "Apple M4"
function = "convert()"
note = "Real Wikipedia documents"

[[crates.readme.languages.typescript.performance.benchmarks]]
name = "Lists (Timeline)"
size = "129KB"
latency = "0.58ms"
throughput = "222 MB/s"

[[crates.readme.languages.typescript.performance.benchmarks]]
name = "Tables (Countries)"
size = "360KB"
latency = "1.89ms"
throughput = "190 MB/s"

[[crates.readme.languages.typescript.performance.benchmarks]]
name = "Mixed (Python wiki)"
size = "656KB"
latency = "4.21ms"
throughput = "156 MB/s"

[crates.readme.languages.typescript.snippets]
basic_usage = "getting-started/basic_usage.md"
with_options = "getting-started/with_options.md"
metadata_extraction = "metadata/basic_extraction.md"
visitor_basic = "visitor/basic_visitor.md"
table_extraction = "table-extraction/basic_extraction.md"

[crates.readme.languages.ruby]
name = "Ruby"
template = "language_package.md"
output_path = "packages/ruby/README.md"
package_manager = ["gem", "bundler"]
package_name = "html-to-markdown"
install_command = "gem install html-to-markdown"
description = """
Blazing-fast HTML to Markdown conversion for Ruby, powered by the same Rust engine used by our Python, Node.js, WebAssembly, and PHP packages.
Ship identical Markdown across every runtime while enjoying native extension performance with Magnus bindings.
"""
migration_version = ""

[crates.readme.languages.ruby.features]
visitor_pattern = true
metadata_extraction = true
cli_proxy = true

[crates.readme.languages.ruby.performance]
platform = "Apple M4"
function = "convert()"
note = "Real Wikipedia documents"

[[crates.readme.languages.ruby.performance.benchmarks]]
name = "Lists (Timeline)"
size = "129KB"
latency = "0.71ms"
throughput = "182 MB/s"

[[crates.readme.languages.ruby.performance.benchmarks]]
name = "Tables (Countries)"
size = "360KB"
latency = "2.15ms"
throughput = "167 MB/s"

[[crates.readme.languages.ruby.performance.benchmarks]]
name = "Mixed (Python wiki)"
size = "656KB"
latency = "4.89ms"
throughput = "134 MB/s"

[crates.readme.languages.ruby.snippets]
basic_usage = "getting-started/basic_usage.md"
with_options = "getting-started/with_options.md"
metadata_extraction = "metadata/basic_extraction.md"
visitor_basic = "visitor/basic_visitor.md"
table_extraction = "table-extraction/basic_extraction.md"

[crates.readme.languages.php]
name = "PHP"
template = "language_package.md"
package_manager = ["composer", "pie"]
package_name = "xberg-io/html-to-markdown"
install_command = "pie install xberg-io/html-to-markdown"
description = """
High-performance HTML to Markdown converter with typed PHP bindings powered by a Rust core.
Provides a type-safe API with full PHPStan level 9 support, modern PHP 8.2+ features, and comprehensive metadata extraction.

Note: The package was previously published as `goldziher/html-to-markdown`, which still works for backward compatibility.
"""
migration_version = ""

[crates.readme.languages.php.features]
visitor_pattern = true
metadata_extraction = true
cli_proxy = false

[crates.readme.languages.php.performance]
platform = "Apple M4"
function = "convert()"
note = "Real Wikipedia documents"

[[crates.readme.languages.php.performance.benchmarks]]
name = "Lists (Timeline)"
size = "129KB"
ops_sec = 3346

[[crates.readme.languages.php.performance.benchmarks]]
name = "Tables (Countries)"
size = "360KB"
ops_sec = 973

[[crates.readme.languages.php.performance.benchmarks]]
name = "Medium (Python)"
size = "657KB"
ops_sec = 485

[crates.readme.languages.php.snippets]
basic_usage = "getting-started/basic_usage.md"
with_options = "getting-started/with_options.md"
metadata_extraction = "metadata/basic_extraction.md"
visitor_basic = "visitor/basic_visitor.md"
table_extraction = "table-extraction/basic_extraction.md"

[crates.readme.languages.go]
name = "Go"
template = "language_package.md"
output_path = "packages/go/v3/README.md"
package_manager = ["go"]
package_name = "github.com/xberg-io/html-to-markdown/packages/go/v3"
install_command = "go get github.com/xberg-io/html-to-markdown/packages/go/v3"
description = """
High-performance HTML to Markdown converter with Go bindings to the Rust core library.
Supports automatic downloading of prebuilt FFI libraries for Linux, macOS, and Windows with customizable caching.
"""
migration_version = ""

[crates.readme.languages.go.features]
visitor_pattern = true
metadata_extraction = false
cli_proxy = false

[crates.readme.languages.go.performance]
platform = "Apple M4"
function = "Convert()"
note = "Real Wikipedia documents"

[[crates.readme.languages.go.performance.benchmarks]]
name = "Lists (Timeline)"
size = "129KB"
latency = "0.46ms"
throughput = "277.5 MB/s"

[[crates.readme.languages.go.performance.benchmarks]]
name = "Tables (Countries)"
size = "360KB"
latency = "1.37ms"
throughput = "262.1 MB/s"

[[crates.readme.languages.go.performance.benchmarks]]
name = "Mixed (Python wiki)"
size = "656KB"
latency = "2.75ms"
throughput = "237.9 MB/s"

[crates.readme.languages.go.snippets]
basic_usage = "getting-started/basic_usage.md"
with_options = "getting-started/with_options.md"
metadata_extraction = "metadata/basic_extraction.md"
table_extraction = "table-extraction/basic_extraction.md"

[crates.readme.languages.java]
name = "Java"
template = "language_package.md"
output_path = "packages/java/README.md"
package_manager = ["maven", "gradle"]
package_name = "io.xberg:html-to-markdown"
install_command = """
<dependency>
    <groupId>io.xberg</groupId>
    <artifactId>html-to-markdown</artifactId>
    <version>{{ version }}</version>
</dependency>"""
description = """
High-performance HTML to Markdown converter with Java Panama FFI bindings to the Rust core.
Uses Foreign Function & Memory API for zero-dependency, thread-safe conversion with full metadata extraction support.
"""
migration_version = "3.1.0"

[crates.readme.languages.java.features]
visitor_pattern = true
metadata_extraction = false
cli_proxy = false

[crates.readme.languages.java.performance]
platform = "Apple M4"
function = "convert()"
note = "Real Wikipedia documents"

[[crates.readme.languages.java.performance.benchmarks]]
name = "Lists (Timeline)"
size = "129KB"
ops_sec = 2308
throughput = "291.5 MB/s"

[[crates.readme.languages.java.performance.benchmarks]]
name = "Tables (Countries)"
size = "360KB"
ops_sec = 773
throughput = "272.0 MB/s"

[[crates.readme.languages.java.performance.benchmarks]]
name = "Mixed (Python)"
size = "656KB"
ops_sec = 403
throughput = "258.5 MB/s"

[crates.readme.languages.java.snippets]
basic_usage = "getting-started/basic_usage.md"
with_options = "getting-started/with_options.md"
metadata_extraction = "metadata/basic_extraction.md"
table_extraction = "table-extraction/basic_extraction.md"

[crates.readme.languages.csharp]
name = "C# / .NET"
template = "language_package.md"
output_path = "packages/csharp/README.md"
package_manager = ["nuget"]
package_name = "XbergIo.HtmlToMarkdown"
install_command = "dotnet add package XbergIo.HtmlToMarkdown"
description = """
High-performance HTML to Markdown converter with C#/.NET bindings using P/Invoke to the Rust core.
Provides type-safe record-based APIs for metadata extraction, visitor patterns, and thread-safe concurrent conversion.
"""
migration_version = "3.1.0"

[crates.readme.languages.csharp.features]
visitor_pattern = true
metadata_extraction = false
cli_proxy = false

[crates.readme.languages.csharp.performance]
platform = "Apple M4"
function = "Convert()"
note = "Real Wikipedia documents"

[[crates.readme.languages.csharp.performance.benchmarks]]
name = "Lists (Timeline)"
size = "129KB"
ops_sec = 3111
throughput = "392.9 MB/s"

[[crates.readme.languages.csharp.performance.benchmarks]]
name = "Tables (Countries)"
size = "360KB"
ops_sec = 853
throughput = "300.1 MB/s"

[[crates.readme.languages.csharp.performance.benchmarks]]
name = "Mixed (Python)"
size = "656KB"
ops_sec = 456
throughput = "292.3 MB/s"

[crates.readme.languages.csharp.snippets]
basic_usage = "getting-started/basic_usage.md"
with_options = "getting-started/with_options.md"
table_extraction = "table-extraction/basic_extraction.md"

[crates.readme.languages.elixir]
name = "Elixir"
template = "language_package.md"
output_path = "packages/elixir/README.md"
package_manager = ["mix"]
package_name = "html_to_markdown_rs"
install_command = 'Add {:html_to_markdown, "~> {{ version }}"} to mix.exs deps'
description = """
Elixir bindings for the Rust html-to-markdown engine. The package exposes a fast HTML to Markdown converter implemented with Rustler.
Ship identical Markdown across every runtime while enjoying native performance with Rustler NIF bindings.
"""
migration_version = ""

[crates.readme.languages.elixir.features]
visitor_pattern = true
metadata_extraction = true
cli_proxy = false

[crates.readme.languages.elixir.performance]
platform = "Apple M4"
function = "convert()"
note = "Real Wikipedia documents"

[[crates.readme.languages.elixir.performance.benchmarks]]
name = "Lists (Timeline)"
size = "129KB"
ops_sec = 2547
throughput = "321.7 MB/s"

[[crates.readme.languages.elixir.performance.benchmarks]]
name = "Tables (Countries)"
size = "360KB"
ops_sec = 835
throughput = "293.8 MB/s"

[[crates.readme.languages.elixir.performance.benchmarks]]
name = "Medium (Python)"
size = "656KB"
ops_sec = 439
throughput = "281.5 MB/s"

[[crates.readme.languages.elixir.performance.benchmarks]]
name = "Large (Rust)"
size = "567KB"
ops_sec = 485
throughput = "268.7 MB/s"

[[crates.readme.languages.elixir.performance.benchmarks]]
name = "Small (Intro)"
size = "463KB"
ops_sec = 581
throughput = "262.9 MB/s"

[crates.readme.languages.elixir.snippets]
basic_usage = "getting-started/basic_usage.md"
with_options = "getting-started/with_options.md"
metadata_extraction = "metadata/basic_extraction.md"
visitor_basic = "visitor/basic_visitor.md"
table_extraction = "table-extraction/basic_extraction.md"

[crates.readme.languages.r]
name = "R"
template = "language_package.md"
output_path = "packages/r/README.md"
package_manager = ["install.packages"]
package_name = "htmltomarkdown"
install_command = 'install.packages("htmltomarkdown", repos = "https://xberg-io.r-universe.dev")'
description = """
High-performance HTML to Markdown converter with R bindings powered by a Rust core via extendr.
Ship identical Markdown across every runtime while enjoying native performance with extendr bindings.
"""
migration_version = ""

[crates.readme.languages.r.features]
visitor_pattern = true
metadata_extraction = true
cli_proxy = false

[crates.readme.languages.r.performance]
platform = "Apple M4"
function = "convert()"
note = "Real Wikipedia documents"

[[crates.readme.languages.r.performance.benchmarks]]
name = "Lists (Timeline)"
size = "129KB"
latency = "0.68ms"
throughput = "190 MB/s"

[[crates.readme.languages.r.performance.benchmarks]]
name = "Tables (Countries)"
size = "360KB"
latency = "2.10ms"
throughput = "171 MB/s"

[[crates.readme.languages.r.performance.benchmarks]]
name = "Mixed (Python wiki)"
size = "656KB"
latency = "4.75ms"
throughput = "138 MB/s"

[crates.readme.languages.r.snippets]
basic_usage = "getting-started/basic_usage.md"
with_options = "getting-started/with_options.md"
metadata_extraction = "metadata/basic_extraction.md"
visitor_basic = "visitor/basic_visitor.md"
table_extraction = "table-extraction/basic_extraction.md"

[crates.readme.languages.kotlin_android]
name = "Kotlin (Android)"
template = "language_package.md"
output_path = "packages/kotlin-android/README.md"
package_manager = ["gradle", "maven"]
package_name = "io.xberg:html-to-markdown-android"
install_command = """
implementation("io.xberg:html-to-markdown-android:{{ version }}")"""
description = """
High-performance HTML to Markdown converter for Android, packaged as an AAR with bundled JNI libraries for `arm64-v8a`, `x86_64`, `armeabi-v7a`, and `x86`.
Server-side Kotlin/JVM consumers should use the `io.xberg:html-to-markdown` Java package directly — Kotlin/JVM treats Java classes as native, and Panama FFM is unavailable on Android, which is why Android ships as its own artifact.
"""
migration_version = ""

[crates.readme.languages.kotlin_android.features]
# Visitor pattern is not yet bridged via JNI for Kotlin Android — NodeContext.attributes()
# and intoOwned() both throw UnsupportedOperationException. Keep this false until the
# visitor callback bridge ships; see docs-site/src/snippets/kotlin_android/visitor/basic_visitor.md. ~keep
visitor_pattern = false
metadata_extraction = false
cli_proxy = false

[crates.readme.languages.kotlin_android.performance]
platform = "Apple M4"
function = "convert()"
note = "Real Wikipedia documents"

[crates.readme.languages.kotlin_android.snippets]
basic_usage = "getting-started/basic_usage.md"
with_options = "getting-started/with_options.md"

[crates.readme.languages.swift]
name = "Swift"
template = "language_package.md"
output_path = "packages/swift/README.md"
package_manager = ["spm"]
package_name = "HtmlToMarkdown"
install_command = """
# SwiftPM URL consumption is not yet available (no published XCFramework).
# Build from source against the Rust static library:
git clone https://github.com/xberg-io/html-to-markdown.git
cd html-to-markdown
cargo build -p html-to-markdown-rs-swift --release
swift build --package-path packages/swift"""
description = """
High-performance HTML to Markdown converter for Swift via `swift-bridge`. Distributed through the Swift Package Manager; targets macOS 13+ and iOS 16+.
Ship identical Markdown across every runtime while keeping idiomatic Swift APIs and Codable-friendly result types.
"""
migration_version = ""

[crates.readme.languages.swift.features]
visitor_pattern = true
metadata_extraction = true
cli_proxy = false

[crates.readme.languages.swift.performance]
platform = "Apple M4"
function = "convert()"
note = "Real Wikipedia documents"

[crates.readme.languages.swift.snippets]
basic_usage = "getting-started/basic_usage.md"
with_options = "getting-started/with_options.md"
metadata_extraction = "metadata/basic_extraction.md"

[crates.readme.languages.dart]
name = "Dart"
template = "language_package.md"
output_path = "packages/dart/README.md"
package_manager = ["pub"]
package_name = "h2m"
install_command = "dart pub add h2m"
description = """
High-performance HTML to Markdown converter for Dart via `flutter_rust_bridge`. Pure-Dart binding (no Flutter SDK required) suitable for server and CLI workloads; Flutter apps can also consume it via the standard pub.dev workflow.
Published as `h2m` on pub.dev because `html_to_markdown` and `html-to-markdown` are taken.
"""
migration_version = ""

[crates.readme.languages.dart.features]
visitor_pattern = true
metadata_extraction = true
cli_proxy = false

[crates.readme.languages.dart.performance]
platform = "Apple M4"
function = "convert()"
note = "Real Wikipedia documents"

[crates.readme.languages.dart.snippets]
basic_usage = "getting-started/basic_usage.md"
with_options = "getting-started/with_options.md"
metadata_extraction = "metadata/basic_extraction.md"

[crates.readme.languages.zig]
name = "Zig"
template = "language_package.md"
output_path = "packages/zig/README.md"
package_manager = ["zig"]
package_name = "html_to_markdown_rs"
install_command = """
zig fetch --save https://github.com/xberg-io/html-to-markdown/releases/download/v{{ version }}/zig.tar.gz"""
description = """
High-performance HTML to Markdown converter for Zig, consuming the existing C FFI surface (`libhtml_to_markdown_ffi`) via `linkSystemLibrary`. Idiomatic error sets, slice-based memory management, requires Zig 0.16+.
Distributed via GitHub Releases — pin a tag in `build.zig.zon` and `zig fetch` handles the rest.
"""
migration_version = ""

[crates.readme.languages.zig.features]
visitor_pattern = true
metadata_extraction = false
cli_proxy = false

[crates.readme.languages.zig.performance]
platform = "Apple M4"
function = "convert()"
note = "Real Wikipedia documents"

[crates.readme.languages.zig.snippets]
basic_usage = "getting-started/basic_usage.md"
with_options = "getting-started/with_options.md"

[crates.readme.languages.wasm]
name = "WebAssembly"
template = "language_package.md"
output_path = "crates/html-to-markdown-wasm/README.md"
package_manager = ["npm", "pnpm", "yarn", "bun"]
package_name = "@xberg-io/html-to-markdown-wasm"
install_command = "pnpm add @xberg-io/html-to-markdown-wasm"
description = """
Pure WebAssembly build of html-to-markdown for browsers, Deno, Cloudflare Workers, and other JS
runtimes without a Node.js native-addon ABI. Single `.wasm` artifact loaded via `wasm-bindgen`,
shipped with TypeScript types and dist targets for nodejs, web, bundler, and deno.
"""
migration_version = ""

[crates.readme.languages.wasm.features]
visitor_pattern = false
metadata_extraction = true
cli_proxy = false

[crates.readme.languages.wasm.performance]
platform = "Apple M4"
function = "convert()"
note = "Real Wikipedia documents"

[crates.readme.languages.wasm.snippets]
basic_usage = "getting-started/basic_usage.md"
with_options = "getting-started/with_options.md"
metadata_extraction = "metadata/basic_extraction.md"
visitor_basic = "visitor/basic_visitor.md"
table_extraction = "table-extraction/basic_extraction.md"

[crates.custom_modules]
# No `node` key: the node backend does not consume `custom_modules`, so `node = ["helpers"]`
# emitted nothing and alef reported it as `[warning:unconsumed_config]` on every run. The r
# backend does consume it -- `options` is what produces packages/r/R/options.R. ~keep
r = ["options", "types"]

[[crates.trait_bridges]]
trait_name = "HtmlVisitor"
type_alias = "VisitorHandle"
param_name = "visitor"
bind_via = "options_field"
options_type = "ConversionOptions"
context_type = "NodeContext"
result_type = "VisitResult"
