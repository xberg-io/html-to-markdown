# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:11be6a77953b9ac9e16ee2b6491fc1342f6787bbb7b4e80ffbd40eb54bef5c56
# To regenerate: alef generate
# To verify freshness: alef verify
# E2e tests for category: conversion

test_that("adjacent_emphasis_merged: Adjacent emphasis elements share one delimiter run", {
  result <- convert(html = "<p><i>A</i><i>B</i><i>C</i></p>", options = ConversionOptions$default())
  expect_equal(result$content, "*ABC*\n")
})

test_that("blockquote_code_block_indentation_preserved: Code block inside a blockquote keeps its line indentation", {
  result <- convert(html = "<blockquote><pre><code>line1\n    line2 indented</code></pre></blockquote>", options = ConversionOptions$default())
  expect_equal(result$content, "> ```\n> line1\n>     line2 indented\n> ```\n")
})

test_that("blockquote_multiple_paragraphs: Blockquote with multiple paragraphs has each paragraph prefixed", {
  result <- convert(html = "<blockquote><p>First paragraph.</p><p>Second paragraph.</p></blockquote>", options = ConversionOptions$default())
  expect_true(any(grepl("> First paragraph.", result$content, fixed = TRUE)))
  expect_true(any(grepl("> Second paragraph.", result$content, fixed = TRUE)))
})

test_that("blockquote_nested: Nested blockquote produces double-prefixed lines", {
  result <- convert(html = "<blockquote><p>Outer quote.</p><blockquote><p>Inner quote.</p></blockquote></blockquote>", options = ConversionOptions$default())
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(any(grepl("Outer quote.", result$content, fixed = TRUE)))
  expect_true(any(grepl("Inner quote.", result$content, fixed = TRUE)))
})

test_that("blockquote_nested_list_indentation_preserved: Nested list inside a blockquote keeps its continuation indentation", {
  result <- convert(html = "<blockquote><ul><li>item a<ul><li>sub a1</li></ul></li></ul></blockquote>", options = ConversionOptions$default())
  expect_equal(result$content, "> - item a\n>   * sub a1\n")
})

test_that("blockquote_simple: Simple blockquote", {
  result <- convert(html = "<blockquote><p>Quote text</p></blockquote>", options = ConversionOptions$default())
  expect_true(any(grepl("> Quote text", result$content, fixed = TRUE)))
})

test_that("blockquote_text_then_paragraph_gets_blank_line: Bare text followed by a paragraph inside a blockquote gets a blank-line separator instead of merging into one line", {
  result <- convert(html = "<blockquote>Just text, then <p>a paragraph</p></blockquote>", options = ConversionOptions$default())
  expect_equal(result$content, "> Just text, then\n>\n> a paragraph\n")
})

test_that("blockquote_with_list: Blockquote containing a list preserves list items inside quote", {
  result <- convert(html = "<blockquote><p>Quote intro:</p><ul><li>Point one</li><li>Point two</li></ul></blockquote>", options = ConversionOptions$default())
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(any(grepl("Quote intro:", result$content, fixed = TRUE)))
  expect_true(any(grepl("Point one", result$content, fixed = TRUE)))
  expect_true(any(grepl("Point two", result$content, fixed = TRUE)))
})

test_that("bold_and_italic: Nested bold and italic", {
  result <- convert(html = "<p><strong><em>both</em></strong></p>", options = ConversionOptions$default())
  expect_true(any(grepl("***both***", result$content, fixed = TRUE)))
})

test_that("bold_strong: Strong tag converts to bold", {
  result <- convert(html = "<p><strong>bold</strong></p>", options = ConversionOptions$default())
  expect_true(any(grepl("**bold**", result$content, fixed = TRUE)))
})

test_that("code_block: Code block with language preserves content", {
  result <- convert(html = "<pre><code class=\"language-python\">print('hello')</code></pre>", options = ConversionOptions$default())
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(any(grepl("print('hello')", result$content, fixed = TRUE)))
})

test_that("code_block_no_language: Code block without a language class preserves content", {
  result <- convert(html = "<pre><code>plain code here</code></pre>", options = ConversionOptions$default())
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(any(grepl("plain code here", result$content, fixed = TRUE)))
})

test_that("code_inline_in_paragraph: Inline code element nested inside a paragraph", {
  result <- convert(html = "<p>Call the <code>initialize()</code> method first.</p>", options = ConversionOptions$default())
  expect_true(any(grepl("`initialize()`", result$content, fixed = TRUE)))
})

test_that("code_with_backticks_in_content: Inline code containing backtick characters is properly escaped", {
  result <- convert(html = "<p>Use <code>`backtick` here</code> carefully.</p>", options = ConversionOptions$default())
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(any(grepl("backtick", result$content, fixed = TRUE)))
})

test_that("conversion_autolink_filename_not_autolinked: Bare filename href (no URI scheme) renders as inline link, not autolink (#397)", {
  result <- convert(html = "<a href=\"foobar.png\">foobar.png</a>", options = ConversionOptions$default())
  expect_equal(result$content, "[foobar.png](foobar.png)\n")
})

test_that("conversion_autolink_https_url: Absolute https URL with matching text renders as autolink (#397)", {
  result <- convert(html = "<a href=\"https://example.com\">https://example.com</a>", options = ConversionOptions$default())
  expect_equal(result$content, "<https://example.com>\n")
})

test_that("conversion_autolink_mailto: mailto: link with matching email text renders as autolink (#397)", {
  result <- convert(html = "<a href=\"mailto:a@b.com\">a@b.com</a>", options = ConversionOptions$default())
  expect_equal(result$content, "<a@b.com>\n")
})

test_that("conversion_autolink_mixed_filename_and_url: Mixed filename + URL: only the URL becomes an autolink (#397)", {
  result <- convert(html = "<a href=\"foobar.png\">foobar.png</a> <a href=\"https://www.heise.de\">https://www.heise.de</a>", options = ConversionOptions$default())
  expect_equal(result$content, "[foobar.png](foobar.png) <https://www.heise.de>\n")
})

test_that("conversion_autolink_relative_path_not_autolinked: Relative path href renders as inline link (#397)", {
  result <- convert(html = "<a href=\"/docs/intro.html\">/docs/intro.html</a>", options = ConversionOptions$default())
  expect_equal(result$content, "[/docs/intro.html](/docs/intro.html)\n")
})

test_that("emphasis_mark_highlight: mark tag produces highlighted output", {
  result <- convert(html = "<p><mark>highlighted</mark></p>", options = ConversionOptions$default())
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(any(grepl("highlighted", result$content, fixed = TRUE)))
})

test_that("emphasis_strikethrough_del: del tag converts to GFM strikethrough", {
  result <- convert(html = "<p><del>deleted text</del></p>", options = ConversionOptions$default())
  expect_true(any(grepl("~~deleted text~~", result$content, fixed = TRUE)))
})

test_that("emphasis_strikethrough_s: s tag converts to GFM strikethrough", {
  result <- convert(html = "<p><s>strikethrough</s></p>", options = ConversionOptions$default())
  expect_true(any(grepl("~~strikethrough~~", result$content, fixed = TRUE)))
})

test_that("emphasis_subscript: sub tag content is preserved", {
  result <- convert(html = "<p>H<sub>2</sub>O</p>", options = ConversionOptions$default())
  expect_true(any(grepl("H", result$content, fixed = TRUE)))
  expect_true(any(grepl("2", result$content, fixed = TRUE)))
  expect_true(any(grepl("O", result$content, fixed = TRUE)))
})

test_that("emphasis_superscript: sup tag content is preserved", {
  result <- convert(html = "<p>x<sup>2</sup></p>", options = ConversionOptions$default())
  expect_true(any(grepl("x", result$content, fixed = TRUE)))
  expect_true(any(grepl("2", result$content, fixed = TRUE)))
})

test_that("emphasis_underline_u: u tag content is preserved in output", {
  result <- convert(html = "<p><u>underlined</u></p>", options = ConversionOptions$default())
  expect_true(any(grepl("underlined", result$content, fixed = TRUE)))
})

test_that("form_input_elements: Form input elements produce readable output without form mechanics", {
  result <- convert(html = "<form><label for=\"name\">Name:</label><input type=\"text\" id=\"name\" placeholder=\"Enter name\"></form>", options = ConversionOptions$from_json(jsonlite::toJSON(list("preprocessing" = list("remove_forms" = FALSE)), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(any(grepl("Name", result$content, fixed = TRUE)))
})

test_that("form_select_options: Select element with options produces readable output", {
  result <- convert(html = "<form><label>Color:</label><select><option value=\"red\">Red</option><option value=\"blue\" selected>Blue</option><option value=\"green\">Green</option></select></form>", options = ConversionOptions$from_json(jsonlite::toJSON(list("preprocessing" = list("remove_forms" = FALSE)), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(any(grepl("Color", result$content, fixed = TRUE)))
})

test_that("form_textarea: Textarea element produces readable output", {
  result <- convert(html = "<form><label>Message:</label><textarea>Default text content</textarea></form>", options = ConversionOptions$from_json(jsonlite::toJSON(list("preprocessing" = list("remove_forms" = FALSE)), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(any(grepl("Message", result$content, fixed = TRUE)))
})

test_that("heading_h1: H1 heading", {
  result <- convert(html = "<h1>Heading 1</h1>", options = ConversionOptions$default())
  expect_equal(result$content, "# Heading 1\n")
})

test_that("heading_h2: H2 heading", {
  result <- convert(html = "<h2>Heading 2</h2>", options = ConversionOptions$default())
  expect_equal(result$content, "## Heading 2\n")
})

test_that("heading_h3: H3 heading", {
  result <- convert(html = "<h3>Heading 3</h3>", options = ConversionOptions$default())
  expect_equal(result$content, "### Heading 3\n")
})

test_that("heading_h4: H4 heading", {
  result <- convert(html = "<h4>Heading 4</h4>", options = ConversionOptions$default())
  expect_equal(result$content, "#### Heading 4\n")
})

test_that("heading_h5: H5 heading", {
  result <- convert(html = "<h5>Heading 5</h5>", options = ConversionOptions$default())
  expect_equal(result$content, "##### Heading 5\n")
})

test_that("heading_h6: H6 heading", {
  result <- convert(html = "<h6>Heading 6</h6>", options = ConversionOptions$default())
  expect_equal(result$content, "###### Heading 6\n")
})

test_that("image_figure_figcaption: Figure with figcaption preserves both image and caption", {
  result <- convert(html = "<figure><img src=\"sunset.jpg\" alt=\"A sunset\"><figcaption>Beautiful sunset over the ocean</figcaption></figure>", options = ConversionOptions$default())
  expect_true(any(grepl("![A sunset](sunset.jpg)", result$content, fixed = TRUE)))
  expect_true(any(grepl("Beautiful sunset over the ocean", result$content, fixed = TRUE)))
})

test_that("image_linked: Image inside an anchor produces a linked image", {
  result <- convert(html = "<a href=\"https://example.com\"><img src=\"icon.png\" alt=\"Icon\"></a>", options = ConversionOptions$default())
  expect_true(any(grepl("![Icon](icon.png)", result$content, fixed = TRUE)))
  expect_true(any(grepl("https://example.com", result$content, fixed = TRUE)))
})

test_that("image_no_alt: Image without alt text produces image markdown", {
  result <- convert(html = "<img src=\"banner.jpg\">", options = ConversionOptions$default())
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(any(grepl("banner.jpg", result$content, fixed = TRUE)))
})

test_that("image_simple: Image with alt text", {
  result <- convert(html = "<img src=\"photo.jpg\" alt=\"A photo\">", options = ConversionOptions$default())
  expect_true(any(grepl("![A photo](photo.jpg)", result$content, fixed = TRUE)))
})

test_that("image_with_title: Image with title attribute includes title in output", {
  result <- convert(html = "<img src=\"chart.png\" alt=\"Sales chart\" title=\"Q3 Sales\">", options = ConversionOptions$default())
  expect_true(any(grepl("![Sales chart](chart.png", result$content, fixed = TRUE)))
  expect_true(any(grepl("Q3 Sales", result$content, fixed = TRUE)))
})

test_that("inline_code: Inline code", {
  result <- convert(html = "<p>Use <code>console.log()</code> to debug</p>", options = ConversionOptions$default())
  expect_true(any(grepl("`console.log()`", result$content, fixed = TRUE)))
})

test_that("italic_em: Em tag converts to italic", {
  result <- convert(html = "<p><em>italic</em></p>", options = ConversionOptions$default())
  expect_true(any(grepl("*italic*", result$content, fixed = TRUE)))
})

test_that("line_break_br_tag: Single br tag produces a line break in output", {
  result <- convert(html = "<p>First line.<br>Second line.</p>", options = ConversionOptions$default())
  expect_true(any(grepl("First line.", result$content, fixed = TRUE)))
  expect_true(any(grepl("Second line.", result$content, fixed = TRUE)))
})

test_that("line_break_hr_tag: hr tag produces a horizontal separator between content", {
  result <- convert(html = "<p>Before rule.</p><hr><p>After rule.</p>", options = ConversionOptions$default())
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(any(grepl("Before rule.", result$content, fixed = TRUE)))
  expect_true(any(grepl("After rule.", result$content, fixed = TRUE)))
})

test_that("line_break_multiple_br: Multiple consecutive br tags in sequence", {
  result <- convert(html = "<p>Start.<br><br>End.</p>", options = ConversionOptions$default())
  expect_true(any(grepl("Start.", result$content, fixed = TRUE)))
  expect_true(any(grepl("End.", result$content, fixed = TRUE)))
})

test_that("link_anchor_fragment: Fragment-only anchor link is preserved", {
  result <- convert(html = "<a href=\"#section\">Jump to section</a>", options = ConversionOptions$default())
  expect_true(any(grepl("[Jump to section](#section)", result$content, fixed = TRUE)))
})

test_that("link_empty_href: Link with empty href produces output with the link text", {
  result <- convert(html = "<a href=\"\">No destination</a>", options = ConversionOptions$default())
  expect_true(any(grepl("No destination", result$content, fixed = TRUE)))
})

test_that("link_image_inside: Image inside a link produces a linked image", {
  result <- convert(html = "<a href=\"https://example.com\"><img src=\"logo.png\" alt=\"Logo\"></a>", options = ConversionOptions$default())
  expect_true(any(grepl("![Logo](logo.png)", result$content, fixed = TRUE)))
  expect_true(any(grepl("https://example.com", result$content, fixed = TRUE)))
})

test_that("link_mailto: Mailto link is preserved with mailto: scheme", {
  result <- convert(html = "<a href=\"mailto:user@example.com\">Email us</a>", options = ConversionOptions$default())
  expect_true(any(grepl("mailto:user@example.com", result$content, fixed = TRUE)))
})

test_that("link_simple: Simple link", {
  result <- convert(html = "<a href=\"https://example.com\">Example</a>", options = ConversionOptions$default())
  expect_true(any(grepl("[Example](https://example.com)", result$content, fixed = TRUE)))
})

test_that("link_with_bold_text: Link containing bold text preserves formatting", {
  result <- convert(html = "<a href=\"https://example.com\"><strong>Bold link</strong></a>", options = ConversionOptions$default())
  expect_true(any(grepl("**Bold link**", result$content, fixed = TRUE)))
  expect_true(any(grepl("https://example.com", result$content, fixed = TRUE)))
})

test_that("link_with_title: Link with title attribute", {
  result <- convert(html = "<a href=\"https://example.com\" title=\"Example Site\">Example</a>", options = ConversionOptions$default())
  expect_true(any(grepl("[Example](https://example.com", result$content, fixed = TRUE)))
  expect_true(any(grepl("Example Site", result$content, fixed = TRUE)))
})

test_that("list_definition_dl: Definition list with dt and dd elements", {
  result <- convert(html = "<dl><dt>Term One</dt><dd>Definition of term one.</dd><dt>Term Two</dt><dd>Definition of term two.</dd></dl>", options = ConversionOptions$default())
  expect_true(any(grepl("Term One", result$content, fixed = TRUE)))
  expect_true(any(grepl("Definition of term one.", result$content, fixed = TRUE)))
  expect_true(any(grepl("Term Two", result$content, fixed = TRUE)))
  expect_true(any(grepl("Definition of term two.", result$content, fixed = TRUE)))
})

test_that("list_item_multiple_paragraphs: List item containing multiple paragraphs", {
  result <- convert(html = "<ul><li><p>First paragraph in item.</p><p>Second paragraph in item.</p></li><li>Simple item</li></ul>", options = ConversionOptions$default())
  expect_true(any(grepl("First paragraph in item.", result$content, fixed = TRUE)))
  expect_true(any(grepl("Second paragraph in item.", result$content, fixed = TRUE)))
  expect_true(any(grepl("Simple item", result$content, fixed = TRUE)))
})

test_that("list_mixed_nested: Mixed list: ordered list nested inside unordered list", {
  result <- convert(html = "<ul><li>Item A<ol><li>Sub 1</li><li>Sub 2</li></ol></li><li>Item B</li></ul>", options = ConversionOptions$default())
  expect_true(any(grepl("Item A", result$content, fixed = TRUE)))
  expect_true(any(grepl("Sub 1", result$content, fixed = TRUE)))
  expect_true(any(grepl("Sub 2", result$content, fixed = TRUE)))
  expect_true(any(grepl("Item B", result$content, fixed = TRUE)))
})

test_that("list_nested_ordered: Nested ordered list with two levels of depth", {
  result <- convert(html = "<ol><li>Step 1<ol><li>Step 1a</li><li>Step 1b</li></ol></li><li>Step 2</li></ol>", options = ConversionOptions$default())
  expect_true(any(grepl("Step 1", result$content, fixed = TRUE)))
  expect_true(any(grepl("Step 1a", result$content, fixed = TRUE)))
  expect_true(any(grepl("Step 1b", result$content, fixed = TRUE)))
  expect_true(any(grepl("Step 2", result$content, fixed = TRUE)))
})

test_that("list_nested_unordered: Nested unordered list with two levels of depth", {
  result <- convert(html = "<ul><li>Parent A<ul><li>Child A1</li><li>Child A2</li></ul></li><li>Parent B</li></ul>", options = ConversionOptions$default())
  expect_true(any(grepl("Parent A", result$content, fixed = TRUE)))
  expect_true(any(grepl("Child A1", result$content, fixed = TRUE)))
  expect_true(any(grepl("Child A2", result$content, fixed = TRUE)))
  expect_true(any(grepl("Parent B", result$content, fixed = TRUE)))
})

test_that("list_task_checkboxes: Task list with checked and unchecked checkboxes", {
  result <- convert(html = "<ul><li><input type=\"checkbox\" checked> Done task</li><li><input type=\"checkbox\"> Pending task</li></ul>", options = ConversionOptions$default())
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(any(grepl("Done task", result$content, fixed = TRUE)))
  expect_true(any(grepl("Pending task", result$content, fixed = TRUE)))
})

test_that("ordered_list: Ordered list", {
  result <- convert(html = "<ol><li>First</li><li>Second</li><li>Third</li></ol>", options = ConversionOptions$default())
  expect_true(any(grepl("1. First", result$content, fixed = TRUE)))
  expect_true(any(grepl("2. Second", result$content, fixed = TRUE)))
  expect_true(any(grepl("3. Third", result$content, fixed = TRUE)))
})

test_that("paragraph_multiple: Multiple paragraphs are separated by a blank line", {
  result <- convert(html = "<p>First paragraph.</p><p>Second paragraph.</p>", options = ConversionOptions$default())
  expect_true(any(grepl("First paragraph.", result$content, fixed = TRUE)))
  expect_true(any(grepl("Second paragraph.", result$content, fixed = TRUE)))
})

test_that("paragraph_nested_divs: Text nested inside divs is extracted correctly", {
  result <- convert(html = "<div><div><p>Nested text</p></div></div>", options = ConversionOptions$default())
  expect_true(any(grepl("Nested text", result$content, fixed = TRUE)))
})

test_that("paragraph_simple: Simple paragraph converts to plain text", {
  result <- convert(html = "<p>Hello World</p>", options = ConversionOptions$default())
  expect_equal(result$content, "Hello World\n")
})

test_that("paragraph_with_inline_formatting: Paragraph with bold, italic, and a link", {
  result <- convert(html = "<p>This has <strong>bold</strong>, <em>italic</em>, and a <a href=\"https://example.com\">link</a>.</p>", options = ConversionOptions$default())
  expect_true(any(grepl("**bold**", result$content, fixed = TRUE)))
  expect_true(any(grepl("*italic*", result$content, fixed = TRUE)))
  expect_true(any(grepl("[link](https://example.com)", result$content, fixed = TRUE)))
})

test_that("paragraph_with_line_breaks: Paragraph with br tags produces line breaks in output", {
  result <- convert(html = "<p>Line one.<br>Line two.<br>Line three.</p>", options = ConversionOptions$default())
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(any(grepl("Line one.", result$content, fixed = TRUE)))
  expect_true(any(grepl("Line two.", result$content, fixed = TRUE)))
  expect_true(any(grepl("Line three.", result$content, fixed = TRUE)))
})

test_that("semantic_abbr: Abbreviation element text is preserved", {
  result <- convert(html = "<p>The <abbr title=\"World Wide Web\">WWW</abbr> is global.</p>", options = ConversionOptions$default())
  expect_true(any(grepl("WWW", result$content, fixed = TRUE)))
})

test_that("semantic_article: Article element wrapping content preserves inner content", {
  result <- convert(html = "<article><h2>Article Title</h2><p>Article body.</p></article>", options = ConversionOptions$default())
  expect_true(any(grepl("Article Title", result$content, fixed = TRUE)))
  expect_true(any(grepl("Article body.", result$content, fixed = TRUE)))
})

test_that("semantic_definition_list: Definition list with term and description", {
  result <- convert(html = "<dl><dt>HTML</dt><dd>HyperText Markup Language</dd><dt>CSS</dt><dd>Cascading Style Sheets</dd></dl>", options = ConversionOptions$default())
  expect_true(any(grepl("HTML", result$content, fixed = TRUE)))
  expect_true(any(grepl("HyperText Markup Language", result$content, fixed = TRUE)))
  expect_true(any(grepl("CSS", result$content, fixed = TRUE)))
  expect_true(any(grepl("Cascading Style Sheets", result$content, fixed = TRUE)))
})

test_that("semantic_details_summary: Details and summary elements produce readable output", {
  result <- convert(html = "<details><summary>Click to expand</summary><p>Hidden content here.</p></details>", options = ConversionOptions$default())
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(any(grepl("Click to expand", result$content, fixed = TRUE)))
})

test_that("semantic_hr: Horizontal rule produces a separator in output", {
  result <- convert(html = "<p>Above</p><hr><p>Below</p>", options = ConversionOptions$default())
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(any(grepl("Above", result$content, fixed = TRUE)))
  expect_true(any(grepl("Below", result$content, fixed = TRUE)))
})

test_that("semantic_mark_highlight: Mark tag produces highlighted output", {
  result <- convert(html = "<p>This is <mark>highlighted text</mark> in a sentence.</p>", options = ConversionOptions$default())
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(any(grepl("highlighted text", result$content, fixed = TRUE)))
})

test_that("semantic_section_with_heading: Section element with heading preserves structure", {
  result <- convert(html = "<section><h3>Section Heading</h3><p>Section content.</p></section>", options = ConversionOptions$default())
  expect_true(any(grepl("Section Heading", result$content, fixed = TRUE)))
  expect_true(any(grepl("Section content.", result$content, fixed = TRUE)))
})

test_that("semantic_sub_superscript: Subscript and superscript elements are preserved in output", {
  result <- convert(html = "<p>H<sub>2</sub>O and E=mc<sup>2</sup></p>", options = ConversionOptions$default())
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(any(grepl("H", result$content, fixed = TRUE)))
  expect_true(any(grepl("2", result$content, fixed = TRUE)))
  expect_true(any(grepl("O", result$content, fixed = TRUE)))
  expect_true(any(grepl("E=mc", result$content, fixed = TRUE)))
})

test_that("simple_table: Simple table with header", {
  result <- convert(html = "<table><thead><tr><th>Name</th><th>Age</th></tr></thead><tbody><tr><td>Alice</td><td>30</td></tr></tbody></table>", options = ConversionOptions$default())
  expect_true(any(grepl("Name", result$content, fixed = TRUE)))
  expect_true(any(grepl("Age", result$content, fixed = TRUE)))
  expect_true(any(grepl("Alice", result$content, fixed = TRUE)))
  expect_true(any(grepl("30", result$content, fixed = TRUE)))
  expect_true(any(grepl("|", result$content, fixed = TRUE)))
  expect_true(any(grepl("---", result$content, fixed = TRUE)))
})

test_that("table_empty: Empty table produces no output or minimal output", {
  result <- convert(html = "<table></table>", options = ConversionOptions$default())
  expect_equal(result$content, "")
})

test_that("table_layout_cell_paragraphs_stay_separated: Adjacent paragraphs in a layout-table cell are separated rather than concatenated into one run of text", {
  result <- convert(html = "<table><tr><td><p><b>Alice Example</b></p><p><i>Customer Service</i></p></td><td>Logo</td></tr><tr><td>A</td><td>B</td><td>C</td></tr></table>", options = ConversionOptions$from_json(jsonlite::toJSON(list("compact_tables" = TRUE), auto_unbox = TRUE)))
  expect_false(grepl("**Alice Example***Customer Service*", result$content, fixed = TRUE))
  expect_true(any(grepl("Alice Example", result$content, fixed = TRUE)))
  expect_true(any(grepl("Customer Service", result$content, fixed = TRUE)))
  expect_true(any(grepl("Logo", result$content, fixed = TRUE)))
})

test_that("table_nested_chain_not_misclassified_as_layout: A straight chain of one-nested-table-per-cell tables renders as pipe tables, not a broken layout list", {
  result <- convert(html = "<table><tr><td><table><tr><td><table><tr><td>leaf</td></tr></table></td></tr></table></td></tr></table>", options = ConversionOptions$default())
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(any(grepl("leaf", result$content, fixed = TRUE)))
  expect_true(any(grepl("| ---", result$content, fixed = TRUE)))
})

test_that("table_nested_in_single_cell_renders_separately: A table nested in a single-cell row renders as its own table", {
  result <- convert(html = "<table><tr><th>Summary</th></tr><tr><td>Before<table><tr><th>Item</th><th>Amount</th></tr><tr><td>Alpha</td><td>10</td></tr></table></td></tr></table>", options = ConversionOptions$default())
  expect_true(any(grepl("| Item  | Amount |", result$content, fixed = TRUE)))
  expect_true(any(grepl("| Alpha | 10     |", result$content, fixed = TRUE)))
  expect_false(grepl("\\|", result$content, fixed = TRUE))
})

test_that("table_no_thead: Table without thead uses first row as implied header", {
  result <- convert(html = "<table><tr><td>Product</td><td>Price</td></tr><tr><td>Apple</td><td>1.00</td></tr></table>", options = ConversionOptions$default())
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(any(grepl("Product", result$content, fixed = TRUE)))
  expect_true(any(grepl("Price", result$content, fixed = TRUE)))
  expect_true(any(grepl("Apple", result$content, fixed = TRUE)))
  expect_true(any(grepl("1.00", result$content, fixed = TRUE)))
  expect_true(any(grepl("|", result$content, fixed = TRUE)))
})

test_that("table_pipe_chars_in_content: Table cells containing pipe characters are escaped in output", {
  result <- convert(html = "<table><thead><tr><th>Expression</th><th>Result</th></tr></thead><tbody><tr><td>a | b</td><td>true</td></tr></tbody></table>", options = ConversionOptions$default())
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(any(grepl("Expression", result$content, fixed = TRUE)))
  expect_true(any(grepl("Result", result$content, fixed = TRUE)))
  expect_true(any(grepl("true", result$content, fixed = TRUE)))
})

test_that("table_ragged_row_fewer_cells_than_header: A data row with fewer cells than the header is padded to the declared column count", {
  result <- convert(html = "<table><tr><th>A</th><th>B</th><th>C</th></tr><tr><td>1</td><td>2</td></tr></table>", options = ConversionOptions$default())
  expect_equal(result$content, "| A | B | C |\n| --- | --- | --- |\n| 1 | 2 |   |\n")
})

test_that("table_ragged_row_more_cells_than_header: A data row with more cells than the header widens the separator so no cell is silently dropped", {
  result <- convert(html = "<table><tr><th>A</th><th>B</th></tr><tr><td>1</td><td>2</td><td>3</td></tr></table>", options = ConversionOptions$default())
  expect_equal(result$content, "| A | B |   |\n| --- | --- | --- |\n| 1 | 2 | 3 |\n")
})

test_that("table_with_alignment: Table with column alignment attributes", {
  result <- convert(html = "<table><thead><tr><th align=\"left\">Left</th><th align=\"center\">Center</th><th align=\"right\">Right</th></tr></thead><tbody><tr><td>L</td><td>C</td><td>R</td></tr></tbody></table>", options = ConversionOptions$default())
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(any(grepl("Left", result$content, fixed = TRUE)))
  expect_true(any(grepl("Center", result$content, fixed = TRUE)))
  expect_true(any(grepl("Right", result$content, fixed = TRUE)))
  expect_true(any(grepl("L", result$content, fixed = TRUE)))
  expect_true(any(grepl("C", result$content, fixed = TRUE)))
  expect_true(any(grepl("R", result$content, fixed = TRUE)))
  expect_true(any(grepl("|", result$content, fixed = TRUE)))
})

test_that("table_with_colspan: Table with colspan attribute in a header cell", {
  result <- convert(html = "<table><thead><tr><th colspan=\"2\">Full Name</th></tr></thead><tbody><tr><td>John</td><td>Doe</td></tr></tbody></table>", options = ConversionOptions$default())
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(any(grepl("Full Name", result$content, fixed = TRUE)))
  expect_true(any(grepl("John", result$content, fixed = TRUE)))
  expect_true(any(grepl("Doe", result$content, fixed = TRUE)))
})

test_that("tagged_space_not_duplicated: A whitespace-only inline element contributes exactly one space", {
  result <- convert(html = "<p>A<i> </i>B</p>", options = ConversionOptions$default())
  expect_equal(result$content, "A B\n")
})

test_that("unordered_list: Unordered list", {
  result <- convert(html = "<ul><li>Item 1</li><li>Item 2</li><li>Item 3</li></ul>", options = ConversionOptions$default())
  expect_true(any(grepl("- Item 1", result$content, fixed = TRUE)))
  expect_true(any(grepl("- Item 2", result$content, fixed = TRUE)))
  expect_true(any(grepl("- Item 3", result$content, fixed = TRUE)))
})
