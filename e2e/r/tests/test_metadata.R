# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:4f4f53fb37254412e081692ad2cc2e861135e346919363b8a395e1327f9fdab2
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# E2e tests for category: metadata

test_that("metadata_author_meta: Extract author from <meta name='author'> tag", {
  result <- convert(html = "<html><head><title>Page</title><meta name=\"author\" content=\"Jane Doe\"></head><body><p>Content</p></body></html>", options = ConversionOptions$from_json(jsonlite::toJSON(list("extract_metadata" = TRUE), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_equal(trimws(result$metadata$document$author), "Jane Doe")
})

test_that("metadata_canonical_url: Extract canonical URL from <link rel='canonical'> tag", {
  result <- convert(html = "<html><head><title>Page</title><link rel=\"canonical\" href=\"https://example.com/canonical-page\"></head><body><p>Content</p></body></html>", options = ConversionOptions$from_json(jsonlite::toJSON(list("extract_metadata" = TRUE), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_equal(trimws(result$metadata$document$canonical_url), "https://example.com/canonical-page")
})

test_that("metadata_description_meta: Extract description from <meta name='description'> tag", {
  result <- convert(html = "<html><head><title>Page</title><meta name=\"description\" content=\"This is the page description.\"></head><body><p>Content</p></body></html>", options = ConversionOptions$from_json(jsonlite::toJSON(list("extract_metadata" = TRUE), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_equal(trimws(result$metadata$document$description), "This is the page description.")
})

test_that("metadata_dublin_core: Extract Dublin Core metadata tags", {
  result <- convert(html = "<html><head><title>Scholarly Work</title><meta name=\"DC.title\" content=\"Principles of Knowledge Management\"><meta name=\"DC.creator\" content=\"Dr. Alice Johnson\"><meta name=\"DC.date\" content=\"2023-06-15\"><meta name=\"DC.subject\" content=\"Knowledge Management\"><meta name=\"DC.publisher\" content=\"Academic Press\"></head><body><p>This is a scholarly article.</p></body></html>", options = ConversionOptions$from_json(jsonlite::toJSON(list("extract_metadata" = TRUE), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(grepl("scholarly article", result$content, fixed = TRUE))
})

test_that("metadata_extract_all_images: Extract all images from a document into metadata", {
  result <- convert(html = "<html><head><title>Gallery</title></head><body><img src=\"https://example.com/photo1.jpg\" alt=\"Photo 1\"><img src=\"https://example.com/photo2.png\" alt=\"Photo 2\"><img src=\"/local/image.webp\" alt=\"Local image\"></body></html>", options = ConversionOptions$from_json(jsonlite::toJSON(list("extract_metadata" = TRUE), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(length(result$metadata$images) >= 2)
})

test_that("metadata_extract_all_links: Extract all links from a document into metadata", {
  result <- convert(html = "<html><head><title>Links Page</title></head><body><p>Visit <a href=\"https://example.com\">Example</a> or <a href=\"https://docs.example.com\">Docs</a>.</p><p>Also see <a href=\"/relative/path\">relative link</a> and <a href=\"mailto:hello@example.com\">email us</a>.</p></body></html>", options = ConversionOptions$from_json(jsonlite::toJSON(list("extract_metadata" = TRUE), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(length(result$metadata$links) >= 2)
})

test_that("metadata_headers_hierarchy: Extract heading hierarchy from document into metadata", {
  result <- convert(html = "<html><head><title>Docs</title></head><body><h1>Introduction</h1><h2>Getting Started</h2><h3>Installation</h3><h3>Configuration</h3><h2>Advanced Usage</h2><h3>Custom Options</h3></body></html>", options = ConversionOptions$from_json(jsonlite::toJSON(list("extract_metadata" = TRUE), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(length(result$metadata$headers) >= 5)
})

test_that("metadata_image_type_data_uri_classified: A data-URI image is extracted into metadata.images when extract_metadata is true", {
  result <- convert(html = "<p><img src=\"data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8/5+hHgAHggJ/PchI7wAAAABJRU5ErkJggg==\" alt=\"pixel\"></p>", options = ConversionOptions$from_json(jsonlite::toJSON(list("extract_metadata" = TRUE), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(length(result$metadata$images) >= 1)
})

test_that("metadata_image_type_external_classified: An https image URL is extracted into metadata.images when extract_metadata is true", {
  result <- convert(html = "<p><img src=\"https://example.com/photo.jpg\" alt=\"A photo\"></p>", options = ConversionOptions$from_json(jsonlite::toJSON(list("extract_metadata" = TRUE), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(length(result$metadata$images) >= 1)
})

test_that("metadata_keywords_meta: Extract keywords from <meta name='keywords'> tag", {
  result <- convert(html = "<html><head><title>Page</title><meta name=\"keywords\" content=\"rust, markdown, html, converter\"></head><body><p>Content</p></body></html>", options = ConversionOptions$from_json(jsonlite::toJSON(list("extract_metadata" = TRUE), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(length(result$metadata$document$keywords) >= 1)
})

test_that("metadata_lang_attribute: Extract language from html lang attribute", {
  result <- convert(html = "<html lang=\"es\"><head><title>Spanish Page</title></head><body><h1>Hola Mundo</h1><p>Este es un documento en español.</p></body></html>", options = ConversionOptions$from_json(jsonlite::toJSON(list("extract_metadata" = TRUE), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(grepl("Hola Mundo", result$content, fixed = TRUE))
})

test_that("metadata_link_type_anchor_classified: A fragment anchor link is extracted into metadata.links when extract_metadata is true", {
  result <- convert(html = "<p>Jump to <a href=\"#section\">section</a> below.</p>", options = ConversionOptions$from_json(jsonlite::toJSON(list("extract_metadata" = TRUE), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(length(result$metadata$links) >= 1)
})

test_that("metadata_link_type_email_classified: A mailto link is extracted into metadata.links when extract_metadata is true", {
  result <- convert(html = "<p>Contact <a href=\"mailto:hello@example.com\">us</a> directly.</p>", options = ConversionOptions$from_json(jsonlite::toJSON(list("extract_metadata" = TRUE), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(length(result$metadata$links) >= 1)
})

test_that("metadata_link_type_external_classified: An https link is extracted into metadata.links when extract_metadata is true", {
  result <- convert(html = "<p>See <a href=\"https://example.com\">Example</a> for details.</p>", options = ConversionOptions$from_json(jsonlite::toJSON(list("extract_metadata" = TRUE), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(length(result$metadata$links) >= 1)
})

test_that("metadata_microdata_schema_article: Extract schema.org microdata for Article", {
  result <- convert(html = "<html><head><title>Article</title></head><body><article itemscope itemtype=\"https://schema.org/Article\"><h1 itemprop=\"headline\">Breaking News Today</h1><span itemprop=\"author\">Jane Reporter</span><span itemprop=\"datePublished\">2024-04-22</span><div itemprop=\"articleBody\"><p>The article content goes here with important information about the breaking news story.</p></div></article></body></html>", options = ConversionOptions$from_json(jsonlite::toJSON(list("extract_metadata" = TRUE), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(grepl("Breaking News Today", result$content, fixed = TRUE))
  expect_true(grepl("Jane Reporter", result$content, fixed = TRUE))
  expect_true(grepl("important information", result$content, fixed = TRUE))
})

test_that("metadata_microdata_schema_breadcrumb: Extract schema.org breadcrumb navigation microdata", {
  result <- convert(html = "<html><head><title>Navigation</title></head><body><nav itemscope itemtype=\"https://schema.org/BreadcrumbList\"><span itemprop=\"itemListElement\" itemscope itemtype=\"https://schema.org/ListItem\"><a itemprop=\"item\" href=\"https://example.com\"><span itemprop=\"name\">Home</span></a></span><span itemprop=\"itemListElement\" itemscope itemtype=\"https://schema.org/ListItem\"><a itemprop=\"item\" href=\"https://example.com/products\"><span itemprop=\"name\">Products</span></a></span><span itemprop=\"itemListElement\" itemscope itemtype=\"https://schema.org/ListItem\"><span itemprop=\"name\">Current Page</span></span></nav></body></html>", options = ConversionOptions$from_json(jsonlite::toJSON(list("extract_metadata" = TRUE, "preprocessing" = list("remove_navigation" = FALSE)), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(grepl("Home", result$content, fixed = TRUE))
  expect_true(grepl("Products", result$content, fixed = TRUE))
  expect_true(grepl("Current Page", result$content, fixed = TRUE))
})

test_that("metadata_microdata_schema_organization: Extract schema.org microdata for Organization", {
  result <- convert(html = "<html><head><title>Company</title></head><body><div itemscope itemtype=\"https://schema.org/Organization\"><span itemprop=\"name\">Acme Corp</span><span itemprop=\"foundingDate\">2020</span><span itemprop=\"url\">https://acmecorp.example.com</span><span itemprop=\"logo\">https://acmecorp.example.com/logo.png</span></div></body></html>", options = ConversionOptions$from_json(jsonlite::toJSON(list("extract_metadata" = TRUE), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(grepl("Acme Corp", result$content, fixed = TRUE))
  expect_true(grepl("2020", result$content, fixed = TRUE))
})

test_that("metadata_microdata_schema_person: Extract schema.org microdata for Person", {
  result <- convert(html = "<html><head><title>Contact</title></head><body><div itemscope itemtype=\"https://schema.org/Person\"><span itemprop=\"name\">John Smith</span><span itemprop=\"email\">john@example.com</span><span itemprop=\"telephone\">+1-555-0100</span></div></body></html>", options = ConversionOptions$from_json(jsonlite::toJSON(list("extract_metadata" = TRUE), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(grepl("John Smith", result$content, fixed = TRUE))
  expect_true(grepl("john@example.com", result$content, fixed = TRUE))
})

test_that("metadata_microdata_schema_product: Extract schema.org microdata for Product", {
  result <- convert(html = "<html><head><title>Product</title></head><body><div itemscope itemtype=\"https://schema.org/Product\"><h1 itemprop=\"name\">Awesome Widget</h1><span itemprop=\"description\">The best widget on the market</span><span itemprop=\"price\">29.99</span><span itemprop=\"priceCurrency\">USD</span><img itemprop=\"image\" src=\"widget.jpg\" alt=\"Widget\"><span itemprop=\"ratingValue\">4.5</span></div></body></html>", options = ConversionOptions$from_json(jsonlite::toJSON(list("extract_metadata" = TRUE), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(grepl("Awesome Widget", result$content, fixed = TRUE))
  expect_true(grepl("best widget", result$content, fixed = TRUE))
  expect_true(grepl("29.99", result$content, fixed = TRUE))
})

test_that("metadata_text_direction_ltr: Extract text direction from lang attribute on html element", {
  result <- convert(html = "<html lang=\"en\" dir=\"ltr\"><head><title>LTR Document</title></head><body><p>This is left-to-right text.</p></body></html>", options = ConversionOptions$from_json(jsonlite::toJSON(list("extract_metadata" = TRUE), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(grepl("left-to-right text", result$content, fixed = TRUE))
})

test_that("metadata_text_direction_rtl: Extract right-to-left text direction", {
  result <- convert(html = "<html lang=\"ar\" dir=\"rtl\"><head><title>RTL Document</title></head><body><p>This is right-to-left text.</p></body></html>", options = ConversionOptions$from_json(jsonlite::toJSON(list("extract_metadata" = TRUE), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(grepl("right-to-left text", result$content, fixed = TRUE))
})

test_that("metadata_title_tag: Extract title from <title> tag", {
  result <- convert(html = "<html><head><title>My Page</title></head><body><p>Content</p></body></html>", options = ConversionOptions$from_json(jsonlite::toJSON(list("extract_metadata" = TRUE), auto_unbox = TRUE)))
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_equal(trimws(result$metadata$document$title), "My Page")
})

test_that("og_basic_tags: Extract og:title, og:description, and og:image from Open Graph meta tags", {
  result <- convert(html = "<html><head><title>Fallback Title</title><meta property=\"og:title\" content=\"OG Title\"><meta property=\"og:description\" content=\"OG description text.\"><meta property=\"og:image\" content=\"https://example.com/image.jpg\"></head><body><p>Content</p></body></html>", options = ConversionOptions$default())
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_equal(trimws(result$metadata$document$open_graph[["title"]]), "OG Title")
  expect_equal(trimws(result$metadata$document$open_graph[["description"]]), "OG description text.")
  expect_equal(trimws(result$metadata$document$open_graph[["image"]]), "https://example.com/image.jpg")
})

test_that("og_multiple_tags: Extract multiple Open Graph tags including type, url, and site_name", {
  result <- convert(html = "<html><head><meta property=\"og:title\" content=\"Article Title\"><meta property=\"og:type\" content=\"article\"><meta property=\"og:url\" content=\"https://example.com/article\"><meta property=\"og:site_name\" content=\"Example Site\"><meta property=\"og:description\" content=\"An interesting article.\"><meta property=\"og:image\" content=\"https://example.com/article.jpg\"></head><body><article><p>Article content here.</p></article></body></html>", options = ConversionOptions$default())
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_equal(trimws(result$metadata$document$open_graph[["title"]]), "Article Title")
  expect_equal(trimws(result$metadata$document$open_graph[["type"]]), "article")
  expect_equal(trimws(result$metadata$document$open_graph[["url"]]), "https://example.com/article")
  expect_equal(trimws(result$metadata$document$open_graph[["site_name"]]), "Example Site")
})

test_that("structured_data_json_ld: JSON-LD script tag is stripped from output (security) but metadata may be extracted", {
  result <- convert(html = "<html><head><title>Article</title><script type=\"application/ld+json\">{\"@context\":\"https://schema.org\",\"@type\":\"Article\",\"headline\":\"My Article\",\"author\":{\"@type\":\"Person\",\"name\":\"Jane Doe\"},\"datePublished\":\"2024-01-15\"}</script></head><body><h1>My Article</h1><p>Article body text.</p></body></html>", options = ConversionOptions$default())
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(any(grepl("My Article", result$content, fixed = TRUE)))
})

test_that("structured_data_multiple_json_ld: Multiple JSON-LD blocks are all stripped from output", {
  result <- convert(html = "<html><head><title>Shop Page</title><script type=\"application/ld+json\">{\"@context\":\"https://schema.org\",\"@type\":\"Product\",\"name\":\"Widget\",\"price\":\"9.99\"}</script><script type=\"application/ld+json\">{\"@context\":\"https://schema.org\",\"@type\":\"BreadcrumbList\",\"itemListElement\":[{\"@type\":\"ListItem\",\"position\":1,\"name\":\"Home\"}]}</script></head><body><h1>Widget</h1><p>A great widget for all purposes.</p></body></html>", options = ConversionOptions$default())
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_true(any(grepl("Widget", result$content, fixed = TRUE)))
})

test_that("twitter_card_tags: Extract Twitter card meta tags", {
  result <- convert(html = "<html><head><meta name=\"twitter:card\" content=\"summary_large_image\"><meta name=\"twitter:site\" content=\"@examplesite\"><meta name=\"twitter:title\" content=\"Twitter Card Title\"><meta name=\"twitter:description\" content=\"Twitter card description.\"><meta name=\"twitter:image\" content=\"https://example.com/twitter-image.jpg\"></head><body><p>Content</p></body></html>", options = ConversionOptions$default())
  expect_true(if (is.character(result$content)) length(result$content) > 0 && any(nchar(result$content) > 0) else length(result$content) > 0)
  expect_equal(trimws(result$metadata$document$twitter_card[["card"]]), "summary_large_image")
  expect_equal(trimws(result$metadata$document$twitter_card[["title"]]), "Twitter Card Title")
  expect_equal(trimws(result$metadata$document$twitter_card[["description"]]), "Twitter card description.")
})
