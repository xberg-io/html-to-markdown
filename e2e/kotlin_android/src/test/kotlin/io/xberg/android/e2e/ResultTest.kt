// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:905d7e005473f991fc55f2e1f4bbfb1eab460cbf330d8fec02ea14455495f4ff
// To regenerate: alef e2e generate
// To verify freshness: alef verify
package io.xberg.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import io.xberg.android.HtmlToMarkdown
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.fasterxml.jackson.module.kotlin.registerKotlinModule
import kotlin.time.Duration.Companion.milliseconds
import io.xberg.android.ConversionOptions

/** E2e tests for category: result. */
class ResultTest {

    companion object {
        init {
            try {
                System.loadLibrary("htm_jni")
            } catch (e: UnsatisfiedLinkError) {
                System.err.println("Failed to load htm_jni library: ${e.message}")
                val libPath = System.getProperty("java.library.path")
                System.err.println("java.library.path: $libPath")
                throw e
            }
        }
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).registerModule(
            com.fasterxml.jackson.databind.module.SimpleModule()
                .addSerializer(kotlin.time.Duration::class.java, object : com.fasterxml.jackson.databind.JsonSerializer<kotlin.time.Duration>() {
                    override fun serialize(value: kotlin.time.Duration, gen: com.fasterxml.jackson.core.JsonGenerator, serializers: com.fasterxml.jackson.databind.SerializerProvider) {
                        gen.writeNumber(value.inWholeMilliseconds)
                    }
                })
                .addDeserializer(kotlin.time.Duration::class.java, object : com.fasterxml.jackson.databind.JsonDeserializer<kotlin.time.Duration>() {
                    override fun deserialize(p: com.fasterxml.jackson.core.JsonParser, ctxt: com.fasterxml.jackson.databind.DeserializationContext): kotlin.time.Duration {
                        return p.longValue.milliseconds
                    }
                })
        ).registerKotlinModule().setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testResultTablesEmptyWhenNoTables(): Unit = runBlocking {
        // Result tables array is empty when input has no tables
        val options = MAPPER.readValue("{\"include_document_structure\":true}", ConversionOptions::class.java)
        val result = io.xberg.android.HtmlToMarkdown.convert("<p>No tables here</p>", options)
        assertTrue(result.content != null, "expected non-empty value")
        assertEquals(0, result.tables.size, "expected exactly 0 elements")
    }

    @Test
    fun testResultTablesMultiple(): Unit = runBlocking {
        // Multiple tables each appear in the tables array
        val options = MAPPER.readValue("{\"include_document_structure\":true}", ConversionOptions::class.java)
        val result = io.xberg.android.HtmlToMarkdown.convert("<table><tr><th>A</th></tr><tr><td>1</td></tr></table><p>Between</p><table><tr><th>B</th></tr><tr><td>2</td></tr></table>", options)
        assertTrue(result.tables.size >= 2, "expected at least 2 elements")
    }

    @Test
    fun testResultTablesSimple(): Unit = runBlocking {
        // Simple table populates the tables array in result
        val options = MAPPER.readValue("{\"include_document_structure\":true}", ConversionOptions::class.java)
        val result = io.xberg.android.HtmlToMarkdown.convert("<table><thead><tr><th>Name</th><th>Age</th></tr></thead><tbody><tr><td>Alice</td><td>30</td></tr></tbody></table>", options)
        assertTrue(result.content != null, "expected non-empty value")
        assertTrue(result.tables.size >= 1, "expected at least 1 elements")
    }

    @Test
    fun testResultTablesWithoutStructureFlag(): Unit = runBlocking {
        // Tables array is empty when includeDocumentStructure is false
        val result = io.xberg.android.HtmlToMarkdown.convert("<table><tr><th>X</th></tr><tr><td>Y</td></tr></table>", null)
        assertTrue(result.content != null, "expected non-empty value")
        assertEquals(0, result.tables.size, "expected exactly 0 elements")
    }

    @Test
    fun testResultWarningKindImageExtractionFailed(): Unit = runBlocking {
        // A malformed data URI with extract_images enabled produces an ImageExtractionFailed warning
        val options = MAPPER.readValue("{\"extract_images\":true}", ConversionOptions::class.java)
        val result = io.xberg.android.HtmlToMarkdown.convert("<p>Text<img src=\"data:BADMIME\" alt=\"broken\">end</p>", options)
        assertTrue(result.content != null, "expected non-empty value")
        assertTrue(result.warnings.size >= 1, "expected at least 1 elements")
    }

    @Test
    fun testResultWarningsEmptyForCleanInput(): Unit = runBlocking {
        // Warnings array is empty for well-formed HTML without problematic content
        val result = io.xberg.android.HtmlToMarkdown.convert("<h1>Title</h1><p>Clean content with <a href='https://example.com'>a link</a>.</p>", null)
        assertTrue(result.content != null, "expected non-empty value")
        assertEquals(0, result.warnings.size, "expected exactly 0 elements")
    }

    @Test
    fun testResultWarningsEmptyForComplexInput(): Unit = runBlocking {
        // Warnings array is empty for complex but valid HTML
        val result = io.xberg.android.HtmlToMarkdown.convert("<article><h1>Article</h1><p>Paragraph with <strong>bold</strong> and <em>italic</em>.</p><table><tr><th>Col</th></tr><tr><td>Val</td></tr></table><ul><li>Item 1</li><li>Item 2</li></ul></article>", null)
        assertTrue(result.content != null, "expected non-empty value")
        assertEquals(0, result.warnings.size, "expected exactly 0 elements")
    }

    @Test
    fun testResultWarningsEmptyForMalformedHtml(): Unit = runBlocking {
        // Warnings array is empty even for malformed HTML (parser is lenient)
        val result = io.xberg.android.HtmlToMarkdown.convert("<p>Unclosed paragraph<div>Mixed nesting</p></div>", null)
        assertTrue(result.content != null, "expected non-empty value")
        assertEquals(0, result.warnings.size, "expected exactly 0 elements")
    }

}
