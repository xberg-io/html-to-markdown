// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:9e73d29e5ad7b807c9227a3e1c6917dbb006fdcd742429737c103bdd95d1e30c
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package dev.kreuzberg.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import dev.kreuzberg.android.HtmlToMarkdownRs
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.fasterxml.jackson.module.kotlin.registerKotlinModule
import dev.kreuzberg.android.ConversionOptions

/** E2e tests for category: result. */
class ResultTest {

    companion object {
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).registerKotlinModule().setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testResultTablesEmptyWhenNoTables() = runBlocking {
        // Result tables array is empty when input has no tables
        val options = MAPPER.readValue("{\"include_document_structure\":true}", ConversionOptions::class.java)
        val result = dev.kreuzberg.android.HtmlToMarkdownRs.convert("<p>No tables here</p>", options)
        assertTrue(result.content != null, "expected non-empty value")
        assertEquals(0, result.tables.size, "expected exactly 0 elements")
    }

    @Test
    fun testResultTablesMultiple() = runBlocking {
        // Multiple tables each appear in the tables array
        val options = MAPPER.readValue("{\"include_document_structure\":true}", ConversionOptions::class.java)
        val result = dev.kreuzberg.android.HtmlToMarkdownRs.convert("<table><tr><th>A</th></tr><tr><td>1</td></tr></table><p>Between</p><table><tr><th>B</th></tr><tr><td>2</td></tr></table>", options)
        assertTrue(result.tables.size >= 2, "expected at least 2 elements")
    }

    @Test
    fun testResultTablesSimple() = runBlocking {
        // Simple table populates the tables array in result
        val options = MAPPER.readValue("{\"include_document_structure\":true}", ConversionOptions::class.java)
        val result = dev.kreuzberg.android.HtmlToMarkdownRs.convert("<table><thead><tr><th>Name</th><th>Age</th></tr></thead><tbody><tr><td>Alice</td><td>30</td></tr></tbody></table>", options)
        assertTrue(result.content != null, "expected non-empty value")
        assertTrue(result.tables.size >= 1, "expected at least 1 elements")
    }

    @Test
    fun testResultTablesWithoutStructureFlag() = runBlocking {
        // Tables array is empty when includeDocumentStructure is false
        val result = dev.kreuzberg.android.HtmlToMarkdownRs.convert("<table><tr><th>X</th></tr><tr><td>Y</td></tr></table>", ConversionOptions())
        assertTrue(result.content != null, "expected non-empty value")
        assertEquals(0, result.tables.size, "expected exactly 0 elements")
    }

    @Test
    fun testResultWarningKindImageExtractionFailed() = runBlocking {
        // A malformed data URI with extract_images enabled produces an ImageExtractionFailed warning
        val options = MAPPER.readValue("{\"extract_images\":true}", ConversionOptions::class.java)
        val result = dev.kreuzberg.android.HtmlToMarkdownRs.convert("<p>Text<img src=\"data:BADMIME\" alt=\"broken\">end</p>", options)
        assertTrue(result.content != null, "expected non-empty value")
        assertTrue(result.warnings.size >= 1, "expected at least 1 elements")
    }

    @Test
    fun testResultWarningsEmptyForCleanInput() = runBlocking {
        // Warnings array is empty for well-formed HTML without problematic content
        val result = dev.kreuzberg.android.HtmlToMarkdownRs.convert("<h1>Title</h1><p>Clean content with <a href='https://example.com'>a link</a>.</p>", ConversionOptions())
        assertTrue(result.content != null, "expected non-empty value")
        assertEquals(0, result.warnings.size, "expected exactly 0 elements")
    }

    @Test
    fun testResultWarningsEmptyForComplexInput() = runBlocking {
        // Warnings array is empty for complex but valid HTML
        val result = dev.kreuzberg.android.HtmlToMarkdownRs.convert("<article><h1>Article</h1><p>Paragraph with <strong>bold</strong> and <em>italic</em>.</p><table><tr><th>Col</th></tr><tr><td>Val</td></tr></table><ul><li>Item 1</li><li>Item 2</li></ul></article>", ConversionOptions())
        assertTrue(result.content != null, "expected non-empty value")
        assertEquals(0, result.warnings.size, "expected exactly 0 elements")
    }

    @Test
    fun testResultWarningsEmptyForMalformedHtml() = runBlocking {
        // Warnings array is empty even for malformed HTML (parser is lenient)
        val result = dev.kreuzberg.android.HtmlToMarkdownRs.convert("<p>Unclosed paragraph<div>Mixed nesting</p></div>", ConversionOptions())
        assertTrue(result.content != null, "expected non-empty value")
        assertEquals(0, result.warnings.size, "expected exactly 0 elements")
    }

}
