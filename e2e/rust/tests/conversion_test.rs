// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3ae9a77283f26221827ddcc229af05718385bec62abfd1e6c411b54449c90427
// To regenerate: alef generate
// To verify freshness: alef verify
//! E2e tests for category: conversion

use html_to_markdown_rs::ConversionOptions;
use html_to_markdown_rs::convert;

#[test]
fn test_adjacent_code_spans_merged() {
    // Adjacent code spans merge instead of colliding backticks
    let html = r#"<p><code>A</code><code>B</code></p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"`AB`
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_adjacent_emphasis_merged() {
    // Adjacent emphasis elements share one delimiter run
    let html = r#"<p><i>A</i><i>B</i><i>C</i></p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"*ABC*
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_adjacent_inserted_merged() {
    // Adjacent inserted elements share one delimiter run
    let html = r#"<p><ins>A</ins><ins>B</ins></p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"==AB==
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_adjacent_strikethrough_merged() {
    // Adjacent strikethrough elements share one delimiter run
    let html = r#"<p><del>A</del><del>B</del></p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"~~AB~~
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_adjacent_variable_merged() {
    // Adjacent variable elements share one delimiter run
    let html = r#"<p><var>A</var><var>B</var></p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"*AB*
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_blockquote_code_block_indentation_preserved() {
    // Code block inside a blockquote keeps its line indentation
    let html = r#"<blockquote><pre><code>line1
    line2 indented</code></pre></blockquote>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"> ```
> line1
>     line2 indented
> ```
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_blockquote_multiple_paragraphs() {
    // Blockquote with multiple paragraphs has each paragraph prefixed
    let html = r#"<blockquote><p>First paragraph.</p><p>Second paragraph.</p></blockquote>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"> First paragraph."#),
        "expected to contain: {}",
        r#"> First paragraph."#
    );
    assert!(
        _content.contains(r#"> Second paragraph."#),
        "expected to contain: {}",
        r#"> Second paragraph."#
    );
}

#[test]
fn test_blockquote_nested() {
    // Nested blockquote produces double-prefixed lines
    let html = r#"<blockquote><p>Outer quote.</p><blockquote><p>Inner quote.</p></blockquote></blockquote>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(!_content.is_empty(), "expected non-empty value");
    assert!(
        _content.contains(r#"Outer quote."#),
        "expected to contain: {}",
        r#"Outer quote."#
    );
    assert!(
        _content.contains(r#"Inner quote."#),
        "expected to contain: {}",
        r#"Inner quote."#
    );
}

#[test]
fn test_blockquote_nested_list_indentation_preserved() {
    // Nested list inside a blockquote keeps its continuation indentation
    let html = r#"<blockquote><ul><li>item a<ul><li>sub a1</li></ul></li></ul></blockquote>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"> - item a
>   * sub a1
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_blockquote_simple() {
    // Simple blockquote
    let html = r#"<blockquote><p>Quote text</p></blockquote>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"> Quote text"#),
        "expected to contain: {}",
        r#"> Quote text"#
    );
}

#[test]
fn test_blockquote_text_then_paragraph_gets_blank_line() {
    // Bare text followed by a paragraph inside a blockquote gets a blank-line separator instead of merging into one line
    let html = r#"<blockquote>Just text, then <p>a paragraph</p></blockquote>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"> Just text, then
>
> a paragraph
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_blockquote_with_list() {
    // Blockquote containing a list preserves list items inside quote
    let html = r#"<blockquote><p>Quote intro:</p><ul><li>Point one</li><li>Point two</li></ul></blockquote>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(!_content.is_empty(), "expected non-empty value");
    assert!(
        _content.contains(r#"Quote intro:"#),
        "expected to contain: {}",
        r#"Quote intro:"#
    );
    assert!(
        _content.contains(r#"Point one"#),
        "expected to contain: {}",
        r#"Point one"#
    );
    assert!(
        _content.contains(r#"Point two"#),
        "expected to contain: {}",
        r#"Point two"#
    );
}

#[test]
fn test_bold_and_italic() {
    // Nested bold and italic
    let html = r#"<p><strong><em>both</em></strong></p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"***both***"#),
        "expected to contain: {}",
        r#"***both***"#
    );
}

#[test]
fn test_bold_strong() {
    // Strong tag converts to bold
    let html = r#"<p><strong>bold</strong></p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"**bold**"#),
        "expected to contain: {}",
        r#"**bold**"#
    );
}

#[test]
fn test_code_block() {
    // Code block with language preserves content
    let html = r#"<pre><code class="language-python">print('hello')</code></pre>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(!_content.is_empty(), "expected non-empty value");
    assert!(
        _content.contains(r#"print('hello')"#),
        "expected to contain: {}",
        r#"print('hello')"#
    );
}

#[test]
fn test_code_block_no_language() {
    // Code block without a language class preserves content
    let html = r#"<pre><code>plain code here</code></pre>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(!_content.is_empty(), "expected non-empty value");
    assert!(
        _content.contains(r#"plain code here"#),
        "expected to contain: {}",
        r#"plain code here"#
    );
}

#[test]
fn test_code_inline_in_paragraph() {
    // Inline code element nested inside a paragraph
    let html = r#"<p>Call the <code>initialize()</code> method first.</p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"`initialize()`"#),
        "expected to contain: {}",
        r#"`initialize()`"#
    );
}

#[test]
fn test_code_span_suppresses_nested_inline_markers() {
    // Inline markers inside a code span stay literal content
    let html = r#"<p><code>a<em>b</em><ins>c</ins></code></p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"`abc`
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_code_with_backticks_in_content() {
    // Inline code containing backtick characters is properly escaped
    let html = r#"<p>Use <code>`backtick` here</code> carefully.</p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(!_content.is_empty(), "expected non-empty value");
    assert!(
        _content.contains(r#"backtick"#),
        "expected to contain: {}",
        r#"backtick"#
    );
}

#[test]
fn test_conversion_autolink_filename_not_autolinked() {
    // Bare filename href (no URI scheme) renders as inline link, not autolink (#397)
    let html = r#"<a href="foobar.png">foobar.png</a>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"[foobar.png](foobar.png)
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_conversion_autolink_https_url() {
    // Absolute https URL with matching text renders as autolink (#397)
    let html = r#"<a href="https://example.com">https://example.com</a>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"<https://example.com>
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_conversion_autolink_mailto() {
    // mailto: link with matching email text renders as autolink (#397)
    let html = r#"<a href="mailto:a@b.com">a@b.com</a>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"<a@b.com>
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_conversion_autolink_mixed_filename_and_url() {
    // Mixed filename + URL: only the URL becomes an autolink (#397)
    let html = r#"<a href="foobar.png">foobar.png</a> <a href="https://www.heise.de">https://www.heise.de</a>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"[foobar.png](foobar.png) <https://www.heise.de>
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_conversion_autolink_relative_path_not_autolinked() {
    // Relative path href renders as inline link (#397)
    let html = r#"<a href="/docs/intro.html">/docs/intro.html</a>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"[/docs/intro.html](/docs/intro.html)
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_emphasis_mark_highlight() {
    // mark tag produces highlighted output
    let html = r#"<p><mark>highlighted</mark></p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(!_content.is_empty(), "expected non-empty value");
    assert!(
        _content.contains(r#"highlighted"#),
        "expected to contain: {}",
        r#"highlighted"#
    );
}

#[test]
fn test_emphasis_strikethrough_del() {
    // del tag converts to GFM strikethrough
    let html = r#"<p><del>deleted text</del></p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"~~deleted text~~"#),
        "expected to contain: {}",
        r#"~~deleted text~~"#
    );
}

#[test]
fn test_emphasis_strikethrough_s() {
    // s tag converts to GFM strikethrough
    let html = r#"<p><s>strikethrough</s></p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"~~strikethrough~~"#),
        "expected to contain: {}",
        r#"~~strikethrough~~"#
    );
}

#[test]
fn test_emphasis_subscript() {
    // sub tag content is preserved
    let html = r#"<p>H<sub>2</sub>O</p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(_content.contains(r#"H"#), "expected to contain: {}", r#"H"#);
    assert!(_content.contains(r#"2"#), "expected to contain: {}", r#"2"#);
    assert!(_content.contains(r#"O"#), "expected to contain: {}", r#"O"#);
}

#[test]
fn test_emphasis_superscript() {
    // sup tag content is preserved
    let html = r#"<p>x<sup>2</sup></p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(_content.contains(r#"x"#), "expected to contain: {}", r#"x"#);
    assert!(_content.contains(r#"2"#), "expected to contain: {}", r#"2"#);
}

#[test]
fn test_emphasis_underline_u() {
    // u tag content is preserved in output
    let html = r#"<p><u>underlined</u></p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"underlined"#),
        "expected to contain: {}",
        r#"underlined"#
    );
}

#[test]
fn test_form_input_elements() {
    // Form input elements produce readable output without form mechanics
    let html = r#"<form><label for="name">Name:</label><input type="text" id="name" placeholder="Enter name"></form>"#;
    let options_json: serde_json::Value = serde_json::from_str(r#"{"preprocessing":{"remove_forms":false}}"#).unwrap();
    let options: ConversionOptions = serde_json::from_value(options_json).unwrap();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(!_content.is_empty(), "expected non-empty value");
    assert!(_content.contains(r#"Name"#), "expected to contain: {}", r#"Name"#);
}

#[test]
fn test_form_select_options() {
    // Select element with options produces readable output
    let html = r#"<form><label>Color:</label><select><option value="red">Red</option><option value="blue" selected>Blue</option><option value="green">Green</option></select></form>"#;
    let options_json: serde_json::Value = serde_json::from_str(r#"{"preprocessing":{"remove_forms":false}}"#).unwrap();
    let options: ConversionOptions = serde_json::from_value(options_json).unwrap();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(!_content.is_empty(), "expected non-empty value");
    assert!(_content.contains(r#"Color"#), "expected to contain: {}", r#"Color"#);
}

#[test]
fn test_form_textarea() {
    // Textarea element produces readable output
    let html = r#"<form><label>Message:</label><textarea>Default text content</textarea></form>"#;
    let options_json: serde_json::Value = serde_json::from_str(r#"{"preprocessing":{"remove_forms":false}}"#).unwrap();
    let options: ConversionOptions = serde_json::from_value(options_json).unwrap();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(!_content.is_empty(), "expected non-empty value");
    assert!(_content.contains(r#"Message"#), "expected to contain: {}", r#"Message"#);
}

#[test]
fn test_heading_h1() {
    // H1 heading
    let html = r#"<h1>Heading 1</h1>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"# Heading 1
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_heading_h2() {
    // H2 heading
    let html = r#"<h2>Heading 2</h2>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"## Heading 2
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_heading_h3() {
    // H3 heading
    let html = r#"<h3>Heading 3</h3>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"### Heading 3
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_heading_h4() {
    // H4 heading
    let html = r#"<h4>Heading 4</h4>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"#### Heading 4
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_heading_h5() {
    // H5 heading
    let html = r#"<h5>Heading 5</h5>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"##### Heading 5
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_heading_h6() {
    // H6 heading
    let html = r#"<h6>Heading 6</h6>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"###### Heading 6
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_image_figure_figcaption() {
    // Figure with figcaption preserves both image and caption
    let html = r#"<figure><img src="sunset.jpg" alt="A sunset"><figcaption>Beautiful sunset over the ocean</figcaption></figure>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"![A sunset](sunset.jpg)"#),
        "expected to contain: {}",
        r#"![A sunset](sunset.jpg)"#
    );
    assert!(
        _content.contains(r#"Beautiful sunset over the ocean"#),
        "expected to contain: {}",
        r#"Beautiful sunset over the ocean"#
    );
}

#[test]
fn test_image_linked() {
    // Image inside an anchor produces a linked image
    let html = r#"<a href="https://example.com"><img src="icon.png" alt="Icon"></a>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"![Icon](icon.png)"#),
        "expected to contain: {}",
        r#"![Icon](icon.png)"#
    );
    assert!(
        _content.contains(r#"https://example.com"#),
        "expected to contain: {}",
        r#"https://example.com"#
    );
}

#[test]
fn test_image_no_alt() {
    // Image without alt text produces image markdown
    let html = r#"<img src="banner.jpg">"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(!_content.is_empty(), "expected non-empty value");
    assert!(
        _content.contains(r#"banner.jpg"#),
        "expected to contain: {}",
        r#"banner.jpg"#
    );
}

#[test]
fn test_image_simple() {
    // Image with alt text
    let html = r#"<img src="photo.jpg" alt="A photo">"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"![A photo](photo.jpg)"#),
        "expected to contain: {}",
        r#"![A photo](photo.jpg)"#
    );
}

#[test]
fn test_image_with_title() {
    // Image with title attribute includes title in output
    let html = r#"<img src="chart.png" alt="Sales chart" title="Q3 Sales">"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"![Sales chart](chart.png"#),
        "expected to contain: {}",
        r#"![Sales chart](chart.png"#
    );
    assert!(
        _content.contains(r#"Q3 Sales"#),
        "expected to contain: {}",
        r#"Q3 Sales"#
    );
}

#[test]
fn test_inline_code() {
    // Inline code
    let html = r#"<p>Use <code>console.log()</code> to debug</p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"`console.log()`"#),
        "expected to contain: {}",
        r#"`console.log()`"#
    );
}

#[test]
fn test_italic_em() {
    // Em tag converts to italic
    let html = r#"<p><em>italic</em></p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"*italic*"#),
        "expected to contain: {}",
        r#"*italic*"#
    );
}

#[test]
fn test_line_break_br_tag() {
    // Single br tag produces a line break in output
    let html = r#"<p>First line.<br>Second line.</p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"First line."#),
        "expected to contain: {}",
        r#"First line."#
    );
    assert!(
        _content.contains(r#"Second line."#),
        "expected to contain: {}",
        r#"Second line."#
    );
}

#[test]
fn test_line_break_hr_tag() {
    // hr tag produces a horizontal separator between content
    let html = r#"<p>Before rule.</p><hr><p>After rule.</p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(!_content.is_empty(), "expected non-empty value");
    assert!(
        _content.contains(r#"Before rule."#),
        "expected to contain: {}",
        r#"Before rule."#
    );
    assert!(
        _content.contains(r#"After rule."#),
        "expected to contain: {}",
        r#"After rule."#
    );
}

#[test]
fn test_line_break_multiple_br() {
    // Multiple consecutive br tags in sequence
    let html = r#"<p>Start.<br><br>End.</p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(_content.contains(r#"Start."#), "expected to contain: {}", r#"Start."#);
    assert!(_content.contains(r#"End."#), "expected to contain: {}", r#"End."#);
}

#[test]
fn test_link_anchor_fragment() {
    // Fragment-only anchor link is preserved
    let html = r##"<a href="#section">Jump to section</a>"##;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"[Jump to section](#section)"#),
        "expected to contain: {}",
        r#"[Jump to section](#section)"#
    );
}

#[test]
fn test_link_empty_href() {
    // Link with empty href produces output with the link text
    let html = r#"<a href="">No destination</a>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"No destination"#),
        "expected to contain: {}",
        r#"No destination"#
    );
}

#[test]
fn test_link_image_inside() {
    // Image inside a link produces a linked image
    let html = r#"<a href="https://example.com"><img src="logo.png" alt="Logo"></a>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"![Logo](logo.png)"#),
        "expected to contain: {}",
        r#"![Logo](logo.png)"#
    );
    assert!(
        _content.contains(r#"https://example.com"#),
        "expected to contain: {}",
        r#"https://example.com"#
    );
}

#[test]
fn test_link_mailto() {
    // Mailto link is preserved with mailto: scheme
    let html = r#"<a href="mailto:user@example.com">Email us</a>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"mailto:user@example.com"#),
        "expected to contain: {}",
        r#"mailto:user@example.com"#
    );
}

#[test]
fn test_link_simple() {
    // Simple link
    let html = r#"<a href="https://example.com">Example</a>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"[Example](https://example.com)"#),
        "expected to contain: {}",
        r#"[Example](https://example.com)"#
    );
}

#[test]
fn test_link_with_bold_text() {
    // Link containing bold text preserves formatting
    let html = r#"<a href="https://example.com"><strong>Bold link</strong></a>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"**Bold link**"#),
        "expected to contain: {}",
        r#"**Bold link**"#
    );
    assert!(
        _content.contains(r#"https://example.com"#),
        "expected to contain: {}",
        r#"https://example.com"#
    );
}

#[test]
fn test_link_with_title() {
    // Link with title attribute
    let html = r#"<a href="https://example.com" title="Example Site">Example</a>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"[Example](https://example.com"#),
        "expected to contain: {}",
        r#"[Example](https://example.com"#
    );
    assert!(
        _content.contains(r#"Example Site"#),
        "expected to contain: {}",
        r#"Example Site"#
    );
}

#[test]
fn test_list_definition_dl() {
    // Definition list with dt and dd elements
    let html = r#"<dl><dt>Term One</dt><dd>Definition of term one.</dd><dt>Term Two</dt><dd>Definition of term two.</dd></dl>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"Term One"#),
        "expected to contain: {}",
        r#"Term One"#
    );
    assert!(
        _content.contains(r#"Definition of term one."#),
        "expected to contain: {}",
        r#"Definition of term one."#
    );
    assert!(
        _content.contains(r#"Term Two"#),
        "expected to contain: {}",
        r#"Term Two"#
    );
    assert!(
        _content.contains(r#"Definition of term two."#),
        "expected to contain: {}",
        r#"Definition of term two."#
    );
}

#[test]
fn test_list_item_multiple_paragraphs() {
    // List item containing multiple paragraphs
    let html =
        r#"<ul><li><p>First paragraph in item.</p><p>Second paragraph in item.</p></li><li>Simple item</li></ul>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"First paragraph in item."#),
        "expected to contain: {}",
        r#"First paragraph in item."#
    );
    assert!(
        _content.contains(r#"Second paragraph in item."#),
        "expected to contain: {}",
        r#"Second paragraph in item."#
    );
    assert!(
        _content.contains(r#"Simple item"#),
        "expected to contain: {}",
        r#"Simple item"#
    );
}

#[test]
fn test_list_mixed_nested() {
    // Mixed list: ordered list nested inside unordered list
    let html = r#"<ul><li>Item A<ol><li>Sub 1</li><li>Sub 2</li></ol></li><li>Item B</li></ul>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(_content.contains(r#"Item A"#), "expected to contain: {}", r#"Item A"#);
    assert!(_content.contains(r#"Sub 1"#), "expected to contain: {}", r#"Sub 1"#);
    assert!(_content.contains(r#"Sub 2"#), "expected to contain: {}", r#"Sub 2"#);
    assert!(_content.contains(r#"Item B"#), "expected to contain: {}", r#"Item B"#);
}

#[test]
fn test_list_nested_ordered() {
    // Nested ordered list with two levels of depth
    let html = r#"<ol><li>Step 1<ol><li>Step 1a</li><li>Step 1b</li></ol></li><li>Step 2</li></ol>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(_content.contains(r#"Step 1"#), "expected to contain: {}", r#"Step 1"#);
    assert!(_content.contains(r#"Step 1a"#), "expected to contain: {}", r#"Step 1a"#);
    assert!(_content.contains(r#"Step 1b"#), "expected to contain: {}", r#"Step 1b"#);
    assert!(_content.contains(r#"Step 2"#), "expected to contain: {}", r#"Step 2"#);
}

#[test]
fn test_list_nested_unordered() {
    // Nested unordered list with two levels of depth
    let html = r#"<ul><li>Parent A<ul><li>Child A1</li><li>Child A2</li></ul></li><li>Parent B</li></ul>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"Parent A"#),
        "expected to contain: {}",
        r#"Parent A"#
    );
    assert!(
        _content.contains(r#"Child A1"#),
        "expected to contain: {}",
        r#"Child A1"#
    );
    assert!(
        _content.contains(r#"Child A2"#),
        "expected to contain: {}",
        r#"Child A2"#
    );
    assert!(
        _content.contains(r#"Parent B"#),
        "expected to contain: {}",
        r#"Parent B"#
    );
}

#[test]
fn test_list_task_checkboxes() {
    // Task list with checked and unchecked checkboxes
    let html =
        r#"<ul><li><input type="checkbox" checked> Done task</li><li><input type="checkbox"> Pending task</li></ul>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(!_content.is_empty(), "expected non-empty value");
    assert!(
        _content.contains(r#"Done task"#),
        "expected to contain: {}",
        r#"Done task"#
    );
    assert!(
        _content.contains(r#"Pending task"#),
        "expected to contain: {}",
        r#"Pending task"#
    );
}

#[test]
fn test_ordered_list() {
    // Ordered list
    let html = r#"<ol><li>First</li><li>Second</li><li>Third</li></ol>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"1. First"#),
        "expected to contain: {}",
        r#"1. First"#
    );
    assert!(
        _content.contains(r#"2. Second"#),
        "expected to contain: {}",
        r#"2. Second"#
    );
    assert!(
        _content.contains(r#"3. Third"#),
        "expected to contain: {}",
        r#"3. Third"#
    );
}

#[test]
fn test_paragraph_multiple() {
    // Multiple paragraphs are separated by a blank line
    let html = r#"<p>First paragraph.</p><p>Second paragraph.</p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"First paragraph."#),
        "expected to contain: {}",
        r#"First paragraph."#
    );
    assert!(
        _content.contains(r#"Second paragraph."#),
        "expected to contain: {}",
        r#"Second paragraph."#
    );
}

#[test]
fn test_paragraph_nested_divs() {
    // Text nested inside divs is extracted correctly
    let html = r#"<div><div><p>Nested text</p></div></div>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"Nested text"#),
        "expected to contain: {}",
        r#"Nested text"#
    );
}

#[test]
fn test_paragraph_simple() {
    // Simple paragraph converts to plain text
    let html = r#"<p>Hello World</p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"Hello World
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_paragraph_with_inline_formatting() {
    // Paragraph with bold, italic, and a link
    let html =
        r#"<p>This has <strong>bold</strong>, <em>italic</em>, and a <a href="https://example.com">link</a>.</p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"**bold**"#),
        "expected to contain: {}",
        r#"**bold**"#
    );
    assert!(
        _content.contains(r#"*italic*"#),
        "expected to contain: {}",
        r#"*italic*"#
    );
    assert!(
        _content.contains(r#"[link](https://example.com)"#),
        "expected to contain: {}",
        r#"[link](https://example.com)"#
    );
}

#[test]
fn test_paragraph_with_line_breaks() {
    // Paragraph with br tags produces line breaks in output
    let html = r#"<p>Line one.<br>Line two.<br>Line three.</p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(!_content.is_empty(), "expected non-empty value");
    assert!(
        _content.contains(r#"Line one."#),
        "expected to contain: {}",
        r#"Line one."#
    );
    assert!(
        _content.contains(r#"Line two."#),
        "expected to contain: {}",
        r#"Line two."#
    );
    assert!(
        _content.contains(r#"Line three."#),
        "expected to contain: {}",
        r#"Line three."#
    );
}

#[test]
fn test_semantic_abbr() {
    // Abbreviation element text is preserved
    let html = r#"<p>The <abbr title="World Wide Web">WWW</abbr> is global.</p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(_content.contains(r#"WWW"#), "expected to contain: {}", r#"WWW"#);
}

#[test]
fn test_semantic_article() {
    // Article element wrapping content preserves inner content
    let html = r#"<article><h2>Article Title</h2><p>Article body.</p></article>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"Article Title"#),
        "expected to contain: {}",
        r#"Article Title"#
    );
    assert!(
        _content.contains(r#"Article body."#),
        "expected to contain: {}",
        r#"Article body."#
    );
}

#[test]
fn test_semantic_definition_list() {
    // Definition list with term and description
    let html = r#"<dl><dt>HTML</dt><dd>HyperText Markup Language</dd><dt>CSS</dt><dd>Cascading Style Sheets</dd></dl>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(_content.contains(r#"HTML"#), "expected to contain: {}", r#"HTML"#);
    assert!(
        _content.contains(r#"HyperText Markup Language"#),
        "expected to contain: {}",
        r#"HyperText Markup Language"#
    );
    assert!(_content.contains(r#"CSS"#), "expected to contain: {}", r#"CSS"#);
    assert!(
        _content.contains(r#"Cascading Style Sheets"#),
        "expected to contain: {}",
        r#"Cascading Style Sheets"#
    );
}

#[test]
fn test_semantic_details_summary() {
    // Details and summary elements produce readable output
    let html = r#"<details><summary>Click to expand</summary><p>Hidden content here.</p></details>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(!_content.is_empty(), "expected non-empty value");
    assert!(
        _content.contains(r#"Click to expand"#),
        "expected to contain: {}",
        r#"Click to expand"#
    );
}

#[test]
fn test_semantic_hr() {
    // Horizontal rule produces a separator in output
    let html = r#"<p>Above</p><hr><p>Below</p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(!_content.is_empty(), "expected non-empty value");
    assert!(_content.contains(r#"Above"#), "expected to contain: {}", r#"Above"#);
    assert!(_content.contains(r#"Below"#), "expected to contain: {}", r#"Below"#);
}

#[test]
fn test_semantic_mark_highlight() {
    // Mark tag produces highlighted output
    let html = r#"<p>This is <mark>highlighted text</mark> in a sentence.</p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(!_content.is_empty(), "expected non-empty value");
    assert!(
        _content.contains(r#"highlighted text"#),
        "expected to contain: {}",
        r#"highlighted text"#
    );
}

#[test]
fn test_semantic_section_with_heading() {
    // Section element with heading preserves structure
    let html = r#"<section><h3>Section Heading</h3><p>Section content.</p></section>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"Section Heading"#),
        "expected to contain: {}",
        r#"Section Heading"#
    );
    assert!(
        _content.contains(r#"Section content."#),
        "expected to contain: {}",
        r#"Section content."#
    );
}

#[test]
fn test_semantic_sub_superscript() {
    // Subscript and superscript elements are preserved in output
    let html = r#"<p>H<sub>2</sub>O and E=mc<sup>2</sup></p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(!_content.is_empty(), "expected non-empty value");
    assert!(_content.contains(r#"H"#), "expected to contain: {}", r#"H"#);
    assert!(_content.contains(r#"2"#), "expected to contain: {}", r#"2"#);
    assert!(_content.contains(r#"O"#), "expected to contain: {}", r#"O"#);
    assert!(_content.contains(r#"E=mc"#), "expected to contain: {}", r#"E=mc"#);
}

#[test]
fn test_simple_table() {
    // Simple table with header
    let html = r#"<table><thead><tr><th>Name</th><th>Age</th></tr></thead><tbody><tr><td>Alice</td><td>30</td></tr></tbody></table>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(_content.contains(r#"Name"#), "expected to contain: {}", r#"Name"#);
    assert!(_content.contains(r#"Age"#), "expected to contain: {}", r#"Age"#);
    assert!(_content.contains(r#"Alice"#), "expected to contain: {}", r#"Alice"#);
    assert!(_content.contains(r#"30"#), "expected to contain: {}", r#"30"#);
    assert!(_content.contains(r#"|"#), "expected to contain: {}", r#"|"#);
    assert!(_content.contains(r#"---"#), "expected to contain: {}", r#"---"#);
}

#[test]
fn test_strike_renders_as_strikethrough() {
    // The deprecated strike tag renders as strikethrough
    let html = r#"<p><strike>gone</strike></p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"~~gone~~
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_table_empty() {
    // Empty table produces no output or minimal output
    let html = r#"<table></table>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(_content.to_string(), r#""#, "equals assertion failed");
}

#[test]
fn test_table_layout_cell_paragraphs_stay_separated() {
    // Adjacent paragraphs in a layout-table cell are separated rather than concatenated into one run of text
    let html = r#"<table><tr><td><p><b>Alice Example</b></p><p><i>Customer Service</i></p></td><td>Logo</td></tr><tr><td>A</td><td>B</td><td>C</td></tr></table>"#;
    let options_json: serde_json::Value = serde_json::from_str(r#"{"compact_tables":true}"#).unwrap();
    let options: ConversionOptions = serde_json::from_value(options_json).unwrap();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        !_content.contains(r#"**Alice Example***Customer Service*"#),
        "expected NOT to contain: {}",
        r#"**Alice Example***Customer Service*"#
    );
    assert!(
        _content.contains(r#"Alice Example"#),
        "expected to contain: {}",
        r#"Alice Example"#
    );
    assert!(
        _content.contains(r#"Customer Service"#),
        "expected to contain: {}",
        r#"Customer Service"#
    );
    assert!(_content.contains(r#"Logo"#), "expected to contain: {}", r#"Logo"#);
}

#[test]
fn test_table_nested_chain_not_misclassified_as_layout() {
    // A straight chain of one-nested-table-per-cell tables renders as pipe tables, not a broken layout list
    let html =
        r#"<table><tr><td><table><tr><td><table><tr><td>leaf</td></tr></table></td></tr></table></td></tr></table>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(!_content.is_empty(), "expected non-empty value");
    assert!(_content.contains(r#"leaf"#), "expected to contain: {}", r#"leaf"#);
    assert!(_content.contains(r#"| ---"#), "expected to contain: {}", r#"| ---"#);
}

#[test]
fn test_table_nested_in_single_cell_renders_separately() {
    // A table nested in a single-cell row renders as its own table
    let html = r#"<table><tr><th>Summary</th></tr><tr><td>Before<table><tr><th>Item</th><th>Amount</th></tr><tr><td>Alpha</td><td>10</td></tr></table></td></tr></table>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"| Item  | Amount |"#),
        "expected to contain: {}",
        r#"| Item  | Amount |"#
    );
    assert!(
        _content.contains(r#"| Alpha | 10     |"#),
        "expected to contain: {}",
        r#"| Alpha | 10     |"#
    );
    assert!(!_content.contains(r#"\|"#), "expected NOT to contain: {}", r#"\|"#);
}

#[test]
fn test_table_no_thead() {
    // Table without thead uses first row as implied header
    let html = r#"<table><tr><td>Product</td><td>Price</td></tr><tr><td>Apple</td><td>1.00</td></tr></table>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(!_content.is_empty(), "expected non-empty value");
    assert!(_content.contains(r#"Product"#), "expected to contain: {}", r#"Product"#);
    assert!(_content.contains(r#"Price"#), "expected to contain: {}", r#"Price"#);
    assert!(_content.contains(r#"Apple"#), "expected to contain: {}", r#"Apple"#);
    assert!(_content.contains(r#"1.00"#), "expected to contain: {}", r#"1.00"#);
    assert!(_content.contains(r#"|"#), "expected to contain: {}", r#"|"#);
}

#[test]
fn test_table_pipe_chars_in_content() {
    // Table cells containing pipe characters are escaped in output
    let html = r#"<table><thead><tr><th>Expression</th><th>Result</th></tr></thead><tbody><tr><td>a | b</td><td>true</td></tr></tbody></table>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(!_content.is_empty(), "expected non-empty value");
    assert!(
        _content.contains(r#"Expression"#),
        "expected to contain: {}",
        r#"Expression"#
    );
    assert!(_content.contains(r#"Result"#), "expected to contain: {}", r#"Result"#);
    assert!(_content.contains(r#"true"#), "expected to contain: {}", r#"true"#);
}

#[test]
fn test_table_ragged_row_fewer_cells_than_header() {
    // A data row with fewer cells than the header is padded to the declared column count
    let html = r#"<table><tr><th>A</th><th>B</th><th>C</th></tr><tr><td>1</td><td>2</td></tr></table>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"| A | B | C |
| --- | --- | --- |
| 1 | 2 |   |
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_table_ragged_row_more_cells_than_header() {
    // A data row with more cells than the header widens the separator so no cell is silently dropped
    let html = r#"<table><tr><th>A</th><th>B</th></tr><tr><td>1</td><td>2</td><td>3</td></tr></table>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"| A | B |   |
| --- | --- | --- |
| 1 | 2 | 3 |
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_table_with_alignment() {
    // Table with column alignment attributes
    let html = r#"<table><thead><tr><th align="left">Left</th><th align="center">Center</th><th align="right">Right</th></tr></thead><tbody><tr><td>L</td><td>C</td><td>R</td></tr></tbody></table>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(!_content.is_empty(), "expected non-empty value");
    assert!(_content.contains(r#"Left"#), "expected to contain: {}", r#"Left"#);
    assert!(_content.contains(r#"Center"#), "expected to contain: {}", r#"Center"#);
    assert!(_content.contains(r#"Right"#), "expected to contain: {}", r#"Right"#);
    assert!(_content.contains(r#"L"#), "expected to contain: {}", r#"L"#);
    assert!(_content.contains(r#"C"#), "expected to contain: {}", r#"C"#);
    assert!(_content.contains(r#"R"#), "expected to contain: {}", r#"R"#);
    assert!(_content.contains(r#"|"#), "expected to contain: {}", r#"|"#);
}

#[test]
fn test_table_with_colspan() {
    // Table with colspan attribute in a header cell
    let html = r#"<table><thead><tr><th colspan="2">Full Name</th></tr></thead><tbody><tr><td>John</td><td>Doe</td></tr></tbody></table>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(!_content.is_empty(), "expected non-empty value");
    assert!(
        _content.contains(r#"Full Name"#),
        "expected to contain: {}",
        r#"Full Name"#
    );
    assert!(_content.contains(r#"John"#), "expected to contain: {}", r#"John"#);
    assert!(_content.contains(r#"Doe"#), "expected to contain: {}", r#"Doe"#);
}

#[test]
fn test_tagged_space_in_strikethrough_kept() {
    // A whitespace-only strikethrough keeps the word separator
    let html = r#"<p>A<del> </del>B</p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"A B
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_tagged_space_in_subscript_kept() {
    // A whitespace-only subscript keeps the word separator
    let html = r#"<p>A<sub> </sub>B</p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"A B
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_tagged_space_not_duplicated() {
    // A whitespace-only inline element contributes exactly one space
    let html = r#"<p>A<i> </i>B</p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"A B
"#,
        "equals assertion failed"
    );
}

#[test]
fn test_unordered_list() {
    // Unordered list
    let html = r#"<ul><li>Item 1</li><li>Item 2</li><li>Item 3</li></ul>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert!(
        _content.contains(r#"- Item 1"#),
        "expected to contain: {}",
        r#"- Item 1"#
    );
    assert!(
        _content.contains(r#"- Item 2"#),
        "expected to contain: {}",
        r#"- Item 2"#
    );
    assert!(
        _content.contains(r#"- Item 3"#),
        "expected to contain: {}",
        r#"- Item 3"#
    );
}

#[test]
fn test_whitespace_only_code_span_kept() {
    // A code span whose content is a single space survives
    let html = r#"<p>A<code> </code>B</p>"#;
    let options: ConversionOptions = Default::default();
    let result = convert(html, Some(options.clone())).expect("call failed");
    let _content = result.content.as_ref().map(|v| v.to_string()).unwrap_or_default();
    assert_eq!(
        _content.to_string(),
        r#"A` `B
"#,
        "equals assertion failed"
    );
}
