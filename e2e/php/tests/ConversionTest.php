<?php

// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ab695501dbbb15d7c752a75d2e34ef504ab5fe0fb0ddc7670182e4fdb6982b2a
// To regenerate: alef generate
// To verify freshness: alef verify

declare(strict_types=1);

namespace HtmlToMarkdown\E2e;

use HtmlToMarkdown\ConversionOptions;
use HtmlToMarkdown\HtmlToMarkdown;
use PHPUnit\Framework\TestCase;

/** E2e tests for category: conversion. */
final class ConversionTest extends TestCase
{
    /** Adjacent code spans merge instead of colliding backticks */
    public function test_adjacent_code_spans_merged(): void
    {
        $result = HtmlToMarkdown::convert('<p><code>A</code><code>B</code></p>');

        $this->assertEquals("`AB`\n", $result->content ?? null);
    }

    /** Adjacent emphasis elements share one delimiter run */
    public function test_adjacent_emphasis_merged(): void
    {
        $result = HtmlToMarkdown::convert('<p><i>A</i><i>B</i><i>C</i></p>');

        $this->assertEquals("*ABC*\n", $result->content ?? null);
    }

    /** Adjacent inserted elements share one delimiter run */
    public function test_adjacent_inserted_merged(): void
    {
        $result = HtmlToMarkdown::convert('<p><ins>A</ins><ins>B</ins></p>');

        $this->assertEquals("==AB==\n", $result->content ?? null);
    }

    /** Adjacent strikethrough elements share one delimiter run */
    public function test_adjacent_strikethrough_merged(): void
    {
        $result = HtmlToMarkdown::convert('<p><del>A</del><del>B</del></p>');

        $this->assertEquals("~~AB~~\n", $result->content ?? null);
    }

    /** Adjacent variable elements share one delimiter run */
    public function test_adjacent_variable_merged(): void
    {
        $result = HtmlToMarkdown::convert('<p><var>A</var><var>B</var></p>');

        $this->assertEquals("*AB*\n", $result->content ?? null);
    }

    /** Code block inside a blockquote keeps its line indentation */
    public function test_blockquote_code_block_indentation_preserved(): void
    {
        $result = HtmlToMarkdown::convert("<blockquote><pre><code>line1\n    line2 indented</code></pre></blockquote>");

        $this->assertEquals("> ```\n> line1\n>     line2 indented\n> ```\n", $result->content ?? null);
    }

    /** Blockquote with multiple paragraphs has each paragraph prefixed */
    public function test_blockquote_multiple_paragraphs(): void
    {
        $result = HtmlToMarkdown::convert('<blockquote><p>First paragraph.</p><p>Second paragraph.</p></blockquote>');

        $this->assertStringContainsString('> First paragraph.', $result->content ?? null);
        $this->assertStringContainsString('> Second paragraph.', $result->content ?? null);
    }

    /** Nested blockquote produces double-prefixed lines */
    public function test_blockquote_nested(): void
    {
        $result = HtmlToMarkdown::convert(
            '<blockquote><p>Outer quote.</p><blockquote><p>Inner quote.</p></blockquote></blockquote>',
        );

        $this->assertNotSame('', $result->content ?? null ?? '', 'expected non-empty value');
        $this->assertStringContainsString('Outer quote.', $result->content ?? null);
        $this->assertStringContainsString('Inner quote.', $result->content ?? null);
    }

    /** Nested list inside a blockquote keeps its continuation indentation */
    public function test_blockquote_nested_list_indentation_preserved(): void
    {
        $result = HtmlToMarkdown::convert('<blockquote><ul><li>item a<ul><li>sub a1</li></ul></li></ul></blockquote>');

        $this->assertEquals("> - item a\n>   * sub a1\n", $result->content ?? null);
    }

    /** Simple blockquote */
    public function test_blockquote_simple(): void
    {
        $result = HtmlToMarkdown::convert('<blockquote><p>Quote text</p></blockquote>');

        $this->assertStringContainsString('> Quote text', $result->content ?? null);
    }

    /** Bare text followed by a paragraph inside a blockquote gets a blank-line separator instead of merging into one line */
    public function test_blockquote_text_then_paragraph_gets_blank_line(): void
    {
        $result = HtmlToMarkdown::convert('<blockquote>Just text, then <p>a paragraph</p></blockquote>');

        $this->assertEquals("> Just text, then\n>\n> a paragraph\n", $result->content ?? null);
    }

    /** Blockquote containing a list preserves list items inside quote */
    public function test_blockquote_with_list(): void
    {
        $result = HtmlToMarkdown::convert(
            '<blockquote><p>Quote intro:</p><ul><li>Point one</li><li>Point two</li></ul></blockquote>',
        );

        $this->assertNotSame('', $result->content ?? null ?? '', 'expected non-empty value');
        $this->assertStringContainsString('Quote intro:', $result->content ?? null);
        $this->assertStringContainsString('Point one', $result->content ?? null);
        $this->assertStringContainsString('Point two', $result->content ?? null);
    }

    /** Nested bold and italic */
    public function test_bold_and_italic(): void
    {
        $result = HtmlToMarkdown::convert('<p><strong><em>both</em></strong></p>');

        $this->assertStringContainsString('***both***', $result->content ?? null);
    }

    /** Strong tag converts to bold */
    public function test_bold_strong(): void
    {
        $result = HtmlToMarkdown::convert('<p><strong>bold</strong></p>');

        $this->assertStringContainsString('**bold**', $result->content ?? null);
    }

    /** Code block with language preserves content */
    public function test_code_block(): void
    {
        $result = HtmlToMarkdown::convert("<pre><code class=\"language-python\">print('hello')</code></pre>");

        $this->assertNotSame('', $result->content ?? null ?? '', 'expected non-empty value');
        $this->assertStringContainsString("print('hello')", $result->content ?? null);
    }

    /** Code block without a language class preserves content */
    public function test_code_block_no_language(): void
    {
        $result = HtmlToMarkdown::convert('<pre><code>plain code here</code></pre>');

        $this->assertNotSame('', $result->content ?? null ?? '', 'expected non-empty value');
        $this->assertStringContainsString('plain code here', $result->content ?? null);
    }

    /** Inline code element nested inside a paragraph */
    public function test_code_inline_in_paragraph(): void
    {
        $result = HtmlToMarkdown::convert('<p>Call the <code>initialize()</code> method first.</p>');

        $this->assertStringContainsString('`initialize()`', $result->content ?? null);
    }

    /** Inline markers inside a code span stay literal content */
    public function test_code_span_suppresses_nested_inline_markers(): void
    {
        $result = HtmlToMarkdown::convert('<p><code>a<em>b</em><ins>c</ins></code></p>');

        $this->assertEquals("`abc`\n", $result->content ?? null);
    }

    /** Inline code containing backtick characters is properly escaped */
    public function test_code_with_backticks_in_content(): void
    {
        $result = HtmlToMarkdown::convert('<p>Use <code>`backtick` here</code> carefully.</p>');

        $this->assertNotSame('', $result->content ?? null ?? '', 'expected non-empty value');
        $this->assertStringContainsString('backtick', $result->content ?? null);
    }

    /** Bare filename href (no URI scheme) renders as inline link, not autolink (#397) */
    public function test_conversion_autolink_filename_not_autolinked(): void
    {
        $result = HtmlToMarkdown::convert('<a href="foobar.png">foobar.png</a>');

        $this->assertEquals("[foobar.png](foobar.png)\n", $result->content ?? null);
    }

    /** Absolute https URL with matching text renders as autolink (#397) */
    public function test_conversion_autolink_https_url(): void
    {
        $result = HtmlToMarkdown::convert('<a href="https://example.com">https://example.com</a>');

        $this->assertEquals("<https://example.com>\n", $result->content ?? null);
    }

    /** mailto: link with matching email text renders as autolink (#397) */
    public function test_conversion_autolink_mailto(): void
    {
        $result = HtmlToMarkdown::convert('<a href="mailto:a@b.com">a@b.com</a>');

        $this->assertEquals("<a@b.com>\n", $result->content ?? null);
    }

    /** Mixed filename + URL: only the URL becomes an autolink (#397) */
    public function test_conversion_autolink_mixed_filename_and_url(): void
    {
        $result = HtmlToMarkdown::convert(
            '<a href="foobar.png">foobar.png</a> <a href="https://www.heise.de">https://www.heise.de</a>',
        );

        $this->assertEquals("[foobar.png](foobar.png) <https://www.heise.de>\n", $result->content ?? null);
    }

    /** Relative path href renders as inline link (#397) */
    public function test_conversion_autolink_relative_path_not_autolinked(): void
    {
        $result = HtmlToMarkdown::convert('<a href="/docs/intro.html">/docs/intro.html</a>');

        $this->assertEquals("[/docs/intro.html](/docs/intro.html)\n", $result->content ?? null);
    }

    /** mark tag produces highlighted output */
    public function test_emphasis_mark_highlight(): void
    {
        $result = HtmlToMarkdown::convert('<p><mark>highlighted</mark></p>');

        $this->assertNotSame('', $result->content ?? null ?? '', 'expected non-empty value');
        $this->assertStringContainsString('highlighted', $result->content ?? null);
    }

    /** del tag converts to GFM strikethrough */
    public function test_emphasis_strikethrough_del(): void
    {
        $result = HtmlToMarkdown::convert('<p><del>deleted text</del></p>');

        $this->assertStringContainsString('~~deleted text~~', $result->content ?? null);
    }

    /** s tag converts to GFM strikethrough */
    public function test_emphasis_strikethrough_s(): void
    {
        $result = HtmlToMarkdown::convert('<p><s>strikethrough</s></p>');

        $this->assertStringContainsString('~~strikethrough~~', $result->content ?? null);
    }

    /** sub tag content is preserved */
    public function test_emphasis_subscript(): void
    {
        $result = HtmlToMarkdown::convert('<p>H<sub>2</sub>O</p>');

        $this->assertStringContainsString('H', $result->content ?? null);
        $this->assertStringContainsString('2', $result->content ?? null);
        $this->assertStringContainsString('O', $result->content ?? null);
    }

    /** sup tag content is preserved */
    public function test_emphasis_superscript(): void
    {
        $result = HtmlToMarkdown::convert('<p>x<sup>2</sup></p>');

        $this->assertStringContainsString('x', $result->content ?? null);
        $this->assertStringContainsString('2', $result->content ?? null);
    }

    /** u tag content is preserved in output */
    public function test_emphasis_underline_u(): void
    {
        $result = HtmlToMarkdown::convert('<p><u>underlined</u></p>');

        $this->assertStringContainsString('underlined', $result->content ?? null);
    }

    /** Form input elements produce readable output without form mechanics */
    public function test_form_input_elements(): void
    {
        $options = \HtmlToMarkdown\ConversionOptions::from_json(json_encode(['preprocessing' => [
            'removeForms' => false,
        ]]));
        $result = HtmlToMarkdown::convert(
            '<form><label for="name">Name:</label><input type="text" id="name" placeholder="Enter name"></form>',
            $options,
        );

        $this->assertNotSame('', $result->content ?? null ?? '', 'expected non-empty value');
        $this->assertStringContainsString('Name', $result->content ?? null);
    }

    /** Select element with options produces readable output */
    public function test_form_select_options(): void
    {
        $options = \HtmlToMarkdown\ConversionOptions::from_json(json_encode(['preprocessing' => [
            'removeForms' => false,
        ]]));
        $result = HtmlToMarkdown::convert(
            '<form><label>Color:</label><select><option value="red">Red</option><option value="blue" selected>Blue</option><option value="green">Green</option></select></form>',
            $options,
        );

        $this->assertNotSame('', $result->content ?? null ?? '', 'expected non-empty value');
        $this->assertStringContainsString('Color', $result->content ?? null);
    }

    /** Textarea element produces readable output */
    public function test_form_textarea(): void
    {
        $options = \HtmlToMarkdown\ConversionOptions::from_json(json_encode(['preprocessing' => [
            'removeForms' => false,
        ]]));
        $result = HtmlToMarkdown::convert(
            '<form><label>Message:</label><textarea>Default text content</textarea></form>',
            $options,
        );

        $this->assertNotSame('', $result->content ?? null ?? '', 'expected non-empty value');
        $this->assertStringContainsString('Message', $result->content ?? null);
    }

    /** H1 heading */
    public function test_heading_h1(): void
    {
        $result = HtmlToMarkdown::convert('<h1>Heading 1</h1>');

        $this->assertEquals("# Heading 1\n", $result->content ?? null);
    }

    /** H2 heading */
    public function test_heading_h2(): void
    {
        $result = HtmlToMarkdown::convert('<h2>Heading 2</h2>');

        $this->assertEquals("## Heading 2\n", $result->content ?? null);
    }

    /** H3 heading */
    public function test_heading_h3(): void
    {
        $result = HtmlToMarkdown::convert('<h3>Heading 3</h3>');

        $this->assertEquals("### Heading 3\n", $result->content ?? null);
    }

    /** H4 heading */
    public function test_heading_h4(): void
    {
        $result = HtmlToMarkdown::convert('<h4>Heading 4</h4>');

        $this->assertEquals("#### Heading 4\n", $result->content ?? null);
    }

    /** H5 heading */
    public function test_heading_h5(): void
    {
        $result = HtmlToMarkdown::convert('<h5>Heading 5</h5>');

        $this->assertEquals("##### Heading 5\n", $result->content ?? null);
    }

    /** H6 heading */
    public function test_heading_h6(): void
    {
        $result = HtmlToMarkdown::convert('<h6>Heading 6</h6>');

        $this->assertEquals("###### Heading 6\n", $result->content ?? null);
    }

    /** Figure with figcaption preserves both image and caption */
    public function test_image_figure_figcaption(): void
    {
        $result = HtmlToMarkdown::convert(
            '<figure><img src="sunset.jpg" alt="A sunset"><figcaption>Beautiful sunset over the ocean</figcaption></figure>',
        );

        $this->assertStringContainsString('![A sunset](sunset.jpg)', $result->content ?? null);
        $this->assertStringContainsString('Beautiful sunset over the ocean', $result->content ?? null);
    }

    /** Image inside an anchor produces a linked image */
    public function test_image_linked(): void
    {
        $result = HtmlToMarkdown::convert('<a href="https://example.com"><img src="icon.png" alt="Icon"></a>');

        $this->assertStringContainsString('![Icon](icon.png)', $result->content ?? null);
        $this->assertStringContainsString('https://example.com', $result->content ?? null);
    }

    /** Image without alt text produces image markdown */
    public function test_image_no_alt(): void
    {
        $result = HtmlToMarkdown::convert('<img src="banner.jpg">');

        $this->assertNotSame('', $result->content ?? null ?? '', 'expected non-empty value');
        $this->assertStringContainsString('banner.jpg', $result->content ?? null);
    }

    /** Image with alt text */
    public function test_image_simple(): void
    {
        $result = HtmlToMarkdown::convert('<img src="photo.jpg" alt="A photo">');

        $this->assertStringContainsString('![A photo](photo.jpg)', $result->content ?? null);
    }

    /** Image with title attribute includes title in output */
    public function test_image_with_title(): void
    {
        $result = HtmlToMarkdown::convert('<img src="chart.png" alt="Sales chart" title="Q3 Sales">');

        $this->assertStringContainsString('![Sales chart](chart.png', $result->content ?? null);
        $this->assertStringContainsString('Q3 Sales', $result->content ?? null);
    }

    /** Inline code */
    public function test_inline_code(): void
    {
        $result = HtmlToMarkdown::convert('<p>Use <code>console.log()</code> to debug</p>');

        $this->assertStringContainsString('`console.log()`', $result->content ?? null);
    }

    /** Em tag converts to italic */
    public function test_italic_em(): void
    {
        $result = HtmlToMarkdown::convert('<p><em>italic</em></p>');

        $this->assertStringContainsString('*italic*', $result->content ?? null);
    }

    /** Single br tag produces a line break in output */
    public function test_line_break_br_tag(): void
    {
        $result = HtmlToMarkdown::convert('<p>First line.<br>Second line.</p>');

        $this->assertStringContainsString('First line.', $result->content ?? null);
        $this->assertStringContainsString('Second line.', $result->content ?? null);
    }

    /** hr tag produces a horizontal separator between content */
    public function test_line_break_hr_tag(): void
    {
        $result = HtmlToMarkdown::convert('<p>Before rule.</p><hr><p>After rule.</p>');

        $this->assertNotSame('', $result->content ?? null ?? '', 'expected non-empty value');
        $this->assertStringContainsString('Before rule.', $result->content ?? null);
        $this->assertStringContainsString('After rule.', $result->content ?? null);
    }

    /** Multiple consecutive br tags in sequence */
    public function test_line_break_multiple_br(): void
    {
        $result = HtmlToMarkdown::convert('<p>Start.<br><br>End.</p>');

        $this->assertStringContainsString('Start.', $result->content ?? null);
        $this->assertStringContainsString('End.', $result->content ?? null);
    }

    /** Fragment-only anchor link is preserved */
    public function test_link_anchor_fragment(): void
    {
        $result = HtmlToMarkdown::convert('<a href="#section">Jump to section</a>');

        $this->assertStringContainsString('[Jump to section](#section)', $result->content ?? null);
    }

    /** Link with empty href produces output with the link text */
    public function test_link_empty_href(): void
    {
        $result = HtmlToMarkdown::convert('<a href="">No destination</a>');

        $this->assertStringContainsString('No destination', $result->content ?? null);
    }

    /** Image inside a link produces a linked image */
    public function test_link_image_inside(): void
    {
        $result = HtmlToMarkdown::convert('<a href="https://example.com"><img src="logo.png" alt="Logo"></a>');

        $this->assertStringContainsString('![Logo](logo.png)', $result->content ?? null);
        $this->assertStringContainsString('https://example.com', $result->content ?? null);
    }

    /** Mailto link is preserved with mailto: scheme */
    public function test_link_mailto(): void
    {
        $result = HtmlToMarkdown::convert('<a href="mailto:user@example.com">Email us</a>');

        $this->assertStringContainsString('mailto:user@example.com', $result->content ?? null);
    }

    /** Simple link */
    public function test_link_simple(): void
    {
        $result = HtmlToMarkdown::convert('<a href="https://example.com">Example</a>');

        $this->assertStringContainsString('[Example](https://example.com)', $result->content ?? null);
    }

    /** Link containing bold text preserves formatting */
    public function test_link_with_bold_text(): void
    {
        $result = HtmlToMarkdown::convert('<a href="https://example.com"><strong>Bold link</strong></a>');

        $this->assertStringContainsString('**Bold link**', $result->content ?? null);
        $this->assertStringContainsString('https://example.com', $result->content ?? null);
    }

    /** Link with title attribute */
    public function test_link_with_title(): void
    {
        $result = HtmlToMarkdown::convert('<a href="https://example.com" title="Example Site">Example</a>');

        $this->assertStringContainsString('[Example](https://example.com', $result->content ?? null);
        $this->assertStringContainsString('Example Site', $result->content ?? null);
    }

    /** Definition list with dt and dd elements */
    public function test_list_definition_dl(): void
    {
        $result = HtmlToMarkdown::convert(
            '<dl><dt>Term One</dt><dd>Definition of term one.</dd><dt>Term Two</dt><dd>Definition of term two.</dd></dl>',
        );

        $this->assertStringContainsString('Term One', $result->content ?? null);
        $this->assertStringContainsString('Definition of term one.', $result->content ?? null);
        $this->assertStringContainsString('Term Two', $result->content ?? null);
        $this->assertStringContainsString('Definition of term two.', $result->content ?? null);
    }

    /** List item containing multiple paragraphs */
    public function test_list_item_multiple_paragraphs(): void
    {
        $result = HtmlToMarkdown::convert(
            '<ul><li><p>First paragraph in item.</p><p>Second paragraph in item.</p></li><li>Simple item</li></ul>',
        );

        $this->assertStringContainsString('First paragraph in item.', $result->content ?? null);
        $this->assertStringContainsString('Second paragraph in item.', $result->content ?? null);
        $this->assertStringContainsString('Simple item', $result->content ?? null);
    }

    /** Mixed list: ordered list nested inside unordered list */
    public function test_list_mixed_nested(): void
    {
        $result = HtmlToMarkdown::convert(
            '<ul><li>Item A<ol><li>Sub 1</li><li>Sub 2</li></ol></li><li>Item B</li></ul>',
        );

        $this->assertStringContainsString('Item A', $result->content ?? null);
        $this->assertStringContainsString('Sub 1', $result->content ?? null);
        $this->assertStringContainsString('Sub 2', $result->content ?? null);
        $this->assertStringContainsString('Item B', $result->content ?? null);
    }

    /** Nested ordered list with two levels of depth */
    public function test_list_nested_ordered(): void
    {
        $result = HtmlToMarkdown::convert(
            '<ol><li>Step 1<ol><li>Step 1a</li><li>Step 1b</li></ol></li><li>Step 2</li></ol>',
        );

        $this->assertStringContainsString('Step 1', $result->content ?? null);
        $this->assertStringContainsString('Step 1a', $result->content ?? null);
        $this->assertStringContainsString('Step 1b', $result->content ?? null);
        $this->assertStringContainsString('Step 2', $result->content ?? null);
    }

    /** Nested unordered list with two levels of depth */
    public function test_list_nested_unordered(): void
    {
        $result = HtmlToMarkdown::convert(
            '<ul><li>Parent A<ul><li>Child A1</li><li>Child A2</li></ul></li><li>Parent B</li></ul>',
        );

        $this->assertStringContainsString('Parent A', $result->content ?? null);
        $this->assertStringContainsString('Child A1', $result->content ?? null);
        $this->assertStringContainsString('Child A2', $result->content ?? null);
        $this->assertStringContainsString('Parent B', $result->content ?? null);
    }

    /** Task list with checked and unchecked checkboxes */
    public function test_list_task_checkboxes(): void
    {
        $result = HtmlToMarkdown::convert(
            '<ul><li><input type="checkbox" checked> Done task</li><li><input type="checkbox"> Pending task</li></ul>',
        );

        $this->assertNotSame('', $result->content ?? null ?? '', 'expected non-empty value');
        $this->assertStringContainsString('Done task', $result->content ?? null);
        $this->assertStringContainsString('Pending task', $result->content ?? null);
    }

    /** Ordered list */
    public function test_ordered_list(): void
    {
        $result = HtmlToMarkdown::convert('<ol><li>First</li><li>Second</li><li>Third</li></ol>');

        $this->assertStringContainsString('1. First', $result->content ?? null);
        $this->assertStringContainsString('2. Second', $result->content ?? null);
        $this->assertStringContainsString('3. Third', $result->content ?? null);
    }

    /** Multiple paragraphs are separated by a blank line */
    public function test_paragraph_multiple(): void
    {
        $result = HtmlToMarkdown::convert('<p>First paragraph.</p><p>Second paragraph.</p>');

        $this->assertStringContainsString('First paragraph.', $result->content ?? null);
        $this->assertStringContainsString('Second paragraph.', $result->content ?? null);
    }

    /** Text nested inside divs is extracted correctly */
    public function test_paragraph_nested_divs(): void
    {
        $result = HtmlToMarkdown::convert('<div><div><p>Nested text</p></div></div>');

        $this->assertStringContainsString('Nested text', $result->content ?? null);
    }

    /** Simple paragraph converts to plain text */
    public function test_paragraph_simple(): void
    {
        $result = HtmlToMarkdown::convert('<p>Hello World</p>');

        $this->assertEquals("Hello World\n", $result->content ?? null);
    }

    /** Paragraph with bold, italic, and a link */
    public function test_paragraph_with_inline_formatting(): void
    {
        $result = HtmlToMarkdown::convert(
            '<p>This has <strong>bold</strong>, <em>italic</em>, and a <a href="https://example.com">link</a>.</p>',
        );

        $this->assertStringContainsString('**bold**', $result->content ?? null);
        $this->assertStringContainsString('*italic*', $result->content ?? null);
        $this->assertStringContainsString('[link](https://example.com)', $result->content ?? null);
    }

    /** Paragraph with br tags produces line breaks in output */
    public function test_paragraph_with_line_breaks(): void
    {
        $result = HtmlToMarkdown::convert('<p>Line one.<br>Line two.<br>Line three.</p>');

        $this->assertNotSame('', $result->content ?? null ?? '', 'expected non-empty value');
        $this->assertStringContainsString('Line one.', $result->content ?? null);
        $this->assertStringContainsString('Line two.', $result->content ?? null);
        $this->assertStringContainsString('Line three.', $result->content ?? null);
    }

    /** Abbreviation element text is preserved */
    public function test_semantic_abbr(): void
    {
        $result = HtmlToMarkdown::convert('<p>The <abbr title="World Wide Web">WWW</abbr> is global.</p>');

        $this->assertStringContainsString('WWW', $result->content ?? null);
    }

    /** Article element wrapping content preserves inner content */
    public function test_semantic_article(): void
    {
        $result = HtmlToMarkdown::convert('<article><h2>Article Title</h2><p>Article body.</p></article>');

        $this->assertStringContainsString('Article Title', $result->content ?? null);
        $this->assertStringContainsString('Article body.', $result->content ?? null);
    }

    /** Definition list with term and description */
    public function test_semantic_definition_list(): void
    {
        $result = HtmlToMarkdown::convert(
            '<dl><dt>HTML</dt><dd>HyperText Markup Language</dd><dt>CSS</dt><dd>Cascading Style Sheets</dd></dl>',
        );

        $this->assertStringContainsString('HTML', $result->content ?? null);
        $this->assertStringContainsString('HyperText Markup Language', $result->content ?? null);
        $this->assertStringContainsString('CSS', $result->content ?? null);
        $this->assertStringContainsString('Cascading Style Sheets', $result->content ?? null);
    }

    /** Details and summary elements produce readable output */
    public function test_semantic_details_summary(): void
    {
        $result = HtmlToMarkdown::convert(
            '<details><summary>Click to expand</summary><p>Hidden content here.</p></details>',
        );

        $this->assertNotSame('', $result->content ?? null ?? '', 'expected non-empty value');
        $this->assertStringContainsString('Click to expand', $result->content ?? null);
    }

    /** Horizontal rule produces a separator in output */
    public function test_semantic_hr(): void
    {
        $result = HtmlToMarkdown::convert('<p>Above</p><hr><p>Below</p>');

        $this->assertNotSame('', $result->content ?? null ?? '', 'expected non-empty value');
        $this->assertStringContainsString('Above', $result->content ?? null);
        $this->assertStringContainsString('Below', $result->content ?? null);
    }

    /** Mark tag produces highlighted output */
    public function test_semantic_mark_highlight(): void
    {
        $result = HtmlToMarkdown::convert('<p>This is <mark>highlighted text</mark> in a sentence.</p>');

        $this->assertNotSame('', $result->content ?? null ?? '', 'expected non-empty value');
        $this->assertStringContainsString('highlighted text', $result->content ?? null);
    }

    /** Section element with heading preserves structure */
    public function test_semantic_section_with_heading(): void
    {
        $result = HtmlToMarkdown::convert('<section><h3>Section Heading</h3><p>Section content.</p></section>');

        $this->assertStringContainsString('Section Heading', $result->content ?? null);
        $this->assertStringContainsString('Section content.', $result->content ?? null);
    }

    /** Subscript and superscript elements are preserved in output */
    public function test_semantic_sub_superscript(): void
    {
        $result = HtmlToMarkdown::convert('<p>H<sub>2</sub>O and E=mc<sup>2</sup></p>');

        $this->assertNotSame('', $result->content ?? null ?? '', 'expected non-empty value');
        $this->assertStringContainsString('H', $result->content ?? null);
        $this->assertStringContainsString('2', $result->content ?? null);
        $this->assertStringContainsString('O', $result->content ?? null);
        $this->assertStringContainsString('E=mc', $result->content ?? null);
    }

    /** Simple table with header */
    public function test_simple_table(): void
    {
        $result = HtmlToMarkdown::convert(
            '<table><thead><tr><th>Name</th><th>Age</th></tr></thead><tbody><tr><td>Alice</td><td>30</td></tr></tbody></table>',
        );

        $this->assertStringContainsString('Name', $result->content ?? null);
        $this->assertStringContainsString('Age', $result->content ?? null);
        $this->assertStringContainsString('Alice', $result->content ?? null);
        $this->assertStringContainsString('30', $result->content ?? null);
        $this->assertStringContainsString('|', $result->content ?? null);
        $this->assertStringContainsString('---', $result->content ?? null);
    }

    /** The deprecated strike tag renders as strikethrough */
    public function test_strike_renders_as_strikethrough(): void
    {
        $result = HtmlToMarkdown::convert('<p><strike>gone</strike></p>');

        $this->assertEquals("~~gone~~\n", $result->content ?? null);
    }

    /** Empty table produces no output or minimal output */
    public function test_table_empty(): void
    {
        $result = HtmlToMarkdown::convert('<table></table>');

        $this->assertEquals('', $result->content ?? null);
    }

    /** Adjacent paragraphs in a layout-table cell are separated rather than concatenated into one run of text */
    public function test_table_layout_cell_paragraphs_stay_separated(): void
    {
        $options = \HtmlToMarkdown\ConversionOptions::from_json(json_encode(['compactTables' => true]));
        $result = HtmlToMarkdown::convert(
            '<table><tr><td><p><b>Alice Example</b></p><p><i>Customer Service</i></p></td><td>Logo</td></tr><tr><td>A</td><td>B</td><td>C</td></tr></table>',
            $options,
        );

        $this->assertStringNotContainsString('**Alice Example***Customer Service*', $result->content ?? null);
        $this->assertStringContainsString('Alice Example', $result->content ?? null);
        $this->assertStringContainsString('Customer Service', $result->content ?? null);
        $this->assertStringContainsString('Logo', $result->content ?? null);
    }

    /** A straight chain of one-nested-table-per-cell tables renders as pipe tables, not a broken layout list */
    public function test_table_nested_chain_not_misclassified_as_layout(): void
    {
        $result = HtmlToMarkdown::convert(
            '<table><tr><td><table><tr><td><table><tr><td>leaf</td></tr></table></td></tr></table></td></tr></table>',
        );

        $this->assertNotSame('', $result->content ?? null ?? '', 'expected non-empty value');
        $this->assertStringContainsString('leaf', $result->content ?? null);
        $this->assertStringContainsString('| ---', $result->content ?? null);
    }

    /** A table nested in a single-cell row renders as its own table */
    public function test_table_nested_in_single_cell_renders_separately(): void
    {
        $result = HtmlToMarkdown::convert(
            '<table><tr><th>Summary</th></tr><tr><td>Before<table><tr><th>Item</th><th>Amount</th></tr><tr><td>Alpha</td><td>10</td></tr></table></td></tr></table>',
        );

        $this->assertStringContainsString('| Item  | Amount |', $result->content ?? null);
        $this->assertStringContainsString('| Alpha | 10     |', $result->content ?? null);
        $this->assertStringNotContainsString("\\|", $result->content ?? null);
    }

    /** Table without thead uses first row as implied header */
    public function test_table_no_thead(): void
    {
        $result = HtmlToMarkdown::convert(
            '<table><tr><td>Product</td><td>Price</td></tr><tr><td>Apple</td><td>1.00</td></tr></table>',
        );

        $this->assertNotSame('', $result->content ?? null ?? '', 'expected non-empty value');
        $this->assertStringContainsString('Product', $result->content ?? null);
        $this->assertStringContainsString('Price', $result->content ?? null);
        $this->assertStringContainsString('Apple', $result->content ?? null);
        $this->assertStringContainsString('1.00', $result->content ?? null);
        $this->assertStringContainsString('|', $result->content ?? null);
    }

    /** Table cells containing pipe characters are escaped in output */
    public function test_table_pipe_chars_in_content(): void
    {
        $result = HtmlToMarkdown::convert(
            '<table><thead><tr><th>Expression</th><th>Result</th></tr></thead><tbody><tr><td>a | b</td><td>true</td></tr></tbody></table>',
        );

        $this->assertNotSame('', $result->content ?? null ?? '', 'expected non-empty value');
        $this->assertStringContainsString('Expression', $result->content ?? null);
        $this->assertStringContainsString('Result', $result->content ?? null);
        $this->assertStringContainsString('true', $result->content ?? null);
    }

    /** A data row with fewer cells than the header is padded to the declared column count */
    public function test_table_ragged_row_fewer_cells_than_header(): void
    {
        $result = HtmlToMarkdown::convert(
            '<table><tr><th>A</th><th>B</th><th>C</th></tr><tr><td>1</td><td>2</td></tr></table>',
        );

        $this->assertEquals("| A | B | C |\n| --- | --- | --- |\n| 1 | 2 |   |\n", $result->content ?? null);
    }

    /** A data row with more cells than the header widens the separator so no cell is silently dropped */
    public function test_table_ragged_row_more_cells_than_header(): void
    {
        $result = HtmlToMarkdown::convert(
            '<table><tr><th>A</th><th>B</th></tr><tr><td>1</td><td>2</td><td>3</td></tr></table>',
        );

        $this->assertEquals("| A | B |   |\n| --- | --- | --- |\n| 1 | 2 | 3 |\n", $result->content ?? null);
    }

    /** Table with column alignment attributes */
    public function test_table_with_alignment(): void
    {
        $result = HtmlToMarkdown::convert(
            '<table><thead><tr><th align="left">Left</th><th align="center">Center</th><th align="right">Right</th></tr></thead><tbody><tr><td>L</td><td>C</td><td>R</td></tr></tbody></table>',
        );

        $this->assertNotSame('', $result->content ?? null ?? '', 'expected non-empty value');
        $this->assertStringContainsString('Left', $result->content ?? null);
        $this->assertStringContainsString('Center', $result->content ?? null);
        $this->assertStringContainsString('Right', $result->content ?? null);
        $this->assertStringContainsString('L', $result->content ?? null);
        $this->assertStringContainsString('C', $result->content ?? null);
        $this->assertStringContainsString('R', $result->content ?? null);
        $this->assertStringContainsString('|', $result->content ?? null);
    }

    /** Table with colspan attribute in a header cell */
    public function test_table_with_colspan(): void
    {
        $result = HtmlToMarkdown::convert(
            '<table><thead><tr><th colspan="2">Full Name</th></tr></thead><tbody><tr><td>John</td><td>Doe</td></tr></tbody></table>',
        );

        $this->assertNotSame('', $result->content ?? null ?? '', 'expected non-empty value');
        $this->assertStringContainsString('Full Name', $result->content ?? null);
        $this->assertStringContainsString('John', $result->content ?? null);
        $this->assertStringContainsString('Doe', $result->content ?? null);
    }

    /** A whitespace-only strikethrough keeps the word separator */
    public function test_tagged_space_in_strikethrough_kept(): void
    {
        $result = HtmlToMarkdown::convert('<p>A<del> </del>B</p>');

        $this->assertEquals("A B\n", $result->content ?? null);
    }

    /** A whitespace-only subscript keeps the word separator */
    public function test_tagged_space_in_subscript_kept(): void
    {
        $result = HtmlToMarkdown::convert('<p>A<sub> </sub>B</p>');

        $this->assertEquals("A B\n", $result->content ?? null);
    }

    /** A whitespace-only inline element contributes exactly one space */
    public function test_tagged_space_not_duplicated(): void
    {
        $result = HtmlToMarkdown::convert('<p>A<i> </i>B</p>');

        $this->assertEquals("A B\n", $result->content ?? null);
    }

    /** Unordered list */
    public function test_unordered_list(): void
    {
        $result = HtmlToMarkdown::convert('<ul><li>Item 1</li><li>Item 2</li><li>Item 3</li></ul>');

        $this->assertStringContainsString('- Item 1', $result->content ?? null);
        $this->assertStringContainsString('- Item 2', $result->content ?? null);
        $this->assertStringContainsString('- Item 3', $result->content ?? null);
    }

    /** A code span whose content is a single space survives */
    public function test_whitespace_only_code_span_kept(): void
    {
        $result = HtmlToMarkdown::convert('<p>A<code> </code>B</p>');

        $this->assertEquals("A` `B\n", $result->content ?? null);
    }
}
