<?php

// E2e tests for category: options
// Generated by html-to-markdown-e2e-generator — do not edit manually.

declare(strict_types=1);

namespace HtmlToMarkdown\Tests;

use PHPUnit\Framework\TestCase;

class OptionsTest extends TestCase
{
    /**
     * Backticks code block style uses triple backtick fences
     */
    public function testOptionsCodeBlockBackticks(): void
    {
        $html = "<pre><code class=\"language-js\">console.log('hi');</code></pre>";
        $options = json_decode("{\"codeBlockStyle\":\"backticks\"}", true);
        $result = html_to_markdown_convert($html, $options);
        $content = $result['content'] ?? '';

        $this->assertStringContainsString("```", $content);
        $this->assertStringContainsString("console.log('hi');", $content);
    }

    /**
     * Tildes code block style uses triple tilde fences
     */
    public function testOptionsCodeBlockTildes(): void
    {
        $html = "<pre><code>some code</code></pre>";
        $options = json_decode("{\"codeBlockStyle\":\"tildes\"}", true);
        $result = html_to_markdown_convert($html, $options);
        $content = $result['content'] ?? '';

        $this->assertStringContainsString("~~~", $content);
        $this->assertStringContainsString("some code", $content);
    }

    /**
     * escape_asterisks option escapes asterisks in plain text
     */
    public function testOptionsEscapeAsterisks(): void
    {
        $html = "<p>Use 2*3 = 6 in math.</p>";
        $options = json_decode("{\"escapeAsterisks\":true}", true);
        $result = html_to_markdown_convert($html, $options);
        $content = $result['content'] ?? '';

        $this->assertNotEmpty(trim($content), 'expected non-empty content');
        $this->assertStringContainsString("2", $content);
        $this->assertStringContainsString("3", $content);
        $this->assertStringContainsString("6", $content);
    }

    /**
     * escape_misc option escapes miscellaneous markdown characters
     */
    public function testOptionsEscapeMisc(): void
    {
        $html = "<p>Use # and | and ~ in text.</p>";
        $options = json_decode("{\"escapeMisc\":true}", true);
        $result = html_to_markdown_convert($html, $options);
        $content = $result['content'] ?? '';

        $this->assertNotEmpty(trim($content), 'expected non-empty content');
        $this->assertStringContainsString("Use", $content);
        $this->assertStringContainsString("and", $content);
        $this->assertStringContainsString("in text.", $content);
    }

    /**
     * escape_underscores option escapes underscores in plain text
     */
    public function testOptionsEscapeUnderscores(): void
    {
        $html = "<p>The variable_name is defined.</p>";
        $options = json_decode("{\"escapeUnderscores\":true}", true);
        $result = html_to_markdown_convert($html, $options);
        $content = $result['content'] ?? '';

        $this->assertNotEmpty(trim($content), 'expected non-empty content');
        $this->assertStringContainsString("variable", $content);
        $this->assertStringContainsString("name", $content);
        $this->assertStringContainsString("defined.", $content);
    }

    /**
     * ATX heading style produces hash-prefixed headings
     */
    public function testOptionsHeadingStyleAtx(): void
    {
        $html = "<h1>Title</h1><h2>Subtitle</h2>";
        $options = json_decode("{\"headingStyle\":\"atx\"}", true);
        $result = html_to_markdown_convert($html, $options);
        $content = $result['content'] ?? '';

        $this->assertStringContainsString("# Title", $content);
        $this->assertStringContainsString("## Subtitle", $content);
    }

    /**
     * ATX closed heading style adds closing hashes
     */
    public function testOptionsHeadingStyleAtxClosed(): void
    {
        $html = "<h1>Closed Heading</h1>";
        $options = json_decode("{\"headingStyle\":\"atxClosed\"}", true);
        $result = html_to_markdown_convert($html, $options);
        $content = $result['content'] ?? '';

        $this->assertStringContainsString("# Closed Heading #", $content);
    }

    /**
     * Underlined heading style produces setext-style headings for h1 and h2
     */
    public function testOptionsHeadingStyleUnderlined(): void
    {
        $html = "<h1>Main Title</h1>";
        $options = json_decode("{\"headingStyle\":\"underlined\"}", true);
        $result = html_to_markdown_convert($html, $options);
        $content = $result['content'] ?? '';

        $this->assertNotEmpty(trim($content), 'expected non-empty content');
        $this->assertStringContainsString("Main Title", $content);
    }

    /**
     * Custom bullet character for unordered lists
     */
    public function testOptionsListCustomBullets(): void
    {
        $html = "<ul><li>Item A</li><li>Item B</li></ul>";
        $options = json_decode("{\"bullets\":\"*\"}", true);
        $result = html_to_markdown_convert($html, $options);
        $content = $result['content'] ?? '';

        $this->assertStringContainsString("* Item A", $content);
        $this->assertStringContainsString("* Item B", $content);
    }

    /**
     * Tab indentation type for nested list items
     */
    public function testOptionsListIndentTabs(): void
    {
        $html = "<ul><li>Parent<ul><li>Child</li></ul></li></ul>";
        $options = json_decode("{\"listIndentType\":\"tabs\"}", true);
        $result = html_to_markdown_convert($html, $options);
        $content = $result['content'] ?? '';

        $this->assertNotEmpty(trim($content), 'expected non-empty content');
        $this->assertStringContainsString("Parent", $content);
        $this->assertStringContainsString("Child", $content);
    }

    /**
     * Without max_depth, deeply nested HTML converts successfully
     */
    public function testOptionsMaxDepthDefaultUnlimited(): void
    {
        $html = "<div><div><div><div><div><p>Deep content</p></div></div></div></div></div>";
        $result = html_to_markdown_convert($html);
        $content = $result['content'] ?? '';

        $this->assertNotEmpty(trim($content), 'expected non-empty content');
        $this->assertStringContainsString("Deep content", $content);
    }

    /**
     * Conversion returns an error when DOM depth exceeds max_depth
     */
    public function testOptionsMaxDepthExceedsLimit(): void
    {
        $html = "<div><div><div><div><p>Too deep</p></div></div></div></div>";
        $options = json_decode("{\"maxDepth\":2}", true);
        $this->expectException(\Exception::class);
        $this->expectExceptionMessageMatches('/max_depth/');
        html_to_markdown_convert($html, $options);
    }

    /**
     * Conversion succeeds when DOM depth is within max_depth
     */
    public function testOptionsMaxDepthWithinLimit(): void
    {
        $html = "<div><p>Shallow content</p></div>";
        $options = json_decode("{\"maxDepth\":10}", true);
        $result = html_to_markdown_convert($html, $options);
        $content = $result['content'] ?? '';

        $this->assertNotEmpty(trim($content), 'expected non-empty content');
        $this->assertStringContainsString("Shallow content", $content);
    }

    /**
     * Djot output format produces djot-compatible markup
     */
    public function testOptionsOutputFormatDjot(): void
    {
        $html = "<p>Simple paragraph.</p>";
        $options = json_decode("{\"outputFormat\":\"djot\"}", true);
        $result = html_to_markdown_convert($html, $options);
        $content = $result['content'] ?? '';

        $this->assertNotEmpty(trim($content), 'expected non-empty content');
        $this->assertStringContainsString("Simple paragraph.", $content);
    }

    /**
     * Default markdown output format produces standard markdown
     */
    public function testOptionsOutputFormatMarkdown(): void
    {
        $html = "<h1>Title</h1><p>Some text.</p>";
        $options = json_decode("{\"outputFormat\":\"markdown\"}", true);
        $result = html_to_markdown_convert($html, $options);
        $content = $result['content'] ?? '';

        $this->assertStringContainsString("# Title", $content);
        $this->assertStringContainsString("Some text.", $content);
    }

    /**
     * Plain text output format strips markdown syntax
     */
    public function testOptionsOutputFormatPlain(): void
    {
        $html = "<h1>Title</h1><p>Some <strong>bold</strong> text.</p>";
        $options = json_decode("{\"outputFormat\":\"plain\"}", true);
        $result = html_to_markdown_convert($html, $options);
        $content = $result['content'] ?? '';

        $this->assertStringContainsString("Title", $content);
        $this->assertStringContainsString("bold", $content);
        $this->assertStringContainsString("text.", $content);
        $this->assertStringNotContainsString("**bold**", $content);
        $this->assertStringNotContainsString("# Title", $content);
    }

    /**
     * Normalized whitespace mode collapses multiple spaces
     */
    public function testOptionsWhitespaceNormalized(): void
    {
        $html = "<p>Text   with    extra   spaces.</p>";
        $options = json_decode("{\"whitespaceMode\":\"normalized\"}", true);
        $result = html_to_markdown_convert($html, $options);
        $content = $result['content'] ?? '';

        $this->assertNotEmpty(trim($content), 'expected non-empty content');
        $this->assertStringContainsString("Text", $content);
        $this->assertStringContainsString("with", $content);
        $this->assertStringContainsString("extra", $content);
        $this->assertStringContainsString("spaces.", $content);
    }

    /**
     * Strict whitespace mode preserves whitespace as-is
     */
    public function testOptionsWhitespaceStrict(): void
    {
        $html = "<p>Preserved   spacing.</p>";
        $options = json_decode("{\"whitespaceMode\":\"strict\"}", true);
        $result = html_to_markdown_convert($html, $options);
        $content = $result['content'] ?? '';

        $this->assertNotEmpty(trim($content), 'expected non-empty content');
        $this->assertStringContainsString("Preserved", $content);
        $this->assertStringContainsString("spacing.", $content);
    }

    /**
     * Wrap option disabled preserves long lines without breaking
     */
    public function testOptionsWrapDisabled(): void
    {
        $html = "<p>This is a long paragraph that should not be wrapped at all because wrapping is disabled.</p>";
        $options = json_decode("{\"wrap\":false}", true);
        $result = html_to_markdown_convert($html, $options);
        $content = $result['content'] ?? '';

        $this->assertStringContainsString("This is a long paragraph that should not be wrapped at all because wrapping is disabled.", $content);
    }

    /**
     * Wrap option enabled with custom width wraps long lines
     */
    public function testOptionsWrapEnabled(): void
    {
        $html = "<p>This is a long paragraph that should be wrapped at the specified column width when the wrap option is enabled.</p>";
        $options = json_decode("{\"wrap\":true,\"wrapWidth\":40}", true);
        $result = html_to_markdown_convert($html, $options);
        $content = $result['content'] ?? '';

        $this->assertNotEmpty(trim($content), 'expected non-empty content');
        $this->assertStringContainsString("This is a long paragraph", $content);
    }

}
