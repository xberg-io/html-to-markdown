// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:cb17f151d028a481a20959f465c73c1974164a0ea218e02fbc91b405992a148d
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
const std = @import("std");
const testing = std.testing;
const html_to_markdown_rs = @import("html_to_markdown_rs");

// Suppress C++ global destructor aborts that break zig's --listen=- IPC
extern "c" fn signal(sig: i32, handler: usize) usize;
var _abort_handler_installed: bool = false;
fn suppress_abort() void {
    if (!_abort_handler_installed) {
        // SIGABRT = 6 on POSIX; SIG_IGN = 1
        _ = signal(6, 1);
        _abort_handler_installed = true;
    }
}

// E2e tests for category: real-world

test "real_world_blog_post" {
    // Blog post with headings, paragraphs, code blocks, and links converts to readable Markdown
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<article><h1>Getting Started with Rust</h1><p>Rust is a systems programming language focused on <strong>safety</strong>, <em>performance</em>, and concurrency. It was created by <a href=\"https://www.mozilla.org\">Mozilla</a> and has grown significantly in popularity.</p><h2>Installation</h2><p>Install Rust using the official installer:</p><pre><code class=\"language-bash\">curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh</code></pre><h2>Hello World</h2><p>Create your first Rust program:</p><pre><code class=\"language-rust\">fn main() {\n    println!(\"Hello, world!\");\n}</code></pre><p>Run it with <code>cargo run</code> from your project directory.</p><h2>Key Concepts</h2><ul><li>Ownership and borrowing</li><li>Lifetimes</li><li>Traits and generics</li><li>Pattern matching</li></ul><p>For more information, visit the <a href=\"https://doc.rust-lang.org/book/\">Rust Book</a>.</p></article>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "# Getting Started with Rust") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "## Installation") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "## Hello World") != null);
    }
    {
        const _jva3 = result.object.get("content").?;
        const _jsa3 = if (_jva3 == .string) _jva3.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva3, .{});
        defer if (_jva3 != .string) std.heap.c_allocator.free(_jsa3);
        try testing.expect(std.mem.indexOf(u8, _jsa3, "## Key Concepts") != null);
    }
    {
        const _jva4 = result.object.get("content").?;
        const _jsa4 = if (_jva4 == .string) _jva4.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva4, .{});
        defer if (_jva4 != .string) std.heap.c_allocator.free(_jsa4);
        try testing.expect(std.mem.indexOf(u8, _jsa4, "cargo run") != null);
    }
    {
        const _jva5 = result.object.get("content").?;
        const _jsa5 = if (_jva5 == .string) _jva5.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva5, .{});
        defer if (_jva5 != .string) std.heap.c_allocator.free(_jsa5);
        try testing.expect(std.mem.indexOf(u8, _jsa5, "[Mozilla](https://www.mozilla.org)") != null);
    }
    {
        const _jva6 = result.object.get("content").?;
        const _jsa6 = if (_jva6 == .string) _jva6.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva6, .{});
        defer if (_jva6 != .string) std.heap.c_allocator.free(_jsa6);
        try testing.expect(std.mem.indexOf(u8, _jsa6, "- Ownership and borrowing") != null);
    }
}

test "real_world_documentation_page" {
    // Documentation page with nested lists, code examples, and blockquotes converts correctly
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<div class=\"docs\"><h1>API Reference</h1><p>This guide covers the core API for the <code>html-to-markdown</code> library.</p><blockquote><p><strong>Note:</strong> All functions are thread-safe and can be called from multiple threads concurrently.</p></blockquote><h2>convert_html</h2><p>Converts an HTML string to Markdown format.</p><pre><code class=\"language-rust\">pub fn convert_html(html: &amp;str) -&gt; Result&lt;String, ConversionError&gt;</code></pre><h3>Parameters</h3><ul><li><code>html</code> - The HTML input string<ul><li>Must be valid UTF-8</li><li>Maximum size: 50MB</li></ul></li></ul><h3>Returns</h3><ul><li><code>Ok(String)</code> - The converted Markdown</li><li><code>Err(ConversionError)</code> - If conversion fails</li></ul><h3>Example</h3><pre><code class=\"language-rust\">let markdown = convert_html(\"&lt;h1&gt;Hello&lt;/h1&gt;\").unwrap();\nassert_eq!(markdown, \"# Hello\");</code></pre><h2>ConversionOptions</h2><p>Configure conversion behavior using the builder pattern:</p><pre><code class=\"language-rust\">let options = ConversionOptions::builder()\n    .heading_style(HeadingStyle::ATX)\n    .code_block_style(CodeBlockStyle::Fenced)\n    .build();</code></pre><blockquote><p>See the <a href=\"/docs/options\">options reference</a> for a full list of configuration values.</p></blockquote></div>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "# API Reference") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "## convert_html") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "### Parameters") != null);
    }
    {
        const _jva3 = result.object.get("content").?;
        const _jsa3 = if (_jva3 == .string) _jva3.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva3, .{});
        defer if (_jva3 != .string) std.heap.c_allocator.free(_jsa3);
        try testing.expect(std.mem.indexOf(u8, _jsa3, "### Returns") != null);
    }
    {
        const _jva4 = result.object.get("content").?;
        const _jsa4 = if (_jva4 == .string) _jva4.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva4, .{});
        defer if (_jva4 != .string) std.heap.c_allocator.free(_jsa4);
        try testing.expect(std.mem.indexOf(u8, _jsa4, "### Example") != null);
    }
    {
        const _jva5 = result.object.get("content").?;
        const _jsa5 = if (_jva5 == .string) _jva5.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva5, .{});
        defer if (_jva5 != .string) std.heap.c_allocator.free(_jsa5);
        try testing.expect(std.mem.indexOf(u8, _jsa5, "## ConversionOptions") != null);
    }
    {
        const _jva6 = result.object.get("content").?;
        const _jsa6 = if (_jva6 == .string) _jva6.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva6, .{});
        defer if (_jva6 != .string) std.heap.c_allocator.free(_jsa6);
        try testing.expect(std.mem.indexOf(u8, _jsa6, "> ") != null);
    }
    {
        const _jva7 = result.object.get("content").?;
        const _jsa7 = if (_jva7 == .string) _jva7.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva7, .{});
        defer if (_jva7 != .string) std.heap.c_allocator.free(_jsa7);
        try testing.expect(std.mem.indexOf(u8, _jsa7, "thread-safe") != null);
    }
    {
        const _jva8 = result.object.get("content").?;
        const _jsa8 = if (_jva8 == .string) _jva8.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva8, .{});
        defer if (_jva8 != .string) std.heap.c_allocator.free(_jsa8);
        try testing.expect(std.mem.indexOf(u8, _jsa8, "convert_html") != null);
    }
    {
        const _jva9 = result.object.get("content").?;
        const _jsa9 = if (_jva9 == .string) _jva9.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva9, .{});
        defer if (_jva9 != .string) std.heap.c_allocator.free(_jsa9);
        try testing.expect(std.mem.indexOf(u8, _jsa9, "ConversionOptions") != null);
    }
}

test "real_world_product_page" {
    // Product page with table, images, and lists converts correctly
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<div class=\"product\"><h1>Wireless Keyboard Pro</h1><img src=\"https://example.com/keyboard.jpg\" alt=\"Wireless Keyboard Pro\"><p>The ultimate wireless keyboard for professionals. Features a comfortable layout with <strong>backlit keys</strong> and <em>ultra-long battery life</em>.</p><h2>Specifications</h2><table><thead><tr><th>Feature</th><th>Details</th></tr></thead><tbody><tr><td>Battery Life</td><td>Up to 12 months</td></tr><tr><td>Connectivity</td><td>Bluetooth 5.0</td></tr><tr><td>Key Travel</td><td>2mm</td></tr><tr><td>Weight</td><td>750g</td></tr></tbody></table><h2>What's in the Box</h2><ul><li>Wireless Keyboard Pro</li><li>USB-C charging cable</li><li>USB receiver dongle</li><li>Quick start guide</li></ul><h2>Compatibility</h2><p>Compatible with <strong>Windows</strong>, <strong>macOS</strong>, <strong>Linux</strong>, <strong>iOS</strong>, and <strong>Android</strong>.</p></div>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "# Wireless Keyboard Pro") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "![Wireless Keyboard Pro](https://example.com/keyboard.jpg)") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "## Specifications") != null);
    }
    {
        const _jva3 = result.object.get("content").?;
        const _jsa3 = if (_jva3 == .string) _jva3.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva3, .{});
        defer if (_jva3 != .string) std.heap.c_allocator.free(_jsa3);
        try testing.expect(std.mem.indexOf(u8, _jsa3, "Battery Life") != null);
    }
    {
        const _jva4 = result.object.get("content").?;
        const _jsa4 = if (_jva4 == .string) _jva4.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva4, .{});
        defer if (_jva4 != .string) std.heap.c_allocator.free(_jsa4);
        try testing.expect(std.mem.indexOf(u8, _jsa4, "12 months") != null);
    }
    {
        const _jva5 = result.object.get("content").?;
        const _jsa5 = if (_jva5 == .string) _jva5.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva5, .{});
        defer if (_jva5 != .string) std.heap.c_allocator.free(_jsa5);
        try testing.expect(std.mem.indexOf(u8, _jsa5, "Bluetooth 5.0") != null);
    }
    {
        const _jva6 = result.object.get("content").?;
        const _jsa6 = if (_jva6 == .string) _jva6.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva6, .{});
        defer if (_jva6 != .string) std.heap.c_allocator.free(_jsa6);
        try testing.expect(std.mem.indexOf(u8, _jsa6, "## What's in the Box") != null);
    }
    {
        const _jva7 = result.object.get("content").?;
        const _jsa7 = if (_jva7 == .string) _jva7.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva7, .{});
        defer if (_jva7 != .string) std.heap.c_allocator.free(_jsa7);
        try testing.expect(std.mem.indexOf(u8, _jsa7, "USB-C charging cable") != null);
    }
    {
        const _jva8 = result.object.get("content").?;
        const _jsa8 = if (_jva8 == .string) _jva8.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva8, .{});
        defer if (_jva8 != .string) std.heap.c_allocator.free(_jsa8);
        try testing.expect(std.mem.indexOf(u8, _jsa8, "|") != null);
    }
    {
        const _jva9 = result.object.get("content").?;
        const _jsa9 = if (_jva9 == .string) _jva9.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva9, .{});
        defer if (_jva9 != .string) std.heap.c_allocator.free(_jsa9);
        try testing.expect(std.mem.indexOf(u8, _jsa9, "---") != null);
    }
}
