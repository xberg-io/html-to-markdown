// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:fdbb9cafd32b125b64abd4acbff204711d3d5b50282abb93bbf1b9ff46c6f1c4
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
const std = @import("std");
const testing = std.testing;
const html_to_markdown_rs = @import("html_to_markdown_rs");

// Suppress C++ global destructor aborts that break zig's --listen=- IPC
extern "c" fn signal(sig: i32, handler: usize) usize;
var _abort_handler_installed: bool = false;
fn suppress_abort() void {
    if (!_abort_handler_installed) {
        // SIGABRT = 6 on POSIX; SIG_IGN = 1
        _ = signal(6, 1);
        _abort_handler_installed = true;
    }
}

// E2e tests for category: conversion

test "blockquote_multiple_paragraphs" {
    // Blockquote with multiple paragraphs has each paragraph prefixed
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<blockquote><p>First paragraph.</p><p>Second paragraph.</p></blockquote>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "> First paragraph.") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "> Second paragraph.") != null);
    }
}

test "blockquote_nested" {
    // Nested blockquote produces double-prefixed lines
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<blockquote><p>Outer quote.</p><blockquote><p>Inner quote.</p></blockquote></blockquote>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Outer quote.") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "Inner quote.") != null);
    }
}

test "blockquote_simple" {
    // Simple blockquote
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<blockquote><p>Quote text</p></blockquote>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "> Quote text") != null);
    }
}

test "blockquote_with_list" {
    // Blockquote containing a list preserves list items inside quote
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<blockquote><p>Quote intro:</p><ul><li>Point one</li><li>Point two</li></ul></blockquote>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Quote intro:") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "Point one") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "Point two") != null);
    }
}

test "bold_and_italic" {
    // Nested bold and italic
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p><strong><em>both</em></strong></p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "***both***") != null);
    }
}

test "bold_strong" {
    // Strong tag converts to bold
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p><strong>bold</strong></p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "**bold**") != null);
    }
}

test "code_block" {
    // Code block with language preserves content
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<pre><code class=\"language-python\">print('hello')</code></pre>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "print('hello')") != null);
    }
}

test "code_block_no_language" {
    // Code block without a language class preserves content
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<pre><code>plain code here</code></pre>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "plain code here") != null);
    }
}

test "code_inline_in_paragraph" {
    // Inline code element nested inside a paragraph
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>Call the <code>initialize()</code> method first.</p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "`initialize()`") != null);
    }
}

test "code_with_backticks_in_content" {
    // Inline code containing backtick characters is properly escaped
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>Use <code>`backtick` here</code> carefully.</p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "backtick") != null);
    }
}

test "conversion_autolink_filename_not_autolinked" {
    // Bare filename href (no URI scheme) renders as inline link, not autolink (#397)
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<a href=\"foobar.png\">foobar.png</a>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("[foobar.png](foobar.png)", std.mem.trim(u8, result.object.get("content").?.string, " \n\r\t"));
}

test "conversion_autolink_https_url" {
    // Absolute https URL with matching text renders as autolink (#397)
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<a href=\"https://example.com\">https://example.com</a>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("<https://example.com>", std.mem.trim(u8, result.object.get("content").?.string, " \n\r\t"));
}

test "conversion_autolink_mailto" {
    // mailto: link with matching email text renders as autolink (#397)
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<a href=\"mailto:a@b.com\">a@b.com</a>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("<a@b.com>", std.mem.trim(u8, result.object.get("content").?.string, " \n\r\t"));
}

test "conversion_autolink_mixed_filename_and_url" {
    // Mixed filename + URL: only the URL becomes an autolink (#397)
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<a href=\"foobar.png\">foobar.png</a> <a href=\"https://www.heise.de\">https://www.heise.de</a>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("[foobar.png](foobar.png) <https://www.heise.de>", std.mem.trim(u8, result.object.get("content").?.string, " \n\r\t"));
}

test "conversion_autolink_relative_path_not_autolinked" {
    // Relative path href renders as inline link (#397)
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<a href=\"/docs/intro.html\">/docs/intro.html</a>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("[/docs/intro.html](/docs/intro.html)", std.mem.trim(u8, result.object.get("content").?.string, " \n\r\t"));
}

test "emphasis_mark_highlight" {
    // mark tag produces highlighted output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p><mark>highlighted</mark></p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "highlighted") != null);
    }
}

test "emphasis_strikethrough_del" {
    // del tag converts to GFM strikethrough
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p><del>deleted text</del></p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "~~deleted text~~") != null);
    }
}

test "emphasis_strikethrough_s" {
    // s tag converts to GFM strikethrough
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p><s>strikethrough</s></p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "~~strikethrough~~") != null);
    }
}

test "emphasis_subscript" {
    // sub tag content is preserved
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>H<sub>2</sub>O</p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "H") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "2") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "O") != null);
    }
}

test "emphasis_superscript" {
    // sup tag content is preserved
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>x<sup>2</sup></p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "x") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "2") != null);
    }
}

test "emphasis_underline_u" {
    // u tag content is preserved in output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p><u>underlined</u></p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "underlined") != null);
    }
}

test "form_input_elements" {
    // Form input elements produce readable output without form mechanics
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<form><label for=\"name\">Name:</label><input type=\"text\" id=\"name\" placeholder=\"Enter name\"></form>", "{\"preprocessing\":{\"remove_forms\":false}}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Name") != null);
    }
}

test "form_select_options" {
    // Select element with options produces readable output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<form><label>Color:</label><select><option value=\"red\">Red</option><option value=\"blue\" selected>Blue</option><option value=\"green\">Green</option></select></form>", "{\"preprocessing\":{\"remove_forms\":false}}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Color") != null);
    }
}

test "form_textarea" {
    // Textarea element produces readable output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<form><label>Message:</label><textarea>Default text content</textarea></form>", "{\"preprocessing\":{\"remove_forms\":false}}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Message") != null);
    }
}

test "heading_h1" {
    // H1 heading
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<h1>Heading 1</h1>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("# Heading 1", std.mem.trim(u8, result.object.get("content").?.string, " \n\r\t"));
}

test "heading_h2" {
    // H2 heading
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<h2>Heading 2</h2>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("## Heading 2", std.mem.trim(u8, result.object.get("content").?.string, " \n\r\t"));
}

test "heading_h3" {
    // H3 heading
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<h3>Heading 3</h3>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("### Heading 3", std.mem.trim(u8, result.object.get("content").?.string, " \n\r\t"));
}

test "heading_h4" {
    // H4 heading
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<h4>Heading 4</h4>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("#### Heading 4", std.mem.trim(u8, result.object.get("content").?.string, " \n\r\t"));
}

test "heading_h5" {
    // H5 heading
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<h5>Heading 5</h5>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("##### Heading 5", std.mem.trim(u8, result.object.get("content").?.string, " \n\r\t"));
}

test "heading_h6" {
    // H6 heading
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<h6>Heading 6</h6>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("###### Heading 6", std.mem.trim(u8, result.object.get("content").?.string, " \n\r\t"));
}

test "image_figure_figcaption" {
    // Figure with figcaption preserves both image and caption
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<figure><img src=\"sunset.jpg\" alt=\"A sunset\"><figcaption>Beautiful sunset over the ocean</figcaption></figure>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "![A sunset](sunset.jpg)") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "Beautiful sunset over the ocean") != null);
    }
}

test "image_linked" {
    // Image inside an anchor produces a linked image
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<a href=\"https://example.com\"><img src=\"icon.png\" alt=\"Icon\"></a>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "![Icon](icon.png)") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "https://example.com") != null);
    }
}

test "image_no_alt" {
    // Image without alt text produces image markdown
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<img src=\"banner.jpg\">", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "banner.jpg") != null);
    }
}

test "image_simple" {
    // Image with alt text
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<img src=\"photo.jpg\" alt=\"A photo\">", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "![A photo](photo.jpg)") != null);
    }
}

test "image_with_title" {
    // Image with title attribute includes title in output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<img src=\"chart.png\" alt=\"Sales chart\" title=\"Q3 Sales\">", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "![Sales chart](chart.png") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "Q3 Sales") != null);
    }
}

test "inline_code" {
    // Inline code
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>Use <code>console.log()</code> to debug</p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "`console.log()`") != null);
    }
}

test "italic_em" {
    // Em tag converts to italic
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p><em>italic</em></p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "*italic*") != null);
    }
}

test "line_break_br_tag" {
    // Single br tag produces a line break in output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>First line.<br>Second line.</p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "First line.") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "Second line.") != null);
    }
}

test "line_break_hr_tag" {
    // hr tag produces a horizontal separator between content
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>Before rule.</p><hr><p>After rule.</p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Before rule.") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "After rule.") != null);
    }
}

test "line_break_multiple_br" {
    // Multiple consecutive br tags in sequence
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>Start.<br><br>End.</p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Start.") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "End.") != null);
    }
}

test "link_anchor_fragment" {
    // Fragment-only anchor link is preserved
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<a href=\"#section\">Jump to section</a>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "[Jump to section](#section)") != null);
    }
}

test "link_empty_href" {
    // Link with empty href produces output with the link text
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<a href=\"\">No destination</a>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "No destination") != null);
    }
}

test "link_image_inside" {
    // Image inside a link produces a linked image
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<a href=\"https://example.com\"><img src=\"logo.png\" alt=\"Logo\"></a>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "![Logo](logo.png)") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "https://example.com") != null);
    }
}

test "link_mailto" {
    // Mailto link is preserved with mailto: scheme
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<a href=\"mailto:user@example.com\">Email us</a>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "mailto:user@example.com") != null);
    }
}

test "link_simple" {
    // Simple link
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<a href=\"https://example.com\">Example</a>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "[Example](https://example.com)") != null);
    }
}

test "link_with_bold_text" {
    // Link containing bold text preserves formatting
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<a href=\"https://example.com\"><strong>Bold link</strong></a>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "**Bold link**") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "https://example.com") != null);
    }
}

test "link_with_title" {
    // Link with title attribute
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<a href=\"https://example.com\" title=\"Example Site\">Example</a>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "[Example](https://example.com") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "Example Site") != null);
    }
}

test "list_definition_dl" {
    // Definition list with dt and dd elements
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<dl><dt>Term One</dt><dd>Definition of term one.</dd><dt>Term Two</dt><dd>Definition of term two.</dd></dl>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Term One") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "Definition of term one.") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "Term Two") != null);
    }
    {
        const _jva3 = result.object.get("content").?;
        const _jsa3 = if (_jva3 == .string) _jva3.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva3, .{});
        defer if (_jva3 != .string) std.heap.c_allocator.free(_jsa3);
        try testing.expect(std.mem.indexOf(u8, _jsa3, "Definition of term two.") != null);
    }
}

test "list_item_multiple_paragraphs" {
    // List item containing multiple paragraphs
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<ul><li><p>First paragraph in item.</p><p>Second paragraph in item.</p></li><li>Simple item</li></ul>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "First paragraph in item.") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "Second paragraph in item.") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "Simple item") != null);
    }
}

test "list_mixed_nested" {
    // Mixed list: ordered list nested inside unordered list
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<ul><li>Item A<ol><li>Sub 1</li><li>Sub 2</li></ol></li><li>Item B</li></ul>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Item A") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "Sub 1") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "Sub 2") != null);
    }
    {
        const _jva3 = result.object.get("content").?;
        const _jsa3 = if (_jva3 == .string) _jva3.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva3, .{});
        defer if (_jva3 != .string) std.heap.c_allocator.free(_jsa3);
        try testing.expect(std.mem.indexOf(u8, _jsa3, "Item B") != null);
    }
}

test "list_nested_ordered" {
    // Nested ordered list with two levels of depth
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<ol><li>Step 1<ol><li>Step 1a</li><li>Step 1b</li></ol></li><li>Step 2</li></ol>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Step 1") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "Step 1a") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "Step 1b") != null);
    }
    {
        const _jva3 = result.object.get("content").?;
        const _jsa3 = if (_jva3 == .string) _jva3.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva3, .{});
        defer if (_jva3 != .string) std.heap.c_allocator.free(_jsa3);
        try testing.expect(std.mem.indexOf(u8, _jsa3, "Step 2") != null);
    }
}

test "list_nested_unordered" {
    // Nested unordered list with two levels of depth
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<ul><li>Parent A<ul><li>Child A1</li><li>Child A2</li></ul></li><li>Parent B</li></ul>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Parent A") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "Child A1") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "Child A2") != null);
    }
    {
        const _jva3 = result.object.get("content").?;
        const _jsa3 = if (_jva3 == .string) _jva3.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva3, .{});
        defer if (_jva3 != .string) std.heap.c_allocator.free(_jsa3);
        try testing.expect(std.mem.indexOf(u8, _jsa3, "Parent B") != null);
    }
}

test "list_task_checkboxes" {
    // Task list with checked and unchecked checkboxes
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<ul><li><input type=\"checkbox\" checked> Done task</li><li><input type=\"checkbox\"> Pending task</li></ul>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Done task") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "Pending task") != null);
    }
}

test "ordered_list" {
    // Ordered list
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<ol><li>First</li><li>Second</li><li>Third</li></ol>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "1. First") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "2. Second") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "3. Third") != null);
    }
}

test "paragraph_multiple" {
    // Multiple paragraphs are separated by a blank line
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>First paragraph.</p><p>Second paragraph.</p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "First paragraph.") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "Second paragraph.") != null);
    }
}

test "paragraph_nested_divs" {
    // Text nested inside divs is extracted correctly
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<div><div><p>Nested text</p></div></div>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Nested text") != null);
    }
}

test "paragraph_simple" {
    // Simple paragraph converts to plain text
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>Hello World</p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("Hello World", std.mem.trim(u8, result.object.get("content").?.string, " \n\r\t"));
}

test "paragraph_with_inline_formatting" {
    // Paragraph with bold, italic, and a link
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>This has <strong>bold</strong>, <em>italic</em>, and a <a href=\"https://example.com\">link</a>.</p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "**bold**") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "*italic*") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "[link](https://example.com)") != null);
    }
}

test "paragraph_with_line_breaks" {
    // Paragraph with br tags produces line breaks in output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>Line one.<br>Line two.<br>Line three.</p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Line one.") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "Line two.") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "Line three.") != null);
    }
}

test "semantic_abbr" {
    // Abbreviation element text is preserved
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>The <abbr title=\"World Wide Web\">WWW</abbr> is global.</p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "WWW") != null);
    }
}

test "semantic_article" {
    // Article element wrapping content preserves inner content
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<article><h2>Article Title</h2><p>Article body.</p></article>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Article Title") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "Article body.") != null);
    }
}

test "semantic_definition_list" {
    // Definition list with term and description
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<dl><dt>HTML</dt><dd>HyperText Markup Language</dd><dt>CSS</dt><dd>Cascading Style Sheets</dd></dl>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "HTML") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "HyperText Markup Language") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "CSS") != null);
    }
    {
        const _jva3 = result.object.get("content").?;
        const _jsa3 = if (_jva3 == .string) _jva3.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva3, .{});
        defer if (_jva3 != .string) std.heap.c_allocator.free(_jsa3);
        try testing.expect(std.mem.indexOf(u8, _jsa3, "Cascading Style Sheets") != null);
    }
}

test "semantic_details_summary" {
    // Details and summary elements produce readable output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<details><summary>Click to expand</summary><p>Hidden content here.</p></details>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Click to expand") != null);
    }
}

test "semantic_hr" {
    // Horizontal rule produces a separator in output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>Above</p><hr><p>Below</p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Above") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "Below") != null);
    }
}

test "semantic_mark_highlight" {
    // Mark tag produces highlighted output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>This is <mark>highlighted text</mark> in a sentence.</p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "highlighted text") != null);
    }
}

test "semantic_section_with_heading" {
    // Section element with heading preserves structure
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<section><h3>Section Heading</h3><p>Section content.</p></section>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Section Heading") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "Section content.") != null);
    }
}

test "semantic_sub_superscript" {
    // Subscript and superscript elements are preserved in output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>H<sub>2</sub>O and E=mc<sup>2</sup></p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "H") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "2") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "O") != null);
    }
    {
        const _jva3 = result.object.get("content").?;
        const _jsa3 = if (_jva3 == .string) _jva3.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva3, .{});
        defer if (_jva3 != .string) std.heap.c_allocator.free(_jsa3);
        try testing.expect(std.mem.indexOf(u8, _jsa3, "E=mc") != null);
    }
}

test "simple_table" {
    // Simple table with header
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<table><thead><tr><th>Name</th><th>Age</th></tr></thead><tbody><tr><td>Alice</td><td>30</td></tr></tbody></table>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Name") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "Age") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "Alice") != null);
    }
    {
        const _jva3 = result.object.get("content").?;
        const _jsa3 = if (_jva3 == .string) _jva3.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva3, .{});
        defer if (_jva3 != .string) std.heap.c_allocator.free(_jsa3);
        try testing.expect(std.mem.indexOf(u8, _jsa3, "30") != null);
    }
    {
        const _jva4 = result.object.get("content").?;
        const _jsa4 = if (_jva4 == .string) _jva4.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva4, .{});
        defer if (_jva4 != .string) std.heap.c_allocator.free(_jsa4);
        try testing.expect(std.mem.indexOf(u8, _jsa4, "|") != null);
    }
    {
        const _jva5 = result.object.get("content").?;
        const _jsa5 = if (_jva5 == .string) _jva5.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva5, .{});
        defer if (_jva5 != .string) std.heap.c_allocator.free(_jsa5);
        try testing.expect(std.mem.indexOf(u8, _jsa5, "---") != null);
    }
}

test "table_empty" {
    // Empty table produces no output or minimal output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<table></table>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("", std.mem.trim(u8, result.object.get("content").?.string, " \n\r\t"));
}

test "table_no_thead" {
    // Table without thead uses first row as implied header
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<table><tr><td>Product</td><td>Price</td></tr><tr><td>Apple</td><td>1.00</td></tr></table>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Product") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "Price") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "Apple") != null);
    }
    {
        const _jva3 = result.object.get("content").?;
        const _jsa3 = if (_jva3 == .string) _jva3.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva3, .{});
        defer if (_jva3 != .string) std.heap.c_allocator.free(_jsa3);
        try testing.expect(std.mem.indexOf(u8, _jsa3, "1.00") != null);
    }
    {
        const _jva4 = result.object.get("content").?;
        const _jsa4 = if (_jva4 == .string) _jva4.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva4, .{});
        defer if (_jva4 != .string) std.heap.c_allocator.free(_jsa4);
        try testing.expect(std.mem.indexOf(u8, _jsa4, "|") != null);
    }
}

test "table_pipe_chars_in_content" {
    // Table cells containing pipe characters are escaped in output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<table><thead><tr><th>Expression</th><th>Result</th></tr></thead><tbody><tr><td>a | b</td><td>true</td></tr></tbody></table>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Expression") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "Result") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "true") != null);
    }
}

test "table_with_alignment" {
    // Table with column alignment attributes
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<table><thead><tr><th align=\"left\">Left</th><th align=\"center\">Center</th><th align=\"right\">Right</th></tr></thead><tbody><tr><td>L</td><td>C</td><td>R</td></tr></tbody></table>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Left") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "Center") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "Right") != null);
    }
    {
        const _jva3 = result.object.get("content").?;
        const _jsa3 = if (_jva3 == .string) _jva3.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva3, .{});
        defer if (_jva3 != .string) std.heap.c_allocator.free(_jsa3);
        try testing.expect(std.mem.indexOf(u8, _jsa3, "L") != null);
    }
    {
        const _jva4 = result.object.get("content").?;
        const _jsa4 = if (_jva4 == .string) _jva4.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva4, .{});
        defer if (_jva4 != .string) std.heap.c_allocator.free(_jsa4);
        try testing.expect(std.mem.indexOf(u8, _jsa4, "C") != null);
    }
    {
        const _jva5 = result.object.get("content").?;
        const _jsa5 = if (_jva5 == .string) _jva5.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva5, .{});
        defer if (_jva5 != .string) std.heap.c_allocator.free(_jsa5);
        try testing.expect(std.mem.indexOf(u8, _jsa5, "R") != null);
    }
    {
        const _jva6 = result.object.get("content").?;
        const _jsa6 = if (_jva6 == .string) _jva6.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva6, .{});
        defer if (_jva6 != .string) std.heap.c_allocator.free(_jsa6);
        try testing.expect(std.mem.indexOf(u8, _jsa6, "|") != null);
    }
}

test "table_with_colspan" {
    // Table with colspan attribute in a header cell
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<table><thead><tr><th colspan=\"2\">Full Name</th></tr></thead><tbody><tr><td>John</td><td>Doe</td></tr></tbody></table>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Full Name") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "John") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "Doe") != null);
    }
}

test "unordered_list" {
    // Unordered list
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<ul><li>Item 1</li><li>Item 2</li><li>Item 3</li></ul>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "- Item 1") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "- Item 2") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "- Item 3") != null);
    }
}
