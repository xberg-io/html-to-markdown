// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ff61139834885ca5a1d211a781605cd59e6b65ab2790131754fc440c69668f4c
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
const std = @import("std");
const testing = std.testing;
const html_to_markdown_rs = @import("html_to_markdown_rs");

// Suppress C++ global destructor aborts that break zig's --listen=- IPC
extern "c" fn signal(sig: i32, handler: usize) usize;
var _abort_handler_installed: bool = false;
fn suppress_abort() void {
    if (!_abort_handler_installed) {
        // SIGABRT = 6 on POSIX; SIG_IGN = 1
        _ = signal(6, 1);
        _abort_handler_installed = true;
    }
}

// E2e tests for category: edge-cases

test "empty_html" {
    // Empty HTML document
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html><head></head><body></body></html>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("", std.mem.trim(u8, result.object.get("content").?.string, " \n\r\t"));
}

test "encoding_cjk_characters" {
    // CJK (Chinese, Japanese, Korean) characters are preserved
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>中文内容</p><p>日本語テキスト</p><p>한국어 텍스트</p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "中文内容") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "日本語テキスト") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "한국어 텍스트") != null);
    }
}

test "encoding_html_entities" {
    // Common HTML entities are decoded in output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>&amp; &lt; &gt; &nbsp; &quot; &apos;</p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "&") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "<") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, ">") != null);
    }
}

test "encoding_named_entities" {
    // Named HTML entities like &mdash; and &hellip; are decoded
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>Em dash&mdash;used for parenthetical remarks&mdash;is common. Ellipsis&hellip; indicates omission. Non-breaking&nbsp;space.</p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "—") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "…") != null);
    }
}

test "encoding_numeric_entities" {
    // Numeric HTML entities (decimal and hex) are decoded
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>Copyright: &#169; Trade: &#174; Euro: &#8364; Hex: &#x00A9;</p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "©") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "®") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "€") != null);
    }
}

test "encoding_unicode_emoji" {
    // Emoji and Unicode characters are preserved
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>Hello 🌍 World 🚀</p><p>Stars: ⭐ ✨</p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "🌍") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "🚀") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "⭐") != null);
    }
}

test "html_comments_only" {
    // Document containing only HTML comments produces empty output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<!-- This is a comment --><!-- Another comment -->", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("", std.mem.trim(u8, result.object.get("content").?.string, " \n\r\t"));
}

test "just_whitespace_input" {
    // Input that is only whitespace characters (spaces, tabs, newlines) produces empty output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("   ", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("", std.mem.trim(u8, result.object.get("content").?.string, " \n\r\t"));
}

test "malformed_bogus_comment_triple_dash" {
    // HTML comment ending with ---> (three dashes) must not drop content that follows (issue #339)
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<h1>One</h1>\n<!-- /// --->\n<p>Two</p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "One") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "Two") != null);
    }
}

test "malformed_deeply_nested_elements" {
    // Deeply nested elements (100 levels) are handled without stack overflow
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><p>Deeply nested content</p></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Deeply nested content") != null);
    }
}

test "malformed_missing_block_closing_tags" {
    // Missing closing tags on block elements are auto-closed by parser
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<div><h1>Title<p>First paragraph<p>Second paragraph</div>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Title") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "First paragraph") != null);
    }
    {
        const _jva2 = result.object.get("content").?;
        const _jsa2 = if (_jva2 == .string) _jva2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva2, .{});
        defer if (_jva2 != .string) std.heap.c_allocator.free(_jsa2);
        try testing.expect(std.mem.indexOf(u8, _jsa2, "Second paragraph") != null);
    }
}

test "malformed_overlapping_tags" {
    // Overlapping bold/italic tags are recovered by the HTML parser without panic
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p><b><i>bold and italic</b></i></p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "bold and italic") != null);
    }
}

test "malformed_unclosed_paragraph" {
    // Unclosed <p> tag is recovered gracefully and content is preserved
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>This paragraph is never closed", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "This paragraph is never closed") != null);
    }
}

test "script_tags_only" {
    // Document with only script tags produces empty output (scripts are stripped)
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html><head><script>alert('xss')</script></head><body><script>document.write('hello')</script></body></html>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("", std.mem.trim(u8, result.object.get("content").?.string, " \n\r\t"));
}

test "style_tags_only" {
    // Document with only style tags produces empty output (styles are stripped)
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html><head><style>body { color: red; }</style></head><body><style>.foo { margin: 0; }</style></body></html>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("", std.mem.trim(u8, result.object.get("content").?.string, " \n\r\t"));
}

test "visitor_custom_element_with_nesting" {
    // Visitor handles custom elements with nested content
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_custom_element_with_nesting = struct {
        pub fn visit_custom_element(_ctx: [*c]const c.HTMHtmContext, _user_data: ?*anyopaque, _tag_name: [*c]const u8, _html: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _tag_name;
        _ = _html;
        const _buf = std.heap.c_allocator.dupeZ(u8, "[CUSTOM WIDGET]") catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 1;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_custom_element = &TestVisitor_visitor_custom_element_with_nesting.visit_custom_element;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<div><custom-widget data-value=\"123\"><p>Widget content here</p><span>With nested elements</span></custom-widget></div>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "[CUSTOM WIDGET]") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "Widget content here") == null);
    }
}


test "visitor_deeply_nested_skip" {
    // Visitor skips deeply nested elements
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_deeply_nested_skip = struct {
        pub fn visit_mark(_ctx: [*c]const c.HTMHtmContext, _user_data: ?*anyopaque, _text: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _text;
        _ = out_custom;
        _ = out_len;
        return 2;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_mark = &TestVisitor_visitor_deeply_nested_skip.visit_mark;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<div><p>Outer <em>emphasis <strong>with bold <mark>and highlight</mark></strong></em> text</p></div>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Outer") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "text") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "highlight") == null);
    }
}


test "visitor_element_end_modification" {
    // Visitor modifies element at end after children processed
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_element_end_modification = struct {
        pub fn visit_element_end(_ctx: [*c]const c.HTMHtmContext, _user_data: ?*anyopaque, _output: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _output;
        const _buf = std.heap.c_allocator.dupeZ(u8, "MODIFIED OUTPUT") catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 1;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_element_end = &TestVisitor_visitor_element_end_modification.visit_element_end;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<blockquote><p>Original quote</p></blockquote>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
}


test "visitor_element_start_skip_entire_subtree" {
    // Visitor skips at element_start level removes entire subtree
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_element_start_skip_entire_subtree = struct {
        pub fn visit_element_start(_ctx: [*c]const c.HTMHtmContext, _user_data: ?*anyopaque, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = out_custom;
        _ = out_len;
        return 2;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_element_start = &TestVisitor_visitor_element_start_skip_entire_subtree.visit_element_start;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<div><h1>Title</h1><p>Content</p></div>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "Title") == null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "Content") == null);
    }
}


test "visitor_unknown_tag_preservation" {
    // Visitor preserves unknown HTML tags as raw HTML
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_unknown_tag_preservation = struct {
        pub fn visit_custom_element(_ctx: [*c]const c.HTMHtmContext, _user_data: ?*anyopaque, _tag_name: [*c]const u8, _html: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _tag_name;
        _ = _html;
        _ = out_custom;
        _ = out_len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_custom_element = &TestVisitor_visitor_unknown_tag_preservation.visit_custom_element;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<article><p>Article text</p><x-custom>Custom element with content</x-custom><p>More article text</p></article>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Article text") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "More article text") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "<x-custom>") != null);
    }
}


test "whitespace_only" {
    // Whitespace-only content
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>   </p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqualStrings("", std.mem.trim(u8, result.object.get("content").?.string, " \n\r\t"));
}

test "xss_onclick_handler_removed" {
    // onclick and other on* event handlers are removed from elements
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p><a href=\"https://example.com\" onclick=\"alert('xss')\">Click me</a></p><button onmouseover=\"steal_data()\">Hover me</button>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Click me") != null);
    }
    {
        const _jvnc0 = result.object.get("content").?;
        const _jsnc0 = if (_jvnc0 == .string) _jvnc0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc0, .{});
        defer if (_jvnc0 != .string) std.heap.c_allocator.free(_jsnc0);
        try testing.expect(std.mem.indexOf(u8, _jsnc0, "onclick") == null);
    }
    {
        const _jvnc1 = result.object.get("content").?;
        const _jsnc1 = if (_jvnc1 == .string) _jvnc1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc1, .{});
        defer if (_jvnc1 != .string) std.heap.c_allocator.free(_jsnc1);
        try testing.expect(std.mem.indexOf(u8, _jsnc1, "onmouseover") == null);
    }
    {
        const _jvnc2 = result.object.get("content").?;
        const _jsnc2 = if (_jvnc2 == .string) _jvnc2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc2, .{});
        defer if (_jvnc2 != .string) std.heap.c_allocator.free(_jsnc2);
        try testing.expect(std.mem.indexOf(u8, _jsnc2, "alert(") == null);
    }
    {
        const _jvnc3 = result.object.get("content").?;
        const _jsnc3 = if (_jvnc3 == .string) _jvnc3.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc3, .{});
        defer if (_jvnc3 != .string) std.heap.c_allocator.free(_jsnc3);
        try testing.expect(std.mem.indexOf(u8, _jsnc3, "steal_data") == null);
    }
}

test "xss_script_tag_stripped" {
    // Script tag content is stripped and does not appear in output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>Safe content.</p><script>alert('xss')</script><p>More safe content.</p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Safe content") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "More safe content") != null);
    }
    {
        const _jvnc0 = result.object.get("content").?;
        const _jsnc0 = if (_jvnc0 == .string) _jvnc0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc0, .{});
        defer if (_jvnc0 != .string) std.heap.c_allocator.free(_jsnc0);
        try testing.expect(std.mem.indexOf(u8, _jsnc0, "<script>") == null);
    }
    {
        const _jvnc1 = result.object.get("content").?;
        const _jsnc1 = if (_jvnc1 == .string) _jvnc1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc1, .{});
        defer if (_jvnc1 != .string) std.heap.c_allocator.free(_jsnc1);
        try testing.expect(std.mem.indexOf(u8, _jsnc1, "alert(") == null);
    }
    {
        const _jvnc2 = result.object.get("content").?;
        const _jsnc2 = if (_jvnc2 == .string) _jvnc2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc2, .{});
        defer if (_jvnc2 != .string) std.heap.c_allocator.free(_jsnc2);
        try testing.expect(std.mem.indexOf(u8, _jsnc2, "xss") == null);
    }
}

test "xss_svg_nested_script_stripped" {
    // Script tags nested inside SVG are stripped
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>Before SVG.</p><svg xmlns=\"http://www.w3.org/2000/svg\"><script>alert('svg-xss')</script><text>SVG text</text></svg><p>After SVG.</p>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Before SVG") != null);
    }
    {
        const _jva1 = result.object.get("content").?;
        const _jsa1 = if (_jva1 == .string) _jva1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva1, .{});
        defer if (_jva1 != .string) std.heap.c_allocator.free(_jsa1);
        try testing.expect(std.mem.indexOf(u8, _jsa1, "After SVG") != null);
    }
    {
        const _jvnc0 = result.object.get("content").?;
        const _jsnc0 = if (_jvnc0 == .string) _jvnc0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc0, .{});
        defer if (_jvnc0 != .string) std.heap.c_allocator.free(_jsnc0);
        try testing.expect(std.mem.indexOf(u8, _jsnc0, "<script>") == null);
    }
    {
        const _jvnc1 = result.object.get("content").?;
        const _jsnc1 = if (_jvnc1 == .string) _jvnc1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc1, .{});
        defer if (_jvnc1 != .string) std.heap.c_allocator.free(_jsnc1);
        try testing.expect(std.mem.indexOf(u8, _jsnc1, "alert(") == null);
    }
    {
        const _jvnc2 = result.object.get("content").?;
        const _jsnc2 = if (_jvnc2 == .string) _jvnc2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc2, .{});
        defer if (_jvnc2 != .string) std.heap.c_allocator.free(_jsnc2);
        try testing.expect(std.mem.indexOf(u8, _jsnc2, "svg-xss") == null);
    }
}
