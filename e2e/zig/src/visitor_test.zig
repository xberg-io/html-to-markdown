// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c9e86fbd37245e8d95f0fa47ea92f2e7acd8da76a28a99f9eacd64c126b5f3c2
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const html_to_markdown_rs = @import("html_to_markdown_rs");

// Suppress C++ global destructor aborts that break zig's --listen=- IPC
extern "c" fn signal(sig: i32, handler: usize) usize;
var _abort_handler_installed: bool = false;
fn suppress_abort() void {
    if (!_abort_handler_installed) {
        // SIGABRT = 6 on POSIX; SIG_IGN = 1
        _ = signal(6, 1);
        _abort_handler_installed = true;
    }
}

// E2e tests for category: visitor

test "visitor_audio_custom" {
    // Visitor replaces audio element with custom output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_audio_custom = struct {
        pub fn visit_audio(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _src: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _src;
        const _buf = std.heap.c_allocator.dupeZ(u8, "[AUDIO: podcast.mp3]") catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_audio = &TestVisitor_visitor_audio_custom.visit_audio;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>Listen to this: <audio src=\"podcast.mp3\" controls></audio></p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "[AUDIO: podcast.mp3]") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Listen to this:") != null);
    }
}


test "visitor_audio_skip" {
    // Visitor removes audio elements from output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_audio_skip = struct {
        pub fn visit_audio(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _src: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _src;
        _ = out_custom;
        _ = out_len;
        return 1;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_audio = &TestVisitor_visitor_audio_skip.visit_audio;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>Background music:</p><audio src=\"music.ogg\" autoplay></audio><p>Enjoy!</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Background music:") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Enjoy!") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "music.ogg") == null);
    }
}


test "visitor_button_custom" {
    // Visitor replaces button with bracketed text
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_button_custom = struct {
        pub fn visit_button(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _text: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        const _buf = std.fmt.allocPrintSentinel(std.heap.c_allocator, "[BTN:{s}]", .{ std.mem.span(_text) }, 0) catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_button = &TestVisitor_visitor_button_custom.visit_button;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>Confirm action: <button type=\"submit\">Click me</button> or <button type=\"reset\">Cancel</button></p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "[BTN:Click me]") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "[BTN:Cancel]") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Confirm action:") != null);
    }
}


test "visitor_button_skip" {
    // Visitor removes all buttons from output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_button_skip = struct {
        pub fn visit_button(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _text: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _text;
        _ = out_custom;
        _ = out_len;
        return 1;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_button = &TestVisitor_visitor_button_skip.visit_button;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>Actions available: <button>Save</button> <button>Delete</button> <button>Export</button></p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Actions available:") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "Save") == null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "Delete") == null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "Export") == null);
    }
}


test "visitor_continue_default" {
    // Visitor continue action preserves default conversion
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_continue_default = struct {
        pub fn visit_strong(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _text: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _text;
        _ = out_custom;
        _ = out_len;
        return 0;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_strong = &TestVisitor_visitor_continue_default.visit_strong;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>Hello <strong>World</strong></p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "**World**") != null);
    }
}


test "visitor_custom_blockquote" {
    // Visitor replaces blockquote with custom format
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_custom_blockquote = struct {
        pub fn visit_blockquote(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _content: [*c]const u8, _depth: usize, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _depth;
        const _buf = std.fmt.allocPrintSentinel(std.heap.c_allocator, "QUOTE: \"{s}\"", .{ std.mem.span(_content) }, 0) catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_blockquote = &TestVisitor_visitor_custom_blockquote.visit_blockquote;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<blockquote><p>A wise quote.</p></blockquote>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "QUOTE:") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "A wise quote.") != null);
    }
}


test "visitor_custom_emphasis" {
    // Visitor replaces emphasis with custom output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_custom_emphasis = struct {
        pub fn visit_emphasis(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _text: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        const _buf = std.fmt.allocPrintSentinel(std.heap.c_allocator, ">>>{s}<<<", .{ std.mem.span(_text) }, 0) catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_emphasis = &TestVisitor_visitor_custom_emphasis.visit_emphasis;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>This is <em>important</em> text.</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, ">>>important<<<") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "*important*") == null);
    }
}


test "visitor_custom_heading" {
    // Visitor replaces heading with custom format
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_custom_heading = struct {
        pub fn visit_heading(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _level: u32, _text: [*c]const u8, _id: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _level;
        _ = _id;
        const _buf = std.fmt.allocPrintSentinel(std.heap.c_allocator, "--- {s} ---", .{ std.mem.span(_text) }, 0) catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_heading = &TestVisitor_visitor_custom_heading.visit_heading;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<h2>Section Title</h2><p>Content below heading.</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "--- Section Title ---") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "## Section Title") == null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Content below heading.") != null);
    }
}


test "visitor_custom_image" {
    // Visitor replaces image with custom output using template
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_custom_image = struct {
        pub fn visit_image(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _src: [*c]const u8, _alt: [*c]const u8, _title: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _src;
        _ = _title;
        const _buf = std.fmt.allocPrintSentinel(std.heap.c_allocator, "[Image: {s}]", .{ std.mem.span(_alt) }, 0) catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_image = &TestVisitor_visitor_custom_image.visit_image;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<img src=\"banner.png\" alt=\"Banner\">");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "[Image: Banner]") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "banner.png") == null);
    }
}


test "visitor_custom_link_format" {
    // Visitor reformats links using template interpolation
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_custom_link_format = struct {
        pub fn visit_link(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _href: [*c]const u8, _text: [*c]const u8, _title: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _title;
        const _buf = std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s} ({s})", .{ std.mem.span(_text), std.mem.span(_href) }, 0) catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_link = &TestVisitor_visitor_custom_link_format.visit_link;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>Visit <a href=\"https://example.com\">Example</a> for more info.</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Example (https://example.com)") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "[Example]") == null);
    }
}


test "visitor_custom_link_static" {
    // Visitor replaces link with static custom output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_custom_link_static = struct {
        pub fn visit_link(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _href: [*c]const u8, _text: [*c]const u8, _title: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _href;
        _ = _text;
        _ = _title;
        const _buf = std.heap.c_allocator.dupeZ(u8, "[REDACTED LINK]") catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_link = &TestVisitor_visitor_custom_link_static.visit_link;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<a href=\"https://example.com\">Click here</a>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "[REDACTED LINK]") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "example.com") == null);
    }
}


test "visitor_custom_output" {
    // Visitor custom action replaces element output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_custom_output = struct {
        pub fn visit_heading(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _level: u32, _text: [*c]const u8, _id: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _level;
        _ = _text;
        _ = _id;
        const _buf = std.heap.c_allocator.dupeZ(u8, "## REPLACED HEADING") catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_heading = &TestVisitor_visitor_custom_output.visit_heading;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<h1>Original Heading</h1>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "## REPLACED HEADING") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "# Original Heading") == null);
    }
}


test "visitor_definition_list_custom" {
    // Visitor customizes definition list items
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_definition_list_custom = struct {
        pub fn visit_definition_term(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _text: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        const _buf = std.fmt.allocPrintSentinel(std.heap.c_allocator, "**{s}**", .{ std.mem.span(_text) }, 0) catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_definition_term = &TestVisitor_visitor_definition_list_custom.visit_definition_term;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<dl><dt>HTML</dt><dd>HyperText Markup Language</dd><dt>CSS</dt><dd>Cascading Style Sheets</dd></dl>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "**HTML**") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "**CSS**") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "HyperText Markup Language") != null);
    }
}


test "visitor_definition_list_custom_format" {
    // Visitor formats definition lists with custom templates
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_definition_list_custom_format = struct {
        pub fn visit_definition_description(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _text: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        const _buf = std.fmt.allocPrintSentinel(std.heap.c_allocator, "> {s}", .{ std.mem.span(_text) }, 0) catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
        pub fn visit_definition_term(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _text: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        const _buf = std.fmt.allocPrintSentinel(std.heap.c_allocator, "### {s}", .{ std.mem.span(_text) }, 0) catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_definition_description = &TestVisitor_visitor_definition_list_custom_format.visit_definition_description;
    _callbacks.visit_definition_term = &TestVisitor_visitor_definition_list_custom_format.visit_definition_term;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<dl><dt>Python</dt><dd>A high-level programming language</dd><dt>JavaScript</dt><dd>A scripting language for web browsers</dd></dl>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "### Python") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "### JavaScript") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "> A high-level programming language") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "> A scripting language for web browsers") != null);
    }
}


test "visitor_definition_list_skip" {
    // Visitor skips definition list items from output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_definition_list_skip = struct {
        pub fn visit_definition_description(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _text: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _text;
        _ = out_custom;
        _ = out_len;
        return 1;
        }
        pub fn visit_definition_term(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _text: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _text;
        _ = out_custom;
        _ = out_len;
        return 1;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_definition_description = &TestVisitor_visitor_definition_list_skip.visit_definition_description;
    _callbacks.visit_definition_term = &TestVisitor_visitor_definition_list_skip.visit_definition_term;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>Glossary:</p><dl><dt>Term A</dt><dd>Definition of term A</dd><dt>Term B</dt><dd>Definition of term B</dd></dl><p>End of glossary</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Glossary:") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "End of glossary") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "Term A") == null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "Definition") == null);
    }
}


test "visitor_details_summary_custom" {
    // Visitor customizes details/summary disclosure elements
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_details_summary_custom = struct {
        pub fn visit_summary(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _text: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        const _buf = std.fmt.allocPrintSentinel(std.heap.c_allocator, "[EXPANDABLE] {s}", .{ std.mem.span(_text) }, 0) catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_summary = &TestVisitor_visitor_details_summary_custom.visit_summary;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<details><summary>Click to expand</summary><p>This content is initially hidden.</p><p>But can be revealed by the user.</p></details>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "[EXPANDABLE] Click to expand") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "This content is initially hidden.") != null);
    }
}


test "visitor_details_summary_skip" {
    // Visitor removes details/summary elements entirely
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_details_summary_skip = struct {
        pub fn visit_details(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _open: i32, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _open;
        _ = out_custom;
        _ = out_len;
        return 1;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_details = &TestVisitor_visitor_details_summary_skip.visit_details;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>Main content here.</p><details><summary>Hidden section</summary><p>Secret details</p></details><p>More main content.</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Main content here.") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "More main content.") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "Hidden section") == null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "Secret details") == null);
    }
}


test "visitor_figure_custom" {
    // Visitor customizes figure and figcaption elements
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_figure_custom = struct {
        pub fn visit_figcaption(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _text: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        const _buf = std.fmt.allocPrintSentinel(std.heap.c_allocator, "*{s}*", .{ std.mem.span(_text) }, 0) catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_figcaption = &TestVisitor_visitor_figure_custom.visit_figcaption;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<article><h1>Article Title</h1><p>Introduction paragraph.</p><figure><img src=\"diagram.png\" alt=\"System architecture diagram\"><figcaption>Figure 1: System Architecture</figcaption></figure><p>Explanation of the figure.</p></article>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Article Title") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "*Figure 1: System Architecture*") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Explanation of the figure.") != null);
    }
}


test "visitor_figure_custom_wrap" {
    // Visitor wraps figure content with custom formatting
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_figure_custom_wrap = struct {
        pub fn visit_figure_end(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _output: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        const _buf = std.fmt.allocPrintSentinel(std.heap.c_allocator, "{s}\n[/FIGURE]\n", .{ std.mem.span(_output) }, 0) catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
        pub fn visit_figure_start(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        const _buf = std.heap.c_allocator.dupeZ(u8, "\n[FIGURE]\n") catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_figure_end = &TestVisitor_visitor_figure_custom_wrap.visit_figure_end;
    _callbacks.visit_figure_start = &TestVisitor_visitor_figure_custom_wrap.visit_figure_start;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<section><h2>Gallery</h2><figure><img src=\"photo1.jpg\" alt=\"Photo\"><figcaption>Beautiful sunset</figcaption></figure></section>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "[FIGURE]") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "[/FIGURE]") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Gallery") != null);
    }
}


test "visitor_figure_skip" {
    // Visitor removes figure elements with their captions
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_figure_skip = struct {
        pub fn visit_figure_start(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = out_custom;
        _ = out_len;
        return 1;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_figure_start = &TestVisitor_visitor_figure_skip.visit_figure_start;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>See the chart below:</p><figure><img src=\"chart.svg\"><figcaption>Revenue Trends 2020-2024</figcaption></figure><p>As shown in the chart above.</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "See the chart below:") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "As shown in the chart above.") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "Revenue Trends") == null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "chart.svg") == null);
    }
}


test "visitor_form_custom" {
    // Visitor replaces form with custom output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_form_custom = struct {
        pub fn visit_form(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _action: [*c]const u8, _method: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _action;
        _ = _method;
        const _buf = std.heap.c_allocator.dupeZ(u8, "[FORM PLACEHOLDER]") catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_form = &TestVisitor_visitor_form_custom.visit_form;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<div><form action=\"/submit\" method=\"POST\"><label>Name: <input type=\"text\" name=\"name\"></label><button type=\"submit\">Submit</button></form></div>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "[FORM PLACEHOLDER]") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "submit") == null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "input") == null);
    }
}


test "visitor_form_skip" {
    // Visitor skips form elements entirely
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_form_skip = struct {
        pub fn visit_form(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _action: [*c]const u8, _method: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _action;
        _ = _method;
        _ = out_custom;
        _ = out_len;
        return 1;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_form = &TestVisitor_visitor_form_skip.visit_form;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>Before form</p><form><input type=\"email\" name=\"email\"></form><p>After form</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Before form") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "After form") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "email") == null);
    }
}


test "visitor_heading_bare_string_preserves_case" {
    // Visitor returns rendered heading template as bare string; mixed-case is preserved (regression guard for issue #350)
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_heading_bare_string_preserves_case = struct {
        pub fn visit_heading(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _level: u32, _text: [*c]const u8, _id: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _level;
        _ = _id;
        const _buf = std.fmt.allocPrintSentinel(std.heap.c_allocator, "## {s} ##", .{ std.mem.span(_text) }, 0) catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_heading = &TestVisitor_visitor_heading_bare_string_preserves_case.visit_heading;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<h2>Important Section Title</h2><p>Body.</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "## Important Section Title ##") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "important section title") == null);
    }
}


test "visitor_horizontal_rule_custom" {
    // Visitor replaces horizontal rule with custom output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_horizontal_rule_custom = struct {
        pub fn visit_horizontal_rule(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        const _buf = std.heap.c_allocator.dupeZ(u8, "\n[DIVIDER]\n") catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_horizontal_rule = &TestVisitor_visitor_horizontal_rule_custom.visit_horizontal_rule;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<h1>Section A</h1><p>Content A</p><hr><h1>Section B</h1><p>Content B</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "[DIVIDER]") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Section A") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Section B") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "---") == null);
    }
}


test "visitor_horizontal_rule_skip" {
    // Visitor removes all horizontal rules
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_horizontal_rule_skip = struct {
        pub fn visit_horizontal_rule(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = out_custom;
        _ = out_len;
        return 1;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_horizontal_rule = &TestVisitor_visitor_horizontal_rule_skip.visit_horizontal_rule;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>Part 1</p><hr><p>Part 2</p><hr><p>Part 3</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Part 1") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Part 2") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Part 3") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "---") == null);
    }
}


test "visitor_iframe_custom" {
    // Visitor replaces embedded iframe with custom text
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_iframe_custom = struct {
        pub fn visit_iframe(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _src: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _src;
        const _buf = std.heap.c_allocator.dupeZ(u8, "[EMBEDDED: https://maps.example.com/embed]") catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_iframe = &TestVisitor_visitor_iframe_custom.visit_iframe;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>Embedded map:</p><iframe src=\"https://maps.example.com/embed\" width=\"400\" height=\"300\"></iframe><p>End of map</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "[EMBEDDED: https://maps.example.com/embed]") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Embedded map:") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "End of map") != null);
    }
}


test "visitor_iframe_skip" {
    // Visitor removes embedded iframes
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_iframe_skip = struct {
        pub fn visit_iframe(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _src: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _src;
        _ = out_custom;
        _ = out_len;
        return 1;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_iframe = &TestVisitor_visitor_iframe_skip.visit_iframe;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<h3>Reviews</h3><iframe src=\"https://widget.example.com/reviews\"></iframe><p>See reviews from our partners.</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Reviews") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "See reviews from our partners.") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "widget.example.com") == null);
    }
}


test "visitor_image_bare_string_preserves_case" {
    // Visitor returns bare-string image replacement; mixed-case alt/src preserved (regression guard for issue #350)
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_image_bare_string_preserves_case = struct {
        pub fn visit_image(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _src: [*c]const u8, _alt: [*c]const u8, _title: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _title;
        const _buf = std.fmt.allocPrintSentinel(std.heap.c_allocator, "[image: {s} -> {s}]", .{ std.mem.span(_alt), std.mem.span(_src) }, 0) catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_image = &TestVisitor_visitor_image_bare_string_preserves_case.visit_image;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<img src=\"PhotoOne.JPG\" alt=\"Sunset Over Bay\">");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "[image: Sunset Over Bay -> PhotoOne.JPG]") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "sunset over bay") == null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "photoone.jpg") == null);
    }
}


test "visitor_input_custom" {
    // Visitor replaces input with labeled output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_input_custom = struct {
        pub fn visit_input(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _input_type: [*c]const u8, _name: [*c]const u8, _value: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _name;
        _ = _value;
        const _buf = std.fmt.allocPrintSentinel(std.heap.c_allocator, "[INPUT:{s}]", .{ std.mem.span(_input_type) }, 0) catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_input = &TestVisitor_visitor_input_custom.visit_input;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<form><label>Username: <input type=\"text\" name=\"username\" value=\"\"></label><label>Password: <input type=\"password\" name=\"password\"></label></form>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "[INPUT:text]") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "[INPUT:password]") != null);
    }
}


test "visitor_input_skip" {
    // Visitor skips all input elements
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_input_skip = struct {
        pub fn visit_input(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _input_type: [*c]const u8, _name: [*c]const u8, _value: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _input_type;
        _ = _name;
        _ = _value;
        _ = out_custom;
        _ = out_len;
        return 1;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_input = &TestVisitor_visitor_input_skip.visit_input;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>Sign up:</p><input type=\"text\" name=\"email\" placeholder=\"your@email.com\"><input type=\"checkbox\" name=\"agree\"><p>Continue</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Sign up:") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Continue") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "email") == null);
    }
}


test "visitor_line_break_custom" {
    // Visitor replaces line break with custom output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_line_break_custom = struct {
        pub fn visit_line_break(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        const _buf = std.heap.c_allocator.dupeZ(u8, " | ") catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_line_break = &TestVisitor_visitor_line_break_custom.visit_line_break;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>First line<br>Second line<br>Third line</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "First line | Second line | Third line") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "\n\n") == null);
    }
}


test "visitor_line_break_skip" {
    // Visitor removes all line breaks
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_line_break_skip = struct {
        pub fn visit_line_break(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = out_custom;
        _ = out_len;
        return 1;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_line_break = &TestVisitor_visitor_line_break_skip.visit_line_break;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>Address Line 1<br>Address Line 2<br>Address Line 3</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Address Line 1Address Line 2Address Line 3") != null);
    }
}


test "visitor_link_bare_string_preserves_case" {
    // Visitor returns rendered template as a bare string (not a dict) and original mixed-case content is preserved end-to-end (regression guard for issue #350)
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_link_bare_string_preserves_case = struct {
        pub fn visit_link(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _href: [*c]const u8, _text: [*c]const u8, _title: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _href;
        _ = _title;
        const _buf = std.fmt.allocPrintSentinel(std.heap.c_allocator, "[{s}](https://new-cdn.com/file.pdf)", .{ std.mem.span(_text) }, 0) catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_link = &TestVisitor_visitor_link_bare_string_preserves_case.visit_link;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<a href=\"https://old-cdn.com/file.pdf\">Download</a>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "[Download](https://new-cdn.com/file.pdf)") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "[download]") == null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "old-cdn.com") == null);
    }
}


test "visitor_mark_custom" {
    // Visitor replaces highlight/mark with custom template
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_mark_custom = struct {
        pub fn visit_mark(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _text: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        const _buf = std.fmt.allocPrintSentinel(std.heap.c_allocator, "=={s}==", .{ std.mem.span(_text) }, 0) catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_mark = &TestVisitor_visitor_mark_custom.visit_mark;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>This is a <mark>highlighted passage</mark> in the text.</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "==highlighted passage==") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "This is a") != null);
    }
}


test "visitor_mark_skip" {
    // Visitor skips mark elements entirely
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_mark_skip = struct {
        pub fn visit_mark(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _text: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _text;
        _ = out_custom;
        _ = out_len;
        return 1;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_mark = &TestVisitor_visitor_mark_skip.visit_mark;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>Key insight: <mark>always validate input</mark> for security.</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "always validate input") == null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Key insight:") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "for security.") != null);
    }
}


test "visitor_preserve_html" {
    // Visitor preserve_html action includes raw HTML in output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_preserve_html = struct {
        pub fn visit_custom_element(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _tag_name: [*c]const u8, _html: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _tag_name;
        _ = _html;
        _ = out_custom;
        _ = out_len;
        return 2;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_custom_element = &TestVisitor_visitor_preserve_html.visit_custom_element;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<div><custom-tag>Custom content</custom-tag></div>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "<custom-tag>") != null);
    }
}


test "visitor_skip_code_blocks" {
    // Visitor skips code blocks from output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_skip_code_blocks = struct {
        pub fn visit_code_block(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _lang: [*c]const u8, _code: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _lang;
        _ = _code;
        _ = out_custom;
        _ = out_len;
        return 1;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_code_block = &TestVisitor_visitor_skip_code_blocks.visit_code_block;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>Intro text</p><pre><code>let x = 42;</code></pre><p>Outro text</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Intro text") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Outro text") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "let x = 42") == null);
    }
}


test "visitor_skip_heading" {
    // Visitor skip action omits all headings from output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_skip_heading = struct {
        pub fn visit_heading(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _level: u32, _text: [*c]const u8, _id: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _level;
        _ = _text;
        _ = _id;
        _ = out_custom;
        _ = out_len;
        return 1;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_heading = &TestVisitor_visitor_skip_heading.visit_heading;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<h1>Title</h1><p>Body text remains.</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "Title") == null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Body text remains.") != null);
    }
}


test "visitor_skip_images" {
    // Visitor skips all images from output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_skip_images = struct {
        pub fn visit_image(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _src: [*c]const u8, _alt: [*c]const u8, _title: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _src;
        _ = _alt;
        _ = _title;
        _ = out_custom;
        _ = out_len;
        return 1;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_image = &TestVisitor_visitor_skip_images.visit_image;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>Before image</p><img src=\"photo.jpg\" alt=\"A photo\"><p>After image</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Before image") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "After image") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "photo.jpg") == null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "A photo") == null);
    }
}


test "visitor_skip_links" {
    // Visitor skips all links entirely
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_skip_links = struct {
        pub fn visit_link(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _href: [*c]const u8, _text: [*c]const u8, _title: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _href;
        _ = _text;
        _ = _title;
        _ = out_custom;
        _ = out_len;
        return 1;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_link = &TestVisitor_visitor_skip_links.visit_link;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>Before <a href=\"https://example.com\">link text</a> after</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "link text") == null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "example.com") == null);
    }
}


test "visitor_skip_strong" {
    // Visitor skips bold/strong elements
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_skip_strong = struct {
        pub fn visit_strong(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _text: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _text;
        _ = out_custom;
        _ = out_len;
        return 1;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_strong = &TestVisitor_visitor_skip_strong.visit_strong;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>Normal <strong>bold text</strong> normal</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "bold text") == null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Normal") != null);
    }
}


test "visitor_subscript_custom" {
    // Visitor replaces subscript with custom template
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_subscript_custom = struct {
        pub fn visit_subscript(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _text: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        const _buf = std.fmt.allocPrintSentinel(std.heap.c_allocator, "~{s}~", .{ std.mem.span(_text) }, 0) catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_subscript = &TestVisitor_visitor_subscript_custom.visit_subscript;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>H<sub>2</sub>O is water.</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "H~2~O") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "is water") != null);
    }
}


test "visitor_subscript_skip" {
    // Visitor skips subscript elements entirely
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_subscript_skip = struct {
        pub fn visit_subscript(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _text: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _text;
        _ = out_custom;
        _ = out_len;
        return 1;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_subscript = &TestVisitor_visitor_subscript_skip.visit_subscript;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>The formula C<sub>12</sub>H<sub>22</sub>O<sub>11</sub> is sugar.</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "The formula CHO is sugar.") != null);
    }
}


test "visitor_superscript_custom" {
    // Visitor replaces superscript with custom template
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_superscript_custom = struct {
        pub fn visit_superscript(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _text: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        const _buf = std.fmt.allocPrintSentinel(std.heap.c_allocator, "^{s}^", .{ std.mem.span(_text) }, 0) catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_superscript = &TestVisitor_visitor_superscript_custom.visit_superscript;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>Einstein's E=mc<sup>2</sup> revolutionized physics.</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "E=mc^2^") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "revolutionized physics") != null);
    }
}


test "visitor_superscript_skip" {
    // Visitor skips superscript from output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_superscript_skip = struct {
        pub fn visit_superscript(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _text: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _text;
        _ = out_custom;
        _ = out_len;
        return 1;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_superscript = &TestVisitor_visitor_superscript_skip.visit_superscript;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>The equation x<sup>3</sup> + y<sup>3</sup> = z<sup>3</sup> has no solutions.</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "The equation x + y = z has no solutions.") != null);
    }
}


test "visitor_underline_custom" {
    // Visitor replaces underline with custom markup
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_underline_custom = struct {
        pub fn visit_underline(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _text: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        const _buf = std.fmt.allocPrintSentinel(std.heap.c_allocator, "_{s}_", .{ std.mem.span(_text) }, 0) catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_underline = &TestVisitor_visitor_underline_custom.visit_underline;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>This is <u>very important</u> text.</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "_very important_") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "**") == null);
    }
}


test "visitor_underline_skip" {
    // Visitor skips underline elements from output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_underline_skip = struct {
        pub fn visit_underline(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _text: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _text;
        _ = out_custom;
        _ = out_len;
        return 1;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_underline = &TestVisitor_visitor_underline_skip.visit_underline;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>Normal text with <u>underlined part</u> and more text.</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Normal text with") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "and more text.") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "underlined part") == null);
    }
}


test "visitor_video_custom" {
    // Visitor replaces video with custom link
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_video_custom = struct {
        pub fn visit_video(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _src: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        const _buf = std.fmt.allocPrintSentinel(std.heap.c_allocator, "[VIDEO: {s}]", .{ std.mem.span(_src) }, 0) catch return 0;
        if (out_custom != null) out_custom.* = _buf.ptr;
        if (out_len != null) out_len.* = _buf.len;
        return 3;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_video = &TestVisitor_visitor_video_custom.visit_video;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<p>Watch our tutorial:</p><video src=\"tutorial.mp4\" width=\"320\" height=\"240\" controls></video><p>Great content!</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "[VIDEO: tutorial.mp4]") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Watch our tutorial:") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Great content!") != null);
    }
}


test "visitor_video_skip" {
    // Visitor removes video elements entirely
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const c = html_to_markdown_rs.c;
    const TestVisitor_visitor_video_skip = struct {
        pub fn visit_video(_ctx: [*c]const c.HTMHtmNodeContext, _user_data: ?*anyopaque, _src: [*c]const u8, out_custom: [*c][*c]u8, out_len: [*c]usize) callconv(.c) i32 {
        _ = _ctx;
        _ = _user_data;
        _ = _src;
        _ = out_custom;
        _ = out_len;
        return 1;
        }
    };
    var _callbacks: c.HTMHtmVisitorCallbacks = std.mem.zeroes(c.HTMHtmVisitorCallbacks);
    _callbacks.visit_video = &TestVisitor_visitor_video_skip.visit_video;
    const _visitor = html_to_markdown_rs.c.htm_visitor_create(&_callbacks);
    defer html_to_markdown_rs.c.htm_visitor_free(_visitor);
    const _options_z = try std.heap.c_allocator.dupeZ(u8, "{}");
    defer std.heap.c_allocator.free(_options_z);
    const _options = html_to_markdown_rs.c.htm_conversion_options_from_json(_options_z.ptr);
    defer html_to_markdown_rs.c.htm_conversion_options_free(_options);
    html_to_markdown_rs.c.htm_options_set_visitor_handle(_options, _visitor);
    const _html_z = try std.heap.c_allocator.dupeZ(u8, "<h2>Demo</h2><video src=\"demo.webm\"></video><p>See the demo above.</p>");
    defer std.heap.c_allocator.free(_html_z);
    const _result = html_to_markdown_rs.c.htm_convert(_html_z.ptr, _options);
    try testing.expect(_result != null);
    defer html_to_markdown_rs.c.htm_conversion_result_free(_result.?);
    const _json_ptr = html_to_markdown_rs.c.htm_conversion_result_to_json(_result.?);
    defer html_to_markdown_rs.c.htm_free_string(_json_ptr);
    const _result_json = std.mem.sliceTo(_json_ptr, 0);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Demo") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "See the demo above.") != null);
    }
    {
        const _jvnc = result.object.get("content").?;
        const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
        defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
        try testing.expect(std.mem.indexOf(u8, _jsnc, "demo.webm") == null);
    }
}
