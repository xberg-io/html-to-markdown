// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:602dff71187141788025192a9fca1e3288feb1abd857a11d46edaa3834499870
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
const std = @import("std");
const testing = std.testing;
const html_to_markdown_rs = @import("html_to_markdown_rs");

// Suppress C++ global destructor aborts that break zig's --listen=- IPC
extern "c" fn signal(sig: i32, handler: usize) usize;
var _abort_handler_installed: bool = false;
fn suppress_abort() void {
    if (!_abort_handler_installed) {
        // SIGABRT = 6 on POSIX; SIG_IGN = 1
        _ = signal(6, 1);
        _abort_handler_installed = true;
    }
}

// E2e tests for category: structure

test "structure_code_block" {
    // Fenced code block produces Code node
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>Example code:</p><pre><code class=\"language-rust\">fn main() { println!(\"Hello\"); }</code></pre>", "{\"include_document_structure\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    try testing.expect(result.object.get("document").?.object.get("nodes").? != .null);
    try testing.expect(result.object.get("document").?.object.get("nodes").?.array.items.len >= @as(usize, 2));
}

test "structure_deep_nesting_h1_h2_h3" {
    // H1 > H2 > H3 creates three levels of heading nesting
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<h1>Top Level</h1><p>Top intro.</p><h2>Mid Level</h2><p>Mid content.</p><h3>Deep Level</h3><p>Deep content.</p>", "{\"include_document_structure\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    try testing.expect(result.object.get("document").?.object.get("nodes").? != .null);
    try testing.expect(result.object.get("document").?.object.get("nodes").?.array.items.len >= @as(usize, 5));
}

test "structure_h1_h2_nested_group" {
    // H1 followed by H2 creates a nested group under the H1
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<h1>Chapter One</h1><p>Chapter intro.</p><h2>Section One</h2><p>Section content.</p>", "{\"include_document_structure\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    try testing.expect(result.object.get("document").?.object.get("nodes").? != .null);
    try testing.expect(result.object.get("document").?.object.get("nodes").?.array.items.len >= @as(usize, 3));
}

test "structure_heading_paragraph" {
    // Simple heading followed by paragraph produces Heading and Paragraph nodes
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<h1>Title</h1><p>A paragraph of text.</p>", "{\"include_document_structure\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    try testing.expect(result.object.get("document").?.object.get("nodes").? != .null);
    try testing.expect(result.object.get("document").?.object.get("nodes").?.array.items.len >= @as(usize, 2));
}

test "structure_list" {
    // Unordered list produces List and ListItem nodes
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>Items:</p><ul><li>Alpha</li><li>Beta</li><li>Gamma</li></ul>", "{\"include_document_structure\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    try testing.expect(result.object.get("document").?.object.get("nodes").? != .null);
    try testing.expect(result.object.get("document").?.object.get("nodes").?.array.items.len >= @as(usize, 2));
}

test "structure_multiple_headings" {
    // Multiple headings create multiple Heading nodes with correct levels
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<h1>Main Title</h1><h2>Section One</h2><p>Section one content.</p><h2>Section Two</h2><p>Section two content.</p>", "{\"include_document_structure\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    try testing.expect(result.object.get("document").?.object.get("nodes").? != .null);
    try testing.expect(result.object.get("document").?.object.get("nodes").?.array.items.len >= @as(usize, 4));
}

test "structure_sibling_h1_groups" {
    // H1, H2, then another H1 creates two sibling top-level groups
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<h1>Chapter One</h1><h2>Section A</h2><p>Section A content.</p><h1>Chapter Two</h1><h2>Section B</h2><p>Section B content.</p>", "{\"include_document_structure\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    try testing.expect(result.object.get("document").?.object.get("nodes").? != .null);
    try testing.expect(result.object.get("document").?.object.get("nodes").?.array.items.len >= @as(usize, 4));
}
