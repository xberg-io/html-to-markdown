// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c4123217bb42e6cbe7cc59851037f301994cdb660a4a2934d8b056b2b5fc5adf
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
const std = @import("std");
const testing = std.testing;
const html_to_markdown_rs = @import("html_to_markdown_rs");

// Suppress C++ global destructor aborts that break zig's --listen=- IPC
extern "c" fn signal(sig: i32, handler: usize) usize;
var _abort_handler_installed: bool = false;
fn suppress_abort() void {
    if (!_abort_handler_installed) {
        // SIGABRT = 6 on POSIX; SIG_IGN = 1
        _ = signal(6, 1);
        _abort_handler_installed = true;
    }
}

// E2e tests for category: metadata

test "metadata_author_meta" {
    // Extract author from <meta name='author'> tag
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html><head><title>Page</title><meta name=\"author\" content=\"Jane Doe\"></head><body><p>Content</p></body></html>", "{\"extract_metadata\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    try testing.expectEqualStrings("Jane Doe", std.mem.trim(u8, result.object.get("metadata").?.object.get("document").?.object.get("author").?.string, " \n\r\t"));
}

test "metadata_canonical_url" {
    // Extract canonical URL from <link rel='canonical'> tag
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html><head><title>Page</title><link rel=\"canonical\" href=\"https://example.com/canonical-page\"></head><body><p>Content</p></body></html>", "{\"extract_metadata\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    try testing.expectEqualStrings("https://example.com/canonical-page", std.mem.trim(u8, result.object.get("metadata").?.object.get("document").?.object.get("canonical_url").?.string, " \n\r\t"));
}

test "metadata_description_meta" {
    // Extract description from <meta name='description'> tag
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html><head><title>Page</title><meta name=\"description\" content=\"This is the page description.\"></head><body><p>Content</p></body></html>", "{\"extract_metadata\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    try testing.expectEqualStrings("This is the page description.", std.mem.trim(u8, result.object.get("metadata").?.object.get("document").?.object.get("description").?.string, " \n\r\t"));
}

test "metadata_dublin_core" {
    // Extract Dublin Core metadata tags
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html><head><title>Scholarly Work</title><meta name=\"DC.title\" content=\"Principles of Knowledge Management\"><meta name=\"DC.creator\" content=\"Dr. Alice Johnson\"><meta name=\"DC.date\" content=\"2023-06-15\"><meta name=\"DC.subject\" content=\"Knowledge Management\"><meta name=\"DC.publisher\" content=\"Academic Press\"></head><body><p>This is a scholarly article.</p></body></html>", "{\"extract_metadata\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "scholarly article") != null);
    }
}

test "metadata_extract_all_images" {
    // Extract all images from a document into metadata
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html><head><title>Gallery</title></head><body><img src=\"https://example.com/photo1.jpg\" alt=\"Photo 1\"><img src=\"https://example.com/photo2.png\" alt=\"Photo 2\"><img src=\"/local/image.webp\" alt=\"Local image\"></body></html>", "{\"extract_metadata\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    try testing.expect(result.object.get("metadata").?.object.get("images").?.array.items.len >= @as(usize, 2));
}

test "metadata_extract_all_links" {
    // Extract all links from a document into metadata
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html><head><title>Links Page</title></head><body><p>Visit <a href=\"https://example.com\">Example</a> or <a href=\"https://docs.example.com\">Docs</a>.</p><p>Also see <a href=\"/relative/path\">relative link</a> and <a href=\"mailto:hello@example.com\">email us</a>.</p></body></html>", "{\"extract_metadata\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    try testing.expect(result.object.get("metadata").?.object.get("links").?.array.items.len >= @as(usize, 2));
}

test "metadata_headers_hierarchy" {
    // Extract heading hierarchy from document into metadata
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html><head><title>Docs</title></head><body><h1>Introduction</h1><h2>Getting Started</h2><h3>Installation</h3><h3>Configuration</h3><h2>Advanced Usage</h2><h3>Custom Options</h3></body></html>", "{\"extract_metadata\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    try testing.expect(result.object.get("metadata").?.object.get("headers").?.array.items.len >= @as(usize, 5));
}

test "metadata_image_type_data_uri_classified" {
    // A data-URI image is extracted into metadata.images when extract_metadata is true
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p><img src=\"data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8/5+hHgAHggJ/PchI7wAAAABJRU5ErkJggg==\" alt=\"pixel\"></p>", "{\"extract_metadata\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    try testing.expect(result.object.get("metadata").?.object.get("images").?.array.items.len >= @as(usize, 1));
}

test "metadata_image_type_external_classified" {
    // An https image URL is extracted into metadata.images when extract_metadata is true
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p><img src=\"https://example.com/photo.jpg\" alt=\"A photo\"></p>", "{\"extract_metadata\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    try testing.expect(result.object.get("metadata").?.object.get("images").?.array.items.len >= @as(usize, 1));
}

test "metadata_keywords_meta" {
    // Extract keywords from <meta name='keywords'> tag
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html><head><title>Page</title><meta name=\"keywords\" content=\"rust, markdown, html, converter\"></head><body><p>Content</p></body></html>", "{\"extract_metadata\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    try testing.expect(result.object.get("metadata").?.object.get("document").?.object.get("keywords").?.array.items.len >= @as(usize, 1));
}

test "metadata_lang_attribute" {
    // Extract language from html lang attribute
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html lang=\"es\"><head><title>Spanish Page</title></head><body><h1>Hola Mundo</h1><p>Este es un documento en español.</p></body></html>", "{\"extract_metadata\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Hola Mundo") != null);
    }
}

test "metadata_link_type_anchor_classified" {
    // A fragment anchor link is extracted into metadata.links when extract_metadata is true
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>Jump to <a href=\"#section\">section</a> below.</p>", "{\"extract_metadata\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    try testing.expect(result.object.get("metadata").?.object.get("links").?.array.items.len >= @as(usize, 1));
}

test "metadata_link_type_email_classified" {
    // A mailto link is extracted into metadata.links when extract_metadata is true
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>Contact <a href=\"mailto:hello@example.com\">us</a> directly.</p>", "{\"extract_metadata\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    try testing.expect(result.object.get("metadata").?.object.get("links").?.array.items.len >= @as(usize, 1));
}

test "metadata_link_type_external_classified" {
    // An https link is extracted into metadata.links when extract_metadata is true
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<p>See <a href=\"https://example.com\">Example</a> for details.</p>", "{\"extract_metadata\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    try testing.expect(result.object.get("metadata").?.object.get("links").?.array.items.len >= @as(usize, 1));
}

test "metadata_microdata_schema_article" {
    // Extract schema.org microdata for Article
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html><head><title>Article</title></head><body><article itemscope itemtype=\"https://schema.org/Article\"><h1 itemprop=\"headline\">Breaking News Today</h1><span itemprop=\"author\">Jane Reporter</span><span itemprop=\"datePublished\">2024-04-22</span><div itemprop=\"articleBody\"><p>The article content goes here with important information about the breaking news story.</p></div></article></body></html>", "{\"extract_metadata\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Breaking News Today") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Jane Reporter") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "important information") != null);
    }
}

test "metadata_microdata_schema_breadcrumb" {
    // Extract schema.org breadcrumb navigation microdata
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html><head><title>Navigation</title></head><body><nav itemscope itemtype=\"https://schema.org/BreadcrumbList\"><span itemprop=\"itemListElement\" itemscope itemtype=\"https://schema.org/ListItem\"><a itemprop=\"item\" href=\"https://example.com\"><span itemprop=\"name\">Home</span></a></span><span itemprop=\"itemListElement\" itemscope itemtype=\"https://schema.org/ListItem\"><a itemprop=\"item\" href=\"https://example.com/products\"><span itemprop=\"name\">Products</span></a></span><span itemprop=\"itemListElement\" itemscope itemtype=\"https://schema.org/ListItem\"><span itemprop=\"name\">Current Page</span></span></nav></body></html>", "{\"extract_metadata\":true,\"preprocessing\":{\"remove_navigation\":false}}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Home") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Products") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Current Page") != null);
    }
}

test "metadata_microdata_schema_organization" {
    // Extract schema.org microdata for Organization
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html><head><title>Company</title></head><body><div itemscope itemtype=\"https://schema.org/Organization\"><span itemprop=\"name\">Acme Corp</span><span itemprop=\"foundingDate\">2020</span><span itemprop=\"url\">https://acmecorp.example.com</span><span itemprop=\"logo\">https://acmecorp.example.com/logo.png</span></div></body></html>", "{\"extract_metadata\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Acme Corp") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "2020") != null);
    }
}

test "metadata_microdata_schema_person" {
    // Extract schema.org microdata for Person
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html><head><title>Contact</title></head><body><div itemscope itemtype=\"https://schema.org/Person\"><span itemprop=\"name\">John Smith</span><span itemprop=\"email\">john@example.com</span><span itemprop=\"telephone\">+1-555-0100</span></div></body></html>", "{\"extract_metadata\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "John Smith") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "john@example.com") != null);
    }
}

test "metadata_microdata_schema_product" {
    // Extract schema.org microdata for Product
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html><head><title>Product</title></head><body><div itemscope itemtype=\"https://schema.org/Product\"><h1 itemprop=\"name\">Awesome Widget</h1><span itemprop=\"description\">The best widget on the market</span><span itemprop=\"price\">29.99</span><span itemprop=\"priceCurrency\">USD</span><img itemprop=\"image\" src=\"widget.jpg\" alt=\"Widget\"><span itemprop=\"ratingValue\">4.5</span></div></body></html>", "{\"extract_metadata\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "Awesome Widget") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "best widget") != null);
    }
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "29.99") != null);
    }
}

test "metadata_text_direction_ltr" {
    // Extract text direction from lang attribute on html element
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html lang=\"en\" dir=\"ltr\"><head><title>LTR Document</title></head><body><p>This is left-to-right text.</p></body></html>", "{\"extract_metadata\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "left-to-right text") != null);
    }
}

test "metadata_text_direction_rtl" {
    // Extract right-to-left text direction
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html lang=\"ar\" dir=\"rtl\"><head><title>RTL Document</title></head><body><p>This is right-to-left text.</p></body></html>", "{\"extract_metadata\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jv = result.object.get("content").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "right-to-left text") != null);
    }
}

test "metadata_title_tag" {
    // Extract title from <title> tag
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html><head><title>My Page</title></head><body><p>Content</p></body></html>", "{\"extract_metadata\":true}");
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    try testing.expectEqualStrings("My Page", std.mem.trim(u8, result.object.get("metadata").?.object.get("document").?.object.get("title").?.string, " \n\r\t"));
}

test "og_basic_tags" {
    // Extract og:title, og:description, and og:image from Open Graph meta tags
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html><head><title>Fallback Title</title><meta property=\"og:title\" content=\"OG Title\"><meta property=\"og:description\" content=\"OG description text.\"><meta property=\"og:image\" content=\"https://example.com/image.jpg\"></head><body><p>Content</p></body></html>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    try testing.expectEqualStrings("OG Title", std.mem.trim(u8, result.object.get("metadata").?.object.get("document").?.object.get("open_graph").?.object.get("title").?.string, " \n\r\t"));
    try testing.expectEqualStrings("OG description text.", std.mem.trim(u8, result.object.get("metadata").?.object.get("document").?.object.get("open_graph").?.object.get("description").?.string, " \n\r\t"));
    try testing.expectEqualStrings("https://example.com/image.jpg", std.mem.trim(u8, result.object.get("metadata").?.object.get("document").?.object.get("open_graph").?.object.get("image").?.string, " \n\r\t"));
}

test "og_multiple_tags" {
    // Extract multiple Open Graph tags including type, url, and site_name
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html><head><meta property=\"og:title\" content=\"Article Title\"><meta property=\"og:type\" content=\"article\"><meta property=\"og:url\" content=\"https://example.com/article\"><meta property=\"og:site_name\" content=\"Example Site\"><meta property=\"og:description\" content=\"An interesting article.\"><meta property=\"og:image\" content=\"https://example.com/article.jpg\"></head><body><article><p>Article content here.</p></article></body></html>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    try testing.expectEqualStrings("Article Title", std.mem.trim(u8, result.object.get("metadata").?.object.get("document").?.object.get("open_graph").?.object.get("title").?.string, " \n\r\t"));
    try testing.expectEqualStrings("article", std.mem.trim(u8, result.object.get("metadata").?.object.get("document").?.object.get("open_graph").?.object.get("type").?.string, " \n\r\t"));
    try testing.expectEqualStrings("https://example.com/article", std.mem.trim(u8, result.object.get("metadata").?.object.get("document").?.object.get("open_graph").?.object.get("url").?.string, " \n\r\t"));
    try testing.expectEqualStrings("Example Site", std.mem.trim(u8, result.object.get("metadata").?.object.get("document").?.object.get("open_graph").?.object.get("site_name").?.string, " \n\r\t"));
}

test "structured_data_json_ld" {
    // JSON-LD script tag is stripped from output (security) but metadata may be extracted
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html><head><title>Article</title><script type=\"application/ld+json\">{\"@context\":\"https://schema.org\",\"@type\":\"Article\",\"headline\":\"My Article\",\"author\":{\"@type\":\"Person\",\"name\":\"Jane Doe\"},\"datePublished\":\"2024-01-15\"}</script></head><body><h1>My Article</h1><p>Article body text.</p></body></html>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "My Article") != null);
    }
    {
        const _jvnc0 = result.object.get("content").?;
        const _jsnc0 = if (_jvnc0 == .string) _jvnc0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc0, .{});
        defer if (_jvnc0 != .string) std.heap.c_allocator.free(_jsnc0);
        try testing.expect(std.mem.indexOf(u8, _jsnc0, "application/ld+json") == null);
    }
    {
        const _jvnc1 = result.object.get("content").?;
        const _jsnc1 = if (_jvnc1 == .string) _jvnc1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc1, .{});
        defer if (_jvnc1 != .string) std.heap.c_allocator.free(_jsnc1);
        try testing.expect(std.mem.indexOf(u8, _jsnc1, "@context") == null);
    }
    {
        const _jvnc2 = result.object.get("content").?;
        const _jsnc2 = if (_jvnc2 == .string) _jvnc2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc2, .{});
        defer if (_jvnc2 != .string) std.heap.c_allocator.free(_jsnc2);
        try testing.expect(std.mem.indexOf(u8, _jsnc2, "schema.org") == null);
    }
}

test "structured_data_multiple_json_ld" {
    // Multiple JSON-LD blocks are all stripped from output
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html><head><title>Shop Page</title><script type=\"application/ld+json\">{\"@context\":\"https://schema.org\",\"@type\":\"Product\",\"name\":\"Widget\",\"price\":\"9.99\"}</script><script type=\"application/ld+json\">{\"@context\":\"https://schema.org\",\"@type\":\"BreadcrumbList\",\"itemListElement\":[{\"@type\":\"ListItem\",\"position\":1,\"name\":\"Home\"}]}</script></head><body><h1>Widget</h1><p>A great widget for all purposes.</p></body></html>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    {
        const _jva0 = result.object.get("content").?;
        const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
        defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
        try testing.expect(std.mem.indexOf(u8, _jsa0, "Widget") != null);
    }
    {
        const _jvnc0 = result.object.get("content").?;
        const _jsnc0 = if (_jvnc0 == .string) _jvnc0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc0, .{});
        defer if (_jvnc0 != .string) std.heap.c_allocator.free(_jsnc0);
        try testing.expect(std.mem.indexOf(u8, _jsnc0, "@context") == null);
    }
    {
        const _jvnc1 = result.object.get("content").?;
        const _jsnc1 = if (_jvnc1 == .string) _jvnc1.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc1, .{});
        defer if (_jvnc1 != .string) std.heap.c_allocator.free(_jsnc1);
        try testing.expect(std.mem.indexOf(u8, _jsnc1, "BreadcrumbList") == null);
    }
    {
        const _jvnc2 = result.object.get("content").?;
        const _jsnc2 = if (_jvnc2 == .string) _jvnc2.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc2, .{});
        defer if (_jvnc2 != .string) std.heap.c_allocator.free(_jsnc2);
        try testing.expect(std.mem.indexOf(u8, _jsnc2, "ListItem") == null);
    }
}

test "twitter_card_tags" {
    // Extract Twitter card meta tags
    suppress_abort();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const _result_json = try html_to_markdown_rs.convert("<html><head><meta name=\"twitter:card\" content=\"summary_large_image\"><meta name=\"twitter:site\" content=\"@examplesite\"><meta name=\"twitter:title\" content=\"Twitter Card Title\"><meta name=\"twitter:description\" content=\"Twitter card description.\"><meta name=\"twitter:image\" content=\"https://example.com/twitter-image.jpg\"></head><body><p>Content</p></body></html>", null);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("content").? != .null);
    try testing.expectEqualStrings("summary_large_image", std.mem.trim(u8, result.object.get("metadata").?.object.get("document").?.object.get("twitter_card").?.object.get("card").?.string, " \n\r\t"));
    try testing.expectEqualStrings("Twitter Card Title", std.mem.trim(u8, result.object.get("metadata").?.object.get("document").?.object.get("twitter_card").?.object.get("title").?.string, " \n\r\t"));
    try testing.expectEqualStrings("Twitter card description.", std.mem.trim(u8, result.object.get("metadata").?.object.get("document").?.object.get("twitter_card").?.object.get("description").?.string, " \n\r\t"));
}
