// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c4123217bb42e6cbe7cc59851037f301994cdb660a4a2934d8b056b2b5fc5adf
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// ignore_for_file: unused_local_variable, unused_element

import 'package:test/test.dart';
import 'package:h2m/h2m.dart';
import 'package:h2m/src/html_to_markdown_rs_bridge_generated/frb_generated.dart' show RustLib;

// E2e tests for category: visitor

String _alefE2eText(Object? value) {
  if (value == null) return '';
  // Check if it's an enum by examining its toString representation.
  final str = value.toString();
  if (str.contains('.')) {
    // Enum.toString() returns 'EnumName.variantName'. Extract the variant name.
    final parts = str.split('.');
    if (parts.length == 2) {
      final variantName = parts[1];
      // Convert camelCase variant names to snake_case for serde compatibility.
      // E.g. 'toolCalls' -> 'tool_calls', 'stop' -> 'stop'.
      return _camelToSnake(variantName);
    }
  }
  return str;
}

String _camelToSnake(String camel) {
  final buffer = StringBuffer();
  for (int i = 0; i < camel.length; i++) {
    final char = camel[i];
    if (char.contains(RegExp(r'[A-Z]'))) {
      if (i > 0) buffer.write('_');
      buffer.write(char.toLowerCase());
    } else {
      buffer.write(char);
    }
  }
  return buffer.toString();
}

void main() {
  var _rustLibInitialized = false;

  setUpAll(() async {
    await RustLib.init();
    _rustLibInitialized = true;
  });

  tearDownAll(() async {
    if (_rustLibInitialized) {
      RustLib.dispose();
    }
  });

  test('Visitor replaces audio element with custom output', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.custom(field0: '[AUDIO: podcast.mp3]'),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>Listen to this: <audio src="podcast.mp3" controls></audio></p>', options: _options);
    expect(result.content, contains('[AUDIO: podcast.mp3]'));
    expect(result.content, contains('Listen to this:'));
  });

  test('Visitor removes audio elements from output', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.skip(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>Background music:</p><audio src="music.ogg" autoplay></audio><p>Enjoy!</p>', options: _options);
    expect(result.content, contains('Background music:'));
    expect(result.content, contains('Enjoy!'));
    expect(result.content, isNot(contains('music.ogg')));
  });

  test('Visitor replaces button with bracketed text', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.custom(field0: '[BTN:${text}]'),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>Confirm action: <button type="submit">Click me</button> or <button type="reset">Cancel</button></p>', options: _options);
    expect(result.content, contains('[BTN:Click me]'));
    expect(result.content, contains('[BTN:Cancel]'));
    expect(result.content, contains('Confirm action:'));
  });

  test('Visitor removes all buttons from output', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.skip(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>Actions available: <button>Save</button> <button>Delete</button> <button>Export</button></p>', options: _options);
    expect(result.content, contains('Actions available:'));
    expect(result.content, isNot(contains('Save')));
    expect(result.content, isNot(contains('Delete')));
    expect(result.content, isNot(contains('Export')));
  });

  test('Visitor continue action preserves default conversion', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>Hello <strong>World</strong></p>', options: _options);
    expect(result.content, contains('**World**'));
  });

  test('Visitor replaces blockquote with custom format', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.custom(field0: 'QUOTE: "${content}"'),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<blockquote><p>A wise quote.</p></blockquote>', options: _options);
    expect(result.content, contains('QUOTE:'));
    expect(result.content, contains('A wise quote.'));
  });

  test('Visitor replaces emphasis with custom output', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.custom(field0: '>>>${text}<<<'),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>This is <em>important</em> text.</p>', options: _options);
    expect(result.content, contains('>>>important<<<'));
    expect(result.content, isNot(contains('*important*')));
  });

  test('Visitor replaces heading with custom format', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.custom(field0: '--- ${text} ---'),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<h2>Section Title</h2><p>Content below heading.</p>', options: _options);
    expect(result.content, contains('--- Section Title ---'));
    expect(result.content, isNot(contains('## Section Title')));
    expect(result.content, contains('Content below heading.'));
  });

  test('Visitor replaces image with custom output using template', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.custom(field0: '[Image: ${alt}]'),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<img src="banner.png" alt="Banner">', options: _options);
    expect(result.content, contains('[Image: Banner]'));
    expect(result.content, isNot(contains('banner.png')));
  });

  test('Visitor reformats links using template interpolation', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.custom(field0: '${text} (${href})'),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>Visit <a href="https://example.com">Example</a> for more info.</p>', options: _options);
    expect(result.content, contains('Example (https://example.com)'));
    expect(result.content, isNot(contains('[Example]')));
  });

  test('Visitor replaces link with static custom output', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.custom(field0: '[REDACTED LINK]'),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<a href="https://example.com">Click here</a>', options: _options);
    expect(result.content, contains('[REDACTED LINK]'));
    expect(result.content, isNot(contains('example.com')));
  });

  test('Visitor custom action replaces element output', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.custom(field0: '## REPLACED HEADING'),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<h1>Original Heading</h1>', options: _options);
    expect(result.content, contains('## REPLACED HEADING'));
    expect(result.content, isNot(contains('# Original Heading')));
  });

  test('Visitor customizes definition list items', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.custom(field0: '**${text}**'),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<dl><dt>HTML</dt><dd>HyperText Markup Language</dd><dt>CSS</dt><dd>Cascading Style Sheets</dd></dl>', options: _options);
    expect(result.content, contains('**HTML**'));
    expect(result.content, contains('**CSS**'));
    expect(result.content, contains('HyperText Markup Language'));
  });

  test('Visitor formats definition lists with custom templates', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.custom(field0: '### ${text}'),
          visitDefinitionDescription: (ctx, text) async => VisitResult.custom(field0: '> ${text}'),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<dl><dt>Python</dt><dd>A high-level programming language</dd><dt>JavaScript</dt><dd>A scripting language for web browsers</dd></dl>', options: _options);
    expect(result.content, contains('### Python'));
    expect(result.content, contains('### JavaScript'));
    expect(result.content, contains('> A high-level programming language'));
    expect(result.content, contains('> A scripting language for web browsers'));
  });

  test('Visitor skips definition list items from output', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.skip(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.skip(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>Glossary:</p><dl><dt>Term A</dt><dd>Definition of term A</dd><dt>Term B</dt><dd>Definition of term B</dd></dl><p>End of glossary</p>', options: _options);
    expect(result.content, contains('Glossary:'));
    expect(result.content, contains('End of glossary'));
    expect(result.content, isNot(contains('Term A')));
    expect(result.content, isNot(contains('Definition')));
  });

  test('Visitor customizes details/summary disclosure elements', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.custom(field0: '[EXPANDABLE] ${text}'),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<details><summary>Click to expand</summary><p>This content is initially hidden.</p><p>But can be revealed by the user.</p></details>', options: _options);
    expect(result.content, contains('[EXPANDABLE] Click to expand'));
    expect(result.content, contains('This content is initially hidden.'));
  });

  test('Visitor removes details/summary elements entirely', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.skip(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>Main content here.</p><details><summary>Hidden section</summary><p>Secret details</p></details><p>More main content.</p>', options: _options);
    expect(result.content, contains('Main content here.'));
    expect(result.content, contains('More main content.'));
    expect(result.content, isNot(contains('Hidden section')));
    expect(result.content, isNot(contains('Secret details')));
  });

  test('Visitor customizes figure and figcaption elements', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.custom(field0: '*${text}*'),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<article><h1>Article Title</h1><p>Introduction paragraph.</p><figure><img src="diagram.png" alt="System architecture diagram"><figcaption>Figure 1: System Architecture</figcaption></figure><p>Explanation of the figure.</p></article>', options: _options);
    expect(result.content, contains('Article Title'));
    expect(result.content, contains('*Figure 1: System Architecture*'));
    expect(result.content, contains('Explanation of the figure.'));
  });

  test('Visitor wraps figure content with custom formatting', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.custom(field0: '\n[FIGURE]\n'),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.custom(field0: '${output}\n[/FIGURE]\n')
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<section><h2>Gallery</h2><figure><img src="photo1.jpg" alt="Photo"><figcaption>Beautiful sunset</figcaption></figure></section>', options: _options);
    expect(result.content, contains('[FIGURE]'));
    expect(result.content, contains('[/FIGURE]'));
    expect(result.content, contains('Gallery'));
  });

  test('Visitor removes figure elements with their captions', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.skip(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>See the chart below:</p><figure><img src="chart.svg"><figcaption>Revenue Trends 2020-2024</figcaption></figure><p>As shown in the chart above.</p>', options: _options);
    expect(result.content, contains('See the chart below:'));
    expect(result.content, contains('As shown in the chart above.'));
    expect(result.content, isNot(contains('Revenue Trends')));
    expect(result.content, isNot(contains('chart.svg')));
  });

  test('Visitor replaces form with custom output', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.custom(field0: '[FORM PLACEHOLDER]'),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<div><form action="/submit" method="POST"><label>Name: <input type="text" name="name"></label><button type="submit">Submit</button></form></div>', options: _options);
    expect(result.content, contains('[FORM PLACEHOLDER]'));
    expect(result.content, isNot(contains('submit')));
    expect(result.content, isNot(contains('input')));
  });

  test('Visitor skips form elements entirely', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.skip(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>Before form</p><form><input type="email" name="email"></form><p>After form</p>', options: _options);
    expect(result.content, contains('Before form'));
    expect(result.content, contains('After form'));
    expect(result.content, isNot(contains('email')));
  });

  test('Visitor returns rendered heading template as bare string; mixed-case is preserved (regression guard for issue #350)', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.custom(field0: '## ${text} ##'),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<h2>Important Section Title</h2><p>Body.</p>', options: _options);
    expect(result.content, contains('## Important Section Title ##'));
    expect(result.content, isNot(contains('important section title')));
  });

  test('Visitor replaces horizontal rule with custom output', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.custom(field0: '\n[DIVIDER]\n'),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<h1>Section A</h1><p>Content A</p><hr><h1>Section B</h1><p>Content B</p>', options: _options);
    expect(result.content, contains('[DIVIDER]'));
    expect(result.content, contains('Section A'));
    expect(result.content, contains('Section B'));
    expect(result.content, isNot(contains('---')));
  });

  test('Visitor removes all horizontal rules', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.skip(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>Part 1</p><hr><p>Part 2</p><hr><p>Part 3</p>', options: _options);
    expect(result.content, contains('Part 1'));
    expect(result.content, contains('Part 2'));
    expect(result.content, contains('Part 3'));
    expect(result.content, isNot(contains('---')));
  });

  test('Visitor replaces embedded iframe with custom text', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.custom(field0: '[EMBEDDED: https://maps.example.com/embed]'),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>Embedded map:</p><iframe src="https://maps.example.com/embed" width="400" height="300"></iframe><p>End of map</p>', options: _options);
    expect(result.content, contains('[EMBEDDED: https://maps.example.com/embed]'));
    expect(result.content, contains('Embedded map:'));
    expect(result.content, contains('End of map'));
  });

  test('Visitor removes embedded iframes', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.skip(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<h3>Reviews</h3><iframe src="https://widget.example.com/reviews"></iframe><p>See reviews from our partners.</p>', options: _options);
    expect(result.content, contains('Reviews'));
    expect(result.content, contains('See reviews from our partners.'));
    expect(result.content, isNot(contains('widget.example.com')));
  });

  test('Visitor returns bare-string image replacement; mixed-case alt/src preserved (regression guard for issue #350)', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.custom(field0: '[image: ${alt} -> ${src}]'),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<img src="PhotoOne.JPG" alt="Sunset Over Bay">', options: _options);
    expect(result.content, contains('[image: Sunset Over Bay -> PhotoOne.JPG]'));
    expect(result.content, isNot(contains('sunset over bay')));
    expect(result.content, isNot(contains('photoone.jpg')));
  });

  test('Visitor replaces input with labeled output', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.custom(field0: '[INPUT:${inputType}]'),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<form><label>Username: <input type="text" name="username" value=""></label><label>Password: <input type="password" name="password"></label></form>', options: _options);
    expect(result.content, contains('[INPUT:text]'));
    expect(result.content, contains('[INPUT:password]'));
  });

  test('Visitor skips all input elements', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.skip(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>Sign up:</p><input type="text" name="email" placeholder="your@email.com"><input type="checkbox" name="agree"><p>Continue</p>', options: _options);
    expect(result.content, contains('Sign up:'));
    expect(result.content, contains('Continue'));
    expect(result.content, isNot(contains('email')));
  });

  test('Visitor replaces line break with custom output', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.custom(field0: ' | '),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>First line<br>Second line<br>Third line</p>', options: _options);
    expect(result.content, contains('First line | Second line | Third line'));
    expect(result.content, isNot(contains('\n\n')));
  });

  test('Visitor removes all line breaks', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.skip(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>Address Line 1<br>Address Line 2<br>Address Line 3</p>', options: _options);
    expect(result.content, contains('Address Line 1Address Line 2Address Line 3'));
  });

  test('Visitor returns rendered template as a bare string (not a dict) and original mixed-case content is preserved end-to-end (regression guard for issue #350)', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.custom(field0: '[${text}](https://new-cdn.com/file.pdf)'),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<a href="https://old-cdn.com/file.pdf">Download</a>', options: _options);
    expect(result.content, contains('[Download](https://new-cdn.com/file.pdf)'));
    expect(result.content, isNot(contains('[download]')));
    expect(result.content, isNot(contains('old-cdn.com')));
  });

  test('Visitor replaces highlight/mark with custom template', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.custom(field0: '==${text}=='),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>This is a <mark>highlighted passage</mark> in the text.</p>', options: _options);
    expect(result.content, contains('==highlighted passage=='));
    expect(result.content, contains('This is a'));
  });

  test('Visitor skips mark elements entirely', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.skip(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>Key insight: <mark>always validate input</mark> for security.</p>', options: _options);
    expect(result.content, isNot(contains('always validate input')));
    expect(result.content, contains('Key insight:'));
    expect(result.content, contains('for security.'));
  });

  test('Visitor preserve_html action includes raw HTML in output', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.preserveHtml(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<div><custom-tag>Custom content</custom-tag></div>', options: _options);
    expect(result.content, contains('<custom-tag>'));
  });

  test('Visitor skips code blocks from output', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.skip(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>Intro text</p><pre><code>let x = 42;</code></pre><p>Outro text</p>', options: _options);
    expect(result.content, contains('Intro text'));
    expect(result.content, contains('Outro text'));
    expect(result.content, isNot(contains('let x = 42')));
  });

  test('Visitor skip action omits all headings from output', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.skip(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<h1>Title</h1><p>Body text remains.</p>', options: _options);
    expect(result.content, isNot(contains('Title')));
    expect(result.content, contains('Body text remains.'));
  });

  test('Visitor skips all images from output', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.skip(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>Before image</p><img src="photo.jpg" alt="A photo"><p>After image</p>', options: _options);
    expect(result.content, contains('Before image'));
    expect(result.content, contains('After image'));
    expect(result.content, isNot(contains('photo.jpg')));
    expect(result.content, isNot(contains('A photo')));
  });

  test('Visitor skips all links entirely', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.skip(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>Before <a href="https://example.com">link text</a> after</p>', options: _options);
    expect(result.content, isNot(contains('link text')));
    expect(result.content, isNot(contains('example.com')));
  });

  test('Visitor skips bold/strong elements', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.skip(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>Normal <strong>bold text</strong> normal</p>', options: _options);
    expect(result.content, isNot(contains('bold text')));
    expect(result.content, contains('Normal'));
  });

  test('Visitor replaces subscript with custom template', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.custom(field0: '~${text}~'),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>H<sub>2</sub>O is water.</p>', options: _options);
    expect(result.content, contains('H~2~O'));
    expect(result.content, contains('is water'));
  });

  test('Visitor skips subscript elements entirely', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.skip(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>The formula C<sub>12</sub>H<sub>22</sub>O<sub>11</sub> is sugar.</p>', options: _options);
    expect(result.content, contains('The formula CHO is sugar.'));
  });

  test('Visitor replaces superscript with custom template', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.custom(field0: '^${text}^'),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>Einstein\'s E=mc<sup>2</sup> revolutionized physics.</p>', options: _options);
    expect(result.content, contains('E=mc^2^'));
    expect(result.content, contains('revolutionized physics'));
  });

  test('Visitor skips superscript from output', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.skip(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>The equation x<sup>3</sup> + y<sup>3</sup> = z<sup>3</sup> has no solutions.</p>', options: _options);
    expect(result.content, contains('The equation x + y = z has no solutions.'));
  });

  test('Visitor replaces underline with custom markup', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.custom(field0: '_${text}_'),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>This is <u>very important</u> text.</p>', options: _options);
    expect(result.content, contains('_very important_'));
    expect(result.content, isNot(contains('**')));
  });

  test('Visitor skips underline elements from output', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.skip(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.continue_(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>Normal text with <u>underlined part</u> and more text.</p>', options: _options);
    expect(result.content, contains('Normal text with'));
    expect(result.content, contains('and more text.'));
    expect(result.content, isNot(contains('underlined part')));
  });

  test('Visitor replaces video with custom link', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.custom(field0: '[VIDEO: ${src}]'),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<p>Watch our tutorial:</p><video src="tutorial.mp4" width="320" height="240" controls></video><p>Great content!</p>', options: _options);
    expect(result.content, contains('[VIDEO: tutorial.mp4]'));
    expect(result.content, contains('Watch our tutorial:'));
    expect(result.content, contains('Great content!'));
  });

  test('Visitor removes video elements entirely', () async {
    final _visitor = await createHtmlVisitor(
          visitText: (ctx, text) async => VisitResult.continue_(),
          visitElementStart: (ctx) async => VisitResult.continue_(),
          visitElementEnd: (ctx, output) async => VisitResult.continue_(),
          visitLink: (ctx, href, text, title) async => VisitResult.continue_(),
          visitImage: (ctx, src, alt, title) async => VisitResult.continue_(),
          visitHeading: (ctx, level, text, id) async => VisitResult.continue_(),
          visitCodeBlock: (ctx, lang, code) async => VisitResult.continue_(),
          visitCodeInline: (ctx, code) async => VisitResult.continue_(),
          visitListItem: (ctx, ordered, marker, text) async => VisitResult.continue_(),
          visitListStart: (ctx, ordered) async => VisitResult.continue_(),
          visitListEnd: (ctx, ordered, output) async => VisitResult.continue_(),
          visitTableStart: (ctx) async => VisitResult.continue_(),
          visitTableRow: (ctx, cells, isHeader) async => VisitResult.continue_(),
          visitTableEnd: (ctx, output) async => VisitResult.continue_(),
          visitBlockquote: (ctx, content, depth) async => VisitResult.continue_(),
          visitStrong: (ctx, text) async => VisitResult.continue_(),
          visitEmphasis: (ctx, text) async => VisitResult.continue_(),
          visitStrikethrough: (ctx, text) async => VisitResult.continue_(),
          visitUnderline: (ctx, text) async => VisitResult.continue_(),
          visitSubscript: (ctx, text) async => VisitResult.continue_(),
          visitSuperscript: (ctx, text) async => VisitResult.continue_(),
          visitMark: (ctx, text) async => VisitResult.continue_(),
          visitLineBreak: (ctx) async => VisitResult.continue_(),
          visitHorizontalRule: (ctx) async => VisitResult.continue_(),
          visitCustomElement: (ctx, tagName, html) async => VisitResult.continue_(),
          visitDefinitionListStart: (ctx) async => VisitResult.continue_(),
          visitDefinitionTerm: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionDescription: (ctx, text) async => VisitResult.continue_(),
          visitDefinitionListEnd: (ctx, text) async => VisitResult.continue_(),
          visitForm: (ctx, action, method) async => VisitResult.continue_(),
          visitInput: (ctx, inputType, name, value) async => VisitResult.continue_(),
          visitButton: (ctx, text) async => VisitResult.continue_(),
          visitAudio: (ctx, src) async => VisitResult.continue_(),
          visitVideo: (ctx, src) async => VisitResult.skip(),
          visitIframe: (ctx, src) async => VisitResult.continue_(),
          visitDetails: (ctx, open) async => VisitResult.continue_(),
          visitSummary: (ctx, text) async => VisitResult.continue_(),
          visitFigureStart: (ctx) async => VisitResult.continue_(),
          visitFigcaption: (ctx, text) async => VisitResult.continue_(),
          visitFigureEnd: (ctx, output) async => VisitResult.continue_()
        );
    final _options = await createConversionOptionsFromJsonWithVisitor(visitor: _visitor, json: '{}');
    final result = await H2mBridge.convert('<h2>Demo</h2><video src="demo.webm"></video><p>See the demo above.</p>', options: _options);
    expect(result.content, contains('Demo'));
    expect(result.content, contains('See the demo above.'));
    expect(result.content, isNot(contains('demo.webm')));
  });

}
