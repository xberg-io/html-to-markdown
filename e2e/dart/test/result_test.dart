// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ed9dcd2ad8e6afc726ed137983c74da1c9f6db124e71f331e46313be6bb0dd2e
// To regenerate: alef e2e generate
// To verify freshness: alef verify
// ignore_for_file: unused_local_variable, unused_element

import 'package:test/test.dart';
import 'package:h2m/h2m.dart';
import 'package:h2m/src/html_to_markdown_rs_bridge_generated/frb_generated.dart' show RustLib;

// E2e tests for category: result

void main() {
  var _rustLibInitialized = false;

  setUpAll(() async {
    await RustLib.init();
    _rustLibInitialized = true;
  });

  tearDownAll(() async {
    if (_rustLibInitialized) {
      RustLib.dispose();
    }
  });

  test('Result tables array is empty when input has no tables', () async {
    final options = await createConversionOptionsFromJson(json: '{"include_document_structure":true}');
    final result = await H2mBridge.convert('<p>No tables here</p>', options: options);
    expect(result.content?.toString(), isNotEmpty);
    expect(result.tables.length, equals(0));
  });

  test('Multiple tables each appear in the tables array', () async {
    final options = await createConversionOptionsFromJson(json: '{"include_document_structure":true}');
    final result = await H2mBridge.convert('<table><tr><th>A</th></tr><tr><td>1</td></tr></table><p>Between</p><table><tr><th>B</th></tr><tr><td>2</td></tr></table>', options: options);
    expect(result.tables.length, greaterThanOrEqualTo(2));
  });

  test('Simple table populates the tables array in result', () async {
    final options = await createConversionOptionsFromJson(json: '{"include_document_structure":true}');
    final result = await H2mBridge.convert('<table><thead><tr><th>Name</th><th>Age</th></tr></thead><tbody><tr><td>Alice</td><td>30</td></tr></tbody></table>', options: options);
    expect(result.content?.toString(), isNotEmpty);
    expect(result.tables.length, greaterThanOrEqualTo(1));
  });

  test('Tables array is empty when includeDocumentStructure is false', () async {
    final options = await createConversionOptionsFromJson(json: '{}');
    final result = await H2mBridge.convert('<table><tr><th>X</th></tr><tr><td>Y</td></tr></table>', options: options);
    expect(result.content?.toString(), isNotEmpty);
    expect(result.tables.length, equals(0));
  });

  test('A malformed data URI with extract_images enabled produces an ImageExtractionFailed warning', () async {
    final options = await createConversionOptionsFromJson(json: '{"extract_images":true}');
    final result = await H2mBridge.convert('<p>Text<img src="data:BADMIME" alt="broken">end</p>', options: options);
    expect(result.content?.toString(), isNotEmpty);
    expect(result.warnings.length, greaterThanOrEqualTo(1));
  });

  test('Warnings array is empty for well-formed HTML without problematic content', () async {
    final options = await createConversionOptionsFromJson(json: '{}');
    final result = await H2mBridge.convert('<h1>Title</h1><p>Clean content with <a href=\'https://example.com\'>a link</a>.</p>', options: options);
    expect(result.content?.toString(), isNotEmpty);
    expect(result.warnings.length, equals(0));
  });

  test('Warnings array is empty for complex but valid HTML', () async {
    final options = await createConversionOptionsFromJson(json: '{}');
    final result = await H2mBridge.convert('<article><h1>Article</h1><p>Paragraph with <strong>bold</strong> and <em>italic</em>.</p><table><tr><th>Col</th></tr><tr><td>Val</td></tr></table><ul><li>Item 1</li><li>Item 2</li></ul></article>', options: options);
    expect(result.content?.toString(), isNotEmpty);
    expect(result.warnings.length, equals(0));
  });

  test('Warnings array is empty even for malformed HTML (parser is lenient)', () async {
    final options = await createConversionOptionsFromJson(json: '{}');
    final result = await H2mBridge.convert('<p>Unclosed paragraph<div>Mixed nesting</p></div>', options: options);
    expect(result.content?.toString(), isNotEmpty);
    expect(result.warnings.length, equals(0));
  });

}
