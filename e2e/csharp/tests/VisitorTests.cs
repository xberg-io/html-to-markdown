// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f0091845a62ceffd7737fa8c33e989d0fe5014cdb4b948b8bdc2a0c3ba720f87
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net.Http;
using System.Text;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading.Tasks;
using Xunit;
using HtmlToMarkdown;

namespace Kreuzberg.E2e;

/// <summary>E2e tests for category: visitor.</summary>
public class VisitorTests
{
    private static readonly JsonSerializerOptions ConfigOptions = new() { Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) }, DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault };

    [Fact]
    public void Test_VisitorAudioCustom()
    {
        // Visitor replaces audio element with custom output
        var _visitor_visitor_audio_custom = new VisitorAudioCustomVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>Listen to this: <audio src=\"podcast.mp3\" controls></audio></p>", null, _visitor_visitor_audio_custom);
        Assert.Contains("[audio: podcast.mp3]", result.Content.ToString().ToLower());
        Assert.Contains("listen to this:", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_VisitorAudioSkip()
    {
        // Visitor removes audio elements from output
        var _visitor_visitor_audio_skip = new VisitorAudioSkipVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>Background music:</p><audio src=\"music.ogg\" autoplay></audio><p>Enjoy!</p>", null, _visitor_visitor_audio_skip);
        Assert.Contains("background music:", result.Content.ToString().ToLower());
        Assert.Contains("enjoy!", result.Content.ToString().ToLower());
        Assert.DoesNotContain("music.ogg", result.Content.ToString());
    }

    [Fact]
    public void Test_VisitorButtonCustom()
    {
        // Visitor replaces button with bracketed text
        var _visitor_visitor_button_custom = new VisitorButtonCustomVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>Confirm action: <button type=\"submit\">Click me</button> or <button type=\"reset\">Cancel</button></p>", null, _visitor_visitor_button_custom);
        Assert.Contains("[btn:click me]", result.Content.ToString().ToLower());
        Assert.Contains("[btn:cancel]", result.Content.ToString().ToLower());
        Assert.Contains("confirm action:", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_VisitorButtonSkip()
    {
        // Visitor removes all buttons from output
        var _visitor_visitor_button_skip = new VisitorButtonSkipVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>Actions available: <button>Save</button> <button>Delete</button> <button>Export</button></p>", null, _visitor_visitor_button_skip);
        Assert.Contains("actions available:", result.Content.ToString().ToLower());
        Assert.DoesNotContain("Save", result.Content.ToString());
        Assert.DoesNotContain("Delete", result.Content.ToString());
        Assert.DoesNotContain("Export", result.Content.ToString());
    }

    [Fact]
    public void Test_VisitorContinueDefault()
    {
        // Visitor continue action preserves default conversion
        var _visitor_visitor_continue_default = new VisitorContinueDefaultVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>Hello <strong>World</strong></p>", null, _visitor_visitor_continue_default);
        Assert.Contains("**world**", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_VisitorCustomBlockquote()
    {
        // Visitor replaces blockquote with custom format
        var _visitor_visitor_custom_blockquote = new VisitorCustomBlockquoteVisitor();
        var result = HtmlToMarkdownRs.Convert("<blockquote><p>A wise quote.</p></blockquote>", null, _visitor_visitor_custom_blockquote);
        Assert.Contains("quote:", result.Content.ToString().ToLower());
        Assert.Contains("a wise quote.", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_VisitorCustomEmphasis()
    {
        // Visitor replaces emphasis with custom output
        var _visitor_visitor_custom_emphasis = new VisitorCustomEmphasisVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>This is <em>important</em> text.</p>", null, _visitor_visitor_custom_emphasis);
        Assert.Contains(">>>important<<<", result.Content.ToString().ToLower());
        Assert.DoesNotContain("*important*", result.Content.ToString());
    }

    [Fact]
    public void Test_VisitorCustomHeading()
    {
        // Visitor replaces heading with custom format
        var _visitor_visitor_custom_heading = new VisitorCustomHeadingVisitor();
        var result = HtmlToMarkdownRs.Convert("<h2>Section Title</h2><p>Content below heading.</p>", null, _visitor_visitor_custom_heading);
        Assert.Contains("--- section title ---", result.Content.ToString().ToLower());
        Assert.DoesNotContain("## Section Title", result.Content.ToString());
        Assert.Contains("content below heading.", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_VisitorCustomImage()
    {
        // Visitor replaces image with custom output using template
        var _visitor_visitor_custom_image = new VisitorCustomImageVisitor();
        var result = HtmlToMarkdownRs.Convert("<img src=\"banner.png\" alt=\"Banner\">", null, _visitor_visitor_custom_image);
        Assert.Contains("[image: banner]", result.Content.ToString().ToLower());
        Assert.DoesNotContain("banner.png", result.Content.ToString());
    }

    [Fact]
    public void Test_VisitorCustomLinkFormat()
    {
        // Visitor reformats links using template interpolation
        var _visitor_visitor_custom_link_format = new VisitorCustomLinkFormatVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>Visit <a href=\"https://example.com\">Example</a> for more info.</p>", null, _visitor_visitor_custom_link_format);
        Assert.Contains("example (https://example.com)", result.Content.ToString().ToLower());
        Assert.DoesNotContain("[Example]", result.Content.ToString());
    }

    [Fact]
    public void Test_VisitorCustomLinkStatic()
    {
        // Visitor replaces link with static custom output
        var _visitor_visitor_custom_link_static = new VisitorCustomLinkStaticVisitor();
        var result = HtmlToMarkdownRs.Convert("<a href=\"https://example.com\">Click here</a>", null, _visitor_visitor_custom_link_static);
        Assert.Contains("[redacted link]", result.Content.ToString().ToLower());
        Assert.DoesNotContain("example.com", result.Content.ToString());
    }

    [Fact]
    public void Test_VisitorCustomOutput()
    {
        // Visitor custom action replaces element output
        var _visitor_visitor_custom_output = new VisitorCustomOutputVisitor();
        var result = HtmlToMarkdownRs.Convert("<h1>Original Heading</h1>", null, _visitor_visitor_custom_output);
        Assert.Contains("## replaced heading", result.Content.ToString().ToLower());
        Assert.DoesNotContain("# Original Heading", result.Content.ToString());
    }

    [Fact]
    public void Test_VisitorDefinitionListCustom()
    {
        // Visitor customizes definition list items
        var _visitor_visitor_definition_list_custom = new VisitorDefinitionListCustomVisitor();
        var result = HtmlToMarkdownRs.Convert("<dl><dt>HTML</dt><dd>HyperText Markup Language</dd><dt>CSS</dt><dd>Cascading Style Sheets</dd></dl>", null, _visitor_visitor_definition_list_custom);
        Assert.Contains("**html**", result.Content.ToString().ToLower());
        Assert.Contains("**css**", result.Content.ToString().ToLower());
        Assert.Contains("hypertext markup language", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_VisitorDefinitionListCustomFormat()
    {
        // Visitor formats definition lists with custom templates
        var _visitor_visitor_definition_list_custom_format = new VisitorDefinitionListCustomFormatVisitor();
        var result = HtmlToMarkdownRs.Convert("<dl><dt>Python</dt><dd>A high-level programming language</dd><dt>JavaScript</dt><dd>A scripting language for web browsers</dd></dl>", null, _visitor_visitor_definition_list_custom_format);
        Assert.Contains("### python", result.Content.ToString().ToLower());
        Assert.Contains("### javascript", result.Content.ToString().ToLower());
        Assert.Contains("> a high-level programming language", result.Content.ToString().ToLower());
        Assert.Contains("> a scripting language for web browsers", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_VisitorDefinitionListSkip()
    {
        // Visitor skips definition list items from output
        var _visitor_visitor_definition_list_skip = new VisitorDefinitionListSkipVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>Glossary:</p><dl><dt>Term A</dt><dd>Definition of term A</dd><dt>Term B</dt><dd>Definition of term B</dd></dl><p>End of glossary</p>", null, _visitor_visitor_definition_list_skip);
        Assert.Contains("glossary:", result.Content.ToString().ToLower());
        Assert.Contains("end of glossary", result.Content.ToString().ToLower());
        Assert.DoesNotContain("Term A", result.Content.ToString());
        Assert.DoesNotContain("Definition", result.Content.ToString());
    }

    [Fact]
    public void Test_VisitorDetailsSummaryCustom()
    {
        // Visitor customizes details/summary disclosure elements
        var _visitor_visitor_details_summary_custom = new VisitorDetailsSummaryCustomVisitor();
        var result = HtmlToMarkdownRs.Convert("<details><summary>Click to expand</summary><p>This content is initially hidden.</p><p>But can be revealed by the user.</p></details>", null, _visitor_visitor_details_summary_custom);
        Assert.Contains("[expandable] click to expand", result.Content.ToString().ToLower());
        Assert.Contains("this content is initially hidden.", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_VisitorDetailsSummarySkip()
    {
        // Visitor removes details/summary elements entirely
        var _visitor_visitor_details_summary_skip = new VisitorDetailsSummarySkipVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>Main content here.</p><details><summary>Hidden section</summary><p>Secret details</p></details><p>More main content.</p>", null, _visitor_visitor_details_summary_skip);
        Assert.Contains("main content here.", result.Content.ToString().ToLower());
        Assert.Contains("more main content.", result.Content.ToString().ToLower());
        Assert.DoesNotContain("Hidden section", result.Content.ToString());
        Assert.DoesNotContain("Secret details", result.Content.ToString());
    }

    [Fact]
    public void Test_VisitorFigureCustom()
    {
        // Visitor customizes figure and figcaption elements
        var _visitor_visitor_figure_custom = new VisitorFigureCustomVisitor();
        var result = HtmlToMarkdownRs.Convert("<article><h1>Article Title</h1><p>Introduction paragraph.</p><figure><img src=\"diagram.png\" alt=\"System architecture diagram\"><figcaption>Figure 1: System Architecture</figcaption></figure><p>Explanation of the figure.</p></article>", null, _visitor_visitor_figure_custom);
        Assert.Contains("article title", result.Content.ToString().ToLower());
        Assert.Contains("*figure 1: system architecture*", result.Content.ToString().ToLower());
        Assert.Contains("explanation of the figure.", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_VisitorFigureCustomWrap()
    {
        // Visitor wraps figure content with custom formatting
        var _visitor_visitor_figure_custom_wrap = new VisitorFigureCustomWrapVisitor();
        var result = HtmlToMarkdownRs.Convert("<section><h2>Gallery</h2><figure><img src=\"photo1.jpg\" alt=\"Photo\"><figcaption>Beautiful sunset</figcaption></figure></section>", null, _visitor_visitor_figure_custom_wrap);
        Assert.Contains("[figure]", result.Content.ToString().ToLower());
        Assert.Contains("[/figure]", result.Content.ToString().ToLower());
        Assert.Contains("gallery", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_VisitorFigureSkip()
    {
        // Visitor removes figure elements with their captions
        var _visitor_visitor_figure_skip = new VisitorFigureSkipVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>See the chart below:</p><figure><img src=\"chart.svg\"><figcaption>Revenue Trends 2020-2024</figcaption></figure><p>As shown in the chart above.</p>", null, _visitor_visitor_figure_skip);
        Assert.Contains("see the chart below:", result.Content.ToString().ToLower());
        Assert.Contains("as shown in the chart above.", result.Content.ToString().ToLower());
        Assert.DoesNotContain("Revenue Trends", result.Content.ToString());
        Assert.DoesNotContain("chart.svg", result.Content.ToString());
    }

    [Fact]
    public void Test_VisitorFormCustom()
    {
        // Visitor replaces form with custom output
        var _visitor_visitor_form_custom = new VisitorFormCustomVisitor();
        var result = HtmlToMarkdownRs.Convert("<div><form action=\"/submit\" method=\"POST\"><label>Name: <input type=\"text\" name=\"name\"></label><button type=\"submit\">Submit</button></form></div>", null, _visitor_visitor_form_custom);
        Assert.Contains("[form placeholder]", result.Content.ToString().ToLower());
        Assert.DoesNotContain("submit", result.Content.ToString());
        Assert.DoesNotContain("input", result.Content.ToString());
    }

    [Fact]
    public void Test_VisitorFormSkip()
    {
        // Visitor skips form elements entirely
        var _visitor_visitor_form_skip = new VisitorFormSkipVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>Before form</p><form><input type=\"email\" name=\"email\"></form><p>After form</p>", null, _visitor_visitor_form_skip);
        Assert.Contains("before form", result.Content.ToString().ToLower());
        Assert.Contains("after form", result.Content.ToString().ToLower());
        Assert.DoesNotContain("email", result.Content.ToString());
    }

    [Fact]
    public void Test_VisitorHorizontalRuleCustom()
    {
        // Visitor replaces horizontal rule with custom output
        var _visitor_visitor_horizontal_rule_custom = new VisitorHorizontalRuleCustomVisitor();
        var result = HtmlToMarkdownRs.Convert("<h1>Section A</h1><p>Content A</p><hr><h1>Section B</h1><p>Content B</p>", null, _visitor_visitor_horizontal_rule_custom);
        Assert.Contains("[divider]", result.Content.ToString().ToLower());
        Assert.Contains("section a", result.Content.ToString().ToLower());
        Assert.Contains("section b", result.Content.ToString().ToLower());
        Assert.DoesNotContain("---", result.Content.ToString());
    }

    [Fact]
    public void Test_VisitorHorizontalRuleSkip()
    {
        // Visitor removes all horizontal rules
        var _visitor_visitor_horizontal_rule_skip = new VisitorHorizontalRuleSkipVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>Part 1</p><hr><p>Part 2</p><hr><p>Part 3</p>", null, _visitor_visitor_horizontal_rule_skip);
        Assert.Contains("part 1", result.Content.ToString().ToLower());
        Assert.Contains("part 2", result.Content.ToString().ToLower());
        Assert.Contains("part 3", result.Content.ToString().ToLower());
        Assert.DoesNotContain("---", result.Content.ToString());
    }

    [Fact]
    public void Test_VisitorIframeCustom()
    {
        // Visitor replaces embedded iframe with custom text
        var _visitor_visitor_iframe_custom = new VisitorIframeCustomVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>Embedded map:</p><iframe src=\"https://maps.example.com/embed\" width=\"400\" height=\"300\"></iframe><p>End of map</p>", null, _visitor_visitor_iframe_custom);
        Assert.Contains("[embedded: https://maps.example.com/embed]", result.Content.ToString().ToLower());
        Assert.Contains("embedded map:", result.Content.ToString().ToLower());
        Assert.Contains("end of map", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_VisitorIframeSkip()
    {
        // Visitor removes embedded iframes
        var _visitor_visitor_iframe_skip = new VisitorIframeSkipVisitor();
        var result = HtmlToMarkdownRs.Convert("<h3>Reviews</h3><iframe src=\"https://widget.example.com/reviews\"></iframe><p>See reviews from our partners.</p>", null, _visitor_visitor_iframe_skip);
        Assert.Contains("reviews", result.Content.ToString().ToLower());
        Assert.Contains("see reviews from our partners.", result.Content.ToString().ToLower());
        Assert.DoesNotContain("widget.example.com", result.Content.ToString());
    }

    [Fact]
    public void Test_VisitorInputCustom()
    {
        // Visitor replaces input with labeled output
        var _visitor_visitor_input_custom = new VisitorInputCustomVisitor();
        var result = HtmlToMarkdownRs.Convert("<form><label>Username: <input type=\"text\" name=\"username\" value=\"\"></label><label>Password: <input type=\"password\" name=\"password\"></label></form>", null, _visitor_visitor_input_custom);
        Assert.Contains("[input:text]", result.Content.ToString().ToLower());
        Assert.Contains("[input:password]", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_VisitorInputSkip()
    {
        // Visitor skips all input elements
        var _visitor_visitor_input_skip = new VisitorInputSkipVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>Sign up:</p><input type=\"text\" name=\"email\" placeholder=\"your@email.com\"><input type=\"checkbox\" name=\"agree\"><p>Continue</p>", null, _visitor_visitor_input_skip);
        Assert.Contains("sign up:", result.Content.ToString().ToLower());
        Assert.Contains("continue", result.Content.ToString().ToLower());
        Assert.DoesNotContain("email", result.Content.ToString());
    }

    [Fact]
    public void Test_VisitorLineBreakCustom()
    {
        // Visitor replaces line break with custom output
        var _visitor_visitor_line_break_custom = new VisitorLineBreakCustomVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>First line<br>Second line<br>Third line</p>", null, _visitor_visitor_line_break_custom);
        Assert.Contains("first line | second line | third line", result.Content.ToString().ToLower());
        Assert.DoesNotContain("\n\n", result.Content.ToString());
    }

    [Fact]
    public void Test_VisitorLineBreakSkip()
    {
        // Visitor removes all line breaks
        var _visitor_visitor_line_break_skip = new VisitorLineBreakSkipVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>Address Line 1<br>Address Line 2<br>Address Line 3</p>", null, _visitor_visitor_line_break_skip);
        Assert.Contains("address line 1address line 2address line 3", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_VisitorMarkCustom()
    {
        // Visitor replaces highlight/mark with custom template
        var _visitor_visitor_mark_custom = new VisitorMarkCustomVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>This is a <mark>highlighted passage</mark> in the text.</p>", null, _visitor_visitor_mark_custom);
        Assert.Contains("==highlighted passage==", result.Content.ToString().ToLower());
        Assert.Contains("this is a", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_VisitorMarkSkip()
    {
        // Visitor skips mark elements entirely
        var _visitor_visitor_mark_skip = new VisitorMarkSkipVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>Key insight: <mark>always validate input</mark> for security.</p>", null, _visitor_visitor_mark_skip);
        Assert.DoesNotContain("always validate input", result.Content.ToString());
        Assert.Contains("key insight:", result.Content.ToString().ToLower());
        Assert.Contains("for security.", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_VisitorPreserveHtml()
    {
        // Visitor preserve_html action includes raw HTML in output
        var _visitor_visitor_preserve_html = new VisitorPreserveHtmlVisitor();
        var result = HtmlToMarkdownRs.Convert("<div><custom-tag>Custom content</custom-tag></div>", null, _visitor_visitor_preserve_html);
        Assert.Contains("<custom-tag>", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_VisitorSkipAllHeadings()
    {
        // Visitor skips all headings from document
        var _visitor_visitor_skip_all_headings = new VisitorSkipAllHeadingsVisitor();
        var result = HtmlToMarkdownRs.Convert("<h1>Title</h1><p>Body text remains.</p>", null, _visitor_visitor_skip_all_headings);
        Assert.DoesNotContain("Title", result.Content.ToString());
        Assert.Contains("body text remains.", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_VisitorSkipCodeBlocks()
    {
        // Visitor skips code blocks from output
        var _visitor_visitor_skip_code_blocks = new VisitorSkipCodeBlocksVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>Intro text</p><pre><code>let x = 42;</code></pre><p>Outro text</p>", null, _visitor_visitor_skip_code_blocks);
        Assert.Contains("intro text", result.Content.ToString().ToLower());
        Assert.Contains("outro text", result.Content.ToString().ToLower());
        Assert.DoesNotContain("let x = 42", result.Content.ToString());
    }

    [Fact]
    public void Test_VisitorSkipHeading()
    {
        // Visitor skip action omits all headings from output
        var _visitor_visitor_skip_heading = new VisitorSkipHeadingVisitor();
        var result = HtmlToMarkdownRs.Convert("<h1>Title</h1><p>Body text remains.</p>", null, _visitor_visitor_skip_heading);
        Assert.DoesNotContain("Title", result.Content.ToString());
        Assert.Contains("body text remains.", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_VisitorSkipImages()
    {
        // Visitor skips all images from output
        var _visitor_visitor_skip_images = new VisitorSkipImagesVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>Before image</p><img src=\"photo.jpg\" alt=\"A photo\"><p>After image</p>", null, _visitor_visitor_skip_images);
        Assert.Contains("before image", result.Content.ToString().ToLower());
        Assert.Contains("after image", result.Content.ToString().ToLower());
        Assert.DoesNotContain("photo.jpg", result.Content.ToString());
        Assert.DoesNotContain("A photo", result.Content.ToString());
    }

    [Fact]
    public void Test_VisitorSkipLinks()
    {
        // Visitor skips all links entirely
        var _visitor_visitor_skip_links = new VisitorSkipLinksVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>Before <a href=\"https://example.com\">link text</a> after</p>", null, _visitor_visitor_skip_links);
        Assert.DoesNotContain("link text", result.Content.ToString());
        Assert.DoesNotContain("example.com", result.Content.ToString());
    }

    [Fact]
    public void Test_VisitorSkipStrong()
    {
        // Visitor skips bold/strong elements
        var _visitor_visitor_skip_strong = new VisitorSkipStrongVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>Normal <strong>bold text</strong> normal</p>", null, _visitor_visitor_skip_strong);
        Assert.DoesNotContain("bold text", result.Content.ToString());
        Assert.Contains("normal", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_VisitorSubscriptCustom()
    {
        // Visitor replaces subscript with custom template
        var _visitor_visitor_subscript_custom = new VisitorSubscriptCustomVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>H<sub>2</sub>O is water.</p>", null, _visitor_visitor_subscript_custom);
        Assert.Contains("h~2~o", result.Content.ToString().ToLower());
        Assert.Contains("is water", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_VisitorSubscriptSkip()
    {
        // Visitor skips subscript elements entirely
        var _visitor_visitor_subscript_skip = new VisitorSubscriptSkipVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>The formula C<sub>12</sub>H<sub>22</sub>O<sub>11</sub> is sugar.</p>", null, _visitor_visitor_subscript_skip);
        Assert.Contains("the formula cho is sugar.", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_VisitorSuperscriptCustom()
    {
        // Visitor replaces superscript with custom template
        var _visitor_visitor_superscript_custom = new VisitorSuperscriptCustomVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>Einstein's E=mc<sup>2</sup> revolutionized physics.</p>", null, _visitor_visitor_superscript_custom);
        Assert.Contains("e=mc^2^", result.Content.ToString().ToLower());
        Assert.Contains("revolutionized physics", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_VisitorSuperscriptSkip()
    {
        // Visitor skips superscript from output
        var _visitor_visitor_superscript_skip = new VisitorSuperscriptSkipVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>The equation x<sup>3</sup> + y<sup>3</sup> = z<sup>3</sup> has no solutions.</p>", null, _visitor_visitor_superscript_skip);
        Assert.Contains("the equation x + y = z has no solutions.", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_VisitorUnderlineCustom()
    {
        // Visitor replaces underline with custom markup
        var _visitor_visitor_underline_custom = new VisitorUnderlineCustomVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>This is <u>very important</u> text.</p>", null, _visitor_visitor_underline_custom);
        Assert.Contains("_very important_", result.Content.ToString().ToLower());
        Assert.DoesNotContain("**", result.Content.ToString());
    }

    [Fact]
    public void Test_VisitorUnderlineSkip()
    {
        // Visitor skips underline elements from output
        var _visitor_visitor_underline_skip = new VisitorUnderlineSkipVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>Normal text with <u>underlined part</u> and more text.</p>", null, _visitor_visitor_underline_skip);
        Assert.Contains("normal text with", result.Content.ToString().ToLower());
        Assert.Contains("and more text.", result.Content.ToString().ToLower());
        Assert.DoesNotContain("underlined part", result.Content.ToString());
    }

    [Fact]
    public void Test_VisitorVideoCustom()
    {
        // Visitor replaces video with custom link
        var _visitor_visitor_video_custom = new VisitorVideoCustomVisitor();
        var result = HtmlToMarkdownRs.Convert("<p>Watch our tutorial:</p><video src=\"tutorial.mp4\" width=\"320\" height=\"240\" controls></video><p>Great content!</p>", null, _visitor_visitor_video_custom);
        Assert.Contains("[video: tutorial.mp4]", result.Content.ToString().ToLower());
        Assert.Contains("watch our tutorial:", result.Content.ToString().ToLower());
        Assert.Contains("great content!", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_VisitorVideoSkip()
    {
        // Visitor removes video elements entirely
        var _visitor_visitor_video_skip = new VisitorVideoSkipVisitor();
        var result = HtmlToMarkdownRs.Convert("<h2>Demo</h2><video src=\"demo.webm\"></video><p>See the demo above.</p>", null, _visitor_visitor_video_skip);
        Assert.Contains("demo", result.Content.ToString().ToLower());
        Assert.Contains("see the demo above.", result.Content.ToString().ToLower());
        Assert.DoesNotContain("demo.webm", result.Content.ToString());
    }

    private sealed class VisitorAudioCustomVisitor : IVisitor
    {
        public VisitResult VisitAudio(VisitContext ctx, string src)
        {
            return VisitResult.Custom("[AUDIO: podcast.mp3]");
        }
    }


    private sealed class VisitorAudioSkipVisitor : IVisitor
    {
        public VisitResult VisitAudio(VisitContext ctx, string src)
        {
            return VisitResult.Skip();
        }
    }


    private sealed class VisitorButtonCustomVisitor : IVisitor
    {
        public VisitResult VisitButton(VisitContext ctx, string text)
        {
            return VisitResult.Custom($"[BTN:{text}]");
        }
    }


    private sealed class VisitorButtonSkipVisitor : IVisitor
    {
        public VisitResult VisitButton(VisitContext ctx, string text)
        {
            return VisitResult.Skip();
        }
    }


    private sealed class VisitorContinueDefaultVisitor : IVisitor
    {
        public VisitResult VisitStrong(VisitContext ctx, string text)
        {
            return VisitResult.Continue();
        }
    }


    private sealed class VisitorCustomBlockquoteVisitor : IVisitor
    {
        public VisitResult VisitBlockquote(VisitContext ctx, string content, int depth)
        {
            return VisitResult.Custom($"QUOTE: \"{content}\"");
        }
    }


    private sealed class VisitorCustomEmphasisVisitor : IVisitor
    {
        public VisitResult VisitEmphasis(VisitContext ctx, string text)
        {
            return VisitResult.Custom($">>>{text}<<<");
        }
    }


    private sealed class VisitorCustomHeadingVisitor : IVisitor
    {
        public VisitResult VisitHeading(VisitContext ctx, int level, string text, string id)
        {
            return VisitResult.Custom($"--- {text} ---");
        }
    }


    private sealed class VisitorCustomImageVisitor : IVisitor
    {
        public VisitResult VisitImage(VisitContext ctx, string src, string alt, string title)
        {
            return VisitResult.Custom($"[Image: {alt}]");
        }
    }


    private sealed class VisitorCustomLinkFormatVisitor : IVisitor
    {
        public VisitResult VisitLink(VisitContext ctx, string href, string text, string title)
        {
            return VisitResult.Custom($"{text} ({href})");
        }
    }


    private sealed class VisitorCustomLinkStaticVisitor : IVisitor
    {
        public VisitResult VisitLink(VisitContext ctx, string href, string text, string title)
        {
            return VisitResult.Custom("[REDACTED LINK]");
        }
    }


    private sealed class VisitorCustomOutputVisitor : IVisitor
    {
        public VisitResult VisitHeading(VisitContext ctx, int level, string text, string id)
        {
            return VisitResult.Custom("## REPLACED HEADING");
        }
    }


    private sealed class VisitorDefinitionListCustomVisitor : IVisitor
    {
        public VisitResult VisitDefinitionTerm(VisitContext ctx, string text)
        {
            return VisitResult.Custom($"**{text}**");
        }
    }


    private sealed class VisitorDefinitionListCustomFormatVisitor : IVisitor
    {
        public VisitResult VisitDefinitionTerm(VisitContext ctx, string text)
        {
            return VisitResult.Custom($"### {text}");
        }
        public VisitResult VisitDefinitionDescription(VisitContext ctx, string text)
        {
            return VisitResult.Custom($"> {text}");
        }
    }


    private sealed class VisitorDefinitionListSkipVisitor : IVisitor
    {
        public VisitResult VisitDefinitionDescription(VisitContext ctx, string text)
        {
            return VisitResult.Skip();
        }
        public VisitResult VisitDefinitionTerm(VisitContext ctx, string text)
        {
            return VisitResult.Skip();
        }
    }


    private sealed class VisitorDetailsSummaryCustomVisitor : IVisitor
    {
        public VisitResult VisitSummary(VisitContext ctx, string text)
        {
            return VisitResult.Custom($"[EXPANDABLE] {text}");
        }
    }


    private sealed class VisitorDetailsSummarySkipVisitor : IVisitor
    {
        public VisitResult VisitDetails(VisitContext ctx, bool isOpen)
        {
            return VisitResult.Skip();
        }
    }


    private sealed class VisitorFigureCustomVisitor : IVisitor
    {
        public VisitResult VisitFigcaption(VisitContext ctx, string text)
        {
            return VisitResult.Custom($"*{text}*");
        }
    }


    private sealed class VisitorFigureCustomWrapVisitor : IVisitor
    {
        public VisitResult VisitFigureStart(VisitContext ctx)
        {
            return VisitResult.Custom("\n[FIGURE]\n");
        }
        public VisitResult VisitFigureEnd(VisitContext ctx, string output)
        {
            return VisitResult.Custom($"{output}\n[/FIGURE]\n");
        }
    }


    private sealed class VisitorFigureSkipVisitor : IVisitor
    {
        public VisitResult VisitFigureStart(VisitContext ctx)
        {
            return VisitResult.Skip();
        }
    }


    private sealed class VisitorFormCustomVisitor : IVisitor
    {
        public VisitResult VisitForm(VisitContext ctx, string actionUrl, string method)
        {
            return VisitResult.Custom("[FORM PLACEHOLDER]");
        }
    }


    private sealed class VisitorFormSkipVisitor : IVisitor
    {
        public VisitResult VisitForm(VisitContext ctx, string actionUrl, string method)
        {
            return VisitResult.Skip();
        }
    }


    private sealed class VisitorHorizontalRuleCustomVisitor : IVisitor
    {
        public VisitResult VisitHorizontalRule(VisitContext ctx)
        {
            return VisitResult.Custom("\n[DIVIDER]\n");
        }
    }


    private sealed class VisitorHorizontalRuleSkipVisitor : IVisitor
    {
        public VisitResult VisitHorizontalRule(VisitContext ctx)
        {
            return VisitResult.Skip();
        }
    }


    private sealed class VisitorIframeCustomVisitor : IVisitor
    {
        public VisitResult VisitIframe(VisitContext ctx, string src)
        {
            return VisitResult.Custom("[EMBEDDED: https://maps.example.com/embed]");
        }
    }


    private sealed class VisitorIframeSkipVisitor : IVisitor
    {
        public VisitResult VisitIframe(VisitContext ctx, string src)
        {
            return VisitResult.Skip();
        }
    }


    private sealed class VisitorInputCustomVisitor : IVisitor
    {
        public VisitResult VisitInput(VisitContext ctx, string inputType, string name, string value)
        {
            return VisitResult.Custom($"[INPUT:{input_type}]");
        }
    }


    private sealed class VisitorInputSkipVisitor : IVisitor
    {
        public VisitResult VisitInput(VisitContext ctx, string inputType, string name, string value)
        {
            return VisitResult.Skip();
        }
    }


    private sealed class VisitorLineBreakCustomVisitor : IVisitor
    {
        public VisitResult VisitLineBreak(VisitContext ctx)
        {
            return VisitResult.Custom(" | ");
        }
    }


    private sealed class VisitorLineBreakSkipVisitor : IVisitor
    {
        public VisitResult VisitLineBreak(VisitContext ctx)
        {
            return VisitResult.Skip();
        }
    }


    private sealed class VisitorMarkCustomVisitor : IVisitor
    {
        public VisitResult VisitMark(VisitContext ctx, string text)
        {
            return VisitResult.Custom($"=={text}==");
        }
    }


    private sealed class VisitorMarkSkipVisitor : IVisitor
    {
        public VisitResult VisitMark(VisitContext ctx, string text)
        {
            return VisitResult.Skip();
        }
    }


    private sealed class VisitorPreserveHtmlVisitor : IVisitor
    {
        public VisitResult VisitCustomElement(VisitContext ctx, string tagName, string html)
        {
            return VisitResult.PreserveHtml();
        }
    }


    private sealed class VisitorSkipAllHeadingsVisitor : IVisitor
    {
        public VisitResult VisitHeading(VisitContext ctx, int level, string text, string id)
        {
            return VisitResult.Skip();
        }
    }


    private sealed class VisitorSkipCodeBlocksVisitor : IVisitor
    {
        public VisitResult VisitCodeBlock(VisitContext ctx, string lang, string code)
        {
            return VisitResult.Skip();
        }
    }


    private sealed class VisitorSkipHeadingVisitor : IVisitor
    {
        public VisitResult VisitHeading(VisitContext ctx, int level, string text, string id)
        {
            return VisitResult.Skip();
        }
    }


    private sealed class VisitorSkipImagesVisitor : IVisitor
    {
        public VisitResult VisitImage(VisitContext ctx, string src, string alt, string title)
        {
            return VisitResult.Skip();
        }
    }


    private sealed class VisitorSkipLinksVisitor : IVisitor
    {
        public VisitResult VisitLink(VisitContext ctx, string href, string text, string title)
        {
            return VisitResult.Skip();
        }
    }


    private sealed class VisitorSkipStrongVisitor : IVisitor
    {
        public VisitResult VisitStrong(VisitContext ctx, string text)
        {
            return VisitResult.Skip();
        }
    }


    private sealed class VisitorSubscriptCustomVisitor : IVisitor
    {
        public VisitResult VisitSubscript(VisitContext ctx, string text)
        {
            return VisitResult.Custom($"~{text}~");
        }
    }


    private sealed class VisitorSubscriptSkipVisitor : IVisitor
    {
        public VisitResult VisitSubscript(VisitContext ctx, string text)
        {
            return VisitResult.Skip();
        }
    }


    private sealed class VisitorSuperscriptCustomVisitor : IVisitor
    {
        public VisitResult VisitSuperscript(VisitContext ctx, string text)
        {
            return VisitResult.Custom($"^{text}^");
        }
    }


    private sealed class VisitorSuperscriptSkipVisitor : IVisitor
    {
        public VisitResult VisitSuperscript(VisitContext ctx, string text)
        {
            return VisitResult.Skip();
        }
    }


    private sealed class VisitorUnderlineCustomVisitor : IVisitor
    {
        public VisitResult VisitUnderline(VisitContext ctx, string text)
        {
            return VisitResult.Custom($"_{text}_");
        }
    }


    private sealed class VisitorUnderlineSkipVisitor : IVisitor
    {
        public VisitResult VisitUnderline(VisitContext ctx, string text)
        {
            return VisitResult.Skip();
        }
    }


    private sealed class VisitorVideoCustomVisitor : IVisitor
    {
        public VisitResult VisitVideo(VisitContext ctx, string src)
        {
            return VisitResult.Custom($"[VIDEO: {src}]");
        }
    }


    private sealed class VisitorVideoSkipVisitor : IVisitor
    {
        public VisitResult VisitVideo(VisitContext ctx, string src)
        {
            return VisitResult.Skip();
        }
    }

}
