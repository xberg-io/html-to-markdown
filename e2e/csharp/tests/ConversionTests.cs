// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:9dc5eb49b7ca7c6d97861d849e96f59bd3f7c5c9fa0a0d6208f347bf3157e7e0
// To regenerate: alef generate
// To verify freshness: alef verify

using System;
using System.Collections.Generic;
using System.Linq;
using System.Net.Http;
using System.Text;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading.Tasks;
using Xunit;
using HtmlToMarkdown;
using static HtmlToMarkdown.HtmlToMarkdownConverter;

namespace HtmlToMarkdown
{
    /// <summary>E2e tests for category: conversion.</summary>
    public class ConversionTests
    {
            private static readonly JsonSerializerOptions ConfigOptions = new() { Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) }, DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault };

        [Fact]
        public void Test_AdjacentCodeSpansMerged()
        {
            // Adjacent code spans merge instead of colliding backticks
            var result = HtmlToMarkdownConverter.Convert("<p><code>A</code><code>B</code></p>", new ConversionOptions());
            Assert.Equal("`AB`\n", result.Content);

        }

        [Fact]
        public void Test_AdjacentEmphasisMerged()
        {
            // Adjacent emphasis elements share one delimiter run
            var result = HtmlToMarkdownConverter.Convert("<p><i>A</i><i>B</i><i>C</i></p>", new ConversionOptions());
            Assert.Equal("*ABC*\n", result.Content);

        }

        [Fact]
        public void Test_AdjacentInsertedMerged()
        {
            // Adjacent inserted elements share one delimiter run
            var result = HtmlToMarkdownConverter.Convert("<p><ins>A</ins><ins>B</ins></p>", new ConversionOptions());
            Assert.Equal("==AB==\n", result.Content);

        }

        [Fact]
        public void Test_AdjacentStrikethroughMerged()
        {
            // Adjacent strikethrough elements share one delimiter run
            var result = HtmlToMarkdownConverter.Convert("<p><del>A</del><del>B</del></p>", new ConversionOptions());
            Assert.Equal("~~AB~~\n", result.Content);

        }

        [Fact]
        public void Test_AdjacentVariableMerged()
        {
            // Adjacent variable elements share one delimiter run
            var result = HtmlToMarkdownConverter.Convert("<p><var>A</var><var>B</var></p>", new ConversionOptions());
            Assert.Equal("*AB*\n", result.Content);

        }

        [Fact]
        public void Test_BlockquoteCodeBlockIndentationPreserved()
        {
            // Code block inside a blockquote keeps its line indentation
            var result = HtmlToMarkdownConverter.Convert("<blockquote><pre><code>line1\n    line2 indented</code></pre></blockquote>", new ConversionOptions());
            Assert.Equal("> ```\n> line1\n>     line2 indented\n> ```\n", result.Content);

        }

        [Fact]
        public void Test_BlockquoteMultipleParagraphs()
        {
            // Blockquote with multiple paragraphs has each paragraph prefixed
            var result = HtmlToMarkdownConverter.Convert("<blockquote><p>First paragraph.</p><p>Second paragraph.</p></blockquote>", new ConversionOptions());
            Assert.Contains("> First paragraph.", result.Content.ToString());
            Assert.Contains("> Second paragraph.", result.Content.ToString());

        }

        [Fact]
        public void Test_BlockquoteNested()
        {
            // Nested blockquote produces double-prefixed lines
            var result = HtmlToMarkdownConverter.Convert("<blockquote><p>Outer quote.</p><blockquote><p>Inner quote.</p></blockquote></blockquote>", new ConversionOptions());
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Outer quote.", result.Content.ToString());
            Assert.Contains("Inner quote.", result.Content.ToString());

        }

        [Fact]
        public void Test_BlockquoteNestedListIndentationPreserved()
        {
            // Nested list inside a blockquote keeps its continuation indentation
            var result = HtmlToMarkdownConverter.Convert("<blockquote><ul><li>item a<ul><li>sub a1</li></ul></li></ul></blockquote>", new ConversionOptions());
            Assert.Equal("> - item a\n>   * sub a1\n", result.Content);

        }

        [Fact]
        public void Test_BlockquoteSimple()
        {
            // Simple blockquote
            var result = HtmlToMarkdownConverter.Convert("<blockquote><p>Quote text</p></blockquote>", new ConversionOptions());
            Assert.Contains("> Quote text", result.Content.ToString());

        }

        [Fact]
        public void Test_BlockquoteTextThenParagraphGetsBlankLine()
        {
            // Bare text followed by a paragraph inside a blockquote gets a blank-line separator instead of merging into one line
            var result = HtmlToMarkdownConverter.Convert("<blockquote>Just text, then <p>a paragraph</p></blockquote>", new ConversionOptions());
            Assert.Equal("> Just text, then\n>\n> a paragraph\n", result.Content);

        }

        [Fact]
        public void Test_BlockquoteWithList()
        {
            // Blockquote containing a list preserves list items inside quote
            var result = HtmlToMarkdownConverter.Convert("<blockquote><p>Quote intro:</p><ul><li>Point one</li><li>Point two</li></ul></blockquote>", new ConversionOptions());
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Quote intro:", result.Content.ToString());
            Assert.Contains("Point one", result.Content.ToString());
            Assert.Contains("Point two", result.Content.ToString());

        }

        [Fact]
        public void Test_BoldAndItalic()
        {
            // Nested bold and italic
            var result = HtmlToMarkdownConverter.Convert("<p><strong><em>both</em></strong></p>", new ConversionOptions());
            Assert.Contains("***both***", result.Content.ToString());

        }

        [Fact]
        public void Test_BoldStrong()
        {
            // Strong tag converts to bold
            var result = HtmlToMarkdownConverter.Convert("<p><strong>bold</strong></p>", new ConversionOptions());
            Assert.Contains("**bold**", result.Content.ToString());

        }

        [Fact]
        public void Test_CodeBlock()
        {
            // Code block with language preserves content
            var result = HtmlToMarkdownConverter.Convert("<pre><code class=\"language-python\">print('hello')</code></pre>", new ConversionOptions());
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("print('hello')", result.Content.ToString());

        }

        [Fact]
        public void Test_CodeBlockNoLanguage()
        {
            // Code block without a language class preserves content
            var result = HtmlToMarkdownConverter.Convert("<pre><code>plain code here</code></pre>", new ConversionOptions());
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("plain code here", result.Content.ToString());

        }

        [Fact]
        public void Test_CodeInlineInParagraph()
        {
            // Inline code element nested inside a paragraph
            var result = HtmlToMarkdownConverter.Convert("<p>Call the <code>initialize()</code> method first.</p>", new ConversionOptions());
            Assert.Contains("`initialize()`", result.Content.ToString());

        }

        [Fact]
        public void Test_CodeSpanSuppressesNestedInlineMarkers()
        {
            // Inline markers inside a code span stay literal content
            var result = HtmlToMarkdownConverter.Convert("<p><code>a<em>b</em><ins>c</ins></code></p>", new ConversionOptions());
            Assert.Equal("`abc`\n", result.Content);

        }

        [Fact]
        public void Test_CodeWithBackticksInContent()
        {
            // Inline code containing backtick characters is properly escaped
            var result = HtmlToMarkdownConverter.Convert("<p>Use <code>`backtick` here</code> carefully.</p>", new ConversionOptions());
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("backtick", result.Content.ToString());

        }

        [Fact]
        public void Test_ConversionAutolinkFilenameNotAutolinked()
        {
            // Bare filename href (no URI scheme) renders as inline link, not autolink (#397)
            var result = HtmlToMarkdownConverter.Convert("<a href=\"foobar.png\">foobar.png</a>", new ConversionOptions());
            Assert.Equal("[foobar.png](foobar.png)\n", result.Content);

        }

        [Fact]
        public void Test_ConversionAutolinkHttpsUrl()
        {
            // Absolute https URL with matching text renders as autolink (#397)
            var result = HtmlToMarkdownConverter.Convert("<a href=\"https://example.com\">https://example.com</a>", new ConversionOptions());
            Assert.Equal("<https://example.com>\n", result.Content);

        }

        [Fact]
        public void Test_ConversionAutolinkMailto()
        {
            // mailto: link with matching email text renders as autolink (#397)
            var result = HtmlToMarkdownConverter.Convert("<a href=\"mailto:a@b.com\">a@b.com</a>", new ConversionOptions());
            Assert.Equal("<a@b.com>\n", result.Content);

        }

        [Fact]
        public void Test_ConversionAutolinkMixedFilenameAndUrl()
        {
            // Mixed filename + URL: only the URL becomes an autolink (#397)
            var result = HtmlToMarkdownConverter.Convert("<a href=\"foobar.png\">foobar.png</a> <a href=\"https://www.heise.de\">https://www.heise.de</a>", new ConversionOptions());
            Assert.Equal("[foobar.png](foobar.png) <https://www.heise.de>\n", result.Content);

        }

        [Fact]
        public void Test_ConversionAutolinkRelativePathNotAutolinked()
        {
            // Relative path href renders as inline link (#397)
            var result = HtmlToMarkdownConverter.Convert("<a href=\"/docs/intro.html\">/docs/intro.html</a>", new ConversionOptions());
            Assert.Equal("[/docs/intro.html](/docs/intro.html)\n", result.Content);

        }

        [Fact]
        public void Test_EmphasisMarkHighlight()
        {
            // mark tag produces highlighted output
            var result = HtmlToMarkdownConverter.Convert("<p><mark>highlighted</mark></p>", new ConversionOptions());
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("highlighted", result.Content.ToString());

        }

        [Fact]
        public void Test_EmphasisStrikethroughDel()
        {
            // del tag converts to GFM strikethrough
            var result = HtmlToMarkdownConverter.Convert("<p><del>deleted text</del></p>", new ConversionOptions());
            Assert.Contains("~~deleted text~~", result.Content.ToString());

        }

        [Fact]
        public void Test_EmphasisStrikethroughS()
        {
            // s tag converts to GFM strikethrough
            var result = HtmlToMarkdownConverter.Convert("<p><s>strikethrough</s></p>", new ConversionOptions());
            Assert.Contains("~~strikethrough~~", result.Content.ToString());

        }

        [Fact]
        public void Test_EmphasisSubscript()
        {
            // sub tag content is preserved
            var result = HtmlToMarkdownConverter.Convert("<p>H<sub>2</sub>O</p>", new ConversionOptions());
            Assert.Contains("H", result.Content.ToString());
            Assert.Contains("2", result.Content.ToString());
            Assert.Contains("O", result.Content.ToString());

        }

        [Fact]
        public void Test_EmphasisSuperscript()
        {
            // sup tag content is preserved
            var result = HtmlToMarkdownConverter.Convert("<p>x<sup>2</sup></p>", new ConversionOptions());
            Assert.Contains("x", result.Content.ToString());
            Assert.Contains("2", result.Content.ToString());

        }

        [Fact]
        public void Test_EmphasisUnderlineU()
        {
            // u tag content is preserved in output
            var result = HtmlToMarkdownConverter.Convert("<p><u>underlined</u></p>", new ConversionOptions());
            Assert.Contains("underlined", result.Content.ToString());

        }

        [Fact]
        public void Test_FormInputElements()
        {
            // Form input elements produce readable output without form mechanics
            var result = HtmlToMarkdownConverter.Convert("<form><label for=\"name\">Name:</label><input type=\"text\" id=\"name\" placeholder=\"Enter name\"></form>", new ConversionOptions { Preprocessing = new PreprocessingOptions { RemoveForms = false } });
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Name", result.Content.ToString());

        }

        [Fact]
        public void Test_FormSelectOptions()
        {
            // Select element with options produces readable output
            var result = HtmlToMarkdownConverter.Convert("<form><label>Color:</label><select><option value=\"red\">Red</option><option value=\"blue\" selected>Blue</option><option value=\"green\">Green</option></select></form>", new ConversionOptions { Preprocessing = new PreprocessingOptions { RemoveForms = false } });
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Color", result.Content.ToString());

        }

        [Fact]
        public void Test_FormTextarea()
        {
            // Textarea element produces readable output
            var result = HtmlToMarkdownConverter.Convert("<form><label>Message:</label><textarea>Default text content</textarea></form>", new ConversionOptions { Preprocessing = new PreprocessingOptions { RemoveForms = false } });
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Message", result.Content.ToString());

        }

        [Fact]
        public void Test_HeadingH1()
        {
            // H1 heading
            var result = HtmlToMarkdownConverter.Convert("<h1>Heading 1</h1>", new ConversionOptions());
            Assert.Equal("# Heading 1\n", result.Content);

        }

        [Fact]
        public void Test_HeadingH2()
        {
            // H2 heading
            var result = HtmlToMarkdownConverter.Convert("<h2>Heading 2</h2>", new ConversionOptions());
            Assert.Equal("## Heading 2\n", result.Content);

        }

        [Fact]
        public void Test_HeadingH3()
        {
            // H3 heading
            var result = HtmlToMarkdownConverter.Convert("<h3>Heading 3</h3>", new ConversionOptions());
            Assert.Equal("### Heading 3\n", result.Content);

        }

        [Fact]
        public void Test_HeadingH4()
        {
            // H4 heading
            var result = HtmlToMarkdownConverter.Convert("<h4>Heading 4</h4>", new ConversionOptions());
            Assert.Equal("#### Heading 4\n", result.Content);

        }

        [Fact]
        public void Test_HeadingH5()
        {
            // H5 heading
            var result = HtmlToMarkdownConverter.Convert("<h5>Heading 5</h5>", new ConversionOptions());
            Assert.Equal("##### Heading 5\n", result.Content);

        }

        [Fact]
        public void Test_HeadingH6()
        {
            // H6 heading
            var result = HtmlToMarkdownConverter.Convert("<h6>Heading 6</h6>", new ConversionOptions());
            Assert.Equal("###### Heading 6\n", result.Content);

        }

        [Fact]
        public void Test_ImageFigureFigcaption()
        {
            // Figure with figcaption preserves both image and caption
            var result = HtmlToMarkdownConverter.Convert("<figure><img src=\"sunset.jpg\" alt=\"A sunset\"><figcaption>Beautiful sunset over the ocean</figcaption></figure>", new ConversionOptions());
            Assert.Contains("![A sunset](sunset.jpg)", result.Content.ToString());
            Assert.Contains("Beautiful sunset over the ocean", result.Content.ToString());

        }

        [Fact]
        public void Test_ImageLinked()
        {
            // Image inside an anchor produces a linked image
            var result = HtmlToMarkdownConverter.Convert("<a href=\"https://example.com\"><img src=\"icon.png\" alt=\"Icon\"></a>", new ConversionOptions());
            Assert.Contains("![Icon](icon.png)", result.Content.ToString());
            Assert.Contains("https://example.com", result.Content.ToString());

        }

        [Fact]
        public void Test_ImageNoAlt()
        {
            // Image without alt text produces image markdown
            var result = HtmlToMarkdownConverter.Convert("<img src=\"banner.jpg\">", new ConversionOptions());
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("banner.jpg", result.Content.ToString());

        }

        [Fact]
        public void Test_ImageSimple()
        {
            // Image with alt text
            var result = HtmlToMarkdownConverter.Convert("<img src=\"photo.jpg\" alt=\"A photo\">", new ConversionOptions());
            Assert.Contains("![A photo](photo.jpg)", result.Content.ToString());

        }

        [Fact]
        public void Test_ImageWithTitle()
        {
            // Image with title attribute includes title in output
            var result = HtmlToMarkdownConverter.Convert("<img src=\"chart.png\" alt=\"Sales chart\" title=\"Q3 Sales\">", new ConversionOptions());
            Assert.Contains("![Sales chart](chart.png", result.Content.ToString());
            Assert.Contains("Q3 Sales", result.Content.ToString());

        }

        [Fact]
        public void Test_InlineCode()
        {
            // Inline code
            var result = HtmlToMarkdownConverter.Convert("<p>Use <code>console.log()</code> to debug</p>", new ConversionOptions());
            Assert.Contains("`console.log()`", result.Content.ToString());

        }

        [Fact]
        public void Test_ItalicEm()
        {
            // Em tag converts to italic
            var result = HtmlToMarkdownConverter.Convert("<p><em>italic</em></p>", new ConversionOptions());
            Assert.Contains("*italic*", result.Content.ToString());

        }

        [Fact]
        public void Test_LineBreakBrTag()
        {
            // Single br tag produces a line break in output
            var result = HtmlToMarkdownConverter.Convert("<p>First line.<br>Second line.</p>", new ConversionOptions());
            Assert.Contains("First line.", result.Content.ToString());
            Assert.Contains("Second line.", result.Content.ToString());

        }

        [Fact]
        public void Test_LineBreakHrTag()
        {
            // hr tag produces a horizontal separator between content
            var result = HtmlToMarkdownConverter.Convert("<p>Before rule.</p><hr><p>After rule.</p>", new ConversionOptions());
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Before rule.", result.Content.ToString());
            Assert.Contains("After rule.", result.Content.ToString());

        }

        [Fact]
        public void Test_LineBreakMultipleBr()
        {
            // Multiple consecutive br tags in sequence
            var result = HtmlToMarkdownConverter.Convert("<p>Start.<br><br>End.</p>", new ConversionOptions());
            Assert.Contains("Start.", result.Content.ToString());
            Assert.Contains("End.", result.Content.ToString());

        }

        [Fact]
        public void Test_LinkAnchorFragment()
        {
            // Fragment-only anchor link is preserved
            var result = HtmlToMarkdownConverter.Convert("<a href=\"#section\">Jump to section</a>", new ConversionOptions());
            Assert.Contains("[Jump to section](#section)", result.Content.ToString());

        }

        [Fact]
        public void Test_LinkEmptyHref()
        {
            // Link with empty href produces output with the link text
            var result = HtmlToMarkdownConverter.Convert("<a href=\"\">No destination</a>", new ConversionOptions());
            Assert.Contains("No destination", result.Content.ToString());

        }

        [Fact]
        public void Test_LinkImageInside()
        {
            // Image inside a link produces a linked image
            var result = HtmlToMarkdownConverter.Convert("<a href=\"https://example.com\"><img src=\"logo.png\" alt=\"Logo\"></a>", new ConversionOptions());
            Assert.Contains("![Logo](logo.png)", result.Content.ToString());
            Assert.Contains("https://example.com", result.Content.ToString());

        }

        [Fact]
        public void Test_LinkMailto()
        {
            // Mailto link is preserved with mailto: scheme
            var result = HtmlToMarkdownConverter.Convert("<a href=\"mailto:user@example.com\">Email us</a>", new ConversionOptions());
            Assert.Contains("mailto:user@example.com", result.Content.ToString());

        }

        [Fact]
        public void Test_LinkSimple()
        {
            // Simple link
            var result = HtmlToMarkdownConverter.Convert("<a href=\"https://example.com\">Example</a>", new ConversionOptions());
            Assert.Contains("[Example](https://example.com)", result.Content.ToString());

        }

        [Fact]
        public void Test_LinkWithBoldText()
        {
            // Link containing bold text preserves formatting
            var result = HtmlToMarkdownConverter.Convert("<a href=\"https://example.com\"><strong>Bold link</strong></a>", new ConversionOptions());
            Assert.Contains("**Bold link**", result.Content.ToString());
            Assert.Contains("https://example.com", result.Content.ToString());

        }

        [Fact]
        public void Test_LinkWithTitle()
        {
            // Link with title attribute
            var result = HtmlToMarkdownConverter.Convert("<a href=\"https://example.com\" title=\"Example Site\">Example</a>", new ConversionOptions());
            Assert.Contains("[Example](https://example.com", result.Content.ToString());
            Assert.Contains("Example Site", result.Content.ToString());

        }

        [Fact]
        public void Test_ListDefinitionDl()
        {
            // Definition list with dt and dd elements
            var result = HtmlToMarkdownConverter.Convert("<dl><dt>Term One</dt><dd>Definition of term one.</dd><dt>Term Two</dt><dd>Definition of term two.</dd></dl>", new ConversionOptions());
            Assert.Contains("Term One", result.Content.ToString());
            Assert.Contains("Definition of term one.", result.Content.ToString());
            Assert.Contains("Term Two", result.Content.ToString());
            Assert.Contains("Definition of term two.", result.Content.ToString());

        }

        [Fact]
        public void Test_ListItemMultipleParagraphs()
        {
            // List item containing multiple paragraphs
            var result = HtmlToMarkdownConverter.Convert("<ul><li><p>First paragraph in item.</p><p>Second paragraph in item.</p></li><li>Simple item</li></ul>", new ConversionOptions());
            Assert.Contains("First paragraph in item.", result.Content.ToString());
            Assert.Contains("Second paragraph in item.", result.Content.ToString());
            Assert.Contains("Simple item", result.Content.ToString());

        }

        [Fact]
        public void Test_ListMixedNested()
        {
            // Mixed list: ordered list nested inside unordered list
            var result = HtmlToMarkdownConverter.Convert("<ul><li>Item A<ol><li>Sub 1</li><li>Sub 2</li></ol></li><li>Item B</li></ul>", new ConversionOptions());
            Assert.Contains("Item A", result.Content.ToString());
            Assert.Contains("Sub 1", result.Content.ToString());
            Assert.Contains("Sub 2", result.Content.ToString());
            Assert.Contains("Item B", result.Content.ToString());

        }

        [Fact]
        public void Test_ListNestedOrdered()
        {
            // Nested ordered list with two levels of depth
            var result = HtmlToMarkdownConverter.Convert("<ol><li>Step 1<ol><li>Step 1a</li><li>Step 1b</li></ol></li><li>Step 2</li></ol>", new ConversionOptions());
            Assert.Contains("Step 1", result.Content.ToString());
            Assert.Contains("Step 1a", result.Content.ToString());
            Assert.Contains("Step 1b", result.Content.ToString());
            Assert.Contains("Step 2", result.Content.ToString());

        }

        [Fact]
        public void Test_ListNestedUnordered()
        {
            // Nested unordered list with two levels of depth
            var result = HtmlToMarkdownConverter.Convert("<ul><li>Parent A<ul><li>Child A1</li><li>Child A2</li></ul></li><li>Parent B</li></ul>", new ConversionOptions());
            Assert.Contains("Parent A", result.Content.ToString());
            Assert.Contains("Child A1", result.Content.ToString());
            Assert.Contains("Child A2", result.Content.ToString());
            Assert.Contains("Parent B", result.Content.ToString());

        }

        [Fact]
        public void Test_ListTaskCheckboxes()
        {
            // Task list with checked and unchecked checkboxes
            var result = HtmlToMarkdownConverter.Convert("<ul><li><input type=\"checkbox\" checked> Done task</li><li><input type=\"checkbox\"> Pending task</li></ul>", new ConversionOptions());
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Done task", result.Content.ToString());
            Assert.Contains("Pending task", result.Content.ToString());

        }

        [Fact]
        public void Test_OrderedList()
        {
            // Ordered list
            var result = HtmlToMarkdownConverter.Convert("<ol><li>First</li><li>Second</li><li>Third</li></ol>", new ConversionOptions());
            Assert.Contains("1. First", result.Content.ToString());
            Assert.Contains("2. Second", result.Content.ToString());
            Assert.Contains("3. Third", result.Content.ToString());

        }

        [Fact]
        public void Test_ParagraphMultiple()
        {
            // Multiple paragraphs are separated by a blank line
            var result = HtmlToMarkdownConverter.Convert("<p>First paragraph.</p><p>Second paragraph.</p>", new ConversionOptions());
            Assert.Contains("First paragraph.", result.Content.ToString());
            Assert.Contains("Second paragraph.", result.Content.ToString());

        }

        [Fact]
        public void Test_ParagraphNestedDivs()
        {
            // Text nested inside divs is extracted correctly
            var result = HtmlToMarkdownConverter.Convert("<div><div><p>Nested text</p></div></div>", new ConversionOptions());
            Assert.Contains("Nested text", result.Content.ToString());

        }

        [Fact]
        public void Test_ParagraphSimple()
        {
            // Simple paragraph converts to plain text
            var result = HtmlToMarkdownConverter.Convert("<p>Hello World</p>", new ConversionOptions());
            Assert.Equal("Hello World\n", result.Content);

        }

        [Fact]
        public void Test_ParagraphWithInlineFormatting()
        {
            // Paragraph with bold, italic, and a link
            var result = HtmlToMarkdownConverter.Convert("<p>This has <strong>bold</strong>, <em>italic</em>, and a <a href=\"https://example.com\">link</a>.</p>", new ConversionOptions());
            Assert.Contains("**bold**", result.Content.ToString());
            Assert.Contains("*italic*", result.Content.ToString());
            Assert.Contains("[link](https://example.com)", result.Content.ToString());

        }

        [Fact]
        public void Test_ParagraphWithLineBreaks()
        {
            // Paragraph with br tags produces line breaks in output
            var result = HtmlToMarkdownConverter.Convert("<p>Line one.<br>Line two.<br>Line three.</p>", new ConversionOptions());
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Line one.", result.Content.ToString());
            Assert.Contains("Line two.", result.Content.ToString());
            Assert.Contains("Line three.", result.Content.ToString());

        }

        [Fact]
        public void Test_SemanticAbbr()
        {
            // Abbreviation element text is preserved
            var result = HtmlToMarkdownConverter.Convert("<p>The <abbr title=\"World Wide Web\">WWW</abbr> is global.</p>", new ConversionOptions());
            Assert.Contains("WWW", result.Content.ToString());

        }

        [Fact]
        public void Test_SemanticArticle()
        {
            // Article element wrapping content preserves inner content
            var result = HtmlToMarkdownConverter.Convert("<article><h2>Article Title</h2><p>Article body.</p></article>", new ConversionOptions());
            Assert.Contains("Article Title", result.Content.ToString());
            Assert.Contains("Article body.", result.Content.ToString());

        }

        [Fact]
        public void Test_SemanticDefinitionList()
        {
            // Definition list with term and description
            var result = HtmlToMarkdownConverter.Convert("<dl><dt>HTML</dt><dd>HyperText Markup Language</dd><dt>CSS</dt><dd>Cascading Style Sheets</dd></dl>", new ConversionOptions());
            Assert.Contains("HTML", result.Content.ToString());
            Assert.Contains("HyperText Markup Language", result.Content.ToString());
            Assert.Contains("CSS", result.Content.ToString());
            Assert.Contains("Cascading Style Sheets", result.Content.ToString());

        }

        [Fact]
        public void Test_SemanticDetailsSummary()
        {
            // Details and summary elements produce readable output
            var result = HtmlToMarkdownConverter.Convert("<details><summary>Click to expand</summary><p>Hidden content here.</p></details>", new ConversionOptions());
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Click to expand", result.Content.ToString());

        }

        [Fact]
        public void Test_SemanticHr()
        {
            // Horizontal rule produces a separator in output
            var result = HtmlToMarkdownConverter.Convert("<p>Above</p><hr><p>Below</p>", new ConversionOptions());
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Above", result.Content.ToString());
            Assert.Contains("Below", result.Content.ToString());

        }

        [Fact]
        public void Test_SemanticMarkHighlight()
        {
            // Mark tag produces highlighted output
            var result = HtmlToMarkdownConverter.Convert("<p>This is <mark>highlighted text</mark> in a sentence.</p>", new ConversionOptions());
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("highlighted text", result.Content.ToString());

        }

        [Fact]
        public void Test_SemanticSectionWithHeading()
        {
            // Section element with heading preserves structure
            var result = HtmlToMarkdownConverter.Convert("<section><h3>Section Heading</h3><p>Section content.</p></section>", new ConversionOptions());
            Assert.Contains("Section Heading", result.Content.ToString());
            Assert.Contains("Section content.", result.Content.ToString());

        }

        [Fact]
        public void Test_SemanticSubSuperscript()
        {
            // Subscript and superscript elements are preserved in output
            var result = HtmlToMarkdownConverter.Convert("<p>H<sub>2</sub>O and E=mc<sup>2</sup></p>", new ConversionOptions());
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("H", result.Content.ToString());
            Assert.Contains("2", result.Content.ToString());
            Assert.Contains("O", result.Content.ToString());
            Assert.Contains("E=mc", result.Content.ToString());

        }

        [Fact]
        public void Test_SimpleTable()
        {
            // Simple table with header
            var result = HtmlToMarkdownConverter.Convert("<table><thead><tr><th>Name</th><th>Age</th></tr></thead><tbody><tr><td>Alice</td><td>30</td></tr></tbody></table>", new ConversionOptions());
            Assert.Contains("Name", result.Content.ToString());
            Assert.Contains("Age", result.Content.ToString());
            Assert.Contains("Alice", result.Content.ToString());
            Assert.Contains("30", result.Content.ToString());
            Assert.Contains("|", result.Content.ToString());
            Assert.Contains("---", result.Content.ToString());

        }

        [Fact]
        public void Test_StrikeRendersAsStrikethrough()
        {
            // The deprecated strike tag renders as strikethrough
            var result = HtmlToMarkdownConverter.Convert("<p><strike>gone</strike></p>", new ConversionOptions());
            Assert.Equal("~~gone~~\n", result.Content);

        }

        [Fact]
        public void Test_TableEmpty()
        {
            // Empty table produces no output or minimal output
            var result = HtmlToMarkdownConverter.Convert("<table></table>", new ConversionOptions());
            Assert.Equal("", result.Content);

        }

        [Fact]
        public void Test_TableLayoutCellParagraphsStaySeparated()
        {
            // Adjacent paragraphs in a layout-table cell are separated rather than concatenated into one run of text
            var result = HtmlToMarkdownConverter.Convert("<table><tr><td><p><b>Alice Example</b></p><p><i>Customer Service</i></p></td><td>Logo</td></tr><tr><td>A</td><td>B</td><td>C</td></tr></table>", new ConversionOptions { CompactTables = true });
            Assert.DoesNotContain("**Alice Example***Customer Service*", result.Content.ToString());
            Assert.Contains("Alice Example", result.Content.ToString());
            Assert.Contains("Customer Service", result.Content.ToString());
            Assert.Contains("Logo", result.Content.ToString());

        }

        [Fact]
        public void Test_TableNestedChainNotMisclassifiedAsLayout()
        {
            // A straight chain of one-nested-table-per-cell tables renders as pipe tables, not a broken layout list
            var result = HtmlToMarkdownConverter.Convert("<table><tr><td><table><tr><td><table><tr><td>leaf</td></tr></table></td></tr></table></td></tr></table>", new ConversionOptions());
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("leaf", result.Content.ToString());
            Assert.Contains("| ---", result.Content.ToString());

        }

        [Fact]
        public void Test_TableNestedInSingleCellRendersSeparately()
        {
            // A table nested in a single-cell row renders as its own table
            var result = HtmlToMarkdownConverter.Convert("<table><tr><th>Summary</th></tr><tr><td>Before<table><tr><th>Item</th><th>Amount</th></tr><tr><td>Alpha</td><td>10</td></tr></table></td></tr></table>", new ConversionOptions());
            Assert.Contains("| Item  | Amount |", result.Content.ToString());
            Assert.Contains("| Alpha | 10     |", result.Content.ToString());
            Assert.DoesNotContain("\\|", result.Content.ToString());

        }

        [Fact]
        public void Test_TableNoThead()
        {
            // Table without thead uses first row as implied header
            var result = HtmlToMarkdownConverter.Convert("<table><tr><td>Product</td><td>Price</td></tr><tr><td>Apple</td><td>1.00</td></tr></table>", new ConversionOptions());
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Product", result.Content.ToString());
            Assert.Contains("Price", result.Content.ToString());
            Assert.Contains("Apple", result.Content.ToString());
            Assert.Contains("1.00", result.Content.ToString());
            Assert.Contains("|", result.Content.ToString());

        }

        [Fact]
        public void Test_TablePipeCharsInContent()
        {
            // Table cells containing pipe characters are escaped in output
            var result = HtmlToMarkdownConverter.Convert("<table><thead><tr><th>Expression</th><th>Result</th></tr></thead><tbody><tr><td>a | b</td><td>true</td></tr></tbody></table>", new ConversionOptions());
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Expression", result.Content.ToString());
            Assert.Contains("Result", result.Content.ToString());
            Assert.Contains("true", result.Content.ToString());

        }

        [Fact]
        public void Test_TableRaggedRowFewerCellsThanHeader()
        {
            // A data row with fewer cells than the header is padded to the declared column count
            var result = HtmlToMarkdownConverter.Convert("<table><tr><th>A</th><th>B</th><th>C</th></tr><tr><td>1</td><td>2</td></tr></table>", new ConversionOptions());
            Assert.Equal("| A | B | C |\n| --- | --- | --- |\n| 1 | 2 |   |\n", result.Content);

        }

        [Fact]
        public void Test_TableRaggedRowMoreCellsThanHeader()
        {
            // A data row with more cells than the header widens the separator so no cell is silently dropped
            var result = HtmlToMarkdownConverter.Convert("<table><tr><th>A</th><th>B</th></tr><tr><td>1</td><td>2</td><td>3</td></tr></table>", new ConversionOptions());
            Assert.Equal("| A | B |   |\n| --- | --- | --- |\n| 1 | 2 | 3 |\n", result.Content);

        }

        [Fact]
        public void Test_TableWithAlignment()
        {
            // Table with column alignment attributes
            var result = HtmlToMarkdownConverter.Convert("<table><thead><tr><th align=\"left\">Left</th><th align=\"center\">Center</th><th align=\"right\">Right</th></tr></thead><tbody><tr><td>L</td><td>C</td><td>R</td></tr></tbody></table>", new ConversionOptions());
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Left", result.Content.ToString());
            Assert.Contains("Center", result.Content.ToString());
            Assert.Contains("Right", result.Content.ToString());
            Assert.Contains("L", result.Content.ToString());
            Assert.Contains("C", result.Content.ToString());
            Assert.Contains("R", result.Content.ToString());
            Assert.Contains("|", result.Content.ToString());

        }

        [Fact]
        public void Test_TableWithColspan()
        {
            // Table with colspan attribute in a header cell
            var result = HtmlToMarkdownConverter.Convert("<table><thead><tr><th colspan=\"2\">Full Name</th></tr></thead><tbody><tr><td>John</td><td>Doe</td></tr></tbody></table>", new ConversionOptions());
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Full Name", result.Content.ToString());
            Assert.Contains("John", result.Content.ToString());
            Assert.Contains("Doe", result.Content.ToString());

        }

        [Fact]
        public void Test_TaggedSpaceInStrikethroughKept()
        {
            // A whitespace-only strikethrough keeps the word separator
            var result = HtmlToMarkdownConverter.Convert("<p>A<del> </del>B</p>", new ConversionOptions());
            Assert.Equal("A B\n", result.Content);

        }

        [Fact]
        public void Test_TaggedSpaceInSubscriptKept()
        {
            // A whitespace-only subscript keeps the word separator
            var result = HtmlToMarkdownConverter.Convert("<p>A<sub> </sub>B</p>", new ConversionOptions());
            Assert.Equal("A B\n", result.Content);

        }

        [Fact]
        public void Test_TaggedSpaceNotDuplicated()
        {
            // A whitespace-only inline element contributes exactly one space
            var result = HtmlToMarkdownConverter.Convert("<p>A<i> </i>B</p>", new ConversionOptions());
            Assert.Equal("A B\n", result.Content);

        }

        [Fact]
        public void Test_UnorderedList()
        {
            // Unordered list
            var result = HtmlToMarkdownConverter.Convert("<ul><li>Item 1</li><li>Item 2</li><li>Item 3</li></ul>", new ConversionOptions());
            Assert.Contains("- Item 1", result.Content.ToString());
            Assert.Contains("- Item 2", result.Content.ToString());
            Assert.Contains("- Item 3", result.Content.ToString());

        }

        [Fact]
        public void Test_WhitespaceOnlyCodeSpanKept()
        {
            // A code span whose content is a single space survives
            var result = HtmlToMarkdownConverter.Convert("<p>A<code> </code>B</p>", new ConversionOptions());
            Assert.Equal("A` `B\n", result.Content);

        }


    }
}
