// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:d788929be56b821a43b591f8c7b38dfe7ea52d513e4679cd72f324c1a7cdd460
// To regenerate: alef generate
// To verify freshness: alef verify

using System;
using System.Collections.Generic;
using System.Linq;
using System.Net.Http;
using System.Text;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading.Tasks;
using Xunit;
using HtmlToMarkdown;
using static HtmlToMarkdown.HtmlToMarkdownConverter;

namespace HtmlToMarkdown
{
    /// <summary>E2e tests for category: options.</summary>
    public class OptionsTests
    {
            private static readonly JsonSerializerOptions ConfigOptions = new() { Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) }, DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault };

        [Fact]
        public void Test_Issue396BackticksBlankLineAfterFence()
        {
            // Backticks code block trims trailing newline inside fence and adds blank line after closing fence (issue #396)
            var result = HtmlToMarkdownConverter.Convert("<p>Foo</p><pre><code>1\n2\n</code></pre><p>Bar</p>", new ConversionOptions { CodeBlockStyle = CodeBlockStyle.Backticks });
            Assert.Equal("Foo\n\n```\n1\n2\n```\n\nBar\n", result.Content);

        }

        [Fact]
        public void Test_OptionsAutolinksFalse()
        {
            // Bare URL links rendered as regular markdown links when autolinks disabled
            var result = HtmlToMarkdownConverter.Convert("<p><a href='https://example.com'>https://example.com</a></p>", new ConversionOptions { Autolinks = false });
            Assert.Contains("example.com", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsBrInTablesAnchorWrappedNestedTableInLayoutCell()
        {
            // An anchor-wrapped table inside a layout cell keeps its inner links clickable
            var result = HtmlToMarkdownConverter.Convert("<table><tr><td><a href=\"/outer\"><table><tr><td><a href=\"/one\">One</a><a href=\"/two\">Two</a></td></tr></table></a></td></tr><tr><td>After</td></tr></table>", new ConversionOptions { BrInTables = true, CompactTables = true });
            Assert.Equal("- [/outer](/outer)\n\n| [One](/one)[Two](/two) |\n| --- |\n- After\n", result.Content);

        }

        [Fact]
        public void Test_OptionsBrInTablesFalse()
        {
            // BR elements in table cells are stripped when disabled
            var result = HtmlToMarkdownConverter.Convert("<table><tr><th>Col</th></tr><tr><td>A<br>B</td></tr></table>", new ConversionOptions { BrInTables = false });
            Assert.Contains("Col", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsBrInTablesNestedDataTable()
        {
            // A single-cell layout wrapper preserves its nested data table as structured Markdown
            var result = HtmlToMarkdownConverter.Convert("<table><tr><td><p>Before</p><table><tr><th>ID</th><th>Status</th></tr><tr><td>123</td><td>Done</td></tr></table></td></tr></table>", new ConversionOptions { BrInTables = true, CompactTables = true });
            Assert.Equal("Before\n\n| ID | Status |\n| --- | --- |\n| 123 | Done |\n", result.Content);

        }

        [Fact]
        public void Test_OptionsBrInTablesNestedTableRows()
        {
            // A nested table flattened into a cell keeps its row boundaries as literal <br> when br_in_tables is enabled
            var result = HtmlToMarkdownConverter.Convert("<table><tr><td><p>Before</p><table><tr><th>ID</th><th>Status</th></tr><tr><td>123</td><td>Done</td></tr></table></td><td>Other</td></tr></table>", new ConversionOptions { BrInTables = true, CompactTables = true });
            Assert.Contains("Before<br>", result.Content.ToString());
            Assert.Contains("<br>", result.Content.ToString());
            Assert.Contains("123", result.Content.ToString());
            Assert.Contains("Done", result.Content.ToString());
            Assert.Contains("Status", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsBrInTablesTrue()
        {
            // BR elements in table cells render as line breaks
            var result = HtmlToMarkdownConverter.Convert("<table><tr><th>Header</th></tr><tr><td>Line 1<br>Line 2</td></tr></table>", new ConversionOptions { BrInTables = true });
            Assert.Contains("Header", result.Content.ToString());
            Assert.Contains("Line 1", result.Content.ToString());
            Assert.Contains("Line 2", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsCaptureSvgFalse()
        {
            // Disabling capture_svg still produces content; SVG is not extracted as an image
            var result = HtmlToMarkdownConverter.Convert("<p>Below SVG:</p><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"10\" height=\"10\"><rect width=\"10\" height=\"10\" fill=\"red\"/></svg>", new ConversionOptions { CaptureSvg = false, ExtractImages = true });
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Below SVG", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsCaptureSvgTrue()
        {
            // Enabling capture_svg processes inline SVG elements when extract_images is on
            var result = HtmlToMarkdownConverter.Convert("<p>Below SVG:</p><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"10\" height=\"10\"><rect width=\"10\" height=\"10\" fill=\"red\"/></svg>", new ConversionOptions { CaptureSvg = true, ExtractImages = true });
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Below SVG", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsCodeBlockBackticks()
        {
            // Backticks code block style uses triple backtick fences
            var result = HtmlToMarkdownConverter.Convert("<pre><code class=\"language-js\">console.log('hi');</code></pre>", new ConversionOptions { CodeBlockStyle = CodeBlockStyle.Backticks });
            Assert.Contains("```", result.Content.ToString());
            Assert.Contains("console.log('hi');", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsCodeBlockIndented()
        {
            // Code blocks use 4-space indentation
            var result = HtmlToMarkdownConverter.Convert("<pre><code>print('hello')</code></pre>", new ConversionOptions { CodeBlockStyle = CodeBlockStyle.Indented });
            Assert.Contains("print('hello')", result.Content.ToString());
            Assert.DoesNotContain("```", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsCodeBlockTildes()
        {
            // Code blocks use tilde fences
            var result = HtmlToMarkdownConverter.Convert("<pre><code>let x = 1;</code></pre>", new ConversionOptions { CodeBlockStyle = CodeBlockStyle.Tildes });
            Assert.Contains("~~~", result.Content.ToString());
            Assert.Contains("let x = 1;", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsCodeBlockTildesStyle()
        {
            // Tildes code block style uses triple tilde fences
            var result = HtmlToMarkdownConverter.Convert("<pre><code>some code</code></pre>", new ConversionOptions { CodeBlockStyle = CodeBlockStyle.Tildes });
            Assert.Contains("~~~", result.Content.ToString());
            Assert.Contains("some code", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsCodeLanguagePython()
        {
            // Default code language annotation on blocks without lang attribute
            var result = HtmlToMarkdownConverter.Convert("<pre><code>def hello(): pass</code></pre>", new ConversionOptions { CodeLanguage = "python" });
            Assert.Contains("```python", result.Content.ToString());
            Assert.Contains("def hello", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsCompactTablesFalse()
        {
            // compact_tables false (default) pads cells to column width
            var result = HtmlToMarkdownConverter.Convert("<table><thead><tr><th>Name</th><th>Score</th></tr></thead><tbody><tr><td>Alice</td><td>100</td></tr><tr><td>Bob</td><td>42</td></tr></tbody></table>", new ConversionOptions { CompactTables = false });
            Assert.Contains("| ----- |", result.Content.ToString());
            Assert.Contains("| 42    |", result.Content.ToString());
            Assert.DoesNotContain("| --- |", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsCompactTablesTrue()
        {
            // compact_tables emits unpadded cells and minimal --- separators
            var result = HtmlToMarkdownConverter.Convert("<table><thead><tr><th>Name</th><th>Score</th></tr></thead><tbody><tr><td>Alice</td><td>100</td></tr><tr><td>Bob</td><td>42</td></tr></tbody></table>", new ConversionOptions { CompactTables = true });
            Assert.Contains("| --- |", result.Content.ToString());
            Assert.DoesNotContain("| ----- |", result.Content.ToString());
            Assert.Contains("| 42 |", result.Content.ToString());
            Assert.DoesNotContain("| 42    |", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsConvertAsInline()
        {
            // Block elements treated as inline
            var result = HtmlToMarkdownConverter.Convert("<p>One</p><p>Two</p>", new ConversionOptions { ConvertAsInline = true });
            Assert.Contains("One", result.Content.ToString());
            Assert.Contains("Two", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsCustomBulletsApplyToLayoutTableRows()
        {
            // A layout table renders its rows as list items, and those markers honour the bullets option like any other list
            var result = HtmlToMarkdownConverter.Convert("<table><tr><td>alpha</td><td>beta</td></tr><tr><td><a href=\"/1\">a</a></td><td><a href=\"/2\">b</a></td><td><a href=\"/3\">c</a></td></tr></table>", new ConversionOptions { Bullets = "*+-" });
            Assert.Contains("* alpha", result.Content.ToString());
            Assert.DoesNotContain("- alpha", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsDebugTrue()
        {
            // Debug mode enabled does not crash and produces output
            var result = HtmlToMarkdownConverter.Convert("<p>Debug test</p>", new ConversionOptions { Debug = true });
            Assert.Contains("Debug test", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsDefaultTitleTrue()
        {
            // Links without title get empty title attribute when defaultTitle is true
            var result = HtmlToMarkdownConverter.Convert("<p><a href='https://example.com'>Link</a></p>", new ConversionOptions { DefaultTitle = true });
            Assert.Contains("Link", result.Content.ToString());
            Assert.Contains("https://example.com", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsEncodingUtf8()
        {
            // UTF-8 encoding hint for special characters
            var result = HtmlToMarkdownConverter.Convert("<p>Café naïve résumé</p>", new ConversionOptions { Encoding = "utf-8" });
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");

        }

        [Fact]
        public void Test_OptionsEscapeAsciiEnabled()
        {
            // ASCII Markdown characters are escaped when escapeAscii is true
            var result = HtmlToMarkdownConverter.Convert("<p>Text with # hash and [brackets] and * star</p>", new ConversionOptions { EscapeAscii = true });
            Assert.Contains("Text", result.Content.ToString());
            Assert.Contains("hash", result.Content.ToString());
            Assert.Contains("brackets", result.Content.ToString());
            Assert.Contains("star", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsEscapeAsterisks()
        {
            // escape_asterisks option escapes asterisks in plain text
            var result = HtmlToMarkdownConverter.Convert("<p>Use 2*3 = 6 in math.</p>", new ConversionOptions { EscapeAsterisks = true });
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("2", result.Content.ToString());
            Assert.Contains("3", result.Content.ToString());
            Assert.Contains("6", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsEscapeMisc()
        {
            // escape_misc option escapes miscellaneous markdown characters
            var result = HtmlToMarkdownConverter.Convert("<p>Use # and | and ~ in text.</p>", new ConversionOptions { EscapeMisc = true });
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Use", result.Content.ToString());
            Assert.Contains("and", result.Content.ToString());
            Assert.Contains("in text.", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsEscapeUnderscores()
        {
            // escape_underscores option escapes underscores in plain text
            var result = HtmlToMarkdownConverter.Convert("<p>The variable_name is defined.</p>", new ConversionOptions { EscapeUnderscores = true });
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("variable", result.Content.ToString());
            Assert.Contains("name", result.Content.ToString());
            Assert.Contains("defined.", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsExcludeSelectorsAttribute()
        {
            // Elements matching CSS attribute selector are excluded entirely
            var result = HtmlToMarkdownConverter.Convert("<body><div role=\"complementary\">Sidebar</div><p>Primary text</p></body>", new ConversionOptions { ExcludeSelectors = new List<string>() { "[role='complementary']" } });
            Assert.Contains("Primary text", result.Content.ToString());
            Assert.DoesNotContain("Sidebar", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsExcludeSelectorsClass()
        {
            // Elements matching CSS class selector are excluded entirely
            var result = HtmlToMarkdownConverter.Convert("<body><div class=\"cookie-banner\">Accept cookies</div><p>Main content</p></body>", new ConversionOptions { ExcludeSelectors = new List<string>() { ".cookie-banner" } });
            Assert.Contains("Main content", result.Content.ToString());
            Assert.DoesNotContain("cookies", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsExcludeSelectorsEmptyNoop()
        {
            // Empty exclude_selectors list does not affect output
            var result = HtmlToMarkdownConverter.Convert("<p>Hello world</p>", new ConversionOptions { ExcludeSelectors = new List<string>() {  } });
            Assert.Contains("Hello world", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsExcludeSelectorsId()
        {
            // Elements matching CSS id selector are excluded entirely
            var result = HtmlToMarkdownConverter.Convert("<body><div id=\"ad-container\">Buy stuff</div><p>Article text</p></body>", new ConversionOptions { ExcludeSelectors = new List<string>() { "#ad-container" } });
            Assert.Contains("Article text", result.Content.ToString());
            Assert.DoesNotContain("Buy stuff", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsExcludeSelectorsMultiple()
        {
            // Multiple CSS selectors each exclude their matched elements
            var result = HtmlToMarkdownConverter.Convert("<body><nav class=\"nav\">Menu</nav><p>Content</p><footer>Footer</footer></body>", new ConversionOptions { ExcludeSelectors = new List<string>() { ".nav", "footer" } });
            Assert.Contains("Content", result.Content.ToString());
            Assert.DoesNotContain("Menu", result.Content.ToString());
            Assert.DoesNotContain("Footer", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsExcludeSelectorsNestedContentDropped()
        {
            // All descendants of excluded elements are dropped
            var result = HtmlToMarkdownConverter.Convert("<body><aside class=\"sidebar\"><h2>Related</h2><p>Sidebar text</p></aside><main><p>Main text</p></main></body>", new ConversionOptions { ExcludeSelectors = new List<string>() { ".sidebar" } });
            Assert.Contains("Main text", result.Content.ToString());
            Assert.DoesNotContain("Related", result.Content.ToString());
            Assert.DoesNotContain("Sidebar text", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsExcludeSelectorsPlainTextMode()
        {
            // Exclude selectors work in plain text output mode
            var result = HtmlToMarkdownConverter.Convert("<body><div class=\"nav\">Navigation</div><p>Article body</p></body>", new ConversionOptions { ExcludeSelectors = new List<string>() { ".nav" }, OutputFormat = OutputFormat.Plain });
            Assert.Contains("Article body", result.Content.ToString());
            Assert.DoesNotContain("Navigation", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsExcludeSelectorsVsStripTags()
        {
            // exclude_selectors drops entire subtree unlike strip_tags which keeps children
            var result = HtmlToMarkdownConverter.Convert("<body><div class=\"wrapper\"><p>Inner paragraph</p></div><p>Outer text</p></body>", new ConversionOptions { ExcludeSelectors = new List<string>() { ".wrapper" } });
            Assert.Contains("Outer text", result.Content.ToString());
            Assert.DoesNotContain("Inner paragraph", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsExtractImagesFalse()
        {
            // When extract_images is false, conversion still succeeds and inline images are not extracted
            var result = HtmlToMarkdownConverter.Convert("<p>Text with <img src=\"data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8/5+hHgAHggJ/PchI7wAAAABJRU5ErkJggg==\" alt=\"pixel\"> image.</p>", new ConversionOptions { ExtractImages = false });
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");

        }

        [Fact]
        public void Test_OptionsExtractImagesTrueDataUri()
        {
            // Enabling extract_images processes a data-URI image without crashing
            var result = HtmlToMarkdownConverter.Convert("<p>Before<img src=\"data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8/5+hHgAHggJ/PchI7wAAAABJRU5ErkJggg==\" alt=\"pixel\">After</p>", new ConversionOptions { ExtractImages = true });
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Before", result.Content.ToString());
            Assert.Contains("After", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsExtractMetadataTrue()
        {
            // Extract metadata returns document metadata when enabled
            var result = HtmlToMarkdownConverter.Convert("<html><head><title>Test Page</title><meta name='description' content='A test page'></head><body><p>Content</p></body></html>", new ConversionOptions { ExtractMetadata = true });
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Equal("Test Page", result.Metadata.Document.Title);
            Assert.Equal("A test page", result.Metadata.Document.Description);

        }

        [Fact]
        public void Test_OptionsHeadingStyleAtx()
        {
            // ATX heading style produces hash-prefixed headings
            var result = HtmlToMarkdownConverter.Convert("<h1>Title</h1><h2>Subtitle</h2>", new ConversionOptions { HeadingStyle = HeadingStyle.Atx });
            Assert.Contains("# Title", result.Content.ToString());
            Assert.Contains("## Subtitle", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsHeadingStyleAtxClosed()
        {
            // ATX closed heading style adds closing hashes
            var result = HtmlToMarkdownConverter.Convert("<h1>Closed Heading</h1>", new ConversionOptions { HeadingStyle = HeadingStyle.AtxClosed });
            Assert.Contains("# Closed Heading #", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsHeadingStyleUnderlined()
        {
            // Underlined heading style produces setext-style headings for h1 and h2
            var result = HtmlToMarkdownConverter.Convert("<h1>Main Title</h1>", new ConversionOptions { HeadingStyle = HeadingStyle.Underlined });
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Main Title", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsHighlightBold()
        {
            // Mark tag rendered as bold
            var result = HtmlToMarkdownConverter.Convert("<p>Text with <mark>highlighted</mark> text.</p>", new ConversionOptions { HighlightStyle = HighlightStyle.Bold });
            Assert.Contains("**highlighted**", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsHighlightDoubleEqual()
        {
            // Mark tag with double equal highlight style
            var result = HtmlToMarkdownConverter.Convert("<p>Text with <mark>highlighted</mark> here.</p>", new ConversionOptions { HighlightStyle = HighlightStyle.DoubleEqual });
            Assert.Contains("==highlighted==", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsHighlightNone()
        {
            // Mark tag with no highlight style strips the mark
            var result = HtmlToMarkdownConverter.Convert("<p>Text with <mark>plain</mark> content.</p>", new ConversionOptions { HighlightStyle = HighlightStyle.None });
            Assert.Contains("plain", result.Content.ToString());
            Assert.DoesNotContain("==", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsIncludeDocumentStructureFalse()
        {
            // Disabling include_document_structure still produces content without the document tree overhead
            var result = HtmlToMarkdownConverter.Convert("<article><h1>Heading</h1><p>Paragraph body.</p></article>", new ConversionOptions { IncludeDocumentStructure = false });
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Heading", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsIncludeDocumentStructureTrue()
        {
            // Setting include_document_structure populates the structured document tree on the result
            var result = HtmlToMarkdownConverter.Convert("<article><h1>Heading</h1><p>Paragraph body.</p></article>", new ConversionOptions { IncludeDocumentStructure = true });
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Heading", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsInferDimensionsFalse()
        {
            // Disabling infer_dimensions skips the decode step and still produces content
            var result = HtmlToMarkdownConverter.Convert("<p>No dims: <img src=\"data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8/5+hHgAHggJ/PchI7wAAAABJRU5ErkJggg==\" alt=\"pixel\"></p>", new ConversionOptions { ExtractImages = true, InferDimensions = false });
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("No dims", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsInferDimensionsTrue()
        {
            // Enabling infer_dimensions decodes image bytes to populate dimension metadata when extract_images is on
            var result = HtmlToMarkdownConverter.Convert("<p>With dims: <img src=\"data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8/5+hHgAHggJ/PchI7wAAAABJRU5ErkJggg==\" alt=\"pixel\"></p>", new ConversionOptions { ExtractImages = true, InferDimensions = true });
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("With dims", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsKeepInlineImagesInParagraph()
        {
            // Images inside specified tags stay inline
            var result = HtmlToMarkdownConverter.Convert("<p>Text <img src='icon.png' alt='icon'> more text</p>", new ConversionOptions { KeepInlineImagesIn = new List<string>() { "p" } });
            Assert.Contains("Text", result.Content.ToString());
            Assert.Contains("more text", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsLinkStyleReference()
        {
            // Links use reference-style formatting
            var result = HtmlToMarkdownConverter.Convert("<p><a href='https://example.com'>Example</a> and <a href='https://other.com'>Other</a></p>", new ConversionOptions { LinkStyle = LinkStyle.Reference });
            Assert.Contains("Example", result.Content.ToString());
            Assert.Contains("Other", result.Content.ToString());
            Assert.Contains("example.com", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsListCustomBullets()
        {
            // Custom bullet character for unordered lists
            var result = HtmlToMarkdownConverter.Convert("<ul><li>Item A</li><li>Item B</li></ul>", new ConversionOptions { Bullets = "*" });
            Assert.Contains("* Item A", result.Content.ToString());
            Assert.Contains("* Item B", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsListIndentTabs()
        {
            // Tab indentation type for nested list items
            var result = HtmlToMarkdownConverter.Convert("<ul><li>Parent<ul><li>Child</li></ul></li></ul>", new ConversionOptions { ListIndentType = ListIndentType.Tabs });
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Parent", result.Content.ToString());
            Assert.Contains("Child", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsListIndentWidthFour()
        {
            // Nested lists indented with 4 spaces per level
            var result = HtmlToMarkdownConverter.Convert("<ul><li>Outer<ul><li>Inner</li></ul></li></ul>", new ConversionOptions { ListIndentWidth = 4 });
            Assert.Contains("Outer", result.Content.ToString());
            Assert.Contains("Inner", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsMaxDepthDefaultUnlimited()
        {
            // Default max_depth (null) converts deeply nested content fully
            var result = HtmlToMarkdownConverter.Convert("<div><div><div><div><p>Deep content</p></div></div></div></div>", new ConversionOptions());
            Assert.Contains("Deep content", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsMaxDepthTruncates()
        {
            // max_depth truncates content beyond the specified depth
            var result = HtmlToMarkdownConverter.Convert("<div><p>Shallow</p><div><div><div><p>Too deep</p></div></div></div></div>", new ConversionOptions { MaxDepth = 3 });
            Assert.Contains("Shallow", result.Content.ToString());
            Assert.DoesNotContain("Too deep", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsMaxDepthZeroEmpty()
        {
            // max_depth of 0 produces empty output
            var result = HtmlToMarkdownConverter.Convert("<p>Hello</p>", new ConversionOptions { MaxDepth = 0 });
            Assert.Equal("", result.Content);

        }

        [Fact]
        public void Test_OptionsMaxImageSizeGenerousLimit()
        {
            // A generous max_image_size accommodates a small data-URI image without warnings
            var result = HtmlToMarkdownConverter.Convert("<p>Image: <img src=\"data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8/5+hHgAHggJ/PchI7wAAAABJRU5ErkJggg==\" alt=\"pixel\"></p>", new ConversionOptions { ExtractImages = true, MaxImageSize = 10485760 });
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Image", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsMaxImageSizeTinyLimitSafe()
        {
            // A tiny max_image_size does not crash conversion even when the image exceeds the limit
            var result = HtmlToMarkdownConverter.Convert("<p>Tiny limit: <img src=\"data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8/5+hHgAHggJ/PchI7wAAAABJRU5ErkJggg==\" alt=\"pixel\"></p>", new ConversionOptions { ExtractImages = true, MaxImageSize = 10 });
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Tiny limit", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsNewlineBackslash()
        {
            // Hard line breaks rendered with backslash
            var result = HtmlToMarkdownConverter.Convert("<p>Line one<br>Line two</p>", new ConversionOptions { NewlineStyle = NewlineStyle.Backslash });
            Assert.Contains("Line one", result.Content.ToString());
            Assert.Contains("Line two", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsNewlineSpaces()
        {
            // Hard line breaks rendered with trailing spaces
            var result = HtmlToMarkdownConverter.Convert("<p>First<br>Second</p>", new ConversionOptions { NewlineStyle = NewlineStyle.Spaces });
            Assert.Contains("First", result.Content.ToString());
            Assert.Contains("Second", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsOutputFormatDjot()
        {
            // Djot output format produces djot-compatible markup
            var result = HtmlToMarkdownConverter.Convert("<p>Simple paragraph.</p>", new ConversionOptions { OutputFormat = OutputFormat.Djot });
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Simple paragraph.", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsOutputFormatMarkdown()
        {
            // Default markdown output format produces standard markdown
            var result = HtmlToMarkdownConverter.Convert("<h1>Title</h1><p>Some text.</p>", new ConversionOptions { HeadingStyle = HeadingStyle.Atx, OutputFormat = OutputFormat.Markdown });
            Assert.Contains("# Title", result.Content.ToString());
            Assert.Contains("Some text.", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsOutputFormatPlain()
        {
            // Plain text output format strips markdown syntax
            var result = HtmlToMarkdownConverter.Convert("<h1>Title</h1><p>Some <strong>bold</strong> text.</p>", new ConversionOptions { OutputFormat = OutputFormat.Plain });
            Assert.Contains("Title", result.Content.ToString());
            Assert.Contains("bold", result.Content.ToString());
            Assert.Contains("text.", result.Content.ToString());
            Assert.DoesNotContain("**bold**", result.Content.ToString());
            Assert.DoesNotContain("# Title", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsPreprocessingAggressive()
        {
            // Aggressive preset removes nav, footer, aside unconditionally
            var result = HtmlToMarkdownConverter.Convert("<nav>Menu</nav><article><h1>Title</h1><p>Content</p></article><aside>Sidebar</aside><footer>Footer</footer>", new ConversionOptions { Preprocessing = new PreprocessingOptions { Preset = JsonSerializer.Deserialize<PreprocessingPreset>("\"Aggressive\"", ConfigOptions)! } });
            Assert.Contains("Title", result.Content.ToString());
            Assert.Contains("Content", result.Content.ToString());
            Assert.DoesNotContain("Menu", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsPreprocessingEnabledFalseSkipsCleanup()
        {
            // Disabling preprocessing entirely preserves elements the standard preset would remove
            var result = HtmlToMarkdownConverter.Convert("<nav>NavSection</nav><p>Paragraph</p>", new ConversionOptions { Preprocessing = new PreprocessingOptions { Enabled = false } });
            Assert.Contains("NavSection", result.Content.ToString());
            Assert.Contains("Paragraph", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsPreprocessingMinimal()
        {
            // Minimal preset preserves nav, footer, aside
            var result = HtmlToMarkdownConverter.Convert("<nav>Navigation</nav><p>Content</p><footer>Footer</footer>", new ConversionOptions { Preprocessing = new PreprocessingOptions { Preset = JsonSerializer.Deserialize<PreprocessingPreset>("\"Minimal\"", ConfigOptions)! } });
            Assert.Contains("Navigation", result.Content.ToString());
            Assert.Contains("Content", result.Content.ToString());
            Assert.Contains("Footer", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsPreprocessingRemoveForms()
        {
            // Forms are removed when remove_forms is true
            var result = HtmlToMarkdownConverter.Convert("<p>Before</p><form><input type='text'/><button>Submit</button></form><p>After</p>", new ConversionOptions { Preprocessing = new PreprocessingOptions { RemoveForms = true } });
            Assert.Contains("Before", result.Content.ToString());
            Assert.Contains("After", result.Content.ToString());
            Assert.DoesNotContain("Submit", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsPreprocessingRemoveNavigationFalseKeepsNav()
        {
            // Setting remove_navigation to false preserves nav and aside elements
            var result = HtmlToMarkdownConverter.Convert("<nav>SiteMenu</nav><main><p>MainContent</p></main><aside>SidebarText</aside>", new ConversionOptions { Preprocessing = new PreprocessingOptions { RemoveNavigation = false } });
            Assert.Contains("SiteMenu", result.Content.ToString());
            Assert.Contains("MainContent", result.Content.ToString());
            Assert.Contains("SidebarText", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsPreserveTagsIframe()
        {
            // Iframe tags preserved as raw HTML in output
            var result = HtmlToMarkdownConverter.Convert("<p>Before</p><iframe src='video.html' width='560'></iframe><p>After</p>", new ConversionOptions { PreserveTags = new List<string>() { "iframe" } });
            Assert.Contains("Before", result.Content.ToString());
            Assert.Contains("After", result.Content.ToString());
            Assert.Contains("<iframe", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsSkipImagesTrue()
        {
            // Images are omitted from output when skipImages is true
            var result = HtmlToMarkdownConverter.Convert("<p>Before <img src='test.jpg' alt='photo'> After</p>", new ConversionOptions { SkipImages = true });
            Assert.Contains("Before", result.Content.ToString());
            Assert.Contains("After", result.Content.ToString());
            Assert.DoesNotContain("photo", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsStripNewlines()
        {
            // Strip newlines produces single-line paragraphs
            var result = HtmlToMarkdownConverter.Convert("<p>First paragraph.</p><p>Second paragraph.</p>", new ConversionOptions { StripNewlines = true });
            Assert.Contains("First paragraph.", result.Content.ToString());
            Assert.Contains("Second paragraph.", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsStripTagsDivSpan()
        {
            // Div and span tags stripped but content preserved
            var result = HtmlToMarkdownConverter.Convert("<div class='wrapper'><p>Inside div</p></div><p>Outside <span class='hl'>span text</span></p>", new ConversionOptions { StripTags = new List<string>() { "div", "span" } });
            Assert.Contains("Inside div", result.Content.ToString());
            Assert.Contains("span text", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsStrongEmUnderscore()
        {
            // Strong and em tags use underscore symbol instead of asterisk
            var result = HtmlToMarkdownConverter.Convert("<p><strong>bold</strong> and <em>italic</em></p>", new ConversionOptions { StrongEmSymbol = "_" });
            Assert.Contains("__bold__", result.Content.ToString());
            Assert.Contains("_italic_", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsSubSymbolTilde()
        {
            // Subscript rendered with tilde symbol
            var result = HtmlToMarkdownConverter.Convert("<p>H<sub>2</sub>O</p>", new ConversionOptions { SubSymbol = "~" });
            Assert.Contains("~2~", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsSupSymbolCaret()
        {
            // Superscript rendered with caret symbol
            var result = HtmlToMarkdownConverter.Convert("<p>x<sup>2</sup></p>", new ConversionOptions { SupSymbol = "^" });
            Assert.Contains("^2^", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsUrlEscapeStyleAngleDefault()
        {
            // Default angle style wraps URLs containing spaces in angle brackets
            var result = HtmlToMarkdownConverter.Convert("<a href=\"/file (1).pdf\">file</a>", new ConversionOptions { UrlEscapeStyle = UrlEscapeStyle.Angle });
            Assert.Contains("</file (1).pdf>", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsUrlEscapeStylePercentAngleBracketsInUrl()
        {
            // Percent style encodes angle brackets in link URLs from issue 392
            var result = HtmlToMarkdownConverter.Convert("<a href=\"/file (1) <draft>.pdf\">file</a>", new ConversionOptions { UrlEscapeStyle = UrlEscapeStyle.Percent });
            Assert.Contains("/file%20%281%29%20%3Cdraft%3E.pdf", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsUrlEscapeStylePercentImage()
        {
            // Percent style encodes special characters in image src URLs
            var result = HtmlToMarkdownConverter.Convert("<img src=\"/img (1) <draft>.png\" alt=\"alt\">", new ConversionOptions { UrlEscapeStyle = UrlEscapeStyle.Percent });
            Assert.Contains("/img%20%281%29%20%3Cdraft%3E.png", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsUrlEscapeStylePercentLink()
        {
            // Percent style encodes spaces and parens in link URLs
            var result = HtmlToMarkdownConverter.Convert("<a href=\"/file (1).pdf\">file</a>", new ConversionOptions { UrlEscapeStyle = UrlEscapeStyle.Percent });
            Assert.Contains("/file%20%281%29.pdf", result.Content.ToString());
            Assert.DoesNotContain("</", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsWhitespaceNormalized()
        {
            // Normalized whitespace mode collapses multiple spaces
            var result = HtmlToMarkdownConverter.Convert("<p>Text   with    extra   spaces.</p>", new ConversionOptions { WhitespaceMode = WhitespaceMode.Normalized });
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Text", result.Content.ToString());
            Assert.Contains("with", result.Content.ToString());
            Assert.Contains("extra", result.Content.ToString());
            Assert.Contains("spaces.", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsWhitespaceStrict()
        {
            // Strict whitespace mode preserves whitespace as-is
            var result = HtmlToMarkdownConverter.Convert("<p>Preserved   spacing.</p>", new ConversionOptions { WhitespaceMode = WhitespaceMode.Strict });
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("Preserved", result.Content.ToString());
            Assert.Contains("spacing.", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsWrapDisabled()
        {
            // Wrap option disabled preserves long lines without breaking
            var result = HtmlToMarkdownConverter.Convert("<p>This is a long paragraph that should not be wrapped at all because wrapping is disabled.</p>", new ConversionOptions { Wrap = false });
            Assert.Contains("This is a long paragraph that should not be wrapped at all because wrapping is disabled.", result.Content.ToString());

        }

        [Fact]
        public void Test_OptionsWrapEnabled()
        {
            // Wrap option enabled with custom width wraps long lines
            var result = HtmlToMarkdownConverter.Convert("<p>This is a long paragraph that should be wrapped at the specified column width when the wrap option is enabled.</p>", new ConversionOptions { Wrap = true, WrapWidth = 40 });
            Assert.True(((object?)result.Content) switch
            {
                null => false,
                string text => text.Length > 0,
                System.Collections.ICollection items => items.Count > 0,
                _ => true,
            }, "expected non-empty value");
            Assert.Contains("This is a long paragraph", result.Content.ToString());

        }


    }
}
