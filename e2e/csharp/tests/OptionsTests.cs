// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:430069bb45842d9e9daf626375aed1b6764764301bd9c8546f979e4f1d56ace3
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net.Http;
using System.Text;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading.Tasks;
using Xunit;
using HtmlToMarkdown;
using static HtmlToMarkdown.HtmlToMarkdownRs;

namespace Kreuzberg.E2e;

/// <summary>E2e tests for category: options.</summary>
public class OptionsTests
{
    private static readonly JsonSerializerOptions ConfigOptions = new() { Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) }, DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault };

    [Fact]
    public void Test_OptionsAutolinksFalse()
    {
        // Bare URL links rendered as regular markdown links when autolinks disabled
        var result = HtmlToMarkdownRs.Convert("<p><a href='https://example.com'>https://example.com</a></p>", JsonSerializer.Deserialize<ConversionOptions>("{\"autolinks\":false}", ConfigOptions)!);
        Assert.Contains("example.com", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsBrInTablesFalse()
    {
        // BR elements in table cells are stripped when disabled
        var result = HtmlToMarkdownRs.Convert("<table><tr><th>Col</th></tr><tr><td>A<br>B</td></tr></table>", JsonSerializer.Deserialize<ConversionOptions>("{\"brInTables\":false}", ConfigOptions)!);
        Assert.Contains("col", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsBrInTablesTrue()
    {
        // BR elements in table cells render as line breaks
        var result = HtmlToMarkdownRs.Convert("<table><tr><th>Header</th></tr><tr><td>Line 1<br>Line 2</td></tr></table>", JsonSerializer.Deserialize<ConversionOptions>("{\"brInTables\":true}", ConfigOptions)!);
        Assert.Contains("header", result.Content.ToString().ToLower());
        Assert.Contains("line 1", result.Content.ToString().ToLower());
        Assert.Contains("line 2", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsCodeBlockBackticks()
    {
        // Backticks code block style uses triple backtick fences
        var result = HtmlToMarkdownRs.Convert("<pre><code class=\"language-js\">console.log('hi');</code></pre>", JsonSerializer.Deserialize<ConversionOptions>("{\"codeBlockStyle\":\"backticks\"}", ConfigOptions)!);
        Assert.Contains("```", result.Content.ToString().ToLower());
        Assert.Contains("console.log('hi');", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsCodeBlockIndented()
    {
        // Code blocks use 4-space indentation
        var result = HtmlToMarkdownRs.Convert("<pre><code>print('hello')</code></pre>", JsonSerializer.Deserialize<ConversionOptions>("{\"codeBlockStyle\":\"indented\"}", ConfigOptions)!);
        Assert.Contains("print('hello')", result.Content.ToString().ToLower());
        Assert.DoesNotContain("```", result.Content.ToString());
    }

    [Fact]
    public void Test_OptionsCodeBlockTildes()
    {
        // Code blocks use tilde fences
        var result = HtmlToMarkdownRs.Convert("<pre><code>let x = 1;</code></pre>", JsonSerializer.Deserialize<ConversionOptions>("{\"codeBlockStyle\":\"tildes\"}", ConfigOptions)!);
        Assert.Contains("~~~", result.Content.ToString().ToLower());
        Assert.Contains("let x = 1;", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsCodeBlockTildesStyle()
    {
        // Tildes code block style uses triple tilde fences
        var result = HtmlToMarkdownRs.Convert("<pre><code>some code</code></pre>", JsonSerializer.Deserialize<ConversionOptions>("{\"codeBlockStyle\":\"tildes\"}", ConfigOptions)!);
        Assert.Contains("~~~", result.Content.ToString().ToLower());
        Assert.Contains("some code", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsCodeLanguagePython()
    {
        // Default code language annotation on blocks without lang attribute
        var result = HtmlToMarkdownRs.Convert("<pre><code>def hello(): pass</code></pre>", JsonSerializer.Deserialize<ConversionOptions>("{\"codeLanguage\":\"python\"}", ConfigOptions)!);
        Assert.Contains("```python", result.Content.ToString().ToLower());
        Assert.Contains("def hello", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsConvertAsInline()
    {
        // Block elements treated as inline
        var result = HtmlToMarkdownRs.Convert("<p>One</p><p>Two</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"convertAsInline\":true}", ConfigOptions)!);
        Assert.Contains("one", result.Content.ToString().ToLower());
        Assert.Contains("two", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsDebugTrue()
    {
        // Debug mode enabled does not crash and produces output
        var result = HtmlToMarkdownRs.Convert("<p>Debug test</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"debug\":true}", ConfigOptions)!);
        Assert.Contains("debug test", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsDefaultTitleTrue()
    {
        // Links without title get empty title attribute when defaultTitle is true
        var result = HtmlToMarkdownRs.Convert("<p><a href='https://example.com'>Link</a></p>", JsonSerializer.Deserialize<ConversionOptions>("{\"defaultTitle\":true}", ConfigOptions)!);
        Assert.Contains("link", result.Content.ToString().ToLower());
        Assert.Contains("https://example.com", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsEncodingUtf8()
    {
        // UTF-8 encoding hint for special characters
        var result = HtmlToMarkdownRs.Convert("<p>Café naïve résumé</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"encoding\":\"utf-8\"}", ConfigOptions)!);
        Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    }

    [Fact]
    public void Test_OptionsEscapeAsciiEnabled()
    {
        // ASCII Markdown characters are escaped when escapeAscii is true
        var result = HtmlToMarkdownRs.Convert("<p>Text with # hash and [brackets] and * star</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"escapeAscii\":true}", ConfigOptions)!);
        Assert.Contains("text", result.Content.ToString().ToLower());
        Assert.Contains("hash", result.Content.ToString().ToLower());
        Assert.Contains("brackets", result.Content.ToString().ToLower());
        Assert.Contains("star", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsEscapeAsterisks()
    {
        // escape_asterisks option escapes asterisks in plain text
        var result = HtmlToMarkdownRs.Convert("<p>Use 2*3 = 6 in math.</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"escapeAsterisks\":true}", ConfigOptions)!);
        Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
        Assert.Contains("2", result.Content.ToString().ToLower());
        Assert.Contains("3", result.Content.ToString().ToLower());
        Assert.Contains("6", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsEscapeMisc()
    {
        // escape_misc option escapes miscellaneous markdown characters
        var result = HtmlToMarkdownRs.Convert("<p>Use # and | and ~ in text.</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"escapeMisc\":true}", ConfigOptions)!);
        Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
        Assert.Contains("use", result.Content.ToString().ToLower());
        Assert.Contains("and", result.Content.ToString().ToLower());
        Assert.Contains("in text.", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsEscapeUnderscores()
    {
        // escape_underscores option escapes underscores in plain text
        var result = HtmlToMarkdownRs.Convert("<p>The variable_name is defined.</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"escapeUnderscores\":true}", ConfigOptions)!);
        Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
        Assert.Contains("variable", result.Content.ToString().ToLower());
        Assert.Contains("name", result.Content.ToString().ToLower());
        Assert.Contains("defined.", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsExcludeSelectorsAttribute()
    {
        // Elements matching CSS attribute selector are excluded entirely
        var result = HtmlToMarkdownRs.Convert("<body><div role=\"complementary\">Sidebar</div><p>Primary text</p></body>", JsonSerializer.Deserialize<ConversionOptions>("{\"excludeSelectors\":[\"[role='complementary']\"]}", ConfigOptions)!);
        Assert.Contains("primary text", result.Content.ToString().ToLower());
        Assert.DoesNotContain("Sidebar", result.Content.ToString());
    }

    [Fact]
    public void Test_OptionsExcludeSelectorsClass()
    {
        // Elements matching CSS class selector are excluded entirely
        var result = HtmlToMarkdownRs.Convert("<body><div class=\"cookie-banner\">Accept cookies</div><p>Main content</p></body>", JsonSerializer.Deserialize<ConversionOptions>("{\"excludeSelectors\":[\".cookie-banner\"]}", ConfigOptions)!);
        Assert.Contains("main content", result.Content.ToString().ToLower());
        Assert.DoesNotContain("cookies", result.Content.ToString());
    }

    [Fact]
    public void Test_OptionsExcludeSelectorsEmptyNoop()
    {
        // Empty exclude_selectors list does not affect output
        var result = HtmlToMarkdownRs.Convert("<p>Hello world</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"excludeSelectors\":[]}", ConfigOptions)!);
        Assert.Contains("hello world", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsExcludeSelectorsId()
    {
        // Elements matching CSS id selector are excluded entirely
        var result = HtmlToMarkdownRs.Convert("<body><div id=\"ad-container\">Buy stuff</div><p>Article text</p></body>", JsonSerializer.Deserialize<ConversionOptions>("{\"excludeSelectors\":[\"#ad-container\"]}", ConfigOptions)!);
        Assert.Contains("article text", result.Content.ToString().ToLower());
        Assert.DoesNotContain("Buy stuff", result.Content.ToString());
    }

    [Fact]
    public void Test_OptionsExcludeSelectorsMultiple()
    {
        // Multiple CSS selectors each exclude their matched elements
        var result = HtmlToMarkdownRs.Convert("<body><nav class=\"nav\">Menu</nav><p>Content</p><footer>Footer</footer></body>", JsonSerializer.Deserialize<ConversionOptions>("{\"excludeSelectors\":[\".nav\",\"footer\"]}", ConfigOptions)!);
        Assert.Contains("content", result.Content.ToString().ToLower());
        Assert.DoesNotContain("Menu", result.Content.ToString());
        Assert.DoesNotContain("Footer", result.Content.ToString());
    }

    [Fact]
    public void Test_OptionsExcludeSelectorsNestedContentDropped()
    {
        // All descendants of excluded elements are dropped
        var result = HtmlToMarkdownRs.Convert("<body><aside class=\"sidebar\"><h2>Related</h2><p>Sidebar text</p></aside><main><p>Main text</p></main></body>", JsonSerializer.Deserialize<ConversionOptions>("{\"excludeSelectors\":[\".sidebar\"]}", ConfigOptions)!);
        Assert.Contains("main text", result.Content.ToString().ToLower());
        Assert.DoesNotContain("Related", result.Content.ToString());
        Assert.DoesNotContain("Sidebar text", result.Content.ToString());
    }

    [Fact]
    public void Test_OptionsExcludeSelectorsPlainTextMode()
    {
        // Exclude selectors work in plain text output mode
        var result = HtmlToMarkdownRs.Convert("<body><div class=\"nav\">Navigation</div><p>Article body</p></body>", JsonSerializer.Deserialize<ConversionOptions>("{\"excludeSelectors\":[\".nav\"],\"outputFormat\":\"plain\"}", ConfigOptions)!);
        Assert.Contains("article body", result.Content.ToString().ToLower());
        Assert.DoesNotContain("Navigation", result.Content.ToString());
    }

    [Fact]
    public void Test_OptionsExcludeSelectorsVsStripTags()
    {
        // exclude_selectors drops entire subtree unlike strip_tags which keeps children
        var result = HtmlToMarkdownRs.Convert("<body><div class=\"wrapper\"><p>Inner paragraph</p></div><p>Outer text</p></body>", JsonSerializer.Deserialize<ConversionOptions>("{\"excludeSelectors\":[\".wrapper\"]}", ConfigOptions)!);
        Assert.Contains("outer text", result.Content.ToString().ToLower());
        Assert.DoesNotContain("Inner paragraph", result.Content.ToString());
    }

    [Fact]
    public void Test_OptionsExtractMetadataTrue()
    {
        // Extract metadata returns document metadata when enabled
        var result = HtmlToMarkdownRs.Convert("<html><head><title>Test Page</title><meta name='description' content='A test page'></head><body><p>Content</p></body></html>", JsonSerializer.Deserialize<ConversionOptions>("{\"extractMetadata\":true}", ConfigOptions)!);
        Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
        Assert.Equal("Test Page", result.Metadata.Document.Title!.Trim());
        Assert.Equal("A test page", result.Metadata.Document.Description!.Trim());
    }

    [Fact]
    public void Test_OptionsHeadingStyleAtx()
    {
        // ATX heading style produces hash-prefixed headings
        var result = HtmlToMarkdownRs.Convert("<h1>Title</h1><h2>Subtitle</h2>", JsonSerializer.Deserialize<ConversionOptions>("{\"headingStyle\":\"atx\"}", ConfigOptions)!);
        Assert.Contains("# title", result.Content.ToString().ToLower());
        Assert.Contains("## subtitle", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsHeadingStyleAtxClosed()
    {
        // ATX closed heading style adds closing hashes
        var result = HtmlToMarkdownRs.Convert("<h1>Closed Heading</h1>", JsonSerializer.Deserialize<ConversionOptions>("{\"headingStyle\":\"atxclosed\"}", ConfigOptions)!);
        Assert.Contains("# closed heading #", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsHeadingStyleUnderlined()
    {
        // Underlined heading style produces setext-style headings for h1 and h2
        var result = HtmlToMarkdownRs.Convert("<h1>Main Title</h1>", JsonSerializer.Deserialize<ConversionOptions>("{\"headingStyle\":\"underlined\"}", ConfigOptions)!);
        Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
        Assert.Contains("main title", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsHighlightBold()
    {
        // Mark tag rendered as bold
        var result = HtmlToMarkdownRs.Convert("<p>Text with <mark>highlighted</mark> text.</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"highlightStyle\":\"bold\"}", ConfigOptions)!);
        Assert.Contains("**highlighted**", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsHighlightDoubleEqual()
    {
        // Mark tag with double equal highlight style
        var result = HtmlToMarkdownRs.Convert("<p>Text with <mark>highlighted</mark> here.</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"highlightStyle\":\"doubleequal\"}", ConfigOptions)!);
        Assert.Contains("==highlighted==", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsHighlightNone()
    {
        // Mark tag with no highlight style strips the mark
        var result = HtmlToMarkdownRs.Convert("<p>Text with <mark>plain</mark> content.</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"highlightStyle\":\"none\"}", ConfigOptions)!);
        Assert.Contains("plain", result.Content.ToString().ToLower());
        Assert.DoesNotContain("==", result.Content.ToString());
    }

    [Fact]
    public void Test_OptionsKeepInlineImagesInParagraph()
    {
        // Images inside specified tags stay inline
        var result = HtmlToMarkdownRs.Convert("<p>Text <img src='icon.png' alt='icon'> more text</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"keepInlineImagesIn\":[\"p\"]}", ConfigOptions)!);
        Assert.Contains("text", result.Content.ToString().ToLower());
        Assert.Contains("more text", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsLinkStyleReference()
    {
        // Links use reference-style formatting
        var result = HtmlToMarkdownRs.Convert("<p><a href='https://example.com'>Example</a> and <a href='https://other.com'>Other</a></p>", JsonSerializer.Deserialize<ConversionOptions>("{\"linkStyle\":\"reference\"}", ConfigOptions)!);
        Assert.Contains("example", result.Content.ToString().ToLower());
        Assert.Contains("other", result.Content.ToString().ToLower());
        Assert.Contains("example.com", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsListCustomBullets()
    {
        // Custom bullet character for unordered lists
        var result = HtmlToMarkdownRs.Convert("<ul><li>Item A</li><li>Item B</li></ul>", JsonSerializer.Deserialize<ConversionOptions>("{\"bullets\":\"*\"}", ConfigOptions)!);
        Assert.Contains("* item a", result.Content.ToString().ToLower());
        Assert.Contains("* item b", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsListIndentTabs()
    {
        // Tab indentation type for nested list items
        var result = HtmlToMarkdownRs.Convert("<ul><li>Parent<ul><li>Child</li></ul></li></ul>", JsonSerializer.Deserialize<ConversionOptions>("{\"listIndentType\":\"tabs\"}", ConfigOptions)!);
        Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
        Assert.Contains("parent", result.Content.ToString().ToLower());
        Assert.Contains("child", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsListIndentWidthFour()
    {
        // Nested lists indented with 4 spaces per level
        var result = HtmlToMarkdownRs.Convert("<ul><li>Outer<ul><li>Inner</li></ul></li></ul>", JsonSerializer.Deserialize<ConversionOptions>("{\"listIndentWidth\":4}", ConfigOptions)!);
        Assert.Contains("outer", result.Content.ToString().ToLower());
        Assert.Contains("inner", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsMaxDepthDefaultUnlimited()
    {
        // Default max_depth (null) converts deeply nested content fully
        var result = HtmlToMarkdownRs.Convert("<div><div><div><div><p>Deep content</p></div></div></div></div>", null);
        Assert.Contains("deep content", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsMaxDepthTruncates()
    {
        // max_depth truncates content beyond the specified depth
        var result = HtmlToMarkdownRs.Convert("<div><p>Shallow</p><div><div><div><p>Too deep</p></div></div></div></div>", JsonSerializer.Deserialize<ConversionOptions>("{\"maxDepth\":3}", ConfigOptions)!);
        Assert.Contains("shallow", result.Content.ToString().ToLower());
        Assert.DoesNotContain("Too deep", result.Content.ToString());
    }

    [Fact]
    public void Test_OptionsMaxDepthZeroEmpty()
    {
        // max_depth of 0 produces empty output
        var result = HtmlToMarkdownRs.Convert("<p>Hello</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"maxDepth\":0}", ConfigOptions)!);
        Assert.Equal("", result.Content!.Trim());
    }

    [Fact]
    public void Test_OptionsNewlineBackslash()
    {
        // Hard line breaks rendered with backslash
        var result = HtmlToMarkdownRs.Convert("<p>Line one<br>Line two</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"newlineStyle\":\"backslash\"}", ConfigOptions)!);
        Assert.Contains("line one", result.Content.ToString().ToLower());
        Assert.Contains("line two", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsNewlineSpaces()
    {
        // Hard line breaks rendered with trailing spaces
        var result = HtmlToMarkdownRs.Convert("<p>First<br>Second</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"newlineStyle\":\"spaces\"}", ConfigOptions)!);
        Assert.Contains("first", result.Content.ToString().ToLower());
        Assert.Contains("second", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsOutputFormatDjot()
    {
        // Djot output format produces djot-compatible markup
        var result = HtmlToMarkdownRs.Convert("<p>Simple paragraph.</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"outputFormat\":\"djot\"}", ConfigOptions)!);
        Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
        Assert.Contains("simple paragraph.", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsOutputFormatMarkdown()
    {
        // Default markdown output format produces standard markdown
        var result = HtmlToMarkdownRs.Convert("<h1>Title</h1><p>Some text.</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"headingStyle\":\"atx\",\"outputFormat\":\"markdown\"}", ConfigOptions)!);
        Assert.Contains("# title", result.Content.ToString().ToLower());
        Assert.Contains("some text.", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsOutputFormatPlain()
    {
        // Plain text output format strips markdown syntax
        var result = HtmlToMarkdownRs.Convert("<h1>Title</h1><p>Some <strong>bold</strong> text.</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"outputFormat\":\"plain\"}", ConfigOptions)!);
        Assert.Contains("title", result.Content.ToString().ToLower());
        Assert.Contains("bold", result.Content.ToString().ToLower());
        Assert.Contains("text.", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsPreprocessingAggressive()
    {
        // Aggressive preset removes nav, footer, aside unconditionally
        var result = HtmlToMarkdownRs.Convert("<nav>Menu</nav><article><h1>Title</h1><p>Content</p></article><aside>Sidebar</aside><footer>Footer</footer>", JsonSerializer.Deserialize<ConversionOptions>("{\"preprocessing\":{\"preset\":\"Aggressive\"}}", ConfigOptions)!);
        Assert.Contains("title", result.Content.ToString().ToLower());
        Assert.Contains("content", result.Content.ToString().ToLower());
        Assert.DoesNotContain("Menu", result.Content.ToString());
    }

    [Fact]
    public void Test_OptionsPreprocessingMinimal()
    {
        // Minimal preset preserves nav, footer, aside
        var result = HtmlToMarkdownRs.Convert("<nav>Navigation</nav><p>Content</p><footer>Footer</footer>", JsonSerializer.Deserialize<ConversionOptions>("{\"preprocessing\":{\"preset\":\"Minimal\"}}", ConfigOptions)!);
        Assert.Contains("navigation", result.Content.ToString().ToLower());
        Assert.Contains("content", result.Content.ToString().ToLower());
        Assert.Contains("footer", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsPreprocessingRemoveForms()
    {
        // Forms are removed when remove_forms is true
        var result = HtmlToMarkdownRs.Convert("<p>Before</p><form><input type='text'/><button>Submit</button></form><p>After</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"preprocessing\":{\"remove_forms\":true}}", ConfigOptions)!);
        Assert.Contains("before", result.Content.ToString().ToLower());
        Assert.Contains("after", result.Content.ToString().ToLower());
        Assert.DoesNotContain("Submit", result.Content.ToString());
    }

    [Fact]
    public void Test_OptionsPreserveTagsIframe()
    {
        // Iframe tags preserved as raw HTML in output
        var result = HtmlToMarkdownRs.Convert("<p>Before</p><iframe src='video.html' width='560'></iframe><p>After</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"preserveTags\":[\"iframe\"]}", ConfigOptions)!);
        Assert.Contains("before", result.Content.ToString().ToLower());
        Assert.Contains("after", result.Content.ToString().ToLower());
        Assert.Contains("<iframe", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsSkipImagesTrue()
    {
        // Images are omitted from output when skipImages is true
        var result = HtmlToMarkdownRs.Convert("<p>Before <img src='test.jpg' alt='photo'> After</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"skipImages\":true}", ConfigOptions)!);
        Assert.Contains("before", result.Content.ToString().ToLower());
        Assert.Contains("after", result.Content.ToString().ToLower());
        Assert.DoesNotContain("photo", result.Content.ToString());
    }

    [Fact]
    public void Test_OptionsStripNewlines()
    {
        // Strip newlines produces single-line paragraphs
        var result = HtmlToMarkdownRs.Convert("<p>First paragraph.</p><p>Second paragraph.</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"stripNewlines\":true}", ConfigOptions)!);
        Assert.Contains("first paragraph.", result.Content.ToString().ToLower());
        Assert.Contains("second paragraph.", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsStripTagsDivSpan()
    {
        // Div and span tags stripped but content preserved
        var result = HtmlToMarkdownRs.Convert("<div class='wrapper'><p>Inside div</p></div><p>Outside <span class='hl'>span text</span></p>", JsonSerializer.Deserialize<ConversionOptions>("{\"stripTags\":[\"div\",\"span\"]}", ConfigOptions)!);
        Assert.Contains("inside div", result.Content.ToString().ToLower());
        Assert.Contains("span text", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsStrongEmUnderscore()
    {
        // Strong and em tags use underscore symbol instead of asterisk
        var result = HtmlToMarkdownRs.Convert("<p><strong>bold</strong> and <em>italic</em></p>", JsonSerializer.Deserialize<ConversionOptions>("{\"strongEmSymbol\":\"_\"}", ConfigOptions)!);
        Assert.Contains("__bold__", result.Content.ToString().ToLower());
        Assert.Contains("_italic_", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsSubSymbolTilde()
    {
        // Subscript rendered with tilde symbol
        var result = HtmlToMarkdownRs.Convert("<p>H<sub>2</sub>O</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"subSymbol\":\"~\"}", ConfigOptions)!);
        Assert.Contains("~2~", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsSupSymbolCaret()
    {
        // Superscript rendered with caret symbol
        var result = HtmlToMarkdownRs.Convert("<p>x<sup>2</sup></p>", JsonSerializer.Deserialize<ConversionOptions>("{\"supSymbol\":\"^\"}", ConfigOptions)!);
        Assert.Contains("^2^", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsWhitespaceNormalized()
    {
        // Normalized whitespace mode collapses multiple spaces
        var result = HtmlToMarkdownRs.Convert("<p>Text   with    extra   spaces.</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"whitespaceMode\":\"normalized\"}", ConfigOptions)!);
        Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
        Assert.Contains("text", result.Content.ToString().ToLower());
        Assert.Contains("with", result.Content.ToString().ToLower());
        Assert.Contains("extra", result.Content.ToString().ToLower());
        Assert.Contains("spaces.", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsWhitespaceStrict()
    {
        // Strict whitespace mode preserves whitespace as-is
        var result = HtmlToMarkdownRs.Convert("<p>Preserved   spacing.</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"whitespaceMode\":\"strict\"}", ConfigOptions)!);
        Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
        Assert.Contains("preserved", result.Content.ToString().ToLower());
        Assert.Contains("spacing.", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsWrapDisabled()
    {
        // Wrap option disabled preserves long lines without breaking
        var result = HtmlToMarkdownRs.Convert("<p>This is a long paragraph that should not be wrapped at all because wrapping is disabled.</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"wrap\":false}", ConfigOptions)!);
        Assert.Contains("this is a long paragraph that should not be wrapped at all because wrapping is disabled.", result.Content.ToString().ToLower());
    }

    [Fact]
    public void Test_OptionsWrapEnabled()
    {
        // Wrap option enabled with custom width wraps long lines
        var result = HtmlToMarkdownRs.Convert("<p>This is a long paragraph that should be wrapped at the specified column width when the wrap option is enabled.</p>", JsonSerializer.Deserialize<ConversionOptions>("{\"wrap\":true,\"wrapWidth\":40}", ConfigOptions)!);
        Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
        Assert.Contains("this is a long paragraph", result.Content.ToString().ToLower());
    }
}
