// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:b3e8167ba4fcc9a375ac5221348d81de7d637286bc4103225c71fd6a9fff5bab
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

using System;
using System.Collections.Generic;
using System.Linq;
using System.Net.Http;
using System.Text;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading.Tasks;
using Xunit;
using HtmlToMarkdown;
using static HtmlToMarkdown.HtmlToMarkdownRs;

namespace HtmlToMarkdown
{
    /// <summary>E2e tests for category: structure.</summary>
    public class StructureTests
    {
            private static readonly JsonSerializerOptions ConfigOptions = new() { Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) }, DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault };

        [Fact]
        public void Test_StructureCodeBlock()
        {
            // Fenced code block produces Code node
            var result = HtmlToMarkdownRs.Convert("<p>Example code:</p><pre><code class=\"language-rust\">fn main() { println!(\"Hello\"); }</code></pre>", new ConversionOptions { IncludeDocumentStructure = true });
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.False(string.IsNullOrEmpty(result.Document!.Nodes?.ToString()));
    Assert.True(result.Document!.Nodes.Count >= 2, "expected at least 2 elements");

        }

        [Fact]
        public void Test_StructureDeepNestingH1H2H3()
        {
            // H1 > H2 > H3 creates three levels of heading nesting
            var result = HtmlToMarkdownRs.Convert("<h1>Top Level</h1><p>Top intro.</p><h2>Mid Level</h2><p>Mid content.</p><h3>Deep Level</h3><p>Deep content.</p>", new ConversionOptions { IncludeDocumentStructure = true });
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.False(string.IsNullOrEmpty(result.Document!.Nodes?.ToString()));
    Assert.True(result.Document!.Nodes.Count >= 5, "expected at least 5 elements");

        }

        [Fact]
        public void Test_StructureH1H2NestedGroup()
        {
            // H1 followed by H2 creates a nested group under the H1
            var result = HtmlToMarkdownRs.Convert("<h1>Chapter One</h1><p>Chapter intro.</p><h2>Section One</h2><p>Section content.</p>", new ConversionOptions { IncludeDocumentStructure = true });
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.False(string.IsNullOrEmpty(result.Document!.Nodes?.ToString()));
    Assert.True(result.Document!.Nodes.Count >= 3, "expected at least 3 elements");

        }

        [Fact]
        public void Test_StructureHeadingParagraph()
        {
            // Simple heading followed by paragraph produces Heading and Paragraph nodes
            var result = HtmlToMarkdownRs.Convert("<h1>Title</h1><p>A paragraph of text.</p>", new ConversionOptions { IncludeDocumentStructure = true });
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.False(string.IsNullOrEmpty(result.Document!.Nodes?.ToString()));
    Assert.True(result.Document!.Nodes.Count >= 2, "expected at least 2 elements");

        }

        [Fact]
        public void Test_StructureList()
        {
            // Unordered list produces List and ListItem nodes
            var result = HtmlToMarkdownRs.Convert("<p>Items:</p><ul><li>Alpha</li><li>Beta</li><li>Gamma</li></ul>", new ConversionOptions { IncludeDocumentStructure = true });
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.False(string.IsNullOrEmpty(result.Document!.Nodes?.ToString()));
    Assert.True(result.Document!.Nodes.Count >= 2, "expected at least 2 elements");

        }

        [Fact]
        public void Test_StructureMultipleHeadings()
        {
            // Multiple headings create multiple Heading nodes with correct levels
            var result = HtmlToMarkdownRs.Convert("<h1>Main Title</h1><h2>Section One</h2><p>Section one content.</p><h2>Section Two</h2><p>Section two content.</p>", new ConversionOptions { IncludeDocumentStructure = true });
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.False(string.IsNullOrEmpty(result.Document!.Nodes?.ToString()));
    Assert.True(result.Document!.Nodes.Count >= 4, "expected at least 4 elements");

        }

        [Fact]
        public void Test_StructureSiblingH1Groups()
        {
            // H1, H2, then another H1 creates two sibling top-level groups
            var result = HtmlToMarkdownRs.Convert("<h1>Chapter One</h1><h2>Section A</h2><p>Section A content.</p><h1>Chapter Two</h1><h2>Section B</h2><p>Section B content.</p>", new ConversionOptions { IncludeDocumentStructure = true });
    Assert.False(string.IsNullOrEmpty(result.Content?.ToString()));
    Assert.False(string.IsNullOrEmpty(result.Document!.Nodes?.ToString()));
    Assert.True(result.Document!.Nodes.Count >= 4, "expected at least 4 elements");

        }


    }
}
