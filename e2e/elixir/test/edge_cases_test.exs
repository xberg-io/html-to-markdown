# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:c4123217bb42e6cbe7cc59851037f301994cdb660a4a2934d8b056b2b5fc5adf
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# E2e tests for category: edge-cases
defmodule E2e.EdgeCasesTest do
  use ExUnit.Case, async: false

  describe "empty_html" do
    test "empty_html" do
      {:ok, result} = HtmlToMarkdown.convert("<html><head></head><body></body></html>")
      assert String.trim(result.content) == ""
    end
  end

  describe "encoding_cjk_characters" do
    test "encoding_cjk_characters" do
      {:ok, result} = HtmlToMarkdown.convert("<p>中文内容</p><p>日本語テキスト</p><p>한국어 텍스트</p>")
      assert result.content != ""
      assert String.contains?(to_string(result.content), "中文内容")
      assert String.contains?(to_string(result.content), "日本語テキスト")
      assert String.contains?(to_string(result.content), "한국어 텍스트")
    end
  end

  describe "encoding_html_entities" do
    test "encoding_html_entities" do
      {:ok, result} = HtmlToMarkdown.convert("<p>&amp; &lt; &gt; &nbsp; &quot; &apos;</p>")
      assert result.content != ""
      assert String.contains?(to_string(result.content), "&")
      assert String.contains?(to_string(result.content), "<")
      assert String.contains?(to_string(result.content), ">")
    end
  end

  describe "encoding_named_entities" do
    test "encoding_named_entities" do
      {:ok, result} = HtmlToMarkdown.convert("<p>Em dash&mdash;used for parenthetical remarks&mdash;is common. Ellipsis&hellip; indicates omission. Non-breaking&nbsp;space.</p>")
      assert result.content != ""
      assert String.contains?(to_string(result.content), "—")
      assert String.contains?(to_string(result.content), "…")
    end
  end

  describe "encoding_numeric_entities" do
    test "encoding_numeric_entities" do
      {:ok, result} = HtmlToMarkdown.convert("<p>Copyright: &\#169; Trade: &\#174; Euro: &\#8364; Hex: &\#x00A9;</p>")
      assert result.content != ""
      assert String.contains?(to_string(result.content), "©")
      assert String.contains?(to_string(result.content), "®")
      assert String.contains?(to_string(result.content), "€")
    end
  end

  describe "encoding_unicode_emoji" do
    test "encoding_unicode_emoji" do
      {:ok, result} = HtmlToMarkdown.convert("<p>Hello 🌍 World 🚀</p><p>Stars: ⭐ ✨</p>")
      assert result.content != ""
      assert String.contains?(to_string(result.content), "🌍")
      assert String.contains?(to_string(result.content), "🚀")
      assert String.contains?(to_string(result.content), "⭐")
    end
  end

  describe "html_comments_only" do
    test "html_comments_only" do
      {:ok, result} = HtmlToMarkdown.convert("<!-- This is a comment --><!-- Another comment -->")
      assert String.trim(result.content) == ""
    end
  end

  describe "just_whitespace_input" do
    test "just_whitespace_input" do
      {:ok, result} = HtmlToMarkdown.convert("   ")
      assert String.trim(result.content) == ""
    end
  end

  describe "malformed_bogus_comment_triple_dash" do
    test "malformed_bogus_comment_triple_dash" do
      {:ok, result} = HtmlToMarkdown.convert("<h1>One</h1>\n<!-- /// --->\n<p>Two</p>")
      assert String.contains?(to_string(result.content), "One")
      assert String.contains?(to_string(result.content), "Two")
    end
  end

  describe "malformed_deeply_nested_elements" do
    test "malformed_deeply_nested_elements" do
      {:ok, result} = HtmlToMarkdown.convert("<div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><div><p>Deeply nested content</p></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div></div>")
      assert result.content != ""
      assert String.contains?(to_string(result.content), "Deeply nested content")
    end
  end

  describe "malformed_missing_block_closing_tags" do
    test "malformed_missing_block_closing_tags" do
      {:ok, result} = HtmlToMarkdown.convert("<div><h1>Title<p>First paragraph<p>Second paragraph</div>")
      assert result.content != ""
      assert String.contains?(to_string(result.content), "Title")
      assert String.contains?(to_string(result.content), "First paragraph")
      assert String.contains?(to_string(result.content), "Second paragraph")
    end
  end

  describe "malformed_overlapping_tags" do
    test "malformed_overlapping_tags" do
      {:ok, result} = HtmlToMarkdown.convert("<p><b><i>bold and italic</b></i></p>")
      assert result.content != ""
      assert String.contains?(to_string(result.content), "bold and italic")
    end
  end

  describe "malformed_unclosed_paragraph" do
    test "malformed_unclosed_paragraph" do
      {:ok, result} = HtmlToMarkdown.convert("<p>This paragraph is never closed")
      assert result.content != ""
      assert String.contains?(to_string(result.content), "This paragraph is never closed")
    end
  end

  describe "script_tags_only" do
    test "script_tags_only" do
      {:ok, result} = HtmlToMarkdown.convert("<html><head><script>alert('xss')</script></head><body><script>document.write('hello')</script></body></html>")
      assert String.trim(result.content) == ""
    end
  end

  describe "style_tags_only" do
    test "style_tags_only" do
      {:ok, result} = HtmlToMarkdown.convert("<html><head><style>body { color: red; }</style></head><body><style>.foo { margin: 0; }</style></body></html>")
      assert String.trim(result.content) == ""
    end
  end

  describe "visitor_custom_element_with_nesting" do
    test "visitor_custom_element_with_nesting" do
      visitor = %{
      :handle_custom_element => fn(_args) ->
        {:custom, "[CUSTOM WIDGET]"}
      end,
    }

      {:ok, result} = HtmlToMarkdown.convert("<div><custom-widget data-value=\"123\"><p>Widget content here</p><span>With nested elements</span></custom-widget></div>", %{visitor: visitor})
      assert String.contains?(to_string(result.content), "[CUSTOM WIDGET]")
      refute String.contains?(to_string(result.content), "Widget content here")
    end
  end

  describe "visitor_deeply_nested_skip" do
    test "visitor_deeply_nested_skip" do
      visitor = %{
      :handle_mark => fn(_args) ->
        :skip
      end,
    }

      {:ok, result} = HtmlToMarkdown.convert("<div><p>Outer <em>emphasis <strong>with bold <mark>and highlight</mark></strong></em> text</p></div>", %{visitor: visitor})
      assert String.contains?(to_string(result.content), "Outer")
      assert String.contains?(to_string(result.content), "text")
      refute String.contains?(to_string(result.content), "highlight")
    end
  end

  describe "visitor_element_end_modification" do
    test "visitor_element_end_modification" do
      visitor = %{
      :handle_element_end => fn(_args) ->
        {:custom, "MODIFIED OUTPUT"}
      end,
    }

      {:ok, result} = HtmlToMarkdown.convert("<blockquote><p>Original quote</p></blockquote>", %{visitor: visitor})
      assert result.content != ""
    end
  end

  describe "visitor_element_start_skip_entire_subtree" do
    test "visitor_element_start_skip_entire_subtree" do
      visitor = %{
      :handle_element_start => fn(_args) ->
        :skip
      end,
    }

      {:ok, result} = HtmlToMarkdown.convert("<div><h1>Title</h1><p>Content</p></div>", %{visitor: visitor})
      refute String.contains?(to_string(result.content), "Title")
      refute String.contains?(to_string(result.content), "Content")
    end
  end

  describe "visitor_unknown_tag_preservation" do
    test "visitor_unknown_tag_preservation" do
      visitor = %{
      :handle_custom_element => fn(_args) ->
        :preserve_html
      end,
    }

      {:ok, result} = HtmlToMarkdown.convert("<article><p>Article text</p><x-custom>Custom element with content</x-custom><p>More article text</p></article>", %{visitor: visitor})
      assert String.contains?(to_string(result.content), "Article text")
      assert String.contains?(to_string(result.content), "More article text")
      assert String.contains?(to_string(result.content), "<x-custom>")
    end
  end

  describe "whitespace_only" do
    test "whitespace_only" do
      {:ok, result} = HtmlToMarkdown.convert("<p>   </p>")
      assert String.trim(result.content) == ""
    end
  end

  describe "xss_onclick_handler_removed" do
    test "xss_onclick_handler_removed" do
      {:ok, result} = HtmlToMarkdown.convert("<p><a href=\"https://example.com\" onclick=\"alert('xss')\">Click me</a></p><button onmouseover=\"steal_data()\">Hover me</button>")
      assert result.content != ""
      assert String.contains?(to_string(result.content), "Click me")
    end
  end

  describe "xss_script_tag_stripped" do
    test "xss_script_tag_stripped" do
      {:ok, result} = HtmlToMarkdown.convert("<p>Safe content.</p><script>alert('xss')</script><p>More safe content.</p>")
      assert result.content != ""
      assert String.contains?(to_string(result.content), "Safe content")
      assert String.contains?(to_string(result.content), "More safe content")
    end
  end

  describe "xss_svg_nested_script_stripped" do
    test "xss_svg_nested_script_stripped" do
      {:ok, result} = HtmlToMarkdown.convert("<p>Before SVG.</p><svg xmlns=\"http://www.w3.org/2000/svg\"><script>alert('svg-xss')</script><text>SVG text</text></svg><p>After SVG.</p>")
      assert result.content != ""
      assert String.contains?(to_string(result.content), "Before SVG")
      assert String.contains?(to_string(result.content), "After SVG")
    end
  end
end
