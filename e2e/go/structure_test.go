// Code generated by alef. DO NOT EDIT.

// E2e tests for category: structure
package e2e_test

import (
	"testing"

	"github.com/stretchr/testify/assert"

	htmd "github.com/kreuzberg-dev/html-to-markdown-go"
)

func Test_StructureCodeBlock(t *testing.T) {
	// Fenced code block produces Code node
	result, err := htmd.Convert(`<p>Example code:</p><pre><code class="language-rust">fn main() { println!("Hello"); }</code></pre>`, htmd.NewConversionOptions(htmd.WithConversionOptionsIncludeDocumentStructure(true)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.Content == nil || len(*result.Content) == 0 {
		t.Errorf("expected non-empty value")
	}
	if result.Document != nil {
		if len(result.Document.Nodes) == 0 {
			t.Errorf("expected non-empty value")
		}
	}
	if result.Document != nil {
		assert.GreaterOrEqual(t, len(result.Document.Nodes), 2, "expected at least 2 elements")
	}
}

func Test_StructureDeepNestingH1H2H3(t *testing.T) {
	// H1 > H2 > H3 creates three levels of heading nesting
	result, err := htmd.Convert(`<h1>Top Level</h1><p>Top intro.</p><h2>Mid Level</h2><p>Mid content.</p><h3>Deep Level</h3><p>Deep content.</p>`, htmd.NewConversionOptions(htmd.WithConversionOptionsIncludeDocumentStructure(true)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.Content == nil || len(*result.Content) == 0 {
		t.Errorf("expected non-empty value")
	}
	if result.Document != nil {
		if len(result.Document.Nodes) == 0 {
			t.Errorf("expected non-empty value")
		}
	}
	if result.Document != nil {
		assert.GreaterOrEqual(t, len(result.Document.Nodes), 5, "expected at least 5 elements")
	}
}

func Test_StructureH1H2NestedGroup(t *testing.T) {
	// H1 followed by H2 creates a nested group under the H1
	result, err := htmd.Convert(`<h1>Chapter One</h1><p>Chapter intro.</p><h2>Section One</h2><p>Section content.</p>`, htmd.NewConversionOptions(htmd.WithConversionOptionsIncludeDocumentStructure(true)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.Content == nil || len(*result.Content) == 0 {
		t.Errorf("expected non-empty value")
	}
	if result.Document != nil {
		if len(result.Document.Nodes) == 0 {
			t.Errorf("expected non-empty value")
		}
	}
	if result.Document != nil {
		assert.GreaterOrEqual(t, len(result.Document.Nodes), 3, "expected at least 3 elements")
	}
}

func Test_StructureHeadingParagraph(t *testing.T) {
	// Simple heading followed by paragraph produces Heading and Paragraph nodes
	result, err := htmd.Convert(`<h1>Title</h1><p>A paragraph of text.</p>`, htmd.NewConversionOptions(htmd.WithConversionOptionsIncludeDocumentStructure(true)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.Content == nil || len(*result.Content) == 0 {
		t.Errorf("expected non-empty value")
	}
	if result.Document != nil {
		if len(result.Document.Nodes) == 0 {
			t.Errorf("expected non-empty value")
		}
	}
	if result.Document != nil {
		assert.GreaterOrEqual(t, len(result.Document.Nodes), 2, "expected at least 2 elements")
	}
}

func Test_StructureList(t *testing.T) {
	// Unordered list produces List and ListItem nodes
	result, err := htmd.Convert(`<p>Items:</p><ul><li>Alpha</li><li>Beta</li><li>Gamma</li></ul>`, htmd.NewConversionOptions(htmd.WithConversionOptionsIncludeDocumentStructure(true)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.Content == nil || len(*result.Content) == 0 {
		t.Errorf("expected non-empty value")
	}
	if result.Document != nil {
		if len(result.Document.Nodes) == 0 {
			t.Errorf("expected non-empty value")
		}
	}
	if result.Document != nil {
		assert.GreaterOrEqual(t, len(result.Document.Nodes), 2, "expected at least 2 elements")
	}
}

func Test_StructureMultipleHeadings(t *testing.T) {
	// Multiple headings create multiple Heading nodes with correct levels
	result, err := htmd.Convert(`<h1>Main Title</h1><h2>Section One</h2><p>Section one content.</p><h2>Section Two</h2><p>Section two content.</p>`, htmd.NewConversionOptions(htmd.WithConversionOptionsIncludeDocumentStructure(true)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.Content == nil || len(*result.Content) == 0 {
		t.Errorf("expected non-empty value")
	}
	if result.Document != nil {
		if len(result.Document.Nodes) == 0 {
			t.Errorf("expected non-empty value")
		}
	}
	if result.Document != nil {
		assert.GreaterOrEqual(t, len(result.Document.Nodes), 4, "expected at least 4 elements")
	}
}

func Test_StructureSiblingH1Groups(t *testing.T) {
	// H1, H2, then another H1 creates two sibling top-level groups
	result, err := htmd.Convert(`<h1>Chapter One</h1><h2>Section A</h2><p>Section A content.</p><h1>Chapter Two</h1><h2>Section B</h2><p>Section B content.</p>`, htmd.NewConversionOptions(htmd.WithConversionOptionsIncludeDocumentStructure(true)))
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.Content == nil || len(*result.Content) == 0 {
		t.Errorf("expected non-empty value")
	}
	if result.Document != nil {
		if len(result.Document.Nodes) == 0 {
			t.Errorf("expected non-empty value")
		}
	}
	if result.Document != nil {
		assert.GreaterOrEqual(t, len(result.Document.Nodes), 4, "expected at least 4 elements")
	}
}
