// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:d810be9bcc4948c7d07329cc0c13a5a6fadac2289d7e4b2857fba70b4c94090b
// To regenerate: alef e2e generate
// To verify freshness: alef verify
import { describe, it } from "vitest";

describe("visitor", () => {
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_audio_custom", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_audio_skip", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_button_custom", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_button_skip", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_continue_default", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_custom_blockquote", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_custom_emphasis", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_custom_heading", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_custom_image", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_custom_link_format", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_custom_link_static", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_custom_output", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_definition_list_custom", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_definition_list_custom_format", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_definition_list_skip", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_details_summary_custom", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_details_summary_skip", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_figure_custom", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_figure_custom_wrap", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_figure_skip", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_form_custom", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_form_skip", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_heading_bare_string_preserves_case", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_horizontal_rule_custom", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_horizontal_rule_skip", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_iframe_custom", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_iframe_skip", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_image_bare_string_preserves_case", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_input_custom", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_input_skip", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_line_break_custom", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_line_break_skip", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_link_bare_string_preserves_case", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_mark_custom", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_mark_skip", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_preserve_html", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_skip_code_blocks", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_skip_heading", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_skip_images", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_skip_links", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_skip_strong", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_subscript_custom", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_subscript_skip", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_superscript_custom", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_superscript_skip", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_underline_custom", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_underline_skip", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_video_custom", () => {});
  // WASM visitor bridge not yet exposed via @xberg-io/html-to-markdown-wasm public API
  it.skip("visitor_video_skip", () => {});
});
