/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * To regenerate: alef generate
 * To verify freshness: alef verify --exit-code
 * Issues & docs: https://github.com/kreuzberg-dev/alef
 */
/* E2e tests for category: visitor */

#include "html_to_markdown.h"
#include "test_runner.h"
#include <assert.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

static int32_t c_visitor_visitor_custom_element_with_nesting_visit_custom_element(
    const HTMHtmNodeContext *_ctx, void *_user_data, const char *_tag_name, const char *_html,
    char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)_tag_name;
    (void)_html;
    char *_buf = strdup("[CUSTOM WIDGET]");
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_custom_element_with_nesting(void) {
    /* Visitor handles custom elements with nested content */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_custom_element =
        c_visitor_visitor_custom_element_with_nesting_visit_custom_element;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<div><custom-widget data-value=\"123\"><p>Widget content here</p><span>With "
                    "nested elements</span></custom-widget></div>",
                    _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "[CUSTOM WIDGET]") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "Widget content here") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_deeply_nested_skip_visit_mark(const HTMHtmNodeContext *_ctx,
                                                               void *_user_data, const char *_text,
                                                               char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    (void)_text;
    return 1;
}

void test_visitor_deeply_nested_skip(void) {
    /* Visitor skips deeply nested elements */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_mark = c_visitor_visitor_deeply_nested_skip_visit_mark;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<div><p>Outer <em>emphasis <strong>with bold <mark>and "
                    "highlight</mark></strong></em> text</p></div>",
                    _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "Outer") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "text") != NULL && "expected to contain substring");
    assert((_content == NULL || strstr(_content, "highlight") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t
c_visitor_visitor_element_end_modification_visit_element_end(const HTMHtmNodeContext *_ctx,
                                                             void *_user_data, const char *_output,
                                                             char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)_output;
    char *_buf = strdup("MODIFIED OUTPUT");
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_element_end_modification(void) {
    /* Visitor modifies element at end after children processed */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_element_end = c_visitor_visitor_element_end_modification_visit_element_end;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<blockquote><p>Original quote</p></blockquote>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    /* assertion type 'not_empty' not supported in C visitor tests */

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_element_start_skip_entire_subtree_visit_element_start(
    const HTMHtmNodeContext *_ctx, void *_user_data, char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    return 1;
}

void test_visitor_element_start_skip_entire_subtree(void) {
    /* Visitor skips at element_start level removes entire subtree */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_element_start =
        c_visitor_visitor_element_start_skip_entire_subtree_visit_element_start;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result = htm_convert("<div><h1>Title</h1><p>Content</p></div>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert((_content == NULL || strstr(_content, "Title") == NULL) &&
           "expected NOT to contain substring");
    assert((_content == NULL || strstr(_content, "Content") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_unknown_tag_preservation_visit_custom_element(
    const HTMHtmNodeContext *_ctx, void *_user_data, const char *_tag_name, const char *_html,
    char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    (void)_tag_name;
    (void)_html;
    return 2;
}

void test_visitor_unknown_tag_preservation(void) {
    /* Visitor preserves unknown HTML tags as raw HTML */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_custom_element =
        c_visitor_visitor_unknown_tag_preservation_visit_custom_element;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<article><p>Article text</p><x-custom>Custom element with "
                    "content</x-custom><p>More article text</p></article>",
                    _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "Article text") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "More article text") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "<x-custom>") != NULL &&
           "expected to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_audio_custom_visit_audio(const HTMHtmNodeContext *_ctx,
                                                          void *_user_data, const char *_src,
                                                          char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)_src;
    char *_buf = strdup("[AUDIO: podcast.mp3]");
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_audio_custom(void) {
    /* Visitor replaces audio element with custom output */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_audio = c_visitor_visitor_audio_custom_visit_audio;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result = htm_convert(
        "<p>Listen to this: <audio src=\"podcast.mp3\" controls></audio></p>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "[AUDIO: podcast.mp3]") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "Listen to this:") != NULL &&
           "expected to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_audio_skip_visit_audio(const HTMHtmNodeContext *_ctx,
                                                        void *_user_data, const char *_src,
                                                        char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    (void)_src;
    return 1;
}

void test_visitor_audio_skip(void) {
    /* Visitor removes audio elements from output */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_audio = c_visitor_visitor_audio_skip_visit_audio;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result = htm_convert(
        "<p>Background music:</p><audio src=\"music.ogg\" autoplay></audio><p>Enjoy!</p>",
        _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "Background music:") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "Enjoy!") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "music.ogg") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_button_custom_visit_button(const HTMHtmNodeContext *_ctx,
                                                            void *_user_data, const char *_text,
                                                            char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    char *_buf = (char *)malloc(332);
    if (!_buf) {
        (void)out_custom;
        (void)out_len;
        return 0;
    }
    snprintf(_buf, 332, "[BTN:%s]", (_text ? _text : ""));
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_button_custom(void) {
    /* Visitor replaces button with bracketed text */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_button = c_visitor_visitor_button_custom_visit_button;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<p>Confirm action: <button type=\"submit\">Click me</button> or <button "
                    "type=\"reset\">Cancel</button></p>",
                    _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "[BTN:Click me]") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "[BTN:Cancel]") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "Confirm action:") != NULL &&
           "expected to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_button_skip_visit_button(const HTMHtmNodeContext *_ctx,
                                                          void *_user_data, const char *_text,
                                                          char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    (void)_text;
    return 1;
}

void test_visitor_button_skip(void) {
    /* Visitor removes all buttons from output */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_button = c_visitor_visitor_button_skip_visit_button;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<p>Actions available: <button>Save</button> <button>Delete</button> "
                    "<button>Export</button></p>",
                    _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "Actions available:") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "Save") == NULL) &&
           "expected NOT to contain substring");
    assert((_content == NULL || strstr(_content, "Delete") == NULL) &&
           "expected NOT to contain substring");
    assert((_content == NULL || strstr(_content, "Export") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_continue_default_visit_strong(const HTMHtmNodeContext *_ctx,
                                                               void *_user_data, const char *_text,
                                                               char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    (void)_text;
    return 0;
}

void test_visitor_continue_default(void) {
    /* Visitor continue action preserves default conversion */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_strong = c_visitor_visitor_continue_default_visit_strong;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result = htm_convert("<p>Hello <strong>World</strong></p>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "**World**") != NULL &&
           "expected to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_custom_blockquote_visit_blockquote(
    const HTMHtmNodeContext *_ctx, void *_user_data, const char *_content, size_t _depth,
    char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)_depth;
    char *_buf = (char *)malloc(338);
    if (!_buf) {
        (void)out_custom;
        (void)out_len;
        return 0;
    }
    snprintf(_buf, 338, "QUOTE: \"%s\"", (_content ? _content : ""));
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_custom_blockquote(void) {
    /* Visitor replaces blockquote with custom format */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_blockquote = c_visitor_visitor_custom_blockquote_visit_blockquote;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<blockquote><p>A wise quote.</p></blockquote>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "QUOTE:") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "A wise quote.") != NULL &&
           "expected to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_custom_emphasis_visit_emphasis(const HTMHtmNodeContext *_ctx,
                                                                void *_user_data, const char *_text,
                                                                char **out_custom,
                                                                size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    char *_buf = (char *)malloc(332);
    if (!_buf) {
        (void)out_custom;
        (void)out_len;
        return 0;
    }
    snprintf(_buf, 332, ">>>%s<<<", (_text ? _text : ""));
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_custom_emphasis(void) {
    /* Visitor replaces emphasis with custom output */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_emphasis = c_visitor_visitor_custom_emphasis_visit_emphasis;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result = htm_convert("<p>This is <em>important</em> text.</p>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, ">>>important<<<") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "*important*") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_custom_heading_visit_heading(const HTMHtmNodeContext *_ctx,
                                                              void *_user_data, uint32_t _level,
                                                              const char *_text, const char *_id,
                                                              char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)_level;
    (void)_id;
    char *_buf = (char *)malloc(334);
    if (!_buf) {
        (void)out_custom;
        (void)out_len;
        return 0;
    }
    snprintf(_buf, 334, "--- %s ---", (_text ? _text : ""));
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_custom_heading(void) {
    /* Visitor replaces heading with custom format */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_heading = c_visitor_visitor_custom_heading_visit_heading;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<h2>Section Title</h2><p>Content below heading.</p>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "--- Section Title ---") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "## Section Title") == NULL) &&
           "expected NOT to contain substring");
    assert(_content != NULL && strstr(_content, "Content below heading.") != NULL &&
           "expected to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_custom_image_visit_image(const HTMHtmNodeContext *_ctx,
                                                          void *_user_data, const char *_src,
                                                          const char *_alt, const char *_title,
                                                          char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)_src;
    (void)_title;
    char *_buf = (char *)malloc(334);
    if (!_buf) {
        (void)out_custom;
        (void)out_len;
        return 0;
    }
    snprintf(_buf, 334, "[Image: %s]", (_alt ? _alt : ""));
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_custom_image(void) {
    /* Visitor replaces image with custom output using template */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_image = c_visitor_visitor_custom_image_visit_image;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result = htm_convert("<img src=\"banner.png\" alt=\"Banner\">", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "[Image: Banner]") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "banner.png") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_custom_link_format_visit_link(const HTMHtmNodeContext *_ctx,
                                                               void *_user_data, const char *_href,
                                                               const char *_text,
                                                               const char *_title,
                                                               char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)_title;
    char *_buf = (char *)malloc(591);
    if (!_buf) {
        (void)out_custom;
        (void)out_len;
        return 0;
    }
    snprintf(_buf, 591, "%s (%s)", (_text ? _text : ""), (_href ? _href : ""));
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_custom_link_format(void) {
    /* Visitor reformats links using template interpolation */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_link = c_visitor_visitor_custom_link_format_visit_link;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result = htm_convert(
        "<p>Visit <a href=\"https://example.com\">Example</a> for more info.</p>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "Example (https://example.com)") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "[Example]") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_custom_link_static_visit_link(const HTMHtmNodeContext *_ctx,
                                                               void *_user_data, const char *_href,
                                                               const char *_text,
                                                               const char *_title,
                                                               char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)_href;
    (void)_text;
    (void)_title;
    char *_buf = strdup("[REDACTED LINK]");
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_custom_link_static(void) {
    /* Visitor replaces link with static custom output */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_link = c_visitor_visitor_custom_link_static_visit_link;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<a href=\"https://example.com\">Click here</a>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "[REDACTED LINK]") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "example.com") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_custom_output_visit_heading(const HTMHtmNodeContext *_ctx,
                                                             void *_user_data, uint32_t _level,
                                                             const char *_text, const char *_id,
                                                             char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)_level;
    (void)_text;
    (void)_id;
    char *_buf = strdup("## REPLACED HEADING");
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_custom_output(void) {
    /* Visitor custom action replaces element output */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_heading = c_visitor_visitor_custom_output_visit_heading;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result = htm_convert("<h1>Original Heading</h1>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "## REPLACED HEADING") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "# Original Heading") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t
c_visitor_visitor_definition_list_custom_visit_definition_term(const HTMHtmNodeContext *_ctx,
                                                               void *_user_data, const char *_text,
                                                               char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    char *_buf = (char *)malloc(330);
    if (!_buf) {
        (void)out_custom;
        (void)out_len;
        return 0;
    }
    snprintf(_buf, 330, "**%s**", (_text ? _text : ""));
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_definition_list_custom(void) {
    /* Visitor customizes definition list items */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_definition_term =
        c_visitor_visitor_definition_list_custom_visit_definition_term;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<dl><dt>HTML</dt><dd>HyperText Markup Language</dd><dt>CSS</dt><dd>Cascading "
                    "Style Sheets</dd></dl>",
                    _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "**HTML**") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "**CSS**") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "HyperText Markup Language") != NULL &&
           "expected to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_definition_list_custom_format_visit_definition_description(
    const HTMHtmNodeContext *_ctx, void *_user_data, const char *_text, char **out_custom,
    size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    char *_buf = (char *)malloc(328);
    if (!_buf) {
        (void)out_custom;
        (void)out_len;
        return 0;
    }
    snprintf(_buf, 328, "> %s", (_text ? _text : ""));
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

static int32_t c_visitor_visitor_definition_list_custom_format_visit_definition_term(
    const HTMHtmNodeContext *_ctx, void *_user_data, const char *_text, char **out_custom,
    size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    char *_buf = (char *)malloc(330);
    if (!_buf) {
        (void)out_custom;
        (void)out_len;
        return 0;
    }
    snprintf(_buf, 330, "### %s", (_text ? _text : ""));
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_definition_list_custom_format(void) {
    /* Visitor formats definition lists with custom templates */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_definition_description =
        c_visitor_visitor_definition_list_custom_format_visit_definition_description;
    _callbacks.visit_definition_term =
        c_visitor_visitor_definition_list_custom_format_visit_definition_term;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result = htm_convert(
        "<dl><dt>Python</dt><dd>A high-level programming language</dd><dt>JavaScript</dt><dd>A "
        "scripting language for web browsers</dd></dl>",
        _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "### Python") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "### JavaScript") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "> A high-level programming language") != NULL &&
           "expected to contain substring");
    assert(_content != NULL &&
           strstr(_content, "> A scripting language for web browsers") != NULL &&
           "expected to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_definition_list_skip_visit_definition_description(
    const HTMHtmNodeContext *_ctx, void *_user_data, const char *_text, char **out_custom,
    size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    (void)_text;
    return 1;
}

static int32_t
c_visitor_visitor_definition_list_skip_visit_definition_term(const HTMHtmNodeContext *_ctx,
                                                             void *_user_data, const char *_text,
                                                             char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    (void)_text;
    return 1;
}

void test_visitor_definition_list_skip(void) {
    /* Visitor skips definition list items from output */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_definition_description =
        c_visitor_visitor_definition_list_skip_visit_definition_description;
    _callbacks.visit_definition_term = c_visitor_visitor_definition_list_skip_visit_definition_term;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<p>Glossary:</p><dl><dt>Term A</dt><dd>Definition of term A</dd><dt>Term "
                    "B</dt><dd>Definition of term B</dd></dl><p>End of glossary</p>",
                    _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "Glossary:") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "End of glossary") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "Term A") == NULL) &&
           "expected NOT to contain substring");
    assert((_content == NULL || strstr(_content, "Definition") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_details_summary_custom_visit_summary(const HTMHtmNodeContext *_ctx,
                                                                      void *_user_data,
                                                                      const char *_text,
                                                                      char **out_custom,
                                                                      size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    char *_buf = (char *)malloc(339);
    if (!_buf) {
        (void)out_custom;
        (void)out_len;
        return 0;
    }
    snprintf(_buf, 339, "[EXPANDABLE] %s", (_text ? _text : ""));
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_details_summary_custom(void) {
    /* Visitor customizes details/summary disclosure elements */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_summary = c_visitor_visitor_details_summary_custom_visit_summary;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<details><summary>Click to expand</summary><p>This content is initially "
                    "hidden.</p><p>But can be revealed by the user.</p></details>",
                    _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "[EXPANDABLE] Click to expand") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "This content is initially hidden.") != NULL &&
           "expected to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_details_summary_skip_visit_details(const HTMHtmNodeContext *_ctx,
                                                                    void *_user_data, int32_t _open,
                                                                    char **out_custom,
                                                                    size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    (void)_open;
    return 1;
}

void test_visitor_details_summary_skip(void) {
    /* Visitor removes details/summary elements entirely */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_details = c_visitor_visitor_details_summary_skip_visit_details;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<p>Main content here.</p><details><summary>Hidden section</summary><p>Secret "
                    "details</p></details><p>More main content.</p>",
                    _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "Main content here.") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "More main content.") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "Hidden section") == NULL) &&
           "expected NOT to contain substring");
    assert((_content == NULL || strstr(_content, "Secret details") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_figure_custom_visit_figcaption(const HTMHtmNodeContext *_ctx,
                                                                void *_user_data, const char *_text,
                                                                char **out_custom,
                                                                size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    char *_buf = (char *)malloc(328);
    if (!_buf) {
        (void)out_custom;
        (void)out_len;
        return 0;
    }
    snprintf(_buf, 328, "*%s*", (_text ? _text : ""));
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_figure_custom(void) {
    /* Visitor customizes figure and figcaption elements */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_figcaption = c_visitor_visitor_figure_custom_visit_figcaption;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result = htm_convert(
        "<article><h1>Article Title</h1><p>Introduction paragraph.</p><figure><img "
        "src=\"diagram.png\" alt=\"System architecture diagram\"><figcaption>Figure 1: System "
        "Architecture</figcaption></figure><p>Explanation of the figure.</p></article>",
        _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "Article Title") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "*Figure 1: System Architecture*") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "Explanation of the figure.") != NULL &&
           "expected to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_figure_custom_wrap_visit_figure_end(const HTMHtmNodeContext *_ctx,
                                                                     void *_user_data,
                                                                     const char *_output,
                                                                     char **out_custom,
                                                                     size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    char *_buf = (char *)malloc(339);
    if (!_buf) {
        (void)out_custom;
        (void)out_len;
        return 0;
    }
    snprintf(_buf, 339, "%s\n[/FIGURE]\n", (_output ? _output : ""));
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

static int32_t c_visitor_visitor_figure_custom_wrap_visit_figure_start(
    const HTMHtmNodeContext *_ctx, void *_user_data, char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    char *_buf = strdup("\n[FIGURE]\n");
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_figure_custom_wrap(void) {
    /* Visitor wraps figure content with custom formatting */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_figure_end = c_visitor_visitor_figure_custom_wrap_visit_figure_end;
    _callbacks.visit_figure_start = c_visitor_visitor_figure_custom_wrap_visit_figure_start;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<section><h2>Gallery</h2><figure><img src=\"photo1.jpg\" "
                    "alt=\"Photo\"><figcaption>Beautiful sunset</figcaption></figure></section>",
                    _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "[FIGURE]") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "[/FIGURE]") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "Gallery") != NULL &&
           "expected to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_figure_skip_visit_figure_start(const HTMHtmNodeContext *_ctx,
                                                                void *_user_data, char **out_custom,
                                                                size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    return 1;
}

void test_visitor_figure_skip(void) {
    /* Visitor removes figure elements with their captions */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_figure_start = c_visitor_visitor_figure_skip_visit_figure_start;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<p>See the chart below:</p><figure><img src=\"chart.svg\"><figcaption>Revenue "
                    "Trends 2020-2024</figcaption></figure><p>As shown in the chart above.</p>",
                    _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "See the chart below:") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "As shown in the chart above.") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "Revenue Trends") == NULL) &&
           "expected NOT to contain substring");
    assert((_content == NULL || strstr(_content, "chart.svg") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_form_custom_visit_form(const HTMHtmNodeContext *_ctx,
                                                        void *_user_data, const char *_action,
                                                        const char *_method, char **out_custom,
                                                        size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)_action;
    (void)_method;
    char *_buf = strdup("[FORM PLACEHOLDER]");
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_form_custom(void) {
    /* Visitor replaces form with custom output */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_form = c_visitor_visitor_form_custom_visit_form;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result = htm_convert(
        "<div><form action=\"/submit\" method=\"POST\"><label>Name: <input type=\"text\" "
        "name=\"name\"></label><button type=\"submit\">Submit</button></form></div>",
        _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "[FORM PLACEHOLDER]") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "submit") == NULL) &&
           "expected NOT to contain substring");
    assert((_content == NULL || strstr(_content, "input") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_form_skip_visit_form(const HTMHtmNodeContext *_ctx,
                                                      void *_user_data, const char *_action,
                                                      const char *_method, char **out_custom,
                                                      size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    (void)_action;
    (void)_method;
    return 1;
}

void test_visitor_form_skip(void) {
    /* Visitor skips form elements entirely */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_form = c_visitor_visitor_form_skip_visit_form;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result = htm_convert(
        "<p>Before form</p><form><input type=\"email\" name=\"email\"></form><p>After form</p>",
        _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "Before form") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "After form") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "email") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_heading_bare_string_preserves_case_visit_heading(
    const HTMHtmNodeContext *_ctx, void *_user_data, uint32_t _level, const char *_text,
    const char *_id, char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)_level;
    (void)_id;
    char *_buf = (char *)malloc(332);
    if (!_buf) {
        (void)out_custom;
        (void)out_len;
        return 0;
    }
    snprintf(_buf, 332, "## %s ##", (_text ? _text : ""));
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_heading_bare_string_preserves_case(void) {
    /* Visitor returns rendered heading template as bare string; mixed-case is preserved (regression
     * guard for issue #350) */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_heading = c_visitor_visitor_heading_bare_string_preserves_case_visit_heading;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<h2>Important Section Title</h2><p>Body.</p>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "## Important Section Title ##") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "important section title") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_horizontal_rule_custom_visit_horizontal_rule(
    const HTMHtmNodeContext *_ctx, void *_user_data, char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    char *_buf = strdup("\n[DIVIDER]\n");
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_horizontal_rule_custom(void) {
    /* Visitor replaces horizontal rule with custom output */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_horizontal_rule =
        c_visitor_visitor_horizontal_rule_custom_visit_horizontal_rule;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result = htm_convert(
        "<h1>Section A</h1><p>Content A</p><hr><h1>Section B</h1><p>Content B</p>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "[DIVIDER]") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "Section A") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "Section B") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "---") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_horizontal_rule_skip_visit_horizontal_rule(
    const HTMHtmNodeContext *_ctx, void *_user_data, char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    return 1;
}

void test_visitor_horizontal_rule_skip(void) {
    /* Visitor removes all horizontal rules */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_horizontal_rule = c_visitor_visitor_horizontal_rule_skip_visit_horizontal_rule;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<p>Part 1</p><hr><p>Part 2</p><hr><p>Part 3</p>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "Part 1") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "Part 2") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "Part 3") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "---") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_iframe_custom_visit_iframe(const HTMHtmNodeContext *_ctx,
                                                            void *_user_data, const char *_src,
                                                            char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)_src;
    char *_buf = strdup("[EMBEDDED: https://maps.example.com/embed]");
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_iframe_custom(void) {
    /* Visitor replaces embedded iframe with custom text */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_iframe = c_visitor_visitor_iframe_custom_visit_iframe;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<p>Embedded map:</p><iframe src=\"https://maps.example.com/embed\" "
                    "width=\"400\" height=\"300\"></iframe><p>End of map</p>",
                    _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL &&
           strstr(_content, "[EMBEDDED: https://maps.example.com/embed]") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "Embedded map:") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "End of map") != NULL &&
           "expected to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_iframe_skip_visit_iframe(const HTMHtmNodeContext *_ctx,
                                                          void *_user_data, const char *_src,
                                                          char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    (void)_src;
    return 1;
}

void test_visitor_iframe_skip(void) {
    /* Visitor removes embedded iframes */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_iframe = c_visitor_visitor_iframe_skip_visit_iframe;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result = htm_convert(
        "<h3>Reviews</h3><iframe src=\"https://widget.example.com/reviews\"></iframe><p>See "
        "reviews from our partners.</p>",
        _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "Reviews") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "See reviews from our partners.") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "widget.example.com") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_image_bare_string_preserves_case_visit_image(
    const HTMHtmNodeContext *_ctx, void *_user_data, const char *_src, const char *_alt,
    const char *_title, char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)_title;
    char *_buf = (char *)malloc(599);
    if (!_buf) {
        (void)out_custom;
        (void)out_len;
        return 0;
    }
    snprintf(_buf, 599, "[image: %s -> %s]", (_alt ? _alt : ""), (_src ? _src : ""));
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_image_bare_string_preserves_case(void) {
    /* Visitor returns bare-string image replacement; mixed-case alt/src preserved (regression guard
     * for issue #350) */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_image = c_visitor_visitor_image_bare_string_preserves_case_visit_image;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<img src=\"PhotoOne.JPG\" alt=\"Sunset Over Bay\">", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL &&
           strstr(_content, "[image: Sunset Over Bay -> PhotoOne.JPG]") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "sunset over bay") == NULL) &&
           "expected NOT to contain substring");
    assert((_content == NULL || strstr(_content, "photoone.jpg") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_input_custom_visit_input(const HTMHtmNodeContext *_ctx,
                                                          void *_user_data, const char *_input_type,
                                                          const char *_name, const char *_value,
                                                          char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)_name;
    (void)_value;
    char *_buf = (char *)malloc(340);
    if (!_buf) {
        (void)out_custom;
        (void)out_len;
        return 0;
    }
    snprintf(_buf, 340, "[INPUT:%s]", (_input_type ? _input_type : ""));
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_input_custom(void) {
    /* Visitor replaces input with labeled output */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_input = c_visitor_visitor_input_custom_visit_input;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<form><label>Username: <input type=\"text\" name=\"username\" "
                    "value=\"\"></label><label>Password: <input type=\"password\" "
                    "name=\"password\"></label></form>",
                    _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "[INPUT:text]") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "[INPUT:password]") != NULL &&
           "expected to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_input_skip_visit_input(const HTMHtmNodeContext *_ctx,
                                                        void *_user_data, const char *_input_type,
                                                        const char *_name, const char *_value,
                                                        char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    (void)_input_type;
    (void)_name;
    (void)_value;
    return 1;
}

void test_visitor_input_skip(void) {
    /* Visitor skips all input elements */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_input = c_visitor_visitor_input_skip_visit_input;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result = htm_convert(
        "<p>Sign up:</p><input type=\"text\" name=\"email\" placeholder=\"your@email.com\"><input "
        "type=\"checkbox\" name=\"agree\"><p>Continue</p>",
        _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "Sign up:") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "Continue") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "email") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_line_break_custom_visit_line_break(const HTMHtmNodeContext *_ctx,
                                                                    void *_user_data,
                                                                    char **out_custom,
                                                                    size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    char *_buf = strdup(" | ");
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_line_break_custom(void) {
    /* Visitor replaces line break with custom output */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_line_break = c_visitor_visitor_line_break_custom_visit_line_break;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<p>First line<br>Second line<br>Third line</p>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "First line | Second line | Third line") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "\n\n") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_line_break_skip_visit_line_break(const HTMHtmNodeContext *_ctx,
                                                                  void *_user_data,
                                                                  char **out_custom,
                                                                  size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    return 1;
}

void test_visitor_line_break_skip(void) {
    /* Visitor removes all line breaks */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_line_break = c_visitor_visitor_line_break_skip_visit_line_break;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<p>Address Line 1<br>Address Line 2<br>Address Line 3</p>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL &&
           strstr(_content, "Address Line 1Address Line 2Address Line 3") != NULL &&
           "expected to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_link_bare_string_preserves_case_visit_link(
    const HTMHtmNodeContext *_ctx, void *_user_data, const char *_href, const char *_text,
    const char *_title, char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)_href;
    (void)_title;
    char *_buf = (char *)malloc(358);
    if (!_buf) {
        (void)out_custom;
        (void)out_len;
        return 0;
    }
    snprintf(_buf, 358, "[%s](https://new-cdn.com/file.pdf)", (_text ? _text : ""));
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_link_bare_string_preserves_case(void) {
    /* Visitor returns rendered template as a bare string (not a dict) and original mixed-case
     * content is preserved end-to-end (regression guard for issue #350) */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_link = c_visitor_visitor_link_bare_string_preserves_case_visit_link;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<a href=\"https://old-cdn.com/file.pdf\">Download</a>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL &&
           strstr(_content, "[Download](https://new-cdn.com/file.pdf)") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "[download]") == NULL) &&
           "expected NOT to contain substring");
    assert((_content == NULL || strstr(_content, "old-cdn.com") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_mark_custom_visit_mark(const HTMHtmNodeContext *_ctx,
                                                        void *_user_data, const char *_text,
                                                        char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    char *_buf = (char *)malloc(330);
    if (!_buf) {
        (void)out_custom;
        (void)out_len;
        return 0;
    }
    snprintf(_buf, 330, "==%s==", (_text ? _text : ""));
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_mark_custom(void) {
    /* Visitor replaces highlight/mark with custom template */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_mark = c_visitor_visitor_mark_custom_visit_mark;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<p>This is a <mark>highlighted passage</mark> in the text.</p>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "==highlighted passage==") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "This is a") != NULL &&
           "expected to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_mark_skip_visit_mark(const HTMHtmNodeContext *_ctx,
                                                      void *_user_data, const char *_text,
                                                      char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    (void)_text;
    return 1;
}

void test_visitor_mark_skip(void) {
    /* Visitor skips mark elements entirely */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_mark = c_visitor_visitor_mark_skip_visit_mark;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result = htm_convert(
        "<p>Key insight: <mark>always validate input</mark> for security.</p>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert((_content == NULL || strstr(_content, "always validate input") == NULL) &&
           "expected NOT to contain substring");
    assert(_content != NULL && strstr(_content, "Key insight:") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "for security.") != NULL &&
           "expected to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_preserve_html_visit_custom_element(
    const HTMHtmNodeContext *_ctx, void *_user_data, const char *_tag_name, const char *_html,
    char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    (void)_tag_name;
    (void)_html;
    return 2;
}

void test_visitor_preserve_html(void) {
    /* Visitor preserve_html action includes raw HTML in output */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_custom_element = c_visitor_visitor_preserve_html_visit_custom_element;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<div><custom-tag>Custom content</custom-tag></div>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "<custom-tag>") != NULL &&
           "expected to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t
c_visitor_visitor_skip_code_blocks_visit_code_block(const HTMHtmNodeContext *_ctx, void *_user_data,
                                                    const char *_lang, const char *_code,
                                                    char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    (void)_lang;
    (void)_code;
    return 1;
}

void test_visitor_skip_code_blocks(void) {
    /* Visitor skips code blocks from output */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_code_block = c_visitor_visitor_skip_code_blocks_visit_code_block;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result = htm_convert(
        "<p>Intro text</p><pre><code>let x = 42;</code></pre><p>Outro text</p>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "Intro text") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "Outro text") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "let x = 42") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_skip_heading_visit_heading(const HTMHtmNodeContext *_ctx,
                                                            void *_user_data, uint32_t _level,
                                                            const char *_text, const char *_id,
                                                            char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    (void)_level;
    (void)_text;
    (void)_id;
    return 1;
}

void test_visitor_skip_heading(void) {
    /* Visitor skip action omits all headings from output */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_heading = c_visitor_visitor_skip_heading_visit_heading;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result = htm_convert("<h1>Title</h1><p>Body text remains.</p>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert((_content == NULL || strstr(_content, "Title") == NULL) &&
           "expected NOT to contain substring");
    assert(_content != NULL && strstr(_content, "Body text remains.") != NULL &&
           "expected to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_skip_images_visit_image(const HTMHtmNodeContext *_ctx,
                                                         void *_user_data, const char *_src,
                                                         const char *_alt, const char *_title,
                                                         char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    (void)_src;
    (void)_alt;
    (void)_title;
    return 1;
}

void test_visitor_skip_images(void) {
    /* Visitor skips all images from output */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_image = c_visitor_visitor_skip_images_visit_image;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result = htm_convert(
        "<p>Before image</p><img src=\"photo.jpg\" alt=\"A photo\"><p>After image</p>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "Before image") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "After image") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "photo.jpg") == NULL) &&
           "expected NOT to contain substring");
    assert((_content == NULL || strstr(_content, "A photo") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_skip_links_visit_link(const HTMHtmNodeContext *_ctx,
                                                       void *_user_data, const char *_href,
                                                       const char *_text, const char *_title,
                                                       char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    (void)_href;
    (void)_text;
    (void)_title;
    return 1;
}

void test_visitor_skip_links(void) {
    /* Visitor skips all links entirely */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_link = c_visitor_visitor_skip_links_visit_link;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<p>Before <a href=\"https://example.com\">link text</a> after</p>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert((_content == NULL || strstr(_content, "link text") == NULL) &&
           "expected NOT to contain substring");
    assert((_content == NULL || strstr(_content, "example.com") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_skip_strong_visit_strong(const HTMHtmNodeContext *_ctx,
                                                          void *_user_data, const char *_text,
                                                          char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    (void)_text;
    return 1;
}

void test_visitor_skip_strong(void) {
    /* Visitor skips bold/strong elements */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_strong = c_visitor_visitor_skip_strong_visit_strong;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<p>Normal <strong>bold text</strong> normal</p>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert((_content == NULL || strstr(_content, "bold text") == NULL) &&
           "expected NOT to contain substring");
    assert(_content != NULL && strstr(_content, "Normal") != NULL &&
           "expected to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_subscript_custom_visit_subscript(const HTMHtmNodeContext *_ctx,
                                                                  void *_user_data,
                                                                  const char *_text,
                                                                  char **out_custom,
                                                                  size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    char *_buf = (char *)malloc(328);
    if (!_buf) {
        (void)out_custom;
        (void)out_len;
        return 0;
    }
    snprintf(_buf, 328, "~%s~", (_text ? _text : ""));
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_subscript_custom(void) {
    /* Visitor replaces subscript with custom template */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_subscript = c_visitor_visitor_subscript_custom_visit_subscript;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result = htm_convert("<p>H<sub>2</sub>O is water.</p>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "H~2~O") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "is water") != NULL &&
           "expected to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_subscript_skip_visit_subscript(const HTMHtmNodeContext *_ctx,
                                                                void *_user_data, const char *_text,
                                                                char **out_custom,
                                                                size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    (void)_text;
    return 1;
}

void test_visitor_subscript_skip(void) {
    /* Visitor skips subscript elements entirely */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_subscript = c_visitor_visitor_subscript_skip_visit_subscript;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result = htm_convert(
        "<p>The formula C<sub>12</sub>H<sub>22</sub>O<sub>11</sub> is sugar.</p>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "The formula CHO is sugar.") != NULL &&
           "expected to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_superscript_custom_visit_superscript(const HTMHtmNodeContext *_ctx,
                                                                      void *_user_data,
                                                                      const char *_text,
                                                                      char **out_custom,
                                                                      size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    char *_buf = (char *)malloc(328);
    if (!_buf) {
        (void)out_custom;
        (void)out_len;
        return 0;
    }
    snprintf(_buf, 328, "^%s^", (_text ? _text : ""));
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_superscript_custom(void) {
    /* Visitor replaces superscript with custom template */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_superscript = c_visitor_visitor_superscript_custom_visit_superscript;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<p>Einstein's E=mc<sup>2</sup> revolutionized physics.</p>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "E=mc^2^") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "revolutionized physics") != NULL &&
           "expected to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_superscript_skip_visit_superscript(const HTMHtmNodeContext *_ctx,
                                                                    void *_user_data,
                                                                    const char *_text,
                                                                    char **out_custom,
                                                                    size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    (void)_text;
    return 1;
}

void test_visitor_superscript_skip(void) {
    /* Visitor skips superscript from output */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_superscript = c_visitor_visitor_superscript_skip_visit_superscript;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result = htm_convert(
        "<p>The equation x<sup>3</sup> + y<sup>3</sup> = z<sup>3</sup> has no solutions.</p>",
        _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL &&
           strstr(_content, "The equation x + y = z has no solutions.") != NULL &&
           "expected to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_underline_custom_visit_underline(const HTMHtmNodeContext *_ctx,
                                                                  void *_user_data,
                                                                  const char *_text,
                                                                  char **out_custom,
                                                                  size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    char *_buf = (char *)malloc(328);
    if (!_buf) {
        (void)out_custom;
        (void)out_len;
        return 0;
    }
    snprintf(_buf, 328, "_%s_", (_text ? _text : ""));
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_underline_custom(void) {
    /* Visitor replaces underline with custom markup */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_underline = c_visitor_visitor_underline_custom_visit_underline;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<p>This is <u>very important</u> text.</p>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "_very important_") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "**") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_underline_skip_visit_underline(const HTMHtmNodeContext *_ctx,
                                                                void *_user_data, const char *_text,
                                                                char **out_custom,
                                                                size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    (void)_text;
    return 1;
}

void test_visitor_underline_skip(void) {
    /* Visitor skips underline elements from output */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_underline = c_visitor_visitor_underline_skip_visit_underline;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<p>Normal text with <u>underlined part</u> and more text.</p>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "Normal text with") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "and more text.") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "underlined part") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_video_custom_visit_video(const HTMHtmNodeContext *_ctx,
                                                          void *_user_data, const char *_src,
                                                          char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    char *_buf = (char *)malloc(334);
    if (!_buf) {
        (void)out_custom;
        (void)out_len;
        return 0;
    }
    snprintf(_buf, 334, "[VIDEO: %s]", (_src ? _src : ""));
    if (out_custom)
        *out_custom = _buf;
    if (out_len)
        *out_len = _buf ? strlen(_buf) : 0;
    return 3;
}

void test_visitor_video_custom(void) {
    /* Visitor replaces video with custom link */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_video = c_visitor_visitor_video_custom_visit_video;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result =
        htm_convert("<p>Watch our tutorial:</p><video src=\"tutorial.mp4\" width=\"320\" "
                    "height=\"240\" controls></video><p>Great content!</p>",
                    _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "[VIDEO: tutorial.mp4]") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "Watch our tutorial:") != NULL &&
           "expected to contain substring");
    assert(_content != NULL && strstr(_content, "Great content!") != NULL &&
           "expected to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}

static int32_t c_visitor_visitor_video_skip_visit_video(const HTMHtmNodeContext *_ctx,
                                                        void *_user_data, const char *_src,
                                                        char **out_custom, size_t *out_len) {
    (void)_ctx;
    (void)_user_data;
    (void)out_custom;
    (void)out_len;
    (void)_src;
    return 1;
}

void test_visitor_video_skip(void) {
    /* Visitor removes video elements entirely */

    HTMHtmVisitorCallbacks _callbacks;
    memset(&_callbacks, 0, sizeof(_callbacks));
    _callbacks.visit_video = c_visitor_visitor_video_skip_visit_video;

    HTMHtmVisitor *_visitor = htm_visitor_create(&_callbacks);
    assert(_visitor != NULL && "htm_visitor_create failed");

    HTMConversionOptions *_options = htm_conversion_options_from_json("{}");
    assert(_options != NULL && "htm_conversion_options_from_json failed");

    htm_options_set_visitor_handle(_options, _visitor);

    HTMConversionResult *_result = htm_convert(
        "<h2>Demo</h2><video src=\"demo.webm\"></video><p>See the demo above.</p>", _options);
    assert(_result != NULL && "htm_convert failed");

    char *_json = htm_conversion_result_to_json(_result);
    assert(_json != NULL && "result to_json failed");
    char *_content = alef_json_get_string(_json, "content");

    assert(_content != NULL && strstr(_content, "Demo") != NULL && "expected to contain substring");
    assert(_content != NULL && strstr(_content, "See the demo above.") != NULL &&
           "expected to contain substring");
    assert((_content == NULL || strstr(_content, "demo.webm") == NULL) &&
           "expected NOT to contain substring");

    free(_content);
    htm_free_string(_json);
    htm_conversion_result_free(_result);
    htm_conversion_options_free(_options);
    htm_visitor_free(_visitor);
}
