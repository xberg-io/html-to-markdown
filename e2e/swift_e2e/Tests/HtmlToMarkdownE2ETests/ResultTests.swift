// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:4f4f53fb37254412e081692ad2cc2e861135e346919363b8a395e1327f9fdab2
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import HtmlToMarkdown
import RustBridge

/// E2e tests for category: result.
final class ResultTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        let _existing = ProcessInfo.processInfo.environment["SUT_URL"]
    }

    func testResultTablesEmptyWhenNoTables() throws {
        // Result tables array is empty when input has no tables
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"include_document_structure\":true}")
        let result = try HtmlToMarkdown.convert(html: "<p>No tables here</p>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertEqual(result.tables().count, 0)
    }

    func testResultTablesMultiple() throws {
        // Multiple tables each appear in the tables array
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"include_document_structure\":true}")
        let result = try HtmlToMarkdown.convert(html: "<table><tr><th>A</th></tr><tr><td>1</td></tr></table><p>Between</p><table><tr><th>B</th></tr><tr><td>2</td></tr></table>", options: _options)
        XCTAssertGreaterThanOrEqual(result.tables().count, 2)
    }

    func testResultTablesSimple() throws {
        // Simple table populates the tables array in result
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"include_document_structure\":true}")
        let result = try HtmlToMarkdown.convert(html: "<table><thead><tr><th>Name</th><th>Age</th></tr></thead><tbody><tr><td>Alice</td><td>30</td></tr></tbody></table>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertGreaterThanOrEqual(result.tables().count, 1)
    }

    func testResultTablesWithoutStructureFlag() throws {
        // Tables array is empty when includeDocumentStructure is false
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<table><tr><th>X</th></tr><tr><td>Y</td></tr></table>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertEqual(result.tables().count, 0)
    }

    func testResultWarningKindImageExtractionFailed() throws {
        // A malformed data URI with extract_images enabled produces an ImageExtractionFailed warning
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"extract_images\":true}")
        let result = try HtmlToMarkdown.convert(html: "<p>Text<img src=\"data:BADMIME\" alt=\"broken\">end</p>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertGreaterThanOrEqual(result.warnings().count, 1)
    }

    func testResultWarningsEmptyForCleanInput() throws {
        // Warnings array is empty for well-formed HTML without problematic content
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<h1>Title</h1><p>Clean content with <a href='https://example.com'>a link</a>.</p>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertEqual(result.warnings().count, 0)
    }

    func testResultWarningsEmptyForComplexInput() throws {
        // Warnings array is empty for complex but valid HTML
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<article><h1>Article</h1><p>Paragraph with <strong>bold</strong> and <em>italic</em>.</p><table><tr><th>Col</th></tr><tr><td>Val</td></tr></table><ul><li>Item 1</li><li>Item 2</li></ul></article>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertEqual(result.warnings().count, 0)
    }

    func testResultWarningsEmptyForMalformedHtml() throws {
        // Warnings array is empty even for malformed HTML (parser is lenient)
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p>Unclosed paragraph<div>Mixed nesting</p></div>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertEqual(result.warnings().count, 0)
    }

}
