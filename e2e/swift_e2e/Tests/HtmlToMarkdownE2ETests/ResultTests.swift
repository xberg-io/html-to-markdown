// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:cbf3ace62c32a2c28d87c906449e0e41189d0cbc981e84f41698f269e030bb1a
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import HtmlToMarkdown
import RustBridge

/// E2e tests for category: result.
final class ResultTests: XCTestCase {
    func testResultTablesEmptyWhenNoTables() throws {
        // Result tables array is empty when input has no tables
        let _options = try conversionOptionsFromJson("{\"include_document_structure\":true}")
        let result = try convert("<p>No tables here</p>", _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertEqual(result.tables().count, 0)
    }

    func testResultTablesMultiple() throws {
        // Multiple tables each appear in the tables array
        let _options = try conversionOptionsFromJson("{\"include_document_structure\":true}")
        let result = try convert("<table><tr><th>A</th></tr><tr><td>1</td></tr></table><p>Between</p><table><tr><th>B</th></tr><tr><td>2</td></tr></table>", _options)
        XCTAssertGreaterThanOrEqual(result.tables().count, 2)
    }

    func testResultTablesSimple() throws {
        // Simple table populates the tables array in result
        let _options = try conversionOptionsFromJson("{\"include_document_structure\":true}")
        let result = try convert("<table><thead><tr><th>Name</th><th>Age</th></tr></thead><tbody><tr><td>Alice</td><td>30</td></tr></tbody></table>", _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertGreaterThanOrEqual(result.tables().count, 1)
    }

    func testResultTablesWithoutStructureFlag() throws {
        // Tables array is empty when includeDocumentStructure is false
        let _options = try conversionOptionsFromJson("{}")
        let result = try convert("<table><tr><th>X</th></tr><tr><td>Y</td></tr></table>", _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertEqual(result.tables().count, 0)
    }

    func testResultWarningsEmptyForCleanInput() throws {
        // Warnings array is empty for well-formed HTML without problematic content
        let _options = try conversionOptionsFromJson("{}")
        let result = try convert("<h1>Title</h1><p>Clean content with <a href='https://example.com'>a link</a>.</p>", _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertEqual(result.warnings().count, 0)
    }

    func testResultWarningsEmptyForComplexInput() throws {
        // Warnings array is empty for complex but valid HTML
        let _options = try conversionOptionsFromJson("{}")
        let result = try convert("<article><h1>Article</h1><p>Paragraph with <strong>bold</strong> and <em>italic</em>.</p><table><tr><th>Col</th></tr><tr><td>Val</td></tr></table><ul><li>Item 1</li><li>Item 2</li></ul></article>", _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertEqual(result.warnings().count, 0)
    }

    func testResultWarningsEmptyForMalformedHtml() throws {
        // Warnings array is empty even for malformed HTML (parser is lenient)
        let _options = try conversionOptionsFromJson("{}")
        let result = try convert("<p>Unclosed paragraph<div>Mixed nesting</p></div>", _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertEqual(result.warnings().count, 0)
    }

}
