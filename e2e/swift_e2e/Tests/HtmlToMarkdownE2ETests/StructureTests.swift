// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:e4f187625b722f041d5e8c0170a51839e05f8a27c2546831b8ddb493fdfd8634
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import HtmlToMarkdown
import RustBridge

/// E2e tests for category: structure.
final class StructureTests: XCTestCase {
    func testStructureCodeBlock() throws {
        // Fenced code block produces Code node
        let _options = try conversionOptionsFromJson("{\"include_document_structure\":true}")
        let result = try convert("<p>Example code:</p><pre><code class=\"language-rust\">fn main() { println!(\"Hello\"); }</code></pre>", _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertGreaterThan((result.document()?.nodes().len() ?? 0), 0, "expected non-empty value")
        XCTAssertGreaterThanOrEqual(result.document()?.nodes().count ?? 0, 2)
    }

    func testStructureDeepNestingH1H2H3() throws {
        // H1 > H2 > H3 creates three levels of heading nesting
        let _options = try conversionOptionsFromJson("{\"include_document_structure\":true}")
        let result = try convert("<h1>Top Level</h1><p>Top intro.</p><h2>Mid Level</h2><p>Mid content.</p><h3>Deep Level</h3><p>Deep content.</p>", _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertGreaterThan((result.document()?.nodes().len() ?? 0), 0, "expected non-empty value")
        XCTAssertGreaterThanOrEqual(result.document()?.nodes().count ?? 0, 5)
    }

    func testStructureH1H2NestedGroup() throws {
        // H1 followed by H2 creates a nested group under the H1
        let _options = try conversionOptionsFromJson("{\"include_document_structure\":true}")
        let result = try convert("<h1>Chapter One</h1><p>Chapter intro.</p><h2>Section One</h2><p>Section content.</p>", _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertGreaterThan((result.document()?.nodes().len() ?? 0), 0, "expected non-empty value")
        XCTAssertGreaterThanOrEqual(result.document()?.nodes().count ?? 0, 3)
    }

    func testStructureHeadingParagraph() throws {
        // Simple heading followed by paragraph produces Heading and Paragraph nodes
        let _options = try conversionOptionsFromJson("{\"include_document_structure\":true}")
        let result = try convert("<h1>Title</h1><p>A paragraph of text.</p>", _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertGreaterThan((result.document()?.nodes().len() ?? 0), 0, "expected non-empty value")
        XCTAssertGreaterThanOrEqual(result.document()?.nodes().count ?? 0, 2)
    }

    func testStructureList() throws {
        // Unordered list produces List and ListItem nodes
        let _options = try conversionOptionsFromJson("{\"include_document_structure\":true}")
        let result = try convert("<p>Items:</p><ul><li>Alpha</li><li>Beta</li><li>Gamma</li></ul>", _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertGreaterThan((result.document()?.nodes().len() ?? 0), 0, "expected non-empty value")
        XCTAssertGreaterThanOrEqual(result.document()?.nodes().count ?? 0, 2)
    }

    func testStructureMultipleHeadings() throws {
        // Multiple headings create multiple Heading nodes with correct levels
        let _options = try conversionOptionsFromJson("{\"include_document_structure\":true}")
        let result = try convert("<h1>Main Title</h1><h2>Section One</h2><p>Section one content.</p><h2>Section Two</h2><p>Section two content.</p>", _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertGreaterThan((result.document()?.nodes().len() ?? 0), 0, "expected non-empty value")
        XCTAssertGreaterThanOrEqual(result.document()?.nodes().count ?? 0, 4)
    }

    func testStructureSiblingH1Groups() throws {
        // H1, H2, then another H1 creates two sibling top-level groups
        let _options = try conversionOptionsFromJson("{\"include_document_structure\":true}")
        let result = try convert("<h1>Chapter One</h1><h2>Section A</h2><p>Section A content.</p><h1>Chapter Two</h1><h2>Section B</h2><p>Section B content.</p>", _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertGreaterThan((result.document()?.nodes().len() ?? 0), 0, "expected non-empty value")
        XCTAssertGreaterThanOrEqual(result.document()?.nodes().count ?? 0, 4)
    }

}
