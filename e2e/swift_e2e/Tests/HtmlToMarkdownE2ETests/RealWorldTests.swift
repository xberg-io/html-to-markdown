// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:53a3ae6f5fe1549a1e0808c48fb0a775bb8cd09589009bc26f76717f346b4ac1
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import HtmlToMarkdown
import RustBridge

/// E2e tests for category: real-world.
final class RealWorldTests: XCTestCase {
    func testRealWorldBlogPost() throws {
        // Blog post with headings, paragraphs, code blocks, and links converts to readable Markdown
        let _options = try conversionOptionsFromJson("{}")
        let result = try convert("<article><h1>Getting Started with Rust</h1><p>Rust is a systems programming language focused on <strong>safety</strong>, <em>performance</em>, and concurrency. It was created by <a href=\"https://www.mozilla.org\">Mozilla</a> and has grown significantly in popularity.</p><h2>Installation</h2><p>Install Rust using the official installer:</p><pre><code class=\"language-bash\">curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh</code></pre><h2>Hello World</h2><p>Create your first Rust program:</p><pre><code class=\"language-rust\">fn main() {\n    println!(\"Hello, world!\");\n}</code></pre><p>Run it with <code>cargo run</code> from your project directory.</p><h2>Key Concepts</h2><ul><li>Ownership and borrowing</li><li>Lifetimes</li><li>Traits and generics</li><li>Pattern matching</li></ul><p>For more information, visit the <a href=\"https://doc.rust-lang.org/book/\">Rust Book</a>.</p></article>", _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("# Getting Started with Rust"), "expected to contain: \("# Getting Started with Rust")")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("## Installation"), "expected to contain: \("## Installation")")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("## Hello World"), "expected to contain: \("## Hello World")")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("## Key Concepts"), "expected to contain: \("## Key Concepts")")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("cargo run"), "expected to contain: \("cargo run")")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("[Mozilla](https://www.mozilla.org)"), "expected to contain: \("[Mozilla](https://www.mozilla.org)")")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("- Ownership and borrowing"), "expected to contain: \("- Ownership and borrowing")")
    }

    func testRealWorldDocumentationPage() throws {
        // Documentation page with nested lists, code examples, and blockquotes converts correctly
        let _options = try conversionOptionsFromJson("{}")
        let result = try convert("<div class=\"docs\"><h1>API Reference</h1><p>This guide covers the core API for the <code>html-to-markdown</code> library.</p><blockquote><p><strong>Note:</strong> All functions are thread-safe and can be called from multiple threads concurrently.</p></blockquote><h2>convert_html</h2><p>Converts an HTML string to Markdown format.</p><pre><code class=\"language-rust\">pub fn convert_html(html: &amp;str) -&gt; Result&lt;String, ConversionError&gt;</code></pre><h3>Parameters</h3><ul><li><code>html</code> - The HTML input string<ul><li>Must be valid UTF-8</li><li>Maximum size: 50MB</li></ul></li></ul><h3>Returns</h3><ul><li><code>Ok(String)</code> - The converted Markdown</li><li><code>Err(ConversionError)</code> - If conversion fails</li></ul><h3>Example</h3><pre><code class=\"language-rust\">let markdown = convert_html(\"&lt;h1&gt;Hello&lt;/h1&gt;\").unwrap();\nassert_eq!(markdown, \"# Hello\");</code></pre><h2>ConversionOptions</h2><p>Configure conversion behavior using the builder pattern:</p><pre><code class=\"language-rust\">let options = ConversionOptions::builder()\n    .heading_style(HeadingStyle::ATX)\n    .code_block_style(CodeBlockStyle::Fenced)\n    .build();</code></pre><blockquote><p>See the <a href=\"/docs/options\">options reference</a> for a full list of configuration values.</p></blockquote></div>", _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("# API Reference"), "expected to contain: \("# API Reference")")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("## convert_html"), "expected to contain: \("## convert_html")")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("### Parameters"), "expected to contain: \("### Parameters")")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("### Returns"), "expected to contain: \("### Returns")")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("### Example"), "expected to contain: \("### Example")")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("## ConversionOptions"), "expected to contain: \("## ConversionOptions")")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("> "), "expected to contain: \("> ")")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("thread-safe"), "expected to contain: \("thread-safe")")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("convert_html"), "expected to contain: \("convert_html")")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("ConversionOptions"), "expected to contain: \("ConversionOptions")")
    }

    func testRealWorldProductPage() throws {
        // Product page with table, images, and lists converts correctly
        let _options = try conversionOptionsFromJson("{}")
        let result = try convert("<div class=\"product\"><h1>Wireless Keyboard Pro</h1><img src=\"https://example.com/keyboard.jpg\" alt=\"Wireless Keyboard Pro\"><p>The ultimate wireless keyboard for professionals. Features a comfortable layout with <strong>backlit keys</strong> and <em>ultra-long battery life</em>.</p><h2>Specifications</h2><table><thead><tr><th>Feature</th><th>Details</th></tr></thead><tbody><tr><td>Battery Life</td><td>Up to 12 months</td></tr><tr><td>Connectivity</td><td>Bluetooth 5.0</td></tr><tr><td>Key Travel</td><td>2mm</td></tr><tr><td>Weight</td><td>750g</td></tr></tbody></table><h2>What's in the Box</h2><ul><li>Wireless Keyboard Pro</li><li>USB-C charging cable</li><li>USB receiver dongle</li><li>Quick start guide</li></ul><h2>Compatibility</h2><p>Compatible with <strong>Windows</strong>, <strong>macOS</strong>, <strong>Linux</strong>, <strong>iOS</strong>, and <strong>Android</strong>.</p></div>", _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("# Wireless Keyboard Pro"), "expected to contain: \("# Wireless Keyboard Pro")")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("![Wireless Keyboard Pro](https://example.com/keyboard.jpg)"), "expected to contain: \("![Wireless Keyboard Pro](https://example.com/keyboard.jpg)")")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("## Specifications"), "expected to contain: \("## Specifications")")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("Battery Life"), "expected to contain: \("Battery Life")")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("12 months"), "expected to contain: \("12 months")")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("Bluetooth 5.0"), "expected to contain: \("Bluetooth 5.0")")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("## What's in the Box"), "expected to contain: \("## What's in the Box")")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("USB-C charging cable"), "expected to contain: \("USB-C charging cable")")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("|"), "expected to contain: \("|")")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("---"), "expected to contain: \("---")")
    }

}
