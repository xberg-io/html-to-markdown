// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:13b7bce1b8084474dd907fe08752e4afc7c05b5c42cd4030ec54e77e76527ab1
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import HtmlToMarkdown
import RustBridge

/// E2e tests for category: metadata.
final class MetadataTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        let _existing = ProcessInfo.processInfo.environment["SUT_URL"]
    }

    func testMetadataAuthorMeta() throws {
        // Extract author from <meta name='author'> tag
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"extract_metadata\":true}")
        let result = try HtmlToMarkdown.convert(html: "<html><head><title>Page</title><meta name=\"author\" content=\"Jane Doe\"></head><body><p>Content</p></body></html>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertEqual((result.metadata().document().author()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "Jane Doe")
    }

    func testMetadataCanonicalUrl() throws {
        // Extract canonical URL from <link rel='canonical'> tag
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"extract_metadata\":true}")
        let result = try HtmlToMarkdown.convert(html: "<html><head><title>Page</title><link rel=\"canonical\" href=\"https://example.com/canonical-page\"></head><body><p>Content</p></body></html>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertEqual((result.metadata().document().canonicalUrl()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "https://example.com/canonical-page")
    }

    func testMetadataDescriptionMeta() throws {
        // Extract description from <meta name='description'> tag
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"extract_metadata\":true}")
        let result = try HtmlToMarkdown.convert(html: "<html><head><title>Page</title><meta name=\"description\" content=\"This is the page description.\"></head><body><p>Content</p></body></html>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertEqual((result.metadata().document().description()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "This is the page description.")
    }

    func testMetadataDublinCore() throws {
        // Extract Dublin Core metadata tags
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"extract_metadata\":true}")
        let result = try HtmlToMarkdown.convert(html: "<html><head><title>Scholarly Work</title><meta name=\"DC.title\" content=\"Principles of Knowledge Management\"><meta name=\"DC.creator\" content=\"Dr. Alice Johnson\"><meta name=\"DC.date\" content=\"2023-06-15\"><meta name=\"DC.subject\" content=\"Knowledge Management\"><meta name=\"DC.publisher\" content=\"Academic Press\"></head><body><p>This is a scholarly article.</p></body></html>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertTrue("scholarly article".isEmpty || (result.content()?.toString() ?? "").contains("scholarly article"), "expected to contain: \("scholarly article")")
    }

    func testMetadataExtractAllImages() throws {
        // Extract all images from a document into metadata
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"extract_metadata\":true}")
        let result = try HtmlToMarkdown.convert(html: "<html><head><title>Gallery</title></head><body><img src=\"https://example.com/photo1.jpg\" alt=\"Photo 1\"><img src=\"https://example.com/photo2.png\" alt=\"Photo 2\"><img src=\"/local/image.webp\" alt=\"Local image\"></body></html>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertGreaterThanOrEqual(result.metadata().images().count, 2)
    }

    func testMetadataExtractAllLinks() throws {
        // Extract all links from a document into metadata
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"extract_metadata\":true}")
        let result = try HtmlToMarkdown.convert(html: "<html><head><title>Links Page</title></head><body><p>Visit <a href=\"https://example.com\">Example</a> or <a href=\"https://docs.example.com\">Docs</a>.</p><p>Also see <a href=\"/relative/path\">relative link</a> and <a href=\"mailto:hello@example.com\">email us</a>.</p></body></html>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertGreaterThanOrEqual(result.metadata().links().count, 2)
    }

    func testMetadataHeadersHierarchy() throws {
        // Extract heading hierarchy from document into metadata
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"extract_metadata\":true}")
        let result = try HtmlToMarkdown.convert(html: "<html><head><title>Docs</title></head><body><h1>Introduction</h1><h2>Getting Started</h2><h3>Installation</h3><h3>Configuration</h3><h2>Advanced Usage</h2><h3>Custom Options</h3></body></html>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertGreaterThanOrEqual(result.metadata().headers().count, 5)
    }

    func testMetadataImageTypeDataUriClassified() throws {
        // A data-URI image is extracted into metadata.images when extract_metadata is true
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"extract_metadata\":true}")
        let result = try HtmlToMarkdown.convert(html: "<p><img src=\"data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8/5+hHgAHggJ/PchI7wAAAABJRU5ErkJggg==\" alt=\"pixel\"></p>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertGreaterThanOrEqual(result.metadata().images().count, 1)
    }

    func testMetadataImageTypeExternalClassified() throws {
        // An https image URL is extracted into metadata.images when extract_metadata is true
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"extract_metadata\":true}")
        let result = try HtmlToMarkdown.convert(html: "<p><img src=\"https://example.com/photo.jpg\" alt=\"A photo\"></p>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertGreaterThanOrEqual(result.metadata().images().count, 1)
    }

    func testMetadataKeywordsMeta() throws {
        // Extract keywords from <meta name='keywords'> tag
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"extract_metadata\":true}")
        let result = try HtmlToMarkdown.convert(html: "<html><head><title>Page</title><meta name=\"keywords\" content=\"rust, markdown, html, converter\"></head><body><p>Content</p></body></html>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertGreaterThanOrEqual(result.metadata().document().keywords().count, 1)
    }

    func testMetadataLangAttribute() throws {
        // Extract language from html lang attribute
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"extract_metadata\":true}")
        let result = try HtmlToMarkdown.convert(html: "<html lang=\"es\"><head><title>Spanish Page</title></head><body><h1>Hola Mundo</h1><p>Este es un documento en español.</p></body></html>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertTrue("Hola Mundo".isEmpty || (result.content()?.toString() ?? "").contains("Hola Mundo"), "expected to contain: \("Hola Mundo")")
    }

    func testMetadataLinkTypeAnchorClassified() throws {
        // A fragment anchor link is extracted into metadata.links when extract_metadata is true
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"extract_metadata\":true}")
        let result = try HtmlToMarkdown.convert(html: "<p>Jump to <a href=\"#section\">section</a> below.</p>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertGreaterThanOrEqual(result.metadata().links().count, 1)
    }

    func testMetadataLinkTypeEmailClassified() throws {
        // A mailto link is extracted into metadata.links when extract_metadata is true
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"extract_metadata\":true}")
        let result = try HtmlToMarkdown.convert(html: "<p>Contact <a href=\"mailto:hello@example.com\">us</a> directly.</p>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertGreaterThanOrEqual(result.metadata().links().count, 1)
    }

    func testMetadataLinkTypeExternalClassified() throws {
        // An https link is extracted into metadata.links when extract_metadata is true
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"extract_metadata\":true}")
        let result = try HtmlToMarkdown.convert(html: "<p>See <a href=\"https://example.com\">Example</a> for details.</p>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertGreaterThanOrEqual(result.metadata().links().count, 1)
    }

    func testMetadataMicrodataSchemaArticle() throws {
        // Extract schema.org microdata for Article
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"extract_metadata\":true}")
        let result = try HtmlToMarkdown.convert(html: "<html><head><title>Article</title></head><body><article itemscope itemtype=\"https://schema.org/Article\"><h1 itemprop=\"headline\">Breaking News Today</h1><span itemprop=\"author\">Jane Reporter</span><span itemprop=\"datePublished\">2024-04-22</span><div itemprop=\"articleBody\"><p>The article content goes here with important information about the breaking news story.</p></div></article></body></html>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertTrue("Breaking News Today".isEmpty || (result.content()?.toString() ?? "").contains("Breaking News Today"), "expected to contain: \("Breaking News Today")")
        XCTAssertTrue("Jane Reporter".isEmpty || (result.content()?.toString() ?? "").contains("Jane Reporter"), "expected to contain: \("Jane Reporter")")
        XCTAssertTrue("important information".isEmpty || (result.content()?.toString() ?? "").contains("important information"), "expected to contain: \("important information")")
    }

    func testMetadataMicrodataSchemaBreadcrumb() throws {
        // Extract schema.org breadcrumb navigation microdata
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"extract_metadata\":true,\"preprocessing\":{\"remove_navigation\":false}}")
        let result = try HtmlToMarkdown.convert(html: "<html><head><title>Navigation</title></head><body><nav itemscope itemtype=\"https://schema.org/BreadcrumbList\"><span itemprop=\"itemListElement\" itemscope itemtype=\"https://schema.org/ListItem\"><a itemprop=\"item\" href=\"https://example.com\"><span itemprop=\"name\">Home</span></a></span><span itemprop=\"itemListElement\" itemscope itemtype=\"https://schema.org/ListItem\"><a itemprop=\"item\" href=\"https://example.com/products\"><span itemprop=\"name\">Products</span></a></span><span itemprop=\"itemListElement\" itemscope itemtype=\"https://schema.org/ListItem\"><span itemprop=\"name\">Current Page</span></span></nav></body></html>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertTrue("Home".isEmpty || (result.content()?.toString() ?? "").contains("Home"), "expected to contain: \("Home")")
        XCTAssertTrue("Products".isEmpty || (result.content()?.toString() ?? "").contains("Products"), "expected to contain: \("Products")")
        XCTAssertTrue("Current Page".isEmpty || (result.content()?.toString() ?? "").contains("Current Page"), "expected to contain: \("Current Page")")
    }

    func testMetadataMicrodataSchemaOrganization() throws {
        // Extract schema.org microdata for Organization
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"extract_metadata\":true}")
        let result = try HtmlToMarkdown.convert(html: "<html><head><title>Company</title></head><body><div itemscope itemtype=\"https://schema.org/Organization\"><span itemprop=\"name\">Acme Corp</span><span itemprop=\"foundingDate\">2020</span><span itemprop=\"url\">https://acmecorp.example.com</span><span itemprop=\"logo\">https://acmecorp.example.com/logo.png</span></div></body></html>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertTrue("Acme Corp".isEmpty || (result.content()?.toString() ?? "").contains("Acme Corp"), "expected to contain: \("Acme Corp")")
        XCTAssertTrue("2020".isEmpty || (result.content()?.toString() ?? "").contains("2020"), "expected to contain: \("2020")")
    }

    func testMetadataMicrodataSchemaPerson() throws {
        // Extract schema.org microdata for Person
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"extract_metadata\":true}")
        let result = try HtmlToMarkdown.convert(html: "<html><head><title>Contact</title></head><body><div itemscope itemtype=\"https://schema.org/Person\"><span itemprop=\"name\">John Smith</span><span itemprop=\"email\">john@example.com</span><span itemprop=\"telephone\">+1-555-0100</span></div></body></html>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertTrue("John Smith".isEmpty || (result.content()?.toString() ?? "").contains("John Smith"), "expected to contain: \("John Smith")")
        XCTAssertTrue("john@example.com".isEmpty || (result.content()?.toString() ?? "").contains("john@example.com"), "expected to contain: \("john@example.com")")
    }

    func testMetadataMicrodataSchemaProduct() throws {
        // Extract schema.org microdata for Product
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"extract_metadata\":true}")
        let result = try HtmlToMarkdown.convert(html: "<html><head><title>Product</title></head><body><div itemscope itemtype=\"https://schema.org/Product\"><h1 itemprop=\"name\">Awesome Widget</h1><span itemprop=\"description\">The best widget on the market</span><span itemprop=\"price\">29.99</span><span itemprop=\"priceCurrency\">USD</span><img itemprop=\"image\" src=\"widget.jpg\" alt=\"Widget\"><span itemprop=\"ratingValue\">4.5</span></div></body></html>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertTrue("Awesome Widget".isEmpty || (result.content()?.toString() ?? "").contains("Awesome Widget"), "expected to contain: \("Awesome Widget")")
        XCTAssertTrue("best widget".isEmpty || (result.content()?.toString() ?? "").contains("best widget"), "expected to contain: \("best widget")")
        XCTAssertTrue("29.99".isEmpty || (result.content()?.toString() ?? "").contains("29.99"), "expected to contain: \("29.99")")
    }

    func testMetadataTextDirectionLtr() throws {
        // Extract text direction from lang attribute on html element
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"extract_metadata\":true}")
        let result = try HtmlToMarkdown.convert(html: "<html lang=\"en\" dir=\"ltr\"><head><title>LTR Document</title></head><body><p>This is left-to-right text.</p></body></html>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertTrue("left-to-right text".isEmpty || (result.content()?.toString() ?? "").contains("left-to-right text"), "expected to contain: \("left-to-right text")")
    }

    func testMetadataTextDirectionRtl() throws {
        // Extract right-to-left text direction
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"extract_metadata\":true}")
        let result = try HtmlToMarkdown.convert(html: "<html lang=\"ar\" dir=\"rtl\"><head><title>RTL Document</title></head><body><p>This is right-to-left text.</p></body></html>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertTrue("right-to-left text".isEmpty || (result.content()?.toString() ?? "").contains("right-to-left text"), "expected to contain: \("right-to-left text")")
    }

    func testMetadataTitleTag() throws {
        // Extract title from <title> tag
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"extract_metadata\":true}")
        let result = try HtmlToMarkdown.convert(html: "<html><head><title>My Page</title></head><body><p>Content</p></body></html>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertEqual((result.metadata().document().title()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "My Page")
    }

    func testOgBasicTags() throws {
        // Extract og:title, og:description, and og:image from Open Graph meta tags
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<html><head><title>Fallback Title</title><meta property=\"og:title\" content=\"OG Title\"><meta property=\"og:description\" content=\"OG description text.\"><meta property=\"og:image\" content=\"https://example.com/image.jpg\"></head><body><p>Content</p></body></html>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        let _vec_openGraph_equals_8af527e5 = (try? JSONSerialization.jsonObject(with: (result.metadata().document().openGraph().toString() ?? "{}").data(using: .utf8)!) as? [String: String]) ?? [:]
        XCTAssertEqual((_vec_openGraph_equals_8af527e5["title"] ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "OG Title")
        let _vec_openGraph_equals_678f5256 = (try? JSONSerialization.jsonObject(with: (result.metadata().document().openGraph().toString() ?? "{}").data(using: .utf8)!) as? [String: String]) ?? [:]
        XCTAssertEqual((_vec_openGraph_equals_678f5256["description"] ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "OG description text.")
        let _vec_openGraph_equals_16f2cb10 = (try? JSONSerialization.jsonObject(with: (result.metadata().document().openGraph().toString() ?? "{}").data(using: .utf8)!) as? [String: String]) ?? [:]
        XCTAssertEqual((_vec_openGraph_equals_16f2cb10["image"] ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "https://example.com/image.jpg")
    }

    func testOgMultipleTags() throws {
        // Extract multiple Open Graph tags including type, url, and site_name
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<html><head><meta property=\"og:title\" content=\"Article Title\"><meta property=\"og:type\" content=\"article\"><meta property=\"og:url\" content=\"https://example.com/article\"><meta property=\"og:site_name\" content=\"Example Site\"><meta property=\"og:description\" content=\"An interesting article.\"><meta property=\"og:image\" content=\"https://example.com/article.jpg\"></head><body><article><p>Article content here.</p></article></body></html>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        let _vec_openGraph_equals_7f93d34d = (try? JSONSerialization.jsonObject(with: (result.metadata().document().openGraph().toString() ?? "{}").data(using: .utf8)!) as? [String: String]) ?? [:]
        XCTAssertEqual((_vec_openGraph_equals_7f93d34d["title"] ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "Article Title")
        let _vec_openGraph_equals_ffa65f4f = (try? JSONSerialization.jsonObject(with: (result.metadata().document().openGraph().toString() ?? "{}").data(using: .utf8)!) as? [String: String]) ?? [:]
        XCTAssertEqual((_vec_openGraph_equals_ffa65f4f["type"] ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "article")
        let _vec_openGraph_equals_e73dd9da = (try? JSONSerialization.jsonObject(with: (result.metadata().document().openGraph().toString() ?? "{}").data(using: .utf8)!) as? [String: String]) ?? [:]
        XCTAssertEqual((_vec_openGraph_equals_e73dd9da["url"] ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "https://example.com/article")
        let _vec_openGraph_equals_29259c1c = (try? JSONSerialization.jsonObject(with: (result.metadata().document().openGraph().toString() ?? "{}").data(using: .utf8)!) as? [String: String]) ?? [:]
        XCTAssertEqual((_vec_openGraph_equals_29259c1c["site_name"] ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "Example Site")
    }

    func testStructuredDataJsonLd() throws {
        // JSON-LD script tag is stripped from output (security) but metadata may be extracted
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<html><head><title>Article</title><script type=\"application/ld+json\">{\"@context\":\"https://schema.org\",\"@type\":\"Article\",\"headline\":\"My Article\",\"author\":{\"@type\":\"Person\",\"name\":\"Jane Doe\"},\"datePublished\":\"2024-01-15\"}</script></head><body><h1>My Article</h1><p>Article body text.</p></body></html>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("My Article"), "expected to contain: \("My Article")")
    }

    func testStructuredDataMultipleJsonLd() throws {
        // Multiple JSON-LD blocks are all stripped from output
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<html><head><title>Shop Page</title><script type=\"application/ld+json\">{\"@context\":\"https://schema.org\",\"@type\":\"Product\",\"name\":\"Widget\",\"price\":\"9.99\"}</script><script type=\"application/ld+json\">{\"@context\":\"https://schema.org\",\"@type\":\"BreadcrumbList\",\"itemListElement\":[{\"@type\":\"ListItem\",\"position\":1,\"name\":\"Home\"}]}</script></head><body><h1>Widget</h1><p>A great widget for all purposes.</p></body></html>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        XCTAssertTrue((result.content()?.toString() ?? "").contains("Widget"), "expected to contain: \("Widget")")
    }

    func testTwitterCardTags() throws {
        // Extract Twitter card meta tags
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<html><head><meta name=\"twitter:card\" content=\"summary_large_image\"><meta name=\"twitter:site\" content=\"@examplesite\"><meta name=\"twitter:title\" content=\"Twitter Card Title\"><meta name=\"twitter:description\" content=\"Twitter card description.\"><meta name=\"twitter:image\" content=\"https://example.com/twitter-image.jpg\"></head><body><p>Content</p></body></html>", options: _options)
        XCTAssertNotNil(result.content(), "expected non-nil value")
        let _vec_twitterCard_equals_36f99f76 = (try? JSONSerialization.jsonObject(with: (result.metadata().document().twitterCard().toString() ?? "{}").data(using: .utf8)!) as? [String: String]) ?? [:]
        XCTAssertEqual((_vec_twitterCard_equals_36f99f76["card"] ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "summary_large_image")
        let _vec_twitterCard_equals_f57150d6 = (try? JSONSerialization.jsonObject(with: (result.metadata().document().twitterCard().toString() ?? "{}").data(using: .utf8)!) as? [String: String]) ?? [:]
        XCTAssertEqual((_vec_twitterCard_equals_f57150d6["title"] ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "Twitter Card Title")
        let _vec_twitterCard_equals_af3f5cdd = (try? JSONSerialization.jsonObject(with: (result.metadata().document().twitterCard().toString() ?? "{}").data(using: .utf8)!) as? [String: String]) ?? [:]
        XCTAssertEqual((_vec_twitterCard_equals_af3f5cdd["description"] ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "Twitter card description.")
    }

}
