// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import HtmlToMarkdown
import RustBridge

/// E2e tests for category: visitor.
final class VisitorTests: XCTestCase {
    func testVisitorAudioCustom() throws {
        // Visitor replaces audio element with custom output
        final class LocalVisitor_VisitorAudioCustom: HtmlVisitorProtocol {
    func visitAudio(_ ctx: NodeContext, _ src: String?) -> VisitResult { return .custom(field0: "[AUDIO: podcast.mp3]") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorAudioCustom())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>Listen to this: <audio src=\"podcast.mp3\" controls></audio></p>", options: _options)
        XCTAssertTrue("[AUDIO: podcast.mp3]".isEmpty || (result.content()?.toString() ?? "").contains("[AUDIO: podcast.mp3]"), "expected to contain: \("[AUDIO: podcast.mp3]")")
        XCTAssertTrue("Listen to this:".isEmpty || (result.content()?.toString() ?? "").contains("Listen to this:"), "expected to contain: \("Listen to this:")")
    }

    func testVisitorAudioSkip() throws {
        // Visitor removes audio elements from output
        final class LocalVisitor_VisitorAudioSkip: HtmlVisitorProtocol {
    func visitAudio(_ ctx: NodeContext, _ src: String?) -> VisitResult { return .skip }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorAudioSkip())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>Background music:</p><audio src=\"music.ogg\" autoplay></audio><p>Enjoy!</p>", options: _options)
        XCTAssertTrue("Background music:".isEmpty || (result.content()?.toString() ?? "").contains("Background music:"), "expected to contain: \("Background music:")")
        XCTAssertTrue("Enjoy!".isEmpty || (result.content()?.toString() ?? "").contains("Enjoy!"), "expected to contain: \("Enjoy!")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("music.ogg"), "expected NOT to contain: \("music.ogg")")
    }

    func testVisitorButtonCustom() throws {
        // Visitor replaces button with bracketed text
        final class LocalVisitor_VisitorButtonCustom: HtmlVisitorProtocol {
    func visitButton(_ ctx: NodeContext, _ text: String) -> VisitResult { return .custom(field0: "[BTN:\(text)]") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorButtonCustom())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>Confirm action: <button type=\"submit\">Click me</button> or <button type=\"reset\">Cancel</button></p>", options: _options)
        XCTAssertTrue("[BTN:Click me]".isEmpty || (result.content()?.toString() ?? "").contains("[BTN:Click me]"), "expected to contain: \("[BTN:Click me]")")
        XCTAssertTrue("[BTN:Cancel]".isEmpty || (result.content()?.toString() ?? "").contains("[BTN:Cancel]"), "expected to contain: \("[BTN:Cancel]")")
        XCTAssertTrue("Confirm action:".isEmpty || (result.content()?.toString() ?? "").contains("Confirm action:"), "expected to contain: \("Confirm action:")")
    }

    func testVisitorButtonSkip() throws {
        // Visitor removes all buttons from output
        final class LocalVisitor_VisitorButtonSkip: HtmlVisitorProtocol {
    func visitButton(_ ctx: NodeContext, _ text: String) -> VisitResult { return .skip }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorButtonSkip())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>Actions available: <button>Save</button> <button>Delete</button> <button>Export</button></p>", options: _options)
        XCTAssertTrue("Actions available:".isEmpty || (result.content()?.toString() ?? "").contains("Actions available:"), "expected to contain: \("Actions available:")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("Save"), "expected NOT to contain: \("Save")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("Delete"), "expected NOT to contain: \("Delete")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("Export"), "expected NOT to contain: \("Export")")
    }

    func testVisitorContinueDefault() throws {
        // Visitor continue action preserves default conversion
        final class LocalVisitor_VisitorContinueDefault: HtmlVisitorProtocol {
    func visitStrong(_ ctx: NodeContext, _ text: String) -> VisitResult { return .`continue` }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorContinueDefault())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>Hello <strong>World</strong></p>", options: _options)
        XCTAssertTrue("**World**".isEmpty || (result.content()?.toString() ?? "").contains("**World**"), "expected to contain: \("**World**")")
    }

    func testVisitorCustomBlockquote() throws {
        // Visitor replaces blockquote with custom format
        final class LocalVisitor_VisitorCustomBlockquote: HtmlVisitorProtocol {
    func visitBlockquote(_ ctx: NodeContext, _ content: String, _ depth: Int) -> VisitResult { return .custom(field0: "QUOTE: \"\(content)\"") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorCustomBlockquote())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<blockquote><p>A wise quote.</p></blockquote>", options: _options)
        XCTAssertTrue("QUOTE:".isEmpty || (result.content()?.toString() ?? "").contains("QUOTE:"), "expected to contain: \("QUOTE:")")
        XCTAssertTrue("A wise quote.".isEmpty || (result.content()?.toString() ?? "").contains("A wise quote."), "expected to contain: \("A wise quote.")")
    }

    func testVisitorCustomEmphasis() throws {
        // Visitor replaces emphasis with custom output
        final class LocalVisitor_VisitorCustomEmphasis: HtmlVisitorProtocol {
    func visitEmphasis(_ ctx: NodeContext, _ text: String) -> VisitResult { return .custom(field0: ">>>\(text)<<<") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorCustomEmphasis())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>This is <em>important</em> text.</p>", options: _options)
        XCTAssertTrue(">>>important<<<".isEmpty || (result.content()?.toString() ?? "").contains(">>>important<<<"), "expected to contain: \(">>>important<<<")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("*important*"), "expected NOT to contain: \("*important*")")
    }

    func testVisitorCustomHeading() throws {
        // Visitor replaces heading with custom format
        final class LocalVisitor_VisitorCustomHeading: HtmlVisitorProtocol {
    func visitHeading(_ ctx: NodeContext, _ level: UInt32, _ text: String, _ id: String?) -> VisitResult { return .custom(field0: "--- \(text) ---") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorCustomHeading())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<h2>Section Title</h2><p>Content below heading.</p>", options: _options)
        XCTAssertTrue("--- Section Title ---".isEmpty || (result.content()?.toString() ?? "").contains("--- Section Title ---"), "expected to contain: \("--- Section Title ---")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("## Section Title"), "expected NOT to contain: \("## Section Title")")
        XCTAssertTrue("Content below heading.".isEmpty || (result.content()?.toString() ?? "").contains("Content below heading."), "expected to contain: \("Content below heading.")")
    }

    func testVisitorCustomImage() throws {
        // Visitor replaces image with custom output using template
        final class LocalVisitor_VisitorCustomImage: HtmlVisitorProtocol {
    func visitImage(_ ctx: NodeContext, _ src: String, _ alt: String, _ title: String?) -> VisitResult { return .custom(field0: "[Image: \(alt)]") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorCustomImage())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<img src=\"banner.png\" alt=\"Banner\">", options: _options)
        XCTAssertTrue("[Image: Banner]".isEmpty || (result.content()?.toString() ?? "").contains("[Image: Banner]"), "expected to contain: \("[Image: Banner]")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("banner.png"), "expected NOT to contain: \("banner.png")")
    }

    func testVisitorCustomLinkFormat() throws {
        // Visitor reformats links using template interpolation
        final class LocalVisitor_VisitorCustomLinkFormat: HtmlVisitorProtocol {
    func visitLink(_ ctx: NodeContext, _ href: String, _ text: String, _ title: String?) -> VisitResult { return .custom(field0: "\(text) (\(href))") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorCustomLinkFormat())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>Visit <a href=\"https://example.com\">Example</a> for more info.</p>", options: _options)
        XCTAssertTrue("Example (https://example.com)".isEmpty || (result.content()?.toString() ?? "").contains("Example (https://example.com)"), "expected to contain: \("Example (https://example.com)")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("[Example]"), "expected NOT to contain: \("[Example]")")
    }

    func testVisitorCustomLinkStatic() throws {
        // Visitor replaces link with static custom output
        final class LocalVisitor_VisitorCustomLinkStatic: HtmlVisitorProtocol {
    func visitLink(_ ctx: NodeContext, _ href: String, _ text: String, _ title: String?) -> VisitResult { return .custom(field0: "[REDACTED LINK]") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorCustomLinkStatic())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<a href=\"https://example.com\">Click here</a>", options: _options)
        XCTAssertTrue("[REDACTED LINK]".isEmpty || (result.content()?.toString() ?? "").contains("[REDACTED LINK]"), "expected to contain: \("[REDACTED LINK]")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("example.com"), "expected NOT to contain: \("example.com")")
    }

    func testVisitorCustomOutput() throws {
        // Visitor custom action replaces element output
        final class LocalVisitor_VisitorCustomOutput: HtmlVisitorProtocol {
    func visitHeading(_ ctx: NodeContext, _ level: UInt32, _ text: String, _ id: String?) -> VisitResult { return .custom(field0: "## REPLACED HEADING") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorCustomOutput())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<h1>Original Heading</h1>", options: _options)
        XCTAssertTrue("## REPLACED HEADING".isEmpty || (result.content()?.toString() ?? "").contains("## REPLACED HEADING"), "expected to contain: \("## REPLACED HEADING")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("# Original Heading"), "expected NOT to contain: \("# Original Heading")")
    }

    func testVisitorDefinitionListCustom() throws {
        // Visitor customizes definition list items
        final class LocalVisitor_VisitorDefinitionListCustom: HtmlVisitorProtocol {
    func visitDefinitionTerm(_ ctx: NodeContext, _ text: String) -> VisitResult { return .custom(field0: "**\(text)**") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorDefinitionListCustom())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<dl><dt>HTML</dt><dd>HyperText Markup Language</dd><dt>CSS</dt><dd>Cascading Style Sheets</dd></dl>", options: _options)
        XCTAssertTrue("**HTML**".isEmpty || (result.content()?.toString() ?? "").contains("**HTML**"), "expected to contain: \("**HTML**")")
        XCTAssertTrue("**CSS**".isEmpty || (result.content()?.toString() ?? "").contains("**CSS**"), "expected to contain: \("**CSS**")")
        XCTAssertTrue("HyperText Markup Language".isEmpty || (result.content()?.toString() ?? "").contains("HyperText Markup Language"), "expected to contain: \("HyperText Markup Language")")
    }

    func testVisitorDefinitionListCustomFormat() throws {
        // Visitor formats definition lists with custom templates
        final class LocalVisitor_VisitorDefinitionListCustomFormat: HtmlVisitorProtocol {
    func visitDefinitionTerm(_ ctx: NodeContext, _ text: String) -> VisitResult { return .custom(field0: "### \(text)") }
    func visitDefinitionDescription(_ ctx: NodeContext, _ text: String) -> VisitResult { return .custom(field0: "> \(text)") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorDefinitionListCustomFormat())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<dl><dt>Python</dt><dd>A high-level programming language</dd><dt>JavaScript</dt><dd>A scripting language for web browsers</dd></dl>", options: _options)
        XCTAssertTrue("### Python".isEmpty || (result.content()?.toString() ?? "").contains("### Python"), "expected to contain: \("### Python")")
        XCTAssertTrue("### JavaScript".isEmpty || (result.content()?.toString() ?? "").contains("### JavaScript"), "expected to contain: \("### JavaScript")")
        XCTAssertTrue("> A high-level programming language".isEmpty || (result.content()?.toString() ?? "").contains("> A high-level programming language"), "expected to contain: \("> A high-level programming language")")
        XCTAssertTrue("> A scripting language for web browsers".isEmpty || (result.content()?.toString() ?? "").contains("> A scripting language for web browsers"), "expected to contain: \("> A scripting language for web browsers")")
    }

    func testVisitorDefinitionListSkip() throws {
        // Visitor skips definition list items from output
        final class LocalVisitor_VisitorDefinitionListSkip: HtmlVisitorProtocol {
    func visitDefinitionTerm(_ ctx: NodeContext, _ text: String) -> VisitResult { return .skip }
    func visitDefinitionDescription(_ ctx: NodeContext, _ text: String) -> VisitResult { return .skip }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorDefinitionListSkip())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>Glossary:</p><dl><dt>Term A</dt><dd>Definition of term A</dd><dt>Term B</dt><dd>Definition of term B</dd></dl><p>End of glossary</p>", options: _options)
        XCTAssertTrue("Glossary:".isEmpty || (result.content()?.toString() ?? "").contains("Glossary:"), "expected to contain: \("Glossary:")")
        XCTAssertTrue("End of glossary".isEmpty || (result.content()?.toString() ?? "").contains("End of glossary"), "expected to contain: \("End of glossary")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("Term A"), "expected NOT to contain: \("Term A")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("Definition"), "expected NOT to contain: \("Definition")")
    }

    func testVisitorDetailsSummaryCustom() throws {
        // Visitor customizes details/summary disclosure elements
        final class LocalVisitor_VisitorDetailsSummaryCustom: HtmlVisitorProtocol {
    func visitSummary(_ ctx: NodeContext, _ text: String) -> VisitResult { return .custom(field0: "[EXPANDABLE] \(text)") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorDetailsSummaryCustom())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<details><summary>Click to expand</summary><p>This content is initially hidden.</p><p>But can be revealed by the user.</p></details>", options: _options)
        XCTAssertTrue("[EXPANDABLE] Click to expand".isEmpty || (result.content()?.toString() ?? "").contains("[EXPANDABLE] Click to expand"), "expected to contain: \("[EXPANDABLE] Click to expand")")
        XCTAssertTrue("This content is initially hidden.".isEmpty || (result.content()?.toString() ?? "").contains("This content is initially hidden."), "expected to contain: \("This content is initially hidden.")")
    }

    func testVisitorDetailsSummarySkip() throws {
        // Visitor removes details/summary elements entirely
        final class LocalVisitor_VisitorDetailsSummarySkip: HtmlVisitorProtocol {
    func visitDetails(_ ctx: NodeContext, _ open: Bool) -> VisitResult { return .skip }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorDetailsSummarySkip())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>Main content here.</p><details><summary>Hidden section</summary><p>Secret details</p></details><p>More main content.</p>", options: _options)
        XCTAssertTrue("Main content here.".isEmpty || (result.content()?.toString() ?? "").contains("Main content here."), "expected to contain: \("Main content here.")")
        XCTAssertTrue("More main content.".isEmpty || (result.content()?.toString() ?? "").contains("More main content."), "expected to contain: \("More main content.")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("Hidden section"), "expected NOT to contain: \("Hidden section")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("Secret details"), "expected NOT to contain: \("Secret details")")
    }

    func testVisitorFigureCustom() throws {
        // Visitor customizes figure and figcaption elements
        final class LocalVisitor_VisitorFigureCustom: HtmlVisitorProtocol {
    func visitFigcaption(_ ctx: NodeContext, _ text: String) -> VisitResult { return .custom(field0: "*\(text)*") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorFigureCustom())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<article><h1>Article Title</h1><p>Introduction paragraph.</p><figure><img src=\"diagram.png\" alt=\"System architecture diagram\"><figcaption>Figure 1: System Architecture</figcaption></figure><p>Explanation of the figure.</p></article>", options: _options)
        XCTAssertTrue("Article Title".isEmpty || (result.content()?.toString() ?? "").contains("Article Title"), "expected to contain: \("Article Title")")
        XCTAssertTrue("*Figure 1: System Architecture*".isEmpty || (result.content()?.toString() ?? "").contains("*Figure 1: System Architecture*"), "expected to contain: \("*Figure 1: System Architecture*")")
        XCTAssertTrue("Explanation of the figure.".isEmpty || (result.content()?.toString() ?? "").contains("Explanation of the figure."), "expected to contain: \("Explanation of the figure.")")
    }

    func testVisitorFigureCustomWrap() throws {
        // Visitor wraps figure content with custom formatting
        final class LocalVisitor_VisitorFigureCustomWrap: HtmlVisitorProtocol {
    func visitFigureStart(_ ctx: NodeContext) -> VisitResult { return .custom(field0: "\n[FIGURE]\n") }
    func visitFigureEnd(_ ctx: NodeContext, _ output: String) -> VisitResult { return .custom(field0: "\(output)\n[/FIGURE]\n") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorFigureCustomWrap())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<section><h2>Gallery</h2><figure><img src=\"photo1.jpg\" alt=\"Photo\"><figcaption>Beautiful sunset</figcaption></figure></section>", options: _options)
        XCTAssertTrue("[FIGURE]".isEmpty || (result.content()?.toString() ?? "").contains("[FIGURE]"), "expected to contain: \("[FIGURE]")")
        XCTAssertTrue("[/FIGURE]".isEmpty || (result.content()?.toString() ?? "").contains("[/FIGURE]"), "expected to contain: \("[/FIGURE]")")
        XCTAssertTrue("Gallery".isEmpty || (result.content()?.toString() ?? "").contains("Gallery"), "expected to contain: \("Gallery")")
    }

    func testVisitorFigureSkip() throws {
        // Visitor removes figure elements with their captions
        final class LocalVisitor_VisitorFigureSkip: HtmlVisitorProtocol {
    func visitFigureStart(_ ctx: NodeContext) -> VisitResult { return .skip }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorFigureSkip())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>See the chart below:</p><figure><img src=\"chart.svg\"><figcaption>Revenue Trends 2020-2024</figcaption></figure><p>As shown in the chart above.</p>", options: _options)
        XCTAssertTrue("See the chart below:".isEmpty || (result.content()?.toString() ?? "").contains("See the chart below:"), "expected to contain: \("See the chart below:")")
        XCTAssertTrue("As shown in the chart above.".isEmpty || (result.content()?.toString() ?? "").contains("As shown in the chart above."), "expected to contain: \("As shown in the chart above.")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("Revenue Trends"), "expected NOT to contain: \("Revenue Trends")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("chart.svg"), "expected NOT to contain: \("chart.svg")")
    }

    func testVisitorFormCustom() throws {
        // Visitor replaces form with custom output
        final class LocalVisitor_VisitorFormCustom: HtmlVisitorProtocol {
    func visitForm(_ ctx: NodeContext, _ action: String?, _ method: String?) -> VisitResult { return .custom(field0: "[FORM PLACEHOLDER]") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorFormCustom())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<div><form action=\"/submit\" method=\"POST\"><label>Name: <input type=\"text\" name=\"name\"></label><button type=\"submit\">Submit</button></form></div>", options: _options)
        XCTAssertTrue("[FORM PLACEHOLDER]".isEmpty || (result.content()?.toString() ?? "").contains("[FORM PLACEHOLDER]"), "expected to contain: \("[FORM PLACEHOLDER]")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("submit"), "expected NOT to contain: \("submit")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("input"), "expected NOT to contain: \("input")")
    }

    func testVisitorFormSkip() throws {
        // Visitor skips form elements entirely
        final class LocalVisitor_VisitorFormSkip: HtmlVisitorProtocol {
    func visitForm(_ ctx: NodeContext, _ action: String?, _ method: String?) -> VisitResult { return .skip }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorFormSkip())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>Before form</p><form><input type=\"email\" name=\"email\"></form><p>After form</p>", options: _options)
        XCTAssertTrue("Before form".isEmpty || (result.content()?.toString() ?? "").contains("Before form"), "expected to contain: \("Before form")")
        XCTAssertTrue("After form".isEmpty || (result.content()?.toString() ?? "").contains("After form"), "expected to contain: \("After form")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("email"), "expected NOT to contain: \("email")")
    }

    func testVisitorHeadingBareStringPreservesCase() throws {
        // Visitor returns rendered heading template as bare string; mixed-case is preserved (regression guard for issue #350)
        final class LocalVisitor_VisitorHeadingBareStringPreservesCase: HtmlVisitorProtocol {
    func visitHeading(_ ctx: NodeContext, _ level: UInt32, _ text: String, _ id: String?) -> VisitResult { return .custom(field0: "## \(text) ##") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorHeadingBareStringPreservesCase())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<h2>Important Section Title</h2><p>Body.</p>", options: _options)
        XCTAssertTrue("## Important Section Title ##".isEmpty || (result.content()?.toString() ?? "").contains("## Important Section Title ##"), "expected to contain: \("## Important Section Title ##")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("important section title"), "expected NOT to contain: \("important section title")")
    }

    func testVisitorHorizontalRuleCustom() throws {
        // Visitor replaces horizontal rule with custom output
        final class LocalVisitor_VisitorHorizontalRuleCustom: HtmlVisitorProtocol {
    func visitHorizontalRule(_ ctx: NodeContext) -> VisitResult { return .custom(field0: "\n[DIVIDER]\n") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorHorizontalRuleCustom())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<h1>Section A</h1><p>Content A</p><hr><h1>Section B</h1><p>Content B</p>", options: _options)
        XCTAssertTrue("[DIVIDER]".isEmpty || (result.content()?.toString() ?? "").contains("[DIVIDER]"), "expected to contain: \("[DIVIDER]")")
        XCTAssertTrue("Section A".isEmpty || (result.content()?.toString() ?? "").contains("Section A"), "expected to contain: \("Section A")")
        XCTAssertTrue("Section B".isEmpty || (result.content()?.toString() ?? "").contains("Section B"), "expected to contain: \("Section B")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("---"), "expected NOT to contain: \("---")")
    }

    func testVisitorHorizontalRuleSkip() throws {
        // Visitor removes all horizontal rules
        final class LocalVisitor_VisitorHorizontalRuleSkip: HtmlVisitorProtocol {
    func visitHorizontalRule(_ ctx: NodeContext) -> VisitResult { return .skip }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorHorizontalRuleSkip())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>Part 1</p><hr><p>Part 2</p><hr><p>Part 3</p>", options: _options)
        XCTAssertTrue("Part 1".isEmpty || (result.content()?.toString() ?? "").contains("Part 1"), "expected to contain: \("Part 1")")
        XCTAssertTrue("Part 2".isEmpty || (result.content()?.toString() ?? "").contains("Part 2"), "expected to contain: \("Part 2")")
        XCTAssertTrue("Part 3".isEmpty || (result.content()?.toString() ?? "").contains("Part 3"), "expected to contain: \("Part 3")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("---"), "expected NOT to contain: \("---")")
    }

    func testVisitorIframeCustom() throws {
        // Visitor replaces embedded iframe with custom text
        final class LocalVisitor_VisitorIframeCustom: HtmlVisitorProtocol {
    func visitIframe(_ ctx: NodeContext, _ src: String?) -> VisitResult { return .custom(field0: "[EMBEDDED: https://maps.example.com/embed]") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorIframeCustom())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>Embedded map:</p><iframe src=\"https://maps.example.com/embed\" width=\"400\" height=\"300\"></iframe><p>End of map</p>", options: _options)
        XCTAssertTrue("[EMBEDDED: https://maps.example.com/embed]".isEmpty || (result.content()?.toString() ?? "").contains("[EMBEDDED: https://maps.example.com/embed]"), "expected to contain: \("[EMBEDDED: https://maps.example.com/embed]")")
        XCTAssertTrue("Embedded map:".isEmpty || (result.content()?.toString() ?? "").contains("Embedded map:"), "expected to contain: \("Embedded map:")")
        XCTAssertTrue("End of map".isEmpty || (result.content()?.toString() ?? "").contains("End of map"), "expected to contain: \("End of map")")
    }

    func testVisitorIframeSkip() throws {
        // Visitor removes embedded iframes
        final class LocalVisitor_VisitorIframeSkip: HtmlVisitorProtocol {
    func visitIframe(_ ctx: NodeContext, _ src: String?) -> VisitResult { return .skip }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorIframeSkip())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<h3>Reviews</h3><iframe src=\"https://widget.example.com/reviews\"></iframe><p>See reviews from our partners.</p>", options: _options)
        XCTAssertTrue("Reviews".isEmpty || (result.content()?.toString() ?? "").contains("Reviews"), "expected to contain: \("Reviews")")
        XCTAssertTrue("See reviews from our partners.".isEmpty || (result.content()?.toString() ?? "").contains("See reviews from our partners."), "expected to contain: \("See reviews from our partners.")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("widget.example.com"), "expected NOT to contain: \("widget.example.com")")
    }

    func testVisitorImageBareStringPreservesCase() throws {
        // Visitor returns bare-string image replacement; mixed-case alt/src preserved (regression guard for issue #350)
        final class LocalVisitor_VisitorImageBareStringPreservesCase: HtmlVisitorProtocol {
    func visitImage(_ ctx: NodeContext, _ src: String, _ alt: String, _ title: String?) -> VisitResult { return .custom(field0: "[image: \(alt) -> \(src)]") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorImageBareStringPreservesCase())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<img src=\"PhotoOne.JPG\" alt=\"Sunset Over Bay\">", options: _options)
        XCTAssertTrue("[image: Sunset Over Bay -> PhotoOne.JPG]".isEmpty || (result.content()?.toString() ?? "").contains("[image: Sunset Over Bay -> PhotoOne.JPG]"), "expected to contain: \("[image: Sunset Over Bay -> PhotoOne.JPG]")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("sunset over bay"), "expected NOT to contain: \("sunset over bay")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("photoone.jpg"), "expected NOT to contain: \("photoone.jpg")")
    }

    func testVisitorInputCustom() throws {
        // Visitor replaces input with labeled output
        final class LocalVisitor_VisitorInputCustom: HtmlVisitorProtocol {
    func visitInput(_ ctx: NodeContext, _ inputType: String, _ name: String?, _ value: String?) -> VisitResult { return .custom(field0: "[INPUT:\(inputType)]") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorInputCustom())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<form><label>Username: <input type=\"text\" name=\"username\" value=\"\"></label><label>Password: <input type=\"password\" name=\"password\"></label></form>", options: _options)
        XCTAssertTrue("[INPUT:text]".isEmpty || (result.content()?.toString() ?? "").contains("[INPUT:text]"), "expected to contain: \("[INPUT:text]")")
        XCTAssertTrue("[INPUT:password]".isEmpty || (result.content()?.toString() ?? "").contains("[INPUT:password]"), "expected to contain: \("[INPUT:password]")")
    }

    func testVisitorInputSkip() throws {
        // Visitor skips all input elements
        final class LocalVisitor_VisitorInputSkip: HtmlVisitorProtocol {
    func visitInput(_ ctx: NodeContext, _ inputType: String, _ name: String?, _ value: String?) -> VisitResult { return .skip }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorInputSkip())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>Sign up:</p><input type=\"text\" name=\"email\" placeholder=\"your@email.com\"><input type=\"checkbox\" name=\"agree\"><p>Continue</p>", options: _options)
        XCTAssertTrue("Sign up:".isEmpty || (result.content()?.toString() ?? "").contains("Sign up:"), "expected to contain: \("Sign up:")")
        XCTAssertTrue("Continue".isEmpty || (result.content()?.toString() ?? "").contains("Continue"), "expected to contain: \("Continue")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("email"), "expected NOT to contain: \("email")")
    }

    func testVisitorLineBreakCustom() throws {
        // Visitor replaces line break with custom output
        final class LocalVisitor_VisitorLineBreakCustom: HtmlVisitorProtocol {
    func visitLineBreak(_ ctx: NodeContext) -> VisitResult { return .custom(field0: " | ") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorLineBreakCustom())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>First line<br>Second line<br>Third line</p>", options: _options)
        XCTAssertTrue("First line | Second line | Third line".isEmpty || (result.content()?.toString() ?? "").contains("First line | Second line | Third line"), "expected to contain: \("First line | Second line | Third line")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("\n\n"), "expected NOT to contain: \("\n\n")")
    }

    func testVisitorLineBreakSkip() throws {
        // Visitor removes all line breaks
        final class LocalVisitor_VisitorLineBreakSkip: HtmlVisitorProtocol {
    func visitLineBreak(_ ctx: NodeContext) -> VisitResult { return .skip }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorLineBreakSkip())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>Address Line 1<br>Address Line 2<br>Address Line 3</p>", options: _options)
        XCTAssertTrue("Address Line 1Address Line 2Address Line 3".isEmpty || (result.content()?.toString() ?? "").contains("Address Line 1Address Line 2Address Line 3"), "expected to contain: \("Address Line 1Address Line 2Address Line 3")")
    }

    func testVisitorLinkBareStringPreservesCase() throws {
        // Visitor returns rendered template as a bare string (not a dict) and original mixed-case content is preserved end-to-end (regression guard for issue #350)
        final class LocalVisitor_VisitorLinkBareStringPreservesCase: HtmlVisitorProtocol {
    func visitLink(_ ctx: NodeContext, _ href: String, _ text: String, _ title: String?) -> VisitResult { return .custom(field0: "[\(text)](https://new-cdn.com/file.pdf)") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorLinkBareStringPreservesCase())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<a href=\"https://old-cdn.com/file.pdf\">Download</a>", options: _options)
        XCTAssertTrue("[Download](https://new-cdn.com/file.pdf)".isEmpty || (result.content()?.toString() ?? "").contains("[Download](https://new-cdn.com/file.pdf)"), "expected to contain: \("[Download](https://new-cdn.com/file.pdf)")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("[download]"), "expected NOT to contain: \("[download]")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("old-cdn.com"), "expected NOT to contain: \("old-cdn.com")")
    }

    func testVisitorMarkCustom() throws {
        // Visitor replaces highlight/mark with custom template
        final class LocalVisitor_VisitorMarkCustom: HtmlVisitorProtocol {
    func visitMark(_ ctx: NodeContext, _ text: String) -> VisitResult { return .custom(field0: "==\(text)==") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorMarkCustom())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>This is a <mark>highlighted passage</mark> in the text.</p>", options: _options)
        XCTAssertTrue("==highlighted passage==".isEmpty || (result.content()?.toString() ?? "").contains("==highlighted passage=="), "expected to contain: \("==highlighted passage==")")
        XCTAssertTrue("This is a".isEmpty || (result.content()?.toString() ?? "").contains("This is a"), "expected to contain: \("This is a")")
    }

    func testVisitorMarkSkip() throws {
        // Visitor skips mark elements entirely
        final class LocalVisitor_VisitorMarkSkip: HtmlVisitorProtocol {
    func visitMark(_ ctx: NodeContext, _ text: String) -> VisitResult { return .skip }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorMarkSkip())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>Key insight: <mark>always validate input</mark> for security.</p>", options: _options)
        XCTAssertFalse((result.content()?.toString() ?? "").contains("always validate input"), "expected NOT to contain: \("always validate input")")
        XCTAssertTrue("Key insight:".isEmpty || (result.content()?.toString() ?? "").contains("Key insight:"), "expected to contain: \("Key insight:")")
        XCTAssertTrue("for security.".isEmpty || (result.content()?.toString() ?? "").contains("for security."), "expected to contain: \("for security.")")
    }

    func testVisitorPreserveHtml() throws {
        // Visitor preserve_html action includes raw HTML in output
        final class LocalVisitor_VisitorPreserveHtml: HtmlVisitorProtocol {
    func visitCustomElement(_ ctx: NodeContext, _ tagName: String, _ html: String) -> VisitResult { return .preserveHtml }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorPreserveHtml())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<div><custom-tag>Custom content</custom-tag></div>", options: _options)
        XCTAssertTrue("<custom-tag>".isEmpty || (result.content()?.toString() ?? "").contains("<custom-tag>"), "expected to contain: \("<custom-tag>")")
    }

    func testVisitorSkipCodeBlocks() throws {
        // Visitor skips code blocks from output
        final class LocalVisitor_VisitorSkipCodeBlocks: HtmlVisitorProtocol {
    func visitCodeBlock(_ ctx: NodeContext, _ lang: String?, _ code: String) -> VisitResult { return .skip }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorSkipCodeBlocks())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>Intro text</p><pre><code>let x = 42;</code></pre><p>Outro text</p>", options: _options)
        XCTAssertTrue("Intro text".isEmpty || (result.content()?.toString() ?? "").contains("Intro text"), "expected to contain: \("Intro text")")
        XCTAssertTrue("Outro text".isEmpty || (result.content()?.toString() ?? "").contains("Outro text"), "expected to contain: \("Outro text")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("let x = 42"), "expected NOT to contain: \("let x = 42")")
    }

    func testVisitorSkipHeading() throws {
        // Visitor skip action omits all headings from output
        final class LocalVisitor_VisitorSkipHeading: HtmlVisitorProtocol {
    func visitHeading(_ ctx: NodeContext, _ level: UInt32, _ text: String, _ id: String?) -> VisitResult { return .skip }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorSkipHeading())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<h1>Title</h1><p>Body text remains.</p>", options: _options)
        XCTAssertFalse((result.content()?.toString() ?? "").contains("Title"), "expected NOT to contain: \("Title")")
        XCTAssertTrue("Body text remains.".isEmpty || (result.content()?.toString() ?? "").contains("Body text remains."), "expected to contain: \("Body text remains.")")
    }

    func testVisitorSkipImages() throws {
        // Visitor skips all images from output
        final class LocalVisitor_VisitorSkipImages: HtmlVisitorProtocol {
    func visitImage(_ ctx: NodeContext, _ src: String, _ alt: String, _ title: String?) -> VisitResult { return .skip }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorSkipImages())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>Before image</p><img src=\"photo.jpg\" alt=\"A photo\"><p>After image</p>", options: _options)
        XCTAssertTrue("Before image".isEmpty || (result.content()?.toString() ?? "").contains("Before image"), "expected to contain: \("Before image")")
        XCTAssertTrue("After image".isEmpty || (result.content()?.toString() ?? "").contains("After image"), "expected to contain: \("After image")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("photo.jpg"), "expected NOT to contain: \("photo.jpg")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("A photo"), "expected NOT to contain: \("A photo")")
    }

    func testVisitorSkipLinks() throws {
        // Visitor skips all links entirely
        final class LocalVisitor_VisitorSkipLinks: HtmlVisitorProtocol {
    func visitLink(_ ctx: NodeContext, _ href: String, _ text: String, _ title: String?) -> VisitResult { return .skip }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorSkipLinks())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>Before <a href=\"https://example.com\">link text</a> after</p>", options: _options)
        XCTAssertFalse((result.content()?.toString() ?? "").contains("link text"), "expected NOT to contain: \("link text")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("example.com"), "expected NOT to contain: \("example.com")")
    }

    func testVisitorSkipStrong() throws {
        // Visitor skips bold/strong elements
        final class LocalVisitor_VisitorSkipStrong: HtmlVisitorProtocol {
    func visitStrong(_ ctx: NodeContext, _ text: String) -> VisitResult { return .skip }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorSkipStrong())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>Normal <strong>bold text</strong> normal</p>", options: _options)
        XCTAssertFalse((result.content()?.toString() ?? "").contains("bold text"), "expected NOT to contain: \("bold text")")
        XCTAssertTrue("Normal".isEmpty || (result.content()?.toString() ?? "").contains("Normal"), "expected to contain: \("Normal")")
    }

    func testVisitorSubscriptCustom() throws {
        // Visitor replaces subscript with custom template
        final class LocalVisitor_VisitorSubscriptCustom: HtmlVisitorProtocol {
    func visitSubscript(_ ctx: NodeContext, _ text: String) -> VisitResult { return .custom(field0: "~\(text)~") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorSubscriptCustom())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>H<sub>2</sub>O is water.</p>", options: _options)
        XCTAssertTrue("H~2~O".isEmpty || (result.content()?.toString() ?? "").contains("H~2~O"), "expected to contain: \("H~2~O")")
        XCTAssertTrue("is water".isEmpty || (result.content()?.toString() ?? "").contains("is water"), "expected to contain: \("is water")")
    }

    func testVisitorSubscriptSkip() throws {
        // Visitor skips subscript elements entirely
        final class LocalVisitor_VisitorSubscriptSkip: HtmlVisitorProtocol {
    func visitSubscript(_ ctx: NodeContext, _ text: String) -> VisitResult { return .skip }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorSubscriptSkip())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>The formula C<sub>12</sub>H<sub>22</sub>O<sub>11</sub> is sugar.</p>", options: _options)
        XCTAssertTrue("The formula CHO is sugar.".isEmpty || (result.content()?.toString() ?? "").contains("The formula CHO is sugar."), "expected to contain: \("The formula CHO is sugar.")")
    }

    func testVisitorSuperscriptCustom() throws {
        // Visitor replaces superscript with custom template
        final class LocalVisitor_VisitorSuperscriptCustom: HtmlVisitorProtocol {
    func visitSuperscript(_ ctx: NodeContext, _ text: String) -> VisitResult { return .custom(field0: "^\(text)^") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorSuperscriptCustom())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>Einstein's E=mc<sup>2</sup> revolutionized physics.</p>", options: _options)
        XCTAssertTrue("E=mc^2^".isEmpty || (result.content()?.toString() ?? "").contains("E=mc^2^"), "expected to contain: \("E=mc^2^")")
        XCTAssertTrue("revolutionized physics".isEmpty || (result.content()?.toString() ?? "").contains("revolutionized physics"), "expected to contain: \("revolutionized physics")")
    }

    func testVisitorSuperscriptSkip() throws {
        // Visitor skips superscript from output
        final class LocalVisitor_VisitorSuperscriptSkip: HtmlVisitorProtocol {
    func visitSuperscript(_ ctx: NodeContext, _ text: String) -> VisitResult { return .skip }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorSuperscriptSkip())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>The equation x<sup>3</sup> + y<sup>3</sup> = z<sup>3</sup> has no solutions.</p>", options: _options)
        XCTAssertTrue("The equation x + y = z has no solutions.".isEmpty || (result.content()?.toString() ?? "").contains("The equation x + y = z has no solutions."), "expected to contain: \("The equation x + y = z has no solutions.")")
    }

    func testVisitorUnderlineCustom() throws {
        // Visitor replaces underline with custom markup
        final class LocalVisitor_VisitorUnderlineCustom: HtmlVisitorProtocol {
    func visitUnderline(_ ctx: NodeContext, _ text: String) -> VisitResult { return .custom(field0: "_\(text)_") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorUnderlineCustom())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>This is <u>very important</u> text.</p>", options: _options)
        XCTAssertTrue("_very important_".isEmpty || (result.content()?.toString() ?? "").contains("_very important_"), "expected to contain: \("_very important_")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("**"), "expected NOT to contain: \("**")")
    }

    func testVisitorUnderlineSkip() throws {
        // Visitor skips underline elements from output
        final class LocalVisitor_VisitorUnderlineSkip: HtmlVisitorProtocol {
    func visitUnderline(_ ctx: NodeContext, _ text: String) -> VisitResult { return .skip }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorUnderlineSkip())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>Normal text with <u>underlined part</u> and more text.</p>", options: _options)
        XCTAssertTrue("Normal text with".isEmpty || (result.content()?.toString() ?? "").contains("Normal text with"), "expected to contain: \("Normal text with")")
        XCTAssertTrue("and more text.".isEmpty || (result.content()?.toString() ?? "").contains("and more text."), "expected to contain: \("and more text.")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("underlined part"), "expected NOT to contain: \("underlined part")")
    }

    func testVisitorVideoCustom() throws {
        // Visitor replaces video with custom link
        final class LocalVisitor_VisitorVideoCustom: HtmlVisitorProtocol {
    func visitVideo(_ ctx: NodeContext, _ src: String?) -> VisitResult { return .custom(field0: "[VIDEO: \(src ?? "")]") }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorVideoCustom())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<p>Watch our tutorial:</p><video src=\"tutorial.mp4\" width=\"320\" height=\"240\" controls></video><p>Great content!</p>", options: _options)
        XCTAssertTrue("[VIDEO: tutorial.mp4]".isEmpty || (result.content()?.toString() ?? "").contains("[VIDEO: tutorial.mp4]"), "expected to contain: \("[VIDEO: tutorial.mp4]")")
        XCTAssertTrue("Watch our tutorial:".isEmpty || (result.content()?.toString() ?? "").contains("Watch our tutorial:"), "expected to contain: \("Watch our tutorial:")")
        XCTAssertTrue("Great content!".isEmpty || (result.content()?.toString() ?? "").contains("Great content!"), "expected to contain: \("Great content!")")
    }

    func testVisitorVideoSkip() throws {
        // Visitor removes video elements entirely
        final class LocalVisitor_VisitorVideoSkip: HtmlVisitorProtocol {
    func visitVideo(_ ctx: NodeContext, _ src: String?) -> VisitResult { return .skip }
}

        let _visitorHandle_options = makeHtmlVisitorHandle(LocalVisitor_VisitorVideoSkip())
        let _options = try HtmlToMarkdown.conversionOptionsFromJsonWithVisitor("{}", _visitorHandle_options)
        let result = try HtmlToMarkdown.convert(html: "<h2>Demo</h2><video src=\"demo.webm\"></video><p>See the demo above.</p>", options: _options)
        XCTAssertTrue("Demo".isEmpty || (result.content()?.toString() ?? "").contains("Demo"), "expected to contain: \("Demo")")
        XCTAssertTrue("See the demo above.".isEmpty || (result.content()?.toString() ?? "").contains("See the demo above."), "expected to contain: \("See the demo above.")")
        XCTAssertFalse((result.content()?.toString() ?? "").contains("demo.webm"), "expected NOT to contain: \("demo.webm")")
    }

}
