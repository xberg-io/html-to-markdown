// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:24c0ffd6b17eec9f4e04837bd784d4153cec6ba160c6a367c428a7a3cde6b1aa
// To regenerate: alef generate
// To verify freshness: alef verify
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import HtmlToMarkdown
import RustBridge

/// E2e tests for category: conversion.
final class ConversionTests: XCTestCase {
    override class func setUp() {
        super.setUp()
    }

    func testAdjacentCodeSpansMerged() throws {
        // Adjacent code spans merge instead of colliding backticks
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p><code>A</code><code>B</code></p>", options: _options)
        XCTAssertEqual((result.content ?? ""), "`AB`\n")
    }

    func testAdjacentEmphasisMerged() throws {
        // Adjacent emphasis elements share one delimiter run
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p><i>A</i><i>B</i><i>C</i></p>", options: _options)
        XCTAssertEqual((result.content ?? ""), "*ABC*\n")
    }

    func testAdjacentInsertedMerged() throws {
        // Adjacent inserted elements share one delimiter run
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p><ins>A</ins><ins>B</ins></p>", options: _options)
        XCTAssertEqual((result.content ?? ""), "==AB==\n")
    }

    func testAdjacentStrikethroughMerged() throws {
        // Adjacent strikethrough elements share one delimiter run
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p><del>A</del><del>B</del></p>", options: _options)
        XCTAssertEqual((result.content ?? ""), "~~AB~~\n")
    }

    func testAdjacentVariableMerged() throws {
        // Adjacent variable elements share one delimiter run
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p><var>A</var><var>B</var></p>", options: _options)
        XCTAssertEqual((result.content ?? ""), "*AB*\n")
    }

    func testBlockquoteCodeBlockIndentationPreserved() throws {
        // Code block inside a blockquote keeps its line indentation
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<blockquote><pre><code>line1\n    line2 indented</code></pre></blockquote>", options: _options)
        XCTAssertEqual((result.content ?? ""), "> ```\n> line1\n>     line2 indented\n> ```\n")
    }

    func testBlockquoteMultipleParagraphs() throws {
        // Blockquote with multiple paragraphs has each paragraph prefixed
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<blockquote><p>First paragraph.</p><p>Second paragraph.</p></blockquote>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("> First paragraph."), "expected to contain: \("> First paragraph.")")
        XCTAssertTrue((result.content ?? "").contains("> Second paragraph."), "expected to contain: \("> Second paragraph.")")
    }

    func testBlockquoteNested() throws {
        // Nested blockquote produces double-prefixed lines
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<blockquote><p>Outer quote.</p><blockquote><p>Inner quote.</p></blockquote></blockquote>", options: _options)
        XCTAssertTrue(result.content != nil, "expected non-empty value")
        XCTAssertTrue((result.content ?? "").contains("Outer quote."), "expected to contain: \("Outer quote.")")
        XCTAssertTrue((result.content ?? "").contains("Inner quote."), "expected to contain: \("Inner quote.")")
    }

    func testBlockquoteNestedListIndentationPreserved() throws {
        // Nested list inside a blockquote keeps its continuation indentation
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<blockquote><ul><li>item a<ul><li>sub a1</li></ul></li></ul></blockquote>", options: _options)
        XCTAssertEqual((result.content ?? ""), "> - item a\n>   * sub a1\n")
    }

    func testBlockquoteSimple() throws {
        // Simple blockquote
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<blockquote><p>Quote text</p></blockquote>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("> Quote text"), "expected to contain: \("> Quote text")")
    }

    func testBlockquoteTextThenParagraphGetsBlankLine() throws {
        // Bare text followed by a paragraph inside a blockquote gets a blank-line separator instead of merging into one line
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<blockquote>Just text, then <p>a paragraph</p></blockquote>", options: _options)
        XCTAssertEqual((result.content ?? ""), "> Just text, then\n>\n> a paragraph\n")
    }

    func testBlockquoteWithList() throws {
        // Blockquote containing a list preserves list items inside quote
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<blockquote><p>Quote intro:</p><ul><li>Point one</li><li>Point two</li></ul></blockquote>", options: _options)
        XCTAssertTrue(result.content != nil, "expected non-empty value")
        XCTAssertTrue((result.content ?? "").contains("Quote intro:"), "expected to contain: \("Quote intro:")")
        XCTAssertTrue((result.content ?? "").contains("Point one"), "expected to contain: \("Point one")")
        XCTAssertTrue((result.content ?? "").contains("Point two"), "expected to contain: \("Point two")")
    }

    func testBoldAndItalic() throws {
        // Nested bold and italic
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p><strong><em>both</em></strong></p>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("***both***"), "expected to contain: \("***both***")")
    }

    func testBoldStrong() throws {
        // Strong tag converts to bold
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p><strong>bold</strong></p>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("**bold**"), "expected to contain: \("**bold**")")
    }

    func testCodeBlock() throws {
        // Code block with language preserves content
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<pre><code class=\"language-python\">print('hello')</code></pre>", options: _options)
        XCTAssertTrue(result.content != nil, "expected non-empty value")
        XCTAssertTrue((result.content ?? "").contains("print('hello')"), "expected to contain: \("print('hello')")")
    }

    func testCodeBlockNoLanguage() throws {
        // Code block without a language class preserves content
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<pre><code>plain code here</code></pre>", options: _options)
        XCTAssertTrue(result.content != nil, "expected non-empty value")
        XCTAssertTrue((result.content ?? "").contains("plain code here"), "expected to contain: \("plain code here")")
    }

    func testCodeInlineInParagraph() throws {
        // Inline code element nested inside a paragraph
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p>Call the <code>initialize()</code> method first.</p>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("`initialize()`"), "expected to contain: \("`initialize()`")")
    }

    func testCodeSpanSuppressesNestedInlineMarkers() throws {
        // Inline markers inside a code span stay literal content
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p><code>a<em>b</em><ins>c</ins></code></p>", options: _options)
        XCTAssertEqual((result.content ?? ""), "`abc`\n")
    }

    func testCodeWithBackticksInContent() throws {
        // Inline code containing backtick characters is properly escaped
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p>Use <code>`backtick` here</code> carefully.</p>", options: _options)
        XCTAssertTrue(result.content != nil, "expected non-empty value")
        XCTAssertTrue((result.content ?? "").contains("backtick"), "expected to contain: \("backtick")")
    }

    func testConversionAutolinkFilenameNotAutolinked() throws {
        // Bare filename href (no URI scheme) renders as inline link, not autolink (#397)
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<a href=\"foobar.png\">foobar.png</a>", options: _options)
        XCTAssertEqual((result.content ?? ""), "[foobar.png](foobar.png)\n")
    }

    func testConversionAutolinkHttpsUrl() throws {
        // Absolute https URL with matching text renders as autolink (#397)
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<a href=\"https://example.com\">https://example.com</a>", options: _options)
        XCTAssertEqual((result.content ?? ""), "<https://example.com>\n")
    }

    func testConversionAutolinkMailto() throws {
        // mailto: link with matching email text renders as autolink (#397)
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<a href=\"mailto:a@b.com\">a@b.com</a>", options: _options)
        XCTAssertEqual((result.content ?? ""), "<a@b.com>\n")
    }

    func testConversionAutolinkMixedFilenameAndUrl() throws {
        // Mixed filename + URL: only the URL becomes an autolink (#397)
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<a href=\"foobar.png\">foobar.png</a> <a href=\"https://www.heise.de\">https://www.heise.de</a>", options: _options)
        XCTAssertEqual((result.content ?? ""), "[foobar.png](foobar.png) <https://www.heise.de>\n")
    }

    func testConversionAutolinkRelativePathNotAutolinked() throws {
        // Relative path href renders as inline link (#397)
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<a href=\"/docs/intro.html\">/docs/intro.html</a>", options: _options)
        XCTAssertEqual((result.content ?? ""), "[/docs/intro.html](/docs/intro.html)\n")
    }

    func testEmphasisMarkHighlight() throws {
        // mark tag produces highlighted output
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p><mark>highlighted</mark></p>", options: _options)
        XCTAssertTrue(result.content != nil, "expected non-empty value")
        XCTAssertTrue((result.content ?? "").contains("highlighted"), "expected to contain: \("highlighted")")
    }

    func testEmphasisStrikethroughDel() throws {
        // del tag converts to GFM strikethrough
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p><del>deleted text</del></p>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("~~deleted text~~"), "expected to contain: \("~~deleted text~~")")
    }

    func testEmphasisStrikethroughS() throws {
        // s tag converts to GFM strikethrough
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p><s>strikethrough</s></p>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("~~strikethrough~~"), "expected to contain: \("~~strikethrough~~")")
    }

    func testEmphasisSubscript() throws {
        // sub tag content is preserved
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p>H<sub>2</sub>O</p>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("H"), "expected to contain: \("H")")
        XCTAssertTrue((result.content ?? "").contains("2"), "expected to contain: \("2")")
        XCTAssertTrue((result.content ?? "").contains("O"), "expected to contain: \("O")")
    }

    func testEmphasisSuperscript() throws {
        // sup tag content is preserved
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p>x<sup>2</sup></p>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("x"), "expected to contain: \("x")")
        XCTAssertTrue((result.content ?? "").contains("2"), "expected to contain: \("2")")
    }

    func testEmphasisUnderlineU() throws {
        // u tag content is preserved in output
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p><u>underlined</u></p>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("underlined"), "expected to contain: \("underlined")")
    }

    func testFormInputElements() throws {
        // Form input elements produce readable output without form mechanics
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"preprocessing\":{\"remove_forms\":false}}")
        let result = try HtmlToMarkdown.convert(html: "<form><label for=\"name\">Name:</label><input type=\"text\" id=\"name\" placeholder=\"Enter name\"></form>", options: _options)
        XCTAssertTrue(result.content != nil, "expected non-empty value")
        XCTAssertTrue((result.content ?? "").contains("Name"), "expected to contain: \("Name")")
    }

    func testFormSelectOptions() throws {
        // Select element with options produces readable output
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"preprocessing\":{\"remove_forms\":false}}")
        let result = try HtmlToMarkdown.convert(html: "<form><label>Color:</label><select><option value=\"red\">Red</option><option value=\"blue\" selected>Blue</option><option value=\"green\">Green</option></select></form>", options: _options)
        XCTAssertTrue(result.content != nil, "expected non-empty value")
        XCTAssertTrue((result.content ?? "").contains("Color"), "expected to contain: \("Color")")
    }

    func testFormTextarea() throws {
        // Textarea element produces readable output
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"preprocessing\":{\"remove_forms\":false}}")
        let result = try HtmlToMarkdown.convert(html: "<form><label>Message:</label><textarea>Default text content</textarea></form>", options: _options)
        XCTAssertTrue(result.content != nil, "expected non-empty value")
        XCTAssertTrue((result.content ?? "").contains("Message"), "expected to contain: \("Message")")
    }

    func testHeadingH1() throws {
        // H1 heading
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<h1>Heading 1</h1>", options: _options)
        XCTAssertEqual((result.content ?? ""), "# Heading 1\n")
    }

    func testHeadingH2() throws {
        // H2 heading
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<h2>Heading 2</h2>", options: _options)
        XCTAssertEqual((result.content ?? ""), "## Heading 2\n")
    }

    func testHeadingH3() throws {
        // H3 heading
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<h3>Heading 3</h3>", options: _options)
        XCTAssertEqual((result.content ?? ""), "### Heading 3\n")
    }

    func testHeadingH4() throws {
        // H4 heading
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<h4>Heading 4</h4>", options: _options)
        XCTAssertEqual((result.content ?? ""), "#### Heading 4\n")
    }

    func testHeadingH5() throws {
        // H5 heading
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<h5>Heading 5</h5>", options: _options)
        XCTAssertEqual((result.content ?? ""), "##### Heading 5\n")
    }

    func testHeadingH6() throws {
        // H6 heading
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<h6>Heading 6</h6>", options: _options)
        XCTAssertEqual((result.content ?? ""), "###### Heading 6\n")
    }

    func testImageFigureFigcaption() throws {
        // Figure with figcaption preserves both image and caption
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<figure><img src=\"sunset.jpg\" alt=\"A sunset\"><figcaption>Beautiful sunset over the ocean</figcaption></figure>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("![A sunset](sunset.jpg)"), "expected to contain: \("![A sunset](sunset.jpg)")")
        XCTAssertTrue((result.content ?? "").contains("Beautiful sunset over the ocean"), "expected to contain: \("Beautiful sunset over the ocean")")
    }

    func testImageLinked() throws {
        // Image inside an anchor produces a linked image
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<a href=\"https://example.com\"><img src=\"icon.png\" alt=\"Icon\"></a>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("![Icon](icon.png)"), "expected to contain: \("![Icon](icon.png)")")
        XCTAssertTrue((result.content ?? "").contains("https://example.com"), "expected to contain: \("https://example.com")")
    }

    func testImageNoAlt() throws {
        // Image without alt text produces image markdown
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<img src=\"banner.jpg\">", options: _options)
        XCTAssertTrue(result.content != nil, "expected non-empty value")
        XCTAssertTrue((result.content ?? "").contains("banner.jpg"), "expected to contain: \("banner.jpg")")
    }

    func testImageSimple() throws {
        // Image with alt text
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<img src=\"photo.jpg\" alt=\"A photo\">", options: _options)
        XCTAssertTrue((result.content ?? "").contains("![A photo](photo.jpg)"), "expected to contain: \("![A photo](photo.jpg)")")
    }

    func testImageWithTitle() throws {
        // Image with title attribute includes title in output
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<img src=\"chart.png\" alt=\"Sales chart\" title=\"Q3 Sales\">", options: _options)
        XCTAssertTrue((result.content ?? "").contains("![Sales chart](chart.png"), "expected to contain: \("![Sales chart](chart.png")")
        XCTAssertTrue((result.content ?? "").contains("Q3 Sales"), "expected to contain: \("Q3 Sales")")
    }

    func testInlineCode() throws {
        // Inline code
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p>Use <code>console.log()</code> to debug</p>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("`console.log()`"), "expected to contain: \("`console.log()`")")
    }

    func testItalicEm() throws {
        // Em tag converts to italic
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p><em>italic</em></p>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("*italic*"), "expected to contain: \("*italic*")")
    }

    func testLineBreakBrTag() throws {
        // Single br tag produces a line break in output
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p>First line.<br>Second line.</p>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("First line."), "expected to contain: \("First line.")")
        XCTAssertTrue((result.content ?? "").contains("Second line."), "expected to contain: \("Second line.")")
    }

    func testLineBreakHrTag() throws {
        // hr tag produces a horizontal separator between content
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p>Before rule.</p><hr><p>After rule.</p>", options: _options)
        XCTAssertTrue(result.content != nil, "expected non-empty value")
        XCTAssertTrue((result.content ?? "").contains("Before rule."), "expected to contain: \("Before rule.")")
        XCTAssertTrue((result.content ?? "").contains("After rule."), "expected to contain: \("After rule.")")
    }

    func testLineBreakMultipleBr() throws {
        // Multiple consecutive br tags in sequence
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p>Start.<br><br>End.</p>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("Start."), "expected to contain: \("Start.")")
        XCTAssertTrue((result.content ?? "").contains("End."), "expected to contain: \("End.")")
    }

    func testLinkAnchorFragment() throws {
        // Fragment-only anchor link is preserved
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<a href=\"#section\">Jump to section</a>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("[Jump to section](#section)"), "expected to contain: \("[Jump to section](#section)")")
    }

    func testLinkEmptyHref() throws {
        // Link with empty href produces output with the link text
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<a href=\"\">No destination</a>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("No destination"), "expected to contain: \("No destination")")
    }

    func testLinkImageInside() throws {
        // Image inside a link produces a linked image
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<a href=\"https://example.com\"><img src=\"logo.png\" alt=\"Logo\"></a>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("![Logo](logo.png)"), "expected to contain: \("![Logo](logo.png)")")
        XCTAssertTrue((result.content ?? "").contains("https://example.com"), "expected to contain: \("https://example.com")")
    }

    func testLinkMailto() throws {
        // Mailto link is preserved with mailto: scheme
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<a href=\"mailto:user@example.com\">Email us</a>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("mailto:user@example.com"), "expected to contain: \("mailto:user@example.com")")
    }

    func testLinkSimple() throws {
        // Simple link
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<a href=\"https://example.com\">Example</a>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("[Example](https://example.com)"), "expected to contain: \("[Example](https://example.com)")")
    }

    func testLinkWithBoldText() throws {
        // Link containing bold text preserves formatting
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<a href=\"https://example.com\"><strong>Bold link</strong></a>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("**Bold link**"), "expected to contain: \("**Bold link**")")
        XCTAssertTrue((result.content ?? "").contains("https://example.com"), "expected to contain: \("https://example.com")")
    }

    func testLinkWithTitle() throws {
        // Link with title attribute
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<a href=\"https://example.com\" title=\"Example Site\">Example</a>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("[Example](https://example.com"), "expected to contain: \("[Example](https://example.com")")
        XCTAssertTrue((result.content ?? "").contains("Example Site"), "expected to contain: \("Example Site")")
    }

    func testListDefinitionDl() throws {
        // Definition list with dt and dd elements
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<dl><dt>Term One</dt><dd>Definition of term one.</dd><dt>Term Two</dt><dd>Definition of term two.</dd></dl>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("Term One"), "expected to contain: \("Term One")")
        XCTAssertTrue((result.content ?? "").contains("Definition of term one."), "expected to contain: \("Definition of term one.")")
        XCTAssertTrue((result.content ?? "").contains("Term Two"), "expected to contain: \("Term Two")")
        XCTAssertTrue((result.content ?? "").contains("Definition of term two."), "expected to contain: \("Definition of term two.")")
    }

    func testListItemMultipleParagraphs() throws {
        // List item containing multiple paragraphs
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<ul><li><p>First paragraph in item.</p><p>Second paragraph in item.</p></li><li>Simple item</li></ul>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("First paragraph in item."), "expected to contain: \("First paragraph in item.")")
        XCTAssertTrue((result.content ?? "").contains("Second paragraph in item."), "expected to contain: \("Second paragraph in item.")")
        XCTAssertTrue((result.content ?? "").contains("Simple item"), "expected to contain: \("Simple item")")
    }

    func testListMixedNested() throws {
        // Mixed list: ordered list nested inside unordered list
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<ul><li>Item A<ol><li>Sub 1</li><li>Sub 2</li></ol></li><li>Item B</li></ul>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("Item A"), "expected to contain: \("Item A")")
        XCTAssertTrue((result.content ?? "").contains("Sub 1"), "expected to contain: \("Sub 1")")
        XCTAssertTrue((result.content ?? "").contains("Sub 2"), "expected to contain: \("Sub 2")")
        XCTAssertTrue((result.content ?? "").contains("Item B"), "expected to contain: \("Item B")")
    }

    func testListNestedOrdered() throws {
        // Nested ordered list with two levels of depth
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<ol><li>Step 1<ol><li>Step 1a</li><li>Step 1b</li></ol></li><li>Step 2</li></ol>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("Step 1"), "expected to contain: \("Step 1")")
        XCTAssertTrue((result.content ?? "").contains("Step 1a"), "expected to contain: \("Step 1a")")
        XCTAssertTrue((result.content ?? "").contains("Step 1b"), "expected to contain: \("Step 1b")")
        XCTAssertTrue((result.content ?? "").contains("Step 2"), "expected to contain: \("Step 2")")
    }

    func testListNestedUnordered() throws {
        // Nested unordered list with two levels of depth
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<ul><li>Parent A<ul><li>Child A1</li><li>Child A2</li></ul></li><li>Parent B</li></ul>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("Parent A"), "expected to contain: \("Parent A")")
        XCTAssertTrue((result.content ?? "").contains("Child A1"), "expected to contain: \("Child A1")")
        XCTAssertTrue((result.content ?? "").contains("Child A2"), "expected to contain: \("Child A2")")
        XCTAssertTrue((result.content ?? "").contains("Parent B"), "expected to contain: \("Parent B")")
    }

    func testListTaskCheckboxes() throws {
        // Task list with checked and unchecked checkboxes
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<ul><li><input type=\"checkbox\" checked> Done task</li><li><input type=\"checkbox\"> Pending task</li></ul>", options: _options)
        XCTAssertTrue(result.content != nil, "expected non-empty value")
        XCTAssertTrue((result.content ?? "").contains("Done task"), "expected to contain: \("Done task")")
        XCTAssertTrue((result.content ?? "").contains("Pending task"), "expected to contain: \("Pending task")")
    }

    func testOrderedList() throws {
        // Ordered list
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<ol><li>First</li><li>Second</li><li>Third</li></ol>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("1. First"), "expected to contain: \("1. First")")
        XCTAssertTrue((result.content ?? "").contains("2. Second"), "expected to contain: \("2. Second")")
        XCTAssertTrue((result.content ?? "").contains("3. Third"), "expected to contain: \("3. Third")")
    }

    func testParagraphMultiple() throws {
        // Multiple paragraphs are separated by a blank line
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p>First paragraph.</p><p>Second paragraph.</p>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("First paragraph."), "expected to contain: \("First paragraph.")")
        XCTAssertTrue((result.content ?? "").contains("Second paragraph."), "expected to contain: \("Second paragraph.")")
    }

    func testParagraphNestedDivs() throws {
        // Text nested inside divs is extracted correctly
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<div><div><p>Nested text</p></div></div>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("Nested text"), "expected to contain: \("Nested text")")
    }

    func testParagraphSimple() throws {
        // Simple paragraph converts to plain text
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p>Hello World</p>", options: _options)
        XCTAssertEqual((result.content ?? ""), "Hello World\n")
    }

    func testParagraphWithInlineFormatting() throws {
        // Paragraph with bold, italic, and a link
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p>This has <strong>bold</strong>, <em>italic</em>, and a <a href=\"https://example.com\">link</a>.</p>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("**bold**"), "expected to contain: \("**bold**")")
        XCTAssertTrue((result.content ?? "").contains("*italic*"), "expected to contain: \("*italic*")")
        XCTAssertTrue((result.content ?? "").contains("[link](https://example.com)"), "expected to contain: \("[link](https://example.com)")")
    }

    func testParagraphWithLineBreaks() throws {
        // Paragraph with br tags produces line breaks in output
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p>Line one.<br>Line two.<br>Line three.</p>", options: _options)
        XCTAssertTrue(result.content != nil, "expected non-empty value")
        XCTAssertTrue((result.content ?? "").contains("Line one."), "expected to contain: \("Line one.")")
        XCTAssertTrue((result.content ?? "").contains("Line two."), "expected to contain: \("Line two.")")
        XCTAssertTrue((result.content ?? "").contains("Line three."), "expected to contain: \("Line three.")")
    }

    func testSemanticAbbr() throws {
        // Abbreviation element text is preserved
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p>The <abbr title=\"World Wide Web\">WWW</abbr> is global.</p>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("WWW"), "expected to contain: \("WWW")")
    }

    func testSemanticArticle() throws {
        // Article element wrapping content preserves inner content
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<article><h2>Article Title</h2><p>Article body.</p></article>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("Article Title"), "expected to contain: \("Article Title")")
        XCTAssertTrue((result.content ?? "").contains("Article body."), "expected to contain: \("Article body.")")
    }

    func testSemanticDefinitionList() throws {
        // Definition list with term and description
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<dl><dt>HTML</dt><dd>HyperText Markup Language</dd><dt>CSS</dt><dd>Cascading Style Sheets</dd></dl>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("HTML"), "expected to contain: \("HTML")")
        XCTAssertTrue((result.content ?? "").contains("HyperText Markup Language"), "expected to contain: \("HyperText Markup Language")")
        XCTAssertTrue((result.content ?? "").contains("CSS"), "expected to contain: \("CSS")")
        XCTAssertTrue((result.content ?? "").contains("Cascading Style Sheets"), "expected to contain: \("Cascading Style Sheets")")
    }

    func testSemanticDetailsSummary() throws {
        // Details and summary elements produce readable output
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<details><summary>Click to expand</summary><p>Hidden content here.</p></details>", options: _options)
        XCTAssertTrue(result.content != nil, "expected non-empty value")
        XCTAssertTrue((result.content ?? "").contains("Click to expand"), "expected to contain: \("Click to expand")")
    }

    func testSemanticHr() throws {
        // Horizontal rule produces a separator in output
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p>Above</p><hr><p>Below</p>", options: _options)
        XCTAssertTrue(result.content != nil, "expected non-empty value")
        XCTAssertTrue((result.content ?? "").contains("Above"), "expected to contain: \("Above")")
        XCTAssertTrue((result.content ?? "").contains("Below"), "expected to contain: \("Below")")
    }

    func testSemanticMarkHighlight() throws {
        // Mark tag produces highlighted output
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p>This is <mark>highlighted text</mark> in a sentence.</p>", options: _options)
        XCTAssertTrue(result.content != nil, "expected non-empty value")
        XCTAssertTrue((result.content ?? "").contains("highlighted text"), "expected to contain: \("highlighted text")")
    }

    func testSemanticSectionWithHeading() throws {
        // Section element with heading preserves structure
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<section><h3>Section Heading</h3><p>Section content.</p></section>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("Section Heading"), "expected to contain: \("Section Heading")")
        XCTAssertTrue((result.content ?? "").contains("Section content."), "expected to contain: \("Section content.")")
    }

    func testSemanticSubSuperscript() throws {
        // Subscript and superscript elements are preserved in output
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p>H<sub>2</sub>O and E=mc<sup>2</sup></p>", options: _options)
        XCTAssertTrue(result.content != nil, "expected non-empty value")
        XCTAssertTrue((result.content ?? "").contains("H"), "expected to contain: \("H")")
        XCTAssertTrue((result.content ?? "").contains("2"), "expected to contain: \("2")")
        XCTAssertTrue((result.content ?? "").contains("O"), "expected to contain: \("O")")
        XCTAssertTrue((result.content ?? "").contains("E=mc"), "expected to contain: \("E=mc")")
    }

    func testSimpleTable() throws {
        // Simple table with header
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<table><thead><tr><th>Name</th><th>Age</th></tr></thead><tbody><tr><td>Alice</td><td>30</td></tr></tbody></table>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("Name"), "expected to contain: \("Name")")
        XCTAssertTrue((result.content ?? "").contains("Age"), "expected to contain: \("Age")")
        XCTAssertTrue((result.content ?? "").contains("Alice"), "expected to contain: \("Alice")")
        XCTAssertTrue((result.content ?? "").contains("30"), "expected to contain: \("30")")
        XCTAssertTrue((result.content ?? "").contains("|"), "expected to contain: \("|")")
        XCTAssertTrue((result.content ?? "").contains("---"), "expected to contain: \("---")")
    }

    func testStrikeRendersAsStrikethrough() throws {
        // The deprecated strike tag renders as strikethrough
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p><strike>gone</strike></p>", options: _options)
        XCTAssertEqual((result.content ?? ""), "~~gone~~\n")
    }

    func testTableEmpty() throws {
        // Empty table produces no output or minimal output
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<table></table>", options: _options)
        XCTAssertEqual((result.content ?? ""), "")
    }

    func testTableLayoutCellParagraphsStaySeparated() throws {
        // Adjacent paragraphs in a layout-table cell are separated rather than concatenated into one run of text
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{\"compact_tables\":true}")
        let result = try HtmlToMarkdown.convert(html: "<table><tr><td><p><b>Alice Example</b></p><p><i>Customer Service</i></p></td><td>Logo</td></tr><tr><td>A</td><td>B</td><td>C</td></tr></table>", options: _options)
        XCTAssertFalse((result.content ?? "").contains("**Alice Example***Customer Service*"), "expected NOT to contain: \("**Alice Example***Customer Service*")")
        XCTAssertTrue((result.content ?? "").contains("Alice Example"), "expected to contain: \("Alice Example")")
        XCTAssertTrue((result.content ?? "").contains("Customer Service"), "expected to contain: \("Customer Service")")
        XCTAssertTrue((result.content ?? "").contains("Logo"), "expected to contain: \("Logo")")
    }

    func testTableNestedChainNotMisclassifiedAsLayout() throws {
        // A straight chain of one-nested-table-per-cell tables renders as pipe tables, not a broken layout list
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<table><tr><td><table><tr><td><table><tr><td>leaf</td></tr></table></td></tr></table></td></tr></table>", options: _options)
        XCTAssertTrue(result.content != nil, "expected non-empty value")
        XCTAssertTrue((result.content ?? "").contains("leaf"), "expected to contain: \("leaf")")
        XCTAssertTrue((result.content ?? "").contains("| ---"), "expected to contain: \("| ---")")
    }

    func testTableNestedInSingleCellRendersSeparately() throws {
        // A table nested in a single-cell row renders as its own table
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<table><tr><th>Summary</th></tr><tr><td>Before<table><tr><th>Item</th><th>Amount</th></tr><tr><td>Alpha</td><td>10</td></tr></table></td></tr></table>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("| Item  | Amount |"), "expected to contain: \("| Item  | Amount |")")
        XCTAssertTrue((result.content ?? "").contains("| Alpha | 10     |"), "expected to contain: \("| Alpha | 10     |")")
        XCTAssertFalse((result.content ?? "").contains("\\|"), "expected NOT to contain: \("\\|")")
    }

    func testTableNoThead() throws {
        // Table without thead uses first row as implied header
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<table><tr><td>Product</td><td>Price</td></tr><tr><td>Apple</td><td>1.00</td></tr></table>", options: _options)
        XCTAssertTrue(result.content != nil, "expected non-empty value")
        XCTAssertTrue((result.content ?? "").contains("Product"), "expected to contain: \("Product")")
        XCTAssertTrue((result.content ?? "").contains("Price"), "expected to contain: \("Price")")
        XCTAssertTrue((result.content ?? "").contains("Apple"), "expected to contain: \("Apple")")
        XCTAssertTrue((result.content ?? "").contains("1.00"), "expected to contain: \("1.00")")
        XCTAssertTrue((result.content ?? "").contains("|"), "expected to contain: \("|")")
    }

    func testTablePipeCharsInContent() throws {
        // Table cells containing pipe characters are escaped in output
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<table><thead><tr><th>Expression</th><th>Result</th></tr></thead><tbody><tr><td>a | b</td><td>true</td></tr></tbody></table>", options: _options)
        XCTAssertTrue(result.content != nil, "expected non-empty value")
        XCTAssertTrue((result.content ?? "").contains("Expression"), "expected to contain: \("Expression")")
        XCTAssertTrue((result.content ?? "").contains("Result"), "expected to contain: \("Result")")
        XCTAssertTrue((result.content ?? "").contains("true"), "expected to contain: \("true")")
    }

    func testTableRaggedRowFewerCellsThanHeader() throws {
        // A data row with fewer cells than the header is padded to the declared column count
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<table><tr><th>A</th><th>B</th><th>C</th></tr><tr><td>1</td><td>2</td></tr></table>", options: _options)
        XCTAssertEqual((result.content ?? ""), "| A | B | C |\n| --- | --- | --- |\n| 1 | 2 |   |\n")
    }

    func testTableRaggedRowMoreCellsThanHeader() throws {
        // A data row with more cells than the header widens the separator so no cell is silently dropped
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<table><tr><th>A</th><th>B</th></tr><tr><td>1</td><td>2</td><td>3</td></tr></table>", options: _options)
        XCTAssertEqual((result.content ?? ""), "| A | B |   |\n| --- | --- | --- |\n| 1 | 2 | 3 |\n")
    }

    func testTableWithAlignment() throws {
        // Table with column alignment attributes
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<table><thead><tr><th align=\"left\">Left</th><th align=\"center\">Center</th><th align=\"right\">Right</th></tr></thead><tbody><tr><td>L</td><td>C</td><td>R</td></tr></tbody></table>", options: _options)
        XCTAssertTrue(result.content != nil, "expected non-empty value")
        XCTAssertTrue((result.content ?? "").contains("Left"), "expected to contain: \("Left")")
        XCTAssertTrue((result.content ?? "").contains("Center"), "expected to contain: \("Center")")
        XCTAssertTrue((result.content ?? "").contains("Right"), "expected to contain: \("Right")")
        XCTAssertTrue((result.content ?? "").contains("L"), "expected to contain: \("L")")
        XCTAssertTrue((result.content ?? "").contains("C"), "expected to contain: \("C")")
        XCTAssertTrue((result.content ?? "").contains("R"), "expected to contain: \("R")")
        XCTAssertTrue((result.content ?? "").contains("|"), "expected to contain: \("|")")
    }

    func testTableWithColspan() throws {
        // Table with colspan attribute in a header cell
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<table><thead><tr><th colspan=\"2\">Full Name</th></tr></thead><tbody><tr><td>John</td><td>Doe</td></tr></tbody></table>", options: _options)
        XCTAssertTrue(result.content != nil, "expected non-empty value")
        XCTAssertTrue((result.content ?? "").contains("Full Name"), "expected to contain: \("Full Name")")
        XCTAssertTrue((result.content ?? "").contains("John"), "expected to contain: \("John")")
        XCTAssertTrue((result.content ?? "").contains("Doe"), "expected to contain: \("Doe")")
    }

    func testTaggedSpaceInStrikethroughKept() throws {
        // A whitespace-only strikethrough keeps the word separator
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p>A<del> </del>B</p>", options: _options)
        XCTAssertEqual((result.content ?? ""), "A B\n")
    }

    func testTaggedSpaceInSubscriptKept() throws {
        // A whitespace-only subscript keeps the word separator
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p>A<sub> </sub>B</p>", options: _options)
        XCTAssertEqual((result.content ?? ""), "A B\n")
    }

    func testTaggedSpaceNotDuplicated() throws {
        // A whitespace-only inline element contributes exactly one space
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p>A<i> </i>B</p>", options: _options)
        XCTAssertEqual((result.content ?? ""), "A B\n")
    }

    func testUnorderedList() throws {
        // Unordered list
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<ul><li>Item 1</li><li>Item 2</li><li>Item 3</li></ul>", options: _options)
        XCTAssertTrue((result.content ?? "").contains("- Item 1"), "expected to contain: \("- Item 1")")
        XCTAssertTrue((result.content ?? "").contains("- Item 2"), "expected to contain: \("- Item 2")")
        XCTAssertTrue((result.content ?? "").contains("- Item 3"), "expected to contain: \("- Item 3")")
    }

    func testWhitespaceOnlyCodeSpanKept() throws {
        // A code span whose content is a single space survives
        let _options = try HtmlToMarkdown.conversionOptionsFromJson("{}")
        let result = try HtmlToMarkdown.convert(html: "<p>A<code> </code>B</p>", options: _options)
        XCTAssertEqual((result.content ?? ""), "A` `B\n")
    }

}
