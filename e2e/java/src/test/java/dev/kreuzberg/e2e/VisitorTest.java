// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:e186d3bbc98e7b0712b701e18cd538169f857ce62fbd1f8ae5586470ebd01725
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

package dev.kreuzberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.htmltomarkdown.HtmlToMarkdownRs;
import dev.kreuzberg.htmltomarkdown.ConversionOptions;
import dev.kreuzberg.htmltomarkdown.Visitor;
import dev.kreuzberg.htmltomarkdown.NodeContext;
import dev.kreuzberg.htmltomarkdown.VisitResult;
import java.util.Optional;
import dev.kreuzberg.htmltomarkdown.JsonUtil;

/** E2e tests for category: visitor. */
class VisitorTest {

    @Test
    void testVisitorAudioCustom() throws Exception {
        // Visitor replaces audio element with custom output
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitAudio(NodeContext ctx, String src) {        return VisitResult.custom("[AUDIO: podcast.mp3]");    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>Listen to this: <audio src=\"podcast.mp3\" controls></audio></p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("[AUDIO: podcast.mp3]"), "expected to contain: " + "[AUDIO: podcast.mp3]");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Listen to this:"), "expected to contain: " + "Listen to this:");
    }


    @Test
    void testVisitorAudioSkip() throws Exception {
        // Visitor removes audio elements from output
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitAudio(NodeContext ctx, String src) {        return VisitResult.skip();    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>Background music:</p><audio src=\"music.ogg\" autoplay></audio><p>Enjoy!</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Background music:"), "expected to contain: " + "Background music:");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Enjoy!"), "expected to contain: " + "Enjoy!");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("music.ogg"), "expected NOT to contain: " + "music.ogg");
    }


    @Test
    void testVisitorButtonCustom() throws Exception {
        // Visitor replaces button with bracketed text
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitButton(NodeContext ctx, String text) {        return VisitResult.custom(String.format("[BTN:%s]", text));    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>Confirm action: <button type=\"submit\">Click me</button> or <button type=\"reset\">Cancel</button></p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("[BTN:Click me]"), "expected to contain: " + "[BTN:Click me]");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("[BTN:Cancel]"), "expected to contain: " + "[BTN:Cancel]");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Confirm action:"), "expected to contain: " + "Confirm action:");
    }


    @Test
    void testVisitorButtonSkip() throws Exception {
        // Visitor removes all buttons from output
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitButton(NodeContext ctx, String text) {        return VisitResult.skip();    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>Actions available: <button>Save</button> <button>Delete</button> <button>Export</button></p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Actions available:"), "expected to contain: " + "Actions available:");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("Save"), "expected NOT to contain: " + "Save");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("Delete"), "expected NOT to contain: " + "Delete");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("Export"), "expected NOT to contain: " + "Export");
    }


    @Test
    void testVisitorContinueDefault() throws Exception {
        // Visitor continue action preserves default conversion
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitStrong(NodeContext ctx, String text) {        return VisitResult.continue_();    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>Hello <strong>World</strong></p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("**World**"), "expected to contain: " + "**World**");
    }


    @Test
    void testVisitorCustomBlockquote() throws Exception {
        // Visitor replaces blockquote with custom format
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitBlockquote(NodeContext ctx, String content, long depth) {        return VisitResult.custom(String.format("QUOTE: \"%s\"", content));    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<blockquote><p>A wise quote.</p></blockquote>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("QUOTE:"), "expected to contain: " + "QUOTE:");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("A wise quote."), "expected to contain: " + "A wise quote.");
    }


    @Test
    void testVisitorCustomEmphasis() throws Exception {
        // Visitor replaces emphasis with custom output
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitEmphasis(NodeContext ctx, String text) {        return VisitResult.custom(String.format(">>>%s<<<", text));    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>This is <em>important</em> text.</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains(">>>important<<<"), "expected to contain: " + ">>>important<<<");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("*important*"), "expected NOT to contain: " + "*important*");
    }


    @Test
    void testVisitorCustomHeading() throws Exception {
        // Visitor replaces heading with custom format
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitHeading(NodeContext ctx, int level, String text, String id) {        return VisitResult.custom(String.format("--- %s ---", text));    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<h2>Section Title</h2><p>Content below heading.</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("--- Section Title ---"), "expected to contain: " + "--- Section Title ---");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("## Section Title"), "expected NOT to contain: " + "## Section Title");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Content below heading."), "expected to contain: " + "Content below heading.");
    }


    @Test
    void testVisitorCustomImage() throws Exception {
        // Visitor replaces image with custom output using template
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitImage(NodeContext ctx, String src, String alt, String title) {        return VisitResult.custom(String.format("[Image: %s]", alt));    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<img src=\"banner.png\" alt=\"Banner\">", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("[Image: Banner]"), "expected to contain: " + "[Image: Banner]");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("banner.png"), "expected NOT to contain: " + "banner.png");
    }


    @Test
    void testVisitorCustomLinkFormat() throws Exception {
        // Visitor reformats links using template interpolation
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitLink(NodeContext ctx, String href, String text, String title) {        return VisitResult.custom(String.format("%s (%s)", text, href));    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>Visit <a href=\"https://example.com\">Example</a> for more info.</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Example (https://example.com)"), "expected to contain: " + "Example (https://example.com)");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("[Example]"), "expected NOT to contain: " + "[Example]");
    }


    @Test
    void testVisitorCustomLinkStatic() throws Exception {
        // Visitor replaces link with static custom output
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitLink(NodeContext ctx, String href, String text, String title) {        return VisitResult.custom("[REDACTED LINK]");    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<a href=\"https://example.com\">Click here</a>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("[REDACTED LINK]"), "expected to contain: " + "[REDACTED LINK]");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("example.com"), "expected NOT to contain: " + "example.com");
    }


    @Test
    void testVisitorCustomOutput() throws Exception {
        // Visitor custom action replaces element output
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitHeading(NodeContext ctx, int level, String text, String id) {        return VisitResult.custom("## REPLACED HEADING");    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<h1>Original Heading</h1>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("## REPLACED HEADING"), "expected to contain: " + "## REPLACED HEADING");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("# Original Heading"), "expected NOT to contain: " + "# Original Heading");
    }


    @Test
    void testVisitorDefinitionListCustom() throws Exception {
        // Visitor customizes definition list items
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitDefinitionTerm(NodeContext ctx, String text) {        return VisitResult.custom(String.format("**%s**", text));    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<dl><dt>HTML</dt><dd>HyperText Markup Language</dd><dt>CSS</dt><dd>Cascading Style Sheets</dd></dl>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("**HTML**"), "expected to contain: " + "**HTML**");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("**CSS**"), "expected to contain: " + "**CSS**");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("HyperText Markup Language"), "expected to contain: " + "HyperText Markup Language");
    }


    @Test
    void testVisitorDefinitionListCustomFormat() throws Exception {
        // Visitor formats definition lists with custom templates
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitDefinitionTerm(NodeContext ctx, String text) {        return VisitResult.custom(String.format("### %s", text));    }

            @Override public VisitResult visitDefinitionDescription(NodeContext ctx, String text) {        return VisitResult.custom(String.format("> %s", text));    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<dl><dt>Python</dt><dd>A high-level programming language</dd><dt>JavaScript</dt><dd>A scripting language for web browsers</dd></dl>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("### Python"), "expected to contain: " + "### Python");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("### JavaScript"), "expected to contain: " + "### JavaScript");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("> A high-level programming language"), "expected to contain: " + "> A high-level programming language");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("> A scripting language for web browsers"), "expected to contain: " + "> A scripting language for web browsers");
    }


    @Test
    void testVisitorDefinitionListSkip() throws Exception {
        // Visitor skips definition list items from output
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitDefinitionTerm(NodeContext ctx, String text) {        return VisitResult.skip();    }

            @Override public VisitResult visitDefinitionDescription(NodeContext ctx, String text) {        return VisitResult.skip();    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>Glossary:</p><dl><dt>Term A</dt><dd>Definition of term A</dd><dt>Term B</dt><dd>Definition of term B</dd></dl><p>End of glossary</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Glossary:"), "expected to contain: " + "Glossary:");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("End of glossary"), "expected to contain: " + "End of glossary");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("Term A"), "expected NOT to contain: " + "Term A");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("Definition"), "expected NOT to contain: " + "Definition");
    }


    @Test
    void testVisitorDetailsSummaryCustom() throws Exception {
        // Visitor customizes details/summary disclosure elements
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitSummary(NodeContext ctx, String text) {        return VisitResult.custom(String.format("[EXPANDABLE] %s", text));    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<details><summary>Click to expand</summary><p>This content is initially hidden.</p><p>But can be revealed by the user.</p></details>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("[EXPANDABLE] Click to expand"), "expected to contain: " + "[EXPANDABLE] Click to expand");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("This content is initially hidden."), "expected to contain: " + "This content is initially hidden.");
    }


    @Test
    void testVisitorDetailsSummarySkip() throws Exception {
        // Visitor removes details/summary elements entirely
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitDetails(NodeContext ctx, boolean isOpen) {        return VisitResult.skip();    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>Main content here.</p><details><summary>Hidden section</summary><p>Secret details</p></details><p>More main content.</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Main content here."), "expected to contain: " + "Main content here.");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("More main content."), "expected to contain: " + "More main content.");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("Hidden section"), "expected NOT to contain: " + "Hidden section");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("Secret details"), "expected NOT to contain: " + "Secret details");
    }


    @Test
    void testVisitorFigureCustom() throws Exception {
        // Visitor customizes figure and figcaption elements
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitFigcaption(NodeContext ctx, String text) {        return VisitResult.custom(String.format("*%s*", text));    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<article><h1>Article Title</h1><p>Introduction paragraph.</p><figure><img src=\"diagram.png\" alt=\"System architecture diagram\"><figcaption>Figure 1: System Architecture</figcaption></figure><p>Explanation of the figure.</p></article>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Article Title"), "expected to contain: " + "Article Title");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("*Figure 1: System Architecture*"), "expected to contain: " + "*Figure 1: System Architecture*");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Explanation of the figure."), "expected to contain: " + "Explanation of the figure.");
    }


    @Test
    void testVisitorFigureCustomWrap() throws Exception {
        // Visitor wraps figure content with custom formatting
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitFigureStart(NodeContext ctx) {        return VisitResult.custom("\n[FIGURE]\n");    }

            @Override public VisitResult visitFigureEnd(NodeContext ctx, String output) {        return VisitResult.custom(String.format("%s\n[/FIGURE]\n", output));    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<section><h2>Gallery</h2><figure><img src=\"photo1.jpg\" alt=\"Photo\"><figcaption>Beautiful sunset</figcaption></figure></section>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("[FIGURE]"), "expected to contain: " + "[FIGURE]");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("[/FIGURE]"), "expected to contain: " + "[/FIGURE]");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Gallery"), "expected to contain: " + "Gallery");
    }


    @Test
    void testVisitorFigureSkip() throws Exception {
        // Visitor removes figure elements with their captions
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitFigureStart(NodeContext ctx) {        return VisitResult.skip();    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>See the chart below:</p><figure><img src=\"chart.svg\"><figcaption>Revenue Trends 2020-2024</figcaption></figure><p>As shown in the chart above.</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("See the chart below:"), "expected to contain: " + "See the chart below:");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("As shown in the chart above."), "expected to contain: " + "As shown in the chart above.");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("Revenue Trends"), "expected NOT to contain: " + "Revenue Trends");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("chart.svg"), "expected NOT to contain: " + "chart.svg");
    }


    @Test
    void testVisitorFormCustom() throws Exception {
        // Visitor replaces form with custom output
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitForm(NodeContext ctx, String actionUrl, String method) {        return VisitResult.custom("[FORM PLACEHOLDER]");    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<div><form action=\"/submit\" method=\"POST\"><label>Name: <input type=\"text\" name=\"name\"></label><button type=\"submit\">Submit</button></form></div>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("[FORM PLACEHOLDER]"), "expected to contain: " + "[FORM PLACEHOLDER]");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("submit"), "expected NOT to contain: " + "submit");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("input"), "expected NOT to contain: " + "input");
    }


    @Test
    void testVisitorFormSkip() throws Exception {
        // Visitor skips form elements entirely
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitForm(NodeContext ctx, String actionUrl, String method) {        return VisitResult.skip();    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>Before form</p><form><input type=\"email\" name=\"email\"></form><p>After form</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Before form"), "expected to contain: " + "Before form");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("After form"), "expected to contain: " + "After form");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("email"), "expected NOT to contain: " + "email");
    }


    @Test
    void testVisitorHeadingBareStringPreservesCase() throws Exception {
        // Visitor returns rendered heading template as bare string; mixed-case is preserved (regression guard for issue #350)
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitHeading(NodeContext ctx, int level, String text, String id) {        return VisitResult.custom(String.format("## %s ##", text));    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<h2>Important Section Title</h2><p>Body.</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("## Important Section Title ##"), "expected to contain: " + "## Important Section Title ##");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("important section title"), "expected NOT to contain: " + "important section title");
    }


    @Test
    void testVisitorHorizontalRuleCustom() throws Exception {
        // Visitor replaces horizontal rule with custom output
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitHorizontalRule(NodeContext ctx) {        return VisitResult.custom("\n[DIVIDER]\n");    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<h1>Section A</h1><p>Content A</p><hr><h1>Section B</h1><p>Content B</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("[DIVIDER]"), "expected to contain: " + "[DIVIDER]");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Section A"), "expected to contain: " + "Section A");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Section B"), "expected to contain: " + "Section B");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("---"), "expected NOT to contain: " + "---");
    }


    @Test
    void testVisitorHorizontalRuleSkip() throws Exception {
        // Visitor removes all horizontal rules
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitHorizontalRule(NodeContext ctx) {        return VisitResult.skip();    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>Part 1</p><hr><p>Part 2</p><hr><p>Part 3</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Part 1"), "expected to contain: " + "Part 1");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Part 2"), "expected to contain: " + "Part 2");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Part 3"), "expected to contain: " + "Part 3");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("---"), "expected NOT to contain: " + "---");
    }


    @Test
    void testVisitorIframeCustom() throws Exception {
        // Visitor replaces embedded iframe with custom text
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitIframe(NodeContext ctx, String src) {        return VisitResult.custom("[EMBEDDED: https://maps.example.com/embed]");    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>Embedded map:</p><iframe src=\"https://maps.example.com/embed\" width=\"400\" height=\"300\"></iframe><p>End of map</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("[EMBEDDED: https://maps.example.com/embed]"), "expected to contain: " + "[EMBEDDED: https://maps.example.com/embed]");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Embedded map:"), "expected to contain: " + "Embedded map:");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("End of map"), "expected to contain: " + "End of map");
    }


    @Test
    void testVisitorIframeSkip() throws Exception {
        // Visitor removes embedded iframes
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitIframe(NodeContext ctx, String src) {        return VisitResult.skip();    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<h3>Reviews</h3><iframe src=\"https://widget.example.com/reviews\"></iframe><p>See reviews from our partners.</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Reviews"), "expected to contain: " + "Reviews");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("See reviews from our partners."), "expected to contain: " + "See reviews from our partners.");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("widget.example.com"), "expected NOT to contain: " + "widget.example.com");
    }


    @Test
    void testVisitorImageBareStringPreservesCase() throws Exception {
        // Visitor returns bare-string image replacement; mixed-case alt/src preserved (regression guard for issue #350)
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitImage(NodeContext ctx, String src, String alt, String title) {        return VisitResult.custom(String.format("[image: %s -> %s]", alt, src));    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<img src=\"PhotoOne.JPG\" alt=\"Sunset Over Bay\">", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("[image: Sunset Over Bay -> PhotoOne.JPG]"), "expected to contain: " + "[image: Sunset Over Bay -> PhotoOne.JPG]");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("sunset over bay"), "expected NOT to contain: " + "sunset over bay");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("photoone.jpg"), "expected NOT to contain: " + "photoone.jpg");
    }


    @Test
    void testVisitorInputCustom() throws Exception {
        // Visitor replaces input with labeled output
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitInput(NodeContext ctx, String inputType, String name, String value) {        return VisitResult.custom(String.format("[INPUT:%s]", inputType));    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<form><label>Username: <input type=\"text\" name=\"username\" value=\"\"></label><label>Password: <input type=\"password\" name=\"password\"></label></form>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("[INPUT:text]"), "expected to contain: " + "[INPUT:text]");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("[INPUT:password]"), "expected to contain: " + "[INPUT:password]");
    }


    @Test
    void testVisitorInputSkip() throws Exception {
        // Visitor skips all input elements
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitInput(NodeContext ctx, String inputType, String name, String value) {        return VisitResult.skip();    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>Sign up:</p><input type=\"text\" name=\"email\" placeholder=\"your@email.com\"><input type=\"checkbox\" name=\"agree\"><p>Continue</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Sign up:"), "expected to contain: " + "Sign up:");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Continue"), "expected to contain: " + "Continue");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("email"), "expected NOT to contain: " + "email");
    }


    @Test
    void testVisitorLineBreakCustom() throws Exception {
        // Visitor replaces line break with custom output
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitLineBreak(NodeContext ctx) {        return VisitResult.custom(" | ");    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>First line<br>Second line<br>Third line</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("First line | Second line | Third line"), "expected to contain: " + "First line | Second line | Third line");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("\n\n"), "expected NOT to contain: " + "\n\n");
    }


    @Test
    void testVisitorLineBreakSkip() throws Exception {
        // Visitor removes all line breaks
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitLineBreak(NodeContext ctx) {        return VisitResult.skip();    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>Address Line 1<br>Address Line 2<br>Address Line 3</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Address Line 1Address Line 2Address Line 3"), "expected to contain: " + "Address Line 1Address Line 2Address Line 3");
    }


    @Test
    void testVisitorLinkBareStringPreservesCase() throws Exception {
        // Visitor returns rendered template as a bare string (not a dict) and original mixed-case content is preserved end-to-end (regression guard for issue #350)
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitLink(NodeContext ctx, String href, String text, String title) {        return VisitResult.custom(String.format("[%s](https://new-cdn.com/file.pdf)", text));    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<a href=\"https://old-cdn.com/file.pdf\">Download</a>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("[Download](https://new-cdn.com/file.pdf)"), "expected to contain: " + "[Download](https://new-cdn.com/file.pdf)");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("[download]"), "expected NOT to contain: " + "[download]");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("old-cdn.com"), "expected NOT to contain: " + "old-cdn.com");
    }


    @Test
    void testVisitorMarkCustom() throws Exception {
        // Visitor replaces highlight/mark with custom template
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitMark(NodeContext ctx, String text) {        return VisitResult.custom(String.format("==%s==", text));    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>This is a <mark>highlighted passage</mark> in the text.</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("==highlighted passage=="), "expected to contain: " + "==highlighted passage==");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("This is a"), "expected to contain: " + "This is a");
    }


    @Test
    void testVisitorMarkSkip() throws Exception {
        // Visitor skips mark elements entirely
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitMark(NodeContext ctx, String text) {        return VisitResult.skip();    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>Key insight: <mark>always validate input</mark> for security.</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("always validate input"), "expected NOT to contain: " + "always validate input");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Key insight:"), "expected to contain: " + "Key insight:");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("for security."), "expected to contain: " + "for security.");
    }


    @Test
    void testVisitorPreserveHtml() throws Exception {
        // Visitor preserve_html action includes raw HTML in output
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitCustomElement(NodeContext ctx, String tagName, String html) {        return VisitResult.preserveHtml();    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<div><custom-tag>Custom content</custom-tag></div>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("<custom-tag>"), "expected to contain: " + "<custom-tag>");
    }


    @Test
    void testVisitorSkipCodeBlocks() throws Exception {
        // Visitor skips code blocks from output
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitCodeBlock(NodeContext ctx, String lang, String code) {        return VisitResult.skip();    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>Intro text</p><pre><code>let x = 42;</code></pre><p>Outro text</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Intro text"), "expected to contain: " + "Intro text");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Outro text"), "expected to contain: " + "Outro text");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("let x = 42"), "expected NOT to contain: " + "let x = 42");
    }


    @Test
    void testVisitorSkipHeading() throws Exception {
        // Visitor skip action omits all headings from output
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitHeading(NodeContext ctx, int level, String text, String id) {        return VisitResult.skip();    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<h1>Title</h1><p>Body text remains.</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("Title"), "expected NOT to contain: " + "Title");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Body text remains."), "expected to contain: " + "Body text remains.");
    }


    @Test
    void testVisitorSkipImages() throws Exception {
        // Visitor skips all images from output
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitImage(NodeContext ctx, String src, String alt, String title) {        return VisitResult.skip();    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>Before image</p><img src=\"photo.jpg\" alt=\"A photo\"><p>After image</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Before image"), "expected to contain: " + "Before image");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("After image"), "expected to contain: " + "After image");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("photo.jpg"), "expected NOT to contain: " + "photo.jpg");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("A photo"), "expected NOT to contain: " + "A photo");
    }


    @Test
    void testVisitorSkipLinks() throws Exception {
        // Visitor skips all links entirely
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitLink(NodeContext ctx, String href, String text, String title) {        return VisitResult.skip();    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>Before <a href=\"https://example.com\">link text</a> after</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("link text"), "expected NOT to contain: " + "link text");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("example.com"), "expected NOT to contain: " + "example.com");
    }


    @Test
    void testVisitorSkipStrong() throws Exception {
        // Visitor skips bold/strong elements
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitStrong(NodeContext ctx, String text) {        return VisitResult.skip();    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>Normal <strong>bold text</strong> normal</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("bold text"), "expected NOT to contain: " + "bold text");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Normal"), "expected to contain: " + "Normal");
    }


    @Test
    void testVisitorSubscriptCustom() throws Exception {
        // Visitor replaces subscript with custom template
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitSubscript(NodeContext ctx, String text) {        return VisitResult.custom(String.format("~%s~", text));    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>H<sub>2</sub>O is water.</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("H~2~O"), "expected to contain: " + "H~2~O");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("is water"), "expected to contain: " + "is water");
    }


    @Test
    void testVisitorSubscriptSkip() throws Exception {
        // Visitor skips subscript elements entirely
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitSubscript(NodeContext ctx, String text) {        return VisitResult.skip();    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>The formula C<sub>12</sub>H<sub>22</sub>O<sub>11</sub> is sugar.</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("The formula CHO is sugar."), "expected to contain: " + "The formula CHO is sugar.");
    }


    @Test
    void testVisitorSuperscriptCustom() throws Exception {
        // Visitor replaces superscript with custom template
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitSuperscript(NodeContext ctx, String text) {        return VisitResult.custom(String.format("^%s^", text));    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>Einstein's E=mc<sup>2</sup> revolutionized physics.</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("E=mc^2^"), "expected to contain: " + "E=mc^2^");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("revolutionized physics"), "expected to contain: " + "revolutionized physics");
    }


    @Test
    void testVisitorSuperscriptSkip() throws Exception {
        // Visitor skips superscript from output
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitSuperscript(NodeContext ctx, String text) {        return VisitResult.skip();    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>The equation x<sup>3</sup> + y<sup>3</sup> = z<sup>3</sup> has no solutions.</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("The equation x + y = z has no solutions."), "expected to contain: " + "The equation x + y = z has no solutions.");
    }


    @Test
    void testVisitorUnderlineCustom() throws Exception {
        // Visitor replaces underline with custom markup
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitUnderline(NodeContext ctx, String text) {        return VisitResult.custom(String.format("_%s_", text));    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>This is <u>very important</u> text.</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("_very important_"), "expected to contain: " + "_very important_");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("**"), "expected NOT to contain: " + "**");
    }


    @Test
    void testVisitorUnderlineSkip() throws Exception {
        // Visitor skips underline elements from output
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitUnderline(NodeContext ctx, String text) {        return VisitResult.skip();    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>Normal text with <u>underlined part</u> and more text.</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Normal text with"), "expected to contain: " + "Normal text with");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("and more text."), "expected to contain: " + "and more text.");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("underlined part"), "expected NOT to contain: " + "underlined part");
    }


    @Test
    void testVisitorVideoCustom() throws Exception {
        // Visitor replaces video with custom link
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitVideo(NodeContext ctx, String src) {        return VisitResult.custom(String.format("[VIDEO: %s]", src));    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<p>Watch our tutorial:</p><video src=\"tutorial.mp4\" width=\"320\" height=\"240\" controls></video><p>Great content!</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("[VIDEO: tutorial.mp4]"), "expected to contain: " + "[VIDEO: tutorial.mp4]");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Watch our tutorial:"), "expected to contain: " + "Watch our tutorial:");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Great content!"), "expected to contain: " + "Great content!");
    }


    @Test
    void testVisitorVideoSkip() throws Exception {
        // Visitor removes video elements entirely
        class _TestVisitor implements Visitor {
            @Override public VisitResult visitVideo(NodeContext ctx, String src) {        return VisitResult.skip();    }

        }
        var visitor = new _TestVisitor();
        var result = HtmlToMarkdownRs.convert("<h2>Demo</h2><video src=\"demo.webm\"></video><p>See the demo above.</p>", ConversionOptions.builder().withVisitor(visitor).build());
assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("Demo"), "expected to contain: " + "Demo");assertTrue(java.util.Optional.ofNullable(result.content()).orElse("").contains("See the demo above."), "expected to contain: " + "See the demo above.");assertFalse(java.util.Optional.ofNullable(result.content()).orElse("").contains("demo.webm"), "expected NOT to contain: " + "demo.webm");
    }

}
