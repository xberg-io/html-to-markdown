// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:b11a7c0211497233869a25fbceaf5dc6003fda6181cf80ca992623dbe66ea6e0
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

package dev.kreuzberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.htmltomarkdown.HtmlToMarkdownRs;
import dev.kreuzberg.htmltomarkdown.ConversionOptions;
import java.util.Optional;
import dev.kreuzberg.htmltomarkdown.JsonUtil;

/** E2e tests for category: structure. */
public class StructureTest {

    @Test
    void testStructureCodeBlock() throws Exception {
        // Fenced code block produces Code node
        var options = JsonUtil.fromJson("{\"include_document_structure\":true}", ConversionOptions.class);

        var result = HtmlToMarkdownRs.convert("<p>Example code:</p><pre><code class=\"language-rust\">fn main() { println!(\"Hello\"); }</code></pre>", options);
assertFalse(java.util.Optional.ofNullable(result.content()).isEmpty(), "expected non-empty value");assertFalse(result.document().nodes().isEmpty(), "expected non-empty value");assertTrue(result.document().nodes().size() >= 2, "expected at least 2 elements");

    }


    @Test
    void testStructureDeepNestingH1H2H3() throws Exception {
        // H1 > H2 > H3 creates three levels of heading nesting
        var options = JsonUtil.fromJson("{\"include_document_structure\":true}", ConversionOptions.class);

        var result = HtmlToMarkdownRs.convert("<h1>Top Level</h1><p>Top intro.</p><h2>Mid Level</h2><p>Mid content.</p><h3>Deep Level</h3><p>Deep content.</p>", options);
assertFalse(java.util.Optional.ofNullable(result.content()).isEmpty(), "expected non-empty value");assertFalse(result.document().nodes().isEmpty(), "expected non-empty value");assertTrue(result.document().nodes().size() >= 5, "expected at least 5 elements");

    }


    @Test
    void testStructureH1H2NestedGroup() throws Exception {
        // H1 followed by H2 creates a nested group under the H1
        var options = JsonUtil.fromJson("{\"include_document_structure\":true}", ConversionOptions.class);

        var result = HtmlToMarkdownRs.convert("<h1>Chapter One</h1><p>Chapter intro.</p><h2>Section One</h2><p>Section content.</p>", options);
assertFalse(java.util.Optional.ofNullable(result.content()).isEmpty(), "expected non-empty value");assertFalse(result.document().nodes().isEmpty(), "expected non-empty value");assertTrue(result.document().nodes().size() >= 3, "expected at least 3 elements");

    }


    @Test
    void testStructureHeadingParagraph() throws Exception {
        // Simple heading followed by paragraph produces Heading and Paragraph nodes
        var options = JsonUtil.fromJson("{\"include_document_structure\":true}", ConversionOptions.class);

        var result = HtmlToMarkdownRs.convert("<h1>Title</h1><p>A paragraph of text.</p>", options);
assertFalse(java.util.Optional.ofNullable(result.content()).isEmpty(), "expected non-empty value");assertFalse(result.document().nodes().isEmpty(), "expected non-empty value");assertTrue(result.document().nodes().size() >= 2, "expected at least 2 elements");

    }


    @Test
    void testStructureList() throws Exception {
        // Unordered list produces List and ListItem nodes
        var options = JsonUtil.fromJson("{\"include_document_structure\":true}", ConversionOptions.class);

        var result = HtmlToMarkdownRs.convert("<p>Items:</p><ul><li>Alpha</li><li>Beta</li><li>Gamma</li></ul>", options);
assertFalse(java.util.Optional.ofNullable(result.content()).isEmpty(), "expected non-empty value");assertFalse(result.document().nodes().isEmpty(), "expected non-empty value");assertTrue(result.document().nodes().size() >= 2, "expected at least 2 elements");

    }


    @Test
    void testStructureMultipleHeadings() throws Exception {
        // Multiple headings create multiple Heading nodes with correct levels
        var options = JsonUtil.fromJson("{\"include_document_structure\":true}", ConversionOptions.class);

        var result = HtmlToMarkdownRs.convert("<h1>Main Title</h1><h2>Section One</h2><p>Section one content.</p><h2>Section Two</h2><p>Section two content.</p>", options);
assertFalse(java.util.Optional.ofNullable(result.content()).isEmpty(), "expected non-empty value");assertFalse(result.document().nodes().isEmpty(), "expected non-empty value");assertTrue(result.document().nodes().size() >= 4, "expected at least 4 elements");

    }


    @Test
    void testStructureSiblingH1Groups() throws Exception {
        // H1, H2, then another H1 creates two sibling top-level groups
        var options = JsonUtil.fromJson("{\"include_document_structure\":true}", ConversionOptions.class);

        var result = HtmlToMarkdownRs.convert("<h1>Chapter One</h1><h2>Section A</h2><p>Section A content.</p><h1>Chapter Two</h1><h2>Section B</h2><p>Section B content.</p>", options);
assertFalse(java.util.Optional.ofNullable(result.content()).isEmpty(), "expected non-empty value");assertFalse(result.document().nodes().isEmpty(), "expected non-empty value");assertTrue(result.document().nodes().size() >= 4, "expected at least 4 elements");

    }

}
