name: CI WASM

on:
  push:
    branches: [main]
    paths:
      - crates/html-to-markdown-wasm/**
      - crates/html-to-markdown-wasm-wasi/**
      - scripts/publish/wasm/**
      - e2e/wasm-wasmtime/**
      - crates/html-to-markdown/**
      - package.json
      - pnpm-lock.yaml
      - pnpm-workspace.yaml
      - .task/**
      - Taskfile.yaml
      - .github/workflows/ci-wasm.yaml
  pull_request:
    branches: [main]
    paths:
      - crates/html-to-markdown-wasm/**
      - crates/html-to-markdown-wasm-wasi/**
      - scripts/publish/wasm/**
      - e2e/wasm-wasmtime/**
      - crates/html-to-markdown/**
      - package.json
      - pnpm-lock.yaml
      - pnpm-workspace.yaml
      - .task/**
      - Taskfile.yaml
      - .github/workflows/ci-wasm.yaml

permissions:
  contents: read

concurrency:
  group: ci-wasm-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  wasm-bindings:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Rust
        uses: kreuzberg-dev/actions/setup-rust@c964c3c295858254aa86d78cf36bd66bd9364e9a # v1.0.0
        with:
          target: wasm32-unknown-unknown
          use-sccache: false

      - name: Ensure wasm target installed
        run: scripts/common/ensure-wasm-target.sh
        shell: bash

      - name: Install wasm-pack
        run: scripts/common/install-wasm-pack.sh
        shell: bash

      - name: Setup workspace
        uses: kreuzberg-dev/actions/setup-node-workspace@c964c3c295858254aa86d78cf36bd66bd9364e9a # v1.0.0

      - name: Build WASM (all targets)
        uses: kreuzberg-dev/actions/build-wasm-package@c964c3c295858254aa86d78cf36bd66bd9364e9a # v1.0.0
        with:
          crate-dir: crates/html-to-markdown-wasm

      - name: Test WASM
        working-directory: crates/html-to-markdown-wasm
        run: ../../scripts/ci/wasm/test-wasm-bundle.sh
        shell: bash

      - name: Run Rust Tests
        working-directory: crates/html-to-markdown-wasm
        run: ../../scripts/ci/wasm/test-wasm-rust.sh
        shell: bash

  wasm-wasmtime:
    name: Wasmtime tests (${{ matrix.runner }})
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        runner:
          - ubuntu-latest
          - ubuntu-24.04-arm
          - macos-latest
          - windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Rust
        uses: kreuzberg-dev/actions/setup-rust@c964c3c295858254aa86d78cf36bd66bd9364e9a # v1.0.0
        with:
          toolchain: stable
          target: wasm32-unknown-unknown
          use-sccache: false

      - name: Ensure wasm target installed
        run: scripts/common/ensure-wasm-target.sh
        shell: bash

      - name: Run Wasmtime WASM tests
        run: scripts/ci/wasm/run-wasmtime-tests.sh
        shell: bash
