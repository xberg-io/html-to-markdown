name: CI Java

on:
  push:
    branches: [main]
    paths:
      - packages/java/**
      - scripts/publish/maven/**
      - crates/html-to-markdown-ffi/**
      - crates/html-to-markdown/**
      - .task/**
      - Taskfile.yaml
      - .github/workflows/ci-java.yaml
  pull_request:
    branches: [main]
    paths:
      - packages/java/**
      - scripts/publish/maven/**
      - crates/html-to-markdown-ffi/**
      - crates/html-to-markdown/**
      - .task/**
      - Taskfile.yaml
      - .github/workflows/ci-java.yaml

permissions:
  contents: read

concurrency:
  group: ci-java-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  java-bindings:
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        java: ["25"]
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Rust
        uses: kreuzberg-dev/actions/setup-rust@c964c3c295858254aa86d78cf36bd66bd9364e9a # v1.0.0

      - name: Test Java Panama FFI bindings
        uses: kreuzberg-dev/actions/test-java-ffi@c964c3c295858254aa86d78cf36bd66bd9364e9a # v1.0.0
        with:
          ffi-crate-name: html-to-markdown-ffi
          ffi-lib-name: html_to_markdown_ffi
          java-version: ${{ matrix.java }}
